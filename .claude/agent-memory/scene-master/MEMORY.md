# Memory Index

## 판정 이력
- [ep00s = APPROVED (2026-07-29)](verdict_ep00s.md) — 누적 반려 1회. DRAFT 원문 url null 예외 + 엔진 변경 파급 검증 절차
- [ep01s 재제작 = APPROVED (2026-07-29)](verdict_ep01s.md) — 그림 10종 전면 교체. 돌려막기 판정법 + 다음 회차 금지 3건
- [ep02s = APPROVED (2026-07-30)](verdict_ep02s.md) — 무인 첫 완주. 렌더 계층 화이트리스트로 정직성 규칙. 미참조 kind 2건 잔존
- [ep02s 재작업 = APPROVED (2026-07-30)](verdict_ep02s_redo.md) — 무효 사유로 잘린 분량 복원. 화면 문자열 기계 전수 대조 + "규정 없음"은 통과 근거가 아니다
- [정적 구간 3초 초과 = 1회성 예외](project_static_segment_exception.md) — ep00s 에서 3건 넘김. 다음 회차가 선례로 인용하면 반려
- [ep10s-1 = 1차 반려 → 2차 APPROVED (2026-08-08, 누적 2회에서 정지)](verdict_ep10s_1.md) — seed 한 값으로 해소. 척도 확정 + 🔴 되돌림 지시 「일부 이행」은 다음부터 반려
- 🔴 [사건 단위 재분할 여섯 편 = main 머지 (2026-08-04, PR #10)](../scene-reviewer/project_review_log.md) — ep04s-1/-2/-3 · ep05s-1/-2/-3. 2차 검수까지 완료. 여섯 편 모두 check.mjs 에서 환경 예외(timed.json 없음) 하나만 남음
- [ep10s-3 = APPROVED (2026-08-08)](verdict_ep10s3.md) — 막 전환 예고 URL 대조법 + sfx 파형 실측(sweep 0.02 · latch 0.77) + 「다시 세었다」 명단 4번째 오류 + 선례 인용 불가 3건
- 🔴 [화면-참조 자막 스틸은 `timed.json` 뒤에만 증거](procedure_still_evidence_timing.md) — 임시 타이밍은 실측보다 ~20% 길다. lf01 이 같은 자리에서 세 번 깨진 진짜 원인
- 🎬 [클립 샷 재검 = `in` vs `preS` 산수 두 줄](procedure_clip_in_vs_pres.md) — 스틸 여덟 장을 대신한다. 사건이 1초 넘게 늦으면 죽은 화면 · 콜드 오픈이면 반려
- [cue 타이밍 직접 검산 4줄](procedure_cue_window_math.md) — 창 분모는 `rel`, 어절은 `dur`, 카드는 `TOTAL−2600`. 둘을 섞으면 pauseAfter 만큼 밀린다
- [ep10s-2 = APPROVED (2026-08-08)](verdict_ep10s_2.md) — 반려 0회. sfx 대조표가 두 라운드 연속 틀렸는데 값은 살아남음. freq 전수는 손으로 세지 말 것 + 선례 금지 2건
- 🔴 [ep11s = APPROVED (2026-08-08, 누적 2회에서 정지)](verdict_ep11s.md) — 4막 개막. 예고는 「다음 편이 **버린 것**」과도 대조 + 글자 복제는 런 개수로 증명 + 선례 금지 5건
- 🔴 [ep12s-1 = APPROVED (2026-08-08, 마스터 반려 0)](verdict_ep12s_1.md) — 앞 편 예고와 사건이 어긋날 때의 인계 3조건 + **검수 상관 수치가 재현 안 된 첫 사례**(다음부터 반려) + 선례 금지 5건
- 🔴 [ep12s-2 = APPROVED (2026-08-08, 누적 반려 1회)](verdict_ep12s_2.md) — 「규칙을 낳은 회차가 그 규칙을 어겼다」로 파이프라인 문제 판정 + 정적 구간은 fps 불변이 아니다 + 선례 금지 4건
- 🎬 [**lf01 = 롱폼 1호 APPROVED (2026-08-09, 누적 반려 3회 · 사람 인계 1회)**](verdict_lf01.md) — wide 정본 `Docs/롱폼_대본_문법.md` · 산정 길이 = 실측의 82% · 창 모자라면 `in` 말고 **재슬라이스** · 선례 금지 4건
- 🔴 [**lf02 = 반려 → 재판정 APPROVED (2026-08-09, 누적 3회 · 사람 인계 1회)**](verdict_lf02.md) — 검수 3차 PASS 를 뒤집었다. 화면 행 번호 `03·06`이 실제 `4·5` + 「전수 대조했다」가 전수가 아니었다 + `sfx.why` 시각 검산식 + 콜드오픈 클립 규칙 교정 + 부분수정 재판정법(프레임수·산정총계 대조)
- 🔴 [**ep13s-2 = APPROVED (2026-08-10, 마스터 반려 0 · 누적 1회)**](verdict_ep13s_2.md) — 수치 **「슬롯 결합」** 반려 유형(수는 실재, 주어가 다름) + 척도를 회차 안에서 실측으로 세우기 + 검수 픽셀 2건 교정(동적 획 두께) + 한국어 라벨 첫 회차 + 「인용 vs 추론」 갈라 적기
- 🔴 [ep12s-3 = APPROVED (2026-08-08, 마스터 반려 0)](verdict_ep12s3.md) — 어미 3연속을 **파이프라인 문제로 승격**(9편 중 5편·게이트 0건) + 예고 연출 규칙 문구 교정 + kind 쌍 중복은 비둘기집
- 🔴 [**ep13s-1 = APPROVED (2026-08-10, 누적 반려 1회 · 한국어 라벨 첫 회차)**](verdict_ep13s_1.md) — 🔑 kind 수정 재검증 = **`scene.json` 고정 + `kinds` 만 되돌린 격리 스틸 비교** + 검수 보고 오류 6건(「「봄」 0건」·「X 전부 오탐」이 둘 다 거짓) + 대역 문장 과일반화 + 선례 금지 5건
- 🔴 [**ep14s-3 = APPROVED (2026-08-10, 마스터 반려 0 · 검수 반려 0 · `publish` 전 이행 1건)**](verdict_ep14s_3.md) — 「전수」를 **`hud`·`hook`·`outro` 까지** 넓힘(7번째 사례) + 원문 **압축 허용 경계선** + `notes` 부분 이행을 다음부터 반려로 승격 + 검수 수치 14개 전부 재현
- 🔴 [**`ADR-V25-15` ③-b 재검 = 알파 시간표를 식에서 푼다**](procedure_label_alpha_timetable.md) — 스틸은 노출 길이를 못 잡는다. `cue` 의 `lead` 는 **초**(비율 아님) · `ease(u)=0.6 → u≈0.566` 두 수로 전부 뒤집힌다
- 🔴 [**ep14s-2 = APPROVED (2026-08-10, 마스터 반려 0 · 누적 1회)**](verdict_ep14s_2.md) — `ADR-V25-15` **③-b 첫 통과**(그 조항을 낳은 편이다) + ③-b 직접 검산식 4줄 + **latch 상관 정본 대역 0.628~0.845**(세 층이 다 좁게 적었다) + 「형제 둘」이 실은 **셋**(7번째 전수 누락) + 「닿지 않고 멈춤」 증명 4신호 + 선례 금지 4건
- 🔴 [**ep14s-4 = APPROVED (2026-08-10, 4편 분할 마지막 · 마스터 반려 0 · 누적 1회)**](verdict_ep14s_4.md) — 「N px 여유면 충분한가」는 **최악 위상 프레임의 픽셀**로 답한다(1.95px 계산 = 실제 9.91px) + **`latch` 는 seed 로 안 갈린다 — 개성은 `dur`** + 주석 좌표는 「경로냐 획 바깥이냐」를 적어라 + 「권장대역 도달 불가능」 반증(ep13s-2 34.5초) + 선례 금지 3건
- 🔴 [**ep13s-3 = APPROVED (2026-08-10, 4막 피날레 · 마스터 반려 0 · 검수도 반려 0)**](verdict_ep13s_3.md) — 「반려 0」검증법(결론 말고 **수치 8개 재현**) + **시제 3줄 판정선**(완료형만 반려 · 조건을 지는 것이 앞줄에 · 뒤집힘이 한 줄 뒤에) + 어미 **7연속을 이행 조건으로 승격**(문안은 마스터가 안 정한다) + thud 0.873 은 8위라 통과 — 진짜 원인은 `freq` 기본 150 관행 + 선례 금지 4건

- 🔴 [**ep14s-1 = APPROVED (2026-08-10, 마스터 반려 0 · `ADR-V25-13`~`15` 첫 적용)**](verdict_ep14s_1.md) — 「화면」은 **세 층**(캔버스·HUD·아웃트로 카드)이라 notes 의 「0개」 단언이 **세 번 거짓** + 피크 천장은 **출력으로 못 잰다**(warn 을 가로채라) + 브리프 §6 이 같은 수를 두 편에 배정 + `ADR-V25-15②` 알파 실측 재검법 + 「원문에 없다」 전에 태그 벗기기

## 🔴 2026-08-04 이후 달라진 규칙 (판정 전에 확인)
- 🔴 **길이 정본 = `check.mjs` 출력 하나. 현행은 `38초 fail` · `33~37초 권장(warn)`**(2026-08-10 개정)
  (옛 「상한 50 · 권장 30~45」는 **폐기** — 실행해 보고 인용하라).
  권장대역 **아래**는 반려 사유가 아니다(`ep04s-2` 26.7초 = 사용자 확정, 사유는 그 편 `notes.길이`).
  🔄 **2026-08-10 — 사용자가 대역을 33~37 로 재조정했고, 그 근거가 바로 이 산수였다.**
  4단 고정비 실측 **15.7~17.0초** + 본문 하한 18 = 최소 **33.7초**.
  🔴 **옛 이 칸의 「도달 불가능하다고 쓰지 마라 — 반증됐다(ep13s-2 34.5초)」는 틀렸다.**
  코디네이터가 쓴 문장은 「권장대역**의** 30~33 **구간**」이었는데 「권장대역 = 30~33」으로
  읽었고, 든 반례 34.5초는 하한 33.7 **위**라 반증이 아니었다. 새 하한 33 이 그 산수에서 나왔다.
  🔑 **교훈은 「그렇게 쓰지 마라」가 아니라 「인용 범위를 먼저 맞춰라」다** — 남의 문장을
  반박할 때 **그 문장이 무엇을 한정하고 있었는지**부터 읽는다.
  🔑 새 대역에서 warn 은 **7편 중 2편**(37.3·37.5)이고 그 둘이 fail 38 에 가장 가깝다 —
  경고가 다시 정보를 준다. 권장대역 **아래**가 반려 사유가 아닌 것은 그대로다.
- **훅은 캔버스에 안 그린다** — 아웃트로 카드의 `.oc-hook` 이 맡는다. kind 에 **훅 문구를 그리는
  블록**이 남아 있으면 반려. 걷어낸 자리의 `fade`·주석·`reads` 도 같이 봐야 한다.
  ⚠️ **`spec.hookCue` 라는 필드명 자체는 금지가 아니다**(ep04s-1~ 다수가 훅 샷의 cue 인덱스
  이름으로 쓰고 발행됐다 · ep13s-3 `deathclock` 도 그렇다). **이름 말고 그리는 것을 봐라.**
- **`latch` 에 고유 `seed` 필수** — 없으면 다른 회차와 파형 상관 0.99. `dur` 만 다른 건 안 갈린 것이다.
  🔴 **정본 대역(전수 25개·300쌍 실측, `ep14s-2`): 기본 seed 쌍 제외 0.628~0.845 · 중앙 0.765.**
  **판정선 0.85 — 그 이하는 반려 근거가 못 된다. 0.99 대 = seed 미지정 신호.**
  옛 「0.68~0.79 이 하한」·「0.76~0.83」·「0.713~0.815」는 전부 실측이 아니다 — 인용 금지.
  🔴 **고유 seed 는 필요조건일 뿐이다** — 몸통이 **340/680Hz 고정 + `dur` 포락선**이고 seed 가
  만지는 click 은 `exp(−260t)` 라 210ms 중 **앞 15ms** 뿐이다. **개성은 `dur` 에 있다**
  (`ep14s-2·-3·-4` 가 `dur 0.21` 을 공유해 상호 0.79~0.80). **kind 단위로도 세라** —
  최근 6편 중 **5편이 latch** 인데 `ep12s-1` 의 「세 편 연속 같은 **짝**」 규칙이 못 잡았다.
  🔴 **`thud` 는 `seed` 로 못 가른다** — `rng` 를 아예 안 부르고 `(dur, freq)` 가 파형을 결정한다.
  `freq` 미지정이 **기본 150 으로 몰려** lf01·lf02·ep03s 가 사실상 같은 소리다(전수 190쌍 최댓값 **0.9904**).
- **형제 라벨 = 그 편 `youtube.title` 에서 꼬리만 뗀 것** — `check.mjs` 가 검사한다.
- 🔴 **화면 글자 「전수」 = `lines` + `spec` + `fillText` 리터럴 + `hud.*` + `hook` + `outro.next/siblings`.**
  앞 셋만 센 보고는 전수가 아니다(`ep14s-3` 에서 검수가 여섯 칸을 빠뜨렸다).
- 🔴 **형제 sfx 대조는 층의 목록 말고 `episodes/*/scene.json` 을 스크립트로 훑어라** —
  `ep14s-2` 에서 검수·작성팀 둘 다 「형제 둘」이라 했는데 실은 **셋**이 같은 kind·같은 `dur` 이었다.
- 🔴 **합성 `timed.json` 이 남아 있으면 지운다** — `"voice": "SYNTHETIC-NOT-REAL"`.
  남으면 그 회차 길이 게이트가 **지어낸 값으로 통과**한다. 검수가 만들고 안 지운 전례가 있다.

## 검증 절차
- [그림 회차 소유 = 회귀 검증 4단계](project_kind_ownership_check.md) — kind 해석 경로 확인법 + ep01 의 기존 🔴 실패는 무관하다는 것
- [ep02s 부분 재작업 = APPROVED (2026-07-30)](verdict_ep02s_fix.md) — 렌더 후 국소 수정 판정 4단계 + "밀어내기→소멸"이 reads 를 깨는가
- [효과음 판정 4항목](verdict_ep02s_sfx.md) — ep02s 효과음 = APPROVED. check.mjs 에 sfx 검사가 0건 = 기계 게이트 없는 유일한 계층
- 🔴 [피크 천장 초과는 **출력 피크로 못 잡는다** — `console.warn` 을 가로채라](verdict_ep14s_1.md) — `synth()` 가 걸리면 스스로 gain 을 깎아 출력이 정의상 0.46 을 못 넘는다. 「초과 0건」이 항상 참인 거짓 검사
- 🔴 [`ADR-V25-15②` 재검 = `timed.json` × 엔진 진입 페이드 알파 실측](verdict_ep14s_1.md) — 최대 α · α≥0.6 총 길이 · 발화 개시보다 앞서 끝나나. 검수는 개정 전 잣대(①만)로 본다
- 🔴 [효과음 돌려막기 = **절대 표본 정렬** 파형 상관으로 판정](procedure_sfx_waveform_correlation.md) — 「정규화 상관」이라 쓰지 말 것(두 뜻으로 읽혀 사고남). 정규화 위치 리샘플은 이 결함을 못 잡는다
- [delayMs 는 timed.json 종속](procedure_sfx_retiming_fragility.md) — tts 재실행이 효과음 6개를 통째로 무효화. `t` 기반 그림은 앞줄 길이 합에 매달린다
- 🔴 [다음 편 예고 = 3단계 대조](procedure_next_preview_fact_check.md) — 원문에 있는가 · 그 편이 **버린 것**이 아닌가 · 쪼개져도 참인가. ep02s·ep11s 두 번 틀린 자리
- 🔴 [예고 구간 연출 = 카드 시각 안에서 **완결**](verdict_ep12s3.md#) — 금지는 「끝까지 자라는 것」이 아니라 「끝까지 못 자라는 것」. 상한 `TOTAL−2600`
- 🔴 [정적 구간 잣대는 **fps 불변이 아니다** — fps 30 값으로 반려 금지](verdict_ep12s_2.md) — 문턱 0.0008 이 FPS 미정규화. ep12s-2 0.2→3.4s · 발행된 ep11s 0.0→2.5s. 정본은 기본 fps 5
- [아웃트로 카드 vs 페이오프 = 4줄 산수](procedure_payoff_vs_outro_card.md) — ep08s-3 반려 지점. 여유의 손잡이는 `span` 하나뿐 — `lead` 를 고치라고 요구하지 말 것
- 🔴 [kind 수정 재검증 = **격리 스틸 비교**](verdict_ep13s_1.md) — 1차↔2차 스틸은 kind 수정과 의도한 편집이 섞인다. `kinds` 만 되돌려 뜨면 Δ>8 이 0 으로 떨어진다 + 대조표에 「그 파일이 실제로 바뀌었나」 열 필수
- 🔴 [아웃트로 카드 = `TOTAL−3000`](verdict_ep13s_1.md) — ADR-V25-10 으로 2600→3000 개정됨. 옛 `TOTAL−2600` 은 폐기
