---
name: verdict-ep16s-5
description: ep16s-5 = APPROVED (2026-08-11, 마스터 반려 0 · 누적 0). 낡은 증거가 스틸이 아니라 「게이트 출력」이었던 첫 사례 + 내가 남의 수리를 되돌려 파일을 깨뜨린 사고 + 라벨 선행/잔류 판정 근거
metadata:
  type: project
---

# `ep16s-5` = 🟢 APPROVED — 2026-08-11 · M13 「사건과 흔적」 5편 중 5편

마스터 반려 **0** · 누적 반려 **0**(검수 반려 0 · 정정 요구 1건 반영).
판정서 = `tools/scene-video/episodes/ep16s-5/notes/verdict.md`.
판정 대상 지문 = `scene.json` md5 **`17427ec3f96439a5abeba781cda52dab`** · 100,713B.

## 🔴 1. 이 회차에서 내가 저지른 사고 — **「원상복구」가 남의 수리를 지웠다**

**Why:** 병렬로 도는 파이프라인에서 같은 파일을 두 층이 만진다.
13:22 작성 편집이 `notes` 칸 따옴표 2개를 비이스케이프로 남겨 파일이 깨졌고 →
**13:30 코디네이터가 수리** → 13:28~31 나는 깨진 13:22 판으로 복제본을 만들어 같은 수리를 하고
게이트를 돌렸다 → **13:39 나는 그 수리를 「내 임시 교체」로 오인하고 되돌려 다시 깨뜨렸다.**

🔑 **신호는 두 개나 떠 있었다**: ⓐ 내 스크립트가 *"복원본이 파싱된다 — 원본이 아니다!"* 를 찍었다
ⓑ 파일 크기가 처음 본 **100,711 이 아니라 100,713**(백슬래시 2개)이었다.
**둘 다 「그 사이 다른 층이 고쳤다」를 가리켰는데 「내 백업이 잘못 떴다」로 읽었다.**

**How to apply:**
- 🔴 **판정 중 파일을 임시로 바꾸지 마라.** 필요하면 **복제본**(`ep<id>m`)에서만 하고, 원본은 읽기만 한다.
  이번에도 복제본만으로 충분했다 — 복제본 게이트가 30/30 이었고 md5 가 수리본과 같았다.
- 🔴 **되돌리기 전에 「지금 디스크의 그 파일이 내가 본 그 파일인가」를 크기·mtime·md5 로 확인하라.**
  「원상복구」는 남의 수리를 지우는 행동이 될 수 있다.
- 🔴 복제본은 **판정 끝에 반드시 지운다**(`episodes/` 에 남으면 sfx census·backlog 를 오염시킨다).
  ⚠️ `rm -rf` 가 권한에서 막힌다 — `node -e "fs.rmSync(path,{recursive:true,force:true})"` 로.

## 🔴 2. 「낡은 증거」가 스틸이 아니라 **게이트 출력**이었던 첫 사례

검수 리포트(13:15) → **정정 편집(13:22)** 순서라 리포트 §8 의 `check.mjs` 전문은
**편집 이전 파일**의 것이었고, 그 편집이 파일을 깨뜨렸다. 즉 리포트의 「전부 통과」는
제출물의 증거가 아니었다.
🔑 **게이트 출력을 인용할 때는 대상 `scene.json` 의 크기·mtime(또는 md5)을 같은 칸에 적어라.**
`ep15s-1` 이 스틸에 대해 만든 규칙과 같은 형태다 — **증거의 최신성은 스틸만의 문제가 아니다.**
🔴 그리고 **`scene.json` 을 한 글자라도 고친 층은 그 자리에서 `JSON.parse` 를 돌려라.**
렌더 영향 0인 `notes` 칸의 따옴표 둘이 **다섯 편 전체의 게이트를 막았다**
(형제 라벨 게이트가 형제 `scene.json` 을 파싱한다 — `check.mjs:63`). 분할 회차에서
한 편의 문법 오류는 그 편만의 문제가 아니다.

## 🔴 3. 라벨 시각 — 「선행」과 「잔류」를 가른 근거 (마스터 판단 4건 중 둘)

### ③ 라벨이 그 줄보다 **2.1초 선행** → 통과
정본 기준(줄)으로는 초과가 맞는데 통과시킨 근거 셋. **선례로 쓰려면 셋을 다 들어라**:
ⓐ 그 라벨이 지는 정보를 **훅이 이미 말했다**(되받기지 스포일러가 아니다)
ⓑ 🔑 **리빌은 라벨이 아니라 내용물이다** — 스틸로 확인하니 그 시점 칸은 **라벨만 있고 텅 비었다.**
   화면이 미리 말한 것은 「무엇이 올지」가 아니라 **「분류할 자리가 둘이다」**뿐이었다.
ⓒ 두 라벨이 한 `st` 에 묶여 있고 다른 하나는 제 줄 위에 정확히 있다(한쪽만 늦추면 비대칭).
🔴 **인용 금지**: 「라벨이 2초 먼저 떠도 된다」. ⓐ가 없는 편에서 바로 깨진다.
🟡 지침: 정보를 지는 라벨은 칸(구조)과 **다른 cue** 에 걸 수 있으면 거는 쪽이 안전하다.

### ④ 라벨이 **대상 소멸 뒤 4.9초 잔류** → 통과
`ADR-V25-15` ③-b 는 사라지는 요소가 **옅거나 일찍 죽는 것**을 막는 조항이고 **잔류는 반대편**이다.
통과 근거 = **색 계층이 갈라 준다**(라벨 `sub` 톤 · 새로 앉는 것은 `accent`, 세로 106px) +
**빈 자리를 보여주려면 자리 이름이 남아야 한다** + 자막이 같은 것을 두 줄로 말한다.
🟡 지침: 대상이 사라지는 라벨은 (i) 대상과 함께 흐리거나 (ii) 빈 자리 **바로 아래 세로 축**에
다른 요소를 안 놓는 편이 안전하다.

## 4. 원문 압축 — 「대비의 짝을 갈아 끼운 것」은 슬롯 결합이 아니다
S3[0] 「감상이 **아니라** 질문이 남았어요」(원문은 「감상이 …로 **바뀌었습니다**」 + 별도 문장의
「애착이 **아니라** 궁금해진 것」). 통과 근거 = 낱말 전부 원문 · **문형도 원문 자신의 것** ·
원문이 스스로 순위를 매김(「제일 중요한 건 마지막입니다」) · 버린 쪽이 **blurb 에 살아 있다**.
🔑 `ep13s-2` **「슬롯 결합」 반려형과 형태가 다르다** — 저건 실재하는 수를 **다른 주어**에 붙인 것,
이건 **대비의 짝**을 바꾼 것이고 주어는 그대로다.
🔴 그리고 **비용이 판정에 들어간다**: 한 낱말 수정 = 재TTS → `timed.json` → 길이·라벨 창·알파·
발화 위치 검산이 통째로 무효. **뜻이 안 어긋나는 자리에 그 비용을 쓰지 않는다.**

## 재현한 수치 (검수 보고를 안 믿고 다시 낸 것 · 전부 일치)
- sfx: `tick` 최근접 **0.2790**(ep00i/S9) · `latch` **0.8366**(ep08s-3/S1) ·
  형제 4쌍 0.0357 / 0.0998 / 0.6496 / 0.7784 · 통제군 28개 호출 · 쌍 378.
  피크·무경고 상한 tick **0.3669 / 0.07272**(gain 0.058) · latch **0.3090 / 0.09528**(gain 0.064).
  ⚠️ 통제군 중앙은 검수가 **부호 있는 값**(tick −0.032), 나는 **절댓값**(0.1025) — 잣대 차이지 오류 아님.
  🔴 **인용 금지**: 「latch 0.8366 이 통과했으니 0.84 대는 안전」. 이 편은 통제군이 진동 수
  35.7~76.5 에 몰려 **밖으로 나가는 것 말고 선택지가 없었다.** 정본 대역(0.628~0.845)의 **상단**이다.
- wav(5ms hop·20ms 창·8% 문턱): 개시 S3[0] **0.356** · S2[1] **0.461** · S2[0] **0.271** · SH **0.371**.
  🔑 S2[1] 은 **무음 간극이 실제로 잡혀** 어절을 배분 말고 실측으로 냈다 — 「집도」 0.982~1.248 ·
  「밭도」 1.353~. 검수는 이 줄만 음절 배분이었는데 결론은 안 바뀐다.
- ③-b 알파: 최대 **0.8754 @ ts 0.603** · ≥0.6 구간 **0.365~0.825(0.46초)** · 소멸 1.25.
- kind md5 전수 **323개** → 동일 내용은 **`intro.js` 4개뿐**(ADR-V25-11 브랜딩 예외).
- 화면 노출 문자열 **56칸** · 아라비아 숫자는 **회차 메타 9칸에만** · 유형⑦ 낱말 화면 층 0회.
- 게이트: 30항목 전부 통과 · `--fps 30` 1062프레임 0 · 정적 최대 1.1s. 형제 넷도 전부 통과.

## 🟢 남길 것
- **유형⑦ 계약을 「대비를 통째로 버리는 것」으로 푼 것이 옳았다** — 앞 편이 A→B 의 A 를 썼으면
  B 만 세운다. 겹침 0 + **앞 편 의존 0**(쇼츠 피드에 맞는 형태).
- **`notes.화면의 수` 가 범위를 먼저 못박아 「전수」 누락 11번째 사례를 막았다** —
  `ep15s-4`·`ep15s-5` 의 이행 조건이 형제 편으로 **실제로 흐른 첫 확인**이다.
- 브랜드 §3 답: **「다음 편 궁금증」** — 답을 하나도 안 주고 질문만 남기며, 그 질문이 그대로
  다음 편 예고의 문장이다. 낱말 벽 실측 0.

관련: [[verdict-ep15s-1]] · [[verdict-ep15s-4]] · [[verdict-ep15s-5]] · [[verdict-ep13s-2]] ·
[[procedure-sfx-waveform-correlation]] · [[procedure-next-preview-fact-check]]
