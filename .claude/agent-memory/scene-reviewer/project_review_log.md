---
name: project-scene-video-review-log
description: 회차별 검수 판정과 반려 횟수 — "반려 3회면 사람에게 넘긴다" 규칙을 세려면 대화를 넘어 남아야 한다
metadata:
  type: project
---

회차별 판정 기록. **반려 3회면 멈추고 사람에게 넘긴다**는 규칙 때문에 횟수가 대화를 넘어
남아야 한다.

**Why:** 반려 카운트는 세션 안에서만 세면 모델이 교대하는 순간 0으로 돌아간다.
같은 자리를 세 번 못 고치는 건 작성팀 실력이 아니라 지시가 잘못됐다는 신호라,
그 신호를 놓치면 무한 왕복이 된다.

**How to apply:** 판정할 때마다 아래에 한 줄 추가한다. 같은 회차 3번째 반려면
지적을 쓰지 말고 사람에게 넘긴다.

| 회차 | 회 | 판정 | 사유 요약 |
|---|---|---|---|
| ep01s (초판) | — | (검수 기록 없음, 사용자 "이 정도면 적당해" 판정으로 종결) | |
| ep01s (재제작) | 1 | 🔴 반려 (2026-07-29) | `spoke`(S1B)가 `ep00s/channels`(S9)와 **장치가 같음** — 한 출발점→네 갈래→하나만 채워짐 + 값 3을 표식 3개로 실물화 + 하단 가운데 mono 캡션. 배치만 달랐다. 가중: `channels.js:11` 주석이 *"ep01s 가 카드 그리드를 쓰니 나는 관으로 간다"*고 사 둔 간격을 1편이 그리로 들어가 써 버림 |
| ep01s (재제작) | 2 | ✅ 통과 (2026-07-29) | `pins` 로 교체 — "네 갈래" 구조 자체를 버림(코드에 4원소 그릇 없음). 겹침 8항목 중 6개 해소, 남은 2개는 원문이 강제. 마스터 승인 |
| ep00s | 1 | 🔴 반려 (2026-07-29) | ①`checks.numbers`/`checks.infographic` = 읽히지 않는 키 ②S6 "여섯 중 넷" 도장 우측 잘림 ③S6 막대 순서 역전 → 5.3초간 "감시 장치 0" ④S4 가 자막을 그대로 옮긴 화면 |
| ep00s | 2 | ✅ 통과 (2026-07-29) | 4건 전부 해소. 보고서 = `tools/scene-video/episodes/ep00s/notes/review-v1.md`. 발행 전 정정 1건만 남김 |
| ep02s | 1 | 🔴 반려 (2026-07-30) | ①총 길이 83.6초로 쇼츠 60초 상한 초과(요구 40~60초, 39% 초과) ②S1 원문에 없는 대사 두 개("탐구하고 싶어.", "겁이 나요.")를 회색 예시로 화면 배치 + `writer.md` 6절이 "원문 그대로"라 허위 표기. cue 인덱스 4건 초과(S3·S5·S6·S10)는 재확인 요청. 무인 파이프라인(단일 패스), 재작성 루프 없음 — 마스터에게 안 올림 |
| ep02s | 2 | 🔴 반려 (2026-07-30) | 1차 반려 세 항목 중 두 개(총 길이 47.9초로 안, S1 가짜 대사 제거)는 해소. 그러나 **같은 종류의 사실관계 위반이 다른 자리에서 잔존** — S3 wardline 이 6종 성격 이름 6개(게으름·탐구·용감함·**성실·겁많음·쾌활**)를 화면에 나열하는데, 뒤 3개는 원문에 없음. 원문은 3종만 이름 언급 + `Random.Range(1,7)` 로 6종 존재만 명시. writer.md 7절 "수치 정직성"이 숫자만 대조하고 문자열은 안 봤다는 사각지대가 1차 후에도 잔존 — [[review_method_fact_check_strings.md]] 원칙을 excluded 목록에도 일관 적용해야 |
| ep02s (재작업) | 1 | 🔴 반려 (2026-07-30) | 🔑 **길이 반려가 무효로 확정돼 트랙을 새로 열고 카운트를 0에서 다시 셈**(사람 확정). 처방 P-A~P-F 전부 집행 확인, 사실관계 위반 0(신규 화면 문자열·자막 13줄 전수 대조), cue 15건 전부 정합, 돌려막기 4건 전부 회피, 87.3초. **유일한 지적 = `check.mjs` ⚠ 정적 구간 S8 3.6s** — `tripwire` 의 유일한 움직임(14×12px 화살표)이 판정 임계 아래(실측 m 0.00057 / 임계 0.0008)라 3.6초가 기계에게 정지. P-D 로 자막이 2→3줄 늘며 터졌고, `review-redo.md` §4.2 가 "기존 샷도 자막을 늘리면 넘는다"고 미리 경고한 자리 |
| ep02s (재작업) | 2 | ✅ 통과 (2026-07-30) | `kinds/tripwire.js` **한 파일만** 수정(mtime 으로 확정 — `git diff` 로는 못 가름, [[review-method-what-changed]]). 실선을 훑는 띠 넷을 심어 S8 정적 3.6s → **0.0s**(최악 프레임 m 0.004543 = 임계 5.7배). 회차 최대는 무변경 샷 S4 의 2.4s. 두 번째 사건으로 안 읽힘(색·모양·시작 순간·cue 전부 없음), `wireY` 가 실선과 **동일식**임을 대수로 확인(제어점이 중점인 quadratic = 직선), 도장에 닿는 잉크 최대 α 0.142. 🟡 화살촉이 띠 머리에 잠기는 최장 0.43초는 관찰로 남김 — [[review-method-occlusion-measure]]. 보고서 = `episodes/ep02s/notes/review-redo-report2.md` |
| ep02s (3차 라운드) | — | 📋 처방만 (2026-07-30) | **사람이 직접 연 라운드 — 승인·렌더(87.3초)가 끝난 뒤였다.** ①🔴 S10 예고가 원문 77행을 옮겨 사실과 다름(정본은 ep03s = 2막 · 리셋 · 24,800→5,977) ②🟡 S9B 화면이 자막을 옮겨 적음 — 지목된 `laterHead` 뿐 아니라 `nowLead`/`nowHead` 도 같은 위반. 검수팀은 파일을 안 고치고 처방서만 냄 = `notes/review-redo2.md`. `check.mjs` 는 이 라운드 착수 시점에 **14종 전부 통과** 상태였다 — 두 결함 모두 기계에 항목이 없다 |
| ep02s (효과음 재작업) | 1 | ✅ 통과 (2026-07-31) | 파이프라인 **첫 효과음 회차**. 6샷 6개(S3B tick·S5 drop·S7 sweep·S8 latch·S9B riser·S10 tick), 대본·그림·길이 무수정. `delayMs` 여섯 개를 draw 코드에서 **독립 재계산** → 최대 오차 3ms. 🔑 작성팀이 사용자 참고 배치를 셋 뒤집은 것이 이 작업의 값이다 — 특히 **S8: `tripwire` 의 걸림은 `cue` 가 아니라 `frac(t/5.2)` 로 돈다**(at2 시점 u=0.059 = 곧은 실선). 절제 6개/95.5초 = 90초 환산 5.65개, 최소 간격 7.9초. 음량은 소리 나는 그 순간의 나레이션 단기 RMS 로 실측 — 여섯 다 −9dB 이상 아래(S5 는 발음 골, S8·S9B 는 말 시작 전 무음). tick 두 개는 1750Hz/50ms vs 1044Hz/90ms = 9반음·1.8배로 갈림. 🟡 반려 아닌 기록 정정 5건(`writer-sfx.md` 「영상 내 시각」 열이 S9B·S10 에서 ~1초 과다, `spring` 정점 38ms). 보고서 = `episodes/ep02s/notes/review-sfx.md` |
| ep02s | 3 | ✅ 통과 (2026-07-30) | 지시대로 `wardline.js` 한 파일에서만 화이트리스트(`KNOWN = {'게으름','탐구','용감함'}`) 필터로 처리, `scene.json` 무수정. 화면 나가는 이름은 원문 근거 3개만, 나머지 3자리는 `···` 익명. `picked=0`(게으름)이라 채워지는 원 안 이름도 KNOWN 안. check.mjs 13종 전부 통과, 총 길이 47.9초, 렌더 성공. 문자열 전수 대조 결과 무근거 문자열 0. 반려 3회 규칙 미도달 — 마스터에게 넘김 |
| ep04s (재구성 1편) | 1 | 🔴 반려 (2026-08-03) | 1차본(12샷 158.8초)은 이미 마스터 APPROVED. 지속률 문제로 5샷 70.4초로 **축을 갈라** 1편(명령·거부)·2편(여가·구현)으로 나눈 트랙의 첫 검수. 사실관계 위반 **0건** — 이그레스가 열려 원본 HTML 을 직접 받아 대조했다(post-body 안 table 0·pre 0·svg 0·img 0 이라 "지워진 표" 위험 자체가 없는 글). ①🔴 **S8 `rung60` — `leaveCue`/`returnCue` 를 같은 자막(2)에 합치면서 이탈→복귀가 화면에서 사라졌다.** `my = lerp(lerp(c,z,ease(lk)), c, ease(rk))` 에서 두 인자가 같은 cue·같은 span 이라 `lk ≡ rk` → `my = c + (z−c)(a−a²)`, 최대 이동 **26px/104px(25%)** 이고 창 끝값이 정확히 출발점. 배고픔(100) 칸 `hot = 0.000` 으로 한 번도 안 켜진다. 나레이션은 "밥 먹고, 알아서 명령으로 돌아와요"라고 그 사건을 부른다 — **인덱스는 전부 범위 안이라 기계도 인덱스 검사도 못 잡는 자리** ②🔴 **꼬리 예고 삭제가 리포 안 규칙 충돌** — `check.mjs:82`(2026-08-03 추가, 사용자 근거 없음, 예고를 뺀 그 커밋에서 함께 들어옴) vs `routine-prompt.md:23`·`scene-writer.md:107`(둘 다 **사용자 요청 2026-07-30 인용**). 게다가 같은 커밋의 `LIMIT=75`(fail) 는 `routine-prompt.md` 의 "🔴 길이 상한은 없다 — 사용자 확정" 과 정면 충돌한다. **세 규칙이 동시에 만족 불가**(70.4초 + 최소 예고 5초 > 75) 라 작성팀이 못 푼다 → 사람에게. 🟡 정정 6건(excluded 5항목이 2편으로 간 샷 S3·S4·S10·삭제된 S12 를 근거로 듦 / "현재 컷에 맞춰 다시 썼다"고 선언한 notes 7항목 중 길이·cue_인덱스·pacing 셋이 이전 컷 값 / S9 의 "쉬어도" 전제 / S7 지워진 자막이 excluded 에 없음 + reads 순서 / S1 세 줄이 전부 "-었어요" / sfx why 가 ep00i 대조 누락 — S11 tick dur 0.04 가 ep00i S9 tick 과 dur 완전 일치). ✅ sfx `delayMs` 넷 독립 재계산 최대 오차 5ms, S1 의 `at 2→1` 이동과 why 안 cue 참조 수정 모두 정확. 보고서 = `episodes/ep04s/notes/review-recut.md` |

| ep11s | 1 | 🔴 반려 (길이) (2026-08-08) | 1차 실측 **28.8초**(상한 28 초과) + 예고 3.2초. 처방 = 「첫 두 줄·제목은 손대지 말고 가운데에서 1.2초 이상 덜어내라」. 작성팀이 S2 두 줄·S3 한 줄·예고를 줄여 1.82초 덜어냄(`pauseAfter` 무수정) → 재실측 26.8/21.5/2.7 로 전부 대역 안. **처방 이행은 정확했다** |
| ep11s | 2 | 🔴 반려 (2026-08-08) | `check.mjs` **23종 전부 통과**인데 셋이 걸렸다. ①🔴 **`premise.js` 가 화면에 「쫓아가서 **서** 건네준다」를 띄운다** — 판을 두 번 그리며 클립을 `[0,crackX)`/`[crackX,w)` 로 **맞닿게** 잡아 원본 `x∈[crackX−off, crackX+off)` **20px 가 두 번 그려진다**. 인용문이 21px 로 확정돼 **정확히 한 글자가 복제**. 곁따라 "검은 틈 안에서만 균열" 도 거짓(판 윗변 y=190 행이 전부 흰색, 끊긴 x172~174 는 균열 자신) — 강조색이 흰 글자를 **불투명하게** 덮어 혼합 픽셀 0개라 팔레트 게이트가 구조적으로 못 본다 ②🔴 **같은 강조색 빗금이 같은 강조색 라벨을 훑어 `REWARD`(chase)·`DEBT`(settle 위 캡슐)가 안 읽힌다** — 작성팀이 `ledger.js:20-21` 에 **자기가 적어 둔 규칙**을 어겼고 같은 회차에 정답 둘(S3 카드·S4 아래 카드)이 나란히 있다 ③🔴 **예고가 다음 편 원문과 어긋난다** → [[review-method-next-episode-teaser]]. ✅ 그 밖은 전부 깨끗 — 페이오프 21.15s vs 카드 24.20s(여유 3.05s, 카드 직전 프레임 스틸 확인) · sfx 전수 대조표 참값(24파일·73건, ep11s 제외 23/71/drop7/tick18 정확 일치) · `KIT.drop`·`KIT.tick` 이 `rng` 미호출이라 파라미터=파형 주장 참 · `delayMs` 재계산 오차 3ms/6ms · 피크 천장 미접촉(tick 실효 상한 0.0616 > 0.056) · 원본 HTML table/svg/pre/img 0건 |

| ep11s | 3 | ✅ 통과 (2026-08-08) | 반려 3건 전부 해소, 증거 확보. ①`premise` 클립을 `[0,crackX−off)`/`[crackX+off,w)` 로 밀어 겹침 제거 + **금 자리를 `measureText` 로 낱말 사이 공백 한가운데로** 이동 → 판 윗변 행 계측에서 `off=10` 일 때 **실제 검은 틈 19.6px(=2×off)**, 균열은 틈 한가운데(양옆 검정), 혼합 픽셀 0. `off=0` 이음매도 밝기 215~230/255 로 실배율 비가시 ②빗금을 **테두리 밖 점선 후광**으로 이동 → `REWARD`·`DEBT` 확대 스틸에서 완독. `hatch()` 는 호출부 소멸로 함수째 삭제 ③예고를 ep12s **원문 자기 요약**(「마을에 처음으로 「자리」와 「재난」이 생기고」)으로 교체. 🔑 **작성팀이 내 제안(「밭을 한 개도 못 때려요」)을 기각한 사유가 더 옳았다** — ep12s 가 아직 분할 전이라 사건 단위 문구는 다른 편으로 갈 수 있고 밀스톤 층위 문구는 어떻게 쪼개져도 참이다. 🔴 **내 🟡-3 은 오판이라 철회**(아래 참조). 🟡 정정 5건은 전부 실측 뒤 낡은 `notes` 칸(페이오프 21.36→**21.41**·카드 24.38→**24.47**·정적 노출 0.59→**0.449**·길이 3차 산정치 잔존·어미 분포 6줄분). 후광 교체로 움직임이 줄었다는 걱정은 `check.mjs:534` 판정식 복제로 검산 — 5fps 122프레임 최소 `m` **0.00817 = 임계 10.2배**, 임계 미만 0프레임 |

**ep00s 최종 누적 반려 1회.** 사실관계(수치 원문 대조)는 1·2차 모두 **위반 0건** —
반려 사유는 전부 그림 쪽이었다.

**ep02s 최종 누적 반려 2회 (2026-07-30 · 무인 파이프라인, 3차 통과).** 두 반려 모두 사실관계
(원문에 없는 문자열 배치). 1차는 대사, 2차는 성격 이름 — 위반 자리가 옮겨졌다.
3차 컷은 데이터 계층(scene.json)이 아니라 렌더 계층(wardline.js)에서 화이트리스트로
걸러 해소. **"원문에 있는 문자열만 화면에 나간다"는 규칙을 렌더 계층에서 지킬 수 있다**는
전례. 3회 반려 규칙 미도달로 통과.

## 이 회차에서 배운 것

- **수치 주장이 담긴 `notes` 는 반드시 실측과 대조한다.** 2차에서 작성팀이 정적 구간을
  "전부 3초 상한 안"이라 적었는데 실측은 S7 5.8s·S9 4.4s·S10 3.4s 로 셋이 초과였다.
  원인은 계산 모델 — **정적 구간은 "이벤트 없는 자막의 길이"가 아니라 "앞 자막의 애니메이션이
  끝난 시점부터"** 다. 자막 한 줄만 세면 절반쯤 작게 나온다.
- **`git status` 를 반드시 본다.** 씬 파일만 고쳤다는 보고와 달리 `engine/kinds/bars.js` 와
  `scenes/ep01s.json` 이 함께 바뀌어 있었다. 엔진 변경은 다른 회차로 파급되므로
  **직전 회차에 `check.mjs` 를 재실행해 회귀를 확인**한다(ep01s 재실행 → 통과).
- 영상에 안 들어가는 기록 결함(틀린 `notes`)은 **반려가 아니라 보고서의 정정 요구**로 처리했다.
  반려 카운트를 아끼고, 1차에서 확인 항목으로 분류한 것(정적 구간)을 2차에서 반려로 올려
  기준을 사후에 흔들지 않기 위해서다.

[[review-blind-spots]]

---

## 사건 단위 재분할 여섯 편 (2026-08-04) — 2차 검수까지 완료, PR #10 으로 main 머지

`ep04s`·`ep05s` 두 글을 **사건 단위**로 각 3편씩 재분할한 트랙이다
(`ep04s`·`ep04s2`·`ep05s`·`ep05s2` 는 폐기하고 `scene.json`·`kinds` 를 지웠다 — `notes/` 만 증거로 남김).

| 편 | 1차 | 2차 | 최종 |
|---|---|---|---|
| `ep04s-1` | 통과 | — | 마스터가 죽은 `hkk`·`spec.hookCue`·고아 대조표 정정 |
| `ep04s-2` | 🔴 반려 | 🔴 반려 | 마스터가 훅 뒷정리·`writer.md` 거짓 주장 닫음 |
| `ep04s-3` | 🔴 반려 | ✅ 통과 + 정정 10건 | 전부 반영 |
| `ep05s-1` | 🔴 반려 | 🔴 반려 2건 + 이월 8건 | 전부 반영 |
| `ep05s-2` | 🔴 반려 | 🔴 반려 3건 + 이월 7건 | 전부 반영 |
| `ep05s-3` | 🔴 반려 | ✅ 통과(조건부) + 정정 7건 | 전부 반영 |

지금 여섯 편 모두 `check.mjs` 에서 **환경 예외(`build/timed.json` 없음) 하나만** 남는다.
이 컨테이너에 TTS `vendor/` 가 없어서이고 로컬 렌더에서 닫힌다.

### 이 트랙에서 배운 것 (전부 다음 회차에 그대로 적용된다)

- **`latch` 는 `dur` 로 안 갈린다.** seed 를 안 준 **세 회차**가 정규화 상관 0.99 로 같은
  소리를 내고 있었다. → [[sfx-latch-needs-seed]]
- **훅을 캔버스에 그리던 설계가 통째로 무효였다.** 아웃트로 카드가 픽셀 동일 좌표로 덮어
  `ep04s-3` 은 훅이 한 프레임도 안 보였다. 그리고 훅으로 정적을 막는다는 논거도 같이
  무너졌다 — `ep05s-2` 는 훅을 걷어내자 정적이 3.2 → 7.0초로 뛰었다. → [[hook-card-not-canvas]]
- **전수 인용의 오염이 실제로 판단을 놓쳤다.** 세 회차가 지워진 디렉터리를 현역으로 세는
  바람에 `ep05s-1` tick 이 형제 편과 0.63반음인 것을 아무도 비교한 적이 없었다.
- **자기보고의 절반이 사실이 아니었다.** → [[self-report-is-not-evidence]]
- **형제 라벨 18개 중 하나도 안 맞았고 둘은 사실관계까지 어긋났다.**
  → [[split-episode-conventions]]
- 🔴 **길이 규칙이 지시서 안에서 자기모순이었다** — W3 게이트와 "상한 없음"이 동시에 살아
  있었고 검수 둘이 그것을 근거로 에스컬레이션했다. → [[feedback-length-policy]]

### 도구로 옮긴 것 (반복해서 틀리던 것을 기계가 본다)

`check.mjs` 에 세 가지가 들어갔다 — **「산정 길이 (참고 · 판정 아님)」**(대본이 길이를
자기보고하던 자리를 대체) · **형제 라벨 대조**(`youtube.title` 파생) ·
**아웃트로가 덮는 마지막 2,600ms 를 정적 계측에서 제외**.

---

## 롱폼 트랙 — `lf01` (2026-08-09)

파이프라인 **첫 `format:"wide"` 본편**. 34샷 · 자막 126줄 · 클립 8 · 캔버스 26 ·
효과음 17 · 산정 423.6초.

| 회차 | 회 | 판정 | 사유 요약 |
|---|---|---|---|
| `lf01` | 1 | 🔴 반려 (2026-08-09) | 반려 5 · 정정 4 · 관찰 2. **사실 층은 126줄 전수 대조에서 위반 0** — 무너진 곳은 전부 "화면에서 실제로 확인했는가"였다 |

**반려 5건** — ①게임 HUD 겹침(§4 반려 사유): `wide.css` `.hud{top:3%}` 띠가 S18 의
`■ 굶주리는 주민 C …`, S12 의 `■ 늑대 무리 — 1일 뒤` 를 **가운데부터 지운다** — 하필 그
줄을 자막이 가리키는 샷들이다. 제목을 9자로 줄여도 안 풀린다(폭이 아니라 y 좌표).
②S23 `forecast-…-58` 에 **늑대 예고줄이 한 프레임도 없다**(카탈로그 `detail` 만 믿고 채택).
③S8 `gothome-…-12` 는 입주가 아니라 밭일 장면 — `reads` 가 화면과 다름.
④콜드오픈 25.6초(§1 30~60 미달) · 챕터 「정적」41.7초 등 3개가 1분 미달.
⑤S29 「공성을 먼저 넣었습니다」가 M22 명세 5절(W4 울타리 → W5 공성)과 순서 반대.

**정정 4건** — 쇼츠 유도 시제(「시리즈에 있습니다」인데 `ep13s` 미제작·화폐 미발행) ·
효과음 자기보고 15≠17 · `notes.길이` 미기입 · 캔버스 정적 3샷(zerocode 14.6s ·
twoq 6.6s · motion5 4.6s — 연속 모션이 cue 게이트 뒤에 있다).

**이 회차에서 배운 것**
- 🔴 `episodes/lf01/build/timed.json` 에 **W1 더미(3샷)** 가 남아 있어 첫 check 가
  34샷 회차를 「총 길이 10.6초」로 통과시켰다 → [[review-method-stale-timed-json]]
- 🔴 **클립은 카탈로그 `detail` 로 채택하면 안 된다** — `detail` 은 사건 이름이지 프레임
  안에 그것이 있다는 보증이 아니다. 두 건이 그렇게 샜다 → [[review-method-wide-longform]]
- 🔴 **게임 HUD 겹침은 클립 스틸로는 안 보인다** — `render.mjs --still` 로 엔진 합성
  프레임을 떠야 보인다.
- 🟠 관찰: `render.mjs --still` 과 `check.mjs` 둘 다 **클립 8샷이 샷 길이만큼 정지**라고
  말한다(PSNR inf · 정적 7.4~10.2s). 영상 파일은 멀쩡하므로 엔진 클립 seek·draw 쪽
  의심 — 대본으로 못 고친다. 마스터/파이프라인 몫으로 올림.

### `lf01` 2차 (2026-08-09)

| 회차 | 회 | 판정 | 사유 요약 |
|---|---|---|---|
| `lf01` | 2 | 🔴 반려 (2026-08-09) | **1차 반려 5건 전부 해소.** 남은 반려 = 2차에서 새로 들어온 자막 한 줄(S23 「그 왼쪽에는 무덤 두 기」 → 화면은 무덤 **4기 · 전부 오른쪽**). 정정 2 · 관찰 2 |

- 🟢 엔진 수리 확인: 클립 정지(1차 O1)의 원인이 **정적 서버의 HTTP Range 미지원**이었고
  수리 뒤 같은 측정에서 PSNR `inf` → **18~20dB**, check 정적식 m 이 문턱의 13~30배.
- 🔑 **TTS 전 `check.mjs` 의 클립 정적 경고는 대부분 가짜다** — `timed.json` 이 없으면
  엔진이 `PROVISIONAL_CPS 5.6` 을 쓰는데 그 길이가 클립 창을 넘겨 꼬리가 언다.
  7건 중 6건이 「임시 길이 − 클립 창」과 오차 0.4초 안에서 일치했다. 판정 전에
  `out−in` vs 산정 길이(6.7)를 대조해 가르라.
- 🔴 **같은 실수가 두 번**: 1차 R2(브리프의 「사용자 확인」 표시를 스틸 확인으로 갈음) →
  2차 R6(스틸을 봤다고 적었는데 개수를 잘못 셈). **화면의 수·방향을 말하는 자막은
  그 자막이 떠 있는 시각의 엔진 스틸에서 세라** — 클립 `-a/-b/-c` 스틸은 샷이 쓰는
  창(`in`~`out`) 밖일 수 있다(`forecast-99` 는 `in:6` 이라 `-a`·`-b` 가 창 밖).
- 🔧 화면 오브젝트 개수를 다툴 때는 **색으로 세라**: 무덤 = `SimulationLoop.cs:668`
  `Color(0.45,0.45,0.5,0.9)` 이고 게임 전체에서 그 회색을 쓰는 것은 무덤뿐이다
  (`grep "sr.color" Assets/Scripts/M0/` 전수). 스틸을 480×270 으로 줄여 군집 세기.
  🔴 **단 「없다」를 증명하는 데는 쓰지 마라** — 3차에서 거짓 0을 밟았다(아래).

### `lf01` 3차 (2026-08-09)

| 회차 | 회 | 판정 | 사유 요약 |
|---|---|---|---|
| `lf01` | 3 | ✅ **통과** (2026-08-09) | R6 해소(S23 자막 = 「그리고 화면 오른쪽에는 / 무덤이 서 있고요.」 — 수·방향을 안 말하는 (b)안) · `notes.길이` = 실측 「485.0초 = 발화 473.1 + 꼬리 11.90」 · 새 red 0. **반려 2회로 종료** |

- ✅ **무변경 검산 3중**: ①mtime — `scene.json` 만 07:11, `kinds/*.js` 는 전부 2차 시각
  ②구조 — 샷 34·자막 143·sfx 17·클립 `in`/`out` 8샷 전수 동일 ③**check 출력 델타가
  그 한 줄로 정확히 설명됨**(산정 길이 485.0 불변 = 구두점 제외 17→17자 / 정적 7.0→6.6
  = `say` 25→23자 ÷ `PROVISIONAL_CPS 5.6` = 0.357초 / 결정성 4197→4196프레임).
  → [[review-method-what-changed]] 에 이식.
- 🔴 **내가 거짓 0을 밟았다.** S20 의 무덤 고리(「맨 처음 늑대 장면 화면 **왼쪽**」)를
  검산하려고 S1 클립 18초를 2fps 로 훑어 회색 군집을 셌더니 **0건**. 반려 직전에 확대
  스틸을 떴더니 그 컷에서는 무덤이 **주황 원에 가려져 라벨 `C · Day 9` 만 보였다.**
  자막은 사실이었다(엔진 스틸 `1s.png` 에서 x≈578/1920 = 왼쪽 30%).
  → [[review-method-occlusion-measure]] 에 이식.
- 🔑 **카메라가 움직이는 컷에서 방향은 「자막이 가리키는 시각」으로만 말할 수 있다** —
  같은 무덤이 클립 t=1s 좌 30% → t=6s 우 61% → t=14s 화면 밖이었다(`camDist 2`).
- ⏭ 마스터 이월: TTS 뒤 `check.mjs` 재실행해 ①red 해소 ②총 길이 485.0±2.0 ③**정적 경고
  7건 소멸**(2차 예측 검증) 확인. 그 전까지 scratchpad 의 W1 더미 `build/` 복구 금지.

---

## 롱폼 트랙 — `lf02` (2026-08-09) · 검색 유입형 번외 「Claude Code로 46일」

27샷 · 자막 114줄 · 클립 5 · 캔버스 22(kind 21종) · 효과음 11 · 앵비언트 7 ·
산정 450.7초(→ 실측 추정 549초). 보고서 = `episodes/lf02/notes/review.md`.

| 회차 | 회 | 판정 | 사유 요약 |
|---|---|---|---|
| `lf02` | 1 | 🔴 반려 (2026-08-09) | 반려 3 · 정정 8 · 관찰 5. **사실 층은 114줄 전수 통과**(지어낸 수치 0). 반려는 전부 「렌더해야 보이는 자리」 |

**반려 3건** — ①**캔버스 글자 겹침 5건**(ledger·identity·decay·mechanisms·handoff)
→ [[review-blind-spots]] 5번으로 이식 ②S1 콜드오픈 「이 다섯 명, **각자 다른** 계획」이
프레임과 다름(클립 스틸에서 **둘이 같은 말풍선**, 5.5초엔 넷만 남음 — 작성팀 `source` 도
「문구는 넷」이라 적어 놓고 자막은 「각자 다른」) ③S18 「**제가** 셸 명령 한 줄로 에셋 4개를
날린 날」이 정본(devlog 08-05 = AI 세션 로그, 복구는 `Edit 도구`)과 **주어가 반대**이고
같은 챕터의 주제(「AI도 실수한다」·S17 「AI 자신의 실수」)를 뒤집는다.

**이 회차에서 배운 것**
- 🔑 **정적 경고의 원인이 lf01 과 달랐다.** lf01 = 연속 모션이 cue 게이트 뒤 / lf02 =
  **모션이 문턱보다 작다.** 판정식 복제 실측: `costfloor` 3px 탐색선 m **0.00051** ·
  `bomrecover` **0.00015**(문턱 0.0008). 반대로 경고를 안 받은 `antipattern` 은 **40px 띠**다
  → 처방은 「cue 밖으로」가 아니라 **「면적을 키워라」** ([[review-method-static-motion-floor]] 확증).
- 🔑 **클립 창을 산정(÷6.7)으로 재면 실측에서 넘친다.** 작성팀이 `out−in ≥ 산정 길이`로
  다섯 샷을 잡았는데, 같은 노트가 적어 둔 lf01 배율(**실측 = 산정 × 1.219**)을 적용하면
  다섯 전부 0.9~2.2초 초과다. **창 검산은 산정이 아니라 산정×1.22 로 한다.**
- ✅ **HUD 겹침(lf01 R1)은 재발 없음** — 게임 HUD 는 x ≤ 725px, 엔진 HUD 는 우측 46%
  (x ≥ 1030). 엔진 스틸 3장으로 확인. 겹친 것은 게임 HUD 가 아니라 **캔버스 자기 라벨**.
- ✅ 돌려막기 대조를 **전 186개 kind 자동 유사도**로 돌렸다(정규화 후 `SequenceMatcher`).
  lf02 최고값 0.213 = boilerplate 수준. **이름이 겹치는 5개**(blindspot·handoff·ledger·
  premise·thirdway)도 0.077~0.143 이라 장치가 다르다 — 이름 충돌은 반려 사유가 아니다.
- 🔑 **출처 있는 「제가」와 없는 「제가」가 한 회차에 섞였다.** S2 의 「제가 새로 만든 간호
  액션」은 발행 글 원문 그대로라 정당하고 S18 은 아니다. **1인칭 문장마다 원문에 그
  1인칭이 있는지 따로 본다.**

### `lf02` 2차 (2026-08-09)

| 회차 | 회 | 판정 | 사유 요약 |
|---|---|---|---|
| `lf02` | 2 | 🔴 반려 (2026-08-09) | 반려 4 · 정정 6. 1차 11건 중 **9건 해소**. 무너진 것은 ①**2차 개정이 새 기계 실패를 만들었다** ②**R1 의 「21개 전수 스틸 재확인」이 수행되지 않아** 같은 겹침이 다섯 자리 더 남았다 |

**반려 4건** — R-1 `check.mjs` 🔴 `아래 가장자리 잘림 S5 1673px`(C6 로 넣은 ledger 판독 띠가
캔버스 밖으로 · 1차엔 OK 였다) / R-2a S19 `costfloor` 의 `MIN COST 8`+`GATE`+막대값 `8` 이
한 자리에 포개져 **페이오프 샷이 판독 불가** / R-2b S2 `MIN COST 1` 이 막대 획 관통 /
R-2c S13 `humanonly` 대기 점이 카드 라벨 위를 21.8초 내내 지남 /
R-3 S1 「저마다 계획을 말해요」가 말풍선 다섯 중 둘과 안 맞음.

**이 회차에서 배운 것 (2차)**
- 🔴 **모션 면적을 키우라는 처방은 가장자리 잘림을 낳는다.** C6 대로 3px 선 → 46~52px 면으로
  바꾼 11개 kind 중 **세로로 움직이는 하나(`ledger`)가 캔버스 바닥을 넘었다.**
  `by = lerp(y0-10, h-22, k)` + `height = rowH-6` → `k=1` 에서 7.6px 초과.
  **처방을 낼 때 「띠 끝이 상자 안에 남는가」를 같이 적어라.** x 축 이동은 안전(`decay`),
  y 축 이동만 위험하다.
- 🔴 **「N종을 고쳐라 + 전수 재확인해라」는 후반부가 조용히 빠진다.** 작성팀 환경에 크로미움이
  없으면 전수 스틸이 물리적으로 불가능하다 — 그러면 **1차 검수가 직접 21개를 다 떠야 했다.**
  9개만 보고 「나머지는 작성팀이」로 넘긴 것이 이 왕복의 원인이다. 다음부터는
  **캔버스 kind 전부를 1차에서 뜬다**(31장 = 렌더 3배치 · 약 2분).
- 🔴 **`costfloor` 를 1차에서 「겹침 0」이라고 적은 것은 오판이었다.** 20s 한 장만 보고
  판정했는데 `MIN COST` 라벨은 `cue(4)` 뒤에 뜬다. **한 kind 당 최소 3시점(초·중·말)** 을
  떠야 cue 로 나중에 나타나는 라벨을 본다. `spec.phase` 가 있으면 **phase 마다** 따로.
- 🔑 **자기보고 표의 「전 → 후」를 코드로 검산하라.** `writer.md` 가 「r5 점 → 48px 띠로
  바꿨다」고 적었지만 코드는 **점을 남기고 띠를 더했다**(`humanonly.js:99~106`). 그 남은 점이
  반려 사유가 됐다. [[review-method-self-report-is-not-evidence]] 의 그림 버전.
- 🔑 **드리프트하는 수치는 「기준 시각 못 박기」로 해결되지만 하루를 못 넘긴다.**
  15:30 기준 게이트 381 이 같은 날 16:00 에 **386** 이 됐다(커밋 1001→1008 · 19,859→20,365).
  `blurb` 의 「2026-08-09 기준」은 방어가 안 된다 → **렌더 직전 재실측**을 재검 목록에 고정.
- ✅ **C6 자체는 크게 성공했다** — 정적 경고 17건 → 3건, 남은 셋은 전부 클립.
  창 초과분과 대조하면 S4 는 오차 0.16초로 설명되고(TTS 뒤 사라짐), **S17 만 샷 전체(9.2초)가
  정지**라 클립 자체의 문제다.

### `lf02` 3차 (2026-08-09) — ✅ 통과 · 반려 2회로 종료

| 회차 | 회 | 판정 | 사유 요약 |
|---|---|---|---|
| `lf02` | 3 | ✅ **통과** (2026-08-09) | 2차 반려 4 + 정정 3(N4~N6) **일곱 자리 전부 해소**, 새 스틸 30장으로 실증. 기계 실패 `아래 가장자리 잘림` 소멸, 새 red 0. 🔴 렌더 전 차단 2건(C13·C14)을 인계에 못 박고 통과 |

- ✅ **회귀 위험이 실현되지 않았다.** 작성팀이 스스로 밝힌 「띠 이동 −16% → 정적 재발」은
  일어나지 않았다(check 정적 목록에 S5 없음). 🔑 **띠가 `t % PR` 순수 시간 함수면 TTS 뒤에도
  프레임 간 변화량이 같다** — 이 경우 정적 판정은 TTS 전에 확정해도 된다.
- 🔴 **C14 = 검수 셋이 다 놓친 결함 유형.** 「글자 위에 글자」만 찾다가 **가로 막대·가로선이
  값 라벨의 위쪽 획을 먹는 자리**를 못 봤다. `costfloor` p1 에서 CookMeal 막대의 `6` 이
  게이트 막대(`yOf(8)`, lw 6)에 위쪽 2.4px 을 먹혀 **`o` 로 읽혔다.**
  → [[review-blind-spots]] 에 이식. 산수 한 줄로 훑을 수 있다:
  `라벨 baseline − 글자높이 < 선 y + lw/2` 이면 잘린다.
  🔑 `stills-2nd` 를 복사해 두고 **픽셀 합을 비교**해 「이번 개정이 만든 회귀가 아님」을
  증명했다 — 3차 이후 검수는 직전 스틸을 항상 보존할 것.
- 🔴 **C13 = 「기준 시각을 못 박았다」가 정확성을 보장하지 않는다.** 화면이 「16:20 기준」이라
  선언했는데 `SO ASSETS 110 · SPECS 31` 은 그 시각에 **116 · 32** 였다(에셋은 16:06 부터,
  명세서는 15:33 부터). 작성팀 §N 표가 이 두 칸만 **「변동 없음」이라 적고 재보지 않았다.**
  → **기준 시각을 선언한 수치는 `git ls-tree -r <그 시각 커밋>` 로 전수 재측정하라.**
  드리프트(나중에 낡음)와 **미측정(처음부터 틀림)**은 다르다.
- 🔑 **등급을 사후에 올리지 않았다.** C13·C14 는 2차가 N4~N6 에 준 것과 같은 등급(정정)이라
  반려로 올리지 않고 **「렌더 전 차단」**으로 인계했다. 에스컬레이션 조건은
  「**같은 자리**가 세 번 남으면」인데 지목한 자리는 전부 닫혔으므로 성립하지 않는다.
- 🔑 **셸 없는 작성 환경의 대처가 유효했다** — 스틸에서 캔버스 스케일을 역산해
  (1 CSS px = 2.755 화면 px) 새 좌표를 산술로 미리 푼 `writer.md` §L 의 여덟 자리를
  실스틸로 전부 덮어썼는데 **전부 계산대로였다.** 다음 회차가 그대로 써도 된다.
