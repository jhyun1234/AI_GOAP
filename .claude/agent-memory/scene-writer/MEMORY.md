# Memory Index

## 프로젝트
- [회차 사이 연결 장치 · 엔딩 비트 소유권](project_series_handoffs.md) — "지워집니다" 예고는 ep01s 것, ByReason 로그는 ep00s/ep01s 가 강조 칸을 나눠 씀, Claude 마크는 ep01s S8
- [효과음 delayMs 는 draw 를 풀어서 낸다 · gain 상한은 kind+옵션이 정한다](project_sfx_timing.md) — 실측 나오면 재계산 필수(같은 회차 안에서도 오차 3배). 🟢 **`since(i)` 위에 세우면 delayMs 도 페이오프 완성도 TTS 와 무관해진다**(ep08s-3 지각이 원리적으로 안 남) · 🔴 돌려막기는 형제 편의 kind 「쌍」으로도 난다 · 🔴 `why` 에 「>0.5 임계」 쓰지 마라
- [클립 창 여유는 SHOT_TAIL 0.35 를 더한 샷 길이로 잰다](project_clip_window_margin.md) — 발화 합으로 재면 매번 0.35초 낙관. 7초 클립 자막 예산 = 6.45초. 롱폼 산정 = 실측의 82%(두 편 재현)
- [25초대 편 예산 산수](project_video_25s_budget.md) — 🔴 **총 = 자막합 + 0.35×샷수** · 4단 상한 **38초/본문 18~22** · 🟢 **36~37초 + warn 1개가 정상 착지**(권장대역 30~35 는 본문 18~22 와 충돌) · 겨냥은 **say 원문 자수 × 136.5ms** · 초안 자수는 구성이 같은 직전 편 실측에서
- [제목의 문장 모양은 반복 금지 — 숫자 훅은 반복 가능](project_title_shape_reuse.md) — 「N명이 똑같이 움직였다」를 두 번 썼다. 갈라낼 재료는 ①방아쇠 ②잃은 것. 이미 두 번 쓴 수치는 화면 라벨로 강등. 🔴 **제목 이행 게이트는 어미를 턴 뒤 1글자를 버린다 — 「죽는」은 사라진다**(hit≥1 을 이행의 증거로 삼지 마라)
- [분할 회차 규약 — id·outro.source·형제 라벨·blurb·앞 편 약속](project_split_episode_conventions.md) — 형제 label 은 그 편 youtube.title 에서 꼬리만 뗀 것. 🔴 **`outro.siblings` 와 한국어 blurb 형제 줄은 둘 다 필요**(ep12s-1 반려) · 🔴 **「낡은 서술」이 ep13s-3 지적의 절반** — 값 고친 라운드에 노트·kind 주석도 같이 고쳐라(파일이 자기 필드를 부정한다) · 앞 편 `outro.next` 인계는 게이트가 못 본다
- [latch 는 dur 로 안 갈린다 — seed 를 줘라](project_sfx_latch_seed.md) — 몸통이 340/680Hz 고정. 🔴 **seed 를 주고도 값이 겹치면 같은 함정**(ep13s-1 ↔ lf02 상관 0.9963). 이름으로 훑지 말고 `shot.sfx` 를 파싱해 세라 — 롱폼은 한 편에 latch 가 넷이다. kind 가 다르면 무해
- [비개발자 규격(ADR-V25-13~15) 실무 처리](project_plain_words_gate.md) — 낱말 전수 배정표를 notes 에 표로 · **`비유`는 「없음」이 정상 답**(사유 필수) · ①의 재료는 원문 괄호·뒷문장·**명사→동사** · 라벨 가리기 자문은 `reads` 로
- [표를 제거한 브리프는 오염이 아니라 손실로 실패한다](project_table_stripped_brief.md) — 빈 줄 앞뒤를 읽어 뭐가 사라졌는지 추정. 부분만 남으면 남은 것만 그리고, 전무하면 통째로 버리고 excluded 에 사유
- [비개발자 규격 ADR-V25-13~15 실행법](project_plain_language_regime.md) — 🔑 **①풀어쓰기의 재료는 「사건이 적힌 문단」에 있다**(구현 문단을 풀어쓰려 애쓰지 마라) · 「쉽다」≠「사건이다」 · 라벨 없이 읽히게 하는 가장 싼 길은 **화면에 수를 0개 올리는 것** · 원문 자신의 낱말을 그린 것은 비유가 아니다(그래도 밝혀 둬라)

## 피드백
- [「~어요」 연속은 초고에서 세라 — 3연속 금지가 실제로 집행된다](feedback_ending_variety.md) — ep13s-3 이 7연속(승인 최댓값 5)으로 조건부 승인. 고칠 줄은 **sfx 없는 샷**에서 · `~습니다` 말고 `~거든요`/`~죠` · **증량 방향** · 시제 깨지 말 것
- [승인된 대본은 얼려 둔다 — 사유가 무효가 아닌 한](feedback_approved_script_is_frozen.md) — 그림 반려면 자막은 안 건드린다. 단 반려 사유가 무효면(ep02s "60초 상한") 되살리는 게 일이다. `excluded` 는 늘 다시 본다
- [기본 도형을 먼저 정한다](feedback_base_shape_first.md) — 샷별로 그림을 고르면 남의 회차 어휘가 섞인다. 축 한 문장 → 열 개 파생. 회차 안 되받기는 뒤집을 때만 허용
- [draw 가 순수해도 래스터화는 안 순수할 수 있다](feedback_canvas_determinism.md) — 긴 가로 `setLineDash` 가 30fps 3패스에서 갈렸다(알파 21↔19). `fillRect` 조각으로 그리고 위상은 양수 정규화. **`--fps 30` 을 따로 돌려라**(기본은 5)
- [화면 문자열은 원문 grep 뒤에만 통과 — 축이 셋이다](feedback_string_fact_check.md) — 숫자 · 문자열 · 🔴 **분류** · 🔴 **인용 부호가 붙으면 근거가 아니라 주장**(지어낸 인용 1건 반려 — 원인은 「그림 먼저, 근거 나중」 순서)(그 낱말이 그 라벨에 속한다는 근거는 원문이 아니라 리포 에셋/ADR 에 있다). 근거 없으면 행을 통째로 가린다
- [훅은 캔버스에 안 그린다 — 아웃트로 카드가 덮는다](feedback_hook_card_not_canvas.md) — spec.hookCue 폐기. 훅으로 정적을 막던 설계가 통째로 무효(안 보이는 그림은 정적도 못 막는다). 블록만 지우고 fade·주석·reads 를 남기면 그 자리가 더 나빠진다
- [정적 판정 = 면적 × **프레임당 이동** · 눈금은 **30fps**](feedback_static_segment_is_area.md) — 🔴 「면적」만 믿었다가 ep14s-4 반려(5fps 0.6s → 30fps 5.2s). **`--fps 30` 을 따로 돌려라** · 프레임당 1px 목표 · 상시 숨이 가장 싼 보충. 겹침은 범위 상수로(둘이 상충하니 함께 본다)
- [영어 자막은 앞줄과 붙여 읽는다](feedback_en_subtitle_referent.md) — `That spread` 가 앞줄이 명명한 것을 가리켜 뜻이 뒤집혔다. 회상 관형형(~던)은 영어로 자동으로 안 넘어간다. 긴 blurb 는 맞히고 두 줄 자막에서 틀린다
- [흰색과 강조색을 같은 픽셀에 겹치지 마라 — 양방향 다 뚫린다](feedback_translucent_accent_zorder.md) — 게이트는 혼합을 hue 150~152 로 읽어 **어느 순서든** 통과시킨다. 🔴 **끝에서 끝까지 긋는 배경선**(바닥·축)이 가운데 강조색 글립을 관통한다 — 시작 x 를 잘라라
- [캔버스 글자는 폭을 재서 안쪽에 가둔다](feedback_canvas_edge_text.md) — 폭 제한은 박스가 아니라 글자에. 아래 가장자리는 **한 프레임만 스쳐도 red**이고 보고값은 거리가 아니라 **픽셀 개수**(÷dpr = 범인의 CSS 폭). 🔴 **움직이는 요소는 「가장 밖으로 나가는 순간」으로** · 🔴 **밖에서 슬라이드하는 등장/퇴장 금지 — 「폭이 자란다 / 제자리에서 줄어든다」로** · 🔴 겹침은 좌표가 아니라 부등식으로(방사형 판은 **각도로 통로를 비운다**) · wide 는 엔진 HUD 가 캔버스 y≤21 을 덮는다
- [정보를 지는 글자의 알파를 cue 산수로 검산하라](feedback_label_alpha_arithmetic.md) — 🔴 **ADR-V25-15 는 양방향**(가려도 읽히나 + **안 가린 라벨이 실제로 뜨나**). ep14s-2 반려: 최대 알파 0.145 · 0.26초에 소멸. 샷 첫 자막은 ts 0 에서 이미 `k = lead/W` · **글자 먼저 상자 나중**(둘 다 늦춰야 한다)
- [화면 요약은 수·배분·종류를 다 셀 수 없으면 말하지 않는다](feedback_frame_summary_scope.md) — lf02 S1 이 세 번 반려됐다. 「다섯 명」(수) → 「저마다」(배분) → 「계획」(종류). 🔴 **캔버스 샷도 같다 — `reads` 는 의도가 아니라 결과를 적는 칸**(ep14s-4: 「고리 바깥」이라 썼는데 안쪽이었다. 원인 = 두 뜻에 같은 상수)
