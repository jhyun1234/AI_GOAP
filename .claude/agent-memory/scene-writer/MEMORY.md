# Memory Index

## 프로젝트
- [회차 사이 연결 장치 · 엔딩 비트 소유권](project_series_handoffs.md) — "지워집니다" 예고는 ep01s 것, ByReason 로그는 ep00s/ep01s 가 강조 칸을 나눠 씀 · 🔴 **「브리프에 있었다」는 근거가 아니다 — 브리프 §4 가 앞 글의 금지된 예고 문단을 실어 나른다**(함정 8 네 번째 변종). 예고는 **다음 회차 원문 파일을 직접 열어** 문제 제기 소제목으로
- [색 규약 뒤집기 허용 4조건 — ep15s-5 를 선례로 인용하면 반려](project_color_convention_flip.md) — 통과의 몸통은 **4번(라벨과 색을 둘 다 지워도 사건이 읽힌다)**이지 「회차 안 일관」·「축 되돌림」이 아니다. 자기 편에서 4번을 스스로 증명해야 한다
- [효과음 delayMs 는 draw 를 풀어서 낸다 · gain 상한은 kind+옵션이 정한다](project_sfx_timing.md) — 실측 나오면 재계산 필수(같은 회차 안에서도 오차 3배). 🟢 **`since(i)` 위에 세우면 delayMs 도 페이오프 완성도 TTS 와 무관해진다**(ep08s-3 지각이 원리적으로 안 남) · 🔴 **단 「줄 안의 낱말 위치」는 면역이 아니다 — 소리를 옮기지 말고 그 낱말이 줄 머리에 오게 줄을 잘라라** · 🔴 돌려막기는 형제 편의 kind 「쌍」으로도 난다 · 🔴 `why` 에 「>0.5 임계」 쓰지 마라
- [클립 창 여유는 SHOT_TAIL 0.35 를 더한 샷 길이로 잰다](project_clip_window_margin.md) — 발화 합으로 재면 매번 0.35초 낙관. 7초 클립 자막 예산 = 6.45초. 롱폼 산정 = 실측의 82%(두 편 재현)
- [25초대 편 예산 산수](project_video_25s_budget.md) — 🔴 **총 = 자막합 + 0.35×샷수** · 4단 상한 **38초/본문 18~22** · 🟢 **36~37초 + warn 1개가 정상 착지**(권장대역 30~35 는 본문 18~22 와 충돌) · 겨냥은 **say 원문 자수 × 136.5ms** · 초안 자수는 구성이 같은 직전 편 실측에서
- [제목의 문장 모양은 반복 금지 — 숫자 훅은 반복 가능](project_title_shape_reuse.md) — 「N명이 똑같이 움직였다」를 두 번 썼다. 갈라낼 재료는 ①방아쇠 ②잃은 것. 이미 두 번 쓴 수치는 화면 라벨로 강등. 🔴 **제목 이행 게이트는 어미를 턴 뒤 1글자를 버린다 — 「죽는」은 사라진다**(hit≥1 을 이행의 증거로 삼지 마라) · 🔑 **무대·인물이 앞 회차와 같으면 「피해자」를 바꿔 갈라라**(문장만 고치면 화면이 그대로라 겹친다)
- [분할 회차 규약 — id·outro.source·형제 라벨·blurb·앞 편 약속](project_split_episode_conventions.md) — 형제 label 은 그 편 youtube.title 에서 꼬리만 뗀 것. 🔴 **`outro.siblings` 와 한국어 blurb 형제 줄은 둘 다 필요**(ep12s-1 반려) · 🔴🔴 **blurb 에 사건 서술만 있고 「N-M편」이 없으면 통과가 아니다 — 자기 점검에서 「형제 줄 있음」으로 세진다**(ep16s-1 2차 반려. 잣대 = 「그 문단만 읽고 검색창에 칠 것이 나오는가」. **셋 다** — siblings·ko blurb·**en blurb** — 선례 형은 `… is part N`, `#16-N` 형은 발행분 1편뿐이라 쓰지 마라) · 🔴 **「낡은 서술」이 ep13s-3·ep15s-3 지적의 절반** — 값 고친 라운드에 노트·kind 주석도 같이 고쳐라(파일이 자기 필드를 부정한다. `outro.siblings`·`notes.길이`·`schedule.json` 서술이 단골) · 🔴 **실측이 온 라운드엔 「실측이 없다」류를 전부 grep**(ep15s-1: 한 칸만 고치고 같은 형태를 다른 칸에 남겨 2차에 또 지적) · 🔴 **판정서 본문을 직접 읽어라 — 요약에 없는 값이 있다**(어절 실측 요약 5 vs 판정서 9) · 🔴 **「이 회차의 발명」이라고 쓰기 전에 갈라 세울 그 편의 `kinds/` 를 열어라**(ep15s-3 ↔ ep14s-3 onepath) · 앞 편 `outro.next` 인계는 게이트가 못 본다 · 🔴 **아웃트로 소개 줄은 `Claude Code`(로마자) — 17/17 실적이 근거이고 `text` 만 고치면 게이트 불변**
- [kind 마다 손잡이가 다르다 — latch 는 seed, riser·drop 은 from/to](project_sfx_latch_seed.md) — 몸통이 340/680Hz 고정. 🔴🔴 **`dur` 을 전수 최댓값 밖으로 늘리면 오히려 더 붙는다**(ep15s-2 3차: 0.255 → |r| 0.859 · 0.12 → 0.796. 잡음 kind 는 「범위 판정」과 「파형 판정」이 반대다 — 판정법을 kind 마다 따로) · 🔴🔴 **`dur` 은 잣대가 둘이고 서로 반대 — 짧을수록 상관↓(좋음)·피크↑(나쁨)**(0.105 면 gain 0.068 이 상한의 98%) · 🔴 **통제군 대역은 `seed` 명시분(23건)으로만 성립** — 기본 seed 넣으면 max 1.000 · 🔴 **남이 준 수도 「어느 경로로 잰 값인가」를 받아 적어라**(피크 0.0228 = `gain` 두 번 곱한 값, 실제 0.4299) · 🔴 **seed 를 주고도 값이 겹치면 같은 함정**(ep13s-1 ↔ lf02 상관 0.9963). 이름으로 훑지 말고 `shot.sfx` 를 파싱해 세라. 🟢 **그 kind 를 안 쓰는 것도 가르기다** · 🔴 **단 「X 를 피하라」가 형제 전원을 같은 대안으로 몬다**(ep15s 넷이 다 drop+riser) — 형제 폴더는 **이미 있다고 보고 열어 세라** · 🔴 `drop`/`riser` 는 `freq` 가 아니라 `from`/`to` · 🔴 **셸이 없으면 「리포 어디에도 없다」를 쓰지 마라**(ep15s-4: 손 grep 이 ep10s-2 thud 0.29·230 을 빠뜨렸다. 충돌 축은 파라미터 + **`why` 의 뜻** — 한 축만 고치면 나머지가 겹친다) · 🔴 **kind 마다 따로 grep 하고 「N건」과 목록 항목 수를 대조**(ep15s-2: 필드 이름 몰아 훑기가 `ep08s-1` 한 줄을 삼켜 반려) · 🟢 **`riser`/`drop` 은 `dur` 이 겹쳐도 무해**(ep15s-5 ↔ ep15s-4 `dur 0.78` 동일인데 **r = 0.000** — 음색은 `from`/`to` 가 진다) · 🔴 **형제 census 는 한 라운드 만에 낡는다**(1차 검수가 세어 준 것도 다음 라운드에 틀렸다) — **근거를 파라미터 표가 아니라 파형 상관에 걸어 두면 표가 낡아도 산다**
- [비개발자 규격(ADR-V25-13~15) 실무 처리](project_plain_words_gate.md) — 낱말 전수 배정표를 notes 에 표로 · **`비유`는 「없음」이 정상 답**(사유 필수) · ①의 재료는 원문 괄호·뒷문장·**명사→동사** · 라벨 가리기 자문은 `reads` 로
- [표를 제거한 브리프는 오염이 아니라 손실로 실패한다](project_table_stripped_brief.md) — 빈 줄 앞뒤를 읽어 뭐가 사라졌는지 추정. 부분만 남으면 남은 것만 그리고, 전무하면 통째로 버리고 excluded 에 사유
- [비개발자 규격 ADR-V25-13~15 실행법](project_plain_language_regime.md) — 🔑 **①풀어쓰기의 재료는 「사건이 적힌 문단」에 있다**(구현 문단을 풀어쓰려 애쓰지 마라) · 🔑 **①의 세 번째 길 = 낱말을 남기되 「뜻을 안 지게」 만든다**(타일 — 쉬운 말로 바꿔 부르지 마라) · 「쉽다」≠「사건이다」 · 라벨 없이 읽히게 하는 가장 싼 길은 **화면에 수를 0개 올리는 것** · 원문 자신의 낱말을 그린 것은 비유가 아니다(그래도 밝혀 둬라)

## 피드백
- [어미 연속은 초고에서 세라 — 🔴 **단독 반려로 집행됐다**(ep16s-3 R1)](feedback_ending_variety.md) — 잣대는 **「끝 두 글자 동일 연속 ≤2」·본문 한정**(「-요」류 8연속이 통과해 온 이유) · 고칠 줄은 양 끝이 아니라 **한가운데** · sfx 없는 샷에서 · `~거든요`/`~죠` · 증량 방향
- [승인된 대본은 얼려 둔다 — 사유가 무효가 아닌 한](feedback_approved_script_is_frozen.md) — 그림 반려면 자막은 안 건드린다. 단 반려 사유가 무효면(ep02s "60초 상한") 되살리는 게 일이다. `excluded` 는 늘 다시 본다
- [기본 도형을 먼저 정한다](feedback_base_shape_first.md) — 샷별로 그림을 고르면 남의 회차 어휘가 섞인다. 축 한 문장 → 열 개 파생. 회차 안 되받기는 뒤집을 때만 허용
- [draw 가 순수해도 래스터화는 안 순수할 수 있다](feedback_canvas_determinism.md) — 긴 가로 `setLineDash` 가 30fps 3패스에서 갈렸다(알파 21↔19). `fillRect` 조각으로 그리고 위상은 양수 정규화. **`--fps 30` 을 따로 돌려라**(기본은 5)
- [화면 문자열은 원문 grep 뒤에만 통과 — 축이 셋이다](feedback_string_fact_check.md) — 숫자 · 문자열 · 🔴 **분류** · 🔴 **인용 부호가 붙으면 근거가 아니라 주장**(지어낸 인용 1건 반려 — 원인은 「그림 먼저, 근거 나중」 순서). 근거 없으면 행을 통째로 가린다 · 🔴🔴 **전수에 `shot.chapter`(HUD 챕터 칩)·`hud.*`·`outro.*`·`scene.hook` 도 든다 — `spec` 만 세면 전수가 아니다**(ep15s-5 → ep15s-3 → ep15s-2 → **ep15s-1 로 네 번**. 원인 = **검수가 세어 준 수를 「전수」로 물려받았다**. 총계 한 줄 말고 **「층 × 칸 수」 표**로 · 🔴 **층은 기억하지 말고 `engine.js` 의 `textContent`/`innerHTML` 대입을 grep 해서 뽑아라** · `hud.aiHook` 은 상단이 아니라 **아웃트로 카드**다 · 🔴🔴 **ep15s-1 은 그 탓에 금칙 낱말을 놓쳤다** — `outro.source` 「M12 성격 축」의 **「축」 1회**. 밀스톤 이름 슬롯이라 통과지만 **안 적으면 「등장 0회」가 거짓말**이고, 같은 목록에 **언어·사실 대조·낱말 배정 셋이 얹혀 있다**) · 🔴 **한 파일이 같은 도형에 치수를 두 번 정의하면 안 된다**(overtake 원 반지름 11.5 ↔ 13.5)
- [훅은 캔버스에 안 그린다 — 아웃트로 카드가 덮는다](feedback_hook_card_not_canvas.md) — spec.hookCue 폐기. 훅으로 정적을 막던 설계가 통째로 무효(안 보이는 그림은 정적도 못 막는다). 블록만 지우고 fade·주석·reads 를 남기면 그 자리가 더 나빠진다
- [정적 판정 = 면적 × **프레임당 이동** · 눈금은 **30fps**](feedback_static_segment_is_area.md) — 🔴 「면적」만 믿었다가 ep14s-4 반려(5fps 0.6s → 30fps 5.2s). **`--fps 30` 을 따로 돌려라** · 프레임당 1px 목표 · 상시 숨이 가장 싼 보충. 🔴 **상시 운동을 사건 진행도에 곱하면 「시작 전 구간」에서 0 이다 — 정적은 샷의 꼬리가 아니라 머리에 난다**(ep15s-5) · `track`(흰 12%)으로 도는 것은 사실상 0 · **주석이 광고한 운동 항이 코드에 없을 수 있다** · 🔴🔴 **뜻을 지는 국면 전환을 `t` 루프로 돌리면 「말이 A 인데 화면이 B」가 구조적으로 난다**(ep15s-1 반려 — 반복 등장을 「읽는 창」의 대체물로 쓰지 마라) · 🔑 **「사건 없는 구간의 길이」 ≠ 정적 판정값**(정본은 실측)
- [영어 자막은 앞줄과 붙여 읽는다](feedback_en_subtitle_referent.md) — `That spread` 가 앞줄이 명명한 것을 가리켜 뜻이 뒤집혔다. 회상 관형형(~던)은 영어로 자동으로 안 넘어간다. 긴 blurb 는 맞히고 두 줄 자막에서 틀린다
- [흰색과 강조색을 같은 픽셀에 겹치지 마라 — 양방향 다 뚫린다](feedback_translucent_accent_zorder.md) — 게이트는 혼합을 hue 150~152 로 읽어 **어느 순서든** 통과시킨다. 🔴 **끝에서 끝까지 긋는 배경선**(바닥·축)이 가운데 강조색 글립을 관통한다 — 시작 x 를 잘라라 · 🔴 **충돌은 시간축에서도 난다 — 강조색이 그 선의 y 를 「지나가면」 정지 좌표로는 안 보인다** · 🔴 **여유는 「경로 ± (획 반폭 + shadowBlur)」로 재라**(글립만 재서 3번 틀림) · 🔴 **배열(격자·스택)이면 항이 넷 — `간격 − 글로우 − 획반폭×2 − 숨×2`**(ep15s-1 반려: 9px 이라 적은 게 실제 4.4. 상시 숨은 양쪽에서 두 번 먹는다. 얇으면 **글로우를 줄이는 게 가장 싼 손잡이**) · 자라는 요소는 **가장 작은 배율**에서 겹친다
- [캔버스 글자는 폭을 재서 안쪽에 가둔다](feedback_canvas_edge_text.md) — 🔴🔴 **캔버스 밖에도 충돌이 있다 — `hud.title` 2줄이면 진행 바가 아웃트로 훅을 관통한다**(28자 이하 1줄 · 게이트는 `.vis` 만 봐서 0건 · `--still 35.9` 로만 판정 · `scene.hook` 과 문안 겹치지 마라) · 폭 제한은 박스가 아니라 글자에. 아래 가장자리는 **한 프레임만 스쳐도 red**이고 보고값은 거리가 아니라 **픽셀 개수**(÷dpr = 범인의 CSS 폭). 🔴 **움직이는 요소는 「가장 밖으로 나가는 순간」으로** · 🔴 **밖에서 슬라이드하는 등장/퇴장 금지 — 「폭이 자란다 / 제자리에서 줄어든다」로** · 🔴 겹침은 좌표가 아니라 부등식으로(방사형 판은 **각도로 통로를 비운다**) · wide 는 엔진 HUD 가 캔버스 y≤21 을 덮는다
- [정보를 지는 글자의 알파를 cue 산수로 검산하라](feedback_label_alpha_arithmetic.md) — 🔴 **ADR-V25-15 는 양방향**(가려도 읽히나 + **안 가린 라벨이 실제로 뜨나**). ep14s-2 반려: 최대 알파 0.145 · 0.26초에 소멸. 샷 첫 자막은 ts 0 에서 이미 `k = lead/W` · **글자 먼저 상자 나중** · 🔴 **한 슬롯에서 값을 갈아 끼우지 마라** · 🔴 **잣대는 자막 줄이 아니라 「그 낱말의 발화 개시」 — 쉼표 무음을 재라**(ep15s-3 반려) · 🔴🔴 **어절 시각을 `dur` 로 균등 배분하지 마라 — `dur` 에 후행 무음 0.56초가 들어 최대 0.72초 밀린다**(ep15s-1 이 형제 넷에 전파. 부호도 일정치 않으니 **보정 계수 금지, 재라**) · 🔴🔴 **정본 기준점은 「낱말」이 아니라 「줄」 — 자막이 두 줄을 한꺼번에 띄우므로 줄 안에서는 스포일러가 없다**(ep15s-1: 낱말을 정본으로 착각해 「여유 0.02초」라는 **실재하지 않는 공포**로 한 라운드 소모). 삼분(진입=자막시작 / 리빌=줄·낱말 / **페이오프=그림**)은 살되 **전수 배정표를 표로** — ep15s-1 은 「①④⑥」 오타 한 글자로 리빌이 진입 통에 들어가 **측정 하나가 통째로 사라졌다** · 🔴 **어절 시각은 `dur` 로 배분하면 0.15~0.36초 밀린다 — ①말 끝 먼저 ②간극 ③자음 랜드마크** · 🔴 **루프에 물린 라벨은 「주기 단축」으로만 창을 덮는다**(위상 밀기·일회성은 각각 여는 상태·정적 꼬리를 깬다) · 🔴 **계단식 램프는 배율이 `N` 이 아니라 `N + 램프폭`**(ep15s-4 반려: 마지막 한 단이 32%로 얼어 「하나만 남는다」가 거짓이 됐다. 양 끝을 둘 다 대입해라) · 🔴🔴 **어절 시각을 `dur` 위에 배분하지 마라 — 후행 무음이 0.42~0.59초**(ep15s-2: 최대 0.45초 어긋나 「0.22초 먼저」가 「동시」로. 포락선으로 자르고 35ms 간극으로 교차검증)
- [화면 요약은 수·배분·종류를 다 셀 수 없으면 말하지 않는다](feedback_frame_summary_scope.md) — lf02 S1 이 세 번 반려됐다. 「다섯 명」(수) → 「저마다」(배분) → 「계획」(종류). 🔴 **캔버스 샷도 같다 — `reads` 는 의도가 아니라 결과를 적는 칸**(ep14s-4: 「고리 바깥」이라 썼는데 안쪽이었다) · 🔴 **`notes` 의 전칭(「0개」·「전부」)은 범위를 같은 문장에 박아라 — 「화면에 수 0개」는 아웃트로 카드·HUD 때문에 거의 항상 거짓**(같은 문장이 네 자리에 복사돼 있었다) · 🔴🔴 **형제 판정서의 이행 조건은 옆 편으로 자동으로 안 흐른다 — 형제 `verdict.md` 를 직접 읽어라** · 🔴 **같은 도형을 두 샷이 나눠 쓰면 라벨의 수를 샷마다 다시 세라**(ep15s-1: 30칸 격자에 「180칸」. 고칠 것은 라벨이 아니라 **실물** — 칸마다 여섯을 넣었다. 게이트도 수치 슬롯 대조도 못 본다)
