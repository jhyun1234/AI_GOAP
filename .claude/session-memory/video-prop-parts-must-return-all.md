---
name: video-prop-parts-must-return-all
description: 소품 함수가 조각 하나만 반환하면 켜고 끌 때 나머지가 화면에 남는다 — 두 번 밟았다
metadata: 
  node_type: memory
  type: feedback
  modified: 2026-08-13T12:40:20.456Z
  originSessionId: a52102f3-8c22-4d95-b04e-ca6439e4337c
---

`village.py` 의 소품 함수는 **여러 오브젝트를 만들고 대표 하나만 반환**하는 버릇이 있었다.
그러면 부르는 쪽이 `obj.hide_render` 로 켜고 끌 때 **나머지가 그대로 남는다.**
블렌더에서 부모의 `hide_render` 는 자식에게 **안 물린다** — 자식으로 묶어도 마찬가지다.

**같은 버그를 두 번 밟았다. 둘 다 사람 눈이 프레임에서 잡았다 — 검사는 못 잡는다.**

| 언제 | 무엇이 화면에 남았나 |
|---|---|
| ep15s-3 본문1 | `house()` 가 **지붕을 반환 안 했다.** 아직 안 지은 집의 **지붕만 허공에 먼저** 떠 있었다 |
| ep15s-4 본문2 | `_fire_pit()` 이 **돌·장작을 반환 안 했다.** 아직 안 지은 모닥불 자리의 **돌 테두리 아홉**이 처음부터 다 보였다. ⚠️ ep15s-3 아웃트로도 같은 버그였다(뒤늦게 발견) |

**Why:** 「자라 오른다 · 하나씩 는다」는 이 파이프라인이 가장 자주 쓰는 연출인데, 소품이
조각 여럿이라는 것과 반환값이 하나라는 것이 어긋나 있으면 **연출을 걸 때마다 터진다.**

**How to apply:**
- 소품을 켜고 끄거나 키울 거면 **그 소품 전용 show/grow 를 `village.py` 에 만든다.**
  이미 있는 것: `house_show` · `house_grow` · `house_parts` · `pit_show`.
  🔴 부르는 쪽에서 `hide_render` 를 직접 만지지 마라 — 조각이 늘면 그날 또 터진다.
- 새 소품을 만들면 **조각 목록을 반환하거나 show 함수를 같이 만든다.** 대표 하나만
  돌려주는 것이 기본값이면 안 된다.
- 확인은 **프레임을 보고** 한다. 「아직 안 지었을 때 화면에 뭐가 있나」를 눈으로 본다.

관련: [[video-3d-figure-is-blender-model]] · [[video-line-by-line-contract]]
