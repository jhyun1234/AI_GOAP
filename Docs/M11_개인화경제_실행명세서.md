# M11 실행명세서 — 개인화 경제: 각자 먹고살되, 마을은 함께 짓는다

> 작성 2026-07-23. 입력 = M11 브레인스토밍 확정 결정 16항 (memory `project_m11_personal_economy.md`)
> + 명세 착수일 확정 보완 4건 (사용자 승인 2026-07-23):
> ① 슬롯 = 몸 소지만, 집 저장은 별도 (합산은 MyFoodDaysLeft 파생만)
> ② 보상 = 잔고 정산 — 부족하면 연기 (에스크로 아님, "가난한 주민의 빚" 서사)
> ③ 집 부탁 = 보상 지급 여력 트리거 + 피격 경험 트리거 이원, 거절 비영구 (재무장)
> ④ 집 보상액 ≤ 몸 소지 상한 제약 (영구 노숙 데드락 차단)
>
> **전제 확인 (코드 실사 결과, 2026-07-23)**:
> - 부탁은 goal이 아니라 **RequestService 주기 스캔**이다 (의뢰인 조건 = 에셋 SlotCondition,
>   스냅샷 대역 판정). "집 동기"의 실체 = 부탁 에셋의 RequesterConditions + 저축 goal 1개.
> - 의뢰인 거절 쿨다운은 이미 비영구다 (`RequestCooldownSec` 90초) — "영원히 재요구 안 함"은
>   현 코드에 없음. M11의 일은 **여력 조건을 걸어 거절 자체를 드물게** 만드는 것.
> - FleeRunner는 이미 내 집 우선(ResolveAnchor)이다 — M11의 일은 **노숙자 폴백을
>   기지→방향 도피로 교체**하는 것뿐.
> - 슬롯 22/52 사용. 본 명세가 9개 추가 → 31/52 (전투 슬롯 몫 잔여 21).
> - Job_Medic·성격 5종(온순·농사꾼·새침·고집쟁이·방랑벽)은 M10에서 확인된 그대로.

## 0. 목적

M10 실측 진단 3건이 출발점이다: ① 간호 crowding(전원 몰림 → 사망 구조적 불가 = S3 미착탄)
② 구역 밀집(PathBlocked + 부상자 밀집지 즉시 회복) ③ 공용 창고 식량이 있는 한 무노동
주민도 생존(개인의 생존 책임 부재). M11은 **식량 소유를 개인화**해 이 셋을 한 번에 푼다:
개인 식량 = 무노동 생존 소멸, 개인 택지 = 밀집 해소, 간호 2단계 = crowding 자연 해소 + 사망 착탄.

축 분리 원칙: **식량만 개인, 자재(목재·석재)는 공용** — "각자 먹고살되 마을은 함께 짓는다".
전투/격퇴(Warrior·방어 건물·재건·집 타격-식량 상실)는 전부 M12.

## 0.5 넓히기 분류 (①선반 위 물건 / ②새 선반 — 사용자 가치관, 필수 항목)

**②새 선반 3개**:

| 선반 | 내용 | 완성 후 에셋 1개짜리가 되는 것 |
|---|---|---|
| 개인 인벤토리 선반 (M11-A) | 몸 소지(VillagerAgent)+집 저장(HomeStorageService)+EffectApplier 제3 라우팅+ActionCompiler 상한 전제 자동 주입 | **새 개인 재화 = SlotId 1개 + 액션 에셋** (화폐·지갑이 이 선반 위에 얹힘 — 결정 2 후속 호환). 상한 조정 = AgentConfig 에셋 값 |
| 택지 선반 (M11-F) | HomePicker 결정적 점수 배치 + 성격/직업 선호 필드 | **배치 취향 조정 = 성격/직업 에셋 필드 값** (HomeOutskirtsBias). 새 성격의 택지 개성 = 에셋 값, 코드 0줄 |
| 간호 2단계 선반 (M11-I) | 안정화/치료 분리 + GoalSO.RequiredJob + JobSO.CanTreatInjury | **치료 직업 추가/조정 = JobSO 에셋 값** (CanTreatInjury·TendRecoveryMult). M12 전투 부상도 같은 계단을 탄다 |

**①선반 위 물건** (기존 축 데이터만): 게으름뱅이 성격 에셋(M11-J 리허설 = .cs 0 증명),
부탁 에셋 2종(여력/피격), Chatter_Housewarming(M9-E FireScene 패턴), Goal_SaveForHome·
goal 에셋 개인화 개편(트리거 슬롯 교체), 소비/채집/조리 액션 에셋의 슬롯 교체.

## 1. 성공 기준 (측정 가능)

- **M11-S1 개인 순환**: Play 1사이클(12일)에서 채집→몸 소지→집 저장→꺼내 식사 체인이
  Console `[Inventory]` 로그로 관측. 전역 RawFoodStock·CookedFoodStock은 **개전 후 0 유지**
  (개인화 누락 액션 탐지기).
- **M11-S2 상한 작동**: 몸 소지 상한 초과 클램프 경고 0건 (컴파일러 전제 주입이 정상이면
  클램프는 불발) + 상한 근접 주민의 저장/부탁 행동 전환 관측.
- **M11-S3 상실 착탄**: 24일 방치에서 노숙 주민의 굶주림 이탈 **또는** 부상 사망 ≥ 1건
  (M9 "이탈 구조적 불가능" 해소의 실측 증명). 이탈 0~2명 = 정상 범위 유지 (재미 건강 기준).
- **M11-S4 집 서사 체인**: 피격 → Goal_SaveForHome(말풍선) → 부탁 → 완공 → 집들이 릴레이가
  로그로 1회 이상. `[Request]`·`[Ownership]`·`[Chatter]` 로그 연쇄로 검증.
- **M11-S5 보상 실이전**: `[Request] 보상` 로그가 실제 식량 이동(의뢰인 -N / 수행자 +N)을
  기록. 잔고 부족 시 `정산 연기` 로그 (지급 소실 0 — 빚 유지).
- **M11-S6 간호 2단계**: 일반 간호 = `안정화` 로그 1회성, Medic = `회복` 로그. Medic 부재
  부상자의 악화→사망 착탄 가능. **부상자 1명에 동시 간호자 ≤ 2** (crowding 해소 실측).
- **M11-S7 리허설**: 게으름뱅이 성격 추가 커밋의 diff에 .cs 0개 (에셋 + 스폰 풀 등록뿐).
- **M11-S8 게이트**: EditMode M11-T1~T6 전부 green + Play 방치에서 NoSolutionFound 0건.
- **M11-S9 회상 테스트**: M11 종료 절차 — M10 이월 S9 포함 (crowding 해소 후 재미 재판정).

## 2. 재미 검증 — 플레이어가 화면에서 새로 보게 되는 것

이제 주민마다 살림이 다르다. 부지런한 농사꾼은 제 밭에서 거둔 곡식을 제 집 저장고에 쌓아
겨울을 앞두고 여유롭고, 집 없는 게으름뱅이는 몸에 지닌 몇 알로 하루하루를 버틴다. 늑대가
들이닥치면 집 있는 주민은 제 집으로 뛰어들지만 노숙자는 허허벌판으로 달아나다 물리고 —
그제서야 "집값 모으기" 말풍선을 띄우고 열심히 채집해 목수를 찾아간다. 새침한 목수는 선불을
요구하고, 가난한 의뢰인은 빚을 진 채 집을 받아 나중에 마주칠 때 곡식으로 갚는다. 새 집이
완공되면 이웃들이 릴레이로 축하하는 집들이가 벌어진다. 부상자 곁에는 이제 온 마을이 몰리지
않는다 — 한 명이 응급조치를 하고 물러나고, 치료사가 없으면 무덤이 생긴다. 화면의 주민
하나하나가 "저 사람은 어떻게 사나"를 갖게 된다.

## 3. ADR-M11 (구현 중 갈릴 판단, 바꾸려면 사유를 커밋에)

1. **개인 스톡 쓰기 단일 (ADR-M0-3 계승)**: 몸 소지 쓰기 = EffectApplier 경유
   `VillagerAgent.ApplyPersonalStock`만, 집 저장 쓰기 = `HomeStorageService`(TryAdd/TrySpend)만.
   보상 이전·시작 분배·방랑자 초기치도 전부 이 두 문을 지난다. 제3 경로 발견 = 설계 오류.
2. **슬롯 = 몸 소지만**: MyRawFood/MyCookedFood는 몸에 지닌 것. 집 저장은 MyHomeRawFood/
   MyHomeCookedFood 별도 슬롯 — 플래너가 "집에 둔 식량을 들판에서 먹는" 계획을 못 세운다.
   몸+집 합산은 MyFoodDaysLeft 파생 1곳뿐 (트리거 전용, ADR-M9-9 계승).
3. **상한 단일 출처 + 컴파일러 자동 주입**: 상한 수치는 AgentConfigSO에만. ActionCompiler가
   개인 스톡 Add 효과를 가진 액션에 `해당 슬롯 ≤ 상한-Add값` 전제조건을 **자동 주입**한다
   (액션 에셋에 상한 리터럴 기입 금지 — 이중 기입). EffectApplier 클램프는 방어선일 뿐,
   발동하면 경고 로그 = 버그다.
4. **보상 = 실제 이전 + 부족 시 연기**: 정산은 의뢰인 잔고(몸+집)가 전액이고 수행자 수령
   공간이 전액일 때만. 아니면 빚 유지 + `정산 연기` 로그 (소실 없음). 부분 지급 금지 (장부
   복잡화). **떼먹기는 성격 경로만** — 파산은 떼먹기가 아니다 (관계 델타 없음).
5. **택지 = 결정적 점수 (ADR-M1-2 계승)**: 후보 순회 순서 고정, 점수 동률은 (x, y) 사전순.
   랜덤 금지. 선호는 성격/직업 에셋 필드 가산치뿐 (기본 0 = 중립).
6. **간호 2단계 (M10 결정 11 공식 개정)**: 안정화 = 부상 1건당 1회, 유예 부여만 (회복 없음).
   완치 = `JobSO.CanTreatInjury` 직업의 간호만. **ADR-M5-4("직업 전용 goal 금지") 부분 개정**:
   GoalSO.RequiredJob은 치료 goal 전용 예외 — 다른 goal에 설정하면 반려 (남용 = 직업 붕괴
   스위치 부활).
7. **자재 공용 유지**: WoodStock·StoneStock 전역 그대로, 창고 성역(ADR-M9-5)은 자재 창고로
   계승. 전역 식량 슬롯(RawFoodStock 1·CookedFoodStock 10)은 **휴면** — 슬롯 삭제 금지
   (append 규약), 초기치 0, 어느 액션·goal도 참조하지 않게 에셋 개편 (S1이 감시).
8. **결말 이원화 유지 (ADR-M10-3)**: 개인 기근도 이탈 계단(포만<10 지속)으로만. 개인화가
   새 결말 경로를 만들지 않는다.
9. **채집 노드 공용 유지**: 야생 노드(열매·나무·돌)에 소유 없음. 개인화는 소지·저장·밭에만.
   "밭 없이 채집만 하는 노숙 생존"은 겨울(RegenMult 0)+저장 불가가 심판한다 (§8 관찰 ①).
10. **세이브 주석 규약 (ADR-M0-10)**: 몸 소지 2종·집 저장(타일 키)·MyWasAttacked·안정화
    플래그·유예 잔량·빚 목록(_pendingReports는 기존 규약대로 저장 안 함 → **개정: 실식량
    빚은 저장 대상으로 승격** — 소실되면 지급 소실) = 세이브 대상.

## 4. 데이터·수치

### 4.1 SlotId 확장 (append 규약 — 기존 인덱스 불변, 31/52)

`Assets/Scripts/M0/Data/SlotId.cs`:

```csharp
// ── M9 확장 개정 (M11) ───────────────────────────────────────────────
MyFoodDaysLeft = 19, // 수치형 — [개정] 舊 FoodDaysLeft. 내(몸+집) 남은 식량 일수 파생.
                     // enum 이름만 교체 — 직렬화는 int라 에셋 호환 (참조 goal은 M11-D가 전수 개편)

// ── M11 확장 (기존 인덱스 뒤에만 추가 — 에셋 호환 유지) ────────────
MyRawFood        = 22, // 수치형 — 몸 소지 생식 (개인 스톡. 원천 = VillagerAgent, ADR-M11-1)
MyCookedFood     = 23, // 수치형 — 몸 소지 조리식
MyHomeRawFood    = 24, // 수치형 — 내 집 저장 생식 (원천 = HomeStorageService)
MyHomeCookedFood = 25, // 수치형 — 내 집 저장 조리식
MyWasAttacked    = 26, // 논리형 — 피격 경험 (Injure 1회로 영구 true. 집 동기 축, 세이브 대상)
MyFarmPlotCount  = 27, // 수치형 — 내 소유 밭 수 (원천 = FarmService 소유 필터)
MyEmptyPlot      = 28, // 수치형 — 내 빈 밭 수
MyRipeCrop       = 29, // 수치형 — 내 익은 밭 수
UntendedInjuredCount = 30, // 수치형 — 미안정 부상자 수 (안정화 goal 트리거 — crowding 해소 축)
// SlotIds.Count = 31
// IsNumeric 추가: 22·23·24·25·27·28·29·30
// IsStock: 변경 없음 (전역 전용 유지). 신설 IsPersonalStock(22·23)·IsHomeStock(24·25)
```

⚠️ 기존 EmptyFarmPlot(12)·RipeCropAvailable(13)·FoodDaysLeft(19) 참조 goal/액션 에셋은
전수 교체 대상 (M11-B/D/E). FarmPlotCount(11)는 **전역 유지** — ThreatService.VillageScale의
분모라 개인화하지 않는다.

### 4.2 기존 SO 추가 필드 (전부 기본값 = 중립)

| 파일 | 필드 | 기본값 | 용도 |
|---|---|---|---|
| `AgentConfigSO.cs` | `int BodyCarryCap` | 8 (제안치) | 몸 소지 상한 (생식+조리 합산). 컴파일러 주입·클램프의 단일 출처 |
| `AgentConfigSO.cs` | `int HomeStorageCap` | 15 (제안치) | 집 저장 상한 (합산). 주택 사다리(태크트리)에서 BuildingSO로 이관 예정 — 지금은 단일값 |
| `AgentConfigSO.cs` | `float InitialMyRawFood` | 4 (제안치) | 시작 주민 초기 몸 소지 (舊 InitialRawFoodStock 30÷주민 수 근사 — 빠듯하게) |
| `AgentConfigSO.cs` | `float StabilizeGraceDays` | 1.0 (제안치) | 안정화 1회가 멈추는 사망 시계 유예 (악화 곡선: 1.5일 + 유예 1.0 = 총 ~2.5일) |
| `WorldConfigSO.cs` | `int WandererInitialFood` | 1 (제안치) | 방랑자 합류 초기 소지 (가난 — 노숙 서사의 출발) |
| `PersonalitySO.cs` | `float HomeOutskirtsBias` | 0 | 택지 점수 가산 (+ = 마을 바깥 선호, - = 이웃 곁). 0 = 중립 |
| `JobSO.cs` | `float HomeOutskirtsBias` | 0 | 동일 — 성격과 합산 |
| `JobSO.cs` | `bool CanTreatInjury` | false | 완치 가능 직업 (Job_Medic만 true). TendRecoveryMult는 치료 속도로 유지 |
| `GoalSO.cs` | `JobSO RequiredJob` | null | 직업 게이트 (치료 goal 전용 — ADR-M11-6) |
| `BuildingSO.cs` | `int MinSpacingTiles` | 0 | 같은 슬롯 기존 완공과의 최소 간격 (체비쇼프). House만 3 (제안치) — 밀집 해소 |
| `FleeActionSO.cs` | `int FleeDistanceTiles` | 6 (제안치) | 노숙자 방향 도피 거리 |

### 4.3 핵심 수치 관계식 (제안치 — 전부 에셋 값, 관측 후 조정)

```
포만 감쇠 25/일, 생식 +15, 조리식 +35 (기존 — 변경 없음)
→ 1인 하루 필요 ≈ 생식 1.7개 상당
몸 소지 상한 8       ≈ 생식 4.8일치 (겨울 4일을 몸만으로는 못 버티게: 조리 없이는 빠듯)
집 저장 상한 15      ≈ 몸+집 = 13일치 상당 (M9 잉여-안락 재발 방지: 20 이상 금지)
집 부탁 보상 5 (생식) = 상한 8의 62% — 제약 "보상 ≤ 상한 70%" 충족 (보완 ④)
방랑자 초기 소지 1    = 가난 (집값 5까지 4개 차이 = 저축 서사 구간)
Goal_Plant 트리거 MyFoodDaysLeft ≤ 3 (舊 마을 합산 ≤4의 개인 환산)
Goal_WinterPrep 목표 MyFoodDaysLeft ≥ 6 (舊 절대량의 개인 환산 — 집 있어야 실질 달성 가능)
조리 재료 조건 MyRawFood ≥ 5 (M9 위기 레시피 5 유지 — 상한 8 안이므로 성립 가능 검증됨)
```

## 5. 작업 항목 (M11-A ~ M11-J)

### M11-A: 개인 인벤토리 선반 — 슬롯·몸 소지·집 저장·라우팅·상한 주입

**대상**: `SlotId.cs`, `VillagerAgent.cs`, 신규 `World/HomeStorageService.cs`,
`EffectApplier.cs`, `Planning/ActionCompiler.cs`, `AgentConfigSO.cs`,
`Tests/EditMode/M11_InventoryGates.cs` (신규)

**스케치**:

```csharp
// VillagerAgent — 몸 소지 (MySatiety 패턴: 개인 소유, 세이브 대상)
public int MyRaw { get; private set; }
public int MyCooked { get; private set; }
/// <summary>개인 스톡 쓰기 단일 문 (ADR-M11-1) — EffectApplier·이전(TransferTo)만 호출.
/// 상한 클램프는 방어선: 발동 = 컴파일러 주입 누락 버그 (경고 로그).</summary>
public bool ApplyPersonalStock(SlotId slot, EffectOp op, int value) { ... }

// HomeStorageService — 집 저장 (타일 키 = OwnershipService 규약. M12 집 타격-상실 대비)
public sealed class HomeStorageService
{
    private readonly Dictionary<Vector2Int, (int raw, int cooked)> _byTile = new();
    public bool TryAdd(Vector2Int homeTile, SlotId slot, int amount, int cap);   // 상한 검사
    public bool TrySpend(Vector2Int homeTile, SlotId slot, int amount);          // 원자 선검사
    public (int raw, int cooked) Get(Vector2Int homeTile);
    public void ReleaseTile(Vector2Int tile);  // 집 파괴 시 (M12에서 상실 로그 연결)
}

// EffectApplier — 제3·제4 라우팅 (기존 2갈래에 추가)
// ① 전역 스톡 → WorldModel  ② My욕구 → ApplyNeedEffect
// ③ IsPersonalStock → agent.ApplyPersonalStock (Sub는 1단계 선검사에 포함 — 원자성 유지)
// ④ IsHomeStock → world 아님 — agent 소유 집 타일 경유 HomeStorageService
//    (MyHasHome==0인데 IsHomeStock 효과 = 실패 반환. 플래너 전제 MyHasHome==1이 정상 차단)

// ActionCompiler — 상한 전제 자동 주입 (ADR-M11-3)
// 개인/집 스톡 Add 효과 발견 시: 전제 (slot, LessOrEqual, cap - addValue) 자동 추가.
// cap 출처 = AgentConfigSO (BodyCarryCap / HomeStorageCap) — 컴파일 시 1회 읽기.
public static SlotCondition[] InjectCapPreconditions(
    SlotCondition[] existing, List<SlotEffect> effects, int bodyCap, int homeCap); // 순수 — 게이트
```

**DoD**:
- [ ] EditMode M11-T1: ApplyPersonalStock Sub 부족 시 false·무변경 (원자성), Add 상한 클램프
      시 경고 로그 + 초과분 소실 없음(선검사 실패 반환) 검증
- [ ] EditMode M11-T1: InjectCapPreconditions — Add 8 효과 + cap 8 → 전제 `≤ 0` 주입,
      개인 스톡 효과 없는 액션 → 입력 배열 그대로 (중립 불변식)
- [ ] EditMode M11-T1: HomeStorageService TryAdd 상한 초과 false·TrySpend 원자성
- [ ] 기존 M0-T~M10-T 게이트 전체 green (슬롯 Count 31 반영)
- [ ] dotnet build 2종 0오류

**⚠️ 오해 위험**: ① "MyRawFood도 IsStock에 추가"하면 EffectApplier 1단계 선검사가
world.GetStock을 읽어 항상 0 → 개인 식사 전부 실패. IsStock은 전역 전용으로 남기고
IsPersonalStock을 신설한다. ② 상한을 액션 에셋 전제조건에 손으로 기입하면 AgentConfig
값과 어긋난다 — 주입은 컴파일러만 (ADR-M11-3). ③ HomeStorage를 agentId 키로 만들면 M12
집 타격-상실을 연결할 곳이 없다 — 반드시 타일 키.

**작업량** 대 / **심각도** 최상 (모든 후속 항목의 기반) / **의존** 없음 — 첫 커밋.

### M11-B: 먹거리 개인화 — 소비·채집·조리 에셋 개편 + 식량 가치 파생 확장

**대상**: `Assets/M0Config/Actions/` (EatRawFood·EatCookedFood·HarvestWildBerries·CookMeal·
CookMealScarce), `Assets/M0Config/Goals/` (Goal_Snack·Goal_CookAhead·Goal_CookExtra·
Goal_GatherFood), `WorldModel.cs` (TryGetFoodValue), `M0Bootstrap/SimulationLoop` 배선

**스케치**: 에셋 효과·전제의 슬롯 교체가 본체다 (코드는 파생 함수 1곳):

```
EatRawFood:   RawFoodStock Sub 1 → MyRawFood Sub 1  (+MySatiety Add 15 유지)
EatCookedFood: CookedFoodStock Sub 1 → MyCookedFood Sub 1 (+35 유지)
HarvestWildBerries: RawFoodStock Add 1 → MyRawFood Add 1  (전제는 컴파일러 주입에 맡김)
CookMeal:     MyRawFood Sub 1 → MyCookedFood Add 1 (평시 CrisisActive==0 유지)
CookMealScarce: MyRawFood Sub 5 → MyCookedFood Add 1 (위기 유지)
ChopWood/MineStone: 변경 없음 (자재 공용 — ADR-M11-7)
```

```csharp
// WorldModel.TryGetFoodValue — IsStock → (IsStock || IsPersonalStock)로 확장.
// FoodSources 파생이 MyRawFood/MyCookedFood 가치를 인식해야 MyFoodDaysLeft가 계산된다.
```

**DoD**:
- [ ] Play 10분: 주민이 채집→몸 소지 증가→제자리/모닥불 식사 순환, `[Inventory]` 로그 관측
- [ ] 전역 RawFoodStock·CookedFoodStock 증감 로그 0건 (S1 탐지기)
- [ ] EditMode M11-T2: TryGetFoodValue가 MyRawFood Sub 1 + MySatiety Add 15에서
      (MyRawFood, 15) 파생
- [ ] NoSolutionFound 0건 (⚠️ M10 교훈: 카탈로그 min 비용 게이트 0085fc2 통과 확인)

**⚠️ 오해 위험**: ① 조리는 모닥불(공용 시설)에서 자기 재료로 — CampfireBuilt 전제를 지우면
안 된다 (시설 공용 = 결정 9). ② Goal_CookAhead류의 "재료 ≥5" 조건을 지우면 M9 조리
NoSolution 재발 (a351437 교훈) — 슬롯만 MyRawFood로 교체. ③ 전역 식량 슬롯을 IsNumeric·
IsStock에서 빼면 안 된다 — 휴면이지 삭제가 아니다 (append 규약).

**작업량** 중 (에셋 다수 + 코드 1곳) / **심각도** 상 / **의존** M11-A.

### M11-C: 집 저장 액션 — 넣기/꺼내기

**대상**: 신규 액션 에셋 `StoreFoodHome`·`TakeFoodHome` (기존 ConsumeActionSO 계열 재사용 —
EatAtAnchor 패턴으로 내 집 이동), `ActionCatalog` 등록, 신규 goal `Goal_StoreFood`

**스케치**:

```
StoreFoodHome (ConsumeActionSO, EatAtAnchor=true, AnchorPriority=[HouseCount]):
  전제: MyHasHome==1, MyRawFood ≥ 3 (제안치 — 몸에 좀 모이면)
  효과: MyRawFood Sub 3, MyHomeRawFood Add 3   (+조리식 변형 1종 동일 패턴)
TakeFoodHome:
  전제: MyHasHome==1, MyHomeRawFood ≥ 1, MyRawFood ≤ 2 (몸이 비어갈 때)
  효과: MyHomeRawFood Sub 1, MyRawFood Add 1
Goal_StoreFood: 트리거 MyHasHome==1 & MyRawFood ≥ 6 (상한 근접) → 목표 MyRawFood ≤ 3
```

ConsumeRunner의 앵커 이동은 ResolveAnchor 경유 = **내 집 우선**이 이미 구현돼 있다 —
러너 신설 없이 에셋 4개 + goal 1개. EffectApplier ④라우팅(M11-A)이 실체를 처리한다.

**DoD**:
- [ ] Play: 상한 근접 주민이 집으로 걸어가 저장 (`[Inventory] 저장` 로그 + 몸/집 수치 교차 확인)
- [ ] 몸이 빈 주민이 집에서 꺼내 식사하는 체인이 한 플랜에 담김 (플랜 말풍선 확인)
- [ ] EditMode: Goal_StoreFood OnValidate 무한 루프 검사 통과 (목표 ≤3 vs 트리거 ≥6)
- [ ] 노숙 주민 동작 변화 0 (MyHasHome==0 전제 — 중립 불변식)

**⚠️ 오해 위험**: ResolveAnchor는 "내 소유 → 아무 완공" 순서다 — AnchorPriority에
HouseCount를 주면 노숙자가 **남의 집**으로 갈 수 있다. StoreFoodHome은 전제 MyHasHome==1이
차단하지만, 러너 폴백 경로도 소유 집 없으면 실패(재계획)로 막을 것 (남의 집 저장 = 버그).

**작업량** 소 / **심각도** 중 / **의존** M11-A·B.

### M11-D: MyFoodDaysLeft 전환 + goal 임계 개편 + HUD·정보줄

**대상**: `SlotId.cs`(19 rename — M11-A에서 선행 가능), `WorldModel.cs`, `VillagerAgent.cs`
(BuildSnapshot), `SimulationLoop.cs`(HUD 배선), `SeasonHud.cs`, `NameTag.cs`(정보줄),
goal 에셋 (Goal_Plant·Goal_WinterPrep·Goal_CookAhead·Goal_CookExtra·Goal_ExpandFarm)

**스케치**:

```csharp
// 개인 식량 일수 (순수 — ComputeFoodDaysLeft 재사용: 인원=1, 스톡=몸+집 합산 임시 배열)
public static int ComputeMyFoodDays((SlotId slot, int gain)[] foodValues,
    int myRaw, int myCooked, int homeRaw, int homeCooked, float decayPerDay, float seasonMult);
// BuildSnapshot: slots[19] = 위 함수 (파생 슬롯 — 트리거 전용 ADR-M9-9 유지)
// SimulationLoop HUD 배선: World.EstimateFoodDaysLeft() → 전 주민 ComputeMyFoodDays 최솟값
//   (표시 = "가장 위험한 주민" — 관측 대상이 마을 평균이 아니라 낙오자다)
// NameTag 정보줄: "생3 조1" 소지 표기 추가 (개인 경제 관측의 최소 단위)
```

goal 임계 (에셋 값 — §4.3 환산표): Plant ≤3 / WinterPrep ≥6 / ExpandFarm은 M11-E에서 개인화.

**DoD**:
- [ ] HUD 식량 일수 = 전 주민 최솟값 (주민 클릭 없이 낙오자 파악 가능)
- [ ] 정보줄에 개인 소지 표기 렌더 확인
- [ ] EditMode M11-T3: ComputeMyFoodDays — 몸4+집0·감쇠25 → 2일 (15×4÷25=2.4→2),
      집 포함 합산 검증, 미배선 99 중립
- [ ] Goal_Plant가 개인 3일 이하일 때만 발동 (로그로 1회 확인)

**⚠️ 오해 위험**: ① 舊 EstimateFoodDaysLeft(마을 합산)를 남겨두고 HUD만 바꾸면 판정
이원화 — 마을 합산 산식은 **삭제**하고 최솟값 창구로 대체한다 (M9-I "HUD 자체 재계산 금지"
정신). ② WorldModel의 _foodValues 파생은 유지 — 가치표는 개인 계산에도 같은 출처다.

**작업량** 중 / **심각도** 상 (밸런스 축) / **의존** M11-A·B·C. **→ 중간 리뷰 ① (Play 관측:
개인 순환·임계 착지. §7 참조)**

### M11-E: 개인 밭 — FarmPlot 소유·My 슬롯·집 곁 배치

**대상**: `FarmService.cs`, `ConstructionService.cs`(완공 이벤트에 builder 전달),
`WorldModel.cs`(스냅샷), `VillagerAgent.cs`(BuildSnapshot 인자), `BuildRunner.cs`(밭 배치),
`FarmRunner.cs`(소유 필터), 에셋 (FarmPlot.asset ZoneRadius 0, PlantCrop·HarvestCrop·
Goal_BuildFarm·Goal_Plant·Goal_HarvestCrop·Goal_ExpandFarm·Goal_Routine_Farmer)

**스케치**:

```csharp
// FarmPlot: public string OwnerId { get; internal set; }  // 등록 시 1회 기입 — 이후 불변
// FarmService: RegisterPlot(x, y, ownerId) + 소유 필터 조회
public int CountEmptyOf(string ownerId); public int CountRipeOf(string ownerId);
public int CountPlotsOf(string ownerId);
public FarmPlot NearestEmptyOf(string ownerId, int fromX, int fromY);  // 타인 밭 배제
// ConstructionService.Complete에 builderId 전달 → OnCompleted(so, x, y, builderId)
//   기존 구독자는 인자 무시 (시그니처 확장만 — 완공 단일 지점 유지)
// 스냅샷: MyFarmPlotCount/MyEmptyPlot/MyRipeCrop = CountXxxOf(agentId)
//   전역 EmptyFarmPlot/RipeCropAvailable은 계속 채운다 (휴면 — 참조 goal 0이 정상)
// BuildRunner: FarmPlot은 hasZone 경로 대신 "내 집 곁" — 앵커 = 내 소유 집 타일,
//   반경 = FarmPlot.asset ZoneRadius 재해석 않고 신규 필드 없이 BuildingSO.MinSpacingTiles=0
//   + 내 집 체비쇼프 반경 2 (제안치, WorldConfig) 링 탐색. 집 없으면 밭 goal 자체가 안 뜬다
//   (Goal_BuildFarm 트리거에 MyHasHome==1)
```

goal 개편: Goal_BuildFarm 트리거 `MyHasHome==1 & MyFarmPlotCount==0` /
Goal_ExpandFarm `MyHasHome==1 & MyFarmPlotCount ≤1 & MyFoodDaysLeft ≤5` → 목표 `≥2`
(🟡 M9 잔여관찰 "ExpandFarm 절대값 트리거"도 이 개편으로 해소) /
Plant·Harvest 전제 = MyEmptyPlot·MyRipeCrop.

**DoD**:
- [ ] Play: 집 가진 주민이 집 곁에 밭을 만들고 심기·수확 전부 본인 (타인 밭 노동 로그 0건)
- [ ] 수확물이 본인 MyRawFood로 (전역 스톡 불변 — S1 탐지기)
- [ ] EditMode M11-T4: CountXxxOf 소유 필터·NearestEmptyOf 타인 배제, RegisterPlot
      ownerId null(舊 경로) = 전 조회 제외 아닌 **공용 취급 안 함** — null 소유 밭은 존재
      금지 (아래 ⚠️③)
- [ ] ThreatService 밭 타격이 소유 밭에도 착탄 (파괴 문 BlightCrop/RemovePlot 경유 불변)

**⚠️ 오해 위험**: ① FarmPlotCount(11) 전역을 MyFarmPlotCount로 "교체"하면 위협 사다리
규모 산정이 무너진다 — 전역은 유지, 개인은 신설. ② 공용 밭 폐지 = "기존 밭 데이터 삭제"가
아니라 "새 밭은 전부 소유 등록"이다 — 씬 시작 밭 0개라 마이그레이션 없음. ③ ownerId 없는
밭이 생기는 경로(舊 RegisterPlot 호출)를 남기면 아무도 못 가꾸는 유령 밭 — 시그니처에서
ownerId를 필수로 (기본값 금지). ④ 재해·위협의 밭 파괴는 소유와 무관하게 기존 문 그대로 —
소유자 통지는 표현(대사)일 뿐 로직 분기 금지.

**작업량** 대 / **심각도** 상 / **의존** M11-A·D, 배치는 M11-F의 집 존재 전제 (커밋 순서는
F 뒤). 

### M11-F: 개인 택지 — HomePicker·목수 부탁 배치 개편·집들이

**대상**: 신규 `World/HomePicker.cs`(순수 정적), `BuildRunner.cs`(House 배치 분기),
`OwnershipService.cs`(OnAssigned 이벤트), `SimulationLoop.cs`(집들이 배선),
`PersonalitySO.cs`/`JobSO.cs`(HomeOutskirtsBias), `BuildingSO.cs`(MinSpacingTiles),
에셋 (House.asset ZoneRadius→0·MinSpacingTiles 3, 신규 Chatter_Housewarming.asset,
씬 `_housewarmingChatter` 배선)

**스케치**:

```csharp
/// <summary>택지 선정 (순수 — 게이트 대상, ADR-M11-5). 후보 = 마을 앵커(기지) 체비쇼프
/// 반경 VillageRadius 내 통행·비점유 타일 중 기존 집들과 MinSpacingTiles 이상.
/// 점수 = -(앵커 거리) + outskirtsBias × (앵커 거리)  → bias>0이면 바깥 선호로 반전.
/// 동률 = (x, y) 사전순. 후보 0 = 실패 (마을 만원 — 보이는 소프트 상한, 구역 소멸의 계승).</summary>
public static bool PickHomesite(Func<int,int,bool> occupied, IReadOnlyList<Vector2Int> houses,
    Vector2Int villageAnchor, int villageRadius, int minSpacing, float outskirtsBias,
    int minX, int maxX, int minY, int maxY, out Vector2Int tile);

// BuildRunner: House(BlocksMovement+HouseCount) 건설 시 —
//   의뢰인 = _sim.Requests.TryGetAssignment(agent.AgentId, out requesterId, out _) 로 획득,
//   bias = 의뢰인 성격.HomeOutskirtsBias + 의뢰인 직업.HomeOutskirtsBias (부탁 아니면 본인)
//   → HomePicker. 기존 hasZone 경로는 House에서 제거 (밭도 M11-E에서 제거 → ZoneService는
//   휴면 서비스로 남김 — 마을 앵커·테두리 뷰 참조는 WorldConfig.BaseTile로 대체)
// OwnershipService.Assign 말미: OnAssigned?.Invoke(tile, slot, agentId)
// SimulationLoop: OnAssigned(HouseCount) → ShowHousewarming — ShowFarmMeeting 사본:
//   화자 = 새 집주인(최근접 아님 — 주인공), 청중 = 반경 내 CollectEligible,
//   FireScene(_housewarmingChatter, ...). ShowFarmMeeting 배선(OnZoneEstablished)은 제거.
```

Chatter_Housewarming: MaxExtraListeners 3, SceneTempoMult 2 (FarmMeeting 검증값 계승),
대사 = 집들이 축하 릴레이 (신규 기입).

**DoD**:
- [ ] Play: 목수가 짓는 집이 기존 집과 3타일 이상 간격 (겹침 0 — PathBlocked 밀집 완화 관측)
- [ ] 완공→소유 배정→집들이 릴레이 (`[Ownership]`→`[Chatter]` 로그 연쇄 + HUD 알림)
- [ ] EditMode M11-T5: PickHomesite 간격 위반 후보 배제·bias 부호별 원근 반전·동률 사전순·
      만원 false·결정성 (같은 입력 2회 = 같은 출력)
- [ ] FarmMeeting 미발화 (구독 제거 확인 — Chatter_FarmMeeting.asset은 휴면 보존)

**⚠️ 오해 위험**: ① 택지 선호는 **의뢰인**(집주인 될 사람) 것이다 — 목수 성격으로 계산하면
"목수 취향의 집"이 된다. TryGetAssignment로 의뢰인을 찾는 게 핵심. ② ZoneService 삭제
금지 — 휴면. 향후 구역 이전 백로그([project_zone_relocation])가 재사용 후보. ③ 집들이
화자는 새 집주인 — FarmMeeting의 "앵커 최근접 화자"를 그대로 베끼면 옆집 사람이 주인공이
된다. ④ 집 부탁의 GrantOwnership "최근접 무주" 배정은 유지되지만, 이제 택지가 의뢰인
선호로 지어지므로 "남의 새 집을 가로채는" 시나리오는 1건 부탁 원칙(HasInFlightFrom)이 막는다
— 동시 집 부탁 2건이 가능해지면 여기부터 재검토 (§8 관찰 ④).

**작업량** 대 / **심각도** 상 / **의존** M11-A. E보다 먼저.

### M11-G: 집 동기 — 피격 플래그·저축 goal·부탁 에셋 이원·노숙 도망

**대상**: `VillagerAgent.cs`(Injure에 MyWasAttacked 기록, BuildSnapshot 인자),
`FleeRunner.cs`+`FleeActionSO.cs`(노숙 방향 도피), `ThreatService.cs`(최근접 위협 좌표 창구),
에셋 (신규 Goal_SaveForHome, 기존 집 부탁 에셋 개편 + 신규 Request_House_Urgent,
Reward 수치 §4.3)

**스케치**:

```csharp
// VillagerAgent.Injure(): MyWasAttacked = true (영구 — 세이브 대상. 회복돼도 잊지 않는다)
// FleeRunner.Prepare: ResolveAnchor 소유 집 있음 → 기존 경로 (내 집 = 피난처, 결정 7)
//   없음 → 방향 도피: _sim.Threats.TryGetNearestThreatPos(x, y, out tp) 기준
//   반대 방향 FleeDistanceTiles 지점의 통행 타일 (기지 폴백 삭제 — 노숙 = 위험)
//   ⚠️ FleeActionSO BaseCost ≥ 5 유지 (M10 교훈 0085fc2 — 휴리스틱 붕괴 금지)
```

```
Goal_SaveForHome (신규): Priority 45 (제안치 — 노동 goal대와 동급, P0 아래)
  트리거: MyHasHome==0 & MyWasAttacked==1 & MyRawFood ≤ 4   → 목표: MyRawFood ≥ 5
  (플랜 = 채집. "물리고 나서야 집값을 모은다" — 결정 6 서사의 실체)
집 부탁 에셋(기존): RequesterConditions = MyHasHome==0 & MyRawFood ≥ 5  (여력 경로 — 보완 ③)
Request_House_Urgent (신규): RequesterConditions = MyHasHome==0 & MyWasAttacked==1
  (가난해도 부탁 가능 — 선불 목수엔 거절당하고 후불 목수에겐 빚으로 성립)
  둘 다: RewardCostSlot=MyRawFood, RewardCostAmount=5, TargetJob=Job_Carpenter,
  InjectGoal=Goal_BuildHouse, GrantOwnership=HouseCount (기존 규약 유지)
```

**DoD**:
- [ ] Play: 노숙 주민 피격 → "집값 모으기" 채집 → 5 도달 → 부탁 → 완공 체인 (S4)
- [ ] 노숙 주민 도망이 위협 반대 방향 (기지로 안 감 — 로그·화면 확인)
- [ ] EditMode M11-T6: 방향 도피 목적지 산출 순수 함수 (위협-주민 벡터 반대·경계 클램프),
      MyWasAttacked 영구성 (회복 후에도 true)
- [ ] 거절 후 쿨다운 만료 시 재부탁 발생 (영구 거절 없음 — 로그 2회 확인)

**⚠️ 오해 위험**: ① Goal_SaveForHome의 목표 `MyRawFood ≥ 5`는 달성돼도 부탁 성립까지
시간이 걸린다 — OnValidate 무한루프 검사(트리거 ≤4 vs 목표 ≥5)는 통과하지만, **먹으면
다시 4 이하로 내려가 재발동**하는 순환은 정상이다 (저축과 식사의 줄다리기 = 의도). 버그로
오인해 식사를 막는 수정 금지 (ADR-M4-3 굶주림 앞에 성격 없음). ② Request 두 에셋의 스캔
순서: Urgent가 앞이면 부유한 피격자도 Urgent로 성립 — 무해하다 (같은 보상·같은 goal).
순서 조정으로 "해결"하려 들지 말 것. ③ 방향 도피에서 위협이 없으면(ThreatNear 오탐)
제자리 완료 — 기지 폴백을 되살리면 안 된다.

**작업량** 중 / **심각도** 상 / **의존** M11-A·F(부탁 대상 집 배치), ThreatService 창구.

### M11-H: 보상 실이전 — 개인 잔고 정산·연기

**대상**: `RequestService.cs`(선불·PlayReport·TickRewardSettlement), `VillagerAgent.cs`
(TransferTo — ApplyPersonalStock 경유), `RequestSO.cs`(OnValidate 개인 슬롯 허용)

**스케치**:

```csharp
// 지급 능력 판정 (순수 — 게이트 대상): 몸 우선, 부족분은 집 저장에서
public static bool CanPay(int bodyStock, int homeStock, int amount);
// 이전 실행 (ADR-M11-1: 두 단일 문 경유 — 새 쓰기 경로 아님):
//   의뢰인 몸 Sub → 부족분 집 TrySpend → 수행자 ApplyPersonalStock Add
//   수령 공간 검사 선행: cap - (수행자 몸 합산) ≥ amount 아니면 시도 자체를 안 한다
// TickRewardSettlement / 선불(Ask) 분기:
//   지급 가능 → 이전 + RewardLines   (RewardSatietyGain 직접 포만 지급은 삭제 — 결정 10)
//   불가(잔고) → 연기: 빚 유지 + 로그 "[Request] 정산 연기 — 잔고 부족" (대사 없음 — 다음 마주침)
//   불가(공간) → 연기: 동일 (수행자가 먹어서 공간이 나면 자연 정산)
//   떼먹기 → 기존 성격 경로 그대로 (ShouldStiffReward — 파산과 무관, ADR-M11-4)
// 선불 성격(DemandsRewardUpfront) 수락 판정: _worldModel.TrySpendStock → CanPay(의뢰인)로 교체
//   → 가난한 의뢰인은 선불 목수에게 RefusedNoReward (기존 대사 재사용)
```

_pendingReports는 세이브 대상으로 승격 (ADR-M11-10 — 실식량 빚 소실 방지).
RewardSatietyGain 필드는 휴면 표기 (에셋 값 0 — 포만 직접 지급 경로 폐지).

**DoD**:
- [ ] Play: 완수→마주침→실이전 로그 (의뢰인 -5 / 수행자 +5 — 정보줄 수치로 교차 확인)
- [ ] 잔고 부족 의뢰인 → `정산 연기` 로그 → 이후 잔고 회복 시 지급 (S5)
- [ ] EditMode M11-T6: CanPay 경계 (몸만·집만·합산·부족), 연기 시 빚 불변, 떼먹기 판정에
      잔고 무관 (가난한 온순 성격은 연기, 부유한 고집쟁이는 떼먹기 — 축 분리)
- [ ] 선불 목수가 가난한 의뢰인 거절 (RefusedNoReward 로그)

**⚠️ 오해 위험**: ① RewardSatietyGain(포만 직접)을 남겨두고 식량 이전을 **추가**하면 이중
보상 — 포만 경로는 폐지가 맞다. 수행자는 받은 식량을 먹어서 포만을 얻는다 (경제의 요점).
② "연기가 쌓이면 목수가 손해"는 버그가 아니라 서사다 — 타임아웃 소멸을 넣지 말 것 (조각 Y
"소실 없음" 원칙 계승). ③ 요리 부탁 등 기존 부탁 에셋의 RewardCostSlot(CookedFoodStock
전역)도 개인 슬롯으로 일괄 개편해야 한다 — 집 부탁만 바꾸면 전역 식량 참조가 남아 S1 위반.

**작업량** 중 / **심각도** 상 / **의존** M11-A·C(집 저장이 잔고의 일부). **→ 중간 리뷰 ②
(Play 관측: 집·보상·택지. §7)**

### M11-I: 간호 2단계 — 안정화/치료 분리·crowding 해소

**대상**: `VillagerAgent.cs`(안정화 상태·NextInjuryState 개정), `TendRunner.cs`,
`GoalSO.cs`(RequiredJob)+`GoalSelector.cs`/`IsGoalExcluded`(직업 게이트),
`JobSO.cs`(CanTreatInjury), `SimulationLoop.cs`(UntendedInjuredCount 집계·스냅샷 배선),
에셋 (Goal_TendInjured 개편=안정화, 신규 Goal_TreatInjured RequiredJob=Job_Medic,
Job_Medic.asset CanTreatInjury=true, AgentConfig StabilizeGraceDays)

**스케치**:

```csharp
// 부상 상태 확장 (세이브 대상 +2): bool _stabilized; float _graceLeft; // 게임일
/// <summary>부상 계단 v2 (순수 — 게이트): 치료 간호 = 회복 진행 (기존과 동일).
/// 일반 간호 = 최초 1회만 안정화(유예 부여) — 이후 일반 간호는 무효과 (crowding 해소).
/// 유예 소진 후 = 방치 누적 재개 → 사망. 회복은 치료 간호로만 오른다.</summary>
public static (float recovery, float neglect, bool stabilized, float grace) NextInjuryStateV2(
    float recovery, float neglect, bool stabilized, float grace,
    bool tendedByHealer, bool tendedByHelper, float tendMult, float graceDays, float dt)
{
    if (tendedByHealer) return (recovery + dt * tendMult, neglect, stabilized, grace);
    if (tendedByHelper && !stabilized) return (recovery, neglect, true, graceDays); // 1회성
    if (grace > 0f) return (recovery, neglect, stabilized, grace - dt);             // 유예 소모
    return (recovery, neglect + dt, stabilized, grace);                             // 악화
}
// MarkTended(untilSec, recoveryMult, isHealer) — 호출자 = TendRunner (간호자 Job.CanTreatInjury)
// FindNearestInjured 이원화: 안정화 대상 = !_stabilized 부상자만 / 치료 대상 = 전 부상자
// UntendedInjuredCount 집계 = SimulationLoop (InjuredCount 패턴) — 안정화 완료 시 즉시 감소
//   → Goal_TendInjured(안정화) 트리거 소멸 → 군중 해산 (MaxWorkers 불필요 판정 유지)
// IsGoalExcluded: goal.RequiredJob != null && Job != goal.RequiredJob → 제외 (ADR-M11-6)
```

```
Goal_TendInjured (개편): 트리거 UntendedInjuredCount ≥ 1 (기존 InjuredCount에서 교체)
Goal_TreatInjured (신규): RequiredJob=Job_Medic, 트리거 InjuredCount ≥ 1, Priority 기존과 동일
시작 스폰 풀: Medic 미포함 유지 / 방랑자 풀: 포함 — "치료사 영입" 가치 급등 (결정 15)
```

**DoD**:
- [ ] EditMode M11-T6: NextInjuryStateV2 — 안정화 1회성(재간호 무효)·유예 소모·소진 후
      악화 재개·치료만 회복 증가·(둘 다 없음 = 기존 방치와 동일 누적) 전 분기
- [ ] EditMode: RequiredJob null = 기존 Select와 동일 (중립 불변식), 설정 시 타 직업 제외
- [ ] Play: 부상 발생 → 1명이 안정화 후 해산 (동시 간호자 ≤2 — S6 crowding 실측)
- [ ] Play: Medic 부재 방치 → `[Injury]` 악화 → 사망 (M10-S3 이월 착탄)

**⚠️ 오해 위험**: ① 안정화를 "사망 시계 정지 지속"으로 구현하면 일반 주민이 교대 간호로
영원히 살린다 — crowding 재발 + Medic 무가치. **1회성 유예 부여**가 요점. ② RequiredJob을
GoalSelector 공용 필터가 아니라 TendRunner Prepare 실패로 구현하면 실패 쿨다운 공회전 —
goal 후보에서 제외가 정답. ③ InjuredCount(20)를 UntendedInjuredCount로 교체하면
Goal_TreatInjured 트리거가 사라진다 — 두 카운트는 별개 슬롯. ④ Medic 본인 부상 시 마을에
완치 경로 소멸 = **의도된 상실** (명세 확정) — 자가 치료 예외를 넣지 말 것.

**작업량** 중 / **심각도** 상 / **의존** M11-A(슬롯 30). M10 결정 11 공식 개정 커밋.

### M11-J: 마무리 — 시작 분배·방랑자 소지·게으름뱅이 리허설

**대상**: `SimulationLoop.cs`/`M0Bootstrap.cs`(시작 분배), `WandererService.cs`(초기 소지),
`WorldConfig.asset`(InitialRawFoodStock→0), 에셋 (신규 Personality_Lazy.asset + 스폰 풀,
기존 성격 5종 HomeOutskirtsBias 값 배정)

**스케치**:

```csharp
// 시작: 각 주민 ApplyPersonalStock(MyRawFood, Add, cfg.InitialMyRawFood) — ADR-M11-1 경유
// SpawnVillager(방랑자): 동일 문으로 WandererInitialFood
```

```
Personality_Lazy (리허설 — .cs 0 증명): GatherCostMult 1.3 / FarmCostMult 1.3 /
  BuildCostMult 1.3 (노동 전반 미적), GoalBoosts: Goal_SaveForHome -20 · Goal_StoreFood -15 ·
  Goal_WinterPrep -20 (대비 안 함), HomeOutskirtsBias 0, MoodLines 게으름 대사 (전부 제안치)
기존 성격 택지 선호 (제안치): 새침 +0.5(바깥 — 혼자), 온순 -0.3(이웃 곁), 방랑벽 +0.7,
  고집쟁이 0, 농사꾼 -0.2
```

**DoD**:
- [ ] 게으름뱅이 커밋 diff에 .cs 0개 (M11-S7)
- [ ] 시작 시 전역 식량 0·주민 몸 소지 4씩 (로그 확인)
- [ ] 방랑자 합류 주민 소지 1 (정보줄 확인)
- [ ] Play 24일 방치: S3(상실 착탄)·S6(간호 2단계) 최종 관측

**작업량** 소 / **심각도** 중 / **의존** 전 항목. 마지막 커밋 + 최종 관측.

## 6. 커밋 분할 계획 (1항목 1커밋 — 각 커밋 단독 빌드 가능)

| 순서 | 커밋 | 내용 | 유형 |
|---|---|---|---|
| 1 | M11-A | 슬롯 9종 + 개인 인벤토리 + 라우팅 + 상한 주입 + 게이트 | feat |
| 2 | M11-B | 소비·채집·조리 개인화 (에셋 + 파생 확장) | feat |
| 3 | M11-C | 집 저장 액션·goal | feat |
| 4 | M11-D | MyFoodDaysLeft 전환 + goal 임계 + HUD·정보줄 → **중간 리뷰 ①** | feat |
| 5 | M11-F | 택지 HomePicker + 집들이 + FarmMeeting 배선 제거 | feat |
| 6 | M11-E | 개인 밭 (소유·My 슬롯·집 곁 배치) | feat |
| 7 | M11-G | 집 동기 (피격 플래그·저축 goal·부탁 이원·노숙 도망) | feat |
| 8 | M11-H | 보상 실이전·연기 정산 → **중간 리뷰 ②** | feat |
| 9 | M11-I | 간호 2단계 (M10 결정 11 개정 명기) | feat |
| 10 | M11-J | 시작 분배·방랑자 소지·게으름뱅이 리허설 (.cs 0) | feat |

이후: 회상 테스트(S9) + spec-review.

## 7. 중간 리뷰 (Play 관측 기준)

**① M11-D 후**: 개인 순환이 살림이 되는가 — ⓐ NoSolutionFound 0 ⓑ 전역 식량 0 유지
ⓒ 주민별 소지 격차 발생 (정보줄 편차) ⓓ 겨울 진입 시 MyFoodDaysLeft 최솟값 ≤ 4 (조임 확인
— M9 잉여-안락 재발 시 상한·초기치 하향이 1차 레버). **집·밭이 아직 없어 저장은 관측 불가
— 몸 소지 순환만 판정.**

**② M11-H 후**: 집이 격차를 만드는가 — ⓐ 택지 간격 준수·PathBlocked 빈도 M10 대비 감소
ⓑ 집 보유자 vs 노숙자의 겨울 궤적 분리 ⓒ 부탁-보상-빚 체인 로그 ⓓ 집들이 장면 1회.

## 8. 관찰 항목 (수정 아님 — 발현 조건 명시)

1. **채집만 노숙 생존의 안락 여부** (ADR-M11-9): 온화 계절엔 노숙 채집 생존이 성립하는 게
   정상. 겨울(재생 0)에 몸 소지 8만으로 이탈 위험이 실제 발생하는지 — 24일 방치에서 노숙자
   겨울 포만 궤적 관측. 안락하면 BodyCarryCap 하향이 1차 레버.
2. **정산 연기 무한 지속**: 수행자 만복(공간 없음)·의뢰인 만성 빈곤으로 빚이 12일 이상
   잔존하면 부분 지급 도입 재검토 (지금은 금지 — ADR-M11-4).
3. **Goal_SaveForHome vs 식사 줄다리기**: 저축이 영원히 5에 못 닿는 주민(게으름뱅이+겨울)이
   나오면 — 버그 아님, 관측 후 보상액 조정으로만 대응.
4. **동시 집 부탁**: HasInFlightFrom이 의뢰인당 1건을 보장하나 서로 다른 의뢰인 2건이 동시
   진행되면 GrantOwnership "최근접 무주" 배정이 엇갈릴 수 있음 — 발생 로그 확인 시 완공-의뢰
   매칭 강화.
5. **간호 유예 수치**: 안정화 1.0일 + 사망 문턱 1.5일이 "치료사 없으면 결국 죽는다"를
   보장하는지 — Medic 부재 판에서 사망 소요일 실측 (2.5일 ± 예상).
6. **빈집 재활용**: 사망·이탈로 소유 해제된 집+저장 식량의 거취 — M11은 방치(저장분 동결),
   정책은 M12 재건 브레인스토밍으로.

## 9. 스코프 가드 — 이번에 하지 않는 것

- **전투·격퇴·Warrior 개체 전투·방어 건물·재건** (M12 — 브레인스토밍부터)
- **위협의 집 타격 → 저장 식량 상실** (결정 11 — HomeStorage 타일 키가 준비만 해 둔다)
- **화폐·부채 장부·지갑** (결정 2 — 보상 자리에 화폐만 꽂으면 되는 구조 확인까지가 M11)
- **자재(목재·석재) 개인화** — 공용 유지 (ADR-M11-7)
- **침상·간병 확장** (부상 심각도 필드 예비 유지 — M10 결정 9)
- **시작 드래프트 UI·퀘스트 보드·태크 트리·구역 이전** (기존 백로그 그대로)
- **비주얼·UI 전면 개편** (M10 백로그 계승 — 정보줄 소지 표기 최소 추가만)
- **빈집 재활용 정책** (§8-6)
- **Goal_Leisure·휴식·굶주림 계단 등 기존 축의 밸런스 재조정** — 개인화로 바뀐 슬롯 외 불변

## 10. ADR 개정·계승 대장

| 기존 | 처분 |
|---|---|
| ADR-M9-2 (구역 앵커 불변) | **개정** — 밭·집 구역 폐지로 휴면. 택지가 대체 (ADR-M11-5). ZoneService 코드는 보존 |
| ADR-M9-5 (창고 성역) | **계승** — 자재 창고로 (ADR-M11-7) |
| M10 결정 11 (간호 = 누구나+Medic 가속) | **개정** — 간호 2단계 (ADR-M11-6) |
| ADR-M5-4 (직업 전용 goal 금지) | **부분 개정** — RequiredJob 치료 goal 전용 예외 |
| ADR-M9-9 (파생 슬롯 트리거 전용) | 계승 — MyFoodDaysLeft·UntendedInjuredCount |
| ADR-M9-10 (식량 가치 = 소비 액션) | 계승 — 개인 슬롯으로 확장 (M11-B) |
| ADR-M10-3 (결말 이원화) | 계승 (ADR-M11-8) |
| ADR-M0-3 (쓰기 단일) | 계승 — 개인 스톡 2문 (ADR-M11-1) |
| 조각 Y (마주치면 정산·소실 없음) | 계승 — 실식량 판 (ADR-M11-4) + 빚 세이브 승격 |
