# M20 — 직업 전면 재점검 실행명세서

> 브레인스토밍 확정 (2026-08-05). 배경: 사용자 지시 *"직업 목록을 전부 다 재점검해야 할 것
> 같아"* (M19 명시 이월) → 근본 질문("직업이라는 게 필요한가")부터 재도출.
> 선행: M19 화폐 전면 철거 (목수 효율 모델 = 이 명세의 참조 선례).

---

## 0. 성공 기준 (숫자)

1. **게이트 전부 green** — 기존 304(보장 테스트 삭제분 제외) + 신규 4종 (T1~T4).
2. **중립 불변식**: 무직·비전문 주민의 밭일/조리/채집/건설 실행 시간 = 현행 **완전 동일**
   (새 배율 전부 기본 1.0 — 게이트 T1).
3. **전문가 4종 승격**: 농부 밭일·요리사 조리·나무꾼 벌목·광부 채석의 실행 시간 =
   기본의 **0.5배** (제안치 — §4. 게이트 T2).
4. **식사·휴식 불가침**: 조리 배율이 식사(Eat*) 시간에 **절대 미적용** (ADR-M5-3 유지 —
   게이트 T3).
5. **RequiredJob 독점 = 정확히 1곳(치료)** 유지 (기존 게이트 M19-T3 green 유지).
6. **스폰 풀 6종** (탐험가 제외) · **최소 보장 코드 0줄** (`MustForceGuaranteedJob` 삭제).
7. **Play 관측**: 목수 없는 판 ≥ 1판에서 집 완공 확인 (M19 잔여 겸사 관측과 통합).

---

## 1. 전제 확인 (0단계 실사 산출 — 2026-08-05 코드 확인)

| # | 전제/발견 | 실사 결과 |
|---|---|---|
| 1 | 조리는 전용 러너가 있다 | ❌ **없다** — CookMeal·CookMealScarce는 `ConsumeActionSO` → **ConsumeRunner(식사와 공용)**. 조리 배율은 플래그로 갈라야 식사 불가침(ADR-M5-3)이 지켜진다 → ADR-M20-3 |
| 2 | 채집은 자원별로 식별 가능한가 | ✅ `GatherActionSO.TargetResource` (ChopWood=Wood, MineStone=Stone — 스크립트 GUID 동일 확인). 자원별 배율이 자연스럽다 |
| 3 | 목수 효율 모델의 배선 | ✅ 소비처 `BuildRunner.cs:34` 한 곳, 유도 `VillagerAgent.cs:137` (`BuildDurationMultOfJob`), 배율 판 `IActionRunner.cs:66` (`DurationElapsed(dt, mult)`, 하한 0.05) |
| 4 | `IActionRunner.cs:64` 주석 | ⚠️ "소비처는 BuildRunner뿐 — 다른 러너가 쓰면 ADR-M5-3 위반 신호" — M20에서 **개정 필요** (노동 러너 4곳으로 확대, 위반 기준은 '식사·휴식'으로 정밀화) |
| 5 | `_guaranteedJob` 참조 전수 | `SimulationLoop.cs` :52(필드) :192(플래그) :212~217(발동) :234(기록) + `M0Scene.unity` 1줄 + `M12_TraitGates.cs:1024~1030` 단언 6개 — **이게 전부** |
| 6 | Job_Explorer 참조 전수 | GUID `fa059dde…` grep → **씬 `_jobPool` 1곳뿐**. 휴면 = 씬 한 줄 제거로 완결. `Goal_Routine_Explorer`·`M5_JobGates`는 에셋 차원 검사라 영향 없음 |
| 7 | "게으름뱅이 무직화"는 이월 과제다 | ❌ **이미 구현됨** (M12-H — `NoJobBelowDiligence` 미만만 무직 후보 확률 추가, `SimulationLoop.cs:167~171`). 이번에 손대지 않는다. memory 정정 완료 |
| 8 | 요리사 부탁 라우팅 | ✅ `Request_CookForMe.asset` + `RequestSO.TargetJob` (참조 매핑) — **유지, 변경 없음** |
| 9 | 밭일 러너의 범위 | `FarmActionKind = Plant/Harvest` 2종뿐. `Routine_TendFields`는 다른 SO(FarmRunner 안 탐) → 농부 배율의 영향권 밖 |
| 10 | 치료 축 | `TendRecoveryMult` 소비처 = TendRunner, `CanTreatInjury` 독점 = 치료 goal 1곳 (M19-T3 게이트가 감시 중) — **이번 밀스톤 불변** |

---

## 2. ADR (구현 중 갈릴 판단 — 바꾸려면 사유를 커밋에)

### ADR-M20-1 · 직업 헌장 — "부재가 느껴지는 장치"

모든 직업은 **"○○ 없는 판은 ~한 판이다"라는 부재 시나리오를 한 줄로 쓸 수 있어야
존재 자격이 있다.** 쓸 수 없는 직업은 만들지 않고, 이미 있으면 휴면한다.
근거: 화폐 철거의 교훈("결과를 못 바꾸는 층은 개념만 어지럽힌다" — M19)의 직업판.
북극성("성장하며 버티는 기록 경주")과는 "전문가의 상실 = 연대기의 사건"으로 연결.
- 판정표: 목수(집 2배 느림)·치료사(부상=사형선고)·농부(겨울 대비 느림)·요리사(식량
  변환 느림)·나무꾼(목재 느림)·광부(석재 느림) = 통과. **탐험가 = 미달**(발견은 이동 중
  누구나 수행, ExploreRunner 실사) → 휴면.
- 같은 이유로 **최소 보장(강제 배정)은 헌장 위반** — "없는 판"의 존재 자체가 목적이다.

### ADR-M20-2 · 효율 배율의 구조 — 실행층 Duration, 자원별 배열, 중립 1.0

- **Cost(계획 선호)와 Duration(실행 시간)은 별개 축으로 공존한다.** GatherCostMult 등
  기존 4종은 그대로 두고 Duration 계열을 **추가**한다 (M19 목수 선례 그대로).
- 채집은 자원별로 갈라야 나무꾼·광부가 구분된다 → `GatherDurationMult[]` **배열**
  (GoalBoost[]와 같은 {참조, 값} 패턴). 미정의 자원 = 1.0. Iron(=4)이 이미 수용되므로
  M21+ 격퇴 축(무기 재료)의 선반 자리가 공짜로 생긴다.
- **중립 불변식 (M5-S3·M19-W2 교훈)**: 기본값 전부 1.0 = 무직·일반 현행 불변. 전문가만
  <1 보너스. 페널티 방향(일반을 느리게) 금지 — 공용 러너를 타는 다른 행동까지 느려진다.

### ADR-M20-3 · 조리 = 노동 재분류 (식사 불가침 유지)

조리는 러너가 소비 계열(ConsumeRunner)이지만 **성질은 노동**이다. `ConsumeActionSO`에
`IsCookingWork` 플래그(기본 false)를 신설하고, ConsumeRunner는 **플래그가 켜진 액션만**
조리 배율을 곱한다. 식사(EatRawFood·EatCookedFood)·휴식은 전 직업 영원히 중립 —
ADR-M5-3("몸값 불가침")은 이 형태로 계승된다. 플래그를 켜는 에셋은 CookMeal·
CookMealScarce **2곳뿐** (게이트 T3이 감시).

### ADR-M20-4 · 최소 보장 폐지 (폐기=삭제)

`_guaranteedJob`의 근거(목수 독점 → 목수 없는 판은 집 0채 → 규모 8 정체)는 M19 독점
해제로 소멸했다. 헌장(ADR-M20-1)과 정면충돌하므로 **기능째 삭제**한다 (ADR-M0-4:
폐기=삭제, git이 히스토리다). 비대칭 근거 보강: 치료사는 보장이 없는데 목수만 보장할
이유가 없다. 무직 확률(M12-H NoJob)은 **별개 기능 — 유지**.

### ADR-M20-5 · 탐험가 휴면 (에셋 보존, 풀 제외)

Job_Explorer·Goal_Routine_Explorer 에셋은 **삭제하지 않는다** (M21+ 정찰 — 위협 조기
발견 — 전환 후보. 슬롯 append-only 전례와 같은 보존 원칙). 씬 `_jobPool`에서만 제거.
복원 = 씬 한 줄 (에셋 1개짜리 되돌리기).

---

## 3. 작업 항목 (W)

### W1 — JobSO 선반 확장 + 유도 메서드

**대상**: `Assets/Scripts/M0/Data/JobSO.cs`, `Assets/Scripts/M0/Agent/VillagerAgent.cs`,
`Assets/Scripts/M0/Agent/IActionRunner.cs`

**스케치** (JobSO — GoalBoost 구조체 아래):

```csharp
/// <summary>자원별 채집 실행 시간 배율 항목 (M20 — ADR-M20-2. GoalBoost와 같은 패턴).</summary>
[Serializable]
public struct GatherDurationMult
{
    public ResourceType Resource;
    public float Mult;
}
```

JobSO 필드 (BuildDurationMult 아래에 배치, 툴팁은 그 형식을 미러):

```csharp
public float FarmDurationMult = 1f;          // 밭일(심기/수확) 실행 시간. 소비처 = FarmRunner 하나
public float CookDurationMult = 1f;          // 조리 실행 시간. 소비처 = ConsumeRunner의 IsCookingWork 분기 하나
public GatherDurationMult[] GatherDurationMults;  // 자원별 채집. 비면 전 자원 1(중립)

/// <summary>자원별 채집 배율 조회. 미정의 = 1 (중립). 참조 아닌 enum 비교 — ResourceType은 값 신원.</summary>
public float GatherDurationMultFor(ResourceType r)
{
    if (GatherDurationMults != null)
        foreach (GatherDurationMult g in GatherDurationMults)
            if (g.Resource == r) return g.Mult;
    return 1f;
}
```

VillagerAgent (`BuildDurationMultOfJob` :137 바로 아래, 같은 형식):

```csharp
public float FarmDurationMultOfJob() => Job != null ? Job.FarmDurationMult : 1f;
public float CookDurationMultOfJob() => Job != null ? Job.CookDurationMult : 1f;
public float GatherDurationMultOfJob(ResourceType r) => Job != null ? Job.GatherDurationMultFor(r) : 1f;
```

IActionRunner.cs:64 주석 개정: "소비처는 BuildRunner뿐" → "소비처는 노동 러너 4곳
(Build/Farm/Gather + Consume의 조리 플래그 분기). **식사·휴식에 곱기 시작하면
ADR-M5-3 위반 신호**" (위반 기준을 러너 목록에서 행동 성질로 정밀화).

**DoD**:
- [x] 컴파일 통과 (`dotnet build` 2종 오류·경고 0 — 필드 추가만, 동작 변화 0)
- [x] 신규 게이트 T4 작성: `ScriptableObject.CreateInstance<JobSO>()`에서
      `GatherDurationMultFor(Wood) == 1f` (빈 배열 중립) · 항목 {Wood, 0.5f} 추가 시
      Wood=0.5f·Stone=1f 반환
- [x] 신규 게이트 T1(전반) 작성: Jobs 폴더 전 에셋 순회 — 명시 예외 목록 밖의
      FarmDurationMult/CookDurationMult == 1, GatherDurationMults 비어 있음
      (이 커밋 시점 예외 목록 = 빈 목록 — W3에서 4종 등록)
- [ ] **게이트 실행 = 사용자 Test Runner 이관** (에디터 열림 중 batchmode 불가 —
      2026-07-17·07-31 전례. 실행 확인 후 이 줄 체크)

**⚠️ 오해 위험**: ①"GatherCostMult를 DurationMult로 대체" — 아니다. Cost는 계획 선호,
Duration은 실행 시간, **둘 다 공존** (전제 3). ②배율을 `HarvestAmount()`(수확량)에도 —
**수확량은 불변**, 시간만 준다 (수확량은 플래너 Effects와 단일 출처라 건드리면 계획·실행
불일치). ③유도 메서드를 러너가 매 Tick 호출 — Prepare 1회 캐시가 BuildRunner 선례다.

**작업량** 소 / **심각도** 낮음(추가만) / **의존** 없음 — 첫 커밋

---

### W2 — 러너 배선 3곳 + 조리 플래그

**대상**: `Assets/Scripts/M0/Agent/Runners/FarmRunner.cs`,
`.../GatherRunner.cs`, `.../ConsumeRunner.cs`, `Assets/Scripts/M0/Data/ConsumeActionSO.cs`

**스케치** (BuildRunner:34 패턴 미러 — Prepare에서 1회 캐시, Tick에서 배율 판 사용):

```csharp
// FarmRunner — Prepare 성공 경로 끝에:
_durationMult = agent.FarmDurationMultOfJob();   // M20 — 실행 시점 직업 기준 (중립 = 1)
// Tick: if (!DurationElapsed(dt, _durationMult)) return RunnerResult.Running;

// GatherRunner — Prepare 성공 경로 끝에:
_durationMult = agent.GatherDurationMultOfJob(_so.TargetResource);
// Tick 동일 치환

// ConsumeActionSO — 필드 신설:
[Tooltip("조리 노동인가 (M20 — ADR-M20-3). true = 조리 배율 적용 대상. " +
         "식사·휴식 액션은 절대 켜지 않는다 (ADR-M5-3 몸값 불가침) — 게이트 T3이 감시.")]
public bool IsCookingWork;

// ConsumeRunner — Prepare 끝에:
_durationMult = _so.IsCookingWork ? agent.CookDurationMultOfJob() : 1f;
// Tick: DurationElapsed(dt) → DurationElapsed(dt, _durationMult)
```

**DoD**:
- [x] 컴파일 통과 (`dotnet build` 2종 오류·경고 0. 에셋 전부 기본값이라 동작 변화 0 — 중립 실증)
- [x] ConsumeRunner: `IsCookingWork == false`인 액션은 배율 경로가 1f 고정임을 코드
      리뷰로 확인 (식사 시간 불변) — 삼항 분기, Prepare 진입 즉시 확정
- [x] FarmRunner·GatherRunner의 `Cleanup`/실패 경로에 배율 잔존 상태 없음
      (`_durationMult`는 러너 인스턴스 수명 = 액션 1회 — BuildRunner와 동일.
      배율 대입은 점유 성공 뒤에만)
- [ ] **게이트 실행 = 사용자 Test Runner 이관**

**⚠️ 오해 위험**: ①ConsumeRunner에 무조건 배율 — **식사가 빨라져 ADR-M5-3 위반**.
반드시 플래그 분기. ②탐험·휴식·간호 러너에도 "겸사" 배선 — 스코프 밖 (간호는
TendRecoveryMult가 이미 담당, 탐험은 휴면 직업의 축). ③`DurationElapsed(dt)` 오버로드를
지우고 배율판으로 통일 — 기존 러너(휴식 등) 전부 재컴파일 파급, 두 오버로드 공존이 설계다.

**작업량** 소 / **심각도** 중(공용 러너 3곳) / **의존** W1

---

### W3 — 에셋 값 기입 (전문가 4종 + 조리 액션 2종) + 게이트 본대

**대상**: `Assets/M0Config/Jobs/Job_Farmer.asset`, `Job_Cook.asset`, `Job_Lumberjack.asset`,
`Job_Miner.asset`, `Assets/M0Config/Actions/CookMeal.asset`, `CookMealScarce.asset`,
`Assets/Tests/EditMode/M20_JobGates.cs` (신설)

**값** (전부 제안치 — §4):

| 에셋 | 추가 줄 (YAML) |
|---|---|
| Job_Farmer | `FarmDurationMult: 0.5` |
| Job_Cook | `CookDurationMult: 0.5` |
| Job_Lumberjack | `GatherDurationMults:` + `- Resource: 2` `Mult: 0.5` (Wood=2) |
| Job_Miner | `GatherDurationMults:` + `- Resource: 3` `Mult: 0.5` (Stone=3) |
| CookMeal / CookMealScarce | `IsCookingWork: 1` |

**게이트** (M20_JobGates.cs — M19_T3b 에셋 순회 패턴):
- T1 중립 불변식: Jobs 폴더 전 에셋 — 위 표의 명시 예외 밖은 Farm/Cook == 1,
  GatherDurationMults 비거나 예외 목록과 일치. **목수·치료사에 새 배율이 없는 것도 여기서
  단언** (전문 축 1인 1개 원칙은 두지 않지만, 명시 없이 생긴 값은 오염이다)
- T2 전문가 값: Farmer 0.5 · Cook 0.5 · Lumberjack Wood 0.5(Stone 1.0) · Miner Stone 0.5(Wood 1.0)
- T3 식사 불가침: Actions 폴더 전 ConsumeActionSO 순회 — `IsCookingWork == true`는
  CookMeal·CookMealScarce **정확히 2곳** ("이 단언이 실패하는 구현": Eat 액션에 실수로
  플래그가 켜지면 red — M17 교훈 충족)

**DoD**:
- [x] **에셋 수정은 Edit 도구만 — 셸 일괄 재작성 절대 금지** (M19-W4 사고 재발 방지) —
      6개 파일 전부 Edit 단건 삽입, 기존 줄 0건 변경
- [x] YAML 형상이 기존 배열 필드(GoalBoosts·PreferWeights)와 동일 (`- Resource: 2` /
      `Mult: 0.5` 2줄 들여쓰기), enum은 정수 직렬화 (Wood=2·Stone=3)
- [ ] T1·T2·T3 green + 기존 게이트 green — **사용자 Test Runner 이관**
- [ ] Unity 에디터에서 Job_Lumberjack 인스펙터에 Wood 0.5 항목 표시 확인 (직렬화 검증)
      — **중간 리뷰 지점** (에셋 직접 편집 커밋)

**⚠️ 오해 위험**: ①`Resource:` 값을 이름으로 적기 — YAML은 enum **정수** (Wood=2,
Stone=3 — `ResourceType.cs` 확인 완료). ②나무꾼에 Stone도 0.5 — 아니다, **자원별 구분이
두 직업을 가르는 유일한 선**. ③기존 `GatherCostMult: 0.8` 줄을 지우거나 고치기 —
불변 (ADR-M20-2 공존 원칙).

**작업량** 소 / **심각도** 중(에셋 직접 편집) / **의존** W2

---

### W4 — 탐험가 휴면 (스폰 풀 제외)

**대상**: `Assets/Scenes/M0Scene.unity` (`_jobPool` 배열 — :653 부근)

**스케치**: `_jobPool` 7항목 중 `guid: fa059dde74744ce4bf5d001b01420102` (Job_Explorer)
**한 줄만 Edit로 제거** → 6항목. 에셋·Goal_Routine_Explorer·meta는 손대지 않는다.

**DoD**:
- [ ] 씬 로드 정상 (Unity 콘솔 missing reference 0)
- [ ] 기존 게이트 green — M5_JobGates의 Job_Explorer 에셋 검사는 에셋 차원이라 계속
      통과해야 한다 (전제 6). 깨지면 풀 의존 검사가 숨어 있던 것 — 원인 진단 후 진행
- [ ] Play 스폰 로그에서 탐험가 배정 0건 (다판 표본 — P2 관측에 겸사)

**⚠️ 오해 위험**: ①에셋 파일 삭제 — **금지** (ADR-M20-5 보존). ②Goal_Routine_Explorer를
"고아 정리" — 금지, 휴면 직업의 부품이다 (곁가지 배칭 원칙 — 발견 보고만). ③씬 YAML을
Unity 없이 광범위 재정렬 — 한 줄 삭제 외 어떤 줄도 건드리지 않는다.

**작업량** 극소 / **심각도** 낮음 / **의존** 없음 (W1~W3과 독립 — 언제든 커밋 가능)

---

### W5 — 목수 최소 보장 삭제

**대상**: `Assets/Scripts/M0/SimulationLoop.cs` (:49~52 필드, :189~192 상태, :212~217 발동,
:234 기록), `Assets/Scenes/M0Scene.unity` (`_guaranteedJob:` 1줄),
`Assets/Tests/EditMode/M12_TraitGates.cs` (:1024~1030 단언 6개 포함 테스트)

**스케치**: 참조 전수(전제 5)를 역순으로 제거 — 테스트 → 발동부 → 필드 → 씬 줄.
`MustForceGuaranteedJob` 함수째 삭제 (폐기=삭제). `_initialRoster`·`_jobsAssigned`는
보장 전용인지 다른 소비처가 있는지 **삭제 전 grep 재확인** 후 전용이면 함께 제거.

**DoD**:
- [ ] `grep -ri "guaranteed" Assets/` 결과 0건
- [ ] M12_TraitGates 컴파일·green (보장 테스트 삭제, 나머지 불변)
- [ ] 씬 로드 정상 — `_guaranteedJob` 직렬화 줄 제거 확인
- [ ] 삭제 사유가 커밋 메시지에 (ADR-M20-4 요지 — "근거였던 목수 독점이 M19에서 소멸")

**⚠️ 오해 위험**: ①M12-H **무직 확률**(`NoJobWeight`·`NoJobBelowDiligence`)까지 삭제 —
**금지**, 별개 기능 (ADR-M20-4). ②"코드는 두고 씬만 비우기" — 헌장 위반 기능을 중립
상태로 존치하는 것 = ADR-M0-4 위반. 기능째 삭제가 결정이다. ③PickJobIndex 서명 변경 —
불필요, 보장은 PickJobIndex **밖**(:212 분기)에 있다.

**작업량** 소 / **심각도** 중(스폰 경로) / **의존** 없음 (독립 커밋 가능)

---

### W6 — 문서·주석 정합 (격퇴 자리 명시 포함)

**대상**: `Assets/Scripts/M0/Data/JobSO.cs` (클래스 헤더 주석),
`Assets/Scripts/M0/SimulationLoop.cs` (:49 툴팁 잔재), `Docs/` (이 명세의 P 판정란)

**스케치**:
- JobSO 헤더에 헌장 한 줄: "직업 = 부재가 느껴지는 장치 (ADR-M20-1) — 새 직업은 부재
  시나리오 한 줄을 쓸 수 있어야 한다. **격퇴 계열(사냥꾼·경비)의 선반 자리**:
  전투 효율은 새 필드 그룹(예: CombatDurationMult), 사냥 자원은 GatherDurationMults의
  Iron/신규 항목이 이미 수용 — M21+ 전투 시스템과 한 몸으로만 신설한다."
- 낡은 주석 소거: "집은 목수 부탁 전용" 류 (M19 이전 잔재 — W5에서 툴팁째 삭제되나
  다른 파일 grep로 전수 확인: `grep -rn "목수 전용\|목수만\|Carpenter만" Assets/Scripts`)

**DoD**:
- [ ] 격퇴 자리 문구가 JobSO 헤더에 존재
- [ ] "목수 독점" 전제의 낡은 주석 grep 0건
- [ ] 컴파일·게이트 green (주석만 — 동작 0)

**⚠️ 오해 위험**: 격퇴 자리를 "미리 필드로" 파 두기 (CombatDurationMult 필드 신설 등) —
**금지**. 자리 명시는 주석·명세 문장이지 코드가 아니다 (빈 껍데기 방지 — 브레인스토밍
질문 4 결정).

**작업량** 극소 / **심각도** 낮음 / **의존** W5 뒤 (잔재 주석이 W5에서 일부 사라짐)

---

## 4. 수치 (전부 제안치 — 기존 값 우선 원칙)

| 값 | 제안 | 근거 |
|---|---|---|
| FarmDurationMult (농부) | 0.5 | 유일 선례 = Job_Carpenter BuildDurationMult 0.5 (M19). 발명 아님 — 선례 복제 |
| CookDurationMult (요리사) | 0.5 | 〃 (CookMeal DurationSec 3 → 요리사 1.5초) |
| Wood 0.5 (나무꾼) / Stone 0.5 (광부) | 0.5 | 〃 |
| 배율 하한 | 0.05 | 기존 `DurationElapsed` 배율판에 이미 있음 (`IActionRunner.cs:69`) — 재사용, 신설 아님 |

밸런스 재조정은 Play 관측(P) 후 별도 — 이 명세에서 0.5 외의 값을 조용히 넣지 않는다.

## 5. 커밋 분할 (1항목 1커밋, 각 커밋 단독 실행 가능)

| # | 커밋 | 내용 | 게이트 |
|---|---|---|---|
| C1 | feat(m20): W1 | JobSO 선반 + 유도 + 주석 개정 | T1(빈 예외)·T4 신설 green |
| C2 | feat(m20): W2 | 러너 배선 3곳 + 조리 플래그 | 기존 green (중립 실증) |
| C3 | feat(m20): W3 | 에셋 4+2 값 기입 | T1(예외 4종)·T2·T3 green |
| C4 | feat(m20): W4 | 탐험가 휴면 (씬 1줄) | 기존 green |
| C5 | feat(m20): W5 | 최소 보장 삭제 | M12 게이트 갱신 green |
| C6 | docs(m20): W6 | 헌장 주석·격퇴 자리·잔재 소거 | 전체 green |

중간 리뷰: **C3 직후** — 에셋 직접 편집 커밋이므로 Unity 에디터 로드·인스펙터 확인을
커밋 전 체크에 포함 (대형 마이그레이션은 아니라 별도 리뷰 회차는 두지 않음).

## 6. 스코프 가드 (이번에 하지 않는 것)

- ❌ 격퇴 계열 직업 신설 (사냥꾼·경비) — M21+ 전투 시스템과 한 몸 (자리 명시만 = W6)
- ❌ 치료사 완치 독점 변경 — 브레인스토밍 질문 3에서 **유지 확정**
- ❌ 연대기 "치료사를 잃었다" 기록 강화 — 백로그 N1 (명부에 직업은 이미 표시됨.
  상실 문장 강조는 Play 관측에서 필요가 실증되면)
- ❌ 무직 추첨 파라미터 조정 (M12-H) — 이미 구현, 불변
- ❌ RoutineGoal·GoalBoosts·PreferWeights 재편 — 취향 층은 그대로 (효율 층 추가가 이번 일)
- ❌ 탐험가 에셋 삭제 / 시작 드래프트 UI / 상점·공용 창고 — 기존 백로그 유지
- ❌ 밸런스 수치 실험 (0.5 외) — P 관측 후 별도

## 7. 재미 검증 (플레이어가 화면에서 새로 보게 되는 것)

판을 시작할 때마다 **"이번 마을엔 누가 있는가"가 판의 조건**이 된다 — 명부에 농부가
없으면 겨울 대비가 눈에 띄게 더디고(개별 동작 차이는 미묘하지만 누적이 저장 식량
HUD·겨울 진입 시점의 비축량으로 드러난다), 목수 없는 판은 집이 더디 서는 것을 이번에
처음 실제로 겪게 되며(최소 보장 삭제로 이 판이 비로소 존재 가능), 치료사 없는 판에서
곰이 나오면 부상 하나가 사형선고라는 긴장이 명부 화면 하나로 읽힌다. 직업이 "취향
라벨"에서 **"이 판의 운명 카드"**로 바뀌는 것이 플레이어가 보는 변화다.

## 8. 관측 신호 (P — 사용자 Play 판정란)

- [ ] P1: 목수 없는 판 — 집이 (느리게라도) 완공되는가 (M19 잔여 겸사 ①)
- [ ] P2: 탐험가 배정 0건 + 스폰 구성이 판마다 다른가 (명부 확인)
- [ ] P3: 농부 있는 판 vs 없는 판 — 겨울 1 진입 시 비축량 차이가 체감되는가 (다판 누적)
- [ ] P4: 식사·휴식 속도가 직업과 무관하게 동일한가 (위반 시 ADR-M5-3 사고)
- [ ] (겸사) M19 잔여 관측 ②나눔 장면 ③장시간 방치 — 이 Play에서 함께 확인 후
      M19 명세 P1에 기입

---

*브레인스토밍 결정 이력 (2026-08-05): Q1 존재 이유 = 부재 장치 / Q2 4종 승격+탐험가
전환 / Q3 치료사 독점 유지 / Q4 격퇴 자리만 / Q5 최소 보장 해제. 정본 대화는 세션 로그.*
