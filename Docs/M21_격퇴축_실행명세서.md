# M21 격퇴 축 실행명세서 — "격퇴하며 버티는 기록 경주"

> 승인 2026-08-05 (브레인스토밍 질문 10건 + 림월드 적습 분석 채택 2건).
> 근거 문서: `Docs/게임_전면_재검사_분석노트.md` §3~4 (격퇴 루프 부재 실사),
> `Docs/M20_직업_전면재점검_실행명세서.md` §13 (격퇴 계열 선반 자리).
> 합의 요지: 북극성 양립 개정 · 주민 HP 단일 척도 + HUD · 체류형 위협 + 이중 도주선 ·
> 대규모 적습(마릿수 확장형) · CombatService 신설 · 맨손 가능 + 무기 배율 ·
> 래칫 티어 단조 유지 + 양방향 완충(격퇴 완화 + 자비) · 사냥꾼 신설 + 탐험가 정찰 재탄생.
>
> **재검사 2026-08-06** (ooo interview, W1 구현 실물 대조 — 사용자 결정 3건):
> **A1** 체류 재타격은 부상자 제외 안 함(3대=사망 성립 — §W2) · **A2** 교전 중 부상은
> 러너를 중단하지 않음, 성향(겁) 중도 이탈은 이월(§W4) · **B1** 명령된 Goal_Fight는
> 트리거 우회 원정 특례(§W4). + §4·W1 스케치의 구현 드리프트 정정 (명세→구현 방향 소급).

---

## 0. 성공 기준 (측정 가능)

- **S1** 늑대 1마리 출몰 시 주민(사냥꾼 우선)이 반격하고, Console에 `[Combat] 타격 — <주민> → <위협> (HP n→m)` 로그가 찍힌다.
- **S2** 위협 HP가 도주선 이하로 내려가면 도주하고 `[Combat] 격퇴 — <위협>` 로그 + 연대기 사건(Repelled)이 남는다. HP 0이면 `[Combat] 사냥` 로그 + 고기 드랍(전역 RawFoodStock 증가 로그).
- **S3** 적습(2마리 이상)에서 무리 잔존율이 도주선 아래로 내려가면 잔여 전원이 도주한다 (개체별 반죽음 없이 — 무리 단위 로그 1줄).
- **S4** 주민 선택 정보줄에 `체력 n/100`이 보이고, 포만 0 지속 시 HP가 매 게임일 감소하는 로그가 찍힌다. HP 0 = 사망 (기존 사망 문 Die/StarveToDeath 경유).
- **S5** 무기 보유 주민의 타격 간격이 맨손 대비 배율만큼 짧다 (로그의 타격 소요 시간으로 확인 가능).
- **S6** 명령("싸워라")을 내리면 기존 명령·거부 시스템을 경유하고, 겁 많은 주민은 거부할 수 있다 (RefusedXXX 로그).
- **S7** 중립 불변식: `WorldConfig.Threats`가 비면 M20 동작과 완전 동일 (전투·HP 감소 이벤트 0). 신규 필드 전부 기본값이면 기존 판과 동일 진행.
- **S8** 게이트: 기존 전체 green 유지 + 신규 `M21_CombatGates` green. 특히 부상·간호 게이트(M10_Injury·M11_Care)가 HP 재표현 후에도 green.
- **S9 (W2R 신설, 2026-08-06)** 늑대가 도착 후 **정지하지 않는다** — Play 1판에서 개체 위치가 계속 바뀌고, `밭 0/0 소실` 반복 로그가 **0건**이다 (칠 대상이 없으면 물러난다).
- **S10 (W2R 신설)** 계절이 타깃을 바꾼다 — `Season_Winter`에서만 주민 타깃 확률·체류 상한에 배율이 걸리고 `Season_Summer`(`IsCrisis: 1`이지만 겨울 아님)에서는 안 걸린다. 게이트 `M21_T7`이 경계를 검사한다.
- **S11 (W2R 신설)** Tier1 늑대가 **평시에도 주민을 노리는 출몰이 존재한다** (`VillagerTargetChance` 0 → 0.25 개정). 출몰 서수를 바꿔가며 true가 나오는 서수가 있다 — 결정적으로 검사 가능.

> ⚠️ **S9는 게이트로 증명할 수 없다** — "동선이 보이는가"는 화면에서만 판정된다.
> 그래서 커밋 분할 계획에 **리뷰⓪(W2R 직후 Play)**를 문으로 걸었다 (방법론 M20).

## 0.5 넓히기 분류 (직교성 — 사용자 가치관 필수 항목)

| 요구 | 분류 | 근거 |
|---|---|---|
| 위협 HP·체류·도주선·마릿수 | **① 선반 위 물건** | ThreatSO 필드 append + ThreatService/ThreatAgent 개정. 완성 후 새 위협 종 = 에셋 1개 (몬스터 포함, 코드 0줄) |
| 주민 HP | **① 선반 위 물건** | VillagerAgent 상태 1개 + 기존 아사·부상 사슬의 재표현. 새 개념이 아니라 기존 두 사망 축의 공용 척도 |
| 전투 판정 | **② 새 선반** = `CombatService` | 같은 짐을 나르는 통로 없음 (기존 통로 조사 §1-G). 완성 후 "때리는 자"는 주민이든 몬스터든 같은 문 — 새 교전 규칙 = 에셋 값 |
| 참전 goal/액션 | **① 선반 위 물건** | GoalSO/ActionSO/IActionRunner 기존 선반. Goal_Fight = 에셋 1개 + FightRunner 1개 |
| 무기 | **① 선반 위 물건** | 조리(원료→산출 노동)와 같은 패턴. 슬롯 1칸 + 액션 에셋 |
| 래칫 완충 | **① 선반 위 물건** | ThreatService 스케줄 계산에 보정항 추가. 순수 함수 |
| 사냥꾼·정찰 | **① 선반 위 물건** | JobSO 필드 append + 에셋 2개 + 풀 등록 (M5-S5 "직업 추가 = 에셋 1개" 유지) |
| **위협 배회 (W2R)** | **① 선반 위 물건** | 목표 선정은 기존 통로 `MapBounds.PickWalkableNear` 재사용 (주민 여가 배회 `WanderRunner`가 이미 쓴다). ThreatSO 필드 1개 + ThreatAgent 이동 분기 |
| **계절 → 위협 (W2R)** | **② 새 선반** = 계절 주입 통로 | 같은 짐을 나르는 통로 **없음** (실사 Q: `ThreatService` 생성자에 `SeasonService` 부재). 다만 선반은 얇다 — 판정 슬롯은 기존 `ForageFrozen` 재사용이고(ADR-M21-9) 신설은 **참조 1개 + 순수 함수 3개**. 완성 후 "계절이 성격을 바꾸는 새 위협" = 에셋 값 2개(`HungrySeason*Mult`), 코드 0줄 |

## 1. 전제 확인 (코드 실사 결과, 2026-08-05 — 명세는 이 사실 위에만 선다)

| # | 전제 | 실사 결과 |
|---|---|---|
| A | 위협은 타격 1회 후 자동 퇴장 | ✅ `ThreatService.NotifyArrived` (ThreatService.cs:252) — `ExecuteStrike` 후 퇴장 경로 부여. 위협 HP 없음 |
| B | 스폰은 항상 1마리 | ✅ `Spawn`이 ThreatAgent 1개 생성 (ThreatService.cs:176~203). "늑대 무리"도 개체 1 |
| C | 주민 반격 경로 0 | ✅ Flee(105)뿐. `ThreatAgent` 주석 *"주민과 겹쳐도 무해, M11 전투에서 재검토"* — 재검토 시점이 지금 |
| D | 격퇴 불가는 ADR로 박제돼 있다 | ✅ ThreatSO.cs:7 *"격퇴 불가 (ADR-M10-8)"* — **본 명세가 이 ADR을 개정한다** |
| E | 주민 HP 없음 | ✅ 아사 = `_starvingDays` ≥ `DepartAfterStarvingDays` 즉사 (VillagerAgent.cs:729). 부상 = 상태 머신 (간호 `InjuryRecoverDays=1` 완치 / 방치 `InjuryDeathAfterDays=1.5` 사망) |
| F | `MyWasStarved`는 아사 문턱 80% 지점에서 기록 | ✅ `IsNearStarvation` = `DepartAfterStarvingDays × NearStarvationRatio(0.8)` (VillagerAgent.cs:738) — HP 통합 시 이 희소성 보존 필수 |
| G | 전투 판정을 나를 기존 통로 없음 | ✅ ThreatService는 출몰 수명 전담, DisasterService는 시설 파괴, Injure는 부상의 문. "양방향 HP 교전"을 나르는 서비스 부재 → 신설 ② |
| H | 슬롯 예산 | ✅ 40/52 사용 (37~39 화폐 휴면). 이번 신설 = `MyHasWeapon` 1칸 → 41/52 |
| I | 원시 무기 enum | ✅ `ItemType.PrimitiveWeapon` (나무3+돌2) 정의만, 사용처 0 (ResourceType.cs:40). `Iron` 등도 정의만 |
| J | 직업 선반 자리 | ✅ JobSO.cs:53 주석에 격퇴 계열 append 지점 명시. `GatherDurationMults`는 미정의 자원 자동 1. 직업 풀 = `SimulationLoop._jobPool` (씬 직렬화) |
| K | Goal_Flee 구조 | ✅ Priority 105, Trigger `ThreatNear(21)==1`, Goal `ThreatNear==0`, 겁(Trait 5) 0.8/scale 20 — Goal_Fight가 같은 트리거·반대 가중치로 설 자리 |
| L | 연대기·신기록 | ✅ `EventId`·`ExitCause` append-only (ADR-M13-2). `RunRecordStore` = BestWinters/BestDay/BestPeakPop |
| M | HUD | ✅ `Notify` 1칸 덮어쓰기, `SetPrompt` 상시, `ComposeSelected` 정보줄 (SeasonHud.cs:207) — HP 표기는 정보줄에 얹는다 |
| N | WorldConfig.Threats 툴팁 낡음 | ⚠️ *"활성 티어 = 마을 규모"* 문언 잔존 (WorldConfigSO.cs:73~74) — M10R 이후 게임일 래칫인데 안 따라감. W9 문서 정합에서 수정 |
| O | 위협 에셋 값 | ✅ Tier1 Wolf UnlockDay 0/Period 6, Tier2 Pack UnlockDay 12/Period 5/VTC 0.5, Tier3 Bear — 마릿수·HP 필드는 전부 신설 append |

## 2. 참조 개편 표 (의미·단위가 바뀌는 심볼 — 재보정 전수)

| 심볼 | 현재 의미 | 개편 후 | 재보정 대상 (grep 전수) |
|---|---|---|---|
| `Injure(severity)` | 부상 상태 부여의 유일한 문 | **피해의 문**으로 확장 — 내부에서 HP 차감 + 부상 상태 파생 | ThreatService.ExecuteStrike (호출 유지), M10_InjuryGates (판정 함수 교체분) |
| `ShouldDie(neglect, cfg)` | 미간호 1.5일 → 사망 | 삭제 대상 아님 — **출혈 감쇠로 환산** (부상 중 HP/일 감소, 간호가 정지) . 사망 판정은 `Hp <= 0` 하나 | NextInjuryStateV2 · M10_InjuryGates · M11_CareGates |
| `ShouldStarveToDeath` | 굶주림 누적 → 즉사 | 삭제 대상 아님 — **아사 감쇠로 환산** (포만<문턱 지속 시 HP/일 감소). 사망 판정은 `Hp <= 0` 하나 | SimTick·M6-T3 게이트·`MyWasStarved` 지점 |
| `IsNearStarvation` | 아사 문턱 80% | **HP 기준으로 재정의** — 원인이 굶주림인 피해로 HP ≤ `MaxHp×(1−NearStarvationRatio)` 도달 (W1 구현 확정: 신필드 `NearDeathHpRatio` 대신 기존 `NearStarvationRatio` 재사용 — 0.8이면 HP 20% 선, 동일 효과·필드 0개) | M12-T7 게이트 |
| `CollectVictimCandidates(excludeInjured)` | 타격·추격 후보에서 부상자 제외 (1회 타격 시대의 중복 부상 방지) | **체류형은 부상자 포함** (재검사 A1 — 재타격 허용. 제외 유지 시 위협이 누구도 직접 못 죽이는 모순) | ExecuteStrike·TryPickChaseTile·M10_ThreatGates 중 제외 가정 게이트 (값 기반 역인덱스 대상) |
| `ADR-M10-8` (격퇴 불가) | 위협은 못 이긴다 | **폐기 → ADR-M21-1** | ThreatSO 헤더 주석 |
| `ThreatAgent` 수명 | 도착=타격=퇴장 | 체류형 — 도착 후 반복 타격, 도주선·사냥 판정으로만 퇴장 | NotifyArrived·StrikeAndExit·M10_ThreatGates(T3 등 수명 가정 게이트 역인덱스 — 값 기반 게이트 교훈) |
| `ExitCause` | Injury/Starvation/Unknown | append: **`Combat = 4`** (전투 피해 사망 — Injury와 분리해 연대기가 "늑대에게 죽었다"를 말할 수 있게) | ChronicleService·ComposeGameOver 표기 |

⚠️ **값 기반 게이트 역인덱스** (M20 교훈): 이름 grep으로 안 잡히는, "InjurySeverity를 순회하거나 사망 일수를 검산하는" 게이트를 W1·W2 착수 전에 역방향으로 훑는다 (`M10_InjuryGates`·`M11_CareGates`·`M10_ThreatGates` 전수 Read).

## 3. ADR — 구현 중 갈릴 판단을 미리 결정 (바꾸려면 사유를 커밋에)

### ADR-M21-1 · 북극성 개정 — "격퇴하며 버티는 기록 경주" (ADR-M10-8 폐기)
겨울 = 계절 시험(대비로 버팀), 위협 = 능동 시험(반격 가능). 연대기·기록 경주(M13~M15)는 북극성의 몸 그대로. 격퇴·사냥은 연대기에 사건으로 남아야 한다 — 검증 문장: *"3년차 봄, 늑대 다섯 적습을 사냥꾼이 앞장서 물리쳤다."* 舊 ADR-M10-8("격퇴 불가")은 폐기 — 당시 이유(위협 = 페이스메이커)는 래칫이 계승하고, 격퇴가 래칫을 되돌리지 못하게 하는 것으로 원 의도를 지킨다(ADR-M21-4).

### ADR-M21-2 · 주민 HP = 단일 생존 척도, 쓰기는 문 하나
- `Hp`는 VillagerAgent의 상태. **쓰기 문은 `TakeDamage(amount, DamageCause)` 하나** + SimTick의 자연 변화(아사 감쇠·출혈·간호 회복). 밖에서 Hp를 직접 만지지 않는다 (ADR-M0-3 정신).
- 기존 축의 재표현 (삭제 아님): 부상 상태·간호 2단계·치료사 완치·`MyWasAttacked`/`MyWasStarved`·사망 문(Die/StarveToDeath) 전부 유지. 바뀌는 것은 "죽음까지의 시계"가 일수 카운터에서 HP로 옮겨지는 것뿐.
- 아사는 즉사가 아니라 진행: 포만 < `StarvingBelowSatiety` 지속 시 HP가 깎인다. **HP 회복은 포만 충족 + (부상 시) 간호**로만 — 자연 회복으로 전투 피해가 공짜로 지워지면 위협이 다시 날씨가 된다.
- 플래닝 슬롯 아님: goal·액션은 HP를 조건으로 걸지 않는다 (이번 범위). HUD·판정만 읽는다. 슬롯 승격은 필요가 관측된 뒤 별도 결정.

### ADR-M21-3 · 판정은 CombatService 한 곳 — 에이전트=주민 전제 금지
피해 계산·위협 HP 차감·도주선·무리 도주선·사냥 판정·드랍은 전부 CombatService. ThreatService는 출몰 스케줄·수명, ThreatAgent는 표현+이동, VillagerAgent는 표현+자기 상태의 문(TakeDamage) 유지 — "위협은 표현+이동, 판정은 서비스"(M10-C) 확장. 타격자 식별은 인터페이스·ID 기반 — 몬스터·타 종족이 같은 문으로 들어온다 (CLAUDE.md 후반 확장 규칙 1).

### ADR-M21-4 · 래칫 티어 단조 유지 + 완충은 규모·간격만 (양방향, 클램프)
`UnlockDay` 시간 단조는 불가침 — 격퇴 실적이 티어를 되돌리지 못한다 (영구 안정 루프 구조적 차단, M10R 원 진단 유지). 완충은 **다음 출몰의 마릿수·간격**에만:
- 격퇴 성공(무리 격퇴/사냥) → 다음 출몰 마릿수 완화 (이김의 보상).
- 주민 사망 → 다음 출몰 마릿수 완화 + 간격 지연 (자비 — 손실이 마모가 아니라 리듬이 되게. 림월드 adaptation의 이중 축 그대로).
- 완충은 **1회 소모·클램프(최소 1마리)** — 누적 스택이 무한히 쌓여 위협 0이 되는 길 금지.

### ADR-M21-5 · 무기 = 배율, 게이트 금지 (맨손 가능)
무기 없어도 싸울 수 있다. 무기는 타격 간격 배율 (M20 헌장 문법 — 부재가 느껴지는 장치: 무기 없는 판은 전투가 느려 부상이 는다). `ItemType.PrimitiveWeapon` 재활용, 재료 = 나무 3 + 돌 2 (enum 주석의 기존 값 — 발명 아님). 소유는 개인 논리 슬롯 `MyHasWeapon` (분실·내구도 없음 — 이번 범위).

### ADR-M21-6 · 마릿수·HP는 에셋이 정의한다 (콘텐츠 비용표 유지)
위협 신필드는 전부 ThreatSO append (에셋 YAML은 **Edit만** — M19 교훈: 셸 재작성 금지). 완성 후 몬스터 종 추가 = 에셋 1개 + Threats 등록, 코드 0줄이 유지되어야 한다 (M10-G 리허설 대상).

### ADR-M21-7 · 참전은 하이브리드 — 자율(성향·직업) 기본 + 명령은 거부 가능
Goal_Fight와 Goal_Flee는 **같은 트리거(ThreatNear==1)·반대 성향 가중치** — 겁 많으면 피신이 이기고, 용감하거나 사냥꾼이면 반격이 이긴다. 플레이어 명령("싸워라")은 기존 명령·거부 시스템(JudgeOrder) 경유 — 완전 징집 금지 (정체성 C: 협상. 림월드 분석 결론). 명령을 쓸 시점은 화면이 알려준다 (적습 중 상시 표시 — W8).

### ADR-M21-8 · 위협의 죽음도 문 하나
위협 HP 차감·도주 전환·사망은 CombatService가 ThreatAgent의 문(`ApplyHit`→내부 상태) 경유로만. 도주 = 격퇴 사건, HP 0 = 사냥 사건 + 드랍. **드랍은 전역 RawFoodStock** (개인 아님 — 격퇴는 공용 무대, ADR-M20-9 전이 통로: 사냥꾼의 효율이 마을 비축으로 흐른다).

## 4. 수치 관계식 (전부 제안치 — 기존 값 우선 원칙, 조용히 넣지 않는다)

기존 값 위치: 아사 시계 = `AgentConfigSO.DepartAfterStarvingDays`, 부상 시계 = `InjuryDeathAfterDays(1.5)`·`InjuryRecoverDays(1)`. HP 환산은 이 시계들을 **보존**하도록 역산한다:

- `MaxHp = 100` (제안). 표기 정수.
- 아사 감쇠: 포만<10 지속 시 `StarveHpLossPerDay = 100 / DepartAfterStarvingDays` → 기존과 같은 날수에 죽는다 (아사 소요 시간 불변 — 검증된 밸런스 보존). W1 구현: 필드 0 = 자동 환산 (OnValidate 아닌 접근자 `StarveHpPerDay`).
- 출혈: 부상 상태·미간호 시 `BleedHpLossPerDay = InjuredBelowHp / InjuryDeathAfterDays` (W1 구현 확정 — 0 = 자동 환산. 부상 진입 시 HP ≤ InjuredBelowHp이므로 잔여분 기준. 검산: 67/1.5 ≈ 44.7/일 → 방치 1.5일 사망, 기존 시계 보존). 간호 중 출혈 정지(홀드 — 기존 규약 계승), 회복 = `TendRecoveryMult` 계승.
- HP 자연 회복: `SatedHpRegen`/일 (W1 구현 확정) — **굶주리지 않고 부상도 아닐 때만**. 부상 중 회복은 간호의 몫 (전투 피해가 시간만으로 지워지면 위협이 다시 날씨가 된다 — ADR-M21-2 "포만 충족" 경로의 실체).
- 위협 타격 피해: `ThreatSO.StrikeDamage = 34` (제안 — 3대 = 사망. 기존 "부상 1회 = 사망 아님" 보존: 1대는 부상+감속).
- 부상 상태 파생: 피격으로 `Hp < 67`이면 `Injury = Light` (기존 InjuredMoveSpeedMult 0.4·노동 goal 차단 그대로).
- 위협 HP: Tier1 늑대 60 / Tier2 무리 개체 60 / Tier3 곰 150 (제안). 주민 맨손 타격 피해 `VillagerHitDamage = 10`, 타격 간격 `CombatHitSec = 4초` → 맨손 1:1 늑대 = 6대·24초 (그동안 늑대도 1~2회 반격 — 맨손 솔로는 아프다).
- 무기 배율 `WeaponHitSecMult = 0.5` (2배 빠름), 사냥꾼 `CombatDurationMult = 0.5` — 곱 결합 (무장 사냥꾼 = 4배. M20 배율 문법).
- 개체 도주선 `FleeBelowHpPct = 0.4`, 무리 도주선 `RoutBelowPct = 0.5` (잔존 비율 < 50% → 전원 도주).
- 마릿수: `SpawnCount = clamp(SpawnCountBase + ⌊(day − UnlockDay) / CountGrowthEveryDays⌋ − 완충, 1, SpawnCountMax)`. 에셋 제안: Wolf 1/성장 없음/최대 1 · Pack 2/10일마다 +1/최대 5 · Bear 1/20일마다 +1/최대 2.
- 완충: 무리 격퇴 1회 → 다음 출몰 −1마리 (1회 소모). 주민 사망 1명 → 다음 출몰 −1마리 + 발동 +2일 지연 (1회 소모, 스택 상한 3).
- 정찰 예고 연장: `JobSO.WarnBonusDays = 1` (Job_Scout만) — 생존 정찰꾼 1명 이상이면 WarnDays에 가산 (인원 비례 아님 — 1명이면 족하다).

⚠️ 위 숫자들은 전부 **제안치**다. 구현 시 에셋에 넣고, Play 관측으로 조정한다. 기존 시계 값(아사·부상 일수)은 **바꾸지 않는다** — M17 값의 짝 규율.

### 4-R. W2R 수치 (배회·계절 — 전부 제안치, 근거 병기)

| 값 | 제안 | 근거 |
|---|---|---|
| `WanderRadiusTiles` | **5** | 기존 값 없음. `WanderRunner`의 주민 배회 반경(`RestByCampfire.WanderRadius: 4`)보다 한 칸 크게 — 늑대가 더 넓게 돈다. 타격 반경 3~4보다 커야 "돌다가 다가온다"가 성립 |
| `MaxStayDays` | 2 → **0.75** | 사용자 판정 "머무는 시간이 너무 길다". 재타격 주기 0.25일과 짝: **최대 3회 타격**(기존 최대 8회). 舊 1회 대비 3배 |
| `RepeatStrikePeriodDays` | 0.25 유지 | W2 값 보존 — 이번 개정의 불만은 주기가 아니라 정지·길이였다 |
| `HungrySeasonChanceMult` | **2.0** | 겨울에 타깃이 "주민 쪽으로 기운다"의 최소 표현. 클램프 1.0 |
| `HungrySeasonStayMult` | **2.0** | 겨울 체류 0.75 → 1.5일 (최대 6회 타격). "더 집요해진다" |
| Tier1 늑대 `VillagerTargetChance` | 0 → **0.25** | 🔴 M10 시절 "밭 전용" 결정의 개정. 사용자 판정 "주민을 안 치는 것도 문제". 겨울엔 ×2 = 0.5 |
| Tier2 무리 | 0.5 유지 | 겨울 ×2 → 1.0(클램프) = 겨울 무리는 반드시 주민을 노린다 |
| Tier3 곰 | 0.75 유지 | 겨울 ×2 → 1.0(클램프) |

⚠️ **평시 주민 타깃의 귀결**: 초반 사망률이 오른다. 게임 건강 기준(*"방치가 안락하면 실패"*, 이탈 0~2명 정상)상 방향은 맞으나 **수치는 Play 관측으로 조정**한다 — 특히 Tier1 0.25가 1년차에 과한지.

---

## §W1 — 주민 HP 신설 + 아사·부상 재표현

**대상**: `Assets/Scripts/M0/Agent/VillagerAgent.cs`, `Assets/Scripts/M0/Data/AgentConfigSO.cs`

**스케치**:
```csharp
// AgentConfigSO — append (전부 제안치, 툴팁에 환산 근거 병기)
public float MaxHp = 100f;
public float StarveHpLossPerDay;   // 0이면 자동 환산 100/DepartAfterStarvingDays (OnValidate)
public float BleedHpLossPerDay;    // 부상 미간호 출혈 (간호 중 정지)
// NearDeathHpRatio 신설 계획은 구현에서 철회 — 기존 NearStarvationRatio 재사용
// (hp ≤ MaxHp×(1−0.8) = 20% 선. 재검사 2026-08-06 소급 정정 — 필드 신설 0)
public float InjuredBelowHp = 67f;     // 피격 파생 부상 임계

// VillagerAgent
public float Hp { get; private set; }              // 세이브 대상 (ADR-M0-10 주석 규약)
public enum DamageCause { Combat, Starvation }     // 사망 문 라우팅 키
public void TakeDamage(float amount, DamageCause cause)  // 쓰기 문 하나 (ADR-M21-2)
{
    // Dead 무시 → Hp 차감 → cause==Combat && Hp<InjuredBelowHp → Injure 상태 파생
    // Hp <= 0 → cause별 기존 사망 문 호출 (Combat→Die 경로, Starvation→StarveToDeath)
}
// SimTick: 기존 _starvingDays 사슬을 HP 감쇠로 환산 —
//   포만<StarvingBelowSatiety → TakeDamage(StarveHpLossPerDay×Δ일, Starvation)
//   MyWasStarved = (원인 굶주림으로 Hp ≤ MaxHp×NearDeathHpRatio 도달) — 쓰기 지점 여전히 1곳
//   부상 중 미간호 → 출혈 감쇠 / 간호 누적 → 기존 완치 + Hp 회복
```

**DoD** (전부 수행 완료 — 커밋 `254f271`, EditMode 314/314 green):
- [x] 포만 0 방치 시 기존 `DepartAfterStarvingDays`와 같은 게임일수에 사망한다 — 게이트 `M21_T1_AutoConversion` + `M6_T3_StarveDeath_TimingPreserved`가 `DaysToStarveDeath == DepartAfterStarvingDays`를 검산 (배포 에셋 포함, `M21_T3`)
- [x] 부상 미간호 사망 소요가 기존 1.5일과 같은 자릿수다 — `DaysToBleedDeath == InjuryDeathAfterDays`(`M10_T1_InjuryDeath_TimingPreserved`) + 안정화 타임라인이 체력 루프에서도 25스텝(≈2.5일) 유지(`M11_T8`)
- [x] `MyWasStarved`가 굶주림 원인 HP 저점에서만 true — 쓰기 지점 1곳(`TakeDamage`, grep 확인) + `cause == Starvation` 라우팅. 선 위치는 `M21_T2_NearDeathLine`이 舊 지점과 일치 확인
- [x] Threats 없는 판에서 HP 관련 로그가 아사 감쇠 외 0건 — `Injure()` 호출처가 `ThreatService` 1곳뿐(grep), Threats 비면 서비스가 null이라 부상·출혈 경로 자체가 없음
- [x] `Die`/`StarveToDeath` 외 사망 경로 0 — `State = AgentState.Dead` 대입 지점 2곳(grep), 둘 다 `TakeDamage`의 원인 라우팅으로만 진입

**⚠️ 오해 위험**: ①"HP가 생겼으니 기존 `_starvingDays`·부상 카운터를 지운다" — 지우는 건 **판정**이지 게이트가 검산하는 **시계**가 아니다. 소요 일수가 달라지면 검증된 밸런스가 무너진다 (환산 검산이 DoD 1·2번). ②"전투 피해도 MyWasStarved를 찍는다" — 아니다, 원인 라우팅 필수 (찍으면 경험 우회 ExperienceOverrideWhen이 전투마다 발동해 희소성 소멸). ③"Hp를 public set으로 열어 ThreatService가 직접 깎는다" — 문(TakeDamage) 경유만.

**작업량**: 대 / **심각도**: 🔴 (검증된 사망 축 수술) / **의존**: 없음 (첫 커밋)

## §W2 — 위협 HP·체류·이중 도주선 (수명 주기 개정)

**대상**: `Assets/Scripts/M0/Data/ThreatSO.cs` (append), `Assets/Scripts/M0/Presentation/ThreatAgent.cs`, `Assets/Scripts/M0/World/ThreatService.cs`, 에셋 3종 (**Edit만**)

**스케치**:
```csharp
// ThreatSO — append
public float MaxHp = 60f;
public float FleeBelowHpPct = 0.4f;     // 개체 도주선
public float StrikeDamage = 34f;        // 주민 타격 피해 (Injure 심각도 대체 아님 — W1 파생 유지)
public float RepeatStrikePeriodDays = 0.25f; // 체류 중 재타격 주기
public float MaxStayDays = 2f;          // 체류 안전 상한 (기브업 패턴 — 도달 불가 지형 보루)

// ThreatAgent — 상태 추가: Hp, _fleeing. 도착 후 퇴장 대신 체류 모드:
//   반복: 재타겟 추격 → 사거리 → svc.NotifyStrikeTick(this) (주기 판정은 서비스)
//   _fleeing 전환 시에만 퇴장 경로. DespawnNow는 유지.
// ThreatService —
//   NotifyArrived → NotifyStrikeTick으로 개명 개편: 주기 충족 시 ExecuteStrike 1회 (퇴장 없음)
//   ExecuteStrike의 Injure(Light) 호출 → victim.TakeDamage(so.StrikeDamage, Combat) 로 교체
```

**재검사 결정 A1 (2026-08-06)**: 체류 재타격의 후보 수집은 **부상자를 제외하지 않는다** — `CollectVictimCandidates(excludeInjured: false)` (기존 `true`는 "타격 1회 후 퇴장" 시대의 중복 부상 방지였고, 체류형에서 유지하면 위협이 누구도 직접 못 죽이는 모순 — §4 "3대=사망"과 충돌). 중복 `Injure`는 여전히 무시(상태는 Light 유지)하되 **`TakeDamage`는 매 타격 적용** — HP 0 도달 = 직접 살상, `ExitCause.Combat`. `TryPickChaseTile`도 부상자 포함으로 개정 (감속 0.4 부상자는 따라잡히면 죽는다 — 치사율 유지 방침, 개입 창이 열리는 지점).

**DoD** (구현 완료 — EditMode 317/317 green, 신규 게이트 3종 red 확인 완료):
- [x] 늑대가 타격 후 퇴장하지 않고 체류하며 `RepeatStrikePeriodDays`마다 재타격한다 — `NotifyArrived`가 더는 퇴장 경로를 주지 않고(체류 시작), `SetExitPath` 호출처가 `BeginExit` **1곳뿐**(grep). 주기 판정 = `M21_T4_ShouldRepeatStrike_PeriodBoundary`(경계·0·음수). ⏳ **로그 간격 실측은 Play 잔여**
- [x] `MaxStayDays` 도달 시 스스로 퇴장한다 — `NotifyStrikeTick`이 상한을 재타격보다 **먼저** 판정(마지막 프레임에 한 대 더 치고 나가지 않게). 게이트 `M21_T5`. 로그 문구로 지형 보루("닿지 못하고")와 도주("물러난다")를 구분. ⏳ 방치 시나리오 실측은 Play 잔여
- [x] 밭 타깃 출몰도 체류·반복 파괴한다 — 밭형도 도착 시 `Arrive()`→체류 공용 경로를 타고, `ExecuteStrike`의 타깃 종류 분기(ADR-M10R-3)는 무수정
- [x] 부상자 재타격으로 HP 0 도달 시 전투 사망이 성립한다 — `excludeInjured` 인자가 호출처 4곳 **전부 false**(grep, A1), 타격이 `Injure` 대신 `TakeDamage(StrikeDamage, Combat)`를 호출하고 부상은 그 결과로 파생. 3대=사망은 `M21_T1_FirstStrike_InjuresButDoesNotKill`
- [x] 신규 필드 기본값 에셋이면 컴파일만으로 기존 판과 동일 (OnValidate 경고 0) — 배포 에셋 3종 정합 게이트 `M21_T6`, 실행 로그 `[ThreatSO]` 경고 **0건**

> **W2의 실측 (Play, 2026-08-06)** — ①② 확인 / ③ 미관측:
> - ✅ ①체류·반복 타격 ②상한 퇴장 — 사용자 Play에서 기대 로그가 **순서대로 출현**
>   (`출몰 → 체류 시작 → 반복 타격 → 퇴장`). ※ 0.25일 **간격의 수치 검산은 별도로 하지 않았다**
>   — 반복이 도는 것까지가 확인 범위다.
> - ⏳ ③체류 중 재추격(도망친 주민 재포착)은 **아직 안 밟혔다**: 이번에 온 Tier1 늑대는
>   `VillagerTargetChance: 0`(밭 전용)이라 주민 추격 경로 자체가 실행되지 않는다.
>   **Tier2(무리, UnlockDay 12·주민 확률 0.5) 출몰 때 관측할 것.**
>
> 🔴 **①②가 이번에야 관측된 이유** — 그전까지 밭 타깃 출몰은 **전부 생략되고 있었다**
> (`b51c631`: M11-E가 `FarmPlot.ZoneRadius`를 0으로 내린 뒤 밭 구역이 미등록 → 목적지가
> 기지 폴백 → 집이 그 타일을 막아 JPS Unreachable). 늑대가 도착한 적이 없으므로 W2 체류는
> 한 번도 실행되지 않았다. **그 수정 전에 W2를 판정했다면 "체류가 안 된다"는 오판이 나왔다.**
>
> 🔎 **관측 예정 수치 드리프트**: `StrikeDamage=34`가 부상선(67)을 1 넘어서므로 부상 진입 체력이
> 67이 아니라 66이다 → 출혈 사망이 1.5일이 아니라 **1.478일**(−1.5%). §4 제안치의 산술적 귀결이며
> 게이트는 통과한다(순수 함수는 부상선 기준). 리뷰① HP 환산 검산 때 함께 볼 것.

**⚠️ 오해 위험**: ①"체류형이니 NotifyArrived·SetExitPath를 삭제" — 퇴장 경로 부여는 도주 전환이 그대로 쓴다. ②"위협 HP를 ThreatAgent가 스스로 깎는다" — 차감 판정은 CombatService(W3), 개체는 문만 (ADR-M21-8). ③"에셋에 필드를 넣으며 YAML 셸 재작성" — **Edit만** (M19 교훈: 재작성하면 GUID·미기재 필드 소실). ④"부상자 재타격이니 중복 Injure도 허용" — 아니다, 부상 상태는 Light 1단 유지·HP만 깎인다 (부상 계단 신설은 스코프 밖).

**작업량**: 대 / **심각도**: 🔴 / **의존**: W1 (TakeDamage 문)

## §W2R — 배회형 위협 + 계절 연동 (W2 개정, 2026-08-06 Play 관측)

> **왜 개정하나**: W2의 체류가 화면에서 *"원 하나가 밭 위에 서서 콘솔에만 로그를 뿜는 것"*으로
> 나왔다. 사용자 판정 = **"이러한 늑대 무리의 체류는 내가 원한 게 아니다."**
> 다만 W2가 풀려던 문제(*한 대 치고 사라지면 맞설 대상이 없다*)는 그대로 남아 있으므로,
> **머무르되 정지하지 않는** 형태로 옮긴다. 브레인스토밍 합의(M19 3게이트 통과) 한 줄:
>
> > 출몰한 늑대는 정지하지 않고 마을 주변을 **배회**하다 **짧게** 치고 떠나며, **평시에도 가끔
> > 주민을 노리되** 겨울이 오면 타깃이 주민 쪽으로 기울고 더 집요해진다 — 늑대의 그림이 아니라
> > **동선과 주민의 반응**으로 그것이 화면에 보인다.

### 전제 확인 (2026-08-06 코드 실사 — 이 명세는 이 사실 위에만 선다)

| # | 전제 | 실사 결과 |
|---|---|---|
| P | 도착 후 정지한다 | ✅ W2의 `ThreatAgent.Arrive()`가 `_path = null`로 굳힌다 — 되돌릴 지점 |
| Q | 계절 → 위협 통로가 있다 | ❌ **없다.** `ThreatService` 생성자에 `SeasonService`가 없다 → **신설**(넓히기 ②) |
| R | "겨울" 판정에 `IsCrisis`를 쓴다 | 🔴 **쓰면 안 된다.** `Season_Summer`도 `IsCrisis: 1`이다. **겨울만 참인 것은 `ForageFrozen`**(Winter만 1, Mild·Summer·Autumn 0) |
| S | 배회 목표 선정 통로가 있다 | ✅ `MapBounds.PickWalkableNear(walkable, cx, cy, radius)` — `WanderRunner`(주민 여가 배회)가 이미 쓴다 |
| T | `ThreatService`가 통행을 안다 | ⚠️ 모른다. `Func<int,int,bool>` 주입 필요 (`WandererService`가 `_sim.Walkable`을 직접 읽는 선례) |
| U | 생성자 변경의 파급 | ✅ **`SimulationLoop` 1곳뿐.** 게이트는 static 순수 함수만 쓴다(`M10_ThreatGates` 전수 확인) |
| V | `ZoneService` 참조 | ✅ `b51c631` 이후 **미사용** — 생성자 슬롯 교체 후보 |
| W | 주민 반응 통로 | ✅ `Goal_Flee`(우선순위 105) + `ThreatNear`(슬롯 21) 기존 배선 — 재사용 |
| X | Tier1 늑대의 주민 확률 | ✅ `VillagerTargetChance: 0`(밭 전용) — "평시에도 가끔 주민"의 개정 대상 |
| Y | 대상 0일 때 퇴장 | ⚠️ **주민형만 배선돼 있고 밭형엔 없다** — W2의 결함(밭 0/0 무한 반복의 정체) |
| Z | 배회에 난수를 써도 되나 | `WanderRunner`가 이미 `Random`을 쓴다(표현 축 선례) → ADR-M21-10으로 경계를 못박는다 |

### 참조 개편 표 (뜻이 바뀌는 심볼)

| 심볼 | 현재 의미 | 개편 후 |
|---|---|---|
| `MaxStayDays` | 체류(정지) 안전 상한 2일 | **배회 체류 상한** — 값 축소(§4). 지형 보루 역할은 유지 |
| `VillagerTargetChance` | 출몰별 고정 확률 | **평시 기저값** — 배고픈 계절엔 배율이 곱해진다 |
| `RepeatStrikePeriodDays` | 정지 중 재타격 주기 | 배회 중 재타격 주기 (의미 동일) |
| `ThreatService(zones, …)` | 밭 구역 앵커 조회용 | **`ZoneService` 제거 → `SeasonService` + `isWalkable` 주입** (V·Q·T) |

**대상**: `Assets/Scripts/M0/Data/ThreatSO.cs` (append 3필드), `Assets/Scripts/M0/Presentation/ThreatAgent.cs` (정지 → 배회), `Assets/Scripts/M0/World/ThreatService.cs` (계절 주입 + 순수 판정 3종 + 밭형 대상 0 퇴장), `Assets/Scripts/M0/SimulationLoop.cs` (생성자 인자 교체 — 유일한 생성처), `Assets/Tests/EditMode/M21_CombatGates.cs` (게이트 `M21_T7` append), 에셋: `Assets/M0Config/Threats/*.asset` 3종 (**Edit만** — M19 교훈)

### 스케치

```csharp
// ThreatSO — append (전부 제안치, §4-R 참조)
[Header("배회 (M21-W2R)")]
public int WanderRadiusTiles = 5;      // 도착 지점 기준 배회 반경
public float HungrySeasonChanceMult = 2f;  // 배고픈 계절 주민 타깃 배율 (클램프 1.0)
public float HungrySeasonStayMult   = 2f;  // 배고픈 계절 체류 상한 배율 (더 집요하다)

// ThreatService — 계절은 ForageFrozen으로 읽는다 (ADR-M21-9)
/// <summary>이 계절에 야수가 굶는가 (순수 — 게이트 M21-T7). 야생 먹이가 얼면 늑대도 굶는다.
/// ⚠️ IsCrisis가 아니다 — 여름도 위기라 그걸 쓰면 여름 늑대가 사나워진다.</summary>
public static bool IsPredatorHungry(SeasonSO s) => s != null && s.ForageFrozen;

/// <summary>계절 반영 주민 타깃 확률 (순수 — 게이트 M21-T7). 클램프 [0,1].</summary>
public static float EffectiveVillagerChance(float baseChance, bool hungry, float mult)
    => Mathf.Clamp01(hungry ? baseChance * Mathf.Max(1f, mult) : baseChance);

/// <summary>계절 반영 체류 상한 (순수 — 게이트 M21-T7).</summary>
public static float EffectiveStayDays(float baseDays, bool hungry, float mult)
    => hungry ? baseDays * Mathf.Max(1f, mult) : baseDays;

// RollTargetsVillagers 는 시드 롤을 유지하되 확률만 계절 보정을 받는다 (ADR-M10R-2 결정성 불변)

// ThreatAgent — 정지 폐기, 배회로 교체
//   Arrive() : _path=null 로 굳히던 것을 → 첫 타격 후 곧바로 배회 목적지를 깐다
//   Update() : 배회 경로 소진 = 다음 배회 목적지 재선정 (앵커 = 도착 지점, 반경 = WanderRadiusTiles)
//   ⚠️ 주민 타깃형은 기존 추격이 우선 — 배회는 "칠 대상이 사거리 밖일 때"의 기본 행동
```

### DoD

- [x] 늑대가 도착 후 **정지하지 않는다** — 배회 목적지가 바뀌며 위치가 계속 변한다 (Play: 동선 관측)
      → 2026-08-07 Play 실측: 체류 개체 4마리의 위치를 두 시점(Day 5.345 / 5.466)에 샘플해 **전원 이동 확인**
      (`(33,14)→(32,16)` · `(-33,-12)→(-35,-9)` · `(-29,-13)→(-29,-19)` · `(40,22)→(34,15)`).
- [x] 밭 타깃이 **칠 대상을 다 없애면 물러난다** — `[Threat] 퇴장 개시 … 칠 대상이 없다` 1줄, `밭 0/0 소실` 반복 **0건** (Y 결함 수정)
      → Play 실측 2건(`id-17824`·`id-17800`): 도착 직후 `마지막타격일 = 0.000` + `퇴장중 = True`
      = **한 번도 치지 않고** 물러났다. 상한(0.75일) 도달 전이므로 상한 퇴장과 구분된다.
- [x] `Season_Winter`에서만 주민 타깃 확률·체류 상한에 배율이 걸린다 — `Season_Summer`(IsCrisis=1)에서는 **안 걸린다** (게이트 `M21_T7`이 `IsPredatorHungry` 경계 검사)
      → 게이트 `M21_T7_IsPredatorHungry_IsForageFrozen_NotIsCrisis` + `M21_T7_ShippedSeasons_OnlyWinterFeedsTheTrap`.
      **red-check 완료**: `IsCrisis`로 바꾸자 두 게이트가 red(`여름 Expected False But True` / `배고픈 계절 2개`).
      Play 보강: 여름(IsCrisis=1) 진행 중 전 개체의 체류 상한이 0.75 유지(1.5 아님).
- [x] Tier1 늑대가 **평시에도 주민을 노리는 출몰이 존재한다** — 출몰 서수를 바꿔가며 `RollTargetsVillagers`가 true를 내는 서수가 있다 (게이트, 결정적)
      → 게이트 `M21_T7_Tier1Wolf_TargetsVillagersInPlainSeasons` (red-check: 확률 0으로 되돌리면 red).
      Play: 여름에 주민 타깃 늑대가 실제로 출몰·체류·2회 타격.
- [x] 배회 목적지는 **통행 가능 타일만** — 집 위로 가지 않는다 (`PickWalkableNear` 경유, 게이트 불가 → Play 관측)
      → Play 실측: 체류 개체 전수에서 `통행불가 타일 위 = 0`, 앵커 기준 **최대 체비셰프 거리 = 5 = 반경 그대로**(초과 0).
- [x] 신규 필드 기본값으로 기존 판과 동일 — `OnValidate` 경고 0, EditMode 전체 green
      → EditMode **332/332** (326 → 332, `M21_T7` 6종 신설). 콘솔 `[ThreatSO]` 경고 0 · 에러 0.

### 리뷰⓪ 결과 (사용자 Play, 2026-08-07)

- [x] **① 동선 — 통과.** 사용자 판정: *"동선이 아주 잘 보여. 이전에는 가만히 멈춰있었지만 지금은
      돌아다니는 게 확실히 잘 보여."* → **W3 착수 차단 해제** (이 항목이 유일한 차단 조건이었다).
      🔑 W2 를 뒤집었던 바로 그 축에서 반대 판정이 나왔다 — 고친 것이 맞는 것을 고쳤다는 뜻이다.
- [ ] ② 평시 주민 타깃 출몰이 오는가 — 미확인 (차단 아님)
- [ ] ③ 초반 사망률이 과하지 않은가 — 미확인 (차단 아님). `Tier1 VillagerTargetChance 0.25` 는
      제안치이고 §4-R 이 "1년차에 과한지 Play 로 조정"이라 미리 적어 뒀다. **W3 진행 중에도
      관측이 쌓이면 그때 조정한다** — 이 값은 코드가 아니라 에셋 한 칸이다.

**W2R에서 Play가 잡은 것 (게이트는 green이었다)**: "칠 대상이 없다" 퇴장을 밭형·주민형 **양쪽에**
걸었더니 주민 타깃 늑대가 상한 전에 전부 퇴장했다 — 주민은 도망치므로 "사거리 밖"이 "대상 없음"과
같지 않기 때문이다. 밭형에만 남기고, 주민형은 사거리 밖일 때 **타격만 거르고 추격을 잇도록** 고쳤다
(퇴장 조건은 W2와 같이 `TickChase`의 생존 주민 0명). W2가 게이트 green인 채 뒤집힌 것과 같은 종류다.

### ⚠️ 오해 위험

1. **"겨울 = `IsCrisis`"** — 🔴 아니다. `Season_Summer`도 `IsCrisis: 1`이라 그걸 쓰면 **여름 늑대가 사나워진다.** 판정은 `ForageFrozen` 하나 (ADR-M21-9).
2. **"배회니까 목적지를 매 프레임 새로"** — 아니다. 목적지에 **도착했을 때만** 재선정한다. 매 프레임이면 제자리 떨림이 되고 경로탐색이 폭증한다 (`ADR_경로탐색_확장경계.md`).
3. **"배회가 추격을 대체한다"** — 아니다. 주민 타깃형은 **추격이 우선**이고, 배회는 사거리 밖일 때의 기본 행동이다. 뒤집으면 주민을 못 잡는다.
4. **"확률이 들어갔으니 ADR-M10-1 위반"** — 배회 **동선**은 표현이라 난수 허용(ADR-M21-10). **타깃 종류·출몰 스케줄은 시드 롤 유지** — 그쪽에 난수를 넣으면 위반이다.
5. **"`MaxStayDays`를 줄였으니 지형 보루가 사라진다"** — 역할은 그대로다. 값만 줄었고, 배회는 여전히 이 상한으로 끝난다.
6. **"`ZoneService`를 빼면 밭 구역 기능이 죽는다"** — 이미 죽어 있다(`b51c631`: `FarmPlot.ZoneRadius = 0`이라 등록 자체가 안 된다). 빼는 것이 실상을 코드에 맞추는 것이다.

**작업량**: 중 / **심각도**: 🔴 (W2 수명 재수술) / **의존**: W2 (되돌리는 대상)

---

### ADR-M21-9 · "배고픈 계절"의 판정은 `ForageFrozen` 하나

계절이 위협의 **타깃과 공격성**을 바꾼다. 판정 슬롯은 `SeasonSO.ForageFrozen`이다 — *야생 먹이가 어는 계절이면 늑대도 굶는다.* 의미가 정확히 겹치므로 **새 필드를 만들지 않는다**(기존 통로 우선, M11).
🔴 `IsCrisis`를 쓰면 안 된다: `Season_Summer`도 `IsCrisis: 1`이라 여름 늑대가 사나워진다 — 실사에서 잡힌 함정이다.
⚠️ **갈라지는 조건**: "채집은 막히는데 야수는 안 굶는" 계절이 생기면 그때 전용 필드로 분리한다. 그전까지 재사용이 옳다.
계절이 바꾸는 것은 **타깃 확률·체류 상한 둘뿐**이다 — 출몰 빈도·피해량은 게임일 래칫(ADR-M10R-1)이 그대로 소유한다.

### ADR-M21-10 · 난수의 경계 — 동선은 표현, 타깃은 판정

배회 **목적지 선정에는 난수를 허용**한다(`MapBounds.PickWalkableNear`, `WanderRunner` 선례). 같은 판에서 늑대가 매번 같은 길로 걸으면 그게 더 이상하다.
🔴 **타깃 종류·출몰 스케줄·희생 선정은 시드 롤을 유지**한다 (ADR-M10-1·M10R-2). 경계선: **"무엇을 하는가"는 결정적, "어떻게 걷는가"는 확률**.

## §W3 — CombatService 신설 (판정의 집)

**대상**: `Assets/Scripts/M0/World/CombatService.cs` (신설 ②), `Assets/Scripts/M0/SimulationLoop.cs` (조립)

**스케치**:
```csharp
/// 전투 판정 (M21 — ADR-M21-3). 위협은 표현+이동, 주민은 표현+자기 문 — 판정은 여기.
/// 에이전트=주민 전제 없음: 타격자는 ID+배율만 넘긴다 (몬스터·타 종족 공유 대비).
public sealed class CombatService
{
    // 주민→위협 타격: 피해 적용, 도주선·사냥 판정, 드랍·연대기·완충 통지
    public HitOutcome VillagerHit(string attackerId, float damage, ThreatAgent target);
    // 순수 판정 (게이트 대상)
    public static bool ShouldFlee(float hp, float maxHp, float fleeBelowPct);
    public static bool ShouldRout(int aliveAndFighting, int spawned, float routBelowPct);
    public static float HitInterval(float baseSec, float jobMult, bool hasWeapon, float weaponMult);
    // 사냥 드랍: 전역 RawFoodStock += so.MeatDrop (World 경유 — ADR-M21-8)
}
```
무리 도주선: 같은 출몰 서수의 스폰 그룹을 ThreatService가 등록, CombatService가 잔존율 판정 → 잔여 전원 `BeginFlee()`.

**DoD**:
- [ ] `[Combat] 타격/격퇴/사냥` 로그 3종이 각 판정 지점에서 1회씩만 (S1·S2)
- [ ] 순수 함수 3종이 에디터 게이트로 검증 가능 (인스턴스·씬 의존 0)
- [ ] Threats 비면 CombatService도 null (중립 불변식 — SimulationLoop 조립 분기)

**⚠️ 오해 위험**: ①"ThreatService에 판정을 얹는다" — 신설이 결정 사항 (출몰 수명과 교전 판정은 다른 책무. 주민 HP·아사는 위협과 무관한데 ThreatService가 들면 어색). ②"VillagerAgent 타입을 시그니처에 받는다" — 타격자는 ID+수치로 추상화 (전제 금지 규칙).

**작업량**: 중 / **심각도**: 🟠 / **의존**: W1·W2

## §W4 — Goal_Fight + FightRunner (자율 참전) + 명령 통로

**대상**: `Assets/M0Config/Goals/Goal_Fight.asset` (신설), `Assets/Scripts/M0/Data/FightActionSO.cs`, `Assets/Scripts/M0/Agent/Runners/FightRunner.cs`, `Assets/Scripts/M0/UI/PlayerInputController.cs` (명령 1종)

**스케치**: Goal_Fight = Priority 105 (Flee와 동률 — 성향 보정이 승부를 가른다), Trigger `ThreatNear==1`, Goal `ThreatNear==0`, TraitWeights 겁 **−0.8** (Flee의 +0.8과 대칭 — 겁 낮은 자만 반격이 이긴다), `AllowedWhenInjured=0` (부상자는 못 싸운다 — 기존 goal 필터 재사용). FightActionSO 효과 = `ThreatNear Set 0` (Flee 액션과 같은 파생 슬롯 규약 — 실제 값은 서비스가 재평가). FightRunner: 최근접 위협으로 접근(`TryGetNearestThreatPos` 재사용) → 인접 시 `HitInterval` 대기 → `CombatService.VillagerHit`. 위협 소멸·도주 시 완료. 명령: 기존 보상 명령 문법으로 Goal_Fight 지시 — 거부 판정은 JudgeOrder 그대로 (새 거부 축 없음).

**재검사 결정 A2·B1 (2026-08-06)**:
- **A2 교전 지속** — 진행 중인 FightRunner는 피격·부상으로 **중단하지 않는다** (부상 필터는 다음 참전 선발에서만 작동. §4 검산 "맨손 6대 24초 vs 재타격 25초 — 맨손 솔로는 승부 직전 1대 맞는다"의 해석 = "아프지만 이긴다". 인접한 위협 앞에서 감속 0.4로 이탈하는 것이 오히려 자살이라 지속이 생존적). 성향(겁) 기반 중도 이탈 판정은 **이월** — 자율 참전자는 이미 성향 필터를 통과한 겁 낮은 자들이라 발동 모수가 적다(주로 명령으로 강제 참전한 겁쟁이). Play 관측 후 필요가 보이면 CombatService 순수 함수로 추가 (선반 위 물건 자리 확보).
- **B1 명령 원정 특례** — 명령된 Goal_Fight는 **트리거(ThreatNear==1)·즉시 달성 검사를 우회**하고 러너가 최근접 위협 위치로 원정한다 (완료 = 위협 소멸·도주). 특례 없으면 밭 타깃 체류 위협에 원거리 주민은 ①트리거 미발동=자율 불가 ②명령해도 goal 조건(ThreatNear==0)이 이미 만족돼 빈 플랜 즉시 완료 = 명령 무의미 — W8 "⚔ 습격 중" 상시 프롬프트가 개입을 권하는데 수단이 없는 공수표가 된다. 부상 거절·JudgeOrder·보상 문법은 기존 그대로. **자율 참전은 근접(ThreatNear) 유지** — 특례는 명령에만.

**DoD**:
- [ ] 겁 높은 주민은 피신, 겁 낮은·사냥꾼은 반격 (같은 판에서 두 행동 분화 로그 — M12 분화 검증 방식)
- [ ] 명령 "싸워라"가 JudgeOrder를 경유하고 배고픈 주민이 거부한다 (S6)
- [ ] 부상자는 Goal_Fight 후보에서 빠진다 (BlockedByInjury 재사용 확인) — 단 교전 중 부상은 러너를 끊지 않는다 (재검사 A2)
- [ ] 전투 중 재타겟: 위협이 도주하면 러너가 다음 위협 또는 완료로 넘어간다 (무한 추격 0)
- [ ] 원거리 주민에게 "싸워라" 명령 시 위협 위치로 원정한다 (밭 타깃 체류 시나리오 — 빈 플랜 즉시 완료 0건, 재검사 B1)

**⚠️ 오해 위험**: ①"Fight가 Flee보다 우선순위가 높아야 반격이 나온다" — 아니다, 동률+성향 대칭이 설계다 (높이면 겁쟁이도 싸운다). ②"명령 전용 거부 사유(용기)를 신설" — 금지, 거부는 욕구 2축 유지 (ADR-M1-2). 성향은 goal 순위에서 이미 말한다. ③"FightRunner가 위협 HP를 직접 깎는다" — 판정은 서비스 (⚠️③ 계보). ④"원정 특례를 자율 참전에도 적용" — 금지, 자율은 근접 트리거 유지 (전 맵 주민이 밭 늑대로 몰려가는 과잉 반응 — 특례는 플레이어 개입의 값이다).

**작업량**: 중 / **심각도**: 🟠 / **의존**: W3

## §W5 — 원시 무기 (제작 + 배율)

**대상**: `Assets/Scripts/M0/Data/SlotId.cs` (append `MyHasWeapon = 40`), `Assets/M0Config/Goals/Goal_ArmSelf.asset` + `Actions/Action_CraftWeapon.asset` (신설), `Assets/Scripts/M0/Agent/Runners/CraftRunner.cs` (소형 신설), `EffectApplier`/`WorldModel` 배선

**스케치**: 슬롯 41/52. Goal_ArmSelf: Trigger `MyHasWeapon==0` + `ThreatNear==0` (전투 중 제작 금지) — 우선순위는 낮게 (여가 위·생존 아래, 제안 30). 액션 전제 `WoodStock≥3, StoneStock≥2`, 효과 `Sub Wood 3, Sub Stone 2, Set MyHasWeapon 1`. 실제 소유 쓰기 = 러너 완료 시 `VillagerAgent.AcquireWeapon()` (MyWasAttacked 패턴 — 영구 bool, 세이브 대상. 슬롯 원천 = 에이전트). `HitInterval`에 `hasWeapon` 배선.

**DoD**:
- [ ] 자원 충족 시 주민이 스스로 무기를 만든다 (플랜 로그: 채집→제작 사슬 확인)
- [ ] 무장 주민 타격 간격 = 맨손 × 0.5 (S5 로그 검산)
- [ ] `IsNumeric`·`SlotIds.Count=41` 갱신 + 기존 에셋 인덱스 불변 (append-only)
- [ ] 무기 없어도 Goal_Fight는 발동한다 (게이트 금지 — ADR-M21-5)

**⚠️ 오해 위험**: ①"무기를 전역 스톡으로" — 개인 소유다 (내 손의 창은 나눠지지 않는다. 나눔 축은 이번 범위 밖). ②"Goal_Fight 전제에 MyHasWeapon 추가" — 절대 금지 (게이트가 된다). ③"ConsumeRunner 재사용" — 식사 러너에 제작을 얹으면 몸값 불가침(ADR-M5-3) 감시 경계가 흐려진다. 소형 CraftRunner 신설이 결정.

**작업량**: 중 / **심각도**: 🟡 / **의존**: W4 (배율 소비처)

## §W6 — 대규모 적습 (마릿수 확장형)

**대상**: `ThreatSO` (append: `SpawnCountBase=1`, `CountGrowthEveryDays=0`, `SpawnCountMax=1`, `MeatDrop=2`), `ThreatService.Spawn` (다중 스폰 + 그룹 등록), 에셋 3종 Edit

**스케치**: `Spawn`이 §4 산식으로 count 산출 → 같은 진입점·서수로 count개 생성 (진입 타일 인접 산개 — 겹침 방지 오프셋). 그룹 = 출몰 서수 키. 무리 도주선 판정은 W3.

**DoD**:
- [ ] Pack 밴드 Day22에서 3마리 스폰 (산식 검산: 2 + ⌊10/10⌋)
- [ ] 그룹 2/4 사냥·도주 시 잔여 전원 도주 + `[Combat] 적습 격퇴` 로그 1줄 (S3)
- [ ] count=1 에셋(Wolf)은 기존과 완전 동일 동작 (중립)
- [ ] 예고는 마릿수를 말하지 않는다 (정찰 재탄생 여지 — 아는 것의 가치는 W8 이후 관측으로)

**⚠️ 오해 위험**: ①"마리마다 예고" — 예고는 출몰 1회 그대로. ②"count를 래칫 밴드 에셋 밖(코드 상수)에서 성장" — 산식 계수는 전부 에셋 필드 (콘텐츠 비용표). ③"산개 오프셋으로 통행 불가 타일에 스폰" — 진입점 유효성은 기존 Unreachable 생략 규약을 마리 단위로 적용.

**작업량**: 중 / **심각도**: 🟠 / **의존**: W2·W3

## §W7 — 래칫 양방향 완충 (ADR-M21-4)

**대상**: `ThreatService` (완충 상태 + 순수 함수), `CombatService` (격퇴 통지), `VillagerAgent` 사망 통지 배선 (기존 사망 문에서 1줄)

**스케치**:
```csharp
// ThreatService — 세이브 대상 append: _reliefStacks(int, 상한 3), _delayDays(float)
public static int RelievedCount(int scheduledCount, int reliefStacks); // max(1, n−s)
// 다음 발동 시각 = _lastStrikeDay + Period + _delayDays (소모형)
// 격퇴 성공 → 스택 +1 / 주민 사망(원인 무관) → 스택 +1, 지연 +2일. 스폰 시 전부 소모.
```

**DoD**:
- [ ] 격퇴 다음 출몰이 −1마리 (로그 두 출몰 비교)
- [ ] 주민 사망 다음 발동이 +2일 늦다 (스케줄 로그 검산)
- [ ] 스택 상한 3 — 연속 격퇴 4회에도 다음 출몰 ≥ 1마리 (영구 안정 불가 게이트)
- [ ] `UnlockDay` 판정(PickTier)은 손대지 않는다 (diff 0 — 티어 단조 불가침)

**⚠️ 오해 위험**: ①"자비를 티어 강등으로" — 절대 금지, 마릿수·간격만 (ADR-M21-4). ②"사망 완충이 M10R가 막은 루프의 재발" — 아니다: 옛 루프는 *마을 규모* 축이 사망으로 줄며 티어가 강등되는 **영구** 하향이었고, 이번은 1회 소모·클램프·티어 불변의 **한시** 완충이다 (이 구분을 커밋 본문에 쓴다).

**작업량**: 소 / **심각도**: 🟠 / **의존**: W3·W6

## §W8 — 직업: 사냥꾼 신설 + 탐험가 정찰 재탄생 + HUD

**대상**: `JobSO` (append: `CombatDurationMult=1`, `WarnBonusDays=0`), `Assets/M0Config/Jobs/Job_Hunter.asset` (신설)·`Job_Explorer.asset` (휴면 해제 Edit — 정찰로 재탄생), `SimulationLoop._jobPool` 등록 (씬), `SeasonHud` (HP 표기 + 적습 상시 표시)

**스케치**: Job_Hunter = CombatDurationMult 0.5 + GoalBoost(Goal_Fight +15, 제안) + PreferWeights(겁 음수). Job_Explorer = WarnBonusDays 1 + 기존 ExploreCostMult 유산 유지 — 부재 시나리오(헌장): *"정찰 없는 판은 적습을 하루 늦게 아는 판이다"* / *"사냥꾼 없는 판은 격퇴가 두 배 느려 부상이 는 판이다"*. ThreatService 예고 진입 시 생존 정찰꾼 존재하면 `WarnDays + bonus`. HUD: ①`ComposeSelected`에 `체력 87/100` (읽는 자리 — 헌장 표현 조항) ②적습 체류 중 `SetPrompt` 상시 표시 *"⚔ 늑대 무리 3마리 — 마을 습격 중"* (개입 시점 명시 — ADR-M21-7, `_prompt` 패턴 재사용).

**DoD**:
- [ ] 사냥꾼 타격 간격 = 일반 × 0.5 (S1 로그)
- [ ] 정찰꾼 있는 판의 예고가 1일 이르다 (두 판 비교 로그)
- [ ] 정보줄에 HP 표시 (S4) + 적습 중 상시 프롬프트 표시·종료 시 해제
- [ ] 무직·기존 5종 직업의 전투 간격 불변 (중립 불변식 — CombatDurationMult 기본 1)

**⚠️ 오해 위험**: ①"정찰 보너스를 인원 비례로" — 1명이면 족하다 (비례면 정찰꾼 몰빵 메타). ②"Explorer 에셋 재작성" — Edit만, GoalBoosts의 기존 탐험 유산은 정찰 목적에 맞게 최소 수정. ③"HP를 전 주민 머리 위 상시 바로" — 이번엔 정보줄+프롬프트만 (화면 오염. 상시 바는 관측 후 별도 결정).

**작업량**: 중 / **심각도**: 🟡 / **의존**: W4 (Goal_Fight 참조)·W6 (적습 표시)

## §W9 — 보상·연대기·신기록 + 문서 정합

**대상**: `ChronicleService` (EventId append: `Repelled=9, Hunted=10`, ExitCause append: `Combat=4`), `RunRecordStore` (append: `BestRepels` — 역대 최다 격퇴), `SeasonHud.ComposeGameOver` 격퇴 줄, `WorldConfigSO` 툴팁 정정 (전제 N), `ThreatSO`·`JobSO` 헤더 주석 정합 (ADR-M10-8 폐기 반영)

**DoD**:
- [ ] 격퇴/사냥이 참여 주민 전원의 연대기에 남는다 (Value = 무리 규모)
- [ ] 전투 사망이 `ExitCause.Combat`으로 기록되고 전멸 화면이 "늑대에게"를 말한다
- [ ] 신기록 파일에 BestRepels 추가 (기존 파일 역직렬화 호환 — 필드 부재 = 0)
- [ ] append-only 게이트 (M13-T4 계열) green 유지

**⚠️ 오해 위험**: ①"EventId 중간 삽입·재배열" — append-only (ADR-M13-2, 저장된 연대기가 다른 이야기가 된다). ②"사냥 드랍을 개인 소지로" — 전역 스톡 (ADR-M21-8).

**작업량**: 소 / **심각도**: 🟡 / **의존**: W3·W7

## §W10 — 게이트 본대 (`Assets/Tests/EditMode/M21_CombatGates.cs` 신설)

순수 함수 게이트: ShouldFlee/ShouldRout/HitInterval/RelievedCount/마릿수 산식 경계값 · HP 환산 검산 (아사 소요 일수 = 기존 값) · MyWasStarved 원인 라우팅 · 중립 불변식 (기본값 에셋 = 기존 동작) · Goal_Fight 에셋 정합 (트리거 문법·성향 대칭·AllowedWhenInjured=0) · append-only (SlotId 41·EventId·ExitCause) · **값 기반 역인덱스 결과 반영** (기존 부상·위협 게이트 중 수명·일수 가정 게이트의 재보정).

- [ ] 신규 게이트 전부 "실패하는 구현이 존재하는가"를 먼저 자문하고 작성 (M17 교훈 — 실패 불가능한 테스트 금지)
- [ ] 기존 전체 게이트 green (S8)

**작업량**: 중 / **심각도**: 🟠 / **의존**: W1~W9

---

## 커밋 분할 계획 (1항목 1커밋, 각 커밋 단독 실행 가능)

1. W1 `feat(goap): M21-W1 주민 HP 단일 척도 — 아사·부상 시계의 HP 환산`
2. W2 `feat(goap): M21-W2 위협 체류·HP·도주선 — 날씨에서 적으로 (ADR-M10-8 폐기)`
2R. W2R `feat(goap): M21-W2R 배회형 위협 + 계절 연동 타깃·공격성 (W2 정지 폐기)`
3. W3 `feat(goap): M21-W3 CombatService — 판정의 집 신설`
4. W4 `feat(goap): M21-W4 Goal_Fight 하이브리드 참전 — 같은 트리거, 반대 성향`
5. W5 `feat(goap): M21-W5 원시 무기 — 맨손 가능 + 배율`
6. W6 `feat(goap): M21-W6 대규모 적습 — 마릿수 확장형`
7. W7 `feat(goap): M21-W7 래칫 양방향 완충 — 티어 단조 불가침`
8. W8 `feat(goap): M21-W8 사냥꾼·정찰 + HP HUD·적습 표시`
9. W9 `feat(goap): M21-W9 격퇴 연대기·신기록 + 문서 정합`
10. W10 `test(goap): M21-W10 게이트 본대`

중간 리뷰 **3회**: **리뷰⓪ (신설)** W2R 직후 (Play 1판 — 늑대가 정지하지 않는가·평시 주민 타깃 출몰이 실제로 오는가·초반 사망률이 과하지 않은가. **동선이 안 보이면 W3 착수 금지** — W2가 그대로 재현되는 것이므로) · **리뷰①** W3 후 (HP 환산 검산 — 아사·부상 소요 일수가 기존과 같은가, 다르면 W4 착수 전 정지) · **리뷰②** W7 후 (Play 1판 — 격퇴 루프가 실제로 돌고 방치가 안락하지 않은가).

> 🔔 **리뷰⓪를 새로 끼우는 이유**: W2는 게이트 320개를 통과하고 커밋된 뒤 Play에서 뒤집혔다.
> 이번 개정의 성공 기준(**동선·주민 반응**)은 **게이트로 증명할 수 없는 종류**다 — 화면에서만
> 보인다. 그래서 다음 항목으로 넘어가는 문을 Play에 건다 (방법론 M20: 게이트가 못 보는 축).

## 스코프 가드 — 이번에 하지 않는 것

- **상주형 야생 (마을 밖 지형·구역 사이에 늑대가 상시 존재)** → **맵 대형화 밀스톤 이월** (2026-08-06 사용자 결정). 지금 맵은 100×100 평지라 늑대를 둘 지형이 없다 — 숲·정글·광산 같은 구역이 생겨야 성립하고, 그 자체가 큰 밀스톤이다. 출몰형을 지키므로 세이브(ADR-M10-10)·경로탐색 예산 전제도 그대로 유지된다
- **위협 개체의 스프라이트·공격 모션·밭 헤집는 동작** — 개체는 색 원 그대로다(`ThreatAgent` 주석 *"아트는 후속 에셋 교체"*). W2R 성공 기준은 **동선과 주민 반응**이지 그림이 아니다. 별도 표현층 결정 사항
- **부탁 goal 목표가 굳는 문제 (D3)** — 부탁은 "하나 더"인데 수락 시점 절대값으로 굳어 결손이 누적된다(ADR-M0-12의 사례). 부탁 축(ADR-M8-4·M1-C) 개정이라 덩치가 커서 이월 (2026-08-06 사용자 결정)
- **방어 시설(울타리·망루)·목수 재탄생** → M22 (킬박스 메타 위험 항목 동봉 — 정적 방어 + 경로 착취 합작, `Docs/ADR_경로탐색_확장경계.md` 접점)
- 가죽 등 신자원 · Iron 이상 무기 티어 · 무기 내구도/분실/나눔
- 몬스터 신규 종 (완성 후 에셋 1개로 가능해야 하며, 그것이 M10-G 리허설의 성공 판정)
- 격퇴 전용 권한(독점) — ADR-M19-3 개정 사유 없음
- HP 플래닝 슬롯 승격 · 전 주민 상시 HP 바 · 위협 간 전투 · 세이브 구현 (주석 규약만)
- 이월 관측 (M21 Play 겸사): P15 경험 우회 실증 · P3 농부 비축차 · P2 스폰 다양성 · M19 잔여(나눔 장면·장시간 방치)
- **재검사 이월 (2026-08-06 ooo interview)**: 성향(겁) 기반 교전 중도 이탈 판정 (A2 이월분 — 명령 강제 참전 겁쟁이 관측 후) · 밭 타깃 체류 반복 파괴 밸런스 (0.25일 주기 × 최대 2일 = 최대 8회, 기존 1회 대비 — 제안치 관측) · Goal_ArmSelf 자원 드레인 (전 주민 각 나무3+돌2 — 겨울 대비와 경쟁) · 직업 풀 희석 (사냥꾼·정찰꾼 추가로 기존 5종 스폰률 자연 하락 — 헌장 정합이나 관측)

## 재미 검증 — 플레이어가 화면에서 새로 보게 되는 것

### W2R 몫 (2026-08-06 개정 — 이 항목만으로 화면이 달라진다)

지금까지 늑대는 **밭 위에 붙박여 서 있었고**, 무슨 일이 벌어지는지는 콘솔에만 있었다.
W2R 뒤에는 늑대가 마을 언저리를 **어슬렁거린다** — 밭 사이를 돌고, 사람이 가까이 오면
그쪽으로 붙고, 몇 번 헤집고는 물러난다(최대 0.75일). 겨울이 오면 같은 늑대가 달라진다:
**주민 쪽으로 기울고**(Tier1 0.25→0.5, 무리·곰은 1.0) 더 오래 눌러앉는다(1.5일).
플레이어가 보는 것은 늑대의 그림이 아니라 **움직임과 주민의 반응**이다 — 밭에서 일하던
사람이 하던 일을 놓고 도망치고, 늑대가 지나가면 돌아온다. *"겨울이 오니 늑대가 사람을
노리기 시작했다"*가 로그가 아니라 화면에서 읽힌다.
⚠️ 늑대의 **스프라이트·공격 모션은 이번에도 없다**(색 원 그대로). 사용자 확인 사항 —
성공 기준은 "동선 + 주민 반응"이지 "늑대처럼 보이는 것"이 아니다.

### M21 전체 몫

지금까지 위협은 지나가는 날씨였다 — 예고가 뜨고, 누가 물리고, 늑대는 떠났다. 이번 작업이 끝나면 늑대 무리가 **마을에 머무르며** 프롬프트에 "⚔ 습격 중"이 상시로 떠 있고, 겁 없는 주민과 사냥꾼이 **마주 서서 싸우는 장면**이 벌어진다. 플레이어는 주민을 골라 "싸워라/피해라"를 명령할 수 있고(겁쟁이는 거부한다), 정보줄의 **체력이 깎이는 것**을 보며 개입 시점을 잰다. 이기면 무리가 꼬리를 말고 도주하는 **격퇴 장면**과 고기 보상, 전멸 화면과 연대기에는 "늑대에게 죽었다"와 "적습을 물리쳤다"가 남는다. 굶주림도 즉사가 아니라 체력이 줄어드는 과정이 되어, "제때 개입"의 창이 모든 죽음 앞에 열린다.
