# M22 2차 — 문 고도화 실행명세서 (전역 잠금 + 원클릭 전환 + ADR-M22-8)

*작성일 2026-08-09. 출발 재료 = 같은 날 브레인스토밍 합의 (memory `project-m22-phase2-agreement`,
방법론 M19 3게이트 통과). 1차 정본 = `Docs/M22_방어건설_실행명세서.md`.*

**한 줄 목표 (Restate 승인분)**: 단축키 하나로 모든 문을 걸어 잠가 위협 앞에서 안팎을
가르는 플레이어 개입(이벤트 재계획·한 걸음 관용·파괴가 잠금을 이김) + 완공 울타리 우클릭
원클릭 문 전환(철거+계획 매크로), 그리고 구역 밖 집 위험 감수의 ADR 박제 — 통과는
잠금이 생사를 가르는 장면이 화면에서 읽히는 것.

**목적 (사용자 확정)**: ①잠금 = 개입 수단 (M13 재정의 "제때 개입할 수 있게"의 방어판)
②UX 완결. **비목표 (사용자 명시 제외)**: 위협의 문 우선 타깃 · 경비 배치 · 망루·함정(3차).

---

## 0. 성공 기준 (숫자로)

1. **잠금 중 문 통과 0회**: 잠금 상태에서 주민 신규 경로의 문 타일 포함 0건
   (JPS가 우회 또는 Unreachable). 예외 = 한 걸음 관용: 토글 순간 문을 다음 칸으로
   이미 예약했던 주민의 통과 **최대 1회/인**.
2. **선별 재계획**: 토글 시 잔여 경로에 문 타일이 포함된 주민만 AbortPlan — 판정은
   순수 함수로 분리되어 게이트가 씬 없이 검산 (포함/미포함/관용 3케이스).
3. **해제 시 복원**: 해제 후 같은 출발·도착의 FindPath가 문 경유 PathFound.
4. **전환 원자성**: 완공 울타리 우클릭 1회 = 철거(나무 1 회수) + 문 계획 등록이
   한 호출에서 완결 — 중간 상태(철거됐는데 계획 없음)로 끝나는 경로 0.
5. EditMode 게이트 전체 green (기존 376 + 신규 ≥4). 신규 수치 발명 0
   (전환 비용 = 기존 철거 회수 1 + 문 건설 3 파생).
6. **관측 주 (리뷰①, 완주 후 1회)**: 위협 예고 → L 잠금 → 주민이 안에 머물고,
   밖에 남은 주민의 위험이 갈리는 장면이 화면에서 읽힌다 (합의 4 동형 — 수치는 보조).

---

## 1. 전제 확인 (0단계 실사 산출물 — 2026-08-09 코드 실사)

1. **L 키는 비어 있다** — `PlayerInputController` 어휘 = ESC·B·C·F·Y/N·숫자·Shift·Ctrl+F9.
   충돌 없음.
2. **우클릭 확장 지점이 이미 있다** — B 모드 우클릭은 `TryAddDefenseGate` 실패 시
   "여기에는 문을 둘 수 없습니다"로 끝난다 (`PlayerInputController.cs:342-352`).
   완공 울타리 위 우클릭이 정확히 이 분기로 떨어진다 → 전환 매크로의 자리.
3. **완공 문 열거는 공짜다** — `DefenseService._durability` 키 중 `slot == GateCount`
   (`NotifyBuilt`가 CountSlot으로 등록, `DefenseService.cs:330-336`). 문 등록부 신설 불필요.
4. **파괴가 잠금을 이기는 건 기존 코드가 이미 한다** — 소실 복구가 두 배열을 무조건
   true로 (`SimulationLoop.cs:839-840`, ADR-M22-6). 잠긴 문이 부서지면 구멍이 뚫린다 =
   의도와 일치. 신규 훅은 **잠금 중 신규 문 완공** 하나뿐 (완공 구독 `888-889` 확장).
5. **선별 Abort 재료 완비** — `_waypoints`+`_wpIndex`(잔여 경로), `_hasNextReserved`
   (한 걸음 관용 판정), `AbortPlan(reason, warn:false, cooldown:false)` 선례
   ("상위 목표 전환", `VillagerAgent.cs:521`). 신규 이동 코드 불필요.
6. **위협 배열은 만질 것이 없다** — 문은 `ThreatWalkable`에서 이미 false
   (`BlocksThreatPassage`, `SimulationLoop.cs:203`). 잠금은 주민 배열만 갱신한다.
7. **세이브는 미구현** (ADR-M0-10: 구현 M4+) — `GatesLocked`는 "세이브 대상" 주석
   선언만 한다.

## 1.5 넓히기 분류 (직교성 — ①선반 위 물건 / ②새 선반)

| 구분 | 항목 | 근거 |
|---|---|---|
| ② 새 선반 | **잠금 상태** (전역 1비트 + 토글 문) | 완성 후 개별 문 잠금 = 전역 bool → 타일 등록부 확장 (재설계 아님) |
| ① 선반 위 물건 | 원클릭 전환 = 기존 철거+문 계획 창구의 원자 합성 | 새 건설·제거 경로 0 (ADR-M22-10) |
| ① 선반 위 물건 | ADR-M22-8 (구역 밖 집) | 문서만, 코드 0줄 |

---

## 2. ADR (구현 중 갈릴 판단 — 바꾸려면 사유를 커밋에)

### ADR-M22-8 · 구역 밖 집은 시스템이 구제하지 않는다 (합의 6 종결)

방어 구역(울타리) 밖 집 주민에 대한 시스템 개입은 없다 — **어디를 두를지가 공간 선택의
무게다.** 잠금은 이 무게를 날카롭게 한다(밖에 남은 주민은 못 들어온다) — 이는 결함이
아니라 이 결정의 표현이다. 이주 유도는 구역 이전 백로그([[project-zone-relocation]],
코드 선반영 금지 지시 2026-07-20)와 함께 봉인 유지.
**재론 조건**: 구역 밖 집 기인 사망이 반복 관측되고, 그것이 플레이어의 구역·잠금 선택으로
회피 불가능하다고 판정될 때.

### ADR-M22-9 · 잠금 = 이벤트 재계획 (매 틱 재검사 금지)

잠금은 드문 사건(플레이어 개입)이므로 비용은 **토글 순간에만** 낸다: ①완공 문 타일의
주민 통행 배열 갱신 ②잔여 경로에 문이 포함된 주민만 선별 AbortPlan(ADR-M0-11 재사용)
③표현(마커·정보줄). 이동 코어에 매 틱 Walkable 재검사를 넣지 않는다 — 실수요가 잠금
하나뿐이고, 1차가 ADR-M22-5로 피해 간 수술이다. 동적 개폐 실수요가 생기면 그때 사유와
함께 재론. **한 걸음 관용**: 토글 순간 문을 다음 칸으로 예약한 주민은 통과시킨다 —
예약 시스템과 싸우지 않는다. **파괴는 잠금을 이긴다**: 잠긴 문 파괴 = 두 배열 복구
(기존 ADR-M22-6이 처리) = 구멍. 잠금이 파괴를 막는 코드를 만들면 반려.
에셋(`Gate.asset`)은 무변 — 잠금은 배열 상태이지 건물 속성이 아니다.

### ADR-M22-10 · 전환 = 기존 창구 둘의 원자 합성

완공 울타리 → 문 전환은 `TryDemolishDefenseAt`(철거+회수) + `TryAddGatePlan`(문 계획)의
합성이며 **새 건설·제거·정산 경로를 만들지 않는다** (ADR-M0-3). 시공은 여전히 주민
노동이다(합의 2) — 즉석 완공 교체 금지. 문이 설 때까지 그 칸이 열려 있는 것은 정직한
대가다. 부분 성공 금지: 철거 후 계획 등록이 실패할 조건을 **철거 전에** 검사한다.
문→울타리 역방향은 만들지 않는다 (기존 철거+긋기로 가능, 수요 미관측).

---

## 3. 수치 (신규 발명 0)

| 값 | 출처 |
|---|---|
| 전환 시 나무 회수 1 | 기존 철거 회수 (Fence Wood 1 전액 — W8 규약) |
| 문 건설 비용 3 | 기존 `Gate.asset` Wood (건설 시 차감 — 선차감 없음 규약 유지) |
| 잠금 비용·쿨다운 | 없음 (개입 수단에 요금을 물리지 않는다 — M13 목적) |
| L 키 | 신규 입력 (수치 아님) |

---

## 4. 작업 항목 (W1~W2 — 1항목 1커밋)

> 브레인스토밍의 "W 3개 예상" 중 셋째(ADR 박제)는 코드 0줄이라 이 명세서 커밋 자체가
> 수행한다 — W 항목에서 제외 (정제, 합의 취지 무변).

### §W1 — 전역 잠금 토글 (심장)

**대상**: `Assets/Scripts/M0/World/DefenseService.cs`, `Assets/Scripts/M0/SimulationLoop.cs`,
`Assets/Scripts/M0/Agent/VillagerAgent.cs`, `Assets/Scripts/M0/UI/PlayerInputController.cs`,
`Assets/Scripts/M0/Presentation/GateLockView.cs`(신설, 표현 전용),
`Assets/Tests/EditMode/M22_DefenseGates.cs`(확장)

**스케치**:
```csharp
// DefenseService — 잠금 상태의 집 (ADR-M22-9). 세이브 대상 (ADR-M0-10 — 구현 M4+).
public bool GatesLocked { get; private set; }
public event Action<bool> OnGateLockChanged;                  // 표현 전용 구독
public IEnumerable<Vector2Int> BuiltGateTiles                 // _durability 키 slot==GateCount
public bool ToggleGateLock()                                  // 쓰기 문 하나 — 새 상태 반환

// M0SimulationLoop — 배선 창구 (PlayerInput 전용)
public bool ToggleGateLock()
{
    bool locked = Defense.ToggleGateLock();
    foreach (var t in Defense.BuiltGateTiles)                 // 주민 배열만 (전제 ⑥)
        Walkable[ax, ay] = !locked;                           // 변환은 MapBounds 관례 (ADR-M0-9)
    if (locked) AbortAgentsCrossingGates();                   // 선별 Abort — 전 주민 순회
    return locked;
}
// 완공 구독(기존 888-889) 확장: 잠금 중 신규 문 완공 → Walkable false (전제 ④의 훅)

// VillagerAgent — 선별 판정은 순수 함수 (게이트가 씬 없이 검산)
// 한 걸음 관용: 예약된 다음 칸(startIndex)은 검사에서 제외한다
internal static bool PathCrossesAny(List<Vector2Int> waypoints, int startIndex,
                                    HashSet<Vector2Int> tiles);
public void AbortIfPathCrosses(HashSet<Vector2Int> tiles)     // → AbortPlan("문이 잠겼다 — 재계획",
                                                              //    warn:false, cooldown:false)
// PlayerInputController — 모드 무관 전역 키 (B 모드 밖에서도)
// L: 문 0개면 "잠글 문이 없습니다" no-op / 토글 → HUD "문을 걸어 잠갔습니다 (L 해제)" | "문을 열었습니다"
```
`GateLockView`: `OnGateLockChanged`+`OnDurabilityChanged` 구독, 잠금 중 완공 문 위에
자물쇠 마커 (신규 문 완공·파괴 추적 포함). 시뮬 쓰기 0.

**DoD**:
- [ ] 잠금 상태에서 `Pathfinder.FindPath`(문 안↔밖)가 문 타일을 지나지 않는다
      (게이트: 잠금 → Unreachable/우회, 해제 → 문 경유 PathFound 복원)
- [ ] `PathCrossesAny` 3케이스 검산 (포함 → true / 미포함 → false / 문이 startIndex
      예약 칸이면 관용 → false) (게이트)
- [ ] 잠금 중 신규 문 완공 시 그 타일 Walkable false (게이트: 완공 구독 규칙)
- [ ] 잠긴 문 파괴 → 두 배열 true + `BuiltGateTiles`에서 사라짐 (기존 T5·ADR-M22-6
      게이트가 회귀 감시 — 잠금 상태 추가 케이스 1개)
- [ ] L 토글마다 HUD 정보줄 1건, 문 0개면 상태 무변 no-op (Play 확인)
- [ ] 잠금 중 문 위 자물쇠 마커 표시·해제 시 소거 (Play 확인 — 표현 전용)
- [ ] 기존 게이트 전체 green (376 + 신규)

**⚠️ 오해 위험**:
1. "잠금 = `Gate.asset`의 BlocksMovement를 켠다" — 아니다. 에셋은 밸런스의 집이지 상태의
   집이 아니다 (켜면 모든 판의 문이 잠기고 OnValidate 배타 검사와 충돌). 배열 직접 갱신.
2. "토글 시 전 주민 AbortPlan" — 아니다. **선별**이다. 전원 Abort는 진행 중 노동을 전부
   초기화해 잠금의 숨은 비용이 과대해진다 (개입 수단에 요금을 물리지 않는다 — §3).
3. "위협 배열도 갱신" — 무의미. 문은 위협에게 원래 불통 (전제 ⑥). 갱신 코드를 쓰면
   "잠금이 위협을 막는다"는 거짓 인상만 남긴다.
4. "잠금 중 Unreachable 로그는 버그" — 아니다. 안쪽 주민의 바깥 goal이 막히는 것은
   자기 회복 루프(ADR-M0-11)의 정상 경로다. 웅크림 = 농성의 표현 (🟡 관찰 ①).

**작업량**: 중 / **심각도**: 🔴 (통행 배열의 첫 런타임 토글) / **의존**: 없음

### §W2 — 원클릭 문 전환 (UX 완결)

**대상**: `Assets/Scripts/M0/SimulationLoop.cs`, `Assets/Scripts/M0/UI/PlayerInputController.cs`,
`Assets/Scripts/M0/World/DefenseService.cs`(사전 검사), `Assets/Tests/EditMode/M22_DefenseGates.cs`

**스케치**:
```csharp
// M0SimulationLoop — 전환 창구 (ADR-M22-10: 기존 두 창구의 원자 합성)
public bool TryConvertFenceToGateAt(Vector2Int tile, out int refund)
{
    refund = 0;
    if (Defense == null || !Defense.HasStructureAt(SlotId.FenceCount, tile)) return false;
    if (!Defense.CanPlanGateAt(tile)) return false;           // 철거 전 사전 검사 — 부분 성공 금지
    TryDemolishDefenseAt(tile, out refund);                   // 철거 + 나무 1 회수 (기존 W8)
    return TryAddDefenseGate(tile);                           // 문 계획 (기존 창구)
}
// PlayerInputController 우클릭 분기 개정 (기존 342-352):
//   TryAddDefenseGate 성공 → 기존 그대로
//   실패 && 완공 울타리 타일 → TryConvertFenceToGateAt → "울타리를 헐고 문을 냅니다 — 나무 1 회수"
//   그 외 → "여기에는 문을 둘 수 없습니다" (기존)
```
`CanPlanGateAt` = `TryAddGatePlan`의 실패 조건(중복 계획 등)에서 "타일 점유"만 뺀 순수
검사 — 철거 후 성공이 보장되는지 미리 판정한다.

**DoD**:
- [ ] 완공 울타리 우클릭 1회 → FenceCount −1 · 나무 +1 · 문 계획 +1 (`GatePlannedCount`)이
      한 호출에서 (게이트: 전환 원자성 — 실패 조건에서는 셋 다 무변)
- [ ] 전환된 계획 칸에 주민이 문을 짓는다 — 기존 BuildGate 사슬 그대로, 신규 배선 0
      (기존 T4 게이트가 감시)
- [ ] 나무 부족(문 3 미만) 시 전환은 **허용** — 계획은 서고 건설이 자재 대기 (선차감 없음
      규약 그대로. 프리뷰·안내는 기존 재고 판정 문법)
- [ ] 계획 칸(미완공) 우클릭은 기존 동작 그대로 — 울타리 계획→문 전환 (회귀 게이트 T3b)
- [ ] 게이트 전체 green

**⚠️ 오해 위험**:
1. "전환 = 즉석 문 완공" — 아니다. 시공은 주민 노동 (합의 2 · ADR-M22-10). 즉석 교체는
   건설 사슬 우회 = ADR-M0-3 위반.
2. "차액 정산(3−1=2)의 새 경로" — 아니다. 회수(+1)와 건설 차감(−3)은 기존 두 경로가
   각자 낸다. 새 정산 코드가 생기면 반려.
3. "문→울타리 역방향도 대칭으로" — 스코프 밖 (ADR-M22-10, 수요 미관측).

**작업량**: 소 / **심각도**: 🟠 / **의존**: 없음 (W1과 병행 가능)

---

## 5. 커밋 분할 계획

| 순서 | 커밋 | 단독 실행 가능 근거 |
|---|---|---|
| 1 | spec: 이 명세서 (+ADR-M22-8 박제) | 문서만 |
| 2 | W1 잠금 | 전환 없이도 완결 — L·마커·선별 Abort까지 |
| 3 | W2 전환 | 잠금 없이도 완결 — 기존 창구 합성 |

완주 후 **리뷰① 1회** (사용자 Play — 성공 기준 6). 마이그레이션 없음, 중간 리뷰 불필요.

## 6. 스코프 가드 (이번에 하지 않는 것)

- **개별 문 잠금** — 전역 토글 관측 후 (등록부 확장으로 가능하게 설계됨).
- **이동 중 Walkable 재검사 일반해** — ADR-M22-9. 동적 개폐 실수요가 생기면 재론.
- **문 자동 개폐·경비 배치·위협의 문 우선 타깃** — 사용자 명시 비목표.
- **망루·함정** — 3차 (합의 5 단계).
- **문→울타리 역전환** — ADR-M22-10.
- **잠금 중 바깥 주민 구제** (귀환 우선 goal 등) — ADR-M22-8과 한 몸. 관측만.
- **연대기 이벤트** (잠금·농성 기록) — 정보줄만. 필요가 보이면.
- M21·M22 1차 이월 관측 항목 — 리뷰①에 얹어 관측만, 코드 손대지 않는다.

## 7. 🟡 관찰 항목 (리뷰①에서 함께 본다)

1. 잠금 중 안쪽 주민의 바깥 goal → Unreachable 재계획 루프 빈도 (웅크림 표현 vs 로그 소음).
2. 밖에 남은 주민의 실제 결말 (위협에 잡히는가, 해제까지 버티는가) — ADR-M22-8 재론
   조건의 관측 축.
3. 전환 후 문이 설 때까지의 열린 칸으로 위협이 실제 침입하는가 (정직한 대가의 체감).
4. 1차 이월: 문 자리 점유 시 건설 지연 · 공성 배회 이탈 · 선차감 없음.

## 8. 재미 검증 절 — 플레이어가 화면에서 새로 보게 되는 것

지금까지 위협 예고가 떠도 플레이어가 할 수 있는 일은 F(싸워라)와 구경뿐이었다. 이
명세가 끝나면: 예고가 뜨는 순간 **L 하나로 마을의 모든 문에 자물쇠가 걸리고**, 밖으로
나가려던 주민들이 문 앞에서 발길을 돌려 안에 웅크리는 — 농성이 시작되는 — 것이 보인다.
그리고 하필 그때 밖에 있던 한 명이 잠긴 문 저쪽에서 서성이는 장면이, 이 게임이 처음
갖는 "내 선택이 만든 안팎"의 극이다 (구역 밖에 집을 지은 것도, 문을 잠근 것도 플레이어의
선택이다 — ADR-M22-8). 위협이 지나가고 L을 다시 누르면 문이 열리고 일상이 재개된다.
울타리를 긋다 "여기가 출입구여야 했는데" 싶은 자리는 우클릭 한 번으로 헐리고 문 계획이
서서, 긋기 UX의 마지막 가시가 빠진다.

---

*리뷰① 통과 후: memory `project-m22-phase2-agreement` 갱신 + 3차 범위(망루·함정 vs
위협 스프라이트 vs 기타) 재논의.*
