# M26 2차 — 지형 축 (멀리 갈 이유와 대가) 실행명세서

> **한 줄 요약**: 땅마다 **다른 것이 나게** 하고, 먼 곳일수록 **귀하되 위험하게** 만든 뒤,
> 주민이 *"가까운데 위험 vs 먼데 안전"* 을 **실제로 비교하게** 한다.

1차가 만든 것은 **지도**였다 — 판마다 다른 땅이 깔리고, 호수를 돌아가고, 늪에서 느려진다.
그러나 그 땅 위에서 **일어나는 일은 전과 같다**: 자원은 지형과 무관하게 흩뿌려지고, 주민은
언제나 최근접 노드로 간다. 2차는 그 지도에 **이유**를 얹는다.

---

## 📌 차수 위치

| 차수 | 이름 | 상태 |
|---|---|---|
| 1차 | 땅이 생긴다 | ✅ 종료 (게이트 **464/464** · 사용자 Play 확인) |
| **2차** | **멀리 갈 이유와 대가** | **← 이 문서** |
| 3차 | 희귀자원이 값을 한다 | 대기 (⚠️ 대장간 = 대장장이 직업 → **M25 3차에 막힘**) |
| (M27) | 예약 | 무기·방어구 등급 |

## 📌 진행 상태 (다음 세션은 여기부터)

| W | 내용 | 상태 |
|---|---|:---:|
| W1 | 자원 노드가 **물 위에 서지 않는다** (1차가 남긴 구멍) | ✅ (게이트 **467/467** · +3) |
| W2 | 지형이 **무엇을 낳는지** 정한다 (지형 조건 + `NodeDensityMult` 소비) | ✅ (게이트 **469/469** · +2) |
| W3 | 거리 = 희귀도 (기존 `min/maxDistanceFromBase` 재보정 — **에셋만**) | ⬜ |
| W4 | **채집 선택 통로** — 거리 하나 → 점수 하나 (**이 차수의 심장**) | ⬜ |
| W5 | 들판 상주 위협 (배회 통로 재사용) | ⬜ |
| W6 | 위험 기억 — 당해 보고 안다 | ⬜ |
| W7 | 구조 명령 UI + 위치 없는 부상 알림 | ⬜ |
| W8 | 게이트 마감 | ⬜ |

---

## §0. 전제 확인 (실사 2026-08-10 — 방법론 M9, 생략 금지 절차의 산출물)

착수 전 전제를 전부 적고 코드로 확인했다. **다섯 건이 어긋났고, 그중 하나는 1차가 남긴 결함이다.**

| # | 전제 (예고·memory의 주장) | 실측 | 판정 |
|---|---|---|---|
| 1 | 자원 노드는 통행 가능한 땅에만 선다 | 🔴 [`ResourceNodeSpawner.cs`](../Assets/Scripts/Core/ResourceNodeSpawner.cs) 에 `Walkable`·`Terrain` 참조가 **한 건도 없다**. 배치 조건은 거리·간격뿐 | 🔴 **결함 — W1** |
| 2 | 물고기는 새 자원 타입이 필요하다 | `RawFood = 0` 이 [`ResourceType.cs:22`](../Assets/Scripts/Core/ResourceType.cs:22) 에 **이미 있다** | ✅ 신설 불필요 |
| 3 | 치료사 직업이 없다 (M10 명세 §전제확인의 기록 — 그 시점 사실) | `Job_Medic.asset` · `Goal_TreatInjured.asset` · `TreatInjured.asset` · `TendRunner.cs` · `TendActionSO.cs` **전부 존재** (그 뒤 생겼다) | ✅ **해소된 전제** — 구조 UI는 직업이 아니라 **화면만** |
| 4 | 부상 방치 사망 시계가 없어 구조가 불가능할 수 있다 | `InjurySeverity` · 간호 2단계 · `InjuryDeathAfterDays` · `NextInjuryStateV2` 존재. 순수 함수로 검산 가능 ([`VillagerAgent.cs:681`](../Assets/Scripts/M0/Agent/VillagerAgent.cs:681)) | ✅ 있음 — **W7에서 값을 검산**한다 |
| 5 | 들판 상주 위협은 새 상태가 필요하다 | 배회가 **이미 있다**: `PickWanderTile` · `WanderRadiusTiles` · `WanderAnchor` ([`ThreatService.cs:1364`](../Assets/Scripts/M0/World/ThreatService.cs:1364)) | ✅ 통로 재사용 |
| 6 | `NodeDensityMult` 는 아무도 안 읽는다 | 참조 = `TerrainTypeSO.cs:53` 정의 + 에셋 5장의 값뿐. **소비자 0** | ✅ 예고대로 |
| 7 | 거리 = 희귀도를 담을 통로가 있다 | `minDistanceFromBase` · `maxDistanceFromBase` 가 [`ResourceNodeSpawnConfig.cs:30`](../Assets/Scripts/Core/ResourceNodeSpawnConfig.cs:30) 에 실재하고 스포너가 읽는다 | ✅ 에셋만으로 가능 |
| 8 | 위험 기억을 담을 통로가 있다 | `DangerRegistry`·`DangerMemory` 류 **없음**. grep 0건 | 🔴 **새 선반 (넓히기 ②)** |
| 9 | 슬롯 예산에 여유가 있다 | `SlotId.Count = 51`, 예산 **52** → **잔여 1칸** | 🔴 **제약** — ADR-T2-5 |
| 10 | 채집은 최근접 하나만 본다 | [`DiscoveryService.cs:176`](../Assets/Scripts/M0/World/DiscoveryService.cs:176) `FindNearestDiscovered` — `bestDist` 하나로 최소값만. 호출처는 [`GatherRunner.cs:29`](../Assets/Scripts/M0/Agent/Runners/GatherRunner.cs:29) **한 곳** | ✅ 예고대로 · **호출처가 하나라 개조가 싸다** |

### 🔴 전제 1의 전말 — 1차가 남긴 구멍

1차 W3은 지형을 **통행 배열 두 장**에 먹였고([`SimulationLoop.cs:1217`](../Assets/Scripts/M0/SimulationLoop.cs:1217)),
W6은 **리스폰**에 통행 판정을 넘겼다(`ConfigureRespawn(..., (x,y) => Terrain.IsWalkable(x,y) && ...)`).
그런데 **초기 스폰은 아무것도 안 받았다** — `SpawnAll(baseX, baseY, radius, sink)` 에 판정이 없다.

호출 순서는 `Terrain`(1211) → `SpawnAll`(1434) 이므로 **알 수 있는 자리에 있는데 안 읽는다.**

증상: 판 시작 시 나무·돌이 **호수 위·절벽 위에 설 수 있다.** 그 노드가 발견되면
`FindNearestDiscovered` 가 고르고 → 주민이 가려다 `Unreachable` 로 접힌다 →
**최근접이 계속 그 노드면 goal이 반복해서 접힌다.**

⚠️ 이것은 §14 관찰항목 3(*"물이 길을 너무 막는가"*)의 **진짜 원인 후보**다. 비율을 낮추기 전에
이것부터 고쳐야 한다 — 안 고치고 물을 줄이면 **증상만 옅어지고 원인은 남는다**(안티패턴: 증상 패치).

### 참조 개편 표 (실사 2 — 의미가 바뀌는 심볼)

| 심볼 | 지금 뜻 | 바뀐 뜻 | 참조 수 | 재보정 |
|---|---|---|---|---|
| `TerrainTypeSO.NodeDensityMult` | 아무도 안 읽음 (중립) | 자원 스폰 확률 배수 | 정의 1 + 에셋 5 | 소비자 신설뿐 — **기존 뜻이 없어 깨질 것이 없다** |
| `DiscoveryService.FindNearestDiscovered` | 최근접 1개 | **점수 최고 1개** (거리는 점수의 한 항) | 호출 1(`GatherRunner`) + 게이트 2(`M0_WorldGates`) | ADR-T2-3의 중립 불변식으로 **기존 게이트가 그대로 통과해야 한다** |
| `ResourceTypeSpawnData` | 거리·간격·그림 | + 지형 조건 | 에셋 1(`ResourceNodeSpawnConfig`) | **append만** — 기본값이 "아무 지형이나"라 기존 에셋 무변경 |

**의미 전환은 이 셋뿐이고 전부 append 또는 신규 소비다.** 뜻이 뒤집히는 슬롯은 없다
(반쪽 의미 전환 위험 낮음).

---

## §0.5 넓히기 분류 (사용자 가치관 — "복잡해지지 않고 넓혀지는 개발")

### ① 선반 위 물건 (기존 축에 데이터 추가 — 에셋만, 안전)

| 항목 | 어느 선반 | 완성 후 비용 |
|---|---|---|
| 늪 희귀자원 · 물가 물고기 | `ResourceTypeSpawnData` 배열 | **에셋 1칸 · 코드 0줄** |
| 거리 = 희귀도 | `min/maxDistanceFromBase` | 에셋 필드 2개 |
| 지형별 자원 밀도 | `TerrainTypeSO.NodeDensityMult` | 에셋 1칸 |
| 들판 상주 위협 | `ThreatSO` + 배회 통로 | 에셋 1칸 (`WanderRadiusTiles` + 퇴장 규칙) |

### ② 새 선반 (새 축 — M0급 설계 필요)

| 항목 | 왜 새 선반인가 | **완성 후 무엇이 에셋 1개짜리가 되는가** (착수 조건) |
|---|---|---|
| **채집 선택 점수** (W4) | 지금은 거리 하나뿐이라 **비교할 자리가 구조적으로 없다** | 새 판단 항목(위험·지형 비용·잔량)이 **가중치 필드 1개**로 늘어난다 |
| **위험 기억** (W6) | `DangerRegistry` 류가 grep 0건 | "무엇이 기억을 남기는가"가 **ThreatSO/ActionSO 필드 1개**로 늘어난다 |

두 선반 다 착수 조건을 만족한다 (오른쪽 칸을 쓸 수 있다). W4·W6은 게이트를 동반한다.

---

## §1. 성공 기준 (숫자로 — 회귀가 어디에 보이는가)

1. **통행 불가 타일 위의 자원 노드 = 0개.** 시드 20개를 전수로 돌려 `Terrain.IsWalkable(n.TileX, n.TileY) == false` 인 노드를 세어 0 (게이트 `M26B_T1`).
   🔑 **실패 가능성 증명 병기**: 판정을 끄면 **0이 아니어야** 한다 — 아니면 빈 검사다.
2. **지형 전용 자원이 그 지형에서만 난다.** 늪 전용 자원 노드 전수의 지형이 늪, 물가 자원 전수가 물에 인접 (게이트 `M26B_T2`).
3. **먼 쪽을 고르는 일이 실제로 일어난다.** Play 1판에서 *"가까운 후보를 두고 먼 후보를 골랐다"* 로그 **≥ 1건** (W4의 존재 증명 — 최근접만 보던 시절에는 **구조적으로 0건**).
4. **중립 불변식**: 위험·지형 가중치를 0으로 두면 W4의 선택 결과가 **`FindNearestDiscovered`와 타일 단위로 동일** (게이트 `M26B_T4`). 기존 게이트 `M0_WorldGates` 2건이 무수정 통과.
5. **위험 기억이 선택을 바꾼다.** 들판 위협에 당한 뒤, 같은 자리의 노드가 **여전히 최근접인데도** 다른 노드가 선택되는 로그 ≥ 1건 (게이트 `M26B_T6` + Play).
6. **구조가 구조적으로 가능하다.** 부상 방치 사망 소요(`InjuryDeathAfterDays`)가 **맵 끝에서 마을까지 걷는 시간보다 길다**를 순수 함수로 검산 (게이트 `M26B_T7`). 🔴 **짧으면 W7은 장식이다** — 이 검산이 먼저다.
7. **게이트 총계 464 → 464 + N** (N = W8에서 확정). 기존 464건 무수정 통과.
8. **재미 판정 (사용자 Play)**: *"저 자원은 탐나는데 저기는 가지 말자"* 라는 판단이 한 번이라도 서면 성립.

---

## §2. ADR (구현 중 갈릴 판단을 미리 결정 — 바꾸려면 사유를 커밋에)

### ADR-T2-1 — 노드가 설 수 있는 땅의 판정은 **하나**다

초기 스폰과 리스폰이 **같은 판정 함수**를 쓴다. 리스폰이 이미 쓰는
`(x, y) => Terrain.IsWalkable(x, y) && !Construction.HasBuildingAt(x, y)` 를 **그대로** 스폰에 넘긴다.

🔑 **왜 못 박는가**: 판정이 둘이면 "리스폰은 피하는데 스폰은 서는" 지금 상태가 **다른 모양으로
재발한다.** 舊 아키텍처를 죽인 3대 병 중 하나가 다중 경로다 (`ADR-M0-3`의 정신).

### ADR-T2-2 — 지형이 무엇을 낳는지는 **에셋이 정한다**

`ResourceTypeSpawnData` 에 지형 조건 필드를 append 한다. **코드에 "늪은 X를 낳는다" 표를 두지
않는다** (`ADR-M0-2` · 1차 `ADR-T-5`의 재사용). 지형을 늘려도, 자원을 늘려도 **코드 0줄**.

### ADR-T2-3 — 채집 선택은 **거리 하나 → 점수 하나**로 바꾸고, 최근접은 그 특수 경우다

`FindNearestDiscovered` 를 지우지 않는다. 점수 함수를 새로 만들고 **모든 가중치가 0이면
결과가 최근접과 같아지도록** 만든다 (중립 불변식, 성공 기준 4).

🔑 **왜 이 형태인가**: 舊 함수를 갈아엎으면 회귀가 어디서 났는지 알 수 없다. 중립 불변식이
있으면 **가중치를 켜고 끄는 것만으로 원인을 가른다** (1차 `TerrainService.IsEmpty` 와 같은 규약).

⚠️ **비용은 거리가 아니라 경로 비용이다.** 늪 너머 10칸과 평지 10칸은 맨해튼으로 같지만 실제로는
다르다 — 그러나 후보마다 A\*를 돌리면 예산을 넘는다. **1차 타협**: 맨해튼 거리 × 목적지 타일의
`EnterCost` 를 근사로 쓴다. 정확한 경로 비용은 **3차 이후** (스코프 가드).

### ADR-T2-4 — 위험 기억은 **마을이 아니라 개인**이 갖는다

당한 사람만 안다. 마을 전체가 즉시 아는 것은 (ㄱ)정보가 공짜가 되고 (ㄴ)한 명의 불운이
전원의 행동을 잠근다. 성격이 이미 있는 축(`M12` 성향 벡터)이므로 **겁쟁이는 오래 기억하고
용맹은 빨리 잊는** 자리를 남긴다 — 값은 3차.

⚠️ 전달 통로(대화로 퍼짐)는 **이번에 안 한다** — `ChatterSO` 가 있어 나중에 에셋 1칸이다.

### ADR-T2-5 — 새 `ResourceType` 을 만들지 않는다 (슬롯 잔여 1)

`SlotId.Count = 51`, 예산 **52** → 잔여 **1칸**. 새 자원 타입 하나가 슬롯 하나를 먹는다.
늪 희귀자원은 **기존 타입 재배치**로 표현한다 (예: `Silver` 를 늪·원거리 전용으로).

🔴 이 결정을 뒤집으려면 **슬롯 예산 증액을 먼저 결정**해야 한다 — 명세 안에서 조용히 넘지 않는다.

### ADR-T2-6 — 들판 위협은 **새 상태를 만들지 않는다**

배회(`PickWanderTile` · `WanderAnchor` · `WanderRadiusTiles`)가 이미 있다. 상주 = **퇴장 조건이
없는 배회**다. 새 러너·새 상태 0 (1차 W7이 `EntryPoint` 입력 확장만으로 끝난 것과 같은 규약).

---

## §3. W1 — 자원 노드가 물 위에 서지 않는다

**대상**: [`Assets/Scripts/Core/ResourceNodeSpawner.cs`](../Assets/Scripts/Core/ResourceNodeSpawner.cs) ·
[`Assets/Scripts/M0/SimulationLoop.cs:1434`](../Assets/Scripts/M0/SimulationLoop.cs:1434)

**코드 스케치**

```csharp
// ResourceNodeSpawner.cs — 시그니처에 판정 하나를 더한다 (기본 null = 오늘과 동일 · 중립)
/// <param name="canHostNode">이 칸에 노드가 설 수 있는가. null 이면 전부 허용 (舊 동작).
/// 🔑 리스폰(DiscoveryService.ConfigureRespawn)이 쓰는 것과 **같은 판정**을 받는다 (ADR-T2-1).</param>
public bool SpawnAll(int baseTileX, int baseTileY, int discoveryRadius, IResourceNodeSink sink,
                     System.Func<int, int, bool> canHostNode = null)

// 클러스터 중심 선정과 노드 배치, **두 곳 모두**에서 후보를 거른다.
// ⚠️ 중심만 거르면 spreadRadius 로 흩어진 노드가 물에 빠진다 — 두 곳이다.
if (canHostNode != null && !canHostNode(tx, ty)) continue;
```

```csharp
// SimulationLoop.cs — 리스폰과 **같은 식**을 넘긴다 (복붙이 아니라 같은 뜻)
System.Func<int, int, bool> canHostNode =
    Terrain.IsEmpty ? null
                    : (x, y) => Terrain.IsWalkable(x, y) && !Construction.HasBuildingAt(x, y);
_nodeSpawner.OverrideSeed(RunSeed);
bool ok = _nodeSpawner.SpawnAll(_worldConfig.BaseTileX, _worldConfig.BaseTileY,
                                _worldConfig.BaseDiscoverRadius, Discovery, canHostNode);
```

**DoD**

- [x] `SpawnAll` 에 `canHostNode` 파라미터가 있고 **기본값이 null** — 넘기지 않으면 舊 동작
- [x] 클러스터 중심 선정 루프와 노드 배치 루프 **둘 다** 판정을 통과한 칸만 채택
- [x] `SimulationLoop` 이 `Terrain.IsEmpty` 일 때 **null 을 넘긴다** (지형 없는 판 = 픽셀 동일)
- [x] 게이트 `M26B_T1`: 시드 20개 전수에서 통행 불가 타일 위 노드 **0개** (`467/467`)
- [x] 게이트 **`M26B_T1b`**: **판정을 null 로 두면 0이 아니다** (빈 검사 방지 — 실패 가능성 증명).
      명세는 `T1` 한 건으로 썼으나 **별도 테스트로 분리**했다 — 한 테스트 안에 두면 어느 쪽이
      깨졌는지 red 메시지가 안 갈린다
- [x] 배치 실패 경고가 뜨는 경우 로그에 사유가 남는다 — `지형거절=0/50` 형식으로 9건 확인
      (원인이 지형인지 간격인지 로그만으로 갈린다)
- [x] **추가** `M26B_T1c`: 같은 시드 두 번의 배치가 타일 단위로 동일 (결정성 회귀 감시)

> **중립 불변식의 근거**: `if (_canHostNode != null && !_canHostNode(...))` 는 null 일 때
> **RNG를 한 번도 소비하지 않는다** — 그래서 지형 없는 판의 배치는 코드 검토로 舊 동작과
> 동일함이 보인다. `T1c` 는 그 위에 결정성을 덧댄 것이지 舊 코드와의 동치를 증명하지는 않는다.

**⚠️ 오해 위험**

| 그럴듯한 오해 | 차단 |
|---|---|
| "클러스터 중심만 거르면 된다" | 🔴 **아니다.** `clusterSpreadRadius` 로 흩어진 노드가 물에 빠진다. **두 루프 다** |
| "물에 못 서면 시도 횟수를 늘리면 된다" | `maxPlacementAttempts` 인상은 증상 패치다. 못 서면 **경고를 남기고 그 노드를 포기**한다 |
| "`Walkable` 배열을 직접 보면 된다" | 🔴 배열은 **문**이 섞인다(개체별). 노드가 설 땅의 기준은 지형이다 — `Terrain.IsWalkable` |
| "리스폰 판정을 복사해 쓰면 된다" | 복사가 아니라 **같은 식**을 한 곳에서 만들어 둘 다에 넘긴다 (ADR-T2-1) |

**작업량** 소 · **심각도** 🔴 높음 (1차 결함) · **의존** 없음 — **가장 먼저**

---

## §4. W2 — 지형이 무엇을 낳는지 정한다

**대상**: [`ResourceNodeSpawnConfig.cs`](../Assets/Scripts/Core/ResourceNodeSpawnConfig.cs) ·
`ResourceNodeSpawner.cs` · `Assets/M0Config/ResourceNodeSpawnConfig/*.asset` · `Terrain_*.asset`

**코드 스케치**

```csharp
// ResourceTypeSpawnData — append (기본값이 舊 동작)
[Header("지형 조건 (M26-2차 W2)")]
[Tooltip("이 자원이 설 수 있는 지형. **비우면 아무 지형이나** (舊 동작 · 중립). " +
         "🔑 코드에 '늪은 X를 낳는다' 표를 두지 않는다 (ADR-T2-2) — 지형도 자원도 에셋으로 는다.")]
public TerrainTypeSO[] allowedTerrain;

[Tooltip("이 지형에 **인접**하면 된다 (물가 = 물에 붙은 땅). 비우면 인접 조건 없음. " +
         "⚠️ allowedTerrain 과 **둘 다** 걸면 '평지이면서 물에 붙은 칸'이 된다 — 물고기가 그렇다.")]
public TerrainTypeSO[] adjacentTerrain;
```

```csharp
// 후보 판정에 두 조건 + 밀도를 얹는다. 밀도는 **채택 확률**이지 개수가 아니다
// (개수를 건드리면 nodeCount 의 뜻이 흐려진다).
private bool TerrainAccepts(ResourceTypeSpawnData d, int tx, int ty)
{
    TerrainTypeSO here = _terrainAt?.Invoke(tx, ty);
    if (d.allowedTerrain is { Length: > 0 } && !System.Array.Exists(d.allowedTerrain, t => t == here))
        return false;
    if (d.adjacentTerrain is { Length: > 0 } && !HasNeighbor(tx, ty, d.adjacentTerrain))
        return false;
    return here == null || _rng.NextDouble() < here.NodeDensityMult;   // 1 = 오늘과 동일
}
```

**배포 에셋 (제안치 — 실측 근거 없음, 승인 대상)**

| 자원 | 조건 | 왜 |
|---|---|---|
| `RawFood` (기존 클러스터) | 무조건 (변경 없음) | 굶지 않게 — 기본 생존선은 안 건드린다 |
| `RawFood` **물가 묶음** (새 원소) | `adjacentTerrain = [물]` · `minDistanceFromBase` 제안 **20** | 물가에 서는 "물고기". **새 타입 아님** (ADR-T2-5) |
| `Silver` | `allowedTerrain = [늪]` · `minDistanceFromBase` 제안 **30** | 늪 희귀자원 — 느린 땅을 감수해야 닿는다 |
| 지형 밀도 | 숲 `NodeDensityMult` 제안 **1.5** · 늪 **0.6** · 평지 **1.0** | 숲이 숲답게. 🔑 값은 **에셋 한 칸** |

🔴 **숫자는 전부 제안치다.** 기존 값이 있는 것(`minDistanceFromBase` 기본 10)은 그 값을 우선하고,
새로 정하는 것은 W8 Play 후 재조정한다.

**DoD**

- [x] `allowedTerrain` · `adjacentTerrain` 이 **비어 있으면 舊 배치와 노드 좌표가 타일 단위로 동일** — `_terrainAt == null` 이면 `TerrainAccepts` 가 즉시 참이라 **RNG를 안 쓴다** (+ `M26B_T1c`)
- [x] `NodeDensityMult` 가 전 지형 같은 값이면 채택률이 안 바뀐다 — 분모가 최대값이라 전부 1.0
- [x] 게이트 `M26B_T2`: 늪 전용 자원 노드 **전수**의 지형이 늪 (시드 20판 · **배포 팔레트**)
- [x] 게이트 `M26B_T2`: 물가 자원 노드 **전수**가 물에 인접 (8방향)
- [x] 게이트 `M26B_T3`: 통행 불가 지형만 허용하면 노드 **0개** (조건 둘 중 하나만 걸려도 red)
- [x] 조건을 만족하는 칸이 없는 판에서 스폰이 **실패하지 않고 개수만 줄어든다** — 실측:
      은 최저 1/4 · 물가 식량 최저 1/8 (**0이 된 판은 없다**), 기본 식량 12/12 무영향

> **명세와 달리한 것 3건**
> 1. **밀도는 절대 확률이 아니라 `NodeDensityMult / 팔레트 최대` 다.** 스케치대로 확률로만 쓰면
>    **1을 넘는 값이 전부 "항상 채택"** 이 되어 숲 1.5가 평지 1.0과 구별되지 않는다(산수).
>    최대값으로 나누면 숲:평지:늪 = 1.5:1.0:0.6 의 **비율이 그대로** 산다. 분모는 팔레트를 아는
>    `SimulationLoop` 이 구해 넘긴다.
> 2. **`allowedTerrain` 이 걸린 자원에는 밀도를 적용하지 않는다.** 둘 다 걸면 늪 자원이 늪 밀도
>    0.6으로 **한 번 더** 깎여 설계자가 지정한 자리에서 의도의 절반만 남는다.
> 3. **`Silver.minDistanceFromBase` 는 제안치 30이 아니라 기존 값 40을 유지**했다 — 명세 스스로
>    "기존 값이 있으면 그 값을 우선"이라 적었고, 30으로 낮추면 *희귀 = 멀다* 와 반대로 간다.
>
> ⚠️ **제안치 1건 추가** (명세 표에 없던 것): 물가 식량 `nodeCount 8` · `clusterCount 6`.
> 명세는 최소거리 20만 정했고 개수는 정하지 않았다 — 기본 식량 12의 보조로 8을 넣었다. **Play 후 재조정 대상.**

**⚠️ 오해 위험**

| 그럴듯한 오해 | 차단 |
|---|---|
| "물고기니까 물 **위**에 세운다" | 🔴 물은 통행 불가다 — 서면 못 닿는다. **물에 인접한 땅**이다 (`adjacentTerrain`) |
| "`NodeDensityMult` 로 개수를 곱한다" | 🔴 `nodeCount` 의 뜻이 흐려진다. **채택 확률**만 바꾼다 |
| "새 자원 타입을 만들면 깔끔하다" | 슬롯 잔여 1칸 (ADR-T2-5). 기존 타입 재배치로 표현한다 |
| "늪 전용이니 `allowedTerrain`에 늪만 넣으면 끝" | 늪이 0%인 시드가 실제로 있다 (1차 실측: 절벽 0.2% 판). **없는 판에서 조용히 0개**가 되지 않게 경고 |

**작업량** 중 · **심각도** 중 · **의존** W1 (판정 자리를 W1이 만든다)

---

## §5. W3 — 거리 = 희귀도 (에셋만)

**대상**: `Assets/M0Config/ResourceNodeSpawnConfig/*.asset` — **코드 0줄**

기존 `minDistanceFromBase` · `maxDistanceFromBase` 를 자원별로 벌린다. W2가 지형 조건을 얹었으므로
이제 **거리와 위험이 짝지어진다** (호수 너머 · 늪 안쪽).

**DoD**

- [ ] 코드 diff **0줄** (에셋 커밋에 `.cs` 가 섞이면 반려)
- [ ] `Silver`·물가 `RawFood` 의 `minDistanceFromBase` 가 기본 자원보다 크다
- [ ] 게이트 `M26B_T5`: 자원 타입별 노드의 기지 거리 **중앙값**이 희귀도 순서와 일치
- [ ] 안전반경 12 안에는 어떤 자원도 스폰되지 않는다 (기존 규약 유지 확인)

**⚠️ 오해 위험**: *"멀수록 좋으니 최대한 멀리"* → 🔴 `maxDistanceFromBase` 를 안 걸면 맵 구석에
한 개만 나는 판이 생긴다. **하한과 상한을 같이** 건다.

**작업량** 소 · **심각도** 저 · **의존** W2

---

## §6. W4 — 채집 선택 통로 (**이 차수의 심장**)

**대상**: [`DiscoveryService.cs:176`](../Assets/Scripts/M0/World/DiscoveryService.cs:176) ·
[`GatherRunner.cs:29`](../Assets/Scripts/M0/Agent/Runners/GatherRunner.cs:29) · `AgentConfigSO`

지금은 `bestDist` 하나로 최소값만 고른다. **비교할 자리가 구조적으로 없다** — 들판에 위협을
놓기만 하면 주민은 모른 채 걸어간다.

**코드 스케치**

```csharp
/// <summary>채집 후보 점수 (M26-2차 W4) — **낮을수록 좋다** (거리의 확장이라 부호를 맞춘다).
/// 🔑 모든 가중치가 0이면 이 값은 맨해튼 거리와 같다 = `FindNearestDiscovered` 와 동일 (ADR-T2-3).
/// ⚠️ 경로 비용의 **근사**다: 후보마다 A\*를 돌리면 예산을 넘는다 (정확한 비용은 3차).</summary>
public static float ScoreCandidate(int dist, float enterCost, float dangerPenalty, in GatherWeights w)
    => dist * (1f + w.TerrainWeight * (enterCost - 1f)) + w.DangerWeight * dangerPenalty;

/// <summary>점수가 가장 낮은 채집 가능 노드. `FindNearestDiscovered` 의 확장이다.</summary>
public ResourceNode FindBestDiscovered(ResourceType type, int fromX, int fromY,
                                       in GatherWeights w, System.Func<int, int, float> dangerAt)
```

```csharp
// GatherWeights — 수치는 에셋(AgentConfigSO)에 산다 (ADR-M0-2)
[Tooltip("지형 가중치. 0 = 늪을 평지처럼 본다(舊 동작). 1 = EnterCost 를 그대로 거리에 곱한다.")]
[Range(0f, 3f)] public float TerrainWeight = 0f;   // 제안치: 배포는 1.0
[Tooltip("위험 가중치. 0 = 위험을 안 본다(舊 동작). W6의 위험 기억이 이 항을 채운다.")]
[Range(0f, 50f)] public float DangerWeight = 0f;   // 제안치: 배포는 12
```

**로그 (성공 기준 3의 탐지기)**

```
[채집선택] 영희 — 가까운 후보(참나무, 8칸)를 두고 먼 후보(참나무, 14칸)를 골랐다
           (이유: 가까운 쪽 늪 통과 · 점수 24.0 vs 14.0)
```

**DoD**

- [ ] `FindNearestDiscovered` 를 **지우지 않는다** (기존 게이트 2건이 그대로 통과)
- [ ] 게이트 `M26B_T4`: 가중치 전부 0 → `FindBestDiscovered` 결과가 `FindNearestDiscovered` 와 **동일 노드** (무작위 배치 100판 전수)
- [ ] 게이트 `M26B_T4`: 가중치를 켜면 **결과가 실제로 달라진다** (실패 가능성 증명 — 안 달라지면 빈 검사)
- [ ] `GatherRunner` 가 `FindBestDiscovered` 를 부른다 — 호출처는 여전히 **한 곳**
- [ ] 후보 선택에서 A\* 호출 **0회** (예산 — `enterCost` 는 목적지 타일 조회 한 번)
- [ ] 먼 쪽을 고른 경우에만 `[채집선택]` 로그가 남는다 (같은 답이면 조용히)
- [ ] `GatherWeights` 의 두 수치가 **에셋에** 있다 (코드 상수 0)

**⚠️ 오해 위험**

| 그럴듯한 오해 | 차단 |
|---|---|
| "정확하려면 후보마다 A\*를 돌린다" | 🔴 노드 수 × 주민 수 만큼 경로탐색이 돈다. **근사**로 간다 (ADR-T2-3) |
| "`FindNearestDiscovered` 를 고쳐 쓰면 된다" | 🔴 회귀 원인을 가를 수 없다. 새 함수 + 중립 불변식 |
| "점수는 높을수록 좋게" | 거리의 확장이므로 **낮을수록 좋다**. 부호를 뒤집으면 기존 게이트가 조용히 반대로 통과한다 |
| "위험 항을 지금 채운다" | W4는 **자리만** 만든다. 채우는 것은 W6 — 한 커밋에 섞으면 원인을 못 가른다 |
| "`HasDiscovered` 도 같이 바꾼다" | 🔴 그것은 **존재 여부**(플래너 시야)다. 점수와 무관 — 건드리면 플랜이 흔들린다 |

**작업량** 중 · **심각도** 🔴 높음 (차수의 심장) · **의존** W1 (지형 비용을 읽으려면 노드가 제 땅에 있어야 한다)

---

## §7. W5 — 들판 상주 위협

**대상**: [`ThreatService.cs`](../Assets/Scripts/M0/World/ThreatService.cs) · `ThreatSO` 에셋

**설계**: 새 상태를 만들지 않는다 (ADR-T2-6). 상주 = **퇴장 조건이 없는 배회**다.
`WanderAnchor` 를 진입점이 아니라 **지형 조건을 만족하는 칸**으로 잡고, 목표 달성 퇴장
(`ExitOnGoal`)과 무리 도주선을 적용하지 않는다.

```csharp
// ThreatSO — append
[Tooltip("상주형인가 (M26-2차 W5). true 면 침공 웨이브가 아니라 **판 시작부터 들판에 산다**. " +
         "🔑 퇴장하지 않으므로 압력 예산(ADR-M24-1)에 **계상하지 않는다** — 웨이브와 다른 자다.")]
public bool IsResident;

[Tooltip("어느 지형에 사는가. 비우면 아무 데나 (⚠️ 마을 안전반경은 코드가 이미 막는다).")]
public TerrainTypeSO[] HabitatTerrain;
```

**DoD**

- [ ] `IsResident = false` 인 기존 4종의 동작이 **완전히 동일** (게이트 `M26B_T8` — 압력·편성·진입점 전수 비교)
- [ ] 상주 개체가 마을 안전반경 **안에 스폰되지 않는다**
- [ ] 상주 개체가 압력 예산 계산에 **들어가지 않는다** (`ADR-M24-1` 불변 — 압력은 어떤 경로로도 안 내려간다)
- [ ] 상주 개체는 퇴장하지 않는다 (`BeginExit` 호출 0)
- [ ] 새 러너 0 · 새 상태 0 (1차 W7과 같은 규약 — diff로 확인)
- [ ] 게이트 `M26B_T8`: 상주 위협이 **지형 조건을 만족하는 칸에만** 있다 (전수)

**⚠️ 오해 위험**

| 그럴듯한 오해 | 차단 |
|---|---|
| "상주도 위협이니 압력에 넣는다" | 🔴 **아니다.** 압력은 웨이브 예산이다. 상주를 넣으면 시간이 지나도 안 죽는 개체가 예산을 영구 점유한다 |
| "상주니까 안 움직인다" | 배회는 **정지가 아니다** (M21-W2R 규약). `PickWanderTile` 을 그대로 쓴다 |
| "늪이 방어선이니 상주를 늪에 몰아넣는다" | 그러면 늪이 **더 위험해져** 1차의 "늪 = 방어선" 의도와 반대가 된다. 서식지는 에셋이 정하고 **값은 Play 후** |

**작업량** 중 · **심각도** 중 · **의존** W1·W2 (지형 조건 판정 재사용)

---

## §8. W6 — 위험 기억 (당해 보고 안다)

**대상**: `Assets/Scripts/M0/World/` 신규 1파일 · `VillagerAgent` · `AgentConfigSO`

**넓히기 ② 새 선반**. 완성 후 늘어나는 것: *"무엇이 기억을 남기는가"* 가 **에셋 필드 1개**.

```csharp
/// <summary>위험 기억 (M26-2차 W6) — **개인이 갖는다** (ADR-T2-4). 마을은 모른다.
/// 🔑 좌표가 아니라 **격자 칸** 단위로 기억한다: 정확한 좌표를 기억하면 한 칸 옆으로
/// 비켜서 똑같이 당한다. 잊는 것은 시간이 한다 (감쇠).</summary>
public sealed class DangerMemory
{
    public void Remember(int tileX, int tileY, float amount);
    public float PenaltyAt(int tileX, int tileY);   // W4의 dangerAt 에 그대로 꽂힌다
    public void Decay(float deltaGameDays);
}
```

**DoD**

- [ ] 아무도 안 다친 판에서 `PenaltyAt` 이 **전 칸 0** = W4가 舊 동작 (중립 불변식)
- [ ] 피해 원인이 `DamageCause.Combat` 일 때만 기억이 남는다 (굶주림·재해는 자리 탓이 아니다)
- [ ] 기억은 **개인별**이다 — 한 명이 당해도 다른 주민의 `PenaltyAt` 은 0 (게이트 `M26B_T6`)
- [ ] 감쇠가 순수 함수이고 게이트가 검산한다 (남은 값이 시간에 대해 **단조 감소**)
- [ ] 게이트 `M26B_T6`: 기억이 있으면 **더 가까운 노드를 두고 먼 노드를 고른다**
- [ ] 세이브 대상 여부를 주석으로 **선언**한다 (`ADR-M0-10`)
- [ ] 새 슬롯 **0개** (예산 잔여 1칸을 쓰지 않는다 — 플래너가 알 필요가 없는 정보다)

**⚠️ 오해 위험**

| 그럴듯한 오해 | 차단 |
|---|---|
| "슬롯으로 만들어 플래너가 보게 한다" | 🔴 잔여 1칸이고, 플래너는 **어느 노드인지** 모른다 (`ADR-M0-6` — 플랜은 인덱스다). 러너 선택 단계의 정보다 |
| "마을 전체가 알면 편하다" | ADR-T2-4 — 정보가 공짜가 되고 한 명의 불운이 전원을 잠근다 |
| "정확한 좌표를 기억한다" | 한 칸 옆에서 똑같이 당한다. **격자 칸** 단위 |
| "죽은 사람의 기억은 남긴다" | 개인 기억이다. 죽으면 사라진다 — 그것이 **상실의 대가**다 (재미 건강 기준) |

**작업량** 중 · **심각도** 🔴 높음 (새 선반) · **의존** W4 (`dangerAt` 자리) · W5 (당할 일이 있어야 한다)

---

## §9. W7 — 구조 명령 UI + 위치 없는 부상 알림

**대상**: [`PlayerInputController.cs`](../Assets/Scripts/M0/UI/PlayerInputController.cs) ·
[`SeasonHud.cs`](../Assets/Scripts/M0/UI/SeasonHud.cs)

🔑 **실사 결과 치료사·부상·간호가 전부 이미 있다** (전제 3·4). 이 W는 **화면만** 만든다 —
새 직업 0 · 새 goal 0.

### 🔴 W7-0 — 먼저 검산한다 (성공 기준 6)

부상 방치 사망 소요가 **맵 끝에서 마을까지 걷는 시간보다 짧으면 구조는 구조적으로 불가능하다.**
`InjuryDeathAfterDays(cfg)` 와 맵 대각선 ÷ 이동속도를 순수 함수로 비교한다.
**짧으면 W7을 만들지 않고 수치 판단으로 되돌린다** — 장식을 짓지 않는다.

**DoD**

- [ ] 게이트 `M26B_T7`: `InjuryDeathAfterDays` > 맵 최장 이동 시간 (검산). **미달이면 W7 중단하고 보고**
- [ ] 부상자 발생 시 알림이 뜬다 — **위치를 말하지 않는다** (찾는 것이 플레이어의 일)
- [ ] 치료사를 클릭하면 **부상자만** 목록에 뜬다 (건강한 주민은 안 뜬다)
- [ ] 목록에서 고르면 기존 `Goal_TreatInjured` 경로로 들어간다 (**새 goal 0** — diff로 확인)
- [ ] 치료사가 없는 판에서 UI가 **뜨지 않고 에러도 없다** (전제: 직업 랜덤이라 치료사 없는 판이 실재)
- [ ] 구조하러 간 치료사도 **똑같이 위험하다** (들판 위협의 예외가 아니다 — 확인)

**⚠️ 오해 위험**

| 그럴듯한 오해 | 차단 |
|---|---|
| "치료사 직업을 새로 만든다" | 🔴 **이미 있다** (`Job_Medic.asset`). 실사 전제 3 |
| "알림에 좌표를 찍어 준다" | 🔴 그러면 찾는 재미가 사라진다. **위치 없는 알림**이 예고의 요구다 |
| "치료사에게 무적을 준다" | 🔴 구조가 안전하면 결정이 아니다 (재미 건강 기준) |
| "부상 시계가 짧으면 늘리면 된다" | 🔴 그 값은 M21의 짝이 있다 (`InjuredBelowHp` · `BleedHpPerDay`). **M17 규약** — 짝을 전수 열거하기 전에는 손대지 않는다 |

**작업량** 중 · **심각도** 중 · **의존** W5 (다칠 일) · W7-0 검산 통과

---

## §10. W8 — 게이트 마감

`M26B_T1` ~ `T8` 를 `Assets/Tests/EditMode/M26B_TerrainPhase2Gates.cs` 에 모은다.

**DoD**

- [ ] 기존 **464건 전원 무수정 통과**
- [ ] 새 게이트마다 **실패 가능성 증명**이 붙어 있다 (조건을 끄면 red 가 된다)
- [ ] 🔴 **검사가 답을 강요하지 않는지 먼저 본다** — 1차 `M26_T4` 가 늪을 맵 가로지르는 띠로 깔아 우회 자체를 불가능하게 만들었던 사건의 재발 방지. 각 게이트에 **고정한 축**을 주석으로 적는다 (방법론 M20)
- [ ] 게이트 총계를 명세 진행 상태 표에 기록

**작업량** 중 · **의존** W1~W7

---

## §11. 스코프 가드 — 이번에 하지 않는 것

| 안 하는 것 | 어디로 |
|---|---|
| 정확한 경로 비용 기반 채집 선택 (후보마다 A\*) | 3차 이후 — 2차는 근사 (ADR-T2-3) |
| 위험 기억의 **전달**(대화로 퍼짐) | 3차 — `ChatterSO` 가 있어 에셋 1칸 |
| 성격별 기억 지속(겁쟁이는 오래) | 3차 — 자리만 남긴다 (ADR-T2-4) |
| 새 `ResourceType` · 새 슬롯 | 밖 — 예산 잔여 1칸 (ADR-T2-5) |
| 정글·광산·모래 지형 **추가** | 이번엔 기존 5종에 조건만 건다. 지형 신설은 에셋 1칸이라 언제든 |
| 팩 타일 오토타일 | **여전히 밖** — 1차와 같음. 이 축 최대 덩어리라 별도 판단 |
| 대장간 · 상위 함정 | 3차 (⚠️ 대장장이 = M25 3차에 막힘) |
| 늪 수치·지형 비율·안전반경 조정 | **§14 손잡이** — 에셋이라 언제든. 늪 색은 이미 반영 (`#6B784A` → `#3F5D4A`) |

---

## §12. 커밋 분할 (1항목 1커밋 · 각 커밋 단독 실행 가능)

| # | 커밋 | 단독 실행 시 |
|---|---|---|
| 1 | `fix(goap): M26-2차 W1 — 자원 노드가 물 위에 서지 않는다` | **1차 결함 수정.** 이것만으로 판이 좋아진다 |
| 2 | `feat(goap): M26-2차 W2 — 지형이 무엇을 낳는지` | 에셋 미배선이면 **동작 변화 0** |
| 3 | `chore(goap): M26-2차 W3 — 거리 = 희귀도` | 에셋만. 코드 0줄 |
| 4 | `feat(goap): M26-2차 W4 — 채집 선택 통로` | 가중치 0 배포면 **오늘과 동일** (중립) |
| 5 | `feat(goap): M26-2차 W5 — 들판 상주 위협` | 여기서 들판이 위험해진다 |
| 6 | `feat(goap): M26-2차 W6 — 위험 기억` | **주민이 배운다** — 여기서 게임이 바뀐다 |
| 7 | `feat(goap): M26-2차 W7 — 구조 명령 UI` | 화면만 (검산 통과 시) |
| 8 | `test(goap): M26-2차 W8 — 게이트 마감` | — |

⚠️ **중간 리뷰 = 커밋 4 직후.** 여기서 처음으로 주민의 **선택 규칙**이 바뀐다. 중립 불변식이
깨지지 않았는지(가중치 0 = 오늘과 동일), 채집 성공률이 떨어지지 않았는지를 보고 W5로 간다.
🔴 **커밋 1 직후에도 본다**: 노드가 물에서 나왔는지, 그 결과 `Unreachable` 이 줄었는지.

---

## §13. 재미 검증 — 플레이어가 화면에서 무엇을 새로 보게 되는가

지금 지도는 **예쁘지만 균질하다.** 호수를 돌아가는 것은 보이지만, 돌아가서 얻는 것은
바로 옆 나무와 똑같다. 땅이 달라도 **결과가 같으니 판단할 것이 없다.**

이 차수가 끝나면 땅마다 나는 것이 달라진다. 은은 늪 안쪽에만 있고, 물고기는 물가에만 선다.
그리고 들판에는 **돌아다니는 것들이 산다.** 채집하러 나선 주민이 처음으로 갈림길에 선다 —
가까운 덤불은 늪 건너에 있고, 안전한 덤불은 두 배 멀다. 주민은 늪을 피해 먼 쪽으로 돌아가고,
화면에는 *"가까운 후보를 두고 먼 후보를 골랐다"* 가 뜬다.

그리고 한 번 당하고 나면 **그 자리를 피한다.** 플레이어가 아무것도 안 가르쳐 줬는데, 영희는
어제 오크에게 당한 남쪽 숲을 오늘 지나치고 북쪽으로 간다.

판이 끝났을 때 남는 말이 달라진다 — *"이번 판은 은이 늪 건너에만 있어서, 결국 아무도
못 갔던 판이었어."*

---

## §14. 이월 (1차 §14에서 넘어온 것 중 이 차수가 다루지 않는 것)

| 항목 | 상태 |
|---|---|
| 늪 색 | ✅ **해소** (2026-08-10 — `#6B784A` → `#3F5D4A`, 젖은 땅으로 읽히게) |
| 늪 수치 (`EnterCost 3` · `MoveSpeedMult 0.5`) | 손잡이 — W4가 이 값을 **읽게 되므로** 중간 리뷰에서 재판단 |
| 절벽 비율 `AppearsAbove 0.8` | 🔑 **정정됨**: 0.2%는 시드 `803696646` 한 판의 특성이다 (다른 시드 실측 6.9~7.8%). 설정 문제 아님 |
| 안전반경 12 | 손잡이 — W2가 자원 배치를 좁히므로 함께 본다 |
| 연대기 `RunSeed` 화면 노출 | 이월 유지 |
| 관찰 3종 (자원 이동 · 늪 방어 · 물 차단) | 🔑 **"물이 길을 너무 막는가"는 W1 이후에 다시 재라** — 원인이 노드 배치일 수 있다 |
