# 가로형 롱폼 영상 트랙 실행명세서

*작성 2026-08-09. 브레인스토밍 합의(같은 날, 세션 내 절별 승인) 기반.*
*트랙: `tools/scene-video/` 확장 + 게임 리포 최소 가산. 온디맨드 제작 — 크론·루틴 없음.*

---

## 0. 성공 기준 (숫자)

| # | 기준 | 측정 |
|---|---|---|
| S1 | 파일럿 `lf01` 완주: 길이 **300~600초**, wide 프로필 check 전 항목 green | `check.mjs lf01` 출력 |
| S2 | 쇼츠 무회귀: 기존 쇼츠 1편(ep12s-1) 재렌더 결과가 작업 전과 **프레임 단위 동일** | 스틸 N장 픽셀 diff 0 (W1 DoD) |
| S3 | 게임 리포: 컴파일 오류 0 · M0-T 게이트 green · **촬영 비활성 시 게임플레이 diff 0** | 커밋 전 체크 4종 |
| S4 | 촬영 세션 1회(30~60분 방치)에서 유효 컷 후보 **10건 이상** (제안치 — 미달이면 우선순위표를 조정하지 세션 시간을 늘리는 것부터 하지 않는다) | `cuts.json` 항목 수 |
| S5 | 촬영 중 Claude 토큰 소모 **0** (시작 스크립트 실행 이후 무개입) | 공정 정의로 보장 |

## 0.5 전제 확인 (명세 전 실사 산출물 — 2026-08-09 코드 실사)

| 전제 | 실사 결과 |
|---|---|
| 이벤트가 한 곳에 모인다 | ⚠️ **부분만 참.** `ChronicleService.RecordEvent`(ChronicleService.cs:136)에 10종(Injured·Healed·GotHome·OrderTaken/Refused·Built·Traded·FoodShared·Repelled·Hunted)이 모이지만 **사망·위협 출몰/공성 타격·계절 전환은 Chronicle에 없다.** 서비스별 이벤트(ThreatService.OnRaidRepelled·CombatService.OnHunted·ConstructionService.OnCompleted 등 — Chronicle 자신이 구독 중)를 함께 쓴다. → W3 첫 DoD가 이벤트 소스 전수표 작성 |
| 사건 좌표를 얻을 수 있다 | ✅ `SimulationLoop.Agents`(:262, IReadOnlyList 공개) → subjectId 로 에이전트 → TileX/TileY. 위협 좌표는 ThreatService 활성 목록에서 |
| 게임에 사운드가 있다 | ❌ **없다** (AudioSource/AudioClip 0건). 클립은 무음 — 오디오는 전부 파이프라인 트랙 |
| 해상도 상수가 흩어져 있다 | ❌ 집중돼 있다. `lib-node.mjs:82~85`(STAGE_W 392·OUT 1080×1920·DSF·VIEW_H)가 render/check 공용. CSS 는 `engine/index.html:7` 정적 링크 1곳 |
| 렌더에 길이 상한이 있다 | ❌ 없다. 프레임 수 = `timeline.totalMs` 비례 (render.mjs:158). 실적 149.2초 |
| build 산출물이 git에 있다 | ❌ 없다 (`.gitignore` `build/`). D: 사본이 백업이다 — 특히 **녹화 원본은 재생성 불가능** |
| 쇼츠 무인 실행이 살아 있다 | ⚠️ 클라우드 크론은 폐기. 로컬 태스크 2개(매일 15:00·로그온)는 등록돼 있으나 밀린 대본이 없어 exit 0 반복 — 정리는 곁가지 목록으로 |
| 무료 저장 공간 | ✅ D: 931GB 여유. 기존 26회차 440MB 미러 완료(`D:\AI_GOAP-videos\episodes\`) |

## 0.6 스코프 가드 — 이번에 하지 않는 것

- 클라우드 루틴·크론·`backlog.mjs` 편입 — 롱폼은 온디맨드다
- 디버그 강제 스폰·연출 씬 (ADR-LF-5)
- 게임플레이·밸런스·UI 변경 일절 (녹화는 관전자다)
- BGM 외부 음원 도입 — 파일럿 실측 후 별도 결정
- 영어판(`--lang en`) 롱폼 — 한국어 파일럿 먼저
- 쇼츠 세로 경로의 어떤 수정도 (가산만)
- 로컬 예약 태스크 2개 정리 (곁가지 — 밀스톤 사이)

---

## 1. ADR (구현 중 갈릴 판단 — 바꾸려면 사유를 커밋에)

- **ADR-LF-1 가산 원칙**: 세로(쇼츠) 경로의 상수·CSS·검사 기준은 diff 0. 프로필 분기는
  `scene.json`의 `format` 필드("wide"/생략="tall") **한 곳**에서만 갈라진다.
- **ADR-LF-2 회차 id = `lf\d+`**: `epNNs` 계열 정규식(`backlog.mjs` 등)에 절대 안 잡히는
  이름 공간. 분할 id(`ep05s-1`)가 안전했던 전례를 따른다.
- **ADR-LF-3 엔진 = 유일한 합성기**: 게임 클립도 엔진 무대 안 `<video>`로 합성한다.
  ffmpeg 이어붙이기(합성기 2개) 금지 — 자막·HUD·check 가 두 경로로 갈라진다.
  렌더 시간이 문제로 실측되면 그때 사유와 함께 재검토.
- **ADR-LF-4 촬영 = Claude 무개입 독립 공정**: 시작은 스크립트 1회 실행, 산출물은
  `D:\AI_GOAP-videos\clips\`. 파이프라인은 수확물을 소비만 한다.
- **ADR-LF-5 연출 금지**: 대본이 원하는 장면이 수확에 없으면 **세션을 더 돌린다.**
  디버그 명령으로 장면을 만들지 않는다 — 클립은 관측이지 연출이 아니다.
- **ADR-LF-6 소재 정본 사다리**: 발행 글이 있는 구간은 발행 글이 우선. 없는 구간
  (M16~M23)은 리포 안 1차 소스(`devlog/sessions/`·`Docs/` 명세서·git 커밋)가 정본.
  검수는 모든 수치·사건 서술에 출처 파일을 명시해 대조한다.
- **ADR-LF-7 트랙 간 시점 규칙**: 롱폼의 과거 사건은 완결형("허물었습니다"), 쇼츠는
  연대기 시점 유지. 롱폼이 미래 쇼츠 아크 소재를 언급하면 한두 문장 + "전말은 시리즈에서"
  유도로 절제한다. 미래형 서술은 "지금"·"최종 목표" 절에만 허용.
  (검증 사례: 화폐는 M17 구현 → M19 전면 철거가 확정 상태 — "추가 예정" 서술은 오류다)
  🔑 유도 문구("전말은 시리즈에서")는 **실제로 쇼츠가 나올 소재에만** 약속한다 —
  쇼츠는 블로그 발행 궤도를 따르므로, 그 궤도에 없는 소재를 가리키면 지켜지지 않는
  약속이 된다. 검수는 유도 문구마다 대상 소재가 발행 궤도(발행 글 또는
  `BLOG_COVERAGE.md` 대기열)에 있는지 확인한다.
- **ADR-LF-8 게임 리포 변경 봉인**: 허용 목록 = ①`ChronicleService`에 C# 이벤트 1개 +
  invoke 1줄 ②신규 폴더 `Assets/Scripts/M0/Capture/` ③Unity Recorder 패키지
  (`Packages/manifest.json`) ④(부족 시) 서비스에 가산 이벤트 선언. 이 밖의 게임 파일
  diff 는 반려. 구독자 없으면 전부 no-op.
- **ADR-LF-9 실측 전 규칙 금지**: 렌더 시간 허용치·BGM 보강 여부·챕터 길이 조정은
  파일럿 실측 후에만 결정한다.

---

## 2. 작업 항목

### W1 — 엔진 가로 프로필 (해상도·CSS·뷰포트)

**대상**: `tools/scene-video/lib-node.mjs` · `engine/index.html` · `engine/engine.js` ·
`engine/wide.css`(신규) · `render.mjs` · `check.mjs`

**스케치**:
```js
// lib-node.mjs — 기존 상수는 그대로 두고(쇼츠가 쓴다) 프로필 함수를 추가
export const PROFILES = {
  tall: { STAGE_W: 392, OUT_W: 1080, OUT_H: 1920 },            // 기존 값 그대로
  wide: { STAGE_W: 697, OUT_W: 1920, OUT_H: 1080 },            // 697 = 392×16/9 반올림(제안치
};                                                              //  — 구현 때 폰트 px 정합으로 확정)
export function dimsOf(scene) {
  const p = PROFILES[scene.format === 'wide' ? 'wide' : 'tall'];
  return { ...p, DSF: p.OUT_W / p.STAGE_W, VIEW_H: Math.round(p.OUT_H / (p.OUT_W / p.STAGE_W)) };
}
```
```js
// engine.js boot — scene.json 을 읽은 직후, 그리기 전에
if (scene.format === 'wide')
  document.querySelector('link[href="style.css"]').href = 'wide.css';
```
- `render.mjs`/`check.mjs`: 뷰포트·인코딩 크기를 `OUT_W/OUT_H` 직접 참조 대신
  `dimsOf(scene)` 결과로. tall 회차는 값이 동일하므로 동작 불변.
- `wide.css`: `style.css`를 복사해 % 앵커만 가로 레이아웃으로 재계산
  (상단 HUD 얇은 띠 · 중앙 비주얼 · 하단 자막). **style.css 는 한 글자도 안 바꾼다.**

**DoD** (✅ 2026-08-09 완료):
- [x] `lf01` 더미 씬(`format:"wide"`, 캔버스 샷 2개)이 1920×1080 mp4 로 렌더된다
      — 렌더 로그 `프레임 1920×1080` (스케일 보정 없이 원판), `5.7초 · 1920×1080 · 30fps`
- [x] `format` 없는 기존 씬은 1080×1920 그대로 (dimsOf 기본 tall) — 아래 스틸 diff 가 증명
- [x] ep12s-1 재렌더 → 작업 전 스틸 3장(0s·18s·35s) **바이트 동일** (cmp 3/3)
- [x] serve.js 미리보기에서 wide 회차가 가로 무대로 뜬다 — `.stage` 실측 697×392.06
      (참고: HUD 의 AI 훅 줄은 engine.js:206 이 의도적으로 숨긴다 — 4단 개정 때
      아웃트로 카드로 이동한 것. wide 결함 아님)

**⚠️ 오해 위험**: "가로니까 CSS 폭을 1920 으로" — 금지. render.mjs:9 주석 그대로,
글자 px 고정이라 **CSS 폭은 697 수준 + DSF 로 올린다.** 쇼츠와 같은 원리다.

**작업량/의존**: 중 / 🔴(전 트랙의 기반) / 의존 없음 — 첫 커밋

### W2 — check.mjs wide 검사 프로필

**대상**: `tools/scene-video/check.mjs` · `youtube-editor/CAPTION_SAFE_AREA.md`(참조 정본)

**스케치**: 세로 전용 검사(쇼츠 UI 안전영역 SAFE_BOTTOM=1477 계열)를 프로필로 분기.
wide 는 youtube-editor 16:9 원판 규격(자막 안전영역·수직 예산)을 **처음으로 원판 그대로**
적용한다. 잘림·정적 구간·팔레트·결정성 검사는 공용(이미 해상도 무관 로직).

**DoD** (✅ 2026-08-09 완료):
- [x] wide 회차에서 자막이 16:9 안전영역을 벗어나면 red — 음성 시험 실증: `.cap` 을
      bottom:-1% 로 내리자 `자막 바닥 1091 > 한계 1060` 🔴, 원복 후 전부 통과
- [x] tall 회차 검사 결과가 작업 전과 항목·판정 모두 동일 — ep12s-1 출력 diff 0
      (신규 「무대 프로필 일치」 검사는 이를 위해 wide 전용으로 좁힘)
- [x] 클립 샷(W5)의 정적 검사 예외 규칙: 정적 검사에 프로필·샷 종류 예외를 두지 않았다
      — wide 에서도 그대로 판정됨(lf01 「정적 구간 최대 1.2s」)
- 함께 들어간 것: 4단 구조·쇼츠 길이 대역·예고 사슬 검사는 wide 에서
      「쇼츠 규격 비대상」으로 통과(정본 = W7 문법 문서, 승격은 그 뒤 — ADR-LF-9).
      wide 길이는 판정 없이 실측만 표기

**⚠️ 오해 위험**: "가로는 안전영역이 넓으니 검사를 빼자" — 금지. 유튜브 플레이어
컨트롤 바·자막 토글이 하단을 덮는다. youtube-editor 규격이 이미 그걸 계산해 뒀다.

**작업량/의존**: 소 / 🟠 / W1 뒤

### W3 — Unity 자동 녹화 (게임 리포 — ADR-LF-8 봉인 안에서만)

**대상**: `Assets/Scripts/M0/World/ChronicleService.cs`(이벤트 1개+invoke 1줄) ·
`Assets/Scripts/M0/Capture/`(신규: CaptureDirector.cs·CaptureCameraRig.cs·CutLog.cs) ·
`Packages/manifest.json`(Unity Recorder) · `tools/scene-video/capture-session.cmd`(신규)

**스케치**:
```csharp
// ChronicleService — 가산. 구독자 없으면 no-op (S3 보장)
public event System.Action<ChronicleEvent> OnEventRecorded;   // 신규
public void RecordEvent(...) { ...기존...; OnEventRecorded?.Invoke(ev); }  // invoke 1줄
```
```csharp
// CaptureDirector (씬에서 비활성이 기본 — capture-session 부팅에서만 활성)
// ① 이벤트 소스 전수표(첫 DoD)의 소스들을 구독
// ② 우선순위표: 사망 > 위협 출몰·공성 > 격퇴·사냥 > 완공 > 거부 > B-roll(주기 전경)
// ③ 사건 좌표 해석: subjectId → _sim.Agents / 위협 → ThreatService 활성 목록
// ④ CaptureCameraRig: 메인 카메라를 사건 좌표로 러프(CameraController 무수정 —
//    촬영 세션은 무인이라 카메라 소유권 충돌이 없다)
// ⑤ CutLog: {recSec, kind, subject, detail} 를 cuts.json 에 append
```
- Unity Recorder 는 세션 전체를 상시 녹화(1920×1080, 30fps) —
  출력 `D:\AI_GOAP-videos\clips\<yyMMdd-HHmm>\raw.mp4` + 같은 폴더 `cuts.json`.
- `capture-session.cmd`: Unity 를 열고(또는 열린 에디터에 명령) 촬영 모드 Play 시작.
  이후 사람도 Claude 도 무개입 (ADR-LF-4).

**이벤트 소스 전수표** (2026-08-09 코드 실사 — 첫 DoD 산출물):

| 장면 | 소스 | 판정 |
|---|---|---|
| 위협 예고 | `ThreatService.OnForecast(ThreatSO)` (:93) | ✅ 기존 |
| 위협 타격(주민·밭) | `ThreatService.OnStruck(t, isFarm, n, tile, victims)` (:99) — 좌표 포함 | ✅ 기존 |
| 공성 타격(구조물) | `ThreatService.OnStructureStruck(t, slot, tile, before, after)` (:150) | ✅ 기존 |
| 구조물 파괴 | `ThreatService.OnStructureDestroyed` (:153) | ✅ 기존 |
| 격퇴 | `ThreatService.OnRaidRepelled` (:61) | ✅ 기존 |
| 사냥 | `CombatService.OnHunted` (:47) | ✅ 기존 |
| 완공 | `ConstructionService.OnCompleted` (:25) | ✅ 기존 |
| 입주 | `OwnershipService.OnAssigned` (:22) | ✅ 기존 |
| 재해 | `DisasterService.OnStruck` (:30) | ✅ 기존 |
| 계절 전환 | `SeasonService.OnSeasonChanged` (:66) | ✅ 기존 |
| 부상·치유·거부·수락·거래·나눔 | `ChronicleService.OnEventRecorded` | ➕ 가산 (이벤트 1 + invoke) |
| **사망** | Chronicle EventId 에 없음 → `M0SimulationLoop.OnAgentDied` (RecordDeath = Die/StarveToDeath 공통 문) | ➕ 가산 (이벤트 1 + invoke) |

중복 방지: Chronicle 훅에서는 서비스 이벤트가 따로 있는 종류(GotHome·Built·Repelled·
Hunted)를 받지 않는다 (`PriorityOfChronicle` = -1). 좌표는 월드 = 타일 (ADR-M0-9,
무덤 생성 코드가 증명). 시간축 = 프레임/30 (Recorder CapFPS — 벽시계 아님).

**DoD**:
- [x] **이벤트 소스 전수표** 작성 완료 — 위 표. 가산은 2건뿐(둘 다 구독자 없으면 no-op)
- [ ] 촬영 비활성 상태에서 컴파일 0에러 · M0-T green · 게임플레이 로그 diff 0
- [ ] 촬영 세션 30분 방치 → raw.mp4 + cuts.json 생성, 유효 컷 후보 ≥ 10 (S4)
- [ ] cuts.json 의 recSec 로 raw.mp4 를 열었을 때 해당 사건이 실제로 프레임 안에 있다
      (수동 표본 3건)
- [ ] Editor 종료 시 NativeArray leak 경고 0 (기존 체크 4종 그대로)

**⚠️ 오해 위험**: "사건 감지 후 녹화 시작" — 금지. 사건은 예고 없이 시작되므로 앞부분이
항상 잘린다. **상시 녹화 + 컷 리스트**다. / "Chronicle 에 없는 사건을 Chronicle 에 추가" —
Chronicle 은 주민 서사 아카이브다(M15). 촬영용 사건은 서비스 이벤트 구독으로 해결하고,
Chronicle 의 의미를 넓히지 않는다.

**작업량/의존**: 대 / 🔴 / W1·W2 와 독립 — 병행 가능

### W4 — clips.mjs (수확물 인제스트)

**대상**: `tools/scene-video/clips.mjs`(신규)

**스케치**:
```js
// node clips.mjs ingest <세션폴더>   → cuts.json 앞뒤 여유(제안: 앞 4s·뒤 3s)로 슬라이스,
//                                      library/<kind>-<세션>.mp4 + library.json 갱신
// node clips.mjs stills <clipId>    → 스틸 3장 추출 (컷 채택 전 빈 컷 검사용)
// 원본 raw.mp4 는 지우지 않는다 — 재슬라이스 가능해야 한다
```
`library.json` 항목: `{ id, kind, subject, detail, durSec, session, src }` —
대본 세션에서 저는 이 카탈로그만 읽는다 (토큰 절약, ADR-LF-4 의 소비 창구).

**DoD**:
- [ ] W3 수확물 1세션을 ingest → library.json 에 항목 생성, 슬라이스 mp4 재생 확인
- [ ] stills 로 뽑은 3장에서 사건 주체가 화면 안에 있는 컷/없는 컷이 구분된다
- [ ] 같은 세션을 두 번 ingest 해도 중복 항목이 안 생긴다 (id 멱등)

**⚠️ 오해 위험**: 슬라이스를 스트림 카피(-c copy)로 — 키프레임 경계라 시각이 밀린다.
**재인코딩으로 정확히 자른다** (품질은 CRF 낮게).

**작업량/의존**: 소 / 🟠 / W3 뒤

### W5 — 엔진 클립 샷 (`type:"clip"`)

**대상**: `engine/engine.js` · `engine/lib.js`(필요 시) · `render.mjs` · `serve.js`

**스케치**: 샷에 `clip: {id, in, out}` 이 있으면 무대 비주얼 영역에 `<video>` 를 놓고,
렌더 프레임마다 `video.currentTime = 샷 내 경과초 + in; await seeked` 로 프레임 정합
(결정성 — Math.random 금지 원칙과 같은 이유). 자막·HUD·챕터칩은 기존 레이어 그대로 위에.
클립 파일은 `D:\AI_GOAP-videos\clips\library\` 에서 serve.js 가 서빙 경로로 노출
(리포에 복사하지 않는다 — .gitignore 대상도 아니게).

**DoD**:
- [ ] 클립 샷 1개짜리 테스트 씬이 렌더되고, 같은 명령 2회의 mp4 가 프레임 동일 (결정성)
- [ ] 클립 위 자막이 캔버스 샷과 같은 위치·스타일로 찍힌다
- [ ] "지금 빌드 화면" 구분 표기가 클립 샷에 뜬다 (ADR-LF-7 사실 정직성 — 엔진이
      clip 샷에 자동 배지, 대본이 끌 수 없다)
- [ ] 렌더 시간 실측 기록 (클립 프레임/캔버스 프레임 각각 — ADR-LF-9 의 재료)

**⚠️ 오해 위험**: `video.play()` 로 흘려 두고 캡처 — 프레임 캡처와 어긋나 결정성이 깨진다.
**반드시 seek 방식.** / 클립을 리포로 복사 — 용량·공개 리포 문제. D: 에서 서빙한다.

**작업량/의존**: 중 / 🔴 / W1·W4 뒤

### W6 — 합성 앵비언트 + 아카이브 단계

**대상**: `tools/scene-video/sfx.mjs` · `publish.mjs`

**스케치**: sfx.mjs 에 `pad` 계열(저역 드론, 씨앗 있는 xorshift — 기존 6종과 같은 규약,
단기 RMS 정규화·피크 천장 준수) 추가. 씬이 `ambient: {kind, gain}` 을 선언하면
buildTrack 이 구간에 깐다. publish 완주 마지막에
`robocopy build → D:\AI_GOAP-videos\episodes\<ep>\build` 미러 (쇼츠·롱폼 공통).

**DoD**:
- [ ] pad 가 나레이션 밑에서 들리되 단기 RMS 가 나레이션(0.115~0.122)보다 최소 −12dB
      (제안치 — 기존 DEFAULT_GAIN 0.060 체계와 정합 확인 후 확정)
- [ ] ambient 미선언 회차(기존 쇼츠 전부)는 트랙이 바이트 동일
- [ ] publish 완주 시 D: 미러가 생기고, 재실행 시 변경분만 복사된다

**⚠️ 오해 위험**: 8분 내내 같은 패드 — "정적을 메우려고 깔지 않는다"(효과음 원칙)는
패드에도 적용. 챕터 전환 등 대본이 부르는 자리에만.

**작업량/의존**: 소 / 🟡 / W1 뒤 (앵비언트는 lf01 대본과 병행)

### W7 — 롱폼 대본 문법 문서 + 에이전트 배선

**대상**: `Docs/롱폼_대본_문법.md`(신규) · `.claude/agents/scene-planner.md`·
`scene-writer.md`·`scene-reviewer.md`·`scene-master.md`(각 1~3줄 가산)

**내용 (브레인스토밍 합의의 박제 — memory 가 아니라 에이전트가 읽는 파일에)**:
- 구조: 콜드오픈 30~60초 → 챕터 4~6개(각 1~2분 미니 아크: 질문→사건→관측→교훈) → 마무리
- 계승: 콜드오픈·열린 고리·이름 붙은 개념·모든 샷에 그림·원문에 없는 문자열 금지·돌려막기 금지
- 롱폼 신규: 챕터 카드 허용 / 길이 300~600초(자막 100~180개 규모) / 클립 샷 규칙 /
  youtube-editor 16:9 원판 규격
- ADR-LF-6 소재 정본 사다리 + ADR-LF-7 시점 규칙 전문
- 에이전트 정의서: "`format:"wide"` 회차의 판정 근거는 `Docs/롱폼_대본_문법.md`" 1줄
  (ep02s "60초 상한" 사고 재발 방지 — 게이트 없는 규칙 금지)

**DoD**:
- [ ] 문서에 위 5절이 전부 있고, 쇼츠 문법과 겹치는 규칙은 중복 서술 대신 참조
- [ ] 에이전트 4개 정의서에 wide 분기 1줄씩 — 쇼츠 회차 동작 서술은 무변경
- [ ] 검수 지시서에 "출처 파일 명시 대조"(ADR-LF-6)가 반려 기준으로 들어감

**⚠️ 오해 위험**: "롱폼용 에이전트를 새로 만든다" — 아니다. 기존 4단이 문법 문서만
갈아 끼운다. 에이전트 신설은 유지 비용이고, 설명 다이어트 방침(토큰 절감)과도 충돌.

**작업량/의존**: 소 / 🟠 / 독립 — lf01 대본 전 필수

### W8 — 파일럿 lf01 제작 (리뷰③)

**대상**: `episodes/lf01/` (scene.json·kinds/·notes/)

**소재**: "우리는 왜 GOAP 로 AI 게임을 만드는가" — 8마디 골격(브레인스토밍 확정):
①콜드오픈(지금 빌드 최강 장면 + "주민은 명령을 거부할 수 있습니다") ②목표 ③여정(24,800줄
리셋) ④문제(재미 0 — 관찰자 게임) ⑤왜(유능한 관리 AI 역설·RimWorld 대조) ⑥해결(표현층·
위협 축·화폐 철거는 **과거 완결형 한두 문장**) ⑦지금(M22·M23 — 클립 주 무대) ⑧북극성.
정본: `tools/blog-automation/published/` 의 발행 글(파일명에 DRAFT 표시된 것 제외) +
`Docs/M21·M22·M23 명세서` + devlog (ADR-LF-6).

**절차**: 기획 브리프(8마디+클립 카탈로그 대조) → 작성 → 검수(출처 명시 대조) → 마스터 →
TTS(ME) → 렌더 → check → 시청 판정(사람). 수확에 없는 장면은 촬영 세션 추가(ADR-LF-5).

**DoD**:
- [ ] S1 충족 (300~600초·check green)
- [ ] 클립 샷 ≥ 5개, 전부 "지금 빌드 화면" 배지 + 스틸 검사 통과
- [ ] 검수 보고서에 8마디 전부 출처 파일 명시
- [ ] 화폐 언급이 있다면 과거 완결형 + "전말은 시리즈에서" 유도 (ADR-LF-7)
- [ ] 실측 기록: 총 렌더 시간·클립 프레임 비용·BGM 체감 판정 → memory (ADR-LF-9 재료)

**⚠️ 오해 위험**: "블로그 글 하나를 각색" — lf01 은 특정 글의 각색이 아니라 채널 정문용
창작 구성이다. 기획 브리프가 8마디 골격을 정본으로 삼는다. / 쇼츠 대본을 이어 붙이기 —
금지. 직전 회차 베끼기 실패의 롱폼판이다.

**작업량/의존**: 대 / 🔴 / W1~W7 전부 뒤

---

## 3. 커밋 분할 계획 (1항목 1커밋, 각각 단독 실행 가능)

| 순서 | 커밋 | 게이트 |
|---|---|---|
| 1 | W1 엔진 가로 프로필 | S2 무회귀 diff 0 |
| 2 | W2 check wide 프로필 | tall 판정 불변 |
| 3 | W7 문법 문서 + 에이전트 배선 | — (문서) |
| 4 | W3 게임 녹화 (게임 리포 커밋 — `feat(capture):`) | 커밋 전 체크 4종 + ADR-LF-8 diff 검사 |
| 5 | W4 clips.mjs | 1세션 ingest 실증 |
| 6 | W5 클립 샷 | 결정성 2회 렌더 |
| 7 | W6 앵비언트 + 아카이브 | 기존 트랙 바이트 동일 |
| 8 | W8 lf01 (대본 커밋 → build 는 git 밖) | S1 |

**중간 리뷰**: 리뷰① W1~W2 후(가로 더미 렌더 + 무회귀 증명을 사용자 확인) ·
리뷰② W3~W4 후(첫 촬영 세션 수확을 사용자와 함께 판정 — S4 기준) · 리뷰③ = W8 시청.

## 4. 재미 검증 절

이 트랙이 완주되면 시청자는 **채널 정문에서 이 게임의 존재 이유를 8분에 만난다** —
그리고 처음으로 **모션그래픽이 아닌 실제 게임 화면**(울타리를 두드리는 늑대, 도끼를
휘두르는 주민, 계획 고스트)을 본다. 개발자(사용자)에게는: 촬영 세션을 켜 두면 게임이
스스로 하이라이트를 골라 두는 경험 — M13 이후 쌓아 온 "보이게 만들기"가 마케팅 자산으로
회수되는 첫 지점이다.

## 5. 곁가지 (이 명세 범위 밖 — 밀스톤 사이 처리 목록)

- 로컬 예약 태스크 2개(`AI_GOAP scene-video`·`logon`) 정리 여부 — 유령 상태
- `register-task.cmd` 의 :fail 안내 갱신 (관리자 불필요 우회로)
- 쇼츠 build 미러의 정기 동기화 (지금은 1회 수동 복사만 됨 — W6 이후 자동)
