## [19:32] feat(movement): TR-1 TileReservationRegistry 신설 + AbortReason.PathBlocked

**태그:** #misc

**무엇을 했나:**
- feat(movement): TR-1 TileReservationRegistry 신설 + AbortReason.PathBlocked

**왜 이렇게 했나 (커밋 본문):**
TR-1 이동시스템 타일 예약 리팩터의 첫 단계. 유닛 코드는 아직 소비하지 않는
씨앗 커밋 — TR-2/TR-3에서 VillagerFSM/EnemyFSM이 실제로 사용한다.

- Assets/Scripts/AI/TileReservationRegistry.cs 신설 (정적 클래스, ADR-T2/T10)
  - Dictionary<Vector2Int, string> 한 벌로 관리, 6 API 노출
  - TryReserve/Release/IsOwnedBy/ReleaseAllBy/ResetAll/Count
  - TryReserve는 같은 owner 재요청 시 true (idempotent) — 이동 파이프라인 반복 확인 허용
  - ReleaseAllBy는 별도 리스트로 사후 삭제 (foreach 중 직접 Remove 시 예외 회피)
- VillagerBrain.AbortReason에 PathBlocked = 4 추가 (ADR-T5)
- VillagerBrain.FallbackByReason 크기 int[4] → int[5] (인덱스 폭발 방지)
- GameManager.Awake 최상단(Step -1)에 TileReservationRegistry.ResetAll() 훅

명세: Docs/이동시스템_타일예약_명세서.md §6 TR-1
DoD 전 항목 통과, 컴파일 진단 0건.

플레이어 눈에는 아무 변화가 없다. 이번 커밋은 앞으로 유닛이 다닐 타일을
서로 겹치지 않게 예약해두는 사물함(예약대장)을 만들어 둔 것에 가깝다.
다음 커밋(TR-2)부터 주민이, 그다음 커밋(TR-3)에서 적이 이 대장에 자기
타일을 등록하며 걷기 시작한다.

**변경 파일:**
  - Assets/Scripts/AI/TileReservationRegistry.cs
  - Assets/Scripts/AI/VillagerBrain.cs
  - Assets/Scripts/Core/GameManager.cs

- (+19:59) refactor(fsm): TR-2 VillagerFSM Rigidbody 제거 + 타일 예약 이동
  velocity 기반 Rigidbody 이동을 걷어내고 순수 Transform + TileReservationRegistry
  기반으로 재작성. tangent bias v1/v2/v3 폐기 결정을 코드에 반영하고 head-on
  상쇄를 원천 차단한다.

  주요 변경:
  - 필드/상수 소진: _rigidbody, _sphereCollider, _overlapBuffer, SEPARATION_RADIUS,
    SEPARATION_STRENGTH, STATIONARY_SNAP_THRESHOLD 전부 삭제 (ADR-T1/T9)
  - Awake의 SphereCollider/Rigidbody 자동 부착 블록 통째로 삭제
  - InjectDependencies 끝에 TileReservationRegistry.TryReserve(자기 타일) 1회 (ADR-T6)
  - Update 이동 블록을 다음 타일 예약 → Vector3.MoveTowards → 도착 시 이전 타일
    Release 파이프라인으로 교체. 예약 실패는 _waitFrames++ 후 WAIT_MAX_FRAMES(30)
    초과 시 RequestReplanning(AbortReason.PathBlocked)로 first-class 보고 (ADR-T3/T5)
  - ComputeSeparation 메서드(약 40줄) 전체 삭제
  - AbortCurrentPath: _rigidbody 텔레포트 라인을 transform.position 대입으로 교체 +
    ReleaseAllBy 후 자기 타일 재예약으로 leak 원천 차단
  - EnterDead + OnDestroy에 ReleaseAllBy 이중 안전망 (시체 5초 점유 방지)
  - WAIT_MAX_FRAMES = 30 상수 신설, _waitFrames 인스턴스 필드 신설

  DoD 검증:
  - grep -n 'Rigidbody|SphereCollider|linearVelocity|_rigidbody|_sphereCollider' → 0건
  - grep -n 'Separation|Tangent|_tangentSign|_overlapBuffer' → 0건
  - grep -n 'Brain\.TileX\s*=\s*targetX' → 0건 (ADR-9 무회귀, 커밋 전 체크 ⑦)
  - diagnostics 0건

  명세: Docs/이동시스템_타일예약_명세서.md §6 TR-2
  사용자 육안 5분 방치 테스트(주민 3명 원운동 없음)는 TR-3 진입 전 중간 리뷰
  시점에 진행. 지금 시점엔 씬에 남은 Rigidbody/SphereCollider 컴포넌트 잔재가
  그대로여도 Awake에서 참조하지 않으므로 무해(TR-4에서 씬 정리).

  플레이어 관점: 주민들이 이제 서로의 콜라이더에 밀리지 않고, 각자 자기 타일을
  '예약'해 두고 다음 타일이 비면 걸어가고 막혀 있으면 잠시 멈춰 기다린다.
  반대쪽으로 가려는 두 주민이 정면에서 만나 한 타일을 동시에 차지하려 드는
  상황은 예약대장이 원천적으로 막아 원운동이 사라진다.
  - Assets/Scripts/AI/VillagerFSM.cs

## [23:19] refactor(fsm): TR-3 EnemyFSM Rigidbody 제거 + 타일 예약 이동

**태그:** #misc

**무엇을 했나:**
- refactor(fsm): TR-3 EnemyFSM Rigidbody 제거 + 타일 예약 이동

**왜 이렇게 했나 (커밋 본문):**
VillagerFSM(TR-2)과 대칭. velocity 기반 이동을 걷어내고 순수 Transform +
TileReservationRegistry로 재작성.

주요 변경:
- 필드/상수 소진: _rigidbody, _sphereCollider, _overlapBuffer, SEPARATION_RADIUS,
  SEPARATION_STRENGTH, STATIONARY_SNAP_THRESHOLD 전부 삭제 (ADR-T1/T9)
- Awake의 SphereCollider/Rigidbody 자동 부착 블록 삭제
- Awake 끝에 TileReservationRegistry.TryReserve(초기 타일) 1회 (ADR-T6)
- Update 이동 블록을 transform.MoveTowards + OnMoveArrived 후처리로 교체
- ComputeSeparation 메서드 전체 삭제 (ADR-T9)
- State_Moving: FlowField dir 계산 후 다음 타일 TryReserve → 실패 시 _waitTicks++,
  WAIT_MAX_TICKS(5, ≈0.5초) 초과 시 AbortCurrentPath+Idle 전이 (§9 정책, ADR-T3/T5)
- OnMoveArrived: tile-step(_releaseOnArrive=true) → 이전 타일 반환;
  free-move → 도착 타일 논리 좌표 갱신 + 새 예약
- State_Scouting 텔레포트에 transform 스냅 + 예약 정리
- State_Retreating 진입 시 ReleaseAllBy(EnemyId) — free-move (base 도착 시 재예약)
- EnterDead + OnDestroy(신설)에 ReleaseAllBy 이중 안전망

DoD 검증:
- grep -n 'Rigidbody|SphereCollider|linearVelocity' → 0건
- grep -n 'Separation|Tangent|_overlapBuffer' → 0건
- grep -n 'Brain\.TileX\s*=\s*targetX' → 0건 (커밋 전 체크 ⑦)
- diagnostics 0건

명세: Docs/이동시스템_타일예약_명세서.md §6 TR-3
Villager(30 프레임)와 달리 Enemy는 State_Moving이 Tick(0.1s) 주기이므로
WAIT_MAX_TICKS = 5로 상수명·값을 조정해 실제 대기 시간 등가(≈0.5초) 유지.
§9의 'AbortCurrentPath() 후 Idle' 정책은 대기 초과 분기와 EnterDead 두 지점에서 실행.

플레이어 관점: 이제 적 유닛도 주민과 동일한 예약대장을 공유해 서로의
타일을 침범하지 않는다. 두 적 유닛이 같은 방향으로 오다 앞 유닛이 잠깐
멈추면 뒷 유닛은 순간 대기하고, 앞이 비면 곧장 이어 걷는다. 후퇴 이동은
전투 타일을 즉시 반환하기 때문에 다른 유닛이 그 자리를 빠르게 재사용
할 수 있다.

**변경 파일:**
  - Assets/Scripts/AI/EnemyFSM.cs

