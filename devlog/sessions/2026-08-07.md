## [01:04] chore(video): ep08s-3 의 폐기된 sametick.js 삭제 (onecall 로 교체됨)

**태그:** #misc

**무엇을 했나:**
- chore(video): ep08s-3 의 폐기된 sametick.js 삭제 (onecall 로 교체됨)

**왜 이렇게 했나 (커밋 본문):**
R4(8-2 와 구도 중복)로 교체된 뒤 어느 샷도 참조하지 않던 비석 파일이다.
작성팀이 삭제 수단이 없어 build 에서 throw 하는 비석만 남겼고, 사용자가 지웠다.

**변경 파일:**
  - devlog/sessions/2026-08-06.md
  - tools/scene-video/episodes/ep08s-3/kinds/sametick.js

- (+01:08) spec(goap): M21 §W2R 개정 명세 — 배회형 위협 + 계절 연동 타깃·공격성
  W2 는 게이트 320개를 통과하고 커밋됐지만 Play 에서 뒤집혔다. 화면에 나온 것은
  "원 하나가 밭 위에 서서 콘솔에만 로그를 뿜는 것"이었고, 사용자 판정은
  "이러한 늑대 무리의 체류는 내가 원한 게 아니다"였다.
  
  다만 W2 가 풀려던 문제(한 대 치고 사라지면 맞설 대상이 없다)는 남아 있으므로
  폐기가 아니라 **머무르되 정지하지 않는** 형태로 옮긴다. 브레인스토밍은 방법론 M19
  3게이트(모호성 원장 4트랙 · Refine 되비춤 · Restate 한 줄)로 진행했고,
  되비춤에서 공백 5개가 드러나 그중 둘(상주/출몰 · 움직임의 정의)이 범위를 갈랐다.
  
  합의 한 줄: 출몰한 늑대는 정지하지 않고 마을 주변을 배회하다 짧게 치고 떠나며,
  평시에도 가끔 주민을 노리되 겨울이 오면 타깃이 주민 쪽으로 기울고 더 집요해진다 —
  늑대의 그림이 아니라 동선과 주민의 반응으로 그것이 화면에 보인다.
  
  명세 전 실사(spec-write 0단계)에서 잡힌 것 둘 — 이게 설계를 바꿨다:
  🔴 R. "겨울"을 IsCrisis 로 판정하면 안 된다. Season_Summer 도 IsCrisis: 1 이라
     여름 늑대가 사나워진다. 겨울만 참인 것은 ForageFrozen 이고, 의미도 정확히 겹친다
     (야생 먹이가 얼면 늑대도 굶는다) → ADR-M21-9 로 재사용을 박제, 갈라지는 조건 명시.
  ❌ Q. 계절 → 위협 통로가 아예 없다(ThreatService 생성자에 SeasonService 부재) → 신설.
     다만 ZoneService 참조가 b51c631 이후 죽어 있어(FarmPlot.ZoneRadius=0) 그 슬롯을
     교체한다. 생성자 변경 파급은 SimulationLoop 1곳뿐 — 게이트는 static 순수 함수만 쓴다.
  
  신규 ADR 2건:
  - ADR-M21-9: "배고픈 계절" 판정 = ForageFrozen 하나. 계절이 바꾸는 것은 타깃 확률과
    체류 상한 둘뿐 — 출몰 빈도·피해량은 게임일 래칫(ADR-M10R-1)이 그대로 소유한다.
  - ADR-M21-10: 난수의 경계 — 배회 동선은 표현이라 난수 허용(WanderRunner 선례),
    타깃 종류·출몰 스케줄·희생 선정은 시드 롤 유지(ADR-M10-1). 경계선 =
    "무엇을 하는가"는 결정적, "어떻게 걷는가"는 확률.
  
  성공 기준 S9~S11 추가. ⚠️ S9(동선이 보이는가)는 **게이트로 증명할 수 없다** — 화면에서만
  판정된다. 그래서 커밋 계획에 **리뷰⓪(W2R 직후 Play)**를 문으로 걸고 "동선이 안 보이면
  W3 착수 금지"를 못박았다. W2 가 게이트 green 인 채 뒤집힌 것의 재발 방지이고,
  방법론 M20(게이트가 못 보는 축을 보고에 명시)의 적용이다.
  
  수치는 전부 제안치 + 근거 병기(§4-R). 특히 Tier1 늑대 VillagerTargetChance 0 → 0.25 는
  M10 "밭 전용" 결정의 개정이라 사유를 표에 남겼고, 초반 사망률 상승이 귀결임을 명시했다.
  
  스코프 가드 3건 추가: 상주형 야생(맵 대형화 밀스톤 이월 — 지금 맵은 100×100 평지라
  늑대를 둘 지형이 없다) · 위협 스프라이트/모션(별도 표현층 결정) · 부탁 목표 굳음 D3.
  - Docs/M21_격퇴축_실행명세서.md

**막혔던 문제 / 해결 방법:**

- 🔴 **오늘 세션의 가장 큰 교훈: 게이트 320개가 green 인 채로 W2 가 Play 에서 뒤집혔다.**
  체류형 위협은 명세 합의(2026-08-05)를 그대로 구현했고 DoD·게이트·red-check를 다 통과했는데,
  화면에 나온 것은 *"원 하나가 밭 위에 서서 콘솔에만 로그를 뿜는 것"*이었다. 사용자 판정 =
  *"이러한 늑대 무리의 체류는 내가 원한 게 아니다."*
  🔑 **성공 기준이 "동선·주민 반응"인 항목은 게이트로 증명할 수 없다.** 그래서 개정 명세에
  **리뷰⓪(W2R 직후 Play)**를 문으로 걸고 "동선이 안 보이면 W3 착수 금지"를 못박았다.
- 🔑 **탐색 폭발이 하루에 네 번 나왔고 네 번 다 Play 가 먼저 잡았다** (피로·Plant·GatherStone·
  요리). 개별 수정으로는 다섯 번째가 온다고 판단해 **ADR-M0-12**로 규칙화했다 —
  *"goal 목표는 다 채우기가 아니라 한 걸음"*. 게이트 `M12-T5`(수치형 절대목표 화이트리스트)로
  강제하되, **그 감사가 "검토했는가"만 강제하고 폭발을 탐지하지 못한다**는 한계를 게이트
  주석과 ADR 양쪽에 적었다.
- 🔔 **ADR-M0-5(플래너 잡 동결)의 자폭 트리거가 발동했다.** 본문에 *"세 번째가 오면 재검토"*가
  박혀 있었는데 하루에 네 번, 누적 6건이 왔다. **재검토 결과 = 동결 유지** — 네 건 다 잡
  바깥에서 닫혔고 공통 원인이 휴리스틱이 아니라 목표 서술이었다. 대신 다음 재검토 조건을
  더 엄격하게 못박았다: **ADR-M0-12 를 지킨 goal 에서 폭발이 나오면 그때는 잡을 연다.**
- 🔴 **내 전수 스윕이 거짓 안심을 줬다.** goal × 성격 × 직업 1,176 계획을 돌리고 "폭발 0건"이라
  보고했는데 몇 시간 뒤 Play 가 네 번째를 찾았다. goal 축은 전수했지만 **상태 축(야생 채집
  가능 여부)을 고정**했고 그게 결정적 축이었다. → 방법론 **M20** 신설:
  *"전수 검사는 무엇을 고정했는지를 함께 적는다 — 고정한 축이 곧 사각지대."*
  결론에 조건을 붙인다: "폭발 0건"이 아니라 **"이 상태에서 폭발 0건"**.
- **명세 전 실사가 설계를 두 번 바꿨다** (spec-write 0단계를 생략했으면 둘 다 못 잡았다):
  ①"겨울"을 `IsCrisis`로 판정하려 했는데 **`Season_Summer`도 `IsCrisis: 1`** — 그대로 갔으면
  여름 늑대가 사나워졌다. 겨울만 참인 것은 `ForageFrozen`이고 의미도 정확히 겹친다(야생
  먹이가 얼면 늑대도 굶는다). ②계절→위협 통로가 **아예 없었다**. 다만 `ZoneService` 참조가
  어제부터 죽어 있어 그 슬롯을 교체하면 된다.
- **브레인스토밍은 방법론 M19 3게이트로 돌렸고 되비춤이 값을 했다.** 사용자 자유 서술을
  [결정/근거/제약/비목표]로 재구성하자 **"평소엔 어슬렁, 겨울에 공격"은 상주형 전제의 서술**
  이라는 충돌이 드러났다 — 출몰형이면 늑대에게 "평소"라는 시간이 없다. 그 질문이 범위를
  갈랐고(상주형 = 맵 대형화 밀스톤 이월) 성공 기준도 정밀해졌다(그림 아님, 동선 + 주민 반응).

**다음에 할 일:**

- 🎯 **W2R 구현** (`spec-implement`) — 명세 = `Docs/M21_격퇴축_실행명세서.md` §W2R.
  ThreatSO 3필드 append · ThreatAgent 정지→배회 · ThreatService 계절 주입(ZoneService 슬롯
  교체) + 순수 판정 3종 + **밭형 대상 0 퇴장(W2 결함)** · 에셋 3종 Edit · 게이트 `M21_T7`.
- 🔴 **리뷰⓪ = W2R 직후 Play** (문): ①늑대가 정지하지 않는가 ②평시 주민 타깃 출몰이 오는가
  ③초반 사망률이 과하지 않은가(Tier1 0.25 제안치). **동선이 안 보이면 W3 착수 금지.**
- **누적 Play 확인 목록**: 피로 주민이 쉬는가 · Plant 한 칸씩 리듬 · GatherStone 30 언저리
  정지 · 겨울에 곳간으로 요리가 되는가 · W2 실측 ③(Tier2 무리에서 체류 중 재추격).
- **이월 (M21 후 한 묶음)**: 🟠 절벽근접 4건(TendInjured·TreatInjured 87% · BuildCampfire·
  Irrigation 71% · GatherWood 60%) + BuildHouse·ExpandFarm + D3(부탁 목표 굳음) +
  M0-T2c 전 goal 일반화 + "에셋 값 변경이 타 시스템 분기를 죽였는가" 검사.
- (+02:04) feat(blog): 검수 8번 문체 반복 검사 신설 — 상한은 실측 5편의 관측 최대치
  외부 안티슬롭 스킬을 조사하다 우리 파이프라인에 없는 축 하나를 찾았다. 검수팀이
  보던 7항목(사실·정책·분량·민감정보·구조·SEO·인트로중복) 중 어디에도 "문장 자체가
  AI 티가 나는가"가 없었다.
  
  다만 남의 규칙은 그대로 못 쓴다 — 잡는 게 serves as / Whether you're X or Y 같은
  영어 관용구다. 그래서 우리 산출물 5편(초안 2 + 발행 HTML 3)을 직접 쟀다.
  
    전형적 슬롭 마커(단순히·핵심은·즉,·무엇보다)는 거의 0.
    튀는 것은 둘뿐 — em dash 와 대조 구문("~가 아니라").
    초안 실측: em 1.22~2.55 / 대조 0.59~1.11 / 상투구 0.11~0.20 (1,000자당)
  
  상한을 관측 최대치 + 여유(3.5 / 2.0 / 0.6)로 잡았다. 그러므로 이건 좋은 글의
  기준이 아니라 회귀 방지선이고, 문서에 그렇게 적었다. 어느 밀도가 독자에게 실제로
  나쁜지는 측정된 바 없다(독자 데이터 0). 게다가 이 둘은 승인된 브랜드 톤이 기대는
  리듬이라, 근거 없이 조이면 목소리가 깎인다.
  
  기각한 규칙도 함께 박제했다. 안티슬롭 스킬들이 공통으로 넣는 국소 규칙(한 문장에
  대시 2개 / 한 문단에 대조 구문 2회)을 넣기 전에 재봤더니 오탐 3건·적중 0건이었다.
  오탐은 목록의 `— 설명` 형태가 문장 분할에 걸린 것이고, 유일한 진짜 히트는 정당한
  삽입구였다. 우리 초안의 특징은 뭉침이 아니라 전체 밀도다.
  
  - blog-reviewer 8번: 계측 명령(LC_ALL=C.UTF-8 + awk, 초안에 실행 확인) + 상한표
    + 상한의 출처 + 기각한 국소 규칙의 실측
  - blog-reviewer 출력 형식: style_check 줄 — 밴드 안이어도 숫자를 남겨 관측을 쌓는다
  - blog-writer: 같은 표 + "지금 쓰던 대로 쓰면 통과한다, 일부러 줄이지 마라".
    검수팀만 아는 기준이면 반려 핑퐁이 난다
  - .claude/agents/blog-reviewer.md
  - .claude/agents/blog-writer.md

## [16:15] docs(blog): 사전 점검 2026-08-07 — M14 발행 확인(#18), 표 갱신, M15 ACTIVE 재확인

**태그:** #misc

**무엇을 했나:**
- docs(blog): 사전 점검 2026-08-07 — M14 발행 확인(#18), 표 갱신, M15 ACTIVE 재확인

**변경 파일:**
  - tools/blog-automation/BLOG_COVERAGE.md

- (+16:25) fix(video): 「쇼트 훅」 폐기 + 상한 25→28초 — 예고가 뜻 없이 나갔다
  사용자가 완성본을 보고 잡았다. ep08s-2 의 예고가 「다음 편, 다 같이.」로 나갔고
  판정은 "이게 무슨 말이야, 시청자는 읭?" 이었다. **게이트는 통과했다** — 1.7초로 상한
  안이었으니까. 길이만 봤고 뜻은 아무도 안 봤다.
  
  🔴 근본 원인은 **내가 지시를 잘못 옮긴 것**이다. 원안은 "찰나의 순간에 가장 자극적인
  **장면 하나만** 탁 보여주고" = **시각 컷**이었는데, 나는 그걸 **음성 한 줄**로 구현하고
  「1.8초는 말로 6~7자」라고 안내했다. 6~7자로 뜻이 성립할 리 없다. 게다가 이 엔진에서
  시각 컷 예고는 놓을 자리가 없다(마지막 2.6초는 아웃트로 카드, 그 앞은 본문) —
  **원안은 구조적으로 불가능했고 내 대체안은 말이 안 됐다.**
  
  함께 드러난 것: 음성이 「다 같이」인데 같은 순간 카드는 「너무 AI스럽다」였다.
  **다음 편을 부르는 이름이 둘이고 서로 달랐다.**
  
  개정 (ADR-V25-6·7):
  - 예고 = 「뜻이 통하는 한 문장」, 상한 **3.0초**(2026-08-03 사용자 확정값 복귀).
    판정 문장 = "그 한 줄만 읽고 다음 편이 뭔지 알 수 있는가" — **기계는 길이만 보고
    뜻은 검수팀이 본다.** 글자 수 하한은 발명하지 않는다(ADR-V-7).
  - 음성 예고와 outro.next 가 **같은 것을 가리켜야** 한다. 어긋나면 반려.
  - 총 길이 상한 25 → **28초**(사용자 "25초 ±3초"). 🔴 **상한은 목적이 아니라 한계다** —
    3초를 연 것은 인트로 3초와 아웃트로를 지키라고 연 것이고, **길이를 맞추려고 첫 3초나
    예고를 깎은 회차는 반려**한다. 깎을 것은 가운데다.
  - scene-writer 에 "인트로 3초와 아웃트로가 두 급소"를 명시(사용자 강조).
  
  🔑 예고 상한 이력 4.0 → 1.0(불가능) → 1.8(뜻 없음) → 3.0. **세 번 다 내가 정한 값이고
  세 번 다 실물을 보기 전에는 안 보였다.** 게이트가 통과시킨 것이 규격을 지켰다는 뜻이
  아니라는 사례가 오늘만 두 번째다(앞엣것은 SHOT_TAIL).
  
  ep08s-2 는 그대로 둔다(사용자 판정) — 규칙만 고치고 ep09s 부터 적용.
  기존 3편은 새 임계값에서도 전부 통과 확인.
  - .claude/agents/scene-planner.md
  - .claude/agents/scene-reviewer.md
  - .claude/agents/scene-writer.md
  - Docs/영상_25초_전환_실행명세서.md
  - tools/scene-video/check.mjs
  - tools/scene-video/routine-prompt.md

## [18:40] feat(video): ep09s 2편 — 새 규격(28초·뜻이 통하는 예고) 첫 적용

**태그:** #misc

**무엇을 했나:**
- feat(video): ep09s 2편 — 새 규격(28초·뜻이 통하는 예고) 첫 적용

**왜 이렇게 했나 (커밋 본문):**
M7 상호대화·가시성을 사건 둘로 갈랐다. 실제 mp4 26.167 / 26.200초 — 옛 25초 상한은
넘지만 새 28초 안이다. **±3초를 연 덕에 예고와 본문을 둘 다 지켰다**(예전 규격이었으면
어느 한쪽을 깎아야 했다).

9-1 「제가 만든 주민인데 누가 누군지 몰랐어요」 / 9-2 「주민이 말은 거는데 대화로 안
보여서 4타일을 2타일로 줄였어요」. 예고는 둘 다 뜻이 통하는 한 문장이고 outro.next 와
같은 것을 가리킨다 — ep08s-2 의 「다음 편, 다 같이.」 실패를 정확히 피했다.

🔑 9-1 은 이미 업로드된 ep08s-3 이 「9편 · 주민들이 서로에게 말을 걸다」로 예고해 둔 것을
첫 두 줄로 이어받았다(맡은 사건은 이름표라 어긋난다). 요약이 아니라 **발단**으로 써서
앞 편을 본 사람에겐 인과가 되고 안 본 사람에겐 안 걸린다 — 자막 8줄에 시스템명·밀스톤
번호·앞편 요약 0건.

반려 사유가 전부 **게이트가 구조적으로 못 보는 자리**에서 나왔다:
- 9-1: 화면이 `M5 JOB` 행에 「농사꾼」·「떠돌이」를 넣었는데 **둘 다 성격 이름**이었다.
  하필 이 편이 다루는 밀스톤의 ADR-M7-5 가 「성격 이름이 직업처럼 들리는 혼동 방지」다.
  → 축은 남기고 값을 가리는 것으로 고쳤다(원문에 직업 이름 0건 · 축 진술은 실재).
  이름표 겹침 54% 는 확률 조정이 아니라 **범위 상수**로 막았다(주기 전체 10,800프레임 0건).
- 9-2: 「말이 튀어나왔어요」의 그림이 그 문장이 **끝난 뒤에** 왔다(발화 61프레임 중 0).
  ep08s-3 weightknob 에 이은 **두 번째 위상 사고**. PHASE 3.24 + 램프 조임으로 51/61.

🔴 척도 혼동이 이 회차에서 네 번 났다 — cue 창이 쓰는 값은 `rel.dur` = 말하는 시간 +
pauseAfter 다(engine.js:163). wav 길이가 아니다. 나 한 번 · 검수 한 번 · 작성팀 한 번 ·
sfx why 한 번이고, 매번 다음 층이 잡았다. 각 kind 주석과 노트에 박제했다.

🔴 z 순서 겹침이 세 번 만에 정밀해졌다: 정지 좌표만 봄 → 궤적 양 끝(원만) → 모든 획 +
테두리 반폭. 그런데도 마스터가 **네 번째**를 잡았다 — faceoff 의 증명이 「방향선은 연결선
위에 놓인다」를 전제했는데 그건 fk=1 에서만 참이고, 회전 중에는 방향선이 법선 쪽을 향한다.
전 구간 최소 −1.12px(5프레임). 비차단 승인이나 **복사 전 닫을 것**을 코드 주석에 남겼다.
🔑 내가 스틸로 「떨어졌다」고 확인한 13,921ms 는 겹침 시작 79ms 전이었다.

작성팀 자기검산의 거짓 수치도 마스터가 잡았다(3.5배 → 2.46 · 다섯 번 → 4회 · 「어느
0.6초 창을 잘라도」 → 최장 무접촉 1.59초). 결론만 맞았다.

검수 리포트를 `.staging/` 이 아니라 회차 폴더에 남기기 시작했다 — ep08s-2·-3 기록이
덮어써져 사후 대조가 불가능했던 문제(마스터 지적) 대응.

**변경 파일:**
  - .claude/agent-memory/scene-planner/MEMORY.md
  - .claude/agent-memory/scene-planner/feedback_prev_outro_promise.md
  - .claude/agent-memory/scene-planner/project_ep08s_brief.md
  - .claude/agent-memory/scene-planner/project_ep09s_brief.md
  - .claude/agent-memory/scene-planner/project_split_ledger.md
  - .claude/agent-memory/scene-writer/MEMORY.md
  - .claude/agent-memory/scene-writer/feedback_static_segment_is_area.md
  - .claude/agent-memory/scene-writer/feedback_string_fact_check.md
  - .claude/agent-memory/scene-writer/feedback_translucent_accent_zorder.md
  - .claude/agent-memory/scene-writer/project_sfx_latch_seed.md
  - .claude/agent-memory/scene-writer/project_sfx_timing.md
  - .claude/agent-memory/scene-writer/project_split_episode_conventions.md
  - .claude/agent-memory/scene-writer/project_title_shape_reuse.md
  - .claude/agent-memory/scene-writer/project_video_25s_budget.md
  - devlog/sessions/2026-08-07.md
  - tools/scene-video/.claude/agent-memory/scene-master/MEMORY.md
  - tools/scene-video/.claude/agent-memory/scene-master/project_ep09s_split.md
  - tools/scene-video/.claude/agent-memory/scene-master/project_factcheck_gotchas.md
  - tools/scene-video/.claude/agent-memory/scene-master/project_gate_metric_gaps.md
  - tools/scene-video/.claude/agent-memory/scene-master/project_timing_formulas.md
  - tools/scene-video/.claude/agent-memory/scene-master/project_zorder_and_phase.md
  - tools/scene-video/.claude/agent-memory/scene-reviewer/MEMORY.md
  - tools/scene-video/.claude/agent-memory/scene-reviewer/feedback_cue_timing_audit.md
  - tools/scene-video/.claude/agent-memory/scene-reviewer/feedback_t_driven_value_sweep.md
  - tools/scene-video/.claude/agent-memory/scene-reviewer/project_recurring_defects.md
  - tools/scene-video/episodes/ep09s-1/kinds/infoline.js
  - tools/scene-video/episodes/ep09s-1/kinds/samedots.js
  - tools/scene-video/episodes/ep09s-1/kinds/tagup.js
  - tools/scene-video/episodes/ep09s-1/kinds/undersurface.js
  - tools/scene-video/episodes/ep09s-1/notes/review.md
  - tools/scene-video/episodes/ep09s-1/notes/writer.md
  - tools/scene-video/episodes/ep09s-1/scene.json
  - tools/scene-video/episodes/ep09s-2/kinds/brushpast.js
  - tools/scene-video/episodes/ep09s-2/kinds/faceoff.js
  - tools/scene-video/episodes/ep09s-2/kinds/onepair.js
  - tools/scene-video/episodes/ep09s-2/kinds/wideband.js
  - tools/scene-video/episodes/ep09s-2/notes/review.md
  - tools/scene-video/episodes/ep09s-2/notes/verdict.md
  - tools/scene-video/episodes/ep09s-2/notes/writer.md
  - tools/scene-video/episodes/ep09s-2/scene.json
  - tools/scene-video/episodes/ep09s/notes/planner.md
  - tools/scene-video/state/schedule.json
  - tools/scene-video/state/uploads.json


## [20:48] chore(mcp): MCP for Unity 10.1.2 도입 — Play 관측을 게이트 밖에서 되찾는다

**태그:** #misc

**무엇을 했나:**
- chore(mcp): MCP for Unity 10.1.2 도입 — Play 관측을 게이트 밖에서 되찾는다

**왜 이렇게 했나 (커밋 본문):**
게이트 321개가 green 인 채로 놓친 결함이 반복됐다 (늑대 출몰 영구 생략, W2 체류
설계 뒤집힘, 밭 스프라이트 덮음). 전부 스크린샷 한 장이 잡았고, 그 스크린샷을
지금까지 사람이 찍어 왔다 — 그래서 "누적 Play 확인 목록"이 밀렸다. 이 패키지는
Play 제어·콘솔 읽기·테스트 실행·Game View 캡처를 도구로 열어 그 루프를 닫는다.

CoplayDev/unity-mcp 를 고른 이유: 완전 로컬(127.0.0.1)이라 에디터 상태가 제3자
클라우드로 나가지 않는다. 대안인 IvanMurzak 는 권장 경로가 ai-game.dev 계정 +
OAuth 터널이라, 리포가 공개인 이 프로젝트에는 기본값으로 부적절하다.

- git URL 대신 OpenUPM 레지스트리: git 클론 뒤 rename 이 Windows 에서 EPERM 으로
  죽었다. 레지스트리는 tarball 이라 그 실패 경로가 아예 없다.
- 전제: uv 0.12.1 (winget 서명 패키지), Python 3.14.5 (이미 있었으나 PATH 밖 +
  Store 별칭에 가려져 있었다 — PATH 앞에 추가).
- 설정: Auto-Start Server on Editor Load 켬. LAN 바인드·Insecure Remote HTTP 는 끔.
- .gitignore: 스크린샷 기본 경로가 Assets/Screenshots 라 두면 리포가 오염된다.

검증: EditMode 게이트 321/321 (2회), 콘솔 에러 0. 패키지 asmdef 은 Editor/Runtime
2개뿐이라 테스트 어셈블리를 오염시키지 않는다.

**변경 파일:**
  - .gitignore
  - Packages/manifest.json
  - Packages/packages-lock.json
  - ProjectSettings/PackageManagerSettings.asset

- (+20:48) feat(gates): EditMode 게이트 러너 신설 — exit code 말고 결과 XML 로 판정한다
  리포에 러너가 없어 게이트 실행법이 memory 에만 있었고, 거기 적힌 두 함정을 매번
  사람이 기억해야 했다. 스크립트로 박는다.
  
  ① -quit 함정: 붙이면 Unity 가 테스트를 돌리기 전에 종료하는데 exit code 는 0 이라
     "게이트 green" 으로 읽힌다. 그래서 -quit 을 쓰지 않고, 성공/실패를 결과 XML 의
     집계로만 판정한다 (XML 이 없으면 실패로 본다).
  ② Editor 락: MCP for Unity 를 쓰는 동안 Editor 는 켜져 있는 게 정상인데, 그러면
     batchmode 가 프로젝트를 못 열어 exit 1 이다. 락을 먼저 감지해 무슨 일이 일어날지
     알려주고 (exit 2), -CloseEditor [-ReopenEditor] 를 줬을 때만 닫는다.
  
  작성 중 밟은 것 3건 — 전부 읽어서가 아니라 돌려서 나왔다:
  - BOM 없는 UTF-8 .ps1 을 PowerShell 5.1 이 cp949 로 읽어 한글이 깨지고 따옴표가
    터졌다 → UTF-8 BOM 으로 저장. (?? / ?: / ?. 연산자도 5.1 엔 없어 피했다)
  - 창 제목으로 종료를 판정했더니, 창은 사라졌는데 프로세스가 락을 쥔 채였고 그 틈에
    batchmode 가 죽었다 → Temp/UnityLockfile 을 배타로 열어보는 방식으로 교체
  - 낡은 PATH 를 물려주며 Unity 를 띄워 uvx 를 못 찾았고 MCP Auto-Start 가 조용히
    죽었다. 게이트는 멀쩡히 통과해서 한참 뒤에야 보였다 → 레지스트리에서 PATH 재독
  
  검증: 321/321, exit 0. 닫기 → 락 해제 대기 → 실행 → 재실행 전 경로를 실측했다.
  - tools/unity-gates/run-gates.ps1

## [21:36] feat(hud): 개입 인프라 4건 — 경보 기준을 몸 상태로, 일시정지·자동 실시간·클릭 반경

**태그:** #debug-viz

**무엇을 했나:**
- feat(hud): 개입 인프라 4건 — 경보 기준을 몸 상태로, 일시정지·자동 실시간·클릭 반경

**왜 이렇게 했나 (커밋 본문):**
어제 Play 관측: Day 0 화면이 빨간 줄 8개로 시작하고, 개입하려 해도 판을 멈출 수 없고,
방랑자 창은 8배속에서 스쳐 지나가고, 주민은 눌러도 안 잡혔다. 넷 다 "개입할 수 있는가"의
문제라 한 묶음으로 처리한다.

① 상태 경보 기준: 저장 식량 → 실제 굶주림 (VillagerAgent.JudgeHunger)
   舊 기준(EstimateMyFoodDays <= FOOD_ALERT_DAYS)은 비축이 0인 시작 시점에 전원 참이었다.
   항상 켜져 있는 경보는 개입 신호가 아니라 배경이고, 배경은 아무도 안 본다.
   새 기준은 포만만 본다. 문턱은 기존 에셋 값 재사용(ADR-M0-2, 새 수치 발명 없음):
     배고픔  = OrderRefuseSatiety(35)   — 명령이 거부되기 시작하는 지점 = 촌장이 알아야 할 첫 지점
     굶주림  = StarvingBelowSatiety(10) — 체력이 깎이는 지점 (NextStarvingDays와 같은 문턱)
   두 등급을 색으로 갈랐다(주황/빨강) — "절벽이 아니라 계단"의 표현.
   저장 식량 일수는 판정에서 빠지고 참고 수치로만 줄에 남는다: 포만이 낮은데 식량도 0이면
   명령이 필요하고, 식량이 있으면 곧 알아서 먹는다 — 개입 판단에 여전히 쓸모가 있다.
   ⚠️ 겨울 경보(_unpreparedBuf)는 저장 식량 기준을 유지한다. 겨울 대비는 몸 상태가 아니라
   비축의 문제다 — 기준이 갈리는 것이 옳고, 판정 이원화가 아니다.

② 일시정지 0키 (PlayerInputController.SetSpeed = 배속 쓰기 단일 지점)
   최저 배속이 1배라 "멈추고 무엇을 할지 정하는" 시간이 없었다. 재입력 시 정지 직전
   배속으로 복귀 — 8배속으로 관찰하다 멈춘 사람을 1배속에 떨구지 않는다.
   1~4 키의 1=1배 손버릇을 밀어내지 않으려고 _speedSteps 배열이 아니라 별도 키로 뒀다.

③ 방랑자 프롬프트 = 실시간 보장
   8배속에서는 수락 대기(WandererWaitDays)가 실시간 몇 초로 압축돼, 판단하기 전에 방랑자가
   떠났다. 결정을 요구하는 화면은 결정할 시간과 함께 떠야 한다. 열리는 순간 에지 1회만
   개입하므로, 프롬프트를 띄워 둔 채 다시 배속을 올리는 선택은 플레이어에게 남는다.

④ 주민 클릭 반경 0.8 → 1.5 타일 (코드 기본값 + 씬 직렬화값 둘 다)
   0.8은 스프라이트보다 좁아 "분명히 눌렀는데 안 잡히는" 클릭이 잦았다. 노드 반경과 통일.

부수 변경 — CameraController가 unscaledDeltaTime을 쓴다 (재사용 라이브러리 최소 절제):
카메라는 시뮬레이션이 아니라 플레이어의 눈이다. Time.deltaTime이면 timeScale 0에서 화면이
얼어붙어 "멈추고 살펴본다"는 일시정지의 목적 자체가 성립하지 않는다. 부작용으로 8배속에서
카메라가 8배로 날아가던 것도 사라진다.

게이트: EditMode 326/326 (321 → 326). 신설 M13-T6 3종 — 문턱이 에셋에서 오는가 /
경보 문턱과 명령 거부 문턱이 일치하는가 / 비축 0인 Day 0에 경보가 뜨지 않는가(회귀 방지).
기존 M13-T2 는 새 튜플·2등급 문구로 개정 + 식량 99(중립) 생략 게이트 추가.

Play 실측 (MCP, Day 0): 주민 8명 전원 식량일수 0 · 포만 54.9~81.2 · 경보등급 None,
상태줄 원문 빈 문자열(0줄). 반례 주입으로 포만 5 → 빨간 "굶주리는 주민" 줄,
포만 28 → 주황 "배고픈 주민" 줄, 클릭 매핑(FindStarvingVillagerAt) 정상.
8배속 중 프롬프트 개방 → 다음 프레임 timeScale 1. timeScale 0 에서 Day 정지·
unscaledDeltaTime 생존. Console 에러 0.

**변경 파일:**
  - Assets/Scenes/M0Scene.unity
  - Assets/Scripts/M0/Agent/VillagerAgent.cs
  - Assets/Scripts/M0/SimulationLoop.cs
  - Assets/Scripts/M0/UI/PlayerInputController.cs
  - Assets/Scripts/M0/UI/SeasonHud.cs
  - Assets/Scripts/UI/CameraController.cs
  - Assets/Tests/EditMode/M13_StatusGates.cs

**막혔던 문제 / 해결 방법:**

- 🔴 **코드 기본값만 고쳤으면 ④가 안 먹었다.** `_villagerPickRadius`는 `[SerializeField]`라
  씬 YAML(`M0Scene.unity:1079`)에 `0.8`이 직렬화돼 있었고, 직렬화된 값이 코드 초기화값을
  덮는다. `_speedSteps`는 반대로 YAML에 **없어서** 코드 기본값이 그대로 살아 있었다 —
  같은 파일의 두 필드가 정반대였다. 🔑 **`[SerializeField] private T x = 기본값;`을 고칠 때는
  씬/프리팹에 그 키가 있는지부터 grep한다.** 있으면 코드만 고친 것은 아무 일도 안 한 것이다.
- **①의 진짜 결함은 문턱이 아니라 "무엇을 재는가"였다.** 처음엔 `FOOD_ALERT_DAYS`를 2에서
  낮추는 걸 생각했는데, 그건 Day 0 8줄을 며칠 미룰 뿐이다. 저장 식량은 **시작 시점에 전원
  0**이라 어떤 문턱을 잡아도 초반이 전부 참이 된다. 재는 대상을 몸 상태(포만)로 옮겨야
  풀리는 문제였다 — 문턱 조정으로 가려던 걸 멈춘 게 이 항목의 분기점이었다.
- **새 문턱을 발명하지 않았다.** 포만 기준을 새로 정하면 "왜 30인가"를 답할 수 없고 ADR-M0-2
  위반이다. 기존 `OrderRefuseSatiety`(35)·`StarvingBelowSatiety`(10)를 그대로 썼더니
  덤으로 **경보 = 명령 거부 = 체력 감소가 같은 눈금**이 됐다(게이트 `M13_T6`가 고정).
- **경보를 영구 침묵시킨 게 아님을 증명해야 했다.** Day 0이 0줄인 것만 보면 "필터가 항상
  false"여도 똑같이 보인다. MCP로 Play 중 포만을 5·30으로 반사 주입해 빨강/주황 두 줄이
  실제로 뜨는 것과 클릭 매핑까지 확인했다 — 방법론 M20(전수 검사는 무엇을 고정했는지 함께
  적는다)의 반례 확인 판.
- **일시정지는 카메라를 같이 안 고치면 반쪽이다.** `timeScale = 0`이면 `Time.deltaTime`이 0이라
  화면이 얼어붙는다. "멈추고 살펴본다"가 목적인데 살펴볼 수가 없다 → `CameraController`를
  `unscaledDeltaTime`으로(재사용 라이브러리라 2줄만). 부작용으로 8배속에서 카메라가 8배로
  날아가던 것도 사라졌다 — 손맛이 바뀌는 변경이라 보고에 명시했다.
- **PowerShell 5.1 here-string으로 git 커밋 메시지를 못 넘긴다.** `git commit -m @'...'@`가
  한글 본문에서 토막나 `pathspec '수' did not match` 류로 죽었다. memory에 있던 교훈인데
  다시 밟았다 → 메시지를 파일로 쓰고 `git commit -F`. **다음부터 -F 를 기본으로 쓴다.**

**다음에 할 일:**

- 🎯 **M21 §W2R 구현** (사용자 선택) — 이 항목의 다음 행선지.
- 🟡 이번 변경이 남긴 관찰 3건 (M21 뒤 이월 묶음에 합류):
  ① `FOOD_ALERT_DAYS`가 이름과 달리 경보에 안 쓰이는 상수가 됐다 — 이제 `ComposeReason`
     (선택 주민 정보줄)의 식량 빨간색 전용. 이름 정리 또는 기준 통일 판단 필요.
  ② 카메라 `unscaledDeltaTime` 전환의 체감(8배속에서 느려진 것)이 옳은지 Play 확정.
  ③ 겨울 경보(`_unpreparedBuf`)는 저장 식량 기준을 유지했으므로, 첫 겨울 직전 5일 창에서
     전원이 "준비 부족"으로 뜰 가능성 — 의도인지 과한지는 그때 관측.
- 🔑 **키 입력 자체는 미검증**이었다(0키·클릭 반경). 로직 경로만 MCP로 확인했고
  사용자 Play에서 확인 완료 — 4건 모두 정상.

- (+22:02) feat(goap): M21 W2R — 배회형 위협 + 계절 연동 (체류의 '정지'를 폐기)
  W2 의 체류는 게이트 320개를 통과했지만 화면에 나온 것은 "원 하나가 밭 위에 서서
  콘솔에만 로그를 뿜는 것"이었다. 체류가 틀린 게 아니라 **정지**가 틀렸다 — 머무르되
  움직인다. 명세 = Docs/M21_격퇴축_실행명세서.md §W2R (bb1ee00).
  
  ■ 배회 (ThreatAgent)
    Arrive() 가 _path=null 로 굳히던 것을 폐기하고, 도착 지점을 앵커로 WanderRadiusTiles
    안을 돈다. 목적지 재선정은 **도착했을 때만** + 0.5초 쿨다운 — 매 프레임이면 제자리
    떨림 + JPS 호출 폭증이다(ADR_경로탐색_확장경계). 목적지 선정은 통행 배열을 아는
    ThreatService 가 하고 개체는 걷기만 한다(M10-C ⚠️③ 유지).
    우선순위는 추격 > 배회 — 주민형은 사거리 밖이면 추격이 경로를 덮는다.
  
  ■ 계절 연동 (ThreatService, ADR-M21-9)
    ThreatService 생성자의 ZoneService 슬롯을 SeasonService 로 교체했다. 구역 참조는
    b51c631(FarmPlot.ZoneRadius=0) 이후 죽은 코드였다 — 빼는 것이 실상을 코드에 맞추는 것.
    배회 목적지 필터용 isWalkable 도 함께 주입. 파급은 SimulationLoop 1곳.
    🔴 "겨울" 판정은 ForageFrozen 하나다. IsCrisis 를 쓰면 Season_Summer 도 IsCrisis:1 이라
    여름 늑대가 사나워진다. 순수 판정 3종 신설: IsPredatorHungry / EffectiveVillagerChance /
    EffectiveStayDays. 계절이 바꾸는 것은 타깃 확률과 체류 상한 둘뿐 — 출몰 빈도·피해량은
    게임일 래칫(ADR-M10R-1)이 그대로 소유한다. 시드는 그대로라 결정성 불변(ADR-M10R-2).
  
  ■ 밭형 "칠 대상 소멸" 퇴장 (W2 결함 Y)
    주민형은 TickChase 가 이미 처리했지만 밭형엔 그 경로가 없어, 밭이 다 사라져도 상한까지
    `밭 0/0 소실` 을 주기마다 찍으며 남아 있었다.
  
  ■ 에셋 (Edit 만 — ADR-M21-6, M19 교훈)
    3종 공통: MaxStayDays 2 → 0.75 (재타격 0.25일과 짝 = 최대 3회, 겨울엔 ×2 로 1.5일/6회)
              + WanderRadiusTiles 5 · HungrySeason{Chance,Stay}Mult 2
    Tier1 늑대: VillagerTargetChance 0 → 0.25 (M10 "밭 전용" 결정의 개정, §4-R)
  
  🔴 명세에 없던 것 하나를 채웠다 — Tier1 늑대의 StrikeLinesVillager 가 [] 였다.
    밭 전용이던 시절의 흔적인데, 확률을 0.25 로 올리는 순간 "주민을 무는데 아무도 말이
    없는" 상태가 된다. W2R 합의 한 줄이 요구하는 것은 동선 **과 주민의 반응** 둘 다라
    귀결로 보고 3줄을 썼다(외로운 늑대의 목소리 — 무리·곰과 겹치지 않게).
    게이트로도 막았다. 이건 "에셋 값 변경이 타 시스템 분기를 죽였는가"(이월 항목)의 거울상
    — 값 변경이 **내용 없는 분기를 켠** 경우다.
  
  ■ 게이트 M21_T7 6종 (EditMode 326 → 332, 전부 green)
    ForageFrozen 경계 / 배포 계절 에셋 대조 / 배율 클램프·약화무시 / 계절 롤 결정성 /
    Tier1 평시 주민 타깃 + 대사 존재 / 배포 위협의 배회 값.
    red-check 실측: IsCrisis 로 바꾸니 2종 red, 늑대 확률 0·배회 0 으로 되돌리니 2종 red.
    🔑 `배고픈 계절 2개` red 는 **배포 에셋에 여름이 IsCrisis:1 로 실재한다**는 확인이다.
  
  🔴 Play 가 또 내 버그를 잡았다 (게이트는 green 이었다)
    "칠 대상이 없다" 퇴장을 밭형·주민형 **양쪽에** 걸었더니 주민 타깃 늑대가 상한 전에
    전부 퇴장했다(관측: 상한 7.909 인데 7.717 퇴장). 주민은 **도망치므로** "사거리 밖"이
    "대상 없음"과 같지 않다 — 그래서 추격이 있는 것이다. 밭형에만 남기고(시설은 도망가지
    않는다), 주민형은 사거리 밖일 때 타격만 거르고 추격을 잇게 고쳤다. 시각을 기록하지
    않으므로 주민이 다시 들어오면 곧바로 문다.
  
  Play 실측 (MCP): 체류 4마리 위치를 두 시점에 샘플해 전원 이동 확인 · 앵커 기준 최대
  체비셰프 5(반경 그대로, 초과 0) · 통행불가 타일 위 0 · 밭형 2건이 타격 0회로 즉시 퇴장 ·
  주민형은 체류하며 2회 타격 후 상한(6.053)에서 퇴장 · 여름(IsCrisis=1)에 상한 0.75 유지 ·
  콘솔 에러 0 · [ThreatSO] OnValidate 경고 0.
  
  DoD 6/6 — 명세서 §W2R 체크리스트에 근거와 함께 표시.
  🔴 다음 = 리뷰⓪ (사용자 Play). 동선이 안 보이면 W3 착수 금지.
  - Assets/M0Config/Threats/Threat_Tier1_Wolf.asset
  - Assets/M0Config/Threats/Threat_Tier2_Pack.asset
  - Assets/M0Config/Threats/Threat_Tier3_Bear.asset
  - Assets/Scripts/M0/Data/ThreatSO.cs
  - Assets/Scripts/M0/Presentation/ThreatAgent.cs
  - Assets/Scripts/M0/SimulationLoop.cs
  - Assets/Scripts/M0/World/ThreatService.cs
  - Assets/Tests/EditMode/M21_CombatGates.cs
  - Docs/M21_격퇴축_실행명세서.md

**막혔던 문제 / 해결 방법:**

- 🔴 **Play 가 또 내 버그를 잡았다 — 게이트 332개가 green 인 채로.** W2R 의 "칠 대상이 없다"
  퇴장을 밭형·주민형 **양쪽에** 걸었더니, 관측에서 주민 타깃 늑대가 상한 7.909 인데 7.717 에
  퇴장했다. 원인은 한 줄로 말할 수 있다: **주민은 도망친다.** 시설과 달리 "사거리 밖에 있다"가
  "대상이 없다"와 같지 않고, 그래서 애초에 추격이 있는 것이다. 밭형에만 남기고(시설은 도망가지
  않으므로 사거리 = 존재 여부가 성립), 주민형은 사거리 밖일 때 **타격만 거르고 추격을 잇도록**
  고쳤다. 🔑 W2 가 게이트 green 인 채 뒤집힌 것과 **완전히 같은 종류**이고, 이번엔 커밋 전에
  잡혔다 — 차이는 리뷰⓪를 문으로 걸어 둔 것이 아니라 **구현 중에 Play 를 돌린 것**이다.
- 🔴 **명세가 안 짚은 구멍 하나 — 값 변경이 "내용 없는 분기"를 켰다.** Tier1 늑대의
  `StrikeLinesVillager` 가 `[]` 였다. 밭 전용(`VillagerTargetChance: 0`)이던 시절엔 옳았지만,
  §4-R 대로 0.25 로 올리는 순간 **주민을 무는데 아무도 말이 없는** 상태가 된다. 대사 3줄을
  쓰고 게이트로 막았다. 🔑 이월 목록의 *"에셋 값 변경이 타 시스템 분기를 죽였는가"* 검사와
  **거울상**이다 — 죽인 게 아니라 **켠** 경우. 검사 항목을 양방향으로 적어야 한다.
- 🔑 **red-check 가 배포 에셋의 사실을 증명했다.** `IsPredatorHungry` 를 `IsCrisis` 로 바꿔
  돌렸더니 `배고픈 계절은 정확히 하나여야 한다 / Expected 1 But was 2` 가 나왔다. 이건 게이트가
  동작한다는 확인이자 **`Season_Summer` 가 실제로 `IsCrisis: 1` 로 배포되어 있다**는 확인이다
  (명세 실사 R 이 이론이 아니라 사실이었다). 순수 함수만 검사했으면 못 봤을 것이라
  `M21_T7_ShippedSeasons_OnlyWinterFeedsTheTrap` 에 "함정의 근거가 사라지면 red" 를 함께 걸었다.
- **배회 재선정 주기를 안 두면 경로탐색이 폭증한다.** `PickWalkableNear` 가 현재 타일을 뽑으면
  경로가 즉시 소진되고, 그러면 다음 프레임에 또 뽑는다 — 제자리 떨림 + JPS 매 프레임 호출.
  `WANDER_REPICK_SEC = 0.5` 쿨다운 + "뽑힌 타일 == 현재 타일이면 대기" 로 닫았다. 명세
  ⚠️ 오해 위험 2 가 정확히 이 실패를 예고했고, 읽어 둔 덕에 처음부터 피했다.
- **MCP 콘솔이 `Debug.Log` 를 안 잡는다** (warning/error 만). `[Threat] 체류 시작` 류 로그로
  DoD 를 확인하려던 계획이 막혀서, **로그 대신 상태를 읽는 쪽으로 바꿨다** — `ArrivedDay` ·
  `LastStrikeDay` · `IsExiting` · 위치 시계열. 결과적으로 더 강한 증거가 됐다: "타격 0회 +
  퇴장 중 + 상한 미도달" 은 로그 문구보다 정확히 `밭 0/0 소실 반복 0건` 을 증명한다.

**다음에 할 일:**

- 🔴 **리뷰⓪ = 사용자 Play (문)**. 명세가 "동선이 안 보이면 W3 착수 금지" 로 못박아 둔 자리다.
  볼 것: ①늑대가 정지하지 않는가(내 관측은 좌표 시계열이지 **보이는 동선이 아니다**)
  ②평시 주민 타깃 출몰이 오는가 ③**초반 사망률이 과하지 않은가** — Tier1 `0.25` 는 제안치이고,
  명세 §4-R 이 "1년차에 과한지 Play 로 조정" 이라고 미리 적어 뒀다.
- 🟡 W2R 이 남긴 관찰 2건:
  ① 체류 상한 0.75 일이 **너무 짧을 수** 있다 — 관측에서 밭형이 한 번 치고 나가는 경우가 잦았다
     (타격 1회로 끝난 개체 3마리). 3회는 상한이지 기대값이 아니다.
  ② `[JPSPathfinder] 목표 타일이 이동 불가` 경고 2건. 배회는 `_isWalkable` 로 거르므로
     `ApproachTileOf`(밭 곁 후보가 통행 여부를 안 본다, W2 부터 있던 코드)가 유력하나 **미확정**.
     CLAUDE.md 가 허용하는 자기회복 경고 계열이라 차단 사유는 아니다.
- **누적 Play 확인 목록** (리뷰⓪에 얹으면 세션 한 번으로 끝난다): 피로 주민이 쉬는가 ·
  Plant 한 칸씩 리듬 · GatherStone 30 언저리 정지 · 겨울에 곳간으로 요리가 되는가 ·
  W2 실측 ③(Tier2 무리 체류 중 재추격 — 이번 수정으로 되살아난 경로다).
- **이월 (M21 후 한 묶음)**: 기존 목록 + 개입 인프라가 남긴 3건(FOOD_ALERT_DAYS 이름 ·
  카메라 unscaled 체감 · 겨울 경보 창) + **"에셋 값 변경 검사"를 양방향으로**(죽였는가/켰는가).

## [22:13] docs(goap): 리뷰⓪ ① 통과 — 동선이 보인다, W3 착수 차단 해제

**태그:** #misc

**무엇을 했나:**
- docs(goap): 리뷰⓪ ① 통과 — 동선이 보인다, W3 착수 차단 해제

**왜 이렇게 했나 (커밋 본문):**
사용자 Play 판정: "동선이 아주 잘 보여. 이전에는 가만히 멈춰있었지만 지금은 돌아다니는
게 확실히 잘 보여."

이 항목이 §W2R 의 **유일한 차단 조건**이었다("동선이 안 보이면 W3 착수 금지"). 열렸다.

🔑 W2 를 뒤집었던 **바로 그 축에서 반대 판정**이 나왔다. W2 판정은 "원 하나가 밭 위에
서서 콘솔에만 로그를 뿜는 것"이었고 이번은 "돌아다니는 게 확실히 잘 보여"다 —
고친 것이 맞는 것을 고쳤다는 뜻이고, 게이트로 증명 불가한 성공 기준에 Play 를 문으로
건 판단도 값을 했다.

잔여 2항은 차단이 아니라 관측 항목으로 남긴다:
② 평시 주민 타깃 출몰이 오는가
③ 초반 사망률이 과하지 않은가 — Tier1 VillagerTargetChance 0.25 는 제안치이고
   §4-R 이 "1년차에 과한지 Play 로 조정"이라 미리 적어 뒀다. W3 진행 중 관측이 쌓이면
   그때 조정한다. 이 값은 코드가 아니라 **에셋 한 칸**이라 되돌리기가 싸다.

**변경 파일:**
  - Docs/M21_격퇴축_실행명세서.md

- (+22:18) docs(goap): 리뷰⓪ ②③ 기록 — 피해 경로는 살아 있다, 난이도는 아직 미측정
  사용자 Play 보고: 자연 출몰에서 주민 추격·사망은 못 봤지만, 내가 늑대 10마리를 강제
  투입했을 때는 사망자가 나왔다.
  
  이 둘을 구분해서 적는다 — 같은 "사망 관측"이지만 말하는 바가 다르다.
  
  ② 🎉 **피해 경로 자체는 살아 있다.** W2 시절엔 Tier1 이 밭 전용이라 주민 추격 경로가
     실행조차 안 됐고, 그래서 "위협이 누구도 직접 죽이지 못한다"가 걱정거리였다.
     이제 늑대가 사람을 쫓아 죽인다. 자연 출몰 미관측은 확률 0.25 기준 정상 범위다.
  
  ③ ⚠️ **난이도 판정의 근거는 아니다.** 그 사망은 출몰 밀도를 인위적으로 10배로 만든
     상태에서 나왔다. 방법론 M20 — 전수/관측은 무엇을 고정(또는 조작)했는지 함께 적는다.
     여기서 조작한 축은 **출몰 밀도**이고, 그게 결정적 축이다.
     Tier1 VillagerTargetChance 0.25 는 자연 진행 판에서 관측이 쌓인 뒤 조정한다.
     에셋 한 칸이라 되돌리기가 싸다.
  - Docs/M21_격퇴축_실행명세서.md
  - devlog/sessions/2026-08-07.md

## [22:27] feat(goap): M21 W3 — CombatService 신설, 판정을 한 집에 모은다

**태그:** #debug-viz

**무엇을 했나:**
- feat(goap): M21 W3 — CombatService 신설, 판정을 한 집에 모은다

**왜 이렇게 했나 (커밋 본문):**
지금까지 위협은 때리기만 했다. 이 항목부터 주민이 되받아칠 수 있다.
명세 = Docs/M21_격퇴축_실행명세서.md §W3.

■ 역할 경계 (ADR-M21-3 — M10-C 의 확장)
  ThreatService = 출몰 스케줄·수명 / ThreatAgent = 표현+이동 /
  VillagerAgent = 표현+자기 상태의 문 / **CombatService = 판정**
  ThreatService 에 얹지 않은 이유: 출몰 수명과 교전 판정은 다른 책무다. 주민 체력·아사는
  위협과 무관한데 ThreatService 가 그걸 들면 어색해진다 (명세 ⚠️ 오해 위험 ①).

■ 에이전트=주민 전제 없음 (⚠️ 오해 위험 ②)
  VillagerHit(string attackerId, float damage, ThreatAgent target) — 타격자는 ID + 수치로만
  들어온다. 몬스터·타 종족이 서로를 때릴 때 같은 문으로 들어와야 한다.

■ 순수 판정 3종 (게이트 M21_T8) — ShouldFlee / ShouldRout / HitInterval
  HitInterval 은 직업·무기 배율을 곱 결합한다 (무장 사냥꾼 = 4배, M20 배율 문법).
  무기는 게이트가 아니라 배율이다 (ADR-M21-5) — 맨손도 싸울 수 있고 다만 느리다.
  ShouldRout 은 W6(마릿수) 전까지 실질 무효지만 순수 함수와 게이트를 먼저 둔다 —
  W6 은 배선만 하면 된다.

🔴 명세 밖 결정 3건 (전부 사유 있음)

1. ThreatSO.MeatDrop 을 W6 에서 당겼다. W3 스케치가 드랍을 요구하는데 필드는 W6 목록에
   있었다. 없으면 "사냥"이 로그만 남고 아무 일도 안 일어나 사건이 반쪽이 된다.

2. 🔴 **"도주 중인 개체는 못 때린다"를 자가 재검토에서 뒤집었다.** 처음엔 등 돌린 적을
   때려 잡는 길을 막으려 Missed 로 뒀는데, 배포 수치로 검산하니 **사냥이 영원히 도달
   불가**였다: 늑대 60 · 주민 타격 10 · 도주선 24 → 60→50→40→30→20 에서 도주로 빠지고
   이후 모든 타격이 막힌다. 그러면 MeatDrop 도 Job_Hunter(W8)도 ADR-M21-8 도 전부 죽은
   코드가 된다. 게다가 **그게 사냥의 정의다** — 도망가는 짐승을 끝까지 쫓아가 잡는 것.
   도주선은 여전히 값을 한다: 도주한 개체는 마을을 안 치므로 **격퇴만으로도 이김**이고
   (ADR-M21-1), 쫓아가 잡는 것은 고기를 위한 **선택**이 된다.
   격퇴 사건은 전이 순간 1회만 발신한다(!IsFleeing 확인) — 아니면 때릴 때마다 "물리쳤습니다".

3. HP_EPSILON = 1e-3 신설 (알고리즘 상수). 60f * 0.4f 는 24 가 아니라 **24.0000003** 이라
   체력이 정확히 24 일 때 "문턱에서는 버틴다"가 조용히 뒤집힌다. 가상의 경계가 아니다 —
   체력 60 에 피해 12 면 60→48→36→24 로 정확히 닿는다. **게이트가 red 로 잡았다.**

■ 검증
  EditMode **336/336** (332 → 336, M21_T8 4종 신설).
  Play 실측 — 맨손 10 피해 ×6: Damaged×3 → Repelled(20HP) → Damaged → Killed(0HP),
  격퇴 이벤트 1회 · 사냥 이벤트 1회 · 드랍 2 가 창고로(생고기 0 → 2).
  중립 불변식 양방향 실측: Threats 3개 → Combat 있음 / Threats 0개 → Combat null
  (WorldConfig 를 임시로 비워 확인 후 git 복원, 복원 검증까지 완료).

다음 = W4 (Goal_Fight + FightRunner). 지금은 판정만 있고 **부르는 사람이 없다** —
이 커밋 시점에 VillagerHit 의 호출처는 0 이다.

**변경 파일:**
  - Assets/Scripts/M0/Data/ThreatSO.cs
  - Assets/Scripts/M0/SimulationLoop.cs
  - Assets/Scripts/M0/World/CombatService.cs
  - Assets/Scripts/M0/World/CombatService.cs.meta
  - Assets/Tests/EditMode/M21_CombatGates.cs
  - Docs/M21_격퇴축_실행명세서.md

**막혔던 문제 / 해결 방법:**

- 🔴 **배포 수치로 손 검산했더니 사냥이 도달 불가였다.** 처음엔 "등 돌린 적을 때려 잡는 길을
  막는다"는 이유로 도주 중인 개체를 `Missed` 로 뒀다. 말은 그럴듯한데 숫자를 넣어 보니
  늑대 60 · 주민 타격 10 · 도주선 24 → `60→50→40→30→20`에서 도주로 빠지고 그 뒤 모든 타격이
  막힌다. **`MeatDrop`·`Job_Hunter`(W8)·ADR-M21-8 이 전부 죽은 코드가 되는 설계였다.**
  게다가 뒤집고 나니 그게 사냥의 정의였다 — 도망가는 짐승을 끝까지 쫓아가 잡는 것.
  🔑 **게이트도 컴파일도 이걸 못 잡는다.** 잡은 것은 "이 수치로 실제로 몇 대 때리면 어떻게
  되는가"를 손으로 세어 본 것뿐이다(M17 "수치의 짝" 규율의 실제 용례).
  🔑 그리고 뒤집으면서 알았다 — **도주선은 죽지 않았다.** 도주한 개체는 마을을 안 치므로
  격퇴만으로 이김이고(ADR-M21-1), 쫓아가 잡는 것은 고기를 위한 **선택**이 된다. 오히려
  "격퇴로 끝낼까, 쫓아가 잡을까"라는 판단이 생겼다.
- 🔴 **`60f * 0.4f`는 24가 아니라 24.0000003 이다.** "도주선에 **정확히** 닿으면 아직 버틴다"고
  주석까지 써 놓고 구현은 반대로 동작했다. **게이트가 red 로 잡았다** — 내가 그 경계를
  테스트에 적어 뒀기 때문이다. 가상의 경계도 아니다: 체력 60에 피해 12면 `60→48→36→24`로
  정확히 닿는다. `HP_EPSILON = 1e-3` 으로 닫았다(체력 1눈금의 1/1000 — 밸런스엔 안 보이고
  이 배율대의 float 오차 ~3e-6 보다는 충분히 크다).
  🔑 교훈은 "float 조심"이 아니라 **"경계값을 주석에 쓸 거면 테스트에도 써라"** 다.
  주석만 있었으면 이 버그는 몇 밀스톤 뒤 밸런스 이상으로 나타났을 것이다.
- **명세의 항목 경계가 한 칸 어긋나 있었다** — W3 스케치가 `so.MeatDrop` 을 쓰는데 필드 append 는
  W6 목록에 있었다. 당겨 왔다(W6 목록에서 한 칸 뺀다). 없으면 사냥이 로그만 남고 창고는
  그대로라 "사냥 사건"이 반쪽이 된다.
- **커밋 직전 무관한 변경 1건** — `LiberationSans SDF - Fallback.asset`(TMP 동적 폰트 아틀라스)이
  Play 중 글리프 생성으로 더러워졌다. W3 과 무관하므로 되돌리고 커밋했다.

**다음에 할 일:**

- 🎯 **W4 = Goal_Fight + FightRunner (자율 참전) + 명령 통로.** 🔴 지금은 **판정만 있고 부르는
  사람이 없다** — `VillagerHit` 의 호출처가 0 이다. W4 가 그 자리를 채운다.
  재검사 결정 2건이 명세에 이미 박혀 있다: **A2** 교전 중 부상은 러너를 끊지 않는다(인접한
  위협 앞에서 감속 0.4로 이탈하는 게 오히려 자살) · **B1** 명령된 Goal_Fight 는 트리거·즉시
  달성 검사를 우회하고 원정한다(안 그러면 밭 타깃 체류 위협에 명령이 공수표가 된다).
- 🔴 **리뷰① = W3 후 HP 환산 검산** (명세가 걸어 둔 자리). W4 로 실제 교전이 돌기 시작하면
  "맨손 6대 24초 vs 재타격 0.25일" 이 실제로 *"맨손 솔로는 아프다"* 로 나오는지 잰다.
- 🟡 리뷰⓪ 잔여 ②③(자연 출몰 주민 타깃 · 초반 사망률)은 W4 Play 에서 겸사 관측.
  `Tier1 0.25` 조정은 자연 진행 판 관측 뒤 — **에셋 한 칸**이라 되돌리기가 싸다.


## [22:56] feat(goap): M21 W4 — Goal_Fight + FightRunner, 주민이 되받아친다

**태그:** #goal-selection

**무엇을 했나:**
- feat(goap): M21 W4 — Goal_Fight + FightRunner, 주민이 되받아친다

**왜 이렇게 했나 (커밋 본문):**
W3 가 판정을 만들었지만 부르는 사람이 없었다. 이제 주민이 실제로 싸운다.
명세 = Docs/M21_격퇴축_실행명세서.md §W4.

■ 교전 goal — 피신과 같은 트리거, 반대 성향
  Goal_Fight(105) = Goal_Flee(105) 동률 + 겁 가중치 부호 대칭(−0.8 / +0.8).
  높이면 겁쟁이도 싸우고 낮추면 아무도 안 싸운다 — 승부는 우선순위가 아니라 성향이 가른다
  (⚠️ 오해 위험 ①). Play 실측: 늑대 6마리 투입 직후 H(겁 −10)는 교전, B·C·G 는 피신.

■ FightRunner — 판정은 하지 않는다 (⚠️ 오해 위험 ③)
  최근접 위협에게 다가가 간격마다 CombatService.VillagerHit 을 부를 뿐이다.
  대상 선정에서 도주·퇴장 개체를 제외한다(무한 추격 0) — 격퇴는 그 자체로 이김이라
  쫓아갈 이유가 없다. 도망가는 짐승을 잡는 것은 사냥꾼(W8)의 몫.
  배율은 발명하지 않고 중립값(1·무기 없음)으로 둔다 — W5·W8 이 인자만 갈아 끼우면 된다.

🔴 명세 밖 결정 3건

1. Goal_Fight 를 DirectActionPool goal 로 했다. 이유가 있다: 교전 액션과 도망 액션의
   **효과가 ThreatNear→0 으로 같아서**, 플래너에 맡기면 "싸우라"는 goal 이 도망 액션을
   골라도 정답이 된다. 자격 ⓐⓑⓒ도 실제로 충족한다(끝이 슬롯 밖 = 위협 개체의 상태).
   GoalSO 툴팁과 게이트 M12_T4 목록을 **함께** 고쳤다 — 목록만 고치면 감사가 무력해진다.

2. GoalSO.OrderedIgnoresTrigger 신설 = 재검사 B1 의 구현체. 명령일 때만 트리거를 우회한다.
   자율에 열면 전 맵 주민이 밭 늑대 한 마리로 몰려간다(⚠️④) — 원정은 플레이어 개입의 값이다.

3. 🔴 P0 승인 목록 등재 — 105 > 배고픔 100 이라 게이트 M12_T3 가 막았다. ADR-M12-4 ③의
   3조건을 논증해 통과했다: ⓐ트리거가 피신과 완전히 같다 ⓑ위협이 사라지면 자기 종료
   ⓒ타격 34×3 > 체력 100 = 0.5일 치사 vs 굶주림은 포만 70→10 에만 2.4일.
   겸사로 M12_T3 의 예외를 하드코딩(g == flee)에서 **승인 목록 참조**로 바꿨다 — 목록이
   두 벌이면 갈리고, 그때 "승인했는데 게이트가 막는다"가 되어 절차 자체가 우회된다.

🔴 Play 가 잡은 것 — 좀비 명령 (게이트 341개 green 인 채로)
  늑대가 다 물러난 뒤에도 D 가 「싸워라」를 쥔 채 노동으로 돌아갔고, 쿨다운이 풀릴 때마다
  다시 교전 goal 을 집었다. 원인이 두 겹이었다:
    ① FightRunner 가 "대상 없음"을 실패로 처리 → 실패·쿨다운·재선택의 순환
    ② 명령 완수 판정(TickIdle)이 **GoalConditions 가 있을 때만** 도는데, DirectActionPool
       goal 은 규약상 그 배열이 비어 있다 → 완수 판정이 통째로 건너뛰어진다
  → ①"칠 것이 없다"를 완료로(싸우라 했는데 적이 없으면 그 일은 끝난 것이다)
    ②플랜 완료 지점에 조건 없는 명령의 완수 경로 신설.
  🔑 W4 만의 문제가 아니다 — **DirectActionPool goal 을 명령으로 주면 영영 안 비워지는**
  구조적 구멍이었다.

■ 검증
  EditMode **341/341** (336 → 341, M21_T9 5종 신설).
  Play 실측: 분화(교전 1 vs 피신 3 동시) · 명령 거부(포만 20 → RefusedHungry, 80 → Accepted) ·
  원정(35타일 밖에서 명령 → ThreatNear=0 인 채 이동) · 착탄(늑대 60→50→40→30) ·
  위협 소멸 후 교전 goal 잔류 0 · 좀비 명령 수정 후 재현 시 CurrentOrder 즉시 비워짐.

🟡 관찰 이월: 주민 이동 2.0 < 늑대 2.5 라 **배회하는 늑대를 쫓아가 잡기는 불안정하다.**
접촉은 늑대가 다가올 때 성사된다. 사냥꾼(W8)·무기(W5)가 이 축을 건드리므로 그때 함께 잰다 —
지금 속도를 만지면 W2R 동선이 바뀐다.

다음 = 리뷰①(HP 환산 검산) 또는 W5(원시 무기).

**변경 파일:**
  - Assets/M0Config/ActionCatalog.asset
  - Assets/M0Config/Actions/Action_Fight.asset
  - Assets/M0Config/Actions/Action_Fight.asset.meta
  - Assets/M0Config/Goals/Goal_Fight.asset
  - Assets/M0Config/Goals/Goal_Fight.asset.meta
  - Assets/Scenes/M0Scene.unity
  - Assets/Scripts/M0/Agent/Runners/FightRunner.cs
  - Assets/Scripts/M0/Agent/Runners/FightRunner.cs.meta
  - Assets/Scripts/M0/Agent/VillagerAgent.cs
  - Assets/Scripts/M0/Data/FightActionSO.cs
  - Assets/Scripts/M0/Data/FightActionSO.cs.meta
  - Assets/Scripts/M0/Data/GoalSO.cs
  - Assets/Scripts/M0/Planning/GoalSelector.cs
  - Assets/Scripts/M0/UI/PlayerInputController.cs
  - Assets/Scripts/M0/World/ThreatService.cs
  - Assets/Tests/EditMode/M12_TraitGates.cs
  - Assets/Tests/EditMode/M21_CombatGates.cs
  - Docs/M21_격퇴축_실행명세서.md

