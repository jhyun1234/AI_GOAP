# 블로그 발행 커버리지 대조표 + 주기 점검 절차

*목적: "블로그 글이 개발 순서에 맞게 잘 정리되어 나가고 있는가"를 어느 세션에서든
즉시 점검하기 위한 허브. 사용자가 "블로그 순서/품질 점검해줘"라고 하면 이 파일부터 읽는다.*

---

## 🔴 소재 범위 — 게임 개발만 (2026-08-03 사용자 지시, 이 파일의 옛 기록보다 우선)

**이 블로그는 게임 개발 내용만 다룬다.** `tools/` 아래 자동화 트랙
(`scene-video` 영상 렌더러 · `blog-automation` 자기 자신 · `content-repurpose` X 트랙)은
**소재가 아니다.** 커밋이 아무리 많아도 후보로 올리지 않는다.

⚠️ **아래 점검 기록들은 `scene-video` 를 대기열에 넣어 두었다** — *"미발행 구간은
M13~M17(+scene-video)"*, *"M12 → M13 → scene-video 순서로 박제"* 같은 문장이 여러 회차에
남아 있다. **그건 2026-08-03 이전의 판단이고 지금은 무효다.** 과거 기록이라 지우지 않았을
뿐이니 그 줄을 근거로 영상 트랙을 소재로 고르지 마라. 실제 대기열은
**M13 → M14 → M15 → M16 → M17** 다섯 구간이다.

🔑 그래서 **"게임 밀스톤 커밋 없음"으로 끝나는 회차가 정상적으로 존재한다.**
영상 트랙만 돈 날이 그렇다 — 사고가 아니라 소재 부족(SKIP)이다.

---

**마지막 사전점검**: 2026-08-13 (자동 스케줄, **12:11 KST 실행** — 목표 12:03 대비 8분 늦음.
08-12에 이어 **2회 연속으로 회차 시각(13:03) 이전에 시작**했다). **오늘(13일)은 홀수일** —
실측 cron(`3 4 2-30/2 * *`)이 짝수일만 발동하므로 **오늘 13:03 발행 자체가 없다.**

🎉 **이번 점검의 최대 확인 — 08-12 회차가 Step 8까지 완주했다. 08-10 사고는 일과성이었다.**
`git pull` 이 `chore(blog): auto-run state update (2026-08-12) — M20 발행 + M21 ACTIVE 지정`
(`cd76b9d2`, origin/main 팁)을 끌어왔다. 08-12 점검이 남긴 관측점 — *"발행 후 `chore(blog)`
커밋이 origin/main에 남는지 다음 점검이 확인할 것"* — 에 대한 **답은 예다.** 상태 파일 3종·
로컬 사본(`published/2026-08-12-unity-goap-m20-carpenter-rule.html`)이 전부 push 됐고,
게시팀이 POST 응답 content를 md5로 대조해 byte-identical 을 직접 확인한 기록까지 남겼다.
**08-10 상태 커밋 유실은 반복 고장이 아니라 1회성이었다**(다만 재발 방지책 자체는 아직
미이행 — 아래 ⑤).

🔴 **최대 발견 — 미발행 대기열이 9구간 → 13구간으로 뛰었다.** 08-12 오후 하루에 밀스톤
**넷이 새로 완결**됐고(M28 맵 대형화 마감 · M29 바이옴 1~4차 · M30 슬롯 예산 · 그리고
08-12 점검이 "진행 중"으로 뺐던 **M27 시간 밸런스가 ✅ 완료로 승격**), M31 높이 표현 축이
W1~W3후속까지 진행 중이다. 격일 발행으로 **26일 분량**이며 그동안에도 개발은 계속 쌓인다.
🔑 개발이 발행을 앞지르는 속도가 08-11(10구간) 이후 줄지 않았다 — **회차당 1밀스톤 원칙을
유지할지, 묶음 회차를 허용할지는 여전히 사람의 결정 사항**이라 여기서는 관측만 기록한다.

**커버리지 갱신(①) — 신규 발행 1행(#21) + 신규 완결 4행 + 진행 중 1건.**
- **#21 M20 "직업 전면 재점검" — 2026-08-12 발행 완료.** `blogger_post_id`
  `9139247089049724695`, 정상 승인 경로 완주(마스터 Step 6 2차 APPROVED, 반려 0회).
  대조표에 추가하고 M20 미발행 행을 발행 완료로 갱신했다.
- **M27 시간 밸런스 — 완결로 승격.** 08-12 점검이 "Play 실측 6건 이월"을 이유로 뺐으나,
  memory `MEMORY.md` START HERE가 **"⏱️ 시간 밸런스 축 = M27 — ✅ 완료 (게이트 482)"**로
  갱신됐다(잔여 실측은 §7 후속 관측으로 분리). → 대기열 편입.
- **M28 맵 대형화 — 완결.** `eb7dc75d`(spec) ~ `716a9cff`(W3 200² 스위치, 게이트 485) ~
  `7304dfcd`(소크 러너) ~ `f562f71f`("M28 마감 — 소크 3판 전말 + W3 DoD 실측"). 사용자
  200² 화면 확인 완료("문제 없어"). 300² 승격은 §8 관문 5조건 뒤 **값 회전만**이라 별건.
- **M29 바이옴 — 1~4차 완결.** `a7688b39`(spec) ~ W1~W4(`df2f1239`·`b03392e4`·`39e9aff7`·
  `5188e365`, 게이트 490) ~ 2차 화면 읽힘(`1b3e7a1a`, 491) ~ 3차 장식(`e3b9b658`·`99ead161`) ~
  4차 경계 전이 타일(`0a15838e`, 게이트 **492**).
- **M30 슬롯 예산 — 완결(소품).** `8d07c008`(명세 + W1 증설 52→72, 게이트 485) ·
  `29201153`(세션 기록 "잔여 1칸은 착시였다"). 2커밋짜리 리팩터라 **단독 회차로는 얇다** —
  M29 광물 사다리의 상류라 **M29 회차에 흡수하는 편이 낫다**(다음 기획팀 판단).
- 🟡 **표에 올리지 않는 것 2건**: **M31 높이 표현 축**은 `964e4a8a`(spec) ~ `e7b272ed`
  (W3후속)까지 **W1~W3만 끝난 진행 중**이며 memory START HERE가 W4 연결성·W5 게이트를
  남겨 두고 있다. **M22 3차(망루·함정)**는 리뷰①(관전 7건)이 08-09 이후 나흘째 미완이다.

**게이트 판정(②, Step 0 재계산 — 08-06 개정 계산식, `LAST=40b44176`, origin/main 기준)**:
`game_commits=186` `big=32` `days=1` → **기준 A(≥10)·B 통과**(C는 1일 경과라 미달이나
하나만 충족하면 된다). 최상단이 `STATUS: ACTIVE`(M21, 08-12 게시팀 Step 8 지정)라 **기준 D도
통과**. `publish_status`도 `PUBLISHED`로 정상(무결성 가드 해당 없음). **게이트만 보면 발행
확정이나, 홀수일이라 cron 자체가 오늘 발동하지 않는다.** 다음 실제 회차는 **08-14(짝수일)**.
⚠️ 로컬 HEAD 기준으로는 20커밋(M29 3·4차·M31 전 구간)이 더 있으나 **원격이 읽는 값은
origin/main 기준 186**이다 — 아래 push 로 이 격차도 해소된다.

**순서 점검(③)**: **역전 없음.** M20까지 발행 완료, 다음 차례는 M21이며 08-12 게시팀이
지정한 ACTIVE가 정확히 그 자리를 가리킨다. 대기열은 **M21 → M22 1차 → M23 → M22 2차 →
M24 1차 → M22 4차 → M25 1차 → M26 1차 → M26 2차 → M27 → M28 → M29(+M30) → M31(완결 후)**.
M22 3차는 리뷰① 통과 시 M22 2차와 M24 1차 사이에 끼어든다.

**교정(③)**: **ACTIVE 지정은 덮어쓰지 않았다** — M21은 대기열 선두가 맞고 소재도 미소비다
(08-12 게시팀이 커밋 범위·1차 소스·M20 중복 금지까지 정확히 기입해 뒀다). 대신 그 블록의
🟡 대기열 줄에 **M27·M28·M29·M30 편입 + M31 진행 중**을 덧붙였다 — 08-12 기입의 대기열이
M26 2차에서 끝나 있어 그대로 두면 다음 게시팀이 네 구간을 빠뜨린다(08-10·08-11 점검이
같은 이유로 같은 교정을 했다).

**품질 스팟 체크(④, 최신 발행 글 = #21 M20)**: ✅ **라이브 URL 직접 열람 — 통과.**
https://gamedevclaude.blogspot.com/2026/08/unity-goap-ai-claude-code-ai.html 를 열어 숫자
주장 3건을 커밋·devlog 원문과 대조했고 **전부 일치**했다. ① *"14개 파일"*(집 부탁 사슬
삭제) ↔ `48b61cb3` 커밋 본문 *"삭제 14파일: SeekJobRunner·SeekJobActionSO(호출처 0)·
SeekCarpenter 액션·Goal_SeekCarpenter·Goal_RequestHouse·집 부탁 2종 (+meta) +
FindNearestWithJob"* 및 `devlog/sessions/2026-08-05.md:825` *"W12: 집 부탁 사슬 14파일
삭제"* 와 축자 일치. ② *"하루에 13단계"* ↔ 실제 커밋 범위가 W1~W13(`facada95`~`9a45279d`)
이고 전부 08-05 하루 안. ③ 효율 0.5 승격 4종(농부·요리사·나무꾼·광부)과 목수·탐험가
휴면 ↔ `7040580c`(W3 전문가 4종 승격)·`26012ab6`(W4 탐험가 휴면)·`9a45279d`(W13 목수
휴면). 헌장 인용 *"○○가 없는 판은 ~한 판이다를 한 줄로 쓸 수 없으면 그건 직업이 아니다"*도
`4760b792`(W6 직업 헌장 박제)/ADR-M20-1과 일치. 발행 시각 표기 "August 11"은 Blogger 가
PT 로 표시한 것(`2026-08-11T23:18:28-07:00` = KST 08-12 15:18)이라 **오류가 아니다.**

**파이프라인 건강(⑤)**: 🟢 **08-12 회차 완전 정상 — 발행·상태 갱신·커밋·push 전부 성공.**
403 흔적 없음, 반려 0회, 중복 게시 없음. 🔴 **다만 경보 1건은 OPEN 유지**: "발행은 됐는데
상태 커밋이 유실됐다"(08-10)는 **재발이 없었다는 관측을 추가했으나**, 그 경보가 스스로 적어
둔 CLOSE 조건(*"게시팀 Step 8 이 `git push` 성공까지 확인하도록 `blog-publisher.md` 보강"*)이
아직 이행되지 않았다 — 관측 1회로 닫지 않는다. 🔴 **미해결 부수 결함 1건 그대로**:
`scripts/blogger-client.js:118` `parseArgs` 의 `if (next && !next.startsWith('--'))` 가
빈 문자열 `--labels ""` 를 불리언 `true` 로 오인하는 건 — 08-09부터 **5회 연속 이월**
(이번에도 해당 줄을 직접 열어 미수정 확인. 수정 자체는 이 점검의 범위 밖).
🔴 **사람 몫 2건 그대로**: ⑴ `blog-publisher.md` Step 8 push 확인 보강(다음 로컬 세션 몫)
⑵ Blogger 초안 `9034137679451863013`(M19, DRAFT) 삭제 — 공개본이 존재하므로 계속 시급.

**오늘(08-13) 13:03 예측**: **홀수일이라 cron 미발동 — 발행 없음**(게이트는 A·B·D 통과
상태이나 발동 자체가 없다). 다음 실제 회차는 **08-14(짝수일)**이며, 소재는 08-12 게시팀이
지정한 **M21 "격퇴 축"**(명세 `99c3c416` ~ `2b35f50`, W1~W10 완결 + 리뷰① 통과)이다.
🎁 M20 본문 말미가 예고한 "사냥꾼·목수 재탄생"이 M21에서 실현되므로 회차 연결이 자연스럽다.

### 이전 사전점검 이력 (2026-08-12, 기록 보존용)

**마지막 사전점검**: 2026-08-12 (자동 스케줄, **12:11 KST 실행** — 목표 12:03 대비 8분 늦음.
07-19 이후 8회 연속 이어지던 "점검이 회차 시각을 넘겨 시작"하는 패턴이 **처음으로 끊겼다** —
오늘은 13:03 발행 전에 교정을 끝낼 시간이 있었고, 아래 발견을 보면 그 8분이 결정적이었다).
**오늘(12일)은 짝수일** — cron(`3 4 2-30/2 * *`) 발동 대상일이다.

🔴🔴 **이번 점검의 최대 발견 — 08-10 회차는 무발동이 아니라 "발행 성공 + 상태 커밋 유실"이었다.
그리고 그 오진 때문에 오늘 M19가 두 번째로 발행될 참이었다.**
08-11 점검은 `chore(blog)` 커밋 0건을 근거로 "08-10은 아예 돌지 않았다"고 단정했다.
**틀렸다.** 라이브 블로그를 열어 보니 **2026-08-10 자로 M19 글이 정식 공개 발행돼 있다**:
"완성한 날 전부 지웠습니다: 마을 화폐 경제 45개 파일 2,260줄 철거기"
(https://gamedevclaude.blogspot.com/2026/08/45-2260-unity-goap-claude-code-ai.html).
본문 도입부가 M19 서사(45파일 −2,260줄 철거·GOAP 마을·재정 정책이 개입 수단이었다는 진단)와
일치한다. 즉 **Step 7(발행)은 성공했고 Step 8(상태 갱신·커밋·push)만 남지 않았다.**
- **결과**: `blog_last_published_commit.md`는 M15(`ece7029`)에, `blog_next_material_
  priority.md`는 **M19 `STATUS: ACTIVE`(미소비)**에 멈춰 있었다. 게이트 기준 D는 최상단
  ACTIVE를 무조건 통과시키므로, **오늘 13:03 run 은 M19를 소재로 다시 집어 같은 밀스톤을
  중복 발행**했을 것이다 — 이 블로그가 겪은 적 없는 신종 사고다.
- **교정 완료(아래 ③)**: M19 블록을 `CONSUMED(2026-08-10)`로 닫고, **M20 "직업 전면
  재점검"을 새 `STATUS: ACTIVE`로 지정**했다. 두 상태 파일 모두 소급 복원했다.
- **강제 push 로 지워진 게 아니다**: `git merge-base --is-ancestor dfd6d6bf origin/main`
  통과 — 08-10 당시 origin/main 팁이 현재 이력에 살아 있고, 로컬 117커밋 push 도
  fast-forward 였다. **애초에 push 된 적이 없다.**
- 🔑 **교훈(이 파일에 박제): 파이프라인의 성공 증거는 커밋이 아니라 발행물이다.**
  08-11 점검은 커밋 로그만 보고 결과물을 보지 않았다. **무발행이 의심되면 `git log`가
  아니라 블로그 아카이브를 먼저 열 것.** "DRAFTED 회차조차 커밋을 남겼으니 커밋이 없으면
  안 돈 것"이라는 추론은 **커밋 단계 자체가 고장날 수 있다는 경우**를 빠뜨린 것이었다.
- 🎉 **짝수일 규칙은 깨지지 않았다.** 07-25 이후 지금까지 유효하다 — 08-11이 "규칙이
  깨진 첫 사례"라고 기록한 것도 오진의 산물이므로 인용하지 말 것.

🎉 **부수 해소 — 로컬 미푸시 117커밋이 전부 push 됐다.** `git rev-list --left-right
--count origin/main...HEAD` = `0 0`. origin/main 최신은 `79fef8c4`(08-12 03:12)이고,
08-10·08-11 점검이 "원격이 보는 세계는 08-09에 멈춰 있다"고 경고한 상태는 **해소됐다.**
그래서 이번 게이트 계수가 161 → **254**로 뛰었다(계산 착오가 아니라 실제 유입).

**커버리지 갱신(①) — 신규 발행 1행(#20) + 신규 완결 1행(M26 2차).**
- **#20 M19 "화폐 전면 철거" — 2026-08-10 발행 완료** (위 발견). 대조표에 소급 추가하고
  M19 미발행 행을 발행 완료로 갱신했다.
- **M26 2차 "지형 축" — 완결.** memory START HERE가 "M26 = 1·2차 전체 종료 + Play 후속
  대개정 완료(게이트 488 · 실측 잔여 0)"로 명시. `b58ce0b7`(W8 게이트 마감, 08-11 15:49)
  이후에도 Play 결함 3건 당일 수정(`4fdcd891` UI 팩 첫 적용 · `08a5dbc2` T 목록 시계 기반
  판정 · `ab8cb927` 추격 3초 벌칙 ADR-M2-5 개정)과 **W7R 간호 대개정**(`a9d30967`·
  `cef039d1`·`ec154985`·`2ea5d211`), **위험 기억 기하 조정**(`dfc65358`)까지 붙어 종료 →
  신규 행.
- 🟡 **표에 올리지 않는 것 2건**: **M27 "시간 밸런스"**는 `8e864a7e`(명세) ~ `ec6b4513`
  (W4 게이트 482)로 **W1~W4만 끝났고 Play 실측 6건이 이월**돼 진행 중 — 완결 자격 미충족.
  **M22 3차(망루·함정)**는 리뷰①(관전 7건)이 여전히 미완이다.
- 🟡 **ponytail 감사 2회분**(`d05642aa`~`59551d39`, 순 −1,144줄 · 게이트 488→480)은
  밀스톤이 아니라 전 구간 리팩터 스윕이다 — 대기열에 넣지 않고 관측만 기록한다.

**게이트 판정(②, Step 0 재계산 — 08-06 개정 계산식, `LAST=ece7029`, origin/main 기준)**:
`game_commits=254` `big=38` `days=12` → **기준 A·B·C 전부 통과**. 최상단 항목이
`STATUS: ACTIVE`(**이번 점검이 지정한 M20**)라 **기준 D도 통과**. `publish_status`도
`PUBLISHED`로 정상(무결성 가드 해당 없음). ⚠️ **`LAST`를 `c4c3431`로 옮기지 않았다** —
`ece7029`(M15) 기준을 유지하면 계수가 커져 게이트가 더 쉽게 통과할 뿐 방향이 뒤집히지
않고, `c4c3431` 기준으로도 A는 여유 있게 통과한다(M19 소비분만 빠진다).

**순서 점검(③)**: **역전 없음.** M19까지 발행 완료, 다음 차례는 M20이며 이번 점검의
ACTIVE 지정이 정확히 그 자리를 가리킨다. 대기열은 **M20 → M21 → M22 1차 → M23 →
M22 2차 → M24 1차 → M22 4차 → M25 1차 → M26 1차 → M26 2차** 열 칸. M22 3차는 리뷰①
통과 시 M22 2차와 M24 1차 사이에 끼어든다. 🔴 **누적 미발행은 10구간 유지**(M19 하나가
해소되고 M26 2차 하나가 새로 쌓여 상쇄). 격일 발행으로 20일 분량이며 그동안에도 개발은
계속 쌓인다 — **회차당 1밀스톤 원칙 유지 여부는 여전히 사람의 결정 사항.**

**교정(③)**: 🔴 **이번엔 ACTIVE 지정을 덮어썼다** — 08-09 이후 세 차례 점검이 "소재
미소비이므로 유효"라며 보존해 온 M19 지정이 **실은 이미 소비된 것**이었기 때문이다.
M19 블록은 `CONSUMED(2026-08-10)`로 닫고 발행 URL·제목·유실 경위를 기입했으며, 그
아래에 원 지정 원문(08-08 게시팀 작성분)과 08-09~08-11의 추가 기입을 **전부 보존**했다.
새 ACTIVE(M20)에는 커밋 범위 전체(`4fce684d`~`dce43faa`·`40b44176`, W1~W13)와 1차 소스,
**M19 중복 금지 주의**(`1e572dd0`이 스스로 "M19 독점 해제의 완결"이라 적고 있어 겹치기
쉽다), 갱신된 대기열을 적어 두었다.

**품질 스팟 체크(④)**: ✅ **라이브 열람 — 통과.** 이번 점검은 최신 발행 글이 #19(M15)가
아니라 **#20(M19, 08-10)**임을 발견한 바로 그 열람이 스팟 체크를 겸했다. 대조 2건 일치:
① 제목의 "45개 파일 2,260줄"이 M19 철거 실측(`a9d8337`~`c4c3431`, 45파일 −2,260줄)과
일치. ② 도입부가 밝힌 폐기 이유("재정 정책이 게임의 주된 개입 수단이라 믿었다")가 memory
`project_m19_decurrency`의 정체성 이탈 진단과 일치. ⚠️ **다만 이 글은 승인 경로·반려
횟수를 확인할 수 없다**(`.staging/` 산출물이 상태 커밋과 함께 유실) — 사실관계 심층
대조는 다음 점검이 이어서 할 것.

**파이프라인 건강(⑤)**: 🔴 **경보 1건 OPEN(내용 교체) + 1건 CLOSED(오진 정정)**.
⑴ 08-11이 세운 🔴 "짝수일 무발동"은 **오진으로 CLOSED** 처리하고 인용 금지 경고를 달았다.
⑵ 그 자리에 🔴 **신규 = "발행은 됐는데 상태 커밋이 유실됐다"**를 세웠다 — 진짜 고장난
칸은 Step 8 하나다. ⑶ 08-08 반려 3회 🟡는 M19가 실제 발행되며 사실상 해소됐다.
403 흔적 없음. 🔴 **미해결 부수 결함 1건 그대로**: `scripts/blogger-client.js:118`
`parseArgs`의 빈 문자열 오인(08-09부터 4회 연속 이월 — 이 점검의 범위 밖).
🔴 **사람 몫 2건**: ⑴ **`blog-publisher.md` Step 8에 `git push` 성공 확인 추가**(이번
사고의 근본 재발 방지책, 다음 로컬 세션 몫) ⑵ Blogger 초안 `9034137679451863013`(M19,
DRAFT) 삭제 — **이제 같은 소재의 공개본이 실제로 존재하므로 전보다 시급하다.**

**오늘(08-12) 13:03 예측**: **짝수일 + 게이트 A·B·C·D 전부 통과 → 발행 대상일이며,
소재는 이번 점검이 지정한 M20 "직업 전면 재점검"**(`4fce684d`~`dce43faa`, W1~W13 완결,
Play 리뷰가 명세를 두 번 고친 기록 포함). **교정 전이었다면 M19 중복 발행이었다.**
⚠️ 상태 커밋 유실이 08-10 한 번뿐인 일과성인지 반복되는지는 **오늘 회차가 관측점**이다 —
발행 후 `chore(blog)` 커밋이 origin/main에 남는지 다음 점검이 확인할 것.

### 이전 사전점검 이력 (2026-08-11, 기록 보존용)

> ⚠️ **아래 08-11 기록의 "최대 발견 ①"(08-10 짝수일 무발동)은 오진이다.** 08-10 회차는
> 발동했고 발행에도 성공했다 — 유실된 것은 상태 커밋뿐이다(위 08-12 기록 참조).
> "무발동"·"짝수일 규칙이 깨졌다"는 서술을 근거로 인용하지 마라.

**마지막 사전점검**: 2026-08-11 (자동 스케줄, **15:44 KST 실행** — 목표 12:03 대비 약 3.7시간
늦음. 07-19·07-22·07-28·08-02·08-04·08-09·08-10에 이어 **8회째** "점검이 회차 시각을 넘겨
시작"하는 패턴이며, 이번이 그중 가장 크게 늦었다). **오늘(11일)은 홀수일** — 실측 cron
(`3 4 2-30/2 * *`)이 짝수일만 발동하므로 **오늘 13:03 발행 자체가 없다**는 것이 우세 예측이며,
실측으로도 오늘 날짜 `chore(blog)` 커밋 0건이다.

🔴 **이번 점검의 최대 발견 ① — 08-10(짝수일)이 게이트 전 항목 통과였는데 무발행이었다.
"짝수일이면 돈다"는 실측 패턴이 처음으로 깨졌다.** `git log --all --grep="chore(blog)"`로
전수 확인한 결과 파이프라인 자기 커밋의 최신은 여전히 **`dc527850`(08-08, DRAFTED)**이고,
그 뒤로 08-09(홀수, 정상 무발행)·**08-10(짝수, 발동했어야 함)**·08-11(오늘, 홀수) 사흘간
`chore(blog)` 커밋이 하나도 없다. 08-10 점검은 그날을 "발행 대상일, 소재는 M19"로
예측했고 게이트도 A·B·C·D 전부 통과 상태였는데 **run 자체의 흔적이 없다**. 08-08 회차는
반려 3회로 DRAFTED가 됐을 때조차 `chore(blog): auto-run state update` 커밋을 남겼으므로,
**"돌았는데 조용히 실패"가 아니라 "아예 돌지 않았다"에 가깝다**(cron 미발동, 또는 에이전트
호출 전 단계에서 중단). 07-25 이후 짝수일 8회 연속 적중하던 규칙이 깨진 첫 사례라
`state/blog_pipeline_alerts.md`에 **신규 경보로 기록**했다. 🔴 **다음 짝수일(08-12)이
관측점이다** — 거기서도 무발행이면 원격 routine 자체가 멈춘 것이므로 사람이
claude.ai routines 등록 상태를 직접 확인해야 한다.

🔴 **최대 발견 ② — 로컬 미푸시가 39커밋 → 117커밋으로 3배 불었다.**
`git rev-list --left-right --count origin/main...HEAD` = `0 117`. origin/main 최신은
**`dfd6d6bf`(08-10 20:01, 어제 이 점검이 얹은 커밋 그 자체)**이고, 그 뒤 **M22 4차 개간
전 구간·M24 1차 W7R·M25 1차·M26 1차·M26 2차 W1~W7·영상 트랙(lf02 v2·ep14s·ep16s·
채널 브랜드 문서)이 전부 로컬에만 있다.** 즉 원격 파이프라인이 보는 세계는 **08-09
18:53(`8ee09071`)에서 멈춰 있다.** 다만 ACTIVE 지정 소재(M19, `c4c3431`)는 origin/main에
한참 전부터 있으므로 **차기 회차 소재에는 영향이 없다** — 영향은 대기열 후미뿐이다.
push는 게임 트랙 소관이라 이 점검이 대신하지 않았다(이 점검이 손대는 파일은
`tools/blog-automation/`뿐이며, 아래 커밋은 08-10과 같은 방식으로 origin/main 위에 얹어
그 117커밋을 끌고 가지 않는다).

**커버리지 갱신(①) — 신규 4행(전부 완결, 전부 미푸시).** memory `MEMORY.md` START HERE와
커밋 로그를 직접 대조한 결과, 08-10 점검 이후 밀스톤 **4개가 새로 완결**됐다:
- **M24 1차 W7R — 완결.** `925832da`(우회 판정을 "사거리"에서 "변"으로, 08-10 16:56) ~
  `99062b83`(Play 관측 완료 — 우회가 서쪽 망루를 피해 동쪽으로 돌았다, 17:38). 08-10
  점검이 "진행 중"으로 적었던 M24 1차가 **W7R까지 붙어 종료**됐다 → 아래 표의 M24 행을
  갱신(여전히 미푸시).
- **M22 4차 "개간(clearing)" — 완결.** `a5ff2bf1`(명세, 08-10 17:54) ~ W1~W6
  (`e0e6c618`)·W7 몸짓(`db3c4a3c`)·2차 Play(`8032a474`)·**W8 취소·붉은 고스트
  (`a1279e84`, 18:47)**. memory `project_clearing_axis`가 "✅ W1~W8 종료(게이트 452)"로
  명시. **울타리 구멍을 닫은 축** → 신규 행.
- **M25 1차 "성격 서술(라벨 → 문장)" — 완결.** `85c7db39`(명세, 08-10 13:54) ~ W1~W5
  (`61c7cb18`·`03d0c17d`·`4fc0fb3f`·`bb5d5c7d`·`5ce596b0`) ~ `2b6920bb`(완주 마감,
  14:25) + `615c80de`(S2 Play 판정 통과, 15:06). Project Sid 분석에서 갈라져 나온
  직업 획득형 축의 1차 → 신규 행.
- **M26 1차 "지형 축(땅이 생긴다)" — 완결.** `26a62909`(명세, 08-10 19:59) ~
  `c77730b7`(W1 A\* 경로탐색 + 비용 창구) ~ `4b74d0f5`(W2~W7 지형·시드·느린 땅·색·
  리스폰) ~ `925b9891`(1차 종료 — 사용자 Play 확인, 21:39). **`ADR_경로탐색_확장경계`
  트리거 A를 실제로 발동시킨 축** → 신규 행.

🟡 **표에 올리지 않는 것 2건**: **M26 2차**는 W1~W7까지 구현됐으나(`738190af`~`d035d48a`,
08-10 22:20~08-11 15:32) memory START HERE가 "다음 = W5R Play 확인 → 그 뒤 W6"으로
**진행 중**을 명시 — 완결 자격 미충족. **M22 3차(망루·함정)**는 08-09 이후 진척이 없고
리뷰①(관전 7건)이 여전히 미완이다. ⚠️ **주의 — M22 4차가 3차를 추월했다**: 4차는 완결,
3차는 미완이라 완결 순서와 차수 번호가 어긋났다. 대기열은 완결 순서를 따르므로 3차가
리뷰①을 통과하기 전까지 4차가 앞선다(둘 다 대기열 후미라 당장의 순서 역전은 아니다).

**게이트 판정(②, Step 0 재계산 — 08-06 개정 계산식, `LAST=ece7029`, origin/main 기준)**:
`game_commits=161` `big=22` `days=10` → **기준 A(≥10)·B·C 전부 통과**. `blog_next_
material_priority.md` 최상단이 `STATUS: ACTIVE`라 **기준 D도 무조건 통과**. `publish_status`도
`PUBLISHED`로 정상(무결성 가드 해당 없음). ⚠️ **게임 커밋 수가 08-10과 똑같이 161인 것은
계산 착오가 아니다** — 그 사이 origin/main에 들어온 커밋이 08-10 점검 자신의 문서 커밋
(`dfd6d6bf`) 하나뿐이고 그것은 `tools/` 제외 규칙에 걸려 계수되지 않기 때문이다
(`big`이 25→22로 줄어든 것은 08-10 점검이 로컬 HEAD 기준 값을 옮겨 적은 오기로 보인다 —
origin/main 기준 실측은 22다). 로컬 HEAD 기준으로는 **231개**이나, **원격이 읽는 값은 161**이다.

**순서 점검(③)**: **역전 없음.** M15까지 발행 완료, 다음 차례는 M16~M18을 흡수한 M19이며
ACTIVE 지정도 그 자리를 가리킨다. 대기열은 **M19 → M20 → M21 → M22 1차 → M23 → M22 2차 →
M24 1차 → M22 4차 → M25 1차 → M26 1차**이며, M22 3차는 리뷰① 통과 시 M22 2차와 M24
1차 사이에 끼어든다. 08-10 대비 **또 순증가(4구간)** — 개발이 발행을 앞지르는 속도가
오히려 빨라졌다. 🔴 **누적 미발행이 10구간에 이르렀다**(08-09 5 → 08-10 6 → 오늘 10).
격일 발행으로는 20일이 걸리는 분량이며, 그동안에도 개발은 계속 쌓인다 — **회차당 1밀스톤
원칙을 유지할지, 묶음 회차를 허용할지는 사람의 결정 사항**이라 여기서는 관측만 기록한다.

**교정(③)**: **ACTIVE 지정은 덮어쓰지 않았다**(M19, 소재 미소비로 여전히 유효하며 08-09
점검이 붙인 "승계할 판정·재사용 원고·지뢰 3종" 기입도 그대로 살아 있다). 대신 그 블록의
🟡 순서 참고 절에 **대기열 갱신 한 줄(M24 1차·M22 4차·M25 1차·M26 1차 편입 + 08-10
무발행 사실)**만 덧붙였다 — 08-10 기입이 적어 둔 대기열이 M22 2차에서 끝나 있어 그대로
두면 다음 게시팀이 네 구간을 빠뜨린다.

**품질 스팟 체크(④)**: ✅ **라이브 URL을 직접 열어 재대조했다 — 통과.** 최신 발행 글은
여전히 #19(M15 연대기, 08-08)이고 그 이후 새 발행이 없다. 08-06·08-10 점검은 "같은 글
재열람은 낭비"라며 건너뛰었으나 **연속 2회 생략은 과하다고 판단해 이번엔 직접 열었다**.
대조 3건 전부 일치: ① 결함 2건이 *"같은 판이 목록에 두 줄로 떠 있었습니다"* ·
*"전멸 화면 위에서 C를 누르면 글자가 두 겹으로 겹쳐 읽혔습니다"*로 서술돼 커밋
`3b6cfebe` 메시지("연대기 패널 결함 2건 — 현재 판 중복 행·전멸 화면 겹침")와 항목·순서
일치. ② *"두 건 다 20시 52분 커밋 하나로 닫았습니다"*가 `3b6cfebe`의 실제 커밋 시각
(2026-07-31 **20:52**:38 +0900)과 일치. ③ 제목이 상태 파일 `title` 필드와 일치. 말미
예고("그 축이 어떤 브레인스토밍을 통과했는지…")도 다음 회차(M19)로 사슬이 이어진다.

**파이프라인 건강(⑤)**: 🔴 **경보 2건 OPEN** — ⑴ 기존 🟡(08-08 반려 3회 → DRAFTED,
M19 소재) 유지, ⑵ **신규 🔴 = 위 08-10 짝수일 무발동**(이번 점검이 기록). 403 흔적은
없다(애초에 run이 없었다). 🔴 **미해결 부수 결함 1건도 그대로**: `scripts/blogger-client.js:118`
의 `parseArgs`가 `if (next && !next.startsWith('--'))`로 판정해 **빈 문자열 `--labels ""`를
falsy로 흘려보내 불리언 `true`로 오인**하는 건 — 08-09·08-10 점검이 "다음 로컬 세션 몫"으로
넘긴 뒤 코드가 그대로다(이번 점검에서 해당 줄을 직접 열어 미수정 확인. 수정 자체는 이
점검의 범위 밖). 🔴 **사람 몫 2건**: ⑴ Blogger 초안 `9034137679451863013`(M19, DRAFT)
삭제 — 이 소재를 정식 발행하면 초안과 공개본이 공존한다.
⑵ **원격 routine 등록 상태 확인** — 08-12(짝수일)에도 무발행이면 필수다.

**오늘(08-11) 13:03 예측**: **홀수일이라 cron 미발동 — 발행 없음**(실측: 오늘 날짜
`chore(blog)` 커밋 0건). 다음 실제 회차는 **08-12(짝수일)**이며, 게이트가 전부 통과
상태라 정상 발동한다면 소재는 **M19 "화폐 전면 철거와 실물 마을"**(M16~M18 배경 압축,
`verify_at c4c3431`, 재사용 원고 `state/incidents/2026-08-08/02_draft.md`)이다.
⚠️ 다만 **08-10 무발동 때문에 "발동한다면"의 확신도가 이전 회차들보다 낮다** — 08-12는
소재 예측보다 **routine 생존 여부 관측**이 더 중요한 회차다.

### 이전 사전점검 이력 (2026-08-10, 기록 보존용)

**마지막 사전점검**: 2026-08-10 (자동 스케줄, **13:29 KST 실행** — 목표 12:03 대비 약 1.4시간
늦음, 즉 오늘 13:03 발행 시각을 이미 지난 뒤 시작. 07-19·07-22·07-28·08-02·08-04·08-09와
같은 "점검이 회차 시각을 넘겨 시작"하는 패턴). **오늘(10일)은 짝수일 — 실측 cron
(`3 4 2-30/2 * *`) 발동 대상일이다.** 점검 시작 시점까지 origin/main에 오늘 날짜
`chore(blog)` 커밋 없음 — **아직 미발동이거나 진행 중**이다(08-08 원격 run이 스케줄보다
4시간 반 늦게 돈 선례가 있어 "오늘 발행 없음"으로 단정하지 않는다).

🔴 **이번 점검의 최대 발견 — 로컬 main이 origin/main보다 39커밋 앞서 있다.**
`git rev-list --left-right --count origin/main...HEAD` = `0 39`. origin/main 최신 커밋은
`8ee09071`(08-09 18:53, M22 3차 늑대 그림 불합격 기록)이고, 그 뒤 **M22 3차 W5d·W5e(그림
교체·깊이 정렬)·색 원 3종 교체·M24 1차 W1~W10 전 구간·ep13s 3편·lf02 롱폼이 전부 로컬에만
있다.** `git pull origin main`은 `Already up to date`(로컬이 origin의 후손이라 당길 것이
없음). **원격 파이프라인은 origin/main만 읽으므로 오늘 회차가 M24를 볼 수 없다** — 다만
ACTIVE 지정 소재(M19, `c4c3431`)는 origin/main에 한참 전부터 있으므로 **오늘 회차에는
영향이 없다.** 영향은 대기열 후미(M24 편입 시점)뿐이다. push는 게임 트랙 소관이라 이
점검이 대신하지 않았다(이 점검이 손대는 파일은 `tools/blog-automation/`뿐이며, 아래
커밋은 origin/main 위에 얹어 그 39커밋을 끌고 가지 않는 방식으로 push했다).

**커버리지 갱신(①) — 신규 1행(완결) + 진행 중 2건 기입.** memory `MEMORY.md` START HERE와
커밋 로그를 직접 대조한 결과:
- **M22 2차 "문 고도화" — 완결.** 08-09 점검이 "W1·W2만 끝난 진행 중"으로 표에서 뺐던 그
  밀스톤이 같은 날 오후에 완주했다: `c978cf51`(명세) ~ W3 문 오토타일(`5af025ff`)·W4 자원
  HUD(`0b5d7e7b`) ~ **`4a6fae80` "M22 2차 리뷰① 통과 기록 — 밀스톤 완료(사용자 Play 확인,
  게이트 381/381)"**(08-09 15:05). **완결 → 아래 표의 "진행 중" 행을 정식 미발행 행으로 승격.**
- **M22 3차 "망루·함정" — 진행 중, 표에 올리지 않음.** 명세 `734ea272`(08-09 15:33) ~
  W1~W5e까지 구현됐고 망루·함정 그림도 사용자 확인을 받았으나, memory START HERE가
  **"그림 외 잔여 = 리뷰①(관전 7건)"**으로 미완료를 명시한다 — 과거 회차들이 반복 적용한
  "밀스톤 완결" 자격 게이트 미충족.
- **M24 1차 "종족 침공 축" — 완주했으나 origin/main에 없음.** `42039a7d`(명세, 08-10 02:34)
  ~ `2d927692`(W10 게이트 마감)·`8b3df240`(완주 마감, 08-10 12:12). 늑대·곰을 고블린·오크·
  기사단·타락천사로 갈아치우고 게임일 밴드 3단을 압력 예산제로 대체한 큰 축이다. **위
  divergence 때문에 원격에서는 존재하지 않는 커밋**이라 이번 표에는 "미푸시" 꼬리표를 달아
  기록만 해 둔다.

**게이트 판정(②, Step 0 재계산 — 08-06 개정 계산식, `LAST=ece7029`, origin/main 기준)**:
`game_commits=161` `big=25` `days=2` → **기준 A(≥10) 통과**(B·C도 여유). `blog_next_
material_priority.md` 최상단이 `STATUS: ACTIVE`라 **기준 D도 무조건 통과**. `publish_status`도
`PUBLISHED`로 정상(무결성 가드 해당 없음). 참고로 로컬 HEAD 기준으로는 182개다 —
**원격이 읽는 값은 161** 쪽이다.

**순서 점검(③)**: **역전 없음.** M15까지 발행 완료, 다음 차례는 M16~M18을 흡수한 M19이며
ACTIVE 지정도 그 자리를 가리킨다. 대기열은 **M19 → M20 → M21 → M22 1차 → M23 → M22 2차**
(신규 편입, M23보다 완결이 늦으므로 뒤에 붙는다) → 그 뒤로 M22 3차·M24 1차가 각각
리뷰①·push를 마치면 합류한다. 08-09 대비 **또 순증가** — 개발이 발행을 계속 앞지른다.

**교정(③)**: **ACTIVE 지정은 덮어쓰지 않았다**(M19, 소재 미소비로 여전히 유효하며 08-09
점검이 붙인 "승계할 판정·재사용 원고·지뢰 3종" 기입도 그대로 살아 있다). 대신 그 블록의
🟡 순서 참고 절에 **대기열 갱신 한 줄(M22 2차 편입)**만 덧붙였다 — 08-09 기입이 적어 둔
대기열이 M23에서 끝나 있어 그대로 두면 다음 게시팀이 M22 2차를 빠뜨린다.

**품질 스팟 체크(④)**: **재검증 대상 없음 — 유효.** 최신 발행 글은 여전히 #19(M15 연대기,
08-08)이고 그 이후 새 발행이 없다(`blog_last_published_commit.md` 최신 블록 = M15,
`publish_status: PUBLISHED`, 오늘 날짜 `chore(blog)` 커밋 0건). #19는 **08-09 점검이 이미
라이브 URL을 직접 열어 핵심 사실 3건을 커밋·memory와 대조해 통과 판정**했고 그 뒤 글이
바뀔 사건이 없었으므로, 08-06 점검과 같은 선례에 따라 이번엔 라이브 재열람을 하지 않았다
(같은 내용을 다시 읽는 낭비 회피). **이번 점검이 새로 라이브 대조를 수행한 것은 아니다.**

**파이프라인 건강(⑤)**: 🟡 **경보 1건 OPEN 유지**(08-08 반려 3회 → DRAFTED, M19 소재).
새 경보 없음, 403 흔적 없음, 08-09~08-10 사이 새 run 자체가 없었다(홀수일 + 오늘 미발동).
🔴 **미해결 부수 결함 1건도 그대로**: `scripts/blogger-client.js`의 `parseArgs`가
`--labels ""`를 불리언 `true`로 오인하는 건 — 08-09 점검이 "다음 로컬 세션 몫"으로 넘긴 뒤
아직 손대지 않았다(이 점검의 범위 밖). 🔴 **사람 몫 1건도 그대로**: Blogger 초안
`9034137679451863013`(M19, DRAFT) — 이 소재를 오늘 정식 발행하면 초안과 공개본이 공존한다.

**오늘(08-10) 13:03 예측**: **짝수일 + 게이트 A·D 전부 통과 → 발행 대상일이며, 발행된다면
소재는 M19 "화폐 전면 철거와 실물 마을"**(M16~M18 배경 압축, `verify_at c4c3431`,
재사용 원고 `state/incidents/2026-08-08/02_draft.md`). 이 점검이 목표 시각을 넘겨 시작된
탓에 실제 발동 여부는 지금 확인 불가 — 다음 점검이 결과를 확인할 것.

### 이전 사전점검 이력 (2026-08-09, 기록 보존용)

**마지막 사전점검**: 2026-08-09 (자동 스케줄, **13:46 KST 실행** — 목표 12:03 대비 약 1.7시간
늦음, 즉 오늘 13:03 발행 시각을 이미 지난 뒤 시작. 07-19·07-22·07-28·08-02·08-04와 같은
"점검이 회차 시각을 넘겨 시작"하는 패턴). `git pull origin main` 결과 `Already up to date`
— origin/main 최신 커밋은 `f486347d`(롱폼 lf01 썸네일 확정, 영상 트랙). **오늘(9일)은
홀수일** — 실측 "짝수일만 발동"(cron `3 4 2-30/2 * *`) 패턴대로면 발행 없음이며, 실제로
`git log --since="2026-08-09 00:00" | grep chore(blog)` = **0건**으로 **무발행 확정**.
⚠️ 08-08(짝수일)에는 사전점검 세션이 돌지 않아 이 파일에 08-08 기록이 없다 — 이번 점검이
08-08 두 사건을 소급 반영한다.

🔴 **08-08에 사건이 둘 있었다 — 발행 1건 + 반려 3회 DRAFT 1건.** 커밋 시각을 UTC로 정렬해
확인한 결과(`git log --format=%ad`):
1. **`dde331df` (07:23 UTC = 16:23 KST) — M15 "연대기" 정식 공개 발행.** 원격 auto-run이
   아니라 **로컬 수동 사이클**이다(`blog_last_published_commit.md`의 `cycle_date` 주석이
   "로컬 수동 사이클"로 명시). 마스터 Step 6 2차 APPROVED·반려 0회 정상 승인 경로,
   `blogger_post_id` `1028397470425467506`, `latest_commit`이 `ece7029`로 갱신됨.
   대조표 **#19가 이미 게시팀 Step 8에 의해 정확히 기입돼 있었다** — 이번 점검은 표
   갱신이 아니라 **검증만** 했다(누락 없음).
2. **`dc527850` (08:37 UTC = 17:37 KST) — 원격 auto-run이 M19 소재로 반려 3회 → DRAFTED.**
   ⚠️ 이건 스케줄 시각(04:03 UTC)보다 **4시간 반 늦게** 돈 실행이다. 기획팀이 ACTIVE
   지정(M16)을 브레인스토밍 게이트에서 **재판단해 "M19 단일 밀스톤 + M16~M18 배경 압축"으로
   재구성**했고(이건 ACTIVE 블록이 명시적으로 위임한 판단이므로 **일탈이 아니라 정상
   동작**), 집필까지 갔으나 Step 3 검수 2회 + Step 4 마스터 1회 = 반려 3회로 draft 강등.
   **draft는 소재 소비가 아니므로 `latest_commit`·ACTIVE 지정 모두 갱신되지 않았고,
   무결성 가드도 정상**(`publish_status`가 `PUBLISHED`(M15 회차)로 남아 있어 07-28에
   신설된 "draft가 latest_commit을 갱신함" 위반에 해당하지 않는다 — 지시서를 정확히 지킴).

**커버리지 갱신(①) — 신규 3행 추가.** memory `MEMORY.md` START HERE와 커밋 로그를 직접
대조한 결과, 08-07 점검 이후 **밀스톤 3개가 새로 완결**됐다(전부 사용자 Play 리뷰① 통과
확인, `9627b7db` 커밋이 근거):
- **M21 "격퇴 축"** — `99c3c416`(명세, 08-05 15:19) ~ `b64b3087`(W10 게이트 마감,
  08-08 19:25). 08-07 점검이 "W1·W2만 끝난 진행 중"으로 판정해 표에 올리지 않았던 그
  밀스톤이 하루 만에 W2R~W10까지 완주했다. **완결 → 신규 행.**
- **M22 "방어 건설 1차"** — `7d5e1e0c`(명세, 08-08 20:28) ~ W1~W8 + `9627b7db`
  (리뷰① 통과, 08-09 01:34). **완결 → 신규 행.**
- **M23 "방어물·행동 비주얼"** — `9b7ae7c2`(명세, 08-09 01:04) ~ W1~W3 + `9627b7db`.
  **완결 → 신규 행.**

🟡 **M22 2차(문 고도화)는 표에 올리지 않는다.** `c978cf51`(명세, 08-09 01:58) ~
`1d53b3ea`(W2, 08-09 02:12)로 **W1·W2만 끝난 진행 중**이며 memory START HERE가 "다음 =
M22 2차 리뷰① (사용자 Play)"로 대기 상태를 명시한다 — 과거 회차들이 반복 적용한 "밀스톤
완결" 자격 게이트 미충족. 다음 사전점검이 리뷰① 통과를 확인하면 그때 추가한다.

**게이트 판정(②, Step 0 재계산 — 08-06 개정 계산식, `LAST=ece7029`)**: `game_commits=147`
`big=19` `days=8` → **기준 A(≥10)·B·C 전부 통과**. `blog_next_material_priority.md`
최상단이 `STATUS: ACTIVE`라 **기준 D도 무조건 통과**. `publish_status`도 `PUBLISHED`로
정상. **게이트만 보면 발행 확정이나, 홀수일이라 cron 자체가 오늘 발동하지 않았다** —
위에서 커밋 0건으로 실측 확인됨. 다음 실제 회차는 **08-10(짝수일)**.

🔴 **교정함 — ACTIVE 블록이 "이미 내려진 판단"을 다시 내리라고 지시하고 있었다.**
ACTIVE 블록(2026-08-08 게시팀 Step 8 작성)은 M16을 가리키며 "M16이 소재 자격이 있는가 ·
M19와 묶을 것인가를 다음 기획팀이 브레인스토밍 게이트에서 판정하라"고 세 질문을 남겨
뒀다. **그런데 그 판정은 같은 날 원격 auto-run 기획팀이 이미 내렸고, 그 답대로 11,586자
원고까지 썼다가 반려 3회로 멈춘 상태다.** 이 사실이 ACTIVE 블록에는 한 줄도 없고
`blog_pipeline_alerts.md`에만 있어서, **다음 회차 기획팀이 같은 브레인스토밍과 같은 집필을
통째로 반복하고 이미 밟은 지뢰 3개를 그대로 다시 밟을 상태였다.**
→ **ACTIVE 지정 자체는 덮어쓰지 않고**(소재 미소비이므로 유효), 블록 말미에
"🔴 추가 기입 (2026-08-09 사전점검)" 절을 **덧붙였다**: 승계할 판정 결과(M19 단일 +
M16~M18 배경, `verify_at c4c3431`) · 재사용할 원고(`state/incidents/2026-08-08/02_draft.md`,
반려 사유 반영 최신본) · **다시 밟지 말 지뢰 3종**(ⓐ브리프 커밋 시각 오기로 인과 역전
ⓑ내부 링크 404·시각 오차·H2 불일치 ⓒ`Docs/CLAUDE.md:97` 축자 아닌 인용·"마지막 커밋"
성과 오귀속) · 대기열 갱신(M19 → M20 → M21 → M22 1차 → M23).

🔴 **사람 몫 1건 — Blogger에 초안이 살아 있다.** post_id `9034137679451863013`(status:
DRAFT, `blog-editor` 미경유라 SEO 메타·라벨 없고 마크다운 표 2개가 파이프 문자 그대로
남음). 이 소재(M19)를 다음 회차에 정식 발행하면 **같은 글의 초안과 공개본이 공존**한다 —
2026-07-28 선례처럼 사용자가 직접 지워야 한다:
https://www.blogger.com/blog/posts/6014451945015572125?hl=ko

**순서 점검(③)**: **역전 없음.** M15까지 발행 완료, 다음 차례는 M16~M18을 흡수한 M19이며
ACTIVE 지정도 그 자리를 가리킨다. 미발행 구간은 M16~M23(M22 2차 제외) — 밀스톤 기준
**M19·M20·M21·M22 1차·M23 다섯 구간**(+M16~M18은 M19에 배경으로 흡수, +밀스톤 체인 밖
scene-video). 08-07 대비 M15 하나가 해소되고 M21·M22·M23 셋이 새로 쌓여 **순증가** —
개발이 발행을 다시 앞질렀다.

**품질 스팟 체크(④, 최신 발행 글 = #19 M15)**: 라이브 URL을 직접 열어 핵심 사실 3건을
커밋·memory와 대조했다 — ① 결함 2건이 "같은 판이 목록에 두 줄"·"전멸 화면 위에서 C를
누르면 글자가 두 겹으로 겹쳐"로 서술돼 커밋 `3b6cfeb` 메시지("연대기 패널 결함 2건 —
현재 판 중복 행·전멸 화면 겹침")와 항목·순서까지 일치. ② 글이 적은 "20시 52분 커밋"이
`3b6cfeb`의 실제 커밋 시각(20:52)과 정확히 일치. ③ "파일 분리(ADR-M15-1)"·"스키마는
필드 추가만 허용하고 버전 번호를 필수화"가 memory `project_m15_chronicle`의 "판별
아카이브(chronicle_archive.json, RunRecordStore와 파일 분리)"와 일치. 말미 예고("세션
로그에는 그다음 밀스톤 후보로 화폐와 상거래 축이 최유력으로 적혀 있습니다")도 **당시
세션 로그를 인용한 형식**이라 사후 폐기와 모순되지 않으며, 오히려 다음 회차(M19 화폐
철거)로 예고 사슬이 정확히 이어진다. **통과.**

**파이프라인 건강(⑤)**: 🟡 **경보 1건 OPEN**(위 08-08 반려 3회 → DRAFTED). 파이프라인
자체는 지시서대로 정상 종료했고(소재 미소비 보존·`.staging` 전체를
`state/incidents/2026-08-08/`에 스냅샷 보존·상태 파일 미갱신) 인프라 장애가 아니라
**원고 품질 반려**라 구조적 결함은 아니다. 기존 경보 3건은 CLOSED 유지. 403 흔적 없음.
🔴 다만 **부수 결함 1건이 미해결로 남아 있다**: `scripts/blogger-client.js`의 `parseArgs`가
`--labels ""`(빈 문자열)를 불리언 `true`로 오인해 `labels: ["true"]`를 실제로 게시했다
(최초 게시 post_id `3121106430114146540`에서 발생 → 즉시 DELETE 후 인자 생략으로 재게시).
당장은 우회했으나 근본 수정(빈 문자열 ↔ 플래그 부재 구분) 미완 — 다음 로컬 세션 몫.

**오늘(08-09) 13:03 예측**: 홀수일이라 cron 미발동 — **발행 없음(실측 확정)**. 다음 실제
회차는 **08-10(짝수일)**이며, 게이트가 이미 전부 통과 상태라 발행된다면 소재는
**M19 "화폐 전면 철거와 실물 마을"**(M16~M18 배경 압축, `verify_at c4c3431`)이다.

### 이전 사전점검 이력 (2026-08-07, 기록 보존용)

**마지막 사전점검**: 2026-08-07 (자동 스케줄, 목표 12:03 KST 근접 실행). `git pull` 결과
`Already up to date` — origin/main 최신 커밋은 `bb1ee00`(spec(goap) M21 §W2R 개정 명세).
**오늘(7일)은 홀수일** — 실측 "짝수일만 발동" 패턴대로면 **오늘 13:03 발행 자체가 없다**
(07-19·21·25·29·08-01·03·05와 같은 패턴).

**커버리지 갱신(①) — 🎉 08-06 예측이 적중, 표를 갱신함.** `state/blog_last_published_
commit.md` 확인 — **M14 "가을과 겨울"이 2026-08-06 정식 공개 발행됐다**(`latest_commit:
c4bc93e`, `cycle_date: 2026-08-06`, 마스터 Step 6 2차 APPROVED·반려 0회 정상 승인 경로,
`blogger_post_id: 8259513125902654061`, https://gamedevclaude.blogspot.com/2026/08/
unity-goap-claude-code-ai-3-day-44.html, 제목 "겨울 3번을 넘기고 Day 44에 쓰러졌다,
계절이 기회를 열고 성격이 판정하게 만들다"). 아래 대조표에 **#18로 신규 추가**하고, M14
행을 "미발행(ACTIVE)"에서 "발행 완료"로 갱신했다. `state/blog_next_material_priority.md`도
이미 정확히 갱신돼 있음을 확인 — M14 블록은 `STATUS: CONSUMED(2026-08-06)`으로 닫혔고,
게시팀 Step 8이 `deferred_milestones` 순서대로 **M15 "연대기"**를 새 `STATUS: ACTIVE`로
지정해 두었다(이 사전점검 이전에 이미 정확히 이루어짐 — 교정 불필요).

**게이트 판정(②, Step 0 재계산 — 🔴 2026-08-06 개정 계산식 사용, `LAST=c4bc93e` 기준)**:
`git log --oneline c4bc93e..HEAD -- . ':(exclude)tools/' ':(exclude)youtube-editor/'
':(exclude).claude/' ':(exclude)devlog/' ':(exclude)Docs/영상_*' ':(exclude)Docs/블로그_*'
| grep -cv "chore(blog)"` = **게임 커밋 99개**(대형 spec/refactor 14개 포함)·경과 7일 →
**기준 A(≥10)·B·C 전부 여유 있게 통과**. `blog_next_material_priority.md` 최상단이
`STATUS: ACTIVE`(M15)라 **기준 D도 무조건 통과**. `publish_status`도 `PUBLISHED`로 정상
(무결성 가드 해당 없음). **게이트만 보면 발행 확정이나, 홀수일이라 cron 자체가 오늘
발동하지 않을 전망** — 다음 실제 회차는 08-08(짝수일).

🔴 **미발행 구간 재스캔 — 새로 완결된 밀스톤 없음.** memory `MEMORY.md` START HERE와
커밋 로그(`bb1ee00`까지)를 직접 대조: **M21 "격퇴 축"은 여전히 표에 올릴 단계가 아니다**
— 08-06 이후 `fc95800`(피로 goal 탐색 폭발 수정)·`8066507`(Plant·GatherStone 절대목표
전환)·`b51c631`(밭 타깃 목적지 수정)·`cb0b732`(늑대 밭 곁 정지+파괴 반경)·`8024b56`
(위기철 곳간→요리 구멍)·`8010a4b`(ADR-M0-12 신설)·`bb1ee00`(**W2R 개정 명세 — 아직
구현 전, spec 커밋만**)까지 진행됐지만, memory 기준 W1·W2만 끝났고 W2 자체가 Play에서
뒤집혀 W2R로 재작성 대기 중인 **진행 중** 상태다(다음 세션 할 일 = W2R 구현). "밀스톤
완결" 자격 게이트를 아직 충족하지 않으므로 신규 행으로 올리지 않는다. M15~M20 나머지
미발행 행은 08-06 판단 그대로 변경 없음(M16·M18 "폐기·대체" 함정 경고도 유지, M20은
완료 상태 유지).

**순서 점검(③)**: 역전 없음. M15가 M14 다음으로 가장 오래된 미발행 밀스톤이며 ACTIVE
지정도 정확히 M15를 가리킨다. 미발행 구간은 M15~M20(+scene-video) 일곱 구간으로 줄었다
(M14 발행으로 하나 해소).

**품질 스팟 체크(④, 최신 발행 글 = #18 M14)**: 라이브 URL을 직접 열어 핵심 인용 3건을
`c4bc93e` devlog(`devlog/sessions/2026-07-31.md` "M14 실전 방치 완주 1판" 절)와 대조했다
— ① "겨울 3번을 넘기고 Day 44에 쓰러졌다·최대 8명"은 devlog 원문 "겨울 3번 넘기고 Day
44 전멸 · 최대 8명 — 신기록 줄 작동"과 정확히 일치. ② "치료사가 없는 판은 마지막에는
부상으로 죽어서 결국 마을이 끝이 났다"는 devlog의 회상 테스트 인용문과 자구까지 일치.
③ "게으름뱅이 둘은 둘 다 죽었습니다. 13%짜리 주사위가 안 터진 정상적인 판입니다"도
devlog "게으름뱅이 2/2 아사 = 13% 주사위 안 터진 정상 판"과 일치 — 글 앞부분의 "게으름뱅이
아무것도 안 심습니다"(W1 시절 확정 서술)와 뒤의 13% 확률 서술이 시간순 전개(확정사→
확률화, M14-W3 자체가 다루는 전환)로 자연스럽게 이어져 모순이 아님을 확인. **통과.**

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` — OPEN 경보 0건, 기존 3건
(M10 반려 3회·M12 반려 3회·07-16 REJECTED_3X) 모두 CLOSED 유지. 새 경보 없음. 직전
run(08-06, M14 발행)에 403·반려 흔적 없음. 건강.

**교정**: `blog_next_material_priority.md`는 이미 올바르므로(CONSUMED=M14, ACTIVE=M15,
순서 정확) 수정하지 않았다. 이 파일(BLOG_COVERAGE.md)의 대조표(#18 추가)·이력만 갱신한다.

**오늘(08-07) 13:03 예측**: 게이트 A·B·C·D 전부 통과이나 **홀수일이라 "짝수일만 발동"
실측 패턴으로는 발행 없음**이 우세 예측. 다음 실제 회차는 **08-08(짝수일)**이며, 게이트가
이미 전부 통과 상태(게임 커밋 99개·ACTIVE 지정)라 발행된다면 소재는 **M15 연대기**
(`1580dcf`~`ece7029`).

### 이전 사전점검 이력 (2026-08-06, 기록 보존용)

**마지막 사전점검**: 2026-08-06 (자동 스케줄, 목표 12:03 KST 근접 실행). `git pull` 결과
`Already up to date` — origin/main 최신 커밋은 `3f0f0f1`(scene-video ep07s 2편 분할).
**오늘(6일)은 짝수일** — 실측 "짝수일만 발동" 패턴대로면 **오늘 13:03 발행 대상일**.

**커버리지 갱신(①)**: `state/blog_last_published_commit.md` — 최신 발행은 여전히 #17
M13(`latest_commit: b105360`, 2026-08-04 공개). 08-05는 홀수일이라 예측대로 무발행
확정(`git log --since="2026-08-05 00:00" --until="2026-08-06 00:00"`에 `chore(blog)`
커밋 없음, 08-04 `67665c8` 이후 다음 `chore(blog)` 커밋이 아직 없음). 표 변경 없음.

**게이트 판정(②, Step 0 재계산, `LAST=b105360` 기준)**: `git rev-list --count
b105360..HEAD` = **275개**(전회 243개에서 32개 증가)·경과 2일 → **기준 A(≥10) 단독 통과**.
`blog_next_material_priority.md`가 `STATUS: ACTIVE`로 M14를 지정 중이라 **기준 D도
무조건 통과**. `publish_status`도 `PUBLISHED`로 정상. **오늘 13:03 발행 확정, 소재는
M14 가을과 겨울** — 짝수일이라 cron도 정상 트리거 대상.

🔴 **미발행 구간 재스캔 — M20이 08-05 이후 완전 종료로 갱신됐고, M21이 착수됐다.**
memory `MEMORY.md` START HERE와 커밋 로그를 직접 대조:
- **M20 "직업 전면 재점검"** — 08-05 사전점검 시점엔 "구현 완료·Play 재관측(P10~P12)
  대기 중"으로 표에 남아 있었으나, 그 뒤(`dce43fa`·`40e1135`... 정정: `dce43fa` "M20 종료
  판정 — 게이트 3차 green + P10~P14 사용자 확인", `40b4417` "M20 종료 블록") 로 **Play
  리뷰 4회 + 게이트 3차 green + P10~P14 사용자 확인까지 완결**됐다. 아래 대조표의 M20
  행 상태를 "대기(Play 재관측 대기)"에서 "완료"로 갱신했다.
- **M21 "격퇴 축"은 아직 표에 올릴 단계가 아니다** — `99c3c41`(브레인스토밍+명세, 08-05)
  이후 `254f271`(M21-W1 주민 체력=생존 단일 척도)·`2e46644`(W1 DoD 기입)까지 이미
  구현이 시작됐지만, memory 기준 W1~W10 중 W1 하나만 끝난 **진행 중** 상태다. "밀스톤
  완결" 자격 게이트(과거 회차들이 반복 적용한 기준)를 아직 충족하지 않으므로 표에
  신규 행으로 올리지 않는다 — 다음 사전점검이 W10 완결/종료 선언을 확인하면 그때 추가.
- M14~M19 나머지 미발행 행은 08-05 판단 그대로 변경 없음(M16·M18 "폐기·대체" 함정
  경고도 유지).

**순서 점검(③)**: 역전 없음. M14가 M13 다음으로 가장 오래된 미발행 밀스톤이며 ACTIVE
지정도 정확히 M14를 가리킨다. 미발행 구간은 M14~M20(+scene-video) 여덟 구간 그대로
(M20 상태만 완료로 갱신, 구간 수 변화 없음).

**품질 스팟 체크(④)**: 최신 발행 글(#17, M13)은 08-05 사전점검이 이미 라이브 URL로
직접 사실관계 대조를 완료했다("게으름뱅이 둘 다 그냥 죽었어"→"굶어 죽는게 확인이 됐어"
전후 인용 확인, 6단계 항목 구성 확인). 그 이후 새 발행이 없으므로 재검증 대상 없음 —
유효.

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` — OPEN 경보 0건, 기존 3건
(M10 반려 3회·M12 반려 3회·07-16 REJECTED_3X) 모두 CLOSED 유지. 새 경보 없음. 직전
run(08-04, M13 발행)에 403·반려 흔적 없음. 건강.

**교정**: `blog_next_material_priority.md`는 이미 올바르므로(ACTIVE=M14, 순서 정확)
수정하지 않았다. 이 파일(BLOG_COVERAGE.md)의 대조표(M20 상태 갱신)·이력만 갱신한다.

**오늘(08-06) 13:03 예측**: 짝수일 + 게이트 A·D 전부 통과 → **발행 확정, 소재는 M14
가을과 겨울**(`9c352cc`~, 종료 커밋은 다음 기획팀이 직접 확정). M16·M18 폐기·대체 함정은
이번 회차와 무관.

### 이전 사전점검 이력 (2026-08-05, 기록 보존용)

**마지막 사전점검**: 2026-08-05 (자동 스케줄, 목표 12:03 KST 근접 실행). `git pull` 결과
`Already up to date` — origin/main 최신 커밋은 `3386bbe`(M20-W10 세션 서술). **오늘(5일)은
홀수일** — 실측 "짝수일만 발동" 패턴대로면 **오늘 13:03 발행 자체가 없다**(07-19·07-21·
07-25·07-29·08-01·08-03과 같은 패턴). 점검 시작 시점까지 origin/main에 오늘 날짜
`chore(blog)` 커밋 없음(예측과 일치).

🔴 **개발이 소재 재고를 크게 앞질렀다 — 표에 반영함.** `git rev-list --count b105360..HEAD`
= **243개**(직전 소재 소비 커밋 대비). 어제(08-04) M13 발행 이후 하루 만에 **M14~M20
일곱 밀스톤 전부가 이미 완결 상태**임을 memory·`devlog/INDEX.md`(⚠️ 이 파일은 여전히
"2026-07-27 기준·다음 할 일 = M13"으로 멈춰 있어 신뢰 불가 — 갱신은 이 점검 범위 밖의
`Assets/`·개발 세션 몫)와 커밋 로그로 확인했다. 아래 대조표에 M14~M20 신규 행을 추가하고,
이미 발행된 M12(#16)·M13(#17) 행을 미발행에서 발행 완료로 갱신했다.

🔴 **다음 기획팀이 반드시 알아야 할 함정 — M16·M18은 "완결"이 아니라 "폐기·대체".**
표의 舊 기록(08-02 작성)이 M16 "화폐 기반+직거래"를 완결 소재로 적어 두었으나, **2026-08-04
사용자 지시로 화폐 축(M16~M18) 전체가 폐기 결정됐고, M19가 그 철거를 실행**했다(`a9d8337`
~`c4c3431`, 45파일 −2,260줄, 지갑·세금·물가·발행 전부 제거). M18도 "조건 문법(W1)만 채택,
집값 물가 연동(W2~W5)은 대부분 폐기 대상"이다. **M16·M18을 그대로 "화폐 시스템 구현
완료" 소재로 집필하면 이미 게임에 없는 기능을 있는 것처럼 쓰는 사실 오류가 된다** — 다음
기획팀은 이 순서(M14→M15→M16→M17→M18)에 도달하면 반드시 M19 철거 결정(`memory
project_m19_decurrency`, `Docs/게임_전면_재검사_분석노트.md`)을 먼저 읽고, M16·M18은
"구현했다가 하루 뒤 정체성 이탈 판정으로 스스로 철거한" 서사로 재구성하거나(오히려
소재 가치 있음), 아니면 M19에 흡수해 한 회차로 묶을 것을 권고한다. 순서 자체는 바꾸지
않는다 — M14가 여전히 가장 오래된 미발행 밀스톤이므로 ACTIVE 유지가 옳다.

**게이트 판정(참고, 오늘은 홀수일이라 무의미)**: 미소비 커밋 243개·경과 1일 → 기준 A
(≥10) 단독 통과, 기준 D도 `blog_next_material_priority.md`가 `STATUS: ACTIVE`로 M14를
지정 중이라 무조건 통과. 다만 날짜가 짝수일 cron 범위 밖이라 오늘 발행 자체가 없다.

🔴 **차기 소재 재확인 — 변경 없음, ACTIVE(M14) 유지.** `blog_next_material_priority.md`는
2026-08-04 게시팀 Step 8 지정 그대로 M14 "가을과 겨울"(spec `9c352cc`)이며, `deferred_
milestones` 순서상 M13(방금 발행) 바로 다음이 M14이므로 정확하다 — 덮어쓰지 않았다.
브리프가 M14의 종료 커밋을 명시하지 않아 다음 기획팀이 직접 확정해야 한다고 남겨 뒀는데,
memory 기준 종료 커밋은 방치 완주 관측 `c4bc93e`(07-31)로 보인다 — 다음 기획팀 참고용
힌트로만 남기고 ACTIVE 블록 자체는 수정하지 않았다(직접 재확인 원칙 유지).

**순서는 역전되지 않았다.** M14가 M13 다음으로 가장 오래된 미발행 밀스톤이다. 미발행
구간은 M14~M20(+scene-video) 여덟 구간으로 늘었다(전회 다섯 구간에서 M13 발행으로 하나
줄고 M14~M20 신규 발견으로 일곱 늘어난 순증가).

**품질 스팟 체크(④)**: 최신 발행 글(#17, M13,
https://gamedevclaude.blogspot.com/2026/08/claude-code-ai-unity-goap.html)을 라이브로
직접 확인 — 6단계 항목(A 비석 이름·B 상태 알림 줄·C1 명부+회고·C2 사건 기록·D 계획·문턱·
C3 관계 보존) 구성과 "게으름뱅이 둘 다 그냥 죽었어" → "게으름뱅이는 굶어 죽는게 확인이
됐어" 전후 인용이 memory `project_m13_presentation`·커밋 서사와 일치. **통과.**

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` — OPEN 경보 0건, 기존 2건(M10
반려 3회·M12 반려 3회) 모두 CLOSED 유지. 새 경보 없음.

**교정**: `blog_next_material_priority.md`는 이미 올바르므로(ACTIVE=M14, 순서 정확)
수정하지 않았다. 이 파일(BLOG_COVERAGE.md)의 대조표·이력만 갱신한다.

**오늘(08-05) 13:03 예측**: 홀수일이라 발행 cron 자체가 트리거되지 않음 — **발행 없음**.
다음 실제 회차는 **08-06(짝수일)**이며, 게이트는 이미 전부 통과 상태(미소비 243개·
ACTIVE 지정)라 발행된다면 소재는 **M14 가을과 겨울**(`9c352cc`~). 위에 적은 M16·M18
함정은 이번 회차와 무관하지만 기획팀 인수인계 목적으로 지금 기록해 둔다.

### 이전 사전점검 이력 (2026-08-04, 기록 보존용)

**마지막 사전점검**: 2026-08-04 (자동 스케줄, 목표 12:03 KST 근접이나 **실제 세션 시작은
14:04 KST** — 목표 대비 약 2시간 늦음, 07-19·07-22·07-28·08-02와 이어지는 "점검이 회차
시각을 넘겨 시작"하는 패턴). `git fetch` 결과 origin/main 최신 커밋은 `47f8154`(PR #7
병합, scene-video ep05s 검수 3회차) — 게임 밀스톤 커밋 없음.

⚠️ **로컬 작업 트리와 origin/main이 12개 대 273개 커밋으로 갈라져 있다** — 로컬(main)에는
scene-video 명세 작업 커밋 11개 + blog-automation 결정 1건(`850cab0`, "소재 범위를 게임
개발로 못 박는다")이 아직 origin에 반영되지 않은 채 남아 있다. `git pull`을 시도하니
`tools/scene-video/check.mjs`·`devlog/sessions/2026-08-03.md` 등에서 merge 충돌이
났다 — 이 정리는 scene-video 파이프라인 소관(이 점검의 범위 밖: tools/blog-automation·
devlog만 다룬다)이라 충돌을 풀지 않고 `git merge --abort`로 되돌렸다. 대신 이 파일 갱신은
origin/main 위에 새 브랜치를 얹어 직접 push하는 방식으로 우회했다(로컬 main은 건드리지
않음). **`850cab0`(소재 범위 제한 규칙)이 origin의 `BLOG_COVERAGE.md`·`blog-planner.md`에
아직 없다는 뜻이지만, 실질 위험은 낮다** — `blog_next_material_priority.md`의 현재 ACTIVE
블록 자체가 이미 "다루지 말 것: tools/scene-video/"를 명시하고 있고, blog-planner.md도
ACTIVE 지정을 무조건 최우선으로 따르게 되어 있다(아래 확인). 다음 사람 세션이 scene-video
merge를 정리할 때 `850cab0`도 함께 반영할 것.

🎉 **08-03 예측이 부분 적중 — 짝/홀 판정 불일치가 이번에 해소됐다.** 08-03(월요일,
홀수일)은 "짝수일만 발동" 패턴과 `aigoap-blog-weekly-pipeline` 스킬의 "월/수/금" 등록
문구가 정면으로 갈린 첫 사례였다. 실측 결과, origin/main에 08-03 날짜의 `chore(blog)`
발행 커밋이 하나도 없다 — **무발행으로 확정.** 즉 "월/수/금" 문구는 틀렸거나 갱신되지
않은 것이고, **"짝수일만 발동"이 07-25~08-03 8회차 전부(짝수 4회 발행·홀수 4회 무발행)와
일치하는 실제 규칙임이 확인됐다.**

**게이트 판정 (Step 0 재계산, `LAST=c1d87e2` 기준, M12 발행 이후 — 변화 없음)**: 미소비
커밋 273개(scene-video 트랙 다수 포함)·spec/refactor 커밋 9개·경과 8일 → **기준
A(≥10)·B·C 전부 여유 있게 통과**, 게다가 **기준 D**도 `blog_next_material_priority.md`가
여전히 `STATUS: ACTIVE`로 M13을 지정 중이라 무조건 통과. `publish_status`도
`PUBLISHED`로 정상(무결성 가드 해당 없음).

✅ **오늘(4일)은 짝수 날짜 — 재확인된 실측 패턴대로면 발행 대상일이다.** 다만 이 점검
시작 시점(14:04 KST)까지 origin/main에 오늘 날짜 `chore(blog)` 커밋이 없다 — 목표
발행 시각(13:03)을 이미 지났으나, 07-19·07-22·07-28·08-02 선례처럼 **세션이 늦게
시작해 이 점검 이후 발행될 가능성**과 **오늘은 아예 발동하지 않았을 가능성**을 모두
열어 둔다. 발동한다면 소재는 여전히 **M13 사건과 흔적**(`d6a84bf`~`458e882`) — ACTIVE
블록 본문이 scene-video 트랙을 이미 명시적으로 배제해 두어 오선택 위험 없음.

🔴 **차기 소재 재확인 — 변경 없음, ACTIVE 유지.** `blog_next_material_priority.md`는
08-02 지정 그대로 M13이며, 08-03 점검이 이미 A~D 전 항목 구현 커밋 12개 +
`458e882`(M13 결산)까지 완결을 직접 확인했다. 이번 점검은 재확인만 하고 새로 파고들지
않았다(변화 없는 상태를 매번 재검증하는 낭비를 피함).

**순서는 역전되지 않았다** — M13이 M12 다음으로 가장 오래된 미발행 밀스톤이다. 미발행
구간은 여전히 M13~M17(+scene-video) 다섯 구간.

**품질 스팟 체크(④)**: 최신 발행 글(#16, M12,
https://gamedevclaude.blogspot.com/2026/08/unity-goap-claude-code-ai.html)을 라이브로
재확인 — 겁 성향 수치가 여전히 교정본대로(새침이 110·게으름뱅이/떠돌이 105·고집쟁이 95)
실려 있다. **통과.**

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` — OPEN 경보 0건, 최신 항목
(08-02 반려 3회)도 CLOSED 유지. 새 경보 없음.

**교정**: `blog_next_material_priority.md`는 이미 올바르므로(ACTIVE=M13, scene-video
명시 배제) 수정하지 않았다. 이 파일(BLOG_COVERAGE.md)만 갱신해 origin/main 위에 직접
push한다 — 위 갈라짐 사유로 로컬 main 기준이 아니라 origin/main(`47f8154`) 기준으로
작성했다.

**오늘(08-04) 13:03 예측**: 게이트 A·B·C·D 전부 통과. 짝수일 실측 패턴대로면 **발행
확정, 소재는 M13**. 이 점검이 목표 시각을 넘겨 시작된 탓에 이미 발동했는지는 이 시점에는
확인 불가 — 다음 점검이 실제 결과를 확인할 것.

### 이전 사전점검 이력 (2026-08-03, 기록 보존용)

**마지막 사전점검**: 2026-08-03 (자동 스케줄, 목표 12:03 KST 근접 실행). `git pull` 결과
`3240c07`(scene-video ep04s 노트 4종, 클라우드 루틴) 이후로 로컬이 최신 — 게임 밀스톤
커밋은 없음.

🎉 **08-02 예측이 적중했다 — 단 정상 경로는 아니었다.** 08-02 사전점검은 "게이트 A·B·C·D
전부 통과 → 발행 확정, 소재는 M12"로 예측했는데, 실제로는 원격 auto-run이 검수·마스터
재검증에서 **반려 3회에 도달해 DRAFTED로 종료**(반려 사유: 초안 74행 피신 우선순위 수치
"겁 최고 125/최저 85"가 에셋 정본과 불일치 — `Weight: 0.8` 미반영 산술 오류)됐다가, 같은
날 로컬 세션이 마스터 2차 판정서의 교정 지시(주석 정정 + 실재 성격 수치로 재작성)를
이행한 뒤 **수동으로 정식 발행**했다(`blogger_post_id` 4080593902932668278, 2026-08-02
05:29 UTC LIVE). 경위 전체는 `state/blog_pipeline_alerts.md`의 "🟢 CLOSED — 2026-08-02
반려 3회 도달" 항목에 상세 기록됨.

**게이트 판정 (Step 0 재계산, `LAST=c1d87e2` 기준, M12 발행 이후)**: 미소비 커밋 235개·
spec/refactor 커밋 9개·경과 7일 → **기준 A(≥10)·B·C 전부 여유 있게 통과**, 게다가
**기준 D**도 `state/blog_next_material_priority.md`가 `STATUS: ACTIVE`로 M13을 지정 중
이라 무조건 통과. `publish_status`도 `PUBLISHED`로 정상(무결성 가드 해당 없음).

⚠️ **오늘(3일)은 홀수 날짜 — 실측 트리거 패턴("짝수일만 발동")대로면 발행 없음.** 단,
`aigoap-blog-weekly-pipeline` 스킬의 등록 문구는 "월/수/금 13:00 KST"이고 오늘은
**월요일**이라 문구대로면 오히려 발동 대상이다 — 08-01 사전점검이 이미 이 등록 문구와
실측 요일 패턴의 불일치를 발견해 사용자에게 보고했고(이 스킬은 리포 밖 파일이라 편집
권한 밖), 이번 점검도 동일 판단을 유지한다: **두 해석이 정면으로 갈리는 첫 실측 사례**
(과거엔 짝/홀 판정과 요일 판정이 우연히 일치하는 날만 있었다). 만약 발동한다면 게이트
전부 통과이므로 결과는 아래 예측대로 M13 발행일 것이다.

🔴 **차기 소재 재확인 — ACTIVE 유지, M13 "표현층/사건과 흔적"이 구현까지 완결돼 있음을
직접 확인했다.** `state/blog_next_material_priority.md`는 2026-08-02 로컬 세션이 M12
발행 직후 M13으로 지정한 그대로다(기획팀 브리프 `excluded_notes`의 승계 — c1d87e2 이후
16:25~16:35의 M13 후속 커밋은 verify_at 밖이라 이번 회차 제외, 다음 회차로 넘김). 이번
점검은 그 지정이 "소재 자격 게이트"(브리프가 명시한 "데모 가능한 상태" 요건)를 충족하는지
직접 재검증했다:
- `git log c1d87e2..HEAD --oneline | grep m13` 확인 — spec `d6a84bf`(사건과 흔적 실행명세서)
  부터 비석 이름(A)·상태줄(B)·명부+회고(C1)·사건+정보줄(C2, Play 피드백 반영 3커밋)·관계
  보존(C3)·문턱/이유(D)까지 A~D 전 항목 구현 커밋 12개 + `458e882`(M13 결산 — "항목 6개
  완료·증분 실증·S9 회상 테스트 인계") 확인 — **명세뿐이 아니라 결산까지 완결**, 자격
  게이트 통과.
- `devlog/INDEX.md`(14행)의 "M13 = 사건과 흔적"·39행의 "(참고) 舊 정의 — M12 종료 시점의
  'M13 = 표현층'" 두 표기가 같은 밀스톤을 가리킴을 확인(구 명칭 표현층 → 확정 명칭 사건과
  흔적) — `blog_next_material_priority.md`의 "표현층" 소재명과 불일치가 아니라 동일
  밀스톤의 신구 명칭이었다.

**순서는 역전되지 않았다** — M13이 M12 다음으로 가장 오래된 미발행 밀스톤이다. 미발행
구간은 M13~M17(+scene-video) 다섯 구간(전회 여섯 구간에서 M12 발행으로 하나 해소).

**품질 스팟 체크(④)**: 최신 발행 글(#16, M12)을 **라이브 URL로 직접** 열어 확인했다 —
08-02 반려 사유였던 피신 우선순위 수치가 실제로 교정본대로 실려 있다: **새침이(겁 +30)
110·게으름뱅이/떠돌이(겁 0) 105·고집쟁이(겁 -60) 95**(반려의 원인이던 "125/85"도, 산술만
맞고 실재 성격이 없던 "121/89"도 아닌, 판정서 권장안대로 실재 6성격 값으로 정확히
교체됨). 성향 표(고집쟁이 근면+40·대비-30·사교-40·모험0·자존+90·겁-60 등)도 `<table>`로
정상 렌더링 확인. **통과.**

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` 확인 — OPEN 경보 0건. 08-02
반려 3회 항목은 위 해소 경위대로 CLOSED, 기존 2건도 CLOSED 유지. `GoalSO.cs` 주석 정합성
정정(해소 조건 (a))도 완료 기록 확인 — 게임 코드 쪽 리포 결함까지 정리된 상태.

**오늘(08-03) 13:03 예측**: 게이트 A·B·C·D 전부 통과이나 **홀수일이라 "짝수일만 발동"
실측 패턴으로는 발행 없음**이 우세 예측(단, "월/수/금" 등록 문구대로면 발동 — 위 참조).
발동한다면 소재는 **M13 표현층/사건과 흔적**(`d6a84bf`~`458e882`).

### 이전 사전점검 이력 (2026-08-02, 기록 보존용)

**마지막 사전점검**: 2026-08-02 (자동 스케줄이나 **세션 지연으로 13:40 KST 실행** — 목표
시각 12:03 KST 대비 약 1.5시간 늦음, 즉 오늘 13:03 발행 시각을 이미 지난 뒤에 이 점검이
시작됐다. 07-19·07-22·07-28과 같은 "점검이 회차 시각을 넘겨 시작"하는 패턴). `git pull`
결과 `ffb8eee`(scene-video ep03s 대본, 클라우드 루틴) 이후로 로컬이 최신. 오늘(2일)은
**짝수일**이라 격일 발행 cron이 정상 트리거 대상이나, 이 점검 시작 시점까지 `chore(blog)`
계열 커밋이 origin/main에 하나도 없다 — **오늘 13:03 auto-run이 아직 완료(또는 미착수)
상태로 보인다**(진행 중이거나 이 점검 직후 실행될 가능성 모두 있음). 이 점검은 발행을
트리거하지 않으므로 개입하지 않고, 게이트 판정만 아래에 남긴다.

**게이트 판정 (Step 0 재계산, `LAST=7cd9e46` 기준, M11 발행 이후)**: 미소비 커밋 265개·
spec/refactor 커밋 11개·경과 8일 → **기준 A(≥10)·B·C 전부 여유 있게 통과**, 게다가
**기준 D**도 `state/blog_next_material_priority.md`가 여전히 `STATUS: ACTIVE`로 M12를
지정 중이라 무조건 통과. `publish_status`도 `PUBLISHED`로 정상(무결성 가드 해당 없음).
**발행 확정 예측 — 소재는 M12 "성격 축 성향벡터"**(다음 판단 참조).

🔴 **차기 소재 재확인 — ACTIVE 유지, 단 뒤에 쌓인 미발행 구간이 또 늘었다.**
`state/blog_next_material_priority.md`는 여전히 `STATUS: ACTIVE`로 **M12 "성격 축
성향벡터"**를 정확히 지정 중이다(08-01 사전점검이 이미 확인, 오늘도 재확인 — M11(#15)
다음 순서로 올바르며 덮어쓸 필요 없음). `git log`로 직접 확인한 결과, 08-01 점검 이후
origin/main에 **M17 "촌장 금고와 재정 정책"** 밀스톤이 새로 완결 상태로 들어왔다:
- M17 "촌장 금고와 재정 정책": `de7506f`(spec, 08-01) ~ `57b8585`(마감 블록+M18 경계
  실사 기록, 08-02) — 24커밋, 촌장 금고·원천징수·세율·화폐 신뢰·물가 두 층·발행 한도까지
  전부 구현 완료(memory `project_m17_treasury` 교차 확인).

**순서는 역전되지 않았다** — M12가 여전히 가장 오래된 미발행 밀스톤으로 정확한 다음
차례다. 미발행 구간은 이제 M12~M17(+scene-video) 여섯 구간으로 늘었다 — 다음 몇 회차의
기획팀이 분량 큰 최근 트랙(특히 M17, 24커밋)에 끌려 순서를 건너뛰지 않도록 계속 주의가
필요하다. 참고로 `tools/blog-automation/`에 자체 기능 변경(표·화살표·콜아웃·막대 시각
표현 도입, `0e6d738`·`842b8c1`)도 있었으나 이는 파이프라인 내부 기능이라 게임 밀스톤
커버리지 대상이 아니다 — 표에 반영하지 않음.

**품질 스팟 체크(④)**: 최신 발행 글(#15, M11)은 07-30 사전점검이 이미 라이브 URL로
직접 사실관계 대조를 완료했다(`"새침이"`·`0.7일` 등 정확성 확인). 그 이후 새 발행이
없으므로 재검증 대상 없음 — 유효.

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` 확인 — OPEN 경보 0건, 기존
3건 전부 CLOSED 유지. 다만 위에 적었듯 오늘 auto-run의 완료 여부를 이 시점에서 확인할
수 없다(chore(blog) 커밋 미도착) — 경보로 등재할 결함은 아니고, 다음 점검이 결과를
확인할 사항으로 남긴다.

**오늘(08-02) 13:03 예측**: 게이트 A·B·C·D 전부 통과 → **발행 확정**(단, 이 점검이
13:03을 지나 시작돼 실제 결과는 사후 확인 필요). 예상 소재는 **M12 성격 축 성향벡터**.

### 이전 사전점검 이력 (2026-08-01, 기록 보존용)

**마지막 사전점검**: 2026-08-01 (자동 스케줄, 발행 1시간 전 정상 실행). `git pull` 결과
`Already up to date`. **오늘(1일)은 홀수일**(요일로는 토요일)이라 격일 발행 cron
(`3 4 2-30/2 * *`, 짝수일만 트리거)이 애초에 발동하지 않는다 — **오늘 13:03 발행
자체가 없음**(게이트 계산 불필요, 07-31·07-29·07-25 사전점검과 같은 패턴).

🔴 **주의 발견 — 스케줄 등록 문구와 실제 발행 요일 패턴이 어긋난다.**
`~/.claude/scheduled-tasks/aigoap-blog-weekly-pipeline/SKILL.md`의 frontmatter
description은 "주 3회(월/수/금 13:00 KST)"라고 적혀 있으나, 실제 발행 이력의 요일을
직접 계산해 보면 07-21 화·07-22 수·07-24 금·07-26 **일**·07-28 화·07-30 **목**으로
월/수/금이 아닌 요일이 반복 등장한다 — 실제 트리거는 "격일(짝수 날짜)" 패턴과 정확히
일치하고 "월/수/금" 문구와는 맞지 않는다. 이 SKILL.md는 리포 밖(`~/.claude/
scheduled-tasks/`)에 있어 이 점검의 편집 권한 밖이고, `list_scheduled_tasks` 조회에도
잡히지 않아(이 계정에 등록된 건 `blog-coverage-precheck` 하나뿐) 실제 트리거 주체를
이 세션에서 직접 확인할 수 없다 — **교정하지 않고 사용자에게 보고만 한다**(오늘
판정 자체는 두 해석 모두 "발행 없음"으로 일치해 실질적 영향은 없음).

🔴 **차기 소재 재확인 — ACTIVE 유지, 단 뒤에 쌓인 미발행 구간이 크게 늘었다.**
`state/blog_next_material_priority.md`는 여전히 `STATUS: ACTIVE`로 **M12 "성격 축
성향벡터"**(spec `2e2ce34` ~ 종료 선언 `c1d87e2`)를 정확히 지정 중이다 — M11(#15) 다음
순서로 올바르며 덮어쓸 필요 없음(이미 ACTIVE인 지시 유지 원칙). 다만 이번 점검에서
`git log`로 직접 확인한 결과, 07-30 이후 origin/main에 **M13·M14·M15·M16 네 밀스톤이
전부 완결 상태로 새로 들어와 있다**(상세는 아래 대조표):
- M13 "사건과 흔적": `d6a84bf`(spec, 07-27) ~ `458e882`(결산, 07-30)
- M14 "가을과 겨울": `9c352cc`(spec, 07-31) ~ `c4bc93e`(방치 완주 관측, 07-31)
- M15 "연대기": `1580dcf`(spec, 07-31) ~ `ece7029`(Play 검증 완주 기록, 07-31)
- M16 "화폐 기반+직거래": `06af95f`(spec, 07-31) ~ `b0f041d`(방법론 M17 신설, 08-01)

**순서는 역전되지 않았다** — "가장 오래된 미발행 밀스톤부터" 원칙대로 M12가 여전히
정확한 다음 차례이고, M13~M16이 아무리 커밋 분량이 많아도 그보다 뒤에 놓인다. 다만
미발행 구간이 M12 하나에서 M12~M16(+scene-video) 다섯 구간으로 불어난 것은 **다음
몇 회차의 기획팀이 "분량이 큰 최근 트랙에 끌려 순서를 건너뛰지 않도록" 특히 주의해야
할 대목**이라 아래 대조표에 전부 반영해 두었다.

**품질 스팟 체크(④)**: 최신 발행 글(#15, M11)은 07-30 사전점검이 이미 라이브 URL로
직접 사실관계 대조를 완료했다(`"새침이"`·`0.7일` 등 정확성 확인). 그 이후 새 발행이
없으므로 재검증 대상 없음 — 유효.

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` 확인 — OPEN 경보 0건, 기존
3건 전부 CLOSED 유지. 직전 실행(07-30)에 403이나 반려 흔적 없음. 건강.

**오늘(08-01) 13:03 예측**: 홀수일이라 발행 cron 자체가 트리거되지 않음 — **발행
없음**. 다음 실제 회차는 **08-02(짝수일)**이며, 예상 소재는 위에서 재확인한
**M12 성격 축 성향벡터**.

### 이전 사전점검 이력 (2026-07-31, 기록 보존용)

**마지막 사전점검**: 2026-07-31 (자동 스케줄, 발행 1시간 전 정상 실행). `git pull` 결과
`Already up to date`. **오늘(31일)은 홀수일**이라 격일 발행 cron(`3 4 2-30/2 * *`,
짝수일만 트리거)이 애초에 발동하지 않는다 — **오늘 13:03 발행 자체가 없음**(게이트
계산 불필요, 07-29·07-25 사전점검과 같은 패턴).

🔴 **교정함 — 대조표가 07-30 발행분(M11)을 반영하지 못하고 있었다.** `state/blog_last_
published_commit.md`는 이미 M11을 `publish_status: PUBLISHED`(blogger_post_id
`6558847625680705861`, 2026-07-30 06:20 UTC LIVE)로 정확히 기록 중이었으나, 이 파일
하단 대조표는 여전히 M11을 "미발행 구간" 행으로 남겨 두고 있었다 — 표 갱신이 발행
당일(07-30) 세션에서 누락된 것으로 보인다(`state/blog_next_material_priority.md`와
devlog는 이미 정확히 CONSUMED/ACTIVE(M12)로 갱신돼 있어, 실제 파이프라인 동작에는
영향 없는 표 갱신 누락이었다). 아래 표를 **#15 PUBLISHED**로 갱신하고, M12를 다음
"미발행 대기" 행으로 승격했다.

**차기 소재 재확인**: `state/blog_next_material_priority.md`는 `STATUS: ACTIVE`로
**M12 "성격 축 성향벡터"**(spec `2e2ce34` ~ 종료 선언 `c1d87e2`)를 정확히 지정 중 —
07-30 게시팀 Step 8 지정 그대로이며 덮어쓸 필요 없음(이미 ACTIVE인 지시 유지 원칙).
순서 역전 없음: M11(#15, 발행) → M12(ACTIVE) → M13(사건과 흔적, 대기) → scene-video.
참고로 로컬 devlog(`devlog/sessions/2026-07-30.md`)에 M13이 이미 6개 항목 전부
완료됐다는 기록이 있으나(memory 기준), origin/main 관점에서 아직 M12조차 미발행이라
소재 순번에 영향 없음.

**품질 스팟 체크(④)**: 최신 발행 글(#15, M11)은 **어제(07-30) 사전점검이 이미 라이브
URL로 직접 사실관계 대조를 완료**했다(`"새침이"`·`0.7일` 등 정확성 확인). 그 이후 새
발행이 없으므로 재검증 대상 없음 — 유효.

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` 확인 — OPEN 경보 0건, 기존
3건 전부 CLOSED 유지. 직전 실행(07-30)에 403이나 반려 흔적 없음. 건강.

**오늘(07-31) 13:03 예측**: 홀수일이라 발행 cron 자체가 트리거되지 않음 — **발행
없음**. 다음 실제 회차는 **08-01(홀수일이므로 이것도 아님) → 08-02(짝수일)**이며,
예상 소재는 위에서 재확인한 **M12 성격 축 성향벡터**.

### 이전 사전점검 이력 (2026-07-30, 기록 보존용)

**마지막 사전점검**: 2026-07-30 (자동 스케줄, 발행 1시간 전 정상 실행). `git pull` 결과
`Already up to date` — 로컬 HEAD == origin/main 확인(07-29 점검이 보류했던 scene-video
커밋들이 그 사이 push 완료됨, 최신 `e30f565` 오늘 12:38). 오늘(30일)은 **짝수일**이라
격일 발행 cron(`3 4 2-30/2 * *`, UTC 04:03=KST 13:03)이 정상 트리거된다.

**게이트 판정(②, `LAST=caa23b0` 기준)**: 미소비 커밋 **172개**·spec/refactor 커밋 8개·
경과 7일 → **기준 A(≥10) 단독으로도 넉넉히 통과**. `publish_status`도 `PUBLISHED`로
정상이라 07-28에 신설된 DRAFT 무결성 가드는 해당 없음. **발행 확정.**

🔴 **교정함 — M11을 STATUS: ACTIVE로 명시 지정했다.** 이번 점검의 핵심 판단이다.
07-28·07-29 두 회차는 "07-28 기획팀 드라이런이 사람 지정 없이 M11을 자동 선정했으므로
ACTIVE 지정 불필요"로 판단했는데, **그 전제가 무너진 것을 발견했다** — 그 드라이런 이후
`tools/scene-video/`(개발일지 영상 렌더러) 트랙이 07-28~30 사이 **30커밋 이상**
원격에 들어왔고(`0be28c9`~`e30f565`), M12 종료 선언(`c1d87e2`)·M13 실행명세서(`d6a84bf`)도
전부 origin/main에 있다. 미소비 커밋 172개의 **대부분이 M11이 아니라 최근 트랙**이므로,
기획팀이 분량이 압도적인 최신 트랙을 고르면 **M11을 건너뛴 순서 역전**이 발생한다 —
이 블로그가 과거 3회(07-14 M0·07-16 M2/M3·07-17 M5) 실제로 겪은 사고 유형이다.
직전 상태값이 `CONSUMED`(ACTIVE가 아님)여서 "이미 ACTIVE인 지시는 덮지 않는다" 원칙에도
저촉되지 않는다. 지정 블록에는 **배제 조항 3건**을 명시했다: ①M12·M13(다음·다다음 회차)
②scene-video 트랙(밀스톤 체인 밖, 6일 늦음) ③M11 자체의 종료 판정(회상 테스트·24일 방치
최종 관측 — origin/main에서 검증 불가, M9 회차와 같은 제약). 게시팀 Step 8에 다음 순번을
M12 → M13 → scene-video로 박제해 두었다.

🎉 **M11은 devlog가 살아 있다 — M9·M10 회차와 결정적으로 다른 점**: `2026-07-23.md`
(492줄·고유 헤더 13개·M11 언급 84회)·`2026-07-24.md`(538줄·헤더 10개·63회)를 직접 열어
확인했다. 07-28 소급 보강으로 커밋 본문이 절 단위로 주입돼 "왜 그렇게 판단했는가"가 들어와
있다. `Docs/M11_개인화경제_실행명세서.md`(76,908 bytes)도 존재 — **검수팀이 devlog·Docs·
커밋 3중 대조를 할 수 있는 첫 회차**다(M9·M10은 devlog가 비어 Docs+커밋 2중이었다).
따라서 반려 리스크는 지난 두 사이클보다 낮다.

**품질 스팟 체크(④, 최신 발행 글 = #14 M10)**: 07-29 점검은 **로컬 아카이브**로만
대조했는데, 이번엔 절차 원문대로 **`blog_url`(라이브 공개 글)을 직접 열어** 확인했다 —
07-28 오전 draft 강등의 원인이었던 잔여 결함 2곳이 재집필본에서 실제로 해소됐는지가
라이브에서만 확정되기 때문이다. 결과: 성격 표시명이 `"새침"`이 아니라 **`"새침이"`**(실제
에셋 DisplayName)로, 방랑자 수락 시한이 placeholder가 아니라 **`0.7일`**(WorldConfig
`WandererWaitDays`)로 정확히 들어가 있다. 성격별 감지 반경(0.6/0.8/1.0/1.2)·간호 비용
1→8·티어 임계 12→10→8도 07-29에 커밋 대조로 검증한 값과 일치. **통과** — 이 회차의
결함은 완전히 닫혔다.

**순서 점검(③)**: 역전 없음 — M9(#13) → M10(#14, 발행) → **M11(오늘, ACTIVE 지정)** →
M12 → M13 → scene-video 영상 트랙 순서. 다만 위에 적은 대로 **역전 리스크가 실재했고
이번 교정으로 막았다**(관찰 항목이 아니라 조치 완료 항목).

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` 확인 — OPEN 경보 0건. 3건 전부
CLOSED("2026-07-28 반려 3회 도달"·2026-07-16 REJECTED_3X·403 push), 신규 경보 없음.
직전 실행(07-28)에 403 흔적 없음. 건강.

**오늘(07-30) 13:03 예측**: 게이트 A + 이번 교정으로 D까지 통과 → **발행 확정**, 소재는
**M11 개인화 경제**(`6a0486e`~`7cd9e46`). 커버리지 표의 M11 대기 행이 이 회차로 해소될
전망이다.

### 이전 사전점검 이력 (2026-07-29, 기록 보존용)

**마지막 사전점검**: 2026-07-29 (자동 스케줄, 목표 12:10 KST 정상 실행). `git pull` 결과
`Already up to date` — origin/main 변화 없음. **오늘(29일)은 홀수일**이라 격일 발행 cron
(`3 4 2-30/2 * *`, UTC 04:03=KST 13:03, 짝수일만 트리거)이 애초에 발동하지 않는다 —
**오늘 13:03 발행 자체가 없음**(07-25 사전점검과 같은 패턴). 참고로 게이트는 계산해
봤다: `LAST=caa23b0` 기준 미소비 커밋 145개·spec/refactor 커밋 7개·경과 6일로 기준
A~C 모두 넉넉히 통과하지만 날짜가 cron 범위 밖이라 무의미.

로컬 브랜치가 origin/main보다 **18커밋 앞서 있음을 확인**(`git log origin/main..HEAD`) —
전부 `tools/scene-video/`(개발일지 영상 렌더러) 세션 작업이며 `Assets/`·`Docs/`·
`devlog/`의 게임 밀스톤 서술과 무관, 이 스킬의 편집 권한 밖 파일들이다. 🔑 **07-27/28
기록에 있던 "M13 실행명세서 커밋(`d6a84bf`) push 보류" 경고를 재확인** —
`git merge-base --is-ancestor d6a84bf origin/main` 통과, **이미 origin/main에 반영되어
있어 보류가 해소된 상태**임을 확인했다(사용자가 그 사이 push 승인·실행한 것으로 보임).

⚠️ **push 보류 — 이번 회차도 예외 조치**: 이번 파일 갱신 커밋(`b795d51`) 직전, 이 점검이
진행되는 동안 `tools/scene-video/`에 새 커밋(`abb4600` 폰트 로컬 임베드, 12:14:15)이
**실시간으로 추가되는 것을 확인**했다 — 즉 지금 이 순간에도 다른 세션이 scene-video
작업을 진행 중이다. 오늘은 애초에 발행 회차가 없어(홀수일) 이 커밋을 원격에 올리는 것이
파이프라인 판단에 필수가 아니므로, **작업 중인 다른 세션과 겹치지 않도록 이번엔 push를
건너뛴다**. 게임 milestone push 보류(`d6a84bf`, 위에서 해소 확인됨)와는 무관한 별개
사유다 — scene-video 작업이 일단락되면(또는 사용자가 지시하면) 이 커밋들과 함께
`tools/blog-automation/` 갱신분도 자연히 push될 것.

**커버리지 갱신(①)**: 07-28 회차(#14, M10)가 DRAFT→PUBLISHED로 최종 확정된 상태를 표에서
재확인, 변경 없음. 대신 **표의 미해결 항목을 하나 채웠다** — #14 행에 그동안 빠져 있던
"사실관계 스팟 체크" 주석이 없었음을 발견(아래 ④ 참조), 이번 점검에서 처음 수행하고
표에 결과를 기입했다.

**미발행 구간 재스캔(②)**: `devlog/INDEX.md` 확인 — M11(개인화 경제, A~K 전 항목)이
여전히 다음 후보로 대기 중이고, 커밋 범위(`6a0486e`~`7cd9e46`) 전체가
`git merge-base --is-ancestor`로 origin/main 존재 재확인됨(3개 경계 커밋 샘플 확인:
`6a0486e`·`3d504f5`·`7cd9e46` 전부 통과). M12(성격 축, 완료 선언 `c1d87e2`)·M13(사건과
흔적, 명세 `d6a84bf`)도 이제 **전부 origin/main에 존재**함을 확인(위 push 보류 해소
확인과 동일 사실) — 다만 이는 순서상 M11 다음이라 소재 후보 순번에 영향 없음(③ 참조).
`blog_next_material_priority.md`는 `STATUS: CONSUMED`(2026-07-28 M10 정식 발행 확정)로
정확한 상태이며, **07-28 점검이 이미 확인한 대로 기획팀이 ACTIVE 지정 없이도 M11을
자동 선정하는 경로가 검증되어 있어(드라이런 `deferred_milestones: [M12]`) 별도 ACTIVE
지정 불필요 — 교정 불필요, CONSUMED 유지**.

**순서 점검(③)**: 역전 없음 — M9(#13) → M10(#14, 발행) → M11(다음, origin 존재 확인) →
M12 → M13(둘 다 origin에 이미 존재하나 M11보다 늦은 순서라 대기) 순서 그대로.

**품질 스팟 체크(④, 최신 발행 글 = #14 M10)**: 이 항목이 그동안 누락되어 있었다 —
07-28 회차는 오전 draft→오후 정식 발행 소동 중이라 스팟 체크가 비어 있었다. 로컬 아카이브
`published/2026-07-28-unity-goap-m10-wolf-injury-wanderer.html`을 커밋 범위(`db82ffd`~
`caa23b0`) 및 실제 코드와 직접 대조:
- 성격별 위협 감지 반경(`FleeRadiusMult`) "고집쟁이 0.6·농사꾼 0.8·순둥이/떠돌이 1.0(기본값)·
  새침이 1.2" — `git show caa23b0:Assets/M0Config/Personalities/Personality_{Stubborn,Farmer,
  Prickly}.asset`으로 커밋 시점 값 확인, 정확히 일치(현재 HEAD는 M12 성향벡터 재설계로
  이 필드가 더 이상 쓰이지 않아 값이 다르지만, 글이 서술하는 M10 시점 값은 정확함).
- 방랑자 주기·대기시간·프롬프트 문구 — `WorldConfigSO.cs`(`WandererIntervalDays = 5f`,
  `WandererWaitDays = 0.7f`)와 `WandererService.cs:125`(`$"방랑자 도착: {Describe()} —
  [Y] 수락 / [N] 거절 ({_config.WandererWaitDays:0.#}일 내)"`)가 글의 "게임 내 5일마다"·
  "0.7일 내"·인용 프롬프트 문구와 글자 그대로 일치.
- 사망 대사(`먼저 갈게… 마을을 부탁해.`/`춥다… 미안…`) `AgentConfigSO.cs`에 실존 확인.
- 간호 비용 사고(BaseCost 1→8)·티어 임계값 두 차례 인하(12→10→8)·주민 표적 추격 도입·
  큰 곰 리허설 `.cs 0` — 전부 memory `blog_last_published_commit.md` 커밋 서술과 일치.

과장·미검증 인용 없음 — **통과**. 표 #14 행에 "사실관계 스팟 체크 통과 (2026-07-29)" 주석
추가함.

**파이프라인 건강(⑤)**: `state/blog_pipeline_alerts.md` 확인 — "2026-07-28 반려 3회 도달"
CLOSED, 기존 403/REJECTED_3X 2건도 CLOSED 유지, 신규 경보 없음. 건강.

**오늘(07-29) 13:03 예측**: 홀수일이라 발행 cron 자체가 트리거되지 않음 — **발행 없음**.
다음 실제 회차는 **07-30(짝수일)**이며, 예상 소재는 **M11 개인화 경제**(`6a0486e`~
`7cd9e46`, ACTIVE 지정 없이도 기획팀 자동 선정 경로 검증됨 — 위 ② 참조). 리스크는 검수팀이
devlog 대조에서 반려할 가능성인데, memory 기준 M11 devlog 서술은 07-28에 이미 소급
보강 완료(07-23~25에 "왜" 절 다수 확보)라 M9·M10 사이클보다 근거가 두텁다.

### 이전 사전점검 이력 (2026-07-28, 기록 보존용)

**마지막 사전점검**: 2026-07-28 (자동 스케줄이나 **세션 지연으로 17:03 KST 실행** — 목표
시각 12:03 KST 대비 약 5시간 늦음. 07-19·07-22와 같은 "점검 시작 전에 이미 오늘 회차가
끝나 있었다" 패턴). `git pull` 결과 `d6b63c4`(로컬 M13 명세 커밋) 이후 origin/main에
07-28 auto-run 결과(`240bcb5` 포함 다수)가 merge로 들어옴 — 오늘(28일)은 **짝수일**이라
격일 발행 cron이 정상 트리거되어 이미 실행 완료된 상태.

🔴 **오늘 실제 결과 = 공개 발행 아님, DRAFT로 강등.** 소재는 `blog_next_material_priority.md`
ACTIVE 지정대로 **M10 "야생 위협과 방랑자"**(`db82ffd`~`caa23b0`)였으나, 검수팀 2차·마스터
1차 재검증에서 연속 3회 반려(반려 사유: 인트로 4편 연속 동일 골격 + 화면 인용문 2건이 실제
코드와 불일치 — `WandererService.cs` 프롬프트 조립 순서·`InjuredLines` 대사 오인용, 상세는
`blog_pipeline_alerts.md`의 "2026-07-28 반려 3회 도달" 항목). 지시서의 반려 카운터
안전장치가 발동해 게시팀 Step 7이 blog-editor(Step 5)를 거치지 않은 마크다운 초안을
최소 HTML 변환만 적용해 Blogger **비공개 초안**으로만 생성했다(blogger_post_id
4321240890939765189). **이 사전점검이 개입해서 교정할 여지가 없다** — 게이트 판정이나
소재 선정의 문제가 아니라 검수 정확도 문제였고, 파이프라인이 자체 안전장치대로 정상
종료했다. `blog_next_material_priority.md`는 이미 `STATUS: DRAFT_PENDING`으로 정확히
반영돼 있어 **덮어쓰지 않고 유지**(이미 ACTIVE인 지시를 덮지 말라는 원칙과 동일하게, 이미
정확한 DRAFT_PENDING 판단을 덮지 않음).

> ✅ **후속 (같은 날 오후, 로컬 세션) — 위 판단은 오전 시점 기록이고 결과가 뒤집혔다.**
> 사용자 지시로 파이프라인 지시서를 먼저 고치고(반려 원인 = 검수 정확도) **같은 소재로
> 재집필해 반려 0회로 정식 발행**했다 — #14, post_id `1047866260477949128`.
> 따라서 이 회차의 최종 상태는 DRAFT가 아니라 **PUBLISHED**이며, 아래 커버리지 표도
> 그렇게 갱신했다. `blog_next_material_priority.md`의 `DRAFT_PENDING`은 `CONSUMED`로
> 전환됐고(그 값 자체가 어느 지시서에도 정의되지 않은 미정의 상태였다는 점도 이때 확인),
> `blog_pipeline_alerts.md`의 "2026-07-28 반려 3회 도달" 항목은 CLOSED다.
> 오전 비공개 초안(`4321240890939765189`)은 사용자가 Blogger에서 직접 삭제했다.
> **위 문단은 오전 기록으로 보존한다** — 고쳐 쓰지 않고 이 후속만 덧붙인다.

**커버리지 갱신(①)**: M9(공간과 재해, 2026-07-26 발행)가 그동안 표에 "ACTIVE 지정" 임시
행으로만 있었던 것을 **#13 PUBLISHED로 확정**하고, M10 행을 "미발행 대기"에서 "DRAFT
(미공개)"로 갱신했다(아래 표 참조).

**미발행 구간 재스캔(②)**: `devlog/INDEX.md` 확인 — M12(성격 축 성향벡터, A~H+J 완료)·
M13(사건과 흔적, 브레인스토밍+명세 완료)가 로컬에서 진행 중이나, **M13 명세 커밋
(`d6a84bf`)은 사용자가 "확정되면 올리자"고 명시적으로 push를 보류시킨 로컬 전용 커밋**이다
(memory MEMORY.md 상단 확인). 이 스킬은 devlog/·tools/blog-automation/ 외 편집 권한이 없고,
설령 있었더라도 사용자의 push 보류 지시를 이 무인 점검이 우회해서는 안 된다 — **M12/M13은
소재 후보로 고려하지 않음, origin/main에 없는 한 원격 파이프라인도 못 본다(교정 불필요)**.

**순서 점검(③)**: 역전 없음 — 방법론 명세서(#12) → M9(#13, 발행) → M10(다음 순서,
draft 시도 중) → M11(대기) 순서 그대로.

**품질 스팟 체크(④, 최신 발행 글 = M9)**: 로컬 아카이브
`published/2026-07-26-unity-goap-m9-zone-disaster-flood.html`을 커밋 로그(`afdbc22`~
`a351437`)와 직접 대조 — ZoneService가 배치 유일 결정자·재해가 "확정 발동"(확률 없음)·
창고 불가침·FoodDaysLeft 자동 비례·이탈 오탐 진단(진단 로그 심음→실이탈 0건→로그 제거,
`7863f9d`·`1082c2e`와 일치)·회의 장면 배속+HUD 알림(`724fafe`)·"관개수로 goal 파킹"
(`a16593a`)·"겨울 잔량 40" 관측(`79f1167` 커밋 메시지 원문 "겨울 잔량 40(안락) 대응"과
일치)까지 전부 실제 커밋과 부합. 글 말미도 회상 테스트 판정·M9 종료 선언을 쓰지 않고
"재해가 진짜 위협으로 느껴지는가"라는 열린 질문으로만 닫아 ACTIVE 지정 당시의 집필 범위
제약을 정확히 지켰다. **과장·미검증 인용 없음 — 통과.**

**파이프라인 건강(⑤)**: 🟡 OPEN 경보 1건 신규 — 2026-07-28 반려 3회 도달(M10 draft
강등), 위에 상세 기술. 사람의 판단(초안 수기 교정 후 발행 vs M11로 전환) 대기 중이라
**OPEN 유지가 맞음, 별도 조치 불필요**(발견 즉시 사용자에게 보고 대상). 기존 403/
REJECTED_3X 2건은 CLOSED 유지.

⚠️ **push 보류 — 이번 점검만의 예외 조치**: 이 파일 갱신은 로컬에 커밋했으나 origin/main에
push하지 않았다. 이유: 현재 로컬 HEAD가 `d6a84bf`(M13 실행명세서, 사용자가 push 보류
지시한 커밋) 위에 있어, `git push`를 실행하면 이 커밋도 함께 원격에 올라간다. 사용자의
명시적 보류 지시를 이 무인 점검이 임의로 해제할 수 없다고 판단해 push를 건너뛰었다 —
이번 사전점검의 코드 변경(BLOG_COVERAGE.md 갱신)은 원격 파이프라인 판단에 필수가 아니므로
(오늘 발행은 이미 끝났고, 다음 회차 게이트는 `blog_next_material_priority.md`가 이미
올바르게 반영 중), 대기해도 손실이 없다. **사용자가 M13 커밋 push를 승인하면, 그 시점에
이 커밋들도 함께 origin에 반영해 줄 것.**

### 이전 사전점검 이력 (2026-07-26, 기록 보존용)

**마지막 사전점검**: 2026-07-26 (자동 스케줄, 13:03 발행 1시간 전 정상 실행). `git pull`
결과 `Already up to date` — origin/main은 07-25 사전점검(`af94dd0`) 이후 변화 없음. 오늘
(26일)은 **짝수일**이라 격일 발행 cron(`3 4 2-30/2 * *`)이 정상 트리거된다. 소재 게이트
재계산(`LAST=ee165a5` 기준, **origin/main** 대비 — 로컬 HEAD가 아님, 이유는 아래 참조):
미소비 커밋 135개·spec/refactor 커밋 6개·경과 8일 → **기준 A(≥10) 단독으로도 넉넉히
통과** — Step 1(기획팀) 호출은 확정.

🔴 **교정함 — 4회 연속 PENDING을 해제하고 M9를 ACTIVE로 지정.** 이번 점검의 핵심 판단이다.

`state/blog_next_material_priority.md`는 07-22·07-24·07-25까지 세 차례, 오늘까지 네 번째로
M9/M10/M11을 "devlog 서술 미비"로 PENDING에 묶어두고 있었다. 이번엔 그 게이트의 **해소
가능성 자체를 실사**했고, 보류 지속이 더 이상 합리적이지 않다고 판단했다:

- **게이트가 충족될 경로가 없다.** origin/main의 `devlog/sessions/2026-07-19.md`를 직접
  열어보면 2,999줄이지만 **고유 헤더가 4개뿐**이고, 그 4개조차 전부 `chore(devlog) 세션
  로그 자동 기록분 반영`·`docs(blog) 사전 점검`이다 — **M9-A~J 개발 서사 0줄**. 07-20은
  207줄, 07-21은 `grep "회상 테스트\|이탈\|재미"` 매치 0건. 즉 "devlog가 보완되면 승격"은
  무기한 차단이며, 마지막 발행 후 이미 8일 공백이다.
- **대체 1차 소스가 origin/main에 온전하다.** M9 명세 2건(`db4424a`·`9980876`)이 `Docs/`에
  있고 구현 커밋 22개(`afdbc22`~`a351437`)의 메시지가 자체 서술을 담는다(ancestor 전수
  확인 통과).
- **선례가 검증됐다.** 07-24 발행된 #12(방법론 명세서)가 정확히 이 경로 — devlog 없이
  Docs+커밋 메시지로 집필해 마스터 1차·2차 승인과 07-25 스팟 체크를 모두 통과했다.
- **순서 역전이 없다.** M9는 #12 다음 순서의 가장 오래된 미발행 밀스톤이다.

지정 시 **검증 불가 구간을 명시적으로 배제**했다(07-21 사전점검이 M9 미push 함정을 피할
때 쓴 것과 같은 장치): ① M9 **회상 테스트 판정**("이탈이 없어 재미 미흡")과 **M9 종료·M10
전환 선언** ② 중간 리뷰 ② 재관측 통과 이후의 결론 — 둘 다 사용자 로컬 기록에만 있고
origin/main에 없다. 글은 **밸런스 4커밋 적용까지**로 닫고 성패 판정은 쓰지 않는다.
M10·M11도 말미 예고 한 문단까지만. 게시팀 Step 8이 다음 회차로 M10을 같은 방식으로
지정하도록 후속 지시도 함께 박제했다.

⚠️ **07-25 판단 1건 정정 — "훅 중복 해소" 관찰 종료는 성급했다.** 07-25 점검은 07-18~20
세 파일의 **줄 수만 보고**(1,713·2,999·207) "정상 범위 → 원인 해소, 관찰 종료"로 내렸는데,
이번에 내용을 직접 열어보니 **2,999줄짜리 07-19는 고유 헤더 4개가 약 60회 반복된 중복본
그대로**다. `bdf5fb1`(훅 ERR 트랩 수정)이 **새 중복 발생은 멈췄지만 이미 오염된 파일은
정리되지 않았다** — "발생 중단"과 "정리 완료"는 다른 상태다. 파일 정리는 사용자의 개발
기록에 대한 내용 판단이라 이 무인 점검에서 임의로 손대지 않고 **관찰 항목으로 재개방**한다
(영향: 해당 구간을 소재로 쓸 때 devlog가 사실상 빈 파일이라는 점 — 이번 ACTIVE 지정이
이미 이를 전제로 소스를 Docs+커밋으로 우회했다).

🔴 **참고 (오늘 회차 판단엔 영향 없음, 기록만)**: 로컬 세션이 origin/main보다 20커밋 앞서
있다 — 겨울 야생채집 봉쇄(ForageFrozen)·M10R 위협 에스컬레이션 래칫(마을규모→게임일)·
M12 성향(Trait) 벡터 A~F 신규 구현 등, 전부 `Assets/`·`Docs/` 게임 코드/명세라 이 사전점검
스킬의 편집 권한 밖(제약: tools/blog-automation/·devlog/ 외 파일 불가). 원격 routine은
origin/main만 읽으므로 이 로컬 전진분은 오늘 13:03 회차 판단에 아무 영향이 없다 — push
여부는 이 스킬 범위 밖의 사용자 판단 사안.

**순서 점검**: 역전 없음(방법론 명세서 → M9 → M10 → M11 대기 순서 유지).

**품질 스팟 체크**: 07-24 발행(#12 방법론 명세서) 이후 신규 발행 없음
(`blog_last_published_commit.md`의 `latest_commit`이 여전히 `ee165a5`) — 07-25
사전점검이 이미 검증 완료했고 그 이후 새 발행이 없어 재검증 대상 없음.

**파이프라인 건강**: `state/blog_pipeline_alerts.md` 두 경보(REJECTED_3X·403 push) 모두
CLOSED 유지, 신규 항목 없음.

**오늘 13:03 예측**: 게이트는 A(미소비 135개)·C(경과 8일)에 더해 이번 교정으로 **D(ACTIVE
지정)까지 통과** — 발행 확정. 예상 소재는 위에서 지정한 **M9 공간과 재해**
(`afdbc22`~`a351437`)이며, 커버리지 표의 M9 대기 행이 이 회차로 해소될 전망이다. 리스크는
검수팀이 devlog 대조 실패로 반려할 가능성인데, ACTIVE 블록에 1차 소스를 Docs+커밋 메시지로
명시하고 #12 선례를 근거로 박아 두어 완화했다. 반려 3회 시 REJECTED_3X로 조용히 종료되고
`state/blog_pipeline_alerts.md`에 기록되므로, 실패해도 SKIPPED와 결과 손실은 같다.

### 이전 사전점검 이력 (2026-07-25, 기록 보존용)

**마지막 사전점검**: 2026-07-25 (자동 스케줄, 13:03 발행 1시간 전 정상 실행). `git pull`
결과 07-24 사전점검(`c02b8a4`로 merge) 이후 **오늘 예약이던 "개발 방법론 명세서" 회차가
실제로 발행 완료**된 상태로 들어옴 — 07-24 사전점검이 유지한 ACTIVE 소재가 그대로
적중, 순서 역전 없음(아래 커버리지 표 #12로 갱신). 오늘(25일)은 **홀수일**이라 격일
발행 cron(`3 4 2-30/2 * *`, UTC 04:03=KST 13:03)이 애초에 트리거되지 않는다 — **오늘
13:03 발행 자체가 없음**, gate A~D 계산 불필요(참고로 계산은 해봤다: `LAST=ee165a5`
기준 미소비 커밋 134개·spec/refactor 커밋 6개·경과 7일로 기준 A~C 모두 넉넉히 통과하지만
날짜가 cron 범위 밖이라 무의미).

**최신 발행 글(#12, 방법론 명세서) 사실관계 스팟 체크 통과**: 로컬 아카이브
`published/2026-07-24-unity-goap-methodology-spec-constellation.html` vs
`Docs/개발_방법론_명세서.md` · `Docs/방법론/*.md` 대조 — "여덟 개의 사고 메커니즘 +
북극성 세 개 + MOC 허브 하나 = 노트 12개"(`Docs/방법론/` 실제 파일 수 12개와 일치),
mermaid 다이어그램 2개(문서 내 `grep -c` 확인), 옵시디언 Canvas 별도 파일(`Docs/개발_
방법론.canvas` 존재 확인), "메커니즘 번호(M1~M8)는 밀스톤 번호(M0~M8)와 무관 — 혼동
금지" 경고 문구(명세서 128~129행)까지 전부 실제 산출물과 일치. 과장·미검증 주장 없음.

**미발행 구간 재확인**: 07-24 이후 로컬 HEAD가 M11-J①~③(게으름뱅이 성격·택지 취향
배정, `e53d08a`~`1b38c2b`)·택지 선호 거리 대역 개선(`c31590d`)·M11-K(탈중심 마을 —
개인 모닥불·제자리 식사·맵-비례 확산, `3d504f5`)까지 전진 — **M11 A~K 전 항목 완료**
(memory 기준). 그럼에도 `devlog/sessions/2026-07-24.md`를 직접 열어보면 훅이 자동
기록한 내용은 여전히 "커밋 메시지 1줄 + 변경 파일 목록"뿐 — 왜 그렇게 판단했는지 서사가
없다. `grep "회상 테스트\|M9.*완료 선언\|M10.*완결\|M11.*완료 선언\|M11.*종료"
devlog/sessions/*.md`도 여전히 실질적 서술 0건(매치되는 건 커밋 제목 인용뿐). 07-24
게시팀 Step 8이 이미 이 판단(M9→M10→M11 모두 "devlog 서술 미비"로 승격 보류, ACTIVE
신규 지정 안 함 → PENDING)을 `state/blog_next_material_priority.md`에 남겨뒀고, 이번
재확인으로도 그 판단이 그대로 유효함을 재확인했다 — **교정 불필요, PENDING 유지**.
**순서 변경 불필요**(방법론 명세서 → M9 → M10 → M11 대기 순서 유지, M11은 K까지 끝나며
대기 항목 내용만 최신화 필요 — 아래 표 갱신).

✅ **관찰 해소 확인**: 이전 회차(07-23·07-24)에서 3연속 관찰하던 "훅 연속 append로
`devlog/sessions/2026-07-18~20.md`가 17,783줄까지 불어남" 현상이 이번 점검에서
**재확인되지 않음** — 현재 세 파일은 각각 1,713·2,999·207줄(총 4,919줄)로 정상 범위.
07-24 세션에서 `bdf5fb1`(fix(devlog): 세션 로그 훅 무한 중복 버그 — ERR 트랩 조기 종료
제거)로 근본 수정된 것과 시점이 일치 — **원인이 해소된 것으로 판단, 다음 회차부터
이 관찰 항목 종료**.

**파이프라인 건강**: `state/blog_pipeline_alerts.md` 두 경보 모두 CLOSED 유지, 신규 항목
없음. 07-24 회차 상태 push(`a8cdad3`, `chore(blog): auto-run state update (2026-07-24)`)가
origin/main에 정상 반영됨(GitHub API 직접 커밋 경로 계속 안정적) — MANUAL_STATE_UPDATE
폴백 불필요. 로컬 아카이브(11,595 bytes)도 정상 존재.

### 이전 사전점검 이력 (2026-07-24, 기록 보존용)

**마지막 사전점검**: 2026-07-24 (자동 스케줄, 13:03 발행 1시간 전 정상 실행). `git pull`
결과 `Already up to date` — origin/main은 07-23 사전점검(`b070abe`) 이후 변화 없음. 오늘은
**짝수일**이라 격일 발행 cron(`3 4 2-30/2 * *`, UTC 04:03=KST 13:03)이 정상 트리거된다 —
gate A~D 계산 필요.

**게이트 판정 (Step 0 재계산, `LAST=897605c` 기준)**: 미소비 커밋 123개·spec 커밋 8개·
경과 6일 → 기준 A·B·C 전부 이미 여유 있게 통과. 그런데 **기준 D(ACTIVE 소재 지정)가
그와 무관하게 무조건 통과시킨다** — `state/blog_next_material_priority.md`가 여전히
`STATUS: ACTIVE`로 **"개발 방법론 명세서"**(`1ab4f9c`~`ee165a5`)를 지정 중이므로, 오늘
13:03 회차는 이 소재로 발행될 전망. 커밋 2개(`1ab4f9c`·`ee165a5`) `git merge-base
--is-ancestor HEAD` 재확인 통과 — 교정 불필요, ACTIVE 유지. **(예측 적중 — 실제로
04:24 UTC/13:24 KST에 이 소재로 발행 완료, 위 07-25 사전점검에서 확인함.)**

**미발행 구간 재확인**: 07-23 이후 로컬 HEAD가 `b070abe`→`c1a097c`로 8커밋 더 전진 —
M11-H(보상 실이전, `900ae7c`)·M11-I(간호 2단계+목수 자가 건축, `3601ae0`)·M11-E(개인 밭,
`c1a097c`)까지 진행되며 M11 A~I(E 제외) + E까지 완료. 그럼에도 `devlog/sessions/*.md`
전체를 재검색(`grep "회상 테스트\|M9.*완료 선언\|M10.*완결\|M10.*완료 선언"`)해도 M9
회상 테스트 결론이나 M10/M11 완결 서술은 여전히 0건 — 07-22·07-23 판단("devlog 서술
보완 전 M9/M10/M11을 소재로 승격하지 않는다")이 그대로 유효. **순서 변경 불필요**
(방법론 명세서 → M9 → M10 → M11 대기 순서 유지).

**파이프라인 건강**: `state/blog_pipeline_alerts.md` 두 경보 모두 CLOSED 유지, 신규 항목
없음. 최신 발행 글(07-22, M8후속+M9명세 묶음)은 07-22 사전점검이 이미 사실관계 스팟
체크 통과시켰고 그 이후 새 발행이 없어 재검증 대상 없음 — 그대로 유효.

### 이전 사전점검 이력 (2026-07-22, 기록 보존용)

**마지막 사전점검**: 2026-07-22 (자동 스케줄, 세션 지연으로 실행 — 이번에도 07-21과
같은 패턴: 이번 점검 시작 전에 오늘 13:03 회차가 이미 발행 완료돼 있었다. `git pull`로
`897605c~9980876`(M8 후속 + M9 명세 묶음) 글이 04:39 UTC/13:39 KST 발행된 상태로
merge되어 들어옴 — 07-21 사전점검이 지정한 ACTIVE 소재가 그대로 적중, 순서 역전 없음).

🔴 **주기 정정 인지**: 같은 날 00:36 KST에 사용자 지시로 발행 주기가 **매일 → 격일
(짝수일 13:03 KST)**로 변경됨(`68461bf`, cron `3 4 * * *` → `3 4 2-30/2 * *`). 오늘(22일,
짝수)은 예정대로 발행됐으므로 **다음 회차는 내일(07-23)이 아니라 07-24**. 이번 점검
이후의 "다음 회차" 서술은 전부 07-24 기준.

**오늘 회차 결과**: "M8 후속 보완(완공 보고 마주치면 정산+IPathfinder 경계) + M9 명세
단계" 묶음 글 발행 완료 — blogger_post_id 4764036419422193885,
[링크](https://gamedevclaude.blogspot.com/2026/07/claude-code-ai-unity-goap-ai.html).

**사실관계 스팟 체크 통과**: 로컬 아카이브
`published/2026-07-22-unity-goap-debt-settlement-pathfinder-boundary.html`을 실제 커밋과
대조 — "동속 추격 60초 타임아웃 → 목수 보상 소실 → 재조준 시도 실패 → 재조준 코드 전량
되돌림 → 빚+정산(조각 Y) 재설계"의 시간순 서사가 `897605c` 커밋 메시지 및
`Docs/ADR_경로탐색_확장경계.md`(§경계3 철회 기록: "재조준 코드는 전부 되돌렸다")와
정확히 일치. IPathfinder 창구·통행 질문 단일 지점 설명도 ADR 문서 §2와 일치. 말풍선
3초 침묵 여운은 `f907fc2`와 일치. M9는 명세 단계까지만 예고(구현 언급 없음 — 브리프
지시 그대로 준수). 과장·미검증 주장 없음.

**다음 회차(07-24) ACTIVE 소재 검증**: `blog_next_material_priority.md`가 게시팀 Step 8
지정으로 **"개발 방법론 명세서"**(`1ab4f9c`~`ee165a5`)를 ACTIVE로 지정해 둠 — 이 세 커밋
전부 origin/main 존재 확인 완료(`git merge-base --is-ancestor` 통과). **M9 구현
(`afdbc22`~`a351437`)을 건너뛴 근거를 직접 재검증**: 커밋 자체는 이미 origin/main에
존재하지만(ancestor 확인됨), **회상 테스트 판정("이탈 없어 재미 미흡" → M9 종료 →
M10 전환)을 기록한 devlog 문서가 origin/main 어디에도 없다** — `devlog/sessions/
2026-07-18~22.md` 전체를 검색해도 해당 서술 없음(관련 내용은 세션 로그 훅 오기입으로
날짜가 뒤섞여 있었고, 정작 회상 테스트 결론 문단 자체가 어느 날짜 파일에도 커밋되어
있지 않음). 1차 소스=devlog 원칙상 M9 구현을 지금 딥다이브 소재로 다루면 검수팀이
"회상 테스트 결론"을 devlog로 검증할 수 없다 — **게시팀의 보류 판단이 타당함, 교정
불필요**. (참고로 M10 spec+A~C(`db82ffd`·`8cf2151`·`b255063`·`1e0e612`)도 이미
origin/main에 있으나 devlog 서술이 전무해 같은 사유로 아직 후보 아님 — 대기 순서
1.방법론명세서 → 2.M9구현(devlog 보완 후) → 3.M10 그대로 유효.)

**파이프라인 건강**: 403 / REJECTED_3X 두 경보 모두 CLOSED 유지, 재발 없음. 오늘도
`chore(blog): auto-run state update (2026-07-22)`(`c5c0e2e`)가 origin/main에 정상
push됨(MANUAL_STATE_UPDATE 폴백 불필요) — GitHub API 직접 커밋 경로 안정적으로 계속 동작.
Blogger 응답 content 필드 스트림 이어붙이기 버그(U+FFFD)도 파이프라인이 자체
`Buffer.concat` 재조회로 정상 우회(로컬 사본 바이트 단위 일치 확인, 13,581 bytes).
🟡 관찰(무해): 오늘 devlog 훅이 `2026-07-22.md`에 "갱신 1~12"까지 12회 연속 append
커밋을 만듦(원인: 격일 전환 작업+발행 파이프라인이 같은 세션에서 겹쳐 훅이 여러 번
발동) — 내용 유실은 없고 최종본만 유효하나, 다음 회차에서도 반복되면 훅 트리거 조건
점검 필요.

### 이전 사전점검 이력 (2026-07-21, 기록 보존용)

**오늘 회차 결과**: M8(관계·소유·부탁, `68c6ef8`~`66f690b`) 발행 완료 —
blogger_post_id 2940839193792714370, [링크](https://gamedevclaude.blogspot.com/2026/07/claude-code-ai-goap-ai-m8.html).
07-20 게시팀이 지정한 ACTIVE 소재가 그대로 적중했고 순서 역전 없음(M7 → M8).

**사실관계 스팟 체크 통과**: 로컬 아카이브
`published/2026-07-21-unity-goap-m8-relationship-ownership-request.html`를 실제 코드와
대조 — 글이 주장한 부탁 거절 판정 순서 "바쁨 → 배고픔 → 피로 → 친밀"이
`Assets/Scripts/M0/Agent/VillagerAgent.cs:866~870`(`RefusedBusy` → `RefusedHungry` →
`RefusedTired` → `RefusedLowAffinity`)과 정확히 일치. 관계 방향성(A→B / B→A 분리)·
좌표를 건물 신원으로 사용(OwnershipService + MyHasHome)·자율 건설/자동 배정 폐기·
성격별 보상 태도(떼먹기·선불)·장식성 발화 억제·대화 델타 기본값 0(중립 불변식)·
주민 4명 모두 memory 기록 및 커밋 서사와 일치. 과장·미검증 주장 없음.

**🔴 내일(07-22) 회차 교정 — ACTIVE 신규 지정함.** 게이트 계산(origin/main 기준,
`LAST=66f690b`): 미소비 커밋 45개 · spec 커밋 2개 · 경과 3일 → **기준 A·B 통과, 발행
확정**. 그런데 게시팀 Step 8이 ACTIVE를 비워둔 채 통상 스코핑에 맡긴 상태였고, 그
스코핑에는 함정이 있었다 — **origin/main에는 07-19~21 세션 로그가 커밋되어 M9-A~J
구현 서사가 devlog에 전부 들어와 있지만, 정작 M9 구현 커밋 22개(`afdbc22`~`a351437`)는
로컬 브랜치에만 있고 push되지 않았다.** planner의 1차 소스는 devlog, 교차 검증은 git
커밋이므로 planner가 M9를 골라 산문만으로 집필하고 검수팀의 커밋 대조가 실패하는
어긋남이 발생할 수 있었다. 더구나 M9는 중간 리뷰 ② 재관측 대기로 밀스톤 자체가
미완결이다. → `blog_next_material_priority.md`를 **STATUS: ACTIVE**로 지정해,
origin/main에 온전히 존재하고 M8 다음 순서인 구간(`897605c` 완공 보고 "마주치면 정산"
재설계 + IPathfinder 확장 경계 · `f907fc2` 말풍선 여운 · `db4424a`/`9980876` M9 명세
2건)을 묶음 소재로 고정했다. M9 구현 언급 금지 조항도 함께 명시. M9 구현이 그 전에
push되더라도 이 소재가 시간순으로 앞서므로 순서 역전은 발생하지 않는다.

**파이프라인 건강**: 403 / REJECTED_3X 두 경보 모두 CLOSED 유지, 재발 없음. 07-19·
07-20·07-21 세 회차 모두 원격에서 `chore(blog): auto-run state update` 커밋이 정상
push됨(`202f9b5`·`cb8b0f9`·`ddfe9db`) — GitHub API 직접 커밋 경로가 안정적으로 동작 중.
07-18 M5 회차의 중복 게시 사고도 재발 없음.

### 이전 사전점검 이력 (2026-07-20, 기록 보존용)

**마지막 사전점검**: 2026-07-20 (자동 스케줄, 13:03 발행 1시간 전 정상 실행). 07-19
사전점검 이후 새로 발행된 글 없음 — `blog_last_published_commit.md` latest_commit은
여전히 `7c0dd7f`(M6). `blog_next_material_priority.md`가 `STATUS: ACTIVE`로 **M7(상호대화와
가시성, `a451435`~`08f7f9b`)**을 유지 중이며, 소재 게이트 기준 D(사용자 지정 소재
ACTIVE)가 커밋 수와 무관하게 무조건 통과시키므로 오늘 13:03 회차는 이 M7 소재로 발행될
전망 — 이번 점검에서 추가 교정 불필요. 순서 점검: M6(발행 완료) → M7(다음, ACTIVE) →
M8(완료, 대기) → M9(진행 중, 미완결) — 역전 없음. 사실관계 스팟 체크: 최신 발행 글(M6)은
07-19 사전점검에서 이미 검증 완료(과장·오류 없음, 아래 참조)이고 그 이후 새 발행이 없어
재점검 대상 없음. 파이프라인 경보 2건 모두 CLOSED 유지, 재발 없음 — 건강.

참고로 로컬 작업 브랜치에는 M9 트랙 커밋 다수(M9-A~J, 최신 `5f3de87` M9-E 대화 릴레이)가
아직 origin/main에 push되지 않은 상태다 — 원격 파이프라인은 origin/main만 읽으므로 이
구간은 push 전까지 소재 후보에 잡히지 않는다(정상, 교정 불필요. M9는 위 표에서도 이미
"진행 중·미완결"로 대기 표시되어 있음).

### 이전 사전점검 이력 (2026-07-19, 기록 보존용)

**마지막 사전점검**: 2026-07-19 (자동 스케줄, 13:03 발행 1시간 전 실행 예정이었으나 세션
지연으로 발행 이후 실행됨). 점검 시작 시점에 이미 **M6 글이 오늘 13:03 회차에서 발행
완료**되어 있었음(`blog_last_published_commit.md` latest_commit이 `7c0dd7f`로 갱신,
`blogger_post_id` 4301273701134180743, `blog_next_material_priority.md`가 이미
`STATUS: CONSUMED`) — 07-18 사전점검이 안전망으로 지정한 M6 ACTIVE가 정확히 적중해 순서
역전 없이 발행됨. 게시팀 Step 8이 이미 다음 소재를 **M7(상호대화와 가시성)**으로
`STATUS: ACTIVE` 지정해 둔 상태라 이번 점검에서 추가 교정 불필요.

**#8(M6 예고된 겨울과 설득) 글 사실관계 스팟 체크 완료**(로컬 아카이브
`published/2026-07-19-unity-goap-m6-season-winter-crisis.html` vs
`devlog/sessions/2026-07-17.md` 대조) — 겨울 수지 562(필요) vs 1,110(보유) 2배 잉여,
개체 편차 붕괴 포만 81.6~81.7→FNV-1a 수정 후 62.6~82.8, 겨울 3→4일·감쇠 1.5→1.75×·
주민 5→10명 재조정, GoalBoosts 성격별 위기 대응 분화(농사꾼45~고집쟁이5) 전부 세션
로그·커밋(a0e318b, 44b7538, 7c0dd7f)과 일치, 과장·오류 없음. 실제 소비 커밋 범위는
`aa795e6`~`7c0dd7f`로, 기존 예측 범위(`~80cac26`)보다 위기감 재조정·성격 분화 2커밋
더 포함됨(같은 세션의 M6 결말부로 기획팀이 보정).

**다음 회차(M7) 예측**: `blog_next_material_priority.md`가 이미 M7(상호대화와 가시성,
`a451435`~`08f7f9b`, 1차 소스 `devlog/sessions/2026-07-17.md` `[04:40]` 이후)을 ACTIVE로
지정해 두어 다음 13:03 회차는 이 소재가 나갈 전망. M8(관계·소유·부탁, 완료)·M9(공간·재해,
A~J 다수 항목 진행 중·미완결)은 그 다음 후보로 관찰 계속.

파이프라인 경보: 403/REJECTED_3X 둘 다 CLOSED 유지, 재발 없음 — 건강. 이번 회차 발행
과정에서도 중복 게시 등 이상 없음(M5 회차 때 있었던 중복 게시 사고 재발 없음).

## 발행 커버리지 대조표 (발행마다 갱신 — 점검 세션의 1차 화면)

| # | 발행일 | 글 (주제) | 다룬 개발 구간 | 개발 순서상 위치 | 상태 |
|---|---|---|---|---|---|
| 1 | ~2026-07-09 | 방향③ GatherIron 무해 봉합 | ~4df41cc | 舊 아키텍처 후반 | PUBLISHED |
| 2 | 2026-07-11 | F-A 성격배분 + 허기→포만감 반전 | 549444d~b930365 | 舊 아키텍처 말기 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/goap-ai-ai.html)) |
| 3 | 2026-07-14 | M1 개발기 (여가·명령/거부) | M1 트랙 ~cc4602e | **⚠️ 순서 역전** — M0보다 먼저 발행됨 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap-ai-ai-m1.html)) |
| 4 | 2026-07-15 | **M0 재설계 회고** (24,800→5,977줄) | 8ee845b~b175ddf +04f0975 | 3번의 앞 이야기 (회고 특집으로 복구) | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap-24800-5977-m0.html)) |
| — | 미발행 구간 | 옵시디언 도입·블로그 게이트 전환 등 운영 개선 | f71c0a8, acd0e13 등 | 소재 가치 낮음 — 스킵 허용 | 소비됨 |
| 5 | 2026-07-16 | **M2 생산체인(농사·요리) + M3 집과 기틀** — 밭·부엌·앵커 승격 체인, 집/통행차단/작업클레임 | 651ea47~153f180 | 4번(M0) 다음 소재 — 두 밀스톤 다 spec+feat+test 완결, 이야깃거리 충분 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap.html)) |
| 6 | 2026-07-17 | **M4 주민의 성격(아키타입 재편입)** — PersonalitySO·비용 배율·명령 거부·혼잣말·5번째 아키타입 + JPS 버그 발견 | b982090~862de3b | 5번(M2+M3) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap_0950458123.html)) |
| 7 | 2026-07-18 | **M5 직업과 일과** — JobSO·실효 우선순위·일과 주입(농부/탐험가)·직업 에셋 5종·목수 리허설(.cs 0) + JPS 대각 버그 | fd6b1db~87d7d7f | 6번(M4) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap-ai.html)) |
| 8 | 2026-07-19 | **M6 예고된 겨울 + 보상 재설득** — 계절 선반·굶주림 이탈·보상 재설득·가을 리허설(.cs 0)·개체 편차 붕괴 수정·위기감 재조정·성격별 위기 대응 분화(GoalBoosts) | aa795e6~7c0dd7f | 7번(M5) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/claude-code-unity-0.html)) |
| 9 | 2026-07-20 | **M7 상호대화와 가시성** — 이름표·선택 정보줄·대사 풀 확장(.cs 0)·ChatterSO/ChatterService 상호대화 선반·에셋 정책 게이트·직교성 리허설 + 부수 버그 3건(모닥불 몰림·대화 연출·말풍선 우선순위) | a451435~598e51e | 8번(M6) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/claude-code-ai-goap-ai-m7.html)) |
| 10 | 2026-07-21 | **M8 관계·소유·부탁** — RelationshipService(방향성 관계)·정보줄 단짝/원한·OwnershipService(내 집)·부탁 선반(RequestSO/JudgeRequest)·요리 부탁 리허설(.cs 0) + 후속 보완 4건(보상 태도 떼먹기/선불·집=목수 부탁 전용·대화 흐름 보호·완공 보고 심부름) | 68c6ef8~66f690b | 9번(M7) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/claude-code-ai-goap-ai-m8.html)) — 사실관계 스팟 체크 통과 (2026-07-21) |
| 11 | 2026-07-22 | **M8 후속 + M9 명세 묶음** — 완공 보고 "쫓아가기"→"마주치면 정산" 재설계(동속 추격 미수렴 근본 진단)·IPathfinder 확장 경계 ADR·말풍선 침묵 여운·M9 공간/재해 실행명세서·식량 수지 보완가이드(FoodDaysLeft) | 897605c, f907fc2, db4424a, 9980876 | 10번(M8) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/claude-code-ai-unity-goap-ai.html)) — 사실관계 스팟 체크 통과 (2026-07-22) |
| 12 | 2026-07-24 | **개발 방법론 명세서** — AI 사고·판정·소통 메커니즘(M1~M8) 박제 + mermaid 다이어그램·개념 노트 성좌 | 1ab4f9c~ee165a5 | 11번(M8후속+M9명세) 다음 소재, 밀스톤 체인 밖 메타 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/claude-code-ai-unity-goap-ai-ai.html)) — 사실관계 스팟 체크 통과 (2026-07-25) |
| 13 | 2026-07-26 | **M9 공간·재해** — ZoneService 구역 선반·파괴 문·DisasterService·FoodDaysLeft 슬롯("며칠 버티나")·N명 대화 회의·홍수 리허설(.cs 0)·밸런스 4커밋(작물 재해 반대 작동 진단) | afdbc22~a351437 (ancestor 전수 확인) | 12번(방법론 명세서) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap-ai-claude-code-ai.html)) — 사실관계 스팟 체크 통과 (2026-07-28, 지연 점검) |
| 14 | 2026-07-28 | **M10 야생 위협과 방랑자** — 부상·간호·위협 선반(A~C)·도망·방랑자(D~E)·전멸 종료 화면(F)·티어3 큰 곰(G) — 부상→치료/방치→사망의 상실 축 완결 | db82ffd~caa23b0 (A~G 전부 origin/main, ancestor 확인) | 13번(M9) 다음 소재, 순서 정상 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap-claude-code-ai.html)) — blogger_post_id 1047866260477949128. **경위**: 같은 날 오전 auto-run이 연속 3회 반려로 비공개 초안(4321240890939765189)까지만 만들었으나(→ `blog_pipeline_alerts.md` "2026-07-28 반려 3회 도달", 현재 CLOSED), 오후 로컬 세션에서 파이프라인 지시서를 고친 뒤 **같은 소재로 재집필해 반려 0회로 Step 3/4/6 전부 통과·정식 발행**. 오전 초안은 사용자가 Blogger에서 직접 삭제함. 사실관계 스팟 체크 통과 (2026-07-29 로컬 아카이브 대조, **2026-07-30 라이브 URL 재확인** — draft 강등 원인이던 잔여 결함 2곳(`"새침이"`·`0.7일`)이 공개본에서 해소됨을 확정) |
| 15 | 2026-07-30 | **M11 개인화 경제** — 개인 인벤토리(A)·먹거리 개인화(B)·식량일수 개인화(D)·집 저장(C)·개인 택지(F)·집 동기(G)·보상 실이전(H)·간호 2단계+목수 자가 건축(I)·개인 밭(E)·마무리+게으름뱅이 성격(J)·탈중심 마을=개인 모닥불·제자리 식사·맵-비례 확산(K) — "각자 먹고살되 마을은 함께 짓는다" | 6a0486e~7cd9e46 (A~K 전 항목, ancestor 확인 07-25·07-30 재확인) | 14번(M10) 다음 소재, 순서 정상 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/07/unity-goap-claude-code-ai_0280147432.html)) — blogger_post_id 6558847625680705861. 07-28 드라이런의 자동 선정에 맡기지 않고 07-30 사전점검이 STATUS: ACTIVE로 명시 지정(그 사이 scene-video 30커밋+·M12/M13 명세가 원격에 들어와 순서 역전 리스크가 실재했기 때문). devlog 소급 보강(07-23·07-24)으로 devlog·Docs·커밋 3중 대조 가능했던 첫 회차. 마스터 Step 6 2차 APPROVED, 반려 0회 정상 승인 경로 완주. M11 자체의 종료 판정(24일 방치 관측·회상 테스트)은 본문 배제 — M11-K 완료까지만 다룸. 사실관계 스팟 체크 통과 (2026-07-30 라이브 URL 직접 대조) |
| 16 | 2026-08-02 | **M12 성격 축 성향벡터** — 성향(Trait) 벡터로 O(성격×goal)→O(성격+goal) 재설계, S4 프로파일러 15/15 전 쌍 분화 | 2e2ce34~c1d87e2 | 15번(M11) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/08/unity-goap-claude-code-ai.html)) — blogger_post_id 4080593902932668278. ⚠️ 정상 승인 경로 아님(원격 반려 3회→DRAFTED, 같은 날 로컬 세션이 마스터 교정 지시 이행 후 수동 공개 전환). 사실관계 스팟 체크 통과 (2026-08-03 라이브 URL 대조) |
| 17 | 2026-08-04 | **M13 사건과 흔적** — 표현층/개입 축. 비석 이름(A)·상태 알림 줄(B)·명부+회고(C1)·사건 기록(C2)·계획·문턱(D)·관계 보존(C3) 6항목 완결 + Play 피드백 반영 5회 수정 | dc797d0~b105360 | 16번(M12) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/08/claude-code-ai-unity-goap.html)) — blogger_post_id 2763756011570552738. 정상 승인 경로 완주(마스터 Step 6 2차 APPROVED, 반려 0회). 사실관계 스팟 체크 통과 (2026-08-05, 라이브 URL 직접 대조 — "게으름뱅이 둘 다 그냥 죽었어"→"굶어 죽는게 확인이 됐어" 전후 인용 확인) |
| 18 | 2026-08-06 | **M14 가을과 겨울** — 계절 대비 축(방아쇠 재배선·개인 지터)+최소 기록(연차·역대 최고·겨울 경보) — 겨울 3번 넘기고 Day 44에 쓰러진 실전 방치 관측(치료사 없는 판·게으름뱅이 확률화) | `9c352cc`~`c4bc93e` (07-31, W1~W4 전 항목+방치 완주 관측) | 17번(M13) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/08/unity-goap-claude-code-ai-3-day-44.html)) — blogger_post_id 8259513125902654061. 정상 승인 경로 완주(마스터 Step 6 2차 APPROVED, 반려 0회). 사실관계 스팟 체크 통과 (2026-08-07, 라이브 URL 직접 대조 — "Day 44"·치료사 회상 인용·게으름뱅이 13% 확률 3건 devlog `c4bc93e` 원문과 일치) |
| 19 | 2026-08-08 | **M15 연대기** — 판별 아카이브(ChronicleArchive)+C 토글 패널+전멸 힌트. 실행명세서(`1580dcf`)~Play 검증 완주 세션 로그(`ece7029`) — "통과"로 적어둔 화면을 자가 재검토해 결함 2건(현재 판 중복 행·전멸 화면 겹침) 발견·수정 | `1580dcf`~`ece7029` (07-31, W1~W3 전 항목+결함 수정+세션 로그) | 18번(M14) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/08/unity-goap-2-claude-code-ai.html)) — blogger_post_id 1028397470425467506. 정상 승인 경로 완주(마스터 Step 6 2차 APPROVED, 반려 0회). 게시팀 자체 검증: POST 응답 content가 게시 직전 로컬본과 byte-identical(md5 `429ea058f78e263aa3c4d7631d51290a`, 12,876자, U+FFFD 0건), 화면 문자열 6종 라이브 확인 완료 |
| — | 미발행 구간 (origin/main 존재, ⚠️ **폐기·대체됨, 다음 기획팀 판단 대기**) | **M16 화폐 기반+직거래** — 지갑·임금·웃돈·물가·직거래 부탁 (`06af95f`~`b0f041d`, 08-01). **2026-08-04 사용자 지시로 화폐 축(M16~M18) 전체 폐기 결정**, M19가 전면 철거 실행(`a9d8337`~`c4c3431`, 45파일 −2,260줄) — 게임에 이미 존재하지 않는 기능이다 | M15 다음 후보 | ✅ **자격 판정 완료 (2026-08-08 auto-run 기획팀) — 단독 소재 부적격, M19에 배경으로 흡수.** 08-08 원격 auto-run이 ACTIVE 지정(M16)을 브레인스토밍 게이트에서 재판단해 **"M19 단일 밀스톤 + M16~M18 배경 압축"**으로 재구성했다(근거: `state/incidents/2026-08-08/01_planner_brief.md` `note_on_scope`·`excluded_notes` ①②). 이 행은 더 이상 독립 회차 후보가 아니다 — 아래 M19 행으로 통합 |
| — | 미발행 구간 (origin/main 존재, ⚠️ **대부분 폐기**) | **M17 촌장 금고와 재정 정책** — 촌장 금고·원천징수+세율·화폐 신뢰·물가 두 층·발행 한도 (`de7506f`~`57b8585`, 24커밋, 08-01~02). M16과 같은 화폐 축이라 **M19 철거 대상**(단 W1 조건 문법은 M18에서 살아남음, M17 자체는 촌장 금고 계열 전부 철거) | M16 다음 후보 | 🔴 **집필 시 주의 — M16과 동일 함정**(08-05 사전점검 신규 판단) |
| — | 미발행 구간 (origin/main 존재, ⚠️ **일부만 채택**) | **M18 조건 문법+집값 물가 연동** — W1 조건 문법(생존 판정 문법)은 채택·유지, W2~W5 집값 물가 연동은 화폐 축과 함께 폐기 대상 | M17 다음 후보 | 🔴 **집필 시 W1과 W2~W5를 분리 서술할 것**(08-05 사전점검 신규 판단) |
| 20 | 2026-08-10 | **M19 화폐 전면 철거와 실물 마을** — 방향 전환(화폐 축 폐기, "재정 정책이 개입 수단"이라는 믿음의 정체성 이탈 진단) + 지갑·세금·물가·발행 본체 제거(45파일 −2,260줄). M16~M18을 배경으로 압축한 "화폐 축 흥망사" 구성 | `6eec765`~`c4c3431` (08-05, `verify_at c4c3431`) | 19번(M15) 다음 소재 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/08/45-2260-unity-goap-claude-code-ai.html)) — 제목 "완성한 날 전부 지웠습니다: 마을 화폐 경제 45개 파일 2,260줄 철거기". 🔴 **2026-08-12 사전점검이 라이브 열람으로 소급 발견·기입** — 08-10 원격 run 이 발행에는 성공했으나 **`chore(blog)` 상태 커밋을 남기지 못해** 08-11까지 상태 파일 전체가 "미발행"으로 잘못 알고 있었다(그대로 뒀다면 오늘 M19 중복 발행). ⚠️ `blogger_post_id`·승인 경로·반려 횟수·`.staging` 산출물·로컬 사본 전부 **유실**. 스팟 체크 2건 통과(제목의 "45개 파일 2,260줄" ↔ 철거 실측 / 도입부 폐기 사유 ↔ memory `project_m19_decurrency`) |
| — | ~~미발행 구간~~ → **위 #20으로 발행 완료** | ~~**M19 화폐 전면 철거와 실물 마을**~~ — 방향 전환(화폐 축 폐기, 정체성 이탈 진단) + 집 사슬 실물화(곡식 5알 후불)·목수 독점 해제·직거래→무상 나눔·지갑/세금/물가 본체 제거(45파일 −2,260줄). 실행명세서 `6eec765` ~ 종료 `c4c3431`(08-05) | **M15 다음 = 실질 차기 회차** (M16~M18 흡수, "화폐 축 흥망사" 구성으로 확정) | 🟡 **집필 완료·반려 3회로 DRAFTED (2026-08-08) — 소재 미소비, 다음 회차 유효.** 08-08 auto-run이 `verify_at c4c3431`로 11,586자 원고까지 완성했으나 Step 3 검수 2회 + Step 4 마스터 1회 반려로 draft 강등. 🎁 **재사용 자산**: `state/incidents/2026-08-08/02_draft.md`(반려 사유 반영 최신본) + 판정서 원문 2건. 🚨 **재발 금지 지뢰 3종**(브리프 커밋 시각 오기→인과 역전 · 내부링크 404·시각 오차·H2 불일치 · `Docs/CLAUDE.md:97` 축자 아닌 인용·"마지막 커밋" 성과 오귀속) — 상세는 `blog_next_material_priority.md` 2026-08-09 추가 기입 절. 🔴 사람 몫: Blogger 초안 `9034137679451863013` 삭제 필요(공개본과 공존 방지) |
| 21 | 2026-08-12 | **M20 직업 전면 재점검** — 헌장 "직업은 부재가 느껴지는 장치"(ADR-M20-1)·농부/요리사/나무꾼/광부 효율 0.5 승격·탐험가 휴면·목수 최소 보장 삭제·집 부탁 사슬 14파일 철거(자가 건축 원복). 하루 안에 W1~W13 완주 | `4fce684d`~`dce43faa`·`40b44176` (08-05, `verify_at 40b44176`) | 20번(M19) 다음 소재, 순서 정상 | PUBLISHED ([링크](https://gamedevclaude.blogspot.com/2026/08/unity-goap-ai-claude-code-ai.html)) — blogger_post_id 9139247089049724695, 제목 "Unity GOAP AI 인디게임 개발일지 — 목수를 지운 규칙". 정상 승인 경로 완주(마스터 Step 6 2차 APPROVED, 반려 0회). 게시팀 자체 검증: POST 응답 content가 게시 직전 로컬본과 byte-identical(md5 `e46a5d3e75570d24d4751b63acb7258c`, 14,139자, U+FFFD 0건). 🎉 **08-10 사고 이후 첫 완주 회차 — Step 8 상태 커밋(`cd76b9d2`)이 origin/main 에 정상 push 됨**(08-12 점검이 세운 관측점의 답). 사실관계 스팟 체크 통과 (2026-08-13, 라이브 URL 직접 대조 — "14개 파일" ↔ `48b61cb3` 커밋 본문·devlog 축자 일치 / "13단계" ↔ W1~W13 / 효율 0.5 4종 ↔ `7040580c`) |
| — | ~~미발행 구간~~ → **위 #21로 발행 완료** | **M20 직업 전면 재점검** — 헌장 "직업은 부재가 느껴지는 장치"(ADR-M20-1) + 농부·요리사·나무꾼·광부 효율 0.5 승격·탐험가 휴면·목수 최소 보장 삭제·집 부탁 경로 철거(자가 건축 원복). 실행명세서 `4fce684`(08-05) ~ 종료 판정 `dce43fa`/`40b4417`(08-05) | M19 다음 후보 | 대기 — ✅ **08-06 사전점검 갱신: 종료 완료**(Play 리뷰 4회 + 게이트 3차 green + P10~P14 사용자 확인, memory `project_m20_jobs_prep`). 08-05 시점의 "Play 재관측 대기" 상태는 해소됨 |
| — | 미발행 구간 (origin/main 존재, **신규 — 08-09 사전점검 발견**) | **M21 격퇴 축** — 주민 체력=생존 단일 척도(W1)·배회형 위협+계절 연동(W2R, 체류형이 Play에서 뒤집혀 개정)·CombatService(W3)·Goal_Fight+FightRunner "주민이 되받아친다"(W4)·원시 무기 창(W5)·대규모 적습(W6)·래칫 양방향 완충(W7)·사냥꾼·정찰 재탄생+HUD(W8)·격퇴가 이야기가 된다: 연대기·신기록·전멸 화면(W9)·게이트 본대 마감(W10). 🔑 겨울 판정은 `ForageFrozen`(IsCrisis는 여름도 1) | 명세 `99c3c416`(08-05 15:19) ~ `b64b3087`(W10, 08-08 19:25) | M20 다음 후보 | 대기 — ✅ **완결**(memory: 2026-08-08 리뷰① 통과로 종료, `project_m21_repel_prep`). 🟡 집필 시 참고: W2 "체류형"이 Play 관측에서 뒤집혀 W2R "배회형"으로 개정된 경위(명세 `bb1ee005`)가 이 밀스톤의 핵심 서사 — 이월 관측 잔여 5건은 미해결로 남아 있으니 "전부 해결됨"으로 쓰지 말 것 |
| — | 미발행 구간 (origin/main 존재, **신규 — 08-09 사전점검 발견**) | **M22 방어 건설 1차** — 개체별 통행 이원화(W1)·방어 데이터 층(W2)·**줄 긋기 UX**(W3→W3R→W3R2→W3R3, 사용자 UX 확정으로 3회 개정: 구역 사각형 → 드래그 직선+우클릭 문 → 반 칸 정렬·줌 불변 픽킹)·방어 건설 사슬(W4)·**공성**(W5, ADR-M22-2 "막힌 위협은 출몰을 접지 않고 울타리를 부순다")·수리 루프 1:5(W6)·목수 부활+부재의 화면(W7)·철거(W8) | 명세 `7d5e1e0c`(08-08 20:28) ~ W8 `94ff7725` + 리뷰① 통과 `9627b7db`(08-09 01:34) | M21 다음 후보 | 대기 — ✅ **완결**(사용자 Play 리뷰① 통과, memory `project_m22_defense_prep`). 🟡 UX 개정 3회(W3→W3R3)가 "사용자가 화면을 보고 뒤집은" 서사라 소재 가치 높음 |
| — | 미발행 구간 (origin/main 존재, **신규 — 08-09 사전점검 발견**) | **M23 방어물·행동 비주얼** — 주민 행동 애니메이션(W1, AnimKind 축: 도끼·곡괭이·망치·물뿌리개·맨손 5종)·울타리/문 오토타일(W2, 4방 마스크 조각 선택, 이웃=울타리∪문)·계획 고스트(W3, 흐린 원 → 반투명 실물 그림) | 명세 `9b7ae7c2`(08-09 01:04) ~ W3 `b46b5d98` + 리뷰① 통과 `9627b7db`(08-09 01:34) | M22 1차 다음 후보 | 대기 — ✅ **완결**(사용자 Play 리뷰① 통과, memory `project_m23_visual`). 🟡 위협(늑대·곰) 스프라이트는 **에셋 조달 대기로 미포함** — "비주얼 전부 완성"으로 쓰면 사실 오류 |
| — | 미발행 구간 (origin/main 존재, **신규 — 08-10 사전점검 승격**) | **M22 2차 문 고도화** — 전역 문 잠금 L 토글(W1)·완공 울타리 우클릭 원클릭 문 전환(W2)·문 오토타일 편입(W3, Play 피드백 "문이 뜬금없다")·자원 HUD 신설(W4, 나무·돌 — 목록은 에셋이 정한다) | 명세 `c978cf51`(08-09 01:58) ~ W4 `0b5d7e7b` + 리뷰① 통과 `4a6fae80`(08-09 15:05) | M23 다음 후보 | 대기 — ✅ **완결**(사용자 Play 리뷰① 통과, 게이트 381/381, memory `project_m22_phase2_agreement`). 08-09 점검이 "진행 중"으로 뺐던 것이 같은 날 오후 완주 → 이번에 승격. 🟡 W3·W4가 **리뷰① 피드백으로 명세에 없던 것이 추가된** 항목이라 "계획대로 4주차" 식 서술은 사실 오류 |
| — | 진행 중 (표에 올리지 **않음**) | **M22 3차 망루·함정** — 데이터 층(W1)·B 모드 브러시(W2)·망루 탑승·요격(W3)·함정 발동(W4)·**픽셀 아트 파이프라인 `tools/pixel-art` 자작(W5a~W5e)**. 명세 `734ea272`(08-09 15:33) ~ `8ee09071`(08-09 18:53) | — | M22 2차 뒤 | 🚫 **완결 자격 미충족** — memory START HERE가 **"그림 외 잔여 = 리뷰①(관전 7건: 함정 발동·탑승 희생 제외·망루 파괴 하강·브러시 2/3·고스트 4종·발판 높이·곰)"**으로 미완료 명시. ⚠️ **08-11 갱신: 08-09 이후 이틀간 진척 0이고, 그 사이 M22 4차가 먼저 완결돼 차수 번호를 추월했다** — 대기열은 완결 순서를 따르므로 지금대로면 4차가 3차보다 앞서 나간다. 리뷰①이 계속 미완이면 **3차만 영구히 대기열 밖에 남을 위험**이 있다(다음 점검이 재확인할 것). 🎁 소재 가치는 높다 — 늑대 그림 7라운드 반려 → 조달 조사 → **"늑대 유지"와 "화풍 유지"가 동시에 성립하지 않는다"**는 판정으로 **그림이 기획을 바꾼** 사슬(→ M24) |
| — | 미발행 구간 (**신규 — 08-11 사전점검 승격**, ✅ **origin/main push 완료 — 08-12 확인**) | **M24 1차 종족 침공 축 (+W7R)** — 늑대·곰 폐기 → 고블린·오크·기사단·타락천사 4종(W1)·휴면 슬롯 재사용(W2)·압력 축(W3)·**예산 구매(W4, `PickTier` 폐기로 게임일 밴드 3단 소멸)**·침입 특성+전략 테이블(W5)·그림 배선(W6)·진입점 우회·다지점(W7)·예고 UI(W8)·자비 장치(W9)·게이트 마감(W10, 418/418) + **W7R 우회 판정을 "사거리"에서 "변"으로**(`925832da`) | 명세 `42039a7d`(08-10 02:34) ~ `8b3df240`(W10 완주 마감, 12:12) ~ **W7R `925832da`~`99062b83`(Play 관측 완료, 17:38)** | M22 2차 다음 후보(M22 3차가 리뷰① 통과 시 그 뒤) | 대기 — ✅ **완결**(W7R Play 관측까지, memory `project_race_invasion`). ✅ **08-12 해소 — 117커밋이 전부 push 됐다**(`origin/main...HEAD` = `0 0`). 원격에서 이 해시들을 볼 수 있으므로 소재 자격 판정이 가능해졌다. 정본 = `Docs/M24_1차_종족침공축_실행명세서.md`. 🟡 소재 가치 높음 — **그림이 기획을 바꾼 사슬**(M22 3차 늑대 그림 반려 → 종족 교체)의 착지점이자 "게임일 밴드 3단 → 압력 예산제" 재설계 |
| — | 미발행 구간 (**신규 — 08-11 사전점검 발견**, ✅ **origin/main push 완료 — 08-12 확인**) | **M22 4차 개간(clearing)** — 울타리 구멍을 닫은 축. W1~W6 개간 본체(`e0e6c618`)·W7 개간 몸짓이 자원을 따른다(돌은 곡괭이로, `db3c4a3c`)·2차 Play 검증(`8032a474`)·W8 **취소는 취소다(안 B) + 붉은 고스트가 실제로 붉어진다**(`a1279e84`) | 명세 `a5ff2bf1`(08-10 17:54) ~ `a1279e84`(W8, 18:47) | M24 1차 다음 후보 | 대기 — ✅ **완결**(W1~W8 종료, 게이트 452, memory `project_clearing_axis`). 🟡 집필 시 참고: 이 밀스톤의 교훈 6종(두 수치는 곱해 볼 것 · 산수는 에셋에서 · 문자열 게이트는 산문을 읽는다 · **`dotnet build` 0 오류의 함정** · 표현은 화면을 열어야 · 안 재본 근거는 되돌아온다)이 소재 핵심 |
| — | 미발행 구간 (**신규 — 08-11 사전점검 발견**, ✅ **origin/main push 완료 — 08-12 확인**) | **M25 1차 성격 서술 (라벨 → 문장)** — Project Sid 분석에서 갈라져 나온 직업 획득형 축의 1차. 성격 서술 문장표(W1, 축마다 적는다)·축 문장 조립(W2, 문턱·순서·역접 1회)·goal 문장과 합성(W3, **말이 모자란 사람에게만**)·화면 배선(W4, 정보줄·방랑자 제안)·게이트 7종(W5) | 명세 `85c7db39`(08-10 13:54) ~ `2b6920bb`(완주 마감, 14:25) + `615c80de`(S2 Play 판정 통과, 15:06) | M22 4차 다음 후보 | 대기 — ✅ **완결**(memory `project_job_emergence`). 🔴 집필 시 주의: memory가 **직업 배정 이중 출처**(goal `TraitWeights` ↔ job `PreferWeights`가 8개 중 7개에서 다른 축) 등 **미해결 결함을 명시**하고 있다 — "성격 축 완성"으로 쓰면 사실 오류. 2·3차는 대기 상태 |
| — | 미발행 구간 (**신규 — 08-11 사전점검 발견**, ✅ **origin/main push 완료 — 08-12 확인**) | **M26 1차 지형 축 (땅이 생긴다)** — **A\* 경로탐색 + 비용 창구**(W1, `ADR_경로탐색_확장경계` 트리거 A를 실제로 발동시킨 자리)·지형·시드·느린 땅·색·리스폰(W2~W7) | 명세 `26a62909`(08-10 19:59) ~ `c77730b7`(W1) ~ `4b74d0f5`(W2~W7) ~ `925b9891`(1차 종료 — 사용자 Play 확인, 21:39) | M25 1차 다음 후보 | 대기 — ✅ **완결**(사용자 Play 확인, memory `project_terrain_axis`). 🟡 소재 가치 높음 — 07-22에 미리 적어 둔 확장 경계 ADR의 트리거가 3주 뒤 실제로 당겨진 사례. 정본 = `Docs/M26_2차_지형축_실행명세서.md`(2차 명세가 1차를 승계) |
| — | 미발행 구간 (**신규 — 08-12 사전점검 승격**, ✅ origin/main push 완료) | **M26 2차 지형 축** — 멀리 갈 이유와 대가(W1~W4)·들판 상주 위협(W5·W5b)·**W5R "상주는 예약이다"**(무장→부화→회수/전멸→재무장)·움직이는 것은 현재 시야에서만(W5R-A2)·위험 기억(W6)·구조 명령 UI(W7) | 명세 `738190af`(08-10 22:20) ~ `d035d48a`(W7, 08-11 15:32) | M26 1차 다음 후보(대기열 마지막) | 대기 — ✅ **완결**(memory START HERE: "M26 = 1·2차 전체 종료 + Play 후속 대개정 완료 · 게이트 488 · 실측 잔여 0"). W8 게이트 마감(`b58ce0b7`) 뒤에도 **Play 결함 3건 당일 수정**(`4fdcd891` 구조 목록 겹침 → 중앙 결정 패널·UI 팩 첫 적용 / `08a5dbc2` T 목록 판정을 플래그가 아니라 시계로 / `ab8cb927` 구조 추격이 3초 벌칙에 끊김 — **ADR-M2-5 개정, JPS/A\* 문제가 아니었다**)과 **W7R 간호 대개정**(응급조치 폐기 — 전지 인지 제거 · 회복은 치료사만 + 자가 치료 · 치료 우선순위 45→102 · 부상자 피신 범위), **위험 기억 기하 조정**(`dfc65358` 피격당 강도 3·반경 5 — "한 번 물리면 발길을 돌린다")까지 붙어 종료. 🟡 소재 가치 높음 — 이 구간의 교훈 둘("게이트 통계·기하는 **잴 수 있는가**를 배포 값으로 검산" · "goal 이상 행동 진단은 우선순위 표부터 읽는다")이 핵심. ⏱️ 시간 밸런스 축(M27)이 여기서 갈라져 나왔다 |
| — | 미발행 구간 (**신규 — 08-13 사전점검 승격**, ✅ origin/main push 완료) | **M27 시간 밸런스 축** — 하루 100초 → **600초**·1년 12일(사용자 판정 "하루 플레이 시간이 길어야")·**시계 4분류 환산**(체감 ÷6 / 달력 / 생존 유지 / **율 ×6**)·용량 짝 몸 48·집 90·실시간 대역 게이트 `M27_TimeGates`·게이트 화석 3건 개정 | 명세 `8e864a7e`(08-11 23:11) ~ `ec6b4513`(W4 게이트 482, 23:26) + `284f3eb4`(세션 로그) | M26 2차 다음 후보 | 대기 — ✅ **완결**(memory START HERE가 08-12 이후 "⏱️ M27 — ✅ 완료 (게이트 482)"로 갱신. 08-12 점검이 "Play 실측 6건 이월"을 이유로 뺐던 것을 이번에 승격 — 잔여 6건은 §7 후속 관측으로 분리됐다). 🟡 소재 가치 높음 — 🔑 "**시계는 셋이 아니라 넷**"(율은 기간과 반대로 ×6) · "**게이트도 눈금의 화석을 품는다**"(일수 리터럴 암묵 전제 3건). 🔴 집필 시 주의: 잔여 실측 6건(겨울 40분·종족 1년차·기억·치료·농사·스톱워치)이 남아 있으니 "전부 검증됨"으로 쓰면 사실 오류. 정본 = `Docs/M27_시간밸런스축_실행명세서.md` |
| — | 미발행 구간 (**신규 — 08-13 사전점검 발견**, ✅ origin/main push 완료) | **M28 맵 대형화** — 100²→**200² 스위치**(세계가 4배). 경로탐색 쿼리 예산 스케줄러(W1, 위협 리패스 프레임당 4건 상한 — `ADR_경로탐색_확장경계` 트리거 B를 발동시킨 자리)·자원 개수의 **면적당 승격**(W2, `nodeCountPer10kTiles`)·맵 정합 게이트(W4)·200² 스위치(W3) + **Play 소크 러너 자작**(`M28PlaySoak`+`run-soak.ps1`) | 명세 `eb7dc75d`(08-12 12:15) ~ `716a9cff`(W3, 13:55) ~ `f562f71f`(마감 — 소크 3판 전말, 14:27) | M27 다음 후보 | 대기 — ✅ **완결**(사용자 200² 화면 확인 "문제 없어", 게이트 485, memory `project_map_expansion`). 🟡 소재 핵심 = **분류 원칙**("개수는 면적당 · 거리는 절대/비례 · 덩어리는 절대")과 🔑 "'지금 맵' 기준 절대값 = 미래의 버그". 🔴 집필 시 주의: 300²는 **미착수**(§8 관문 5조건 뒤 값 회전만)이고, 소크에서 드러난 무개입 전멸(Day 9) 문제는 **미해결 이월**이다 |
| — | 미발행 구간 (**신규 — 08-13 사전점검 발견**, 🟡 3·4차는 로컬 전용) | **M29 바이옴 축 (+M30 슬롯 예산)** — 큰 맵에 지리를 놓는다. 바이옴 배정층(W1, **바이옴 추가 = 에셋 1개 + 씬 등록, 코드 0줄**)·정글(W2)·산악+표면 광산 티어(W3, 구리→철→은→다이아)·게이트 5종(W4, 485→490) → **2차 화면 읽힘**(Play 지적 3건 전부 실측 재현 — 심층 산악 신설, 깊이는 거리가 아니라 **장소**) → 3차 장식 산포(색면 → 덩어리) → 4차 경계 전이 타일(게이트 **492**). 상류에 **M30 슬롯 예산 증설 52→72**(`8d07c008`) | 명세 `a7688b39`(08-12 14:40) ~ W1~W4(`df2f1239`~`5188e365`) ~ 2차 `1b3e7a1a` ~ 3차 `e3b9b658`·`99ead161` ~ 4차 `0a15838e`(16:58) | M28 다음 후보 | 대기 — ✅ **완결**(1~4차, memory `project_biome_axis`). 🟡 **M30은 2커밋짜리 리팩터라 단독 회차로 얇다 — 이 회차에 흡수 권장**. 🔑 소재 핵심 = "**색면은 아무리 갈라도 색면이다**"(안 읽히면 색이 아니라 개체·경계를 의심) · "보간 노이즈는 가운데로 쏠린다". 🔴 집필 시 주의: 광물은 **배치만 있고 스톡 슬롯은 없다**(채굴 루프는 공방 축 몫) — "광산 시스템 완성"으로 쓰면 사실 오류 |
| — | 진행 중 (표에 올리지 **않음**, 🔴 로컬 전용) | **M31 높이 표현 축(절벽)** — 고지대 위는 걷고 남쪽 두 줄만 벽. 높이 모델(W1)·벽면 렌더(W2)·고지대 rim·색(W3)·W3후속(벽 두 배·모서리 연결·흙길) | 명세 `964e4a8a`(08-12 17:12) ~ `e7b272ed`(W3후속, 18:20) | M29 뒤 | 🚫 **완결 자격 미충족** — memory START HERE 기준 **W4 연결성 · W5 게이트**가 남았다. 정본 = `Docs/M31_높이표현축_실행명세서.md` |
| — | 미발행 구간 (밀스톤 체인 **밖**, 별도 트랙) | **개발일지 영상 파이프라인 `tools/scene-video/`** — 블로그 글 → 유튜브 쇼츠 자동화. 대본 scene-* 에이전트 4개·Supertonic TTS·크로미움 CDP mp4(의존성 0)·점검 13종·격일 스케줄러. ep00s/ep01s/ep02s + ep00i 특별편, 대본 클라우드 루틴 무인화(08-02) | M20 뒤 (07-28~, 계속 진행 중) | 대기 — 분량은 충분하나 **밀스톤 체인 밖**이라 M14~M20을 앞지르면 순서 역전. ACTIVE 블록에 배제 조항 명시 필요 |

## 주기 점검 절차 (점검 세션용 체크리스트)

1. **커버리지 갱신**: `state/blog_last_published_commit.md` 이력과 위 표를 대조 —
   새 발행분을 표에 추가하고, 다룬 커밋 구간을 git log로 확인해 기입.
1-2. **게이트 재계산 — 🔴 `git rev-list --count` 를 쓰지 마라 (2026-08-06 개정)**.
   그건 영상·블로그 트랙 커밋까지 세어 실제의 3배 가까이 부풀린다(실측: 283 vs 96).
   `routine-prompt.md` Step 0 과 **같은 게임 커밋 계산식**을 쓴다:
   ```bash
   git log --oneline <LAST>..HEAD -- . \
     ':(exclude)tools/' ':(exclude)youtube-editor/' ':(exclude).claude/' \
     ':(exclude)devlog/' ':(exclude)Docs/영상_*' ':(exclude)Docs/블로그_*' \
     | grep -cv "chore(blog)"
   ```
   이 파일의 08-06 이전 사전점검 이력에 적힌 "미소비 커밋 N개"는 **전부 구 계산식 값**이다
   (기록 보존용이므로 고치지 않는다 — 그 숫자로 오늘의 게이트를 판정하지 마라).
2. **미발행 구간 스캔**: `devlog/INDEX.md`의 밀스톤 타임라인 대비, 표에 없는 굵직한
   개발 구간(명세 확정·밀스톤 완결·대형 리팩터)이 있는가? 있으면 소재 가치 판단 후
   `state/blog_next_material_priority.md`에 STATUS: ACTIVE로 지정 (게이트 기준 D가 받아줌).
3. **순서 점검**: 발행 순서가 개발 서사 순서와 어긋났는가? (사례: 3번 M1이 M0보다 먼저 —
   회고 특집으로 복구). 역전 발견 시 같은 방법으로 복구 지정.
4. **품질 스팟 체크**: 최신 글 1편을 열어(URL) 사실관계를 실제 커밋·devlog와 대조 —
   과장·오류·미검증 주장 여부. 문제 시 정정 소재로 지정하거나 사용자에게 보고.
5. **파이프라인 건강**: `state/blog_pipeline_alerts.md` 확인 + 직전 run의 PIPELINE_RESULT에
   403/실패 흔적이 있으면 상태 수동 반영 여부 점검 (routine-prompt.md의 MANUAL_STATE_UPDATE 참조).

## 관련 파일 지도

- 발행 이력(기계용 상태): `state/blog_last_published_commit.md`
- 차기 우선 소재 지정: `state/blog_next_material_priority.md` (ACTIVE면 게이트 무조건 통과)
- 파이프라인 정의: `routine-prompt.md` (Step 0 소재 게이트 포함)
- 개발 타임라인 허브: `devlog/INDEX.md` → `devlog/sessions/*.md` (1차 소스)
- 발행 사본: `published/` (원격 push 성공 시에만 존재)
