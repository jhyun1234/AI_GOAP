---
name: ep09s-split
description: ep09s(M7 상호대화와 가시성) 2편 분할 — 9-1 APPROVED 기록과 남은 정정 목록, 9-2 와 함께 확정해야 하는 이유
metadata:
  type: project
---

**ep09s = 원문 `2026-07-20-unity-goap-m7-chatter-visibility.html` 을 2편으로.**
9-1 = 이름표·정보줄 / 9-2 = 대화가 대화로 안 보여 4타일→2타일.

### 9-1 판정 = ✅ APPROVED (2026-08-07, 반려 1회 소진)

직접 재검증해 전부 일치한 것: `check.mjs` 통과(26.2초·경고 1건) · `delayMs` **latch 2,151 /
tick 1,541**(실측 `timed.json` 으로 재검산, 오차 1ms 미만) · 페이오프 여유 **1.938초** ·
sfx 피크 0.4459/0.3719(천장 0.46 미만) · TOTAL 26,198ms · `fillText` 16곳 전부 `spec` 유래.

🔴 **커밋 전 정정 남음** — `scene.json` `notes.타이밍_규율` 과 `notes/writer.md:280-281` 의
자기검산 세 문장이 거짓이다. 실측값 = **닿음 4회 / 겹침 배수 2.46 / 최장 무접촉 1.59초 /
닿는 구간 0.837초**. (적힌 것: 「다섯 번 이상」·「3.5배」·「어느 0.6초 창을 잘라도」)
결론(위상 사고 불성립)은 맞다 — 문장만 고치면 된다.

### 9-2 판정 = ✅ APPROVED (2026-08-07, 반려 늘지 않음 — 여전히 1회)

직접 재검증: `check.mjs` 24항목 green(26.2초·경고 0) · **R1 위상 사고는 진짜로 닫혔다**
(알파를 직접 풀어 `PHASE 0` → 0/61, `PHASE 3.24` → **51/61**, 알파 1.0 = ts 1.953 재현) ·
수치 3건(4·2·한 쌍) 원문 `grep` 실재 · `fillText` 8곳 전부 `spec` 유래 · `reads` 4/4 스윕·스틸 참 ·
예고 2.564초에 음성과 `outro.next` 가 같은 것을 가리킴 · 페이오프 여유 1,055ms.

🔴 **내가 잡은 2건(검수팀이 놓침, 출고는 막지 않음)** —
①`faceoff` 의 D1 이 **안 닫혔다**(법선 30px 로도 회전 중 −1.12px, 30fps 5프레임. 스틸 14,060ms.
검수가 본 13,921ms 는 겹침 79ms 전이었다) ②`onepair` sweep 의 `delayMs` 가 **틀린 실측값**
(3.50 vs 실측 3.343)으로 풀려 62ms 늦다. 상세·정정 5건은 `episodes/ep09s-2/notes/verdict.md`.
→ 일반화는 [[zorder-and-phase]] · [[timing-formulas]]

🔑 **판정 기록을 회차 폴더에 남기기 시작했다**(`notes/verdict.md`, `.staging/04_verdict.md` 는 사본).
검수 리포트도 같은 이유로 회차 폴더로 옮겨졌다 — `.staging` 은 회차마다 덮어써져 유실된다.

### 🟡 9-2 와 **같이** 확정해야 한다

9-1 의 `outro.siblings` 가 `ep09s-2` 의 `youtube.title` 을 그대로 담고 있어, 그 편 제목이
한 글자라도 바뀌면 9-1 의 `형제 라벨` 게이트가 red 가 된다.
🔑 형제 편이 병렬로 쓰이는 것이 이제 기본이라 규칙이 **「뒷편이 나오면 채운다」가 아니라
「둘 다 검수를 통과하면 채운다」**로 바뀌었다.

**Why:** 두 편이 서로의 제목을 들고 있어 한쪽만 렌더하면 다른 쪽이 조용히 깨진다.

**How to apply:** ep09s-2 판정 때 이 파일을 먼저 읽고, 9-1 이 이미 렌더됐다면 9-2 제목 변경은
9-1 재렌더를 뜻한다는 것을 판정문에 명시한다. 관련 = [[verdict-evidence-grades]] ·
[[timing-formulas]] · [[factcheck-gotchas]]
