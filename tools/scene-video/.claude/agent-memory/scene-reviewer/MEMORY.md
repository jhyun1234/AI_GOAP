# Memory Index

## 검수 방법
- [cue/sfx 물림 실측 계산법](feedback_cue_timing_audit.md) — rel dur = wav dur + pauseAfter(양쪽이 반복해 틀림), ease 역함수는 이분탐색(0.9→0.8065), **cue(i) 는 범위 밖을 0 이 아니라 마지막 줄로 클램프**
- [sfx 피크 천장은 인자마다 다르다](feedback_sfx_gain_ceiling.md) — "kind 별 상한" 표를 믿지 말고 synth() 를 실제 인자로 호출해 경고 확인. 「dur 이 짧아 여유가 있다」는 거꾸로 된 사유다
- [스틸 뽑아 눈으로 보기](feedback_frame_capture.md) — 도형 겹침·라벨 가림·자막 옮겨적기 + **같은 색끼리 붙는 것(흰↔흰 라벨 0px)** + **계열 겹침도 스틸 두 장** + **a>20/a>120 두 번 세기** + **혼합 판별식 `b=r+0.533(g−r)`(자가검증 필수)** + **`check.mjs --fps 30` = mp4 전 프레임**
- [마지막 샷 vs 아웃트로 카드](feedback_last_shot_outro_collision.md) — 카드는 **TOTAL−3000**(2.6 아님) 부터 .vis 를 덮는다. **마지막 줄 `rel < 2.65초` 면 그 줄이 통째로 카드 뒤에서 시작한다**
- [t 로 도는 값 · 도형 겹침은 스윕해서 확인](feedback_t_driven_value_sweep.md) — 문턱 통과 프레임 0 / 넘어도 발화 구간 밖 / 떠다니는 도형 포갬 — 셋 다 스틸로 안 잡힌다
- [라벨 언어는 게임 에셋으로 · 어미 3연속 잣대는 안 서 있다](feedback_label_language_and_ending_rules.md) — ADR-V-10 은 `Assets/` 에 그 이름이 실재하는지로 끝난다 / 「3연속 금지」는 승인 회차에도 위반이 있어 단독 반려 불가

## 반복되는 것
- [작성팀·파이프라인 결함 14종](project_recurring_defects.md) — **⑬`youtube.en.title` 100자 초과 — 게이트가 없고 직전 회차들이 96~99에 몰려 있다(고치는 비용 0)** · **전면 재작성본은 「이전 반려 사유가 새 판본에 살아 있는가」를 0번으로 물어라(ep12s-3 실측)** · **⑫겹침 감사가 같은 색끼리(흰↔흰 라벨)를 안 잰다 — 4단 전환의 새 kind(훅·nextpeek) 스틸 필수** · **⑭아웃트로 소개 줄 문안이 시리즈에서 한 편만 갈린다(게이트는 「개발일지」만 본다)** · **⑪예고·미리보기가 「다음 글」을 보고 `order` 의 「다음 편」(이미 분할됨)을 안 본다 — 음성·카드·그림 셋을 따로 검산** · **⑩재제작 회차는 `build/` 의 mp4·srt·upload 가 옛 판본으로 남는다(mtime 대조 · srt 항목 수 세기)** · **⑨「카드 전에 끝난다」주석이 옛 `rel` 로 계산됨(자막 길이 바꾼 회차는 cue 전수 재검산)** · 자막 옮겨적기 · notes 자기보고 오류 · **sfx 답습(「처음 쓰는 값」 열거는 거짓인 적이 많다 — 기계로 다시 훑어라)** · 형제 편 대조 누락 · 원문이 안 붙인 분류를 화면이 주장 · **훅 재사용 시 앞 편 요약이 틀림** · **분할 회차 blurb 형제 링크 누락 — 🔴반려가 형제 편에 전파 안 됨, 트리오를 한꺼번에 훑어라** · **`why` 가 ease 입출력을 섞음**
