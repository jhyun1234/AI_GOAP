# Memory Index

## 검수 방법
- [cue/sfx 물림 실측 계산법](feedback_cue_timing_audit.md) — rel dur = wav dur + pauseAfter(양쪽이 반복해 틀림), ease 역함수는 이분탐색(0.9→0.8065), **cue(i) 는 범위 밖을 0 이 아니라 마지막 줄로 클램프**
- [sfx 피크 천장은 인자마다 다르다](feedback_sfx_gain_ceiling.md) — "kind 별 상한" 표를 믿지 말고 synth() 를 실제 인자로 호출해 경고 확인. 「dur 이 짧아 여유가 있다」는 거꾸로 된 사유다
- 🔴 [sfx 상관은 승인 회차 분포 안에서 판정한다](feedback_sfx_correlation_baseline.md) — 판정선 **0.85 는 명세 본문이 정본** · **절대 표본 정렬**(리샘플 금지) · **작성팀의 「대체 척도」는 반드시 상관으로 검증**(`freq×dur` 은 ep16s-2·ep16s-4 두 번 반박 · tick 실효축은 **freq 하나** · 🔴코디네이터가 그 척도를 지시해도 재서 근거를 바꿔 적게 하라) · 통제군 실측(latch 중앙 0.760/95% 0.815 · tick 중앙 −0.022/95% 0.564 · **riser 최대 0.31** · **sweep 깨끗한 통제군 ≤0.037 — 오염원은 seed 미지정 4건=기본11**)
- [렌더 후 검증은 mp4 를 자른다](feedback_render_output_audit.md) — **클립 클램프 여유 = 보고된 발화 여유 − SHOT_TAIL 0.35(S1 은 0.041초뿐이었다)** · 정적 경고는 **원본 클립을 따로 재야** 「얼음 vs 고요한 컷」이 갈린다 · 게임 스프라이트가 엔진 HUD 를 덮는다
- [스틸 뽑아 눈으로 보기](feedback_frame_capture.md) — 도형 겹침·라벨 가림·자막 옮겨적기 + **같은 색끼리 붙는 것(흰↔흰 라벨 0px)** + **계열 겹침도 스틸 두 장** + **a>20/a>120 두 번 세기** + **혼합 판별식 `b=r+0.533(g−r)`(자가검증 필수)** + **`check.mjs --fps 30` = mp4 전 프레임**
- 🔴 [형제 편 `scene.json` 하나가 깨지면 분할 회차 전부의 `check.mjs` 가 죽는다](feedback_sibling_json_breaks_gate.md) — 「형제 라벨」 게이트가 형제 파일을 직접 파싱(`check.mjs:63`) · 원인 1순위는 **`notes` 안 검수 인용문의 `*"…"*` 미이스케이프** · 형제 파일은 고치지 말고 **게이트만 손으로 재현**
- [마지막 샷 vs 아웃트로 카드](feedback_last_shot_outro_collision.md) — 카드는 **TOTAL−3000**(2.6 아님) 부터 .vis 를 덮는다. **마지막 줄 `rel < 2.65초` 면 그 줄이 통째로 카드 뒤에서 시작한다**
- [t 로 도는 값 · 도형 겹침은 스윕해서 확인](feedback_t_driven_value_sweep.md) — 문턱 통과 프레임 0 / 넘어도 발화 구간 밖 / 떠다니는 도형 포갬 — 셋 다 스틸로 안 잡힌다
- 🔴 [sfx 파형 상관 · fps30 결정성 대조군](feedback_waveform_and_determinism_probes.md) — latch·sweep 은 **seed 만** 개성(같은 seed 0.996 vs 대역 **0.63~0.80** · 판별선은 「0.99대 + 최대차 0.02 미만」 · **절대 표본 정렬**) · `--fps 30` red 는 대조군 먼저 · 닫는 법 = 점선을 `fillRect` 순수 루프로 · **「모양 동일」은 1차 스틸과 PNG 픽셀 대조로 받는다**(Δ>8 이 의도 상자 밖이면 바뀐 것)
- [🔄 **화면 글자는 한국어다(ep13s~)** · 어미 3연속 잣대는 안 서 있다](feedback_label_language_and_ending_rules.md) — **ADR-V-10 뒤집힘(2026-08-10, 해외 0.7%)**: 잣대 = *"한국어로 옮기면 뜻이 상하나?"* → 안 상하면 한국어. 영어는 ①인용 ②기호·단위 ③고유 기술명뿐. **ep06s~ep12s-3 은 옛 규칙(영어)으로 나갔으니 선례 금지** · 영어 자막은 그대로 / 「3연속 금지」는 승인 회차에도 위반이 있어 단독 반려 불가(단 **끝 두 글자 동일 연속**으로 재면 ep02s 이후 전부 ≤3 — 4 면 「제정 이후 최악」으로 마스터에 올린다)

## 반복되는 것
- [작성팀·파이프라인 결함 18종](project_recurring_defects.md) — **⑱작성팀이 「못 잰 것」 자리에 자체 척도를 세워 근거로 삼는다 — 값은 통과해도 근거가 거짓이다(ep16s-2 의 `freq×dur`). 새 축·새 공식을 말하면 그 축부터 재고, 틀리면 「인자는 그대로·근거 문장만」으로 반려** · **⑰코디네이터 후속(tts 실행·siblings 기입·재시드) 뒤 `notes` 자기보고가 거짓이 된다 — `timed.json` mtime < `scene.json` 이면 의심하고, `sfx[].seed` 실값을 `why` 에서 grep 해 옛 seed 서술을 잡아라(ep16s-1 반려 3건 전부 문서)** · **⑯`source` 의 작은따옴표 인용문이 원문에 실재하지 않는다(ep13s-1 「봄이 옵니다」 — 원문·브리프 0회) · 인용 부호쌍 전수 대조 한 줄이 파일에** · **⑮마스터가 「렌더 단계에서 닫아라」고 넘긴 🔴 이 아무도 안 하는 구간에 떨어져 렌더까지 온다 — 렌더 후 검수의 0번은 인계 목록 실행 여부 대조(lf02 sfx delayMs 11개 무변경)** · **⑬`youtube.en.title` 100자 초과 — 게이트가 없고 직전 회차들이 96~99에 몰려 있다(고치는 비용 0)** · **전면 재작성본은 「이전 반려 사유가 새 판본에 살아 있는가」를 0번으로 물어라(ep12s-3 실측)** · **⑫겹침 감사가 같은 색끼리(흰↔흰 라벨)를 안 잰다 — 4단 전환의 새 kind(훅·nextpeek) 스틸 필수** · **⑭아웃트로 소개 줄 문안이 시리즈에서 한 편만 갈린다(게이트는 「개발일지」만 본다)** · **⑪예고·미리보기가 「다음 글」을 보고 `order` 의 「다음 편」(이미 분할됨)을 안 본다 — 음성·카드·그림 셋을 따로 검산** · **⑩재제작 회차는 `build/` 의 mp4·srt·upload 가 옛 판본으로 남는다(mtime 대조 · srt 항목 수 세기)** · **⑨「카드 전에 끝난다」주석이 옛 `rel` 로 계산됨(자막 길이 바꾼 회차는 cue 전수 재검산)** · 자막 옮겨적기 · notes 자기보고 오류 · **sfx 답습(「처음 쓰는 값」 열거는 거짓인 적이 많다 — 기계로 다시 훑어라)** · 형제 편 대조 누락 · 원문이 안 붙인 분류를 화면이 주장 · **훅 재사용 시 앞 편 요약이 틀림** · **분할 회차 blurb 형제 링크 누락 — 🔴반려가 형제 편에 전파 안 됨, 다섯 쌍둥이를 한꺼번에 훑어라. `ep16s` 는 5편 중 4편이 비었고 마지막 편만 채웠다(먼저 나온 편일수록 빈다) · `ep16s-1` 은 산문만 있고 편 번호가 없어 눈으로는 통과처럼 보인다 — 정규식으로만 판정** · **`why` 가 ease 입출력을 섞음**
