---
name: label-language-and-ending-rules
description: 🔄 ADR-V-10 은 2026-08-10 에 뒤집혔다 — ep13s 부터 화면 글자는 한국어(해외 0.7%). 아래 「게임 리포를 열어라」 절차는 ep06s~ep12s-3 판정에만 쓰던 것이고 이제 안 쓴다 · voice.json 「어미 3연속 금지」는 지금껏 일관되게 적용된 적이 없다(승인 회차에 위반이 있다)
metadata:
  type: feedback
---

## 🔄 먼저 — **ADR-V-10 은 뒤집혔다** (2026-08-10, 사용자 지시)

**ep13s 부터 화면에 뜨는 글자는 한국어다.** 잣대도 뒤집혔다:
**"이걸 한국어로 옮기면 뜻이 상하나?"** → 안 상하면 한국어.
영어로 남는 것은 셋뿐 — ①코드·로그를 **그대로 인용**하는 자리 ②기호·단위 ③고유 기술명(`GOAP`).

**Why:** 시청자 국가 분포 실측 **해외 0.7%**. 옛 규칙이 겨냥한 시청자가 0.7% 인데
**라벨은 CC 와 달리 전원이 본다.** 정본 = `Docs/영상_영어권_확장_실행명세서.md` §ADR-V-10(개정).
🔴 **ep06s~ep12s-3 은 옛 규칙으로 승인돼 나갔다 — 선례로 인용하지 마라.** 재렌더도 없다(ADR-V-15).
🔴 **영어 자막(`lines[].en`)은 그대로 산다.** 화면 글자만 뒤집혔다 — 섞어서 반려하지 마라.
🔑 **잣대가 지시서(`scene-reviewer.md`)로 올라갔다.** 원래 이 규칙은 검수팀 지시서에 한 줄도
없었고 **이 메모리에만 있었다** — 그래서 매번 여기서 다시 배워야 했다. 이제 지시서가 정본이다.

## 🟡 아래는 **옛 규칙(ep06s~ep12s-3) 시절의 절차**다 — 보관용

> 지우지 않는 이유: 그 구간 회차를 다시 볼 일이 생기면 **그때 잣대로** 읽어야 한다.
> 🔑 그리고 이 절차 자체가 옛 잣대가 왜 나빴는지의 증거다 — **라벨 하나를 판정하려고
> 다른 리포를 열어야 했다.** 새 잣대는 화면만 보고 끝난다.

### 🟢 그림 라벨이 영어여야 하는가 — **게임 리포를 열어라**

**규칙:** ADR-V-10 의 잣대 *"이게 코드나 인스펙터에 그대로 있을 법한 말인가"* 는 추측하지 말고
`C:\Users\anjyo\AI_GOAP\Assets\` 에서 **그 이름이 실재하는지 확인**해 판정한다.

**Why:** ep12s-2 가 「폭염·한파」를 `HEATWAVE`·`COLD SNAP` 으로 올리고 작성팀 스스로
*"고유명사처럼 한국어로 볼 여지도 있다"* 며 검수 판단을 요청했다. 리포를 열어 보니
`Assets/M0Config/Disasters/Disaster_Heatwave.asset` · `Disaster_ColdSnap.asset` 이 **실재**했다 —
「있을 법한」 정도가 아니라 인스펙터에 그 글자가 그대로 있다. 판정이 1분에 끝났다.
🔑 반대 방향도 같다: 「고유명사는 한국어」 조항은 **C# 에 한국어 리터럴로 실재하는 이름**
(순둥이·농부)을 위한 것이다. `Disaster_Heatwave.asset` 안에 `DisplayName: "폭염"` 이 **있어도**
그건 게임 내 표시 문자열이지 식별자가 아니므로 라벨 언어를 뒤집지 않는다.

**How to apply:** 라벨 언어가 쟁점이면 `ls Assets/M0Config/**` · `grep -rl "<이름>" Assets/`.
파일명·클래스명·슬롯명으로 나오면 **영어 확정**. C# 문자열 리터럴로만 나오면 한국어 쪽.
게임 파일은 **읽기만** 한다(트랙 경계 — 파이프라인 작업은 게임 파일을 안 건드린다).

## 🟡 「어미 3연속 금지」는 잣대가 서 있지 않다 — 단독 반려 사유로 쓰지 마라

**규칙:** `voice.json` rules.1 이 *"같은 어미 2연속까지, 3연속 금지"* 라고 못 박고 있지만,
**이미 승인되어 나간 회차에 위반이 있다.** 이것만으로 반려하지 말고 🟡 + 마스터 판단 요청으로 올린다.

**Why:** ep12s-2 의 첫 세 문장이 편했어**요**/사십이었어**요**/조였어**요** 로 과거 `-었어요`
3연속이었다. 반려하려다 전 회차를 기계로 훑었더니 **`ep07s-2`(승인)** 와 `ep12s-3` 도 3연속이었다
(ep07s-2 = 터졌어요/못 찾아요/검사를 해요). 규칙이 한 번도 이 잣대로 집행된 적이 없는데
한 편만 반려하면 자의적이고, 자막을 고치면 **TTS 재실행 + `delayMs` 전부 재계산**이 따라온다.

**How to apply:** 종결어미를 `-어요 / -거든요 / -더라고요 / -죠 / -고요 / -습니다` 로 분류해
문장 단위(줄 단위 아님 — 한 줄에 두 문장이 흔하다)로 연속을 센다. 3연속이면
①**과거·현재가 섞였는지**(섞이면 덜 뚜렷하다) ②**첫 세 문장인지**(가장 나쁘다) 를 덧붙여 🟡 로 낸다.
고치는 값이 한 낱말임(조였어요 → 조였죠)도 함께 적는다.

### 🔬 채널 전수 캘리브레이션 — **두 지표를 같이 재라** (2026-08-11, ep16s-3)

분류 지표(`-요` 로 뭉치기)만 쓰면 과탐한다. **끝 두 글자 그대로**를 같이 세면 순위가 뒤집힌다.

| 지표 | 최대 연속 | 그 편 |
|---|---|---|
| 어미 **분류** 기준 | 8 | `ep13s-2`(-요) · 그다음 `ep14s-3` 5 · `ep16s-2` 5 · `ep16s-3` 5 |
| **끝 두 글자 동일** 기준 | 4 | `ep01s`(어요) · **`ep16s-3`(었죠)** — 나머지 전부 **≤3** |

🔑 **분류 기준의 `-요` 8연속은 겉보기만큼 나쁘지 않다**(생겨요/이야기예요/만나요는 앞 음절이
달라 억양이 갈린다). **판정선에 가까운 것은 끝 두 글자 동일 연속**이고, `ep02s` 이후 발행분은
전부 ≤3 이다 — 4 가 나오면 「규칙 제정 이후 최악」이라는 사실을 함께 적어 마스터에 올린다.
🔑 **고치는 비용도 함께 재라**: `sfx` 가 안 붙은 줄이면 `delayMs` 재계산이 없고
(`since()` 원점 설계면 더욱), 총 길이 fail(38초)까지 여유가 있으면 `tts.mjs` + `check.mjs`
재실행뿐이다. `ep16s-3` 은 한 낱말(…이었죠 → …이었어요)로 끊겼다.

관련 = [[recurring-script-defects]] · [[cue-timing-audit-method]]
