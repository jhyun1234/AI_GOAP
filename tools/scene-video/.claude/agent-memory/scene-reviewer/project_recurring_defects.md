---
name: recurring-script-defects
description: 회차를 넘어 반복되는 작성팀 결함 9종 — 화면이 자막을 옮겨 적음 · notes 자기보고 어긋남 · 앞 회차 sfx 답습(「처음 쓰는 값」 열거는 거짓인 적이 많다) · 형제 편 그림 대조 누락 · 원문이 안 붙인 분류를 화면이 주장 · 훅 재사용 시 앞 편 요약 오류 · 분할 회차 blurb 상호참조 누락(반려가 형제 편에 전파되지 않는다) · why 가 ease 입출력을 섞음
metadata:
  type: project
---

여러 회차에서 같은 자리가 반복해서 걸린다. 검수 때 이 셋을 먼저 확인한다.

**① 화면이 자막을 옮겨 적는다.**
선례 = ep00s S4 반려 · ep02s S9B 🔴 반려. eptest-m11 에서도 S3 `badPlan`("집에 둔 곡식을
들판에서 꺼내 먹는")이 같은 순간 자막 그대로였다. 반대로 **허용된 형태**도 있다 —
ep02s S10 처럼 화면은 정확한 표기(`2막 · 리셋`), 자막은 그것을 말로 부르는 경우는 통과했다.
경계 = *문장을 통째로* 겹치면 반려, *표기/라벨*을 화면이 쥐고 말이 그것을 부르면 통과.

**② `notes` 의 자기보고를 그대로 믿으면 안 된다.**
작성팀은 `화면이_자막을_옮겨_적지_않게` · `수치_정직성` 같은 자가 점검표를 notes 에 적는데,
항목이 사실과 다른 경우가 있다(eptest-m11 ④번은 S4 목표 카드를 "자막이 안 부른다" 고 했으나
자막이 두 줄 다 불렀다). **표의 각 항목을 자막과 대조해야 한다.** 반대로 "안 만들었다"는
주장(주민 이름·성격 거리값·간호 수치)은 실제로 지켜지고 있었다 — 대조는 하되 편견은 갖지 말 것.

**③ 앞 회차 sfx 파라미터를 답습한다.**
`kinds` 이름·형태는 대조하는데 **sfx 는 대조를 안 한다.** eptest-m11 의 tick 둘은
ep02s 의 tick 둘과 dur 이 똑같았고(0.05 / 0.09) freq 만 조금 달랐다. thud 쌍(짧고 높음 +
길고 낮음)도 ep00i 와 같은 구성이다. 회차별 sfx 표를 뽑아 대조할 것:
`for ep in ...; do jq '.shots[].sfx' episodes/$ep/scene.json; done`

🔴 **작성팀이 `why` 에 적은 「전 회차 훑어 봤더니 이 값은 처음이다」 열거를 절대 인용하지 마라 —
반드시 기계로 다시 훑어라** (ep10s-1 반려 실측, 2026-08-08). 그 회차 `why` 는 tick freq 14개·
dur 7개를 나열하고 「1180 은 어디에도 없다 / 0.052 는 처음」이라 단언했는데, **`ep09s-1 S3` 이
정확히 `dur 0.052 · freq 1180`** 이었다(열거에서 그 줄만 통째로 빠졌고 dur 도 다섯 개가 빠졌다).
같은 `why` 의 sweep 쪽 「dur 0.58 · seed 43 둘 다 처음」도 거짓이었다(ep04s-2 dur 0.58 ·
ep07s-1 seed 43). **열거가 길수록 신뢰가 가는데, 실제로는 길수록 빠진 줄이 는다.**
🔑 **왜 치명적인가 — `tick`·`thud`·`riser`·`drop` 은 `seed` 를 안 받는다.** 인자가 같으면
**파형이 bit 단위로 같다.** ep10s-1 tick 은 ep09s-1 tick 과 길이 2293 샘플이 동일하고 진폭만
0.26dB 달랐다 = 귀로 구별 불가. (`seed` 를 받는 것은 `latch`·`sweep` 둘뿐.)
🔑 인자가 겹치면 **성격까지 겹치는지**도 보라 — 위 둘은 `why` 가 각각 「없던 것이 자리에 앉는다」·
「뻗은 것이 자리를 잡는다」로 사실상 같은 사건이었다. 인자 복사의 방증이다.
🔴 **바로 다음 회차 ep10s-2 에서 또 났다(2026-08-08) — 이건 우연이 아니라 고정 결함이다.**
`why` 가 「현역 thud **여섯 쌍**」·「tick **열한 쌍**」이라 열거했는데 실제는 **thud 11 · tick 14** 였고,
빠진 줄 안에 겹치는 것이 정확히 들어 있었다: `thud freq **168**` = ep04s-3(둘 다 `why` 가 「막힌다」) ·
`tick freq **1520**` ≈ ep05s-1 `1510`(둘 다 「딱 물린다」). **freq 가 겹치면 성격도 겹친다**는 관찰이
두 회차 연속 성립했다. 🔑 `dur` 이 다르면(+45%/+62%) 감쇠가 달라 **반려까지는 아니지만**,
`freq` 만 바꾸면 `delayMs` 재계산이 없으므로 **정정 요구로 내면 비용 0 이다.**
전수 훑기 한 줄:
```
node -e "const fs=require('fs'),p=require('path');(function w(d){for(const n of fs.readdirSync(d,{withFileTypes:true})){const f=p.join(d,n.name);
 if(n.isDirectory())w(f);else if(n.name==='scene.json'){const s=JSON.parse(fs.readFileSync(f,'utf8'));
 for(const sh of s.shots||[])for(const x of sh.sfx||[])console.log(s.id,sh.id,x.kind,'dur',x.dur,'freq',x.freq,'seed',x.seed,'gain',x.gain);}}})('episodes')"
```

🔑 **`tts.mjs` 를 못 돌린 회차의 notes 는 실측 뒤 반드시 낡는다** (ep08s-2 실측).
작성팀이 `notes.길이` 에 「아직 인용할 실측이 없다 · 미정」이라 적고 `notes.효과음` 에
「delayMs 는 산정치, 1차 실측 뒤 다시 낸다」고 적어 두는데, 렌더가 돌고 나면 그 칸들이
그대로 남는다. 같은 파일 안 `notes.길이_미세조정` 은 실측(25.016초)을 인용하고 있어
**한 파일이 자기 모순**이 된다. `outro.siblings` 도 같다 — 「형제 대본이 없어 비웠다」고
써 놓고 실제로는 3개가 차 있는 일이 났다. 🔑 검수는 notes 를 인용하지 말고 **timed.json ·
scene.json 실물**로 판정하고, 낡은 칸은 정정 요구로 따로 묶어 낸다(반려 사유는 아니다).
🔴 **2차본(재제출)은 낡은 칸이 더 는다** (ep09s-2 실측): `outro.siblings` 「일부러 비웠다」가
2개 차 있는 채로 또 났고, `writer.md` 안에서 §3-0 이 1차 실측을 인용하는데 §3-B 는
「`timed.json` 이 없어 인용할 실측이 없다」·발화 「124자」(§3-0 은 130자)로 남아 있었다.
**한 문서 안의 자기모순부터 찾아라** — 1차 대응으로 고친 절과 안 고친 절이 나란히 있다.
🔑 그리고 **예측 `l.dur` 로 푼 `delayMs` 는 2차 실측 뒤 반드시 다시 풀린다.**
ep09s-2 tick 은 예측 4.25s 로 2,050ms 를 냈는데 실측 rel 은 4.504s → 이상값 **2,177ms** 였고,
그 127ms 차이로 소리가 「멈춰서」가 아니라 앞 어절 「잠깐」에 앉았다(허용선 안이지만 `why` 는 거짓).

🔑 **자주 틀리는 자기보고 두 종** (ep08s-1 실측):
- `notes.checks` 의 **「흰색과 강조색을 같은 픽셀 위에서 섞지 않는다」** — surplus 의 흰
  `1,110` 이 초록 빗금 **위에** 그려져 있었다(코드 순서상 빗금 뒤에 텍스트). 스틸을 봐야 보인다.
- `화면이_자막을_옮겨_적지_않게` 의 **「나레이션이 한 번도 안 부르는 것」** 목록 — `ERRORS`·
  `OBSERVE 30 MIN` 을 올려놓고 나레이션은 「에러 하나 없이」·「삼십 분」 을 말했다.
  설계는 통과 형태(라벨 vs 문장)이므로 **결함은 목록 쪽**이다. 목록을 그대로 인용하지 말 것.

**④ 그림 대조표에서 「형제 편」이 빠진다.**
작성팀은 소재가 닮은 **먼** 회차(ep06s·ep07s-1)는 표로 대조하면서, 바로 옆에서 같이 나가는
형제 편은 안 본다. ep08s-3 `sametick` 은 **ep08s-2 에 같은 이름의 kind 가 있는데**
최종 프레임이 「시간축 하나 + 한 눈금 위에 N개가 세로로 쌓인 기둥 + TICK 라벨」로 거의 같았고,
writer.md 의 대조표에 ep08s-2 행이 아예 없었다(그 시점에 8-2 대본이 없었다는 사유가 적혀 있는데
`scene.json` 의 `outro.siblings` 에는 8-2 제목이 이미 채워져 있었다 = 노트가 뒤처진 것).
**분할 회차는 `episodes/ep..-*/kinds/` 를 파일명으로 먼저 교차 확인하고, 이름이 같으면 스틸 두 장을 나란히 본다.**

🔑 **sfx 도 같은 자리에서 빠진다** (ep10s-3 실측). 작성팀은 `dur`·`seed` 를 전 회차에서 훑어
안 쓰인 값을 고르는 데까지는 하는데(그건 잘한다), **kind 의 「성격」이 형제 편과 겹치는지는
안 본다** — ep10s-1 의 `sweep` 이 「통째로 쓸려 나가는 소리」이고 ep10s-3 의 `sweep` 이
「걷혀 나가는 소리」였다. 🔑 **이건 대개 🟡이다** — 그 화면 사건에 맞는 kind 가 6종 중 하나뿐이면
바꾸라고 요구할 것이 없다. 요구할 것은 **대조표에 형제 편 행을 넣는 것**이다.

**⑤ 🔴 원문이 라벨을 안 붙인 낱말을 화면이 **분류**해 버린다. (ep09s-1 반려)**
낱말 자체는 원문에 실재하니 `grep` 대조는 통과하는데, 화면이 그것을 **행·칸 라벨 아래**
놓는 순간 원문에 없는 주장이 된다. ep09s-1 은 원문의 「농사꾼은 밭을 붙들고, 떠돌이는 잘
돌아다니고, 고집쟁이는 시키는 걸 미룹니다」(어느 쪽인지 안 밝힘)를 받아 `M5 JOB` 행에
「농사꾼·떠돌이」를 놓았는데, **셋 다 `Assets/M0Config/Personalities/` 의 성격**이고
직업은 `Assets/M0Config/Jobs/` 의 「농부·목수·요리사·나무꾼·광부·치료사·탐험가」다.
🔑 그 밀스톤 명세서에 **ADR-M7-5「성격 이름이 직업처럼 들리는 혼동 방지」**가 이미 있었다.
🔑 **판정 근거는 게임 리포 실물이다** — `grep -h DisplayName Assets/M0Config/*/*.asset` 한 줄로
갈린다. 원문에 그 낱말이 있다는 것과, 원문이 그 낱말을 그 칸에 넣었다는 것은 다르다.
**표·격자·행 라벨이 있는 그림을 보면 「원문이 이 격자를 말했는가」를 따로 물을 것.**

**⑥ 훅·수치를 앞 회차에서 재사용할 때, 「그 편은 이런 뜻이었다」는 요약이 틀린다. (ep10s-2 실측)**
지침이 **훅 모양 반복을 허용**하므로 재사용 자체는 반려 사유가 아니다(ep06s 가 겹침을 이유로
`0줄` 을 버려 숫자 0개 회차가 된 것이 금지의 계기다). 검수가 볼 것은 **변별 논거의 사실 여부**다.
ep10s-2 는 `0줄` 을 쓰면서 *"ep05s-3 은 「새 기능 하나」이고 이 편은 「반복 가능성」이라 뜻이 다르다"*
고 적었는데, **ep05s-3 의 실제 제목이 「기능이 두 번 늘었는데 코드는 0줄이에요」**(= 횟수+0줄 쌍)라
그 구분이 성립하지 않았다. 🔑 **`youtube.title` 을 직접 열어라** — 작성팀은 그 편의 *본문 인상*으로
요약하고 제목은 안 본다. 🔑 결론은 대개 안 바뀐다(갈리는 것은 훅 모양이 아니라 **소재**다) —
고칠 것은 제목이 아니라 `notes` 의 근거 문장이고, **제시된 대안이 더 겹치지 않는지도 확인할 것**
(ep10s-2 의 대안 「코드를 한 줄도 안 고쳤어요」는 ep05s-3 화면 문자열
「.cs 파일은 한 줄도 추가하지 않았습니다」와 오히려 더 가까웠다).

**⑦ 🔴 분할 회차인데 한국어 `youtube.blurb` 에 형제 편으로 가는 길이 없다. (ep12s-1 반려)**
`outro.siblings` 카드가 생긴 뒤로 blurb 쪽을 빼먹는다. **카드가 생겼다고 면제된 게 아니다** —
`ep08s-1`·`ep08s-3`·`ep10s-1`·`ep10s-2`·`ep10s-3` 다섯 편이 **siblings 와 blurb 줄을 둘 다**
갖고 있다. 형태는 고정이다: `(같은 글에서 나온 3편 중 N편 — …는 X-2편, …는 X-3편)`.
`en.blurb` 의 `part N of 3` 은 있는데 한국어에만 없는 형태로 난다(ep12s 는 **세 편 전부** 빠졌다).
한 줄 전수:
```
node -e "for (const e of ['ep12s-1','ep12s-2']) {const s=require('./episodes/'+e+'/scene.json');
 console.log(e, [...s.youtube.blurb.matchAll(/\d+-\d+편/g)].map(m=>m[0]).join(',')||'없음');}"
```
🔑 **반려로 내되 「이것만 고치면 통과」라고 못 박아라** — blurb 은 mp4 에 안 들어가서
재렌더·타이밍 재계산이 0 이다. 다른 지적과 섞으면 왕복 비용만 는다.

🔴 **반려는 형제 편에 전파되지 않는다 — 트리오는 한꺼번에 봐라** (ep12s-3 검수, 2026-08-08).
`ep12s-1` 을 이 사유로 반려했는데도 **`ep12s-2`·`ep12s-3` 이 같은 결함을 그대로 갖고 올라왔다**
(셋 다 `en.blurb` 의 `part N of 3` 은 있고 한국어에만 없는 같은 형태). 작성팀은 회차 단위로
움직이므로 옆 편의 반려 사유를 안 받는다. **한 편에서 트리오 공통 결함을 발견하면 그 자리에서
나머지 편도 훑어 마스터에게 「세 편 동시 수정」으로 넘겨라.** 한 줄로 뽑힌다:
```
node -e "const fs=require('fs');for(const e of fs.readdirSync('episodes').filter(x=>/-\d+$/.test(x))){
 const s=JSON.parse(fs.readFileSync('episodes/'+e+'/scene.json','utf8'));
 console.log(e,[...s.youtube.blurb.matchAll(/\d+-\d+편/g)].map(m=>m[0]).join(',')||'❌없음');}"
```
실측 현황(2026-08-08): ep04s-1/-2 · ep05s-1 · **ep08s~ep10s 여덟 편 전부 있음** /
ep05s-3 · ep07s-1(옛 회차) · **ep12s 트리오 셋 다 없음**.

**⑧ `why` 에서 `ease()` 의 출력과 입력을 섞어 적는다. (ep12s-3 관측, 🟡)**
`brk = ease(clamp((c0−0.68)/0.30))` 인데 `why` 는 「brk ≈ 0.20 → c0 = 0.68 + 0.20×0.30 = 0.74」로
적었다 — 0.20 은 **`brk` 가 아니라 ease 의 입력**이라 실제 `brk` 는 `ease(0.20) = 0.104` 다.
sink 문턱이 0.10 이었으므로 「눈에 잡히는 순간」이 아니라 **막 시작한 순간**이었다.
🔑 **결과는 대개 안 뒤집힌다** — 지속음(0.4초대)이면 어택이 시작에, 감쇠가 완성에 닿아 오히려
좋은 배치가 된다. 🔴 **다만 짧은 소리(tick 0.1초대)에서 같은 혼동이 나면 진짜로 빗나간다.**
판정: `ease` 가 한 번 걸린 이름(`brk`·`fall`·`sink`·`rise`)이 `why` 에 숫자로 나오면
**그 숫자가 ease 전인지 후인지 반드시 되물어라.** 정정 요구로 내고 비용은 0(문구만).

**⑨ 🔴 「카드 전에 끝난다」는 주석·notes 가 옛 `rel` 로 계산돼 있다. (ep10s-2 재제작 실측, 2026-08-08)**
작성팀은 `카드 = 자막 시작 + rel + 0.35 − 3.0` 이라는 **맞는 식**을 적어 놓고 **옛 `rel` 을 대입**한다.
ep10s-2 `nextpeek` 은 소개 줄 `rel` 3.3초를 가정해 `linkCue` span 을 0.14 로 골랐는데,
3차 감량으로 그 줄이 20 → 14 발화자가 되어 실측 `rel` 이 **2.467초**가 됐고 — 그 식에 넣으면
**−0.183초(음수)** = **줄 전체가 카드 뒤에서 시작한다.** 이음 바는 **0프레임**이었다.
같은 파일 안에서 **주석은 span 0.20, 코드는 0.14, notes 두 칸도 서로 다른 값**을 적고 있었다.
🔑 **자막 길이를 건드린 회차는 그 샷의 cue 산수를 전부 다시 풀어야 한다** — 값을 안 바꿔도
`rel` 이 바뀌면 결론이 바뀐다(같은 편의 `stripout` split 0.5 는 가리키는 낱말이 세 번 이사했다).
🔑 **페이오프가 아니면 반려는 아니다**(작성팀이 「장식」이라 선언해 뒀으면 더욱). 정정 요구로 낸다.
판별 한 줄 = **마지막 줄 `rel < 2.65초` 면 그 줄의 화면 변화는 전부 안 보인다**
([[last-shot-outro-collision]]).

**How to apply:** 아홉 항목은 `check.mjs` 가 절대 못 본다. 스틸([[frame-capture-for-review]])과
앞 회차 씬 파일 대조로만 잡힌다. 반려 사유로 쓸 때는 반드시 원문 줄·스틸 시각을 같이 적는다.
