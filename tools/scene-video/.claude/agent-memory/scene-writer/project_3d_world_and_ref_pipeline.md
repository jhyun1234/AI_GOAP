# 🧱 그림은 이제 3D 월드에서 그린다 (정본: `notes/3D_전환_인계.md`)

2026-08-12 · ep16s-1 을 여덟 번 다시 만들며 확정. 커밋 `9c77332c` → `f1deab1e`.
**구조·수치·남은 것은 전부 `tools/scene-video/notes/3D_전환_인계.md` 에 있다.**
여기는 대본/그림을 쓸 때 바로 걸리는 것만.

## 🔴 「밋밋하다」는 거의 항상 **그림 어휘** 문제다
한 세션에 여섯 번 지적을 받았고 **다섯 번은 고친 층이 틀렸다** — 카메라·컷·진입모션·
이펙트·촬영박자를 만지는 동안 원인은 늘 그리는 대상 자체였다.
판정 순서를 뒤집어라: **자막·HUD 를 가린 프레임 그리드부터**(명령은 인계 문서 §5).
안 읽히면 연출을 만지지 마라.
🔑 이게 `scene-master` 메모의 「거짓 `reads`」 사각지대를 실제로 재는 방법이다 —
모든 `reads` 가 「라벨을 다 가려도 읽힌다」고 주장해 왔는데 아무도 검증한 적이 없었다.

## 새 kind 를 쓸 때
- 화면 좌표를 쓰지 마라. `_world.js` 의 월드 좌표(x, y, z) + `project()`.
- 형상은 그 물건처럼(동그라미 ≠ 무덤). 주인공은 화면의 **40% 이상**(가이드 최소치).
- 파낸 홈은 **`destination-out`**. `PALETTE.bg` 로 칠하면 경계에서 팔레트 위반 색이 난다
  (v4 에서 다섯 샷이 걸렸다).
- 사람은 **8두신 + 두 마디 관절**. 사타구니가 키의 정확히 절반. 한 마디 막대는
  각도를 줘도 「기울어진 막대」다. 얼굴·손가락은 **구도로 가린다**.
- 무한 루프 금지. 정적 3초 게이트는 **사건**이 넘긴다(카메라가 움직이면 원리적으로 안 걸린다).

## 🔴 되돌린 것 — 다시 제안하지 마라
- 촬영 박자 양자화(`shot.step`, on 2s) → **렉으로 읽힌다.** 손그림 on 2s 는
  매 스텝이 **다른 그림**이라 성립한다. 연속 파라미터를 양자화하면 프레임 드랍이다.
- 흑백 반전 플래시 · 집중선 → **눈이 아프다**(사용자 판정). lib.js 에서도 삭제됨.
- 2D 확대(`camAt`)로 「가까워짐」 표현 → 「커졌다」로만 읽힌다. z 로 움직여라.

## 레퍼런스는 손으로 뜯지 마라
`node tools/scene-video/refs.mjs find "<검색어>" --n 3 --ep <회차>`
검색 → 내려받기 → 실측 → 컨택트 시트 → `episodes/<ep>/notes/refs.md`.
우리 회차를 **같은 함수로** 재서 나란히 놓는다 — 비교가 안 되는 측정은 감상이지 근거가 아니다.
