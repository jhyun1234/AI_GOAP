node_modules/
build/audio/
build/*.mp3
build/compare/
vendor/

# 렌더 산출물 — 대본과 음성만 있으면 그대로 다시 나온다(결정성 보장). 리포에 안 담는다.
build/*.mp4
build/*.track.wav

# 루틴 실행 로그 (기기별 상이)
state/routine.log

# 회차 파생물 (씬 JSON 에서 다시 만들어진다)
build/*.upload.txt
build/*.youtube.json
build/stills/

# 파이프라인 단계별 중간 산출물 (세션마다 새로 남)
.staging/
