# 영상 대본 루틴 — 클라우드용 (사람이 claude.ai 스케줄에 붙여넣는 것)

> # 🛑 2026-08-06 — 이 루틴은 폐기됐다
>
> 사용자 지시로 **크론을 폐기하고 수동 실행으로 전환**했다
> (`Docs/영상_25초_전환_실행명세서.md` W4·ADR-V25-5).
> `state/schedule.json` 이 `mode:"manual"` · `scriptBy:"local"` 이고,
> `publish.mjs` 가 `--routine` 으로 불리면 아무것도 안 하고 끝난다.
>
> ## 🔴 사람이 해야 할 일 — 코드가 못 하는 것
>
> **claude.ai 에 등록된 이 스케줄은 리포와 별개로 도는 사본이다.**
> 리포를 아무리 고쳐도 **그 스케줄은 계속 대본을 만들어 push 한다.**
> 1. claude.ai 의 스케줄 목록에서 **이 루틴을 삭제**한다.
> 2. Windows 예약 작업(`register-task.cmd` 가 만든 것)도 **삭제**한다.
> 3. 삭제 뒤 `claude/scene-*` 브랜치가 더 이상 안 생기는지 하루 지켜본다.
>
> ## 이 파일을 지우지 않는 이유
> ①되살릴 때 필요하다 ②무엇보다 **claude.ai 에 사본이 남아 있을 수 있는데,
> 파일을 지우면 그 사실을 알아차릴 방법이 없어진다.**
> 아래 내용은 **폐기 시점의 기록**이며, 25초 전환(목표 20~25초·예고 1초)은 반영돼 있지 않다.
>
> ## 🔴 되살린다면 먼저 고칠 것 (폐기 후 뒤집힌 규칙)
> - **1-C 「그림 안 짧은 라벨은 영어다」는 폐기됐다** (2026-08-10, 개정 ADR-V-10).
>   ep13s 부터 **화면 글자는 한국어**다 — 계기는 시청자 국가 분포 실측 **해외 0.7%**.
>   영어 자막(`lines[].en`)은 그대로 산다. 정본 = `Docs/영상_영어권_확장_실행명세서.md`.
> - 아래 「이번에 바뀐 것」 표의 첫 줄(`언어 중립(영어)`)도 그래서 **무효**다.
> 🔑 이 블록은 파일을 통째로 고치는 대신 둔 것이다 — 아래는 **폐기 시점의 기록**이므로
> 본문을 고치면 그 기록이 아니게 된다. 되살리는 사람이 여기부터 읽는다.

> 이 파일은 **붙여넣을 프롬프트 원문**이다. 아래 「붙여넣을 것」 절 전체를 복사해
> claude.ai 의 스케줄 루틴에 넣는다. 이 문단과 「왜」 절은 사람이 읽는 설명이라 안 넣어도 된다.

## 🔴 판 `2026-08-06` — 다시 붙여넣어야 한다

**이 프롬프트는 등록 시점의 사본이다. 리포만 고치면 클라우드는 옛 프롬프트로 계속 돈다.**
이번 개정(영어 자막 · 라벨 언어 중립 · 낡은 길이 규칙 제거)은 **재등록해야 효력이 생긴다.**

그래서 아래 「붙여넣을 것」 첫 줄에 **판 날짜**를 박아 두고, 5절이 보고에 그것을 찍게 했다.
클라우드 보고에 `판 2026-08-06` 이 안 보이면 **옛 사본이 아직 돌고 있다는 뜻이다** —
이제는 눈으로 확인할 수 있다.

| 이번에 바뀐 것 | 어디서 왔나 |
|---|---|
| 그림 안 짧은 라벨을 **언어 중립(영어)** 으로 | `Docs/영상_영어권_확장_실행명세서.md` W0 · ADR-V-10 |
| `lines[].en` · `youtube.en` 을 **작성팀이 함께 채운다** | W2 · ADR-V-16 (범위 개정 — 아래 참조) |
| 영어 자막 게이트 2종이 `check.mjs` 에 있다 | W4 |
| 🔴 **낡은 길이 규칙 삭제** — "상한 90초" · `ep04s2` id 규약 | 이탈률 명세 W3 이 이미 기각한 것이 여기만 남아 있었다 |

> 🔄 **W5 결정을 뒤집는다 (2026-08-06, 사용자 지시).**
> W5 는 *"클라우드 프롬프트를 손대지 않는다 — 재붙여넣기라는 사람 몫이 늘고 번역 품질은
> 어차피 사람이 봐야 하니 자동화 이득이 작다"* 였고, 번역을 **세션에서 손으로** 채우기로 했다.
> **왜 뒤집나**: ①그 분담이면 클라우드가 쓴 회차는 매번 `en` 이 빈 채로 나오고, W4 게이트는
> "하나도 없으면 skip" 이라 **조용히 통과한다** — `upload.txt` 경고 하나가 유일한 방어선이다.
> ②사람 몫이 준 게 아니라 **옮겨졌을 뿐**이다(재붙여넣기 1회 vs 회차마다 번역).
> ③재붙여넣기가 필요한 건 맞지만, 위 판 표시로 **누락을 관측할 수 있게** 만들었다.
> 🔑 남는 것은 그대로다 — **직역인지 아닌지는 여전히 사람이 본다**(ADR-V-18 · S7).

## 왜 클라우드인가

영상 파이프라인은 반씩 나뉜다.

| 절반 | 어디서 | 왜 |
|---|---|---|
| **대본** (기획→작성→검수→마스터) | **클라우드** | Claude 만 있으면 된다. PC 가 꺼져 있어도 쌓인다 |
| **렌더** (음성→mp4→점검) | **로컬 PC** | TTS 모델 645MB · 크로미움 · ffmpeg 가 이 PC 에만 있다 |

블로그가 PC 없이 발행되는 것과 같은 구조다. 대본이 브랜치로 올라오면
`.github/workflows/scene-script-auto-merge.yml` 이 main 에 머지하고,
로컬 스케줄러(로그온 3분 뒤 · **매일 15:00**)가 `git pull` 해서 밀린 것을 렌더한다.

🔴 **로컬은 클라우드보다 뒤에 와야 한다.** 옛 등록이 격일 09:00 이었는데 클라우드가
09:09 에 시작해 11:04 에 push 해서, 로컬이 9분 먼저 돌고 가 매 회차가 하루씩 밀렸다.
박자는 언제나 **대본을 만드는 쪽**이 쥔다 — 로컬에서 또 잠그면 박자가 두 군데가 되고,
하루 어긋나면 영영 하루씩 밀린 채로 간다.

⚠️ **켜기 전에 `state/schedule.json` 에 `"scriptBy": "cloud"` 를 넣어야 한다.**
안 넣으면 로컬도 대본을 만들려 들어 **양쪽이 같은 회차를 두 번 쓴다.**

## 클라우드가 못 하는 것 (알고 시작할 것)

크로미움이 없으면 **검수팀이 픽셀을 못 본다.** 그러면 좌우 잘림 · 정적 구간 · 팔레트 ·
하단 안전영역이 클라우드에서 안 잡힌다. **대신 로컬이 렌더할 때 `check.mjs` 전 항목이
잡고, 실패하면 되돌림 루프가 고친다.** 즉 늦게 발견될 뿐 새어 나가지는 않는다.

🔴 **그리고 TTS 모델이 없으니 `build/timed.json` 이 절대 안 생긴다.** 그래서 클라우드에서
`check.mjs` 를 돌리면 「실측 타임라인 존재」가 **언제나 red 다.** 그건 정상이고 반려 사유가
아니다 — 그 자리에 「산정 길이 (참고 · 판정 아님)」 한 줄이 대신 찍힌다.
🔴 **그 산정치로 길이를 판정하지 마라.** 그리고 **길이를 재려고 합성 `timed.json` 을 만들지
마라** — 남으면 다음 `check.mjs` 가 지어낸 값으로 길이 게이트를 통과시킨다.

원문 대조·사실 확인·다음 편 예고·**영어 자막 대조** 같은 **글 수준 검수는 클라우드에서
그대로 된다.** 영어 자막 게이트 2종(전 줄 존재 · 2줄 상한)은 파일만 보므로
크로미움 없이도 돈다. **`제목 이행`·`제목 후킹 (수치)` 도 마찬가지다** —
`scene.json` 만 읽으므로 클라우드에서 그대로 돌고, 여기서 red 면 그 자리에서 고쳐야 한다.
🔴 **제목은 클라우드가 마지막으로 만질 수 있는 자리다.** 로컬 렌더 단계에서 제목을
고치면 대본 첫 두 줄과의 이행 관계가 깨진다(`제목 이행` 게이트가 같이 무너진다).

## 주기 — **하루 두 편** (2026-08-03 사용자 지시로 하루 1편 → 2편)

```
0 0,12 * * *   (UTC 00:00·12:00 = KST 09:00·21:00, 매일 두 번)
```

🔴 **여기서 크론이 본체다.** 게이트만 낮추고 크론을 하루 한 번으로 두면
게이트를 0으로 만들어도 **여전히 하루 한 편이다.** 둘을 함께 바꿔야 한다.

박자는 프롬프트의 **0-A 게이트**가 잡는다. 크론이 하루 두 번 도니 게이트를
0.5일에서 **0.25일(6시간)** 로 낮춘다 — 같은 회차가 두 번 도는 것만 막고
12시간 간격의 두 실행은 통과시킨다.

🔑 왜 이 변경이 필요했나: 실측 절대 시청 시간이 16~27초인데 영상이 80~122초였고,
한 글에 사건이 둘이면 제목이 약속한 장면이 30초 뒤에 나왔다. 그래서 회차를
**사건 단위로 30~45초씩 쪼갰고**(Docs/영상_이탈률_개선_실행명세서.md §W1),
글 하나가 2~3편이 되어 발행 박자를 올릴 재료가 생겼다.

⚠️ **하루 2편은 재료가 있을 때만이다.** 공급(격일 1글 × 2~3편 = 하루 1~1.5편)이
소비(하루 2편)보다 적어 구조적으로 적자다. 밀린 미발행 소재가 완충재라 2~3주는
버티고, 마르면 `backlog.mjs` 가 `IDLE` 을 내고 그날은 1편만 나간다.
**그건 사고가 아니다**(ADR-V-6). 아무것도 고치지 마라.

🔴 **크론 숫자로 주기를 표현하려 하지 마라.** 격일이 다시 필요해져도 `0 0 */2 * *` 는
1·3·5…31일이라 **31일 다음이 1일이라 이틀 연속으로 돈다.** 주기는 언제나 여기 게이트에서,
달력이 아니라 **경과 시간**으로 잰다. 실행이 하루 밀려도 다음 날 알아서 따라잡는다.

🔑 **매일이라고 매일 만들어지는 건 아니다.** 블로그가 격일 발행이므로, 순서표에 쌓아 둔
회차를 다 소진하고 나면 실제 산출은 **블로그가 글을 내는 속도**를 따라간다. 재료가 없는
날은 0절이 "만들 회차 없음"으로 조용히 끝낸다 — 그게 정상이고, 새 글이 나오면 저절로 재개된다.

블로그 루틴(짝수일 13:03)과는 시각이 겹치지 않는다.

## 커넥터 — 붙이지 마라

이 루틴이 쓰는 것은 **git 저장소 하나**뿐이다. Google Drive·Notion 같은 커넥터를 붙이면
에이전트가 실행 중 **권한을 묻지 않고 쓰기까지 할 수 있다**(생성 화면의 경고 그대로).
대본을 쓰는 데 필요 없는 권한이므로 **전부 제거한다.**

---

## 붙여넣을 것 (여기서부터 끝까지)

프롬프트 판: **2026-08-06**

리포 `jhyun1234/AI_GOAP` 에서 **다음 회차의 영상 대본**을 만들어라.
대본은 **한국어 자막과 영어 자막을 함께** 만든다(1-B 절).

### 0-A. 간격 게이트 — 하루에 두 편
마지막으로 대본이 만들어진 때를 **git 이력**에서 본다:
```
git log -1 --format=%cI -- $(node -p 'require("./tools/scene-video/state/schedule.json").order.map(i=>"tools/scene-video/episodes/"+i+"/scene.json").join(" ")')
```
그때로부터 **0.25일(6시간)이 안 지났으면 아무것도 하지 말고 "아직 이르다"만 보고하고 끝낸다.**
커밋도 하지 마라.

🔑 **0.25일인 이유**: 목표는 **하루 두 편**이고 크론은 12시간 간격으로 돈다. 그러니 이
게이트가 막아야 하는 것은 "두 번째 실행"이 아니라 **같은 실행이 두 번 도는 것**
(재실행·수동 실행)뿐이다. 0.5일로 두면 21:00 실행이 몇 분만 늦어져도 그 편을 통째로
건너뛴다 — 격일 시절에 실제로 겪은 밀림과 같은 종류의 사고다. 12시간 간격에 6시간
게이트면 양쪽으로 반씩 여유가 있다.

🔴 **`order` 에 있는 회차만 본다 — 시리즈 밖 특별편은 박자를 먹지 않는다.**
그래서 `episodes/*/scene.json` 같은 넓은 글롭을 쓰지 마라. 회차 목록은 `schedule.json` 의
`order` 가 정본이고, 0절에서 회차를 고를 때 쓰는 기준과 같은 것을 쓰는 셈이다.

🔑 **왜 좁혔나(2026-08-01 사용자 판정)**: 처음엔 넓은 글롭이었는데, 손으로 만든 특별편
`ep00i` 가 게이트를 물어 `ep03s` 착수가 **하루 밀렸다** — 크론이 하루 1회라
15.6시간 차이가 하루가 된다. **시리즈 박자는 시리즈가 정한다.** 특별편은 본편과 별개로
아무 때나 만들 수 있어야 하고, 그것 때문에 본편이 밀리면 안 된다.

🔑 **왜 크론이 아니라 여기서 재나**: 크론으로는 격일을 제대로 표현할 수 없다.
`*/2` 는 1·3·5…31일이라 **31일 다음이 1일이라 이틀 연속으로 돈다.**
달력에 기대지 말고 "마지막으로 만든 때로부터 며칠"을 직접 본다 —
로컬 렌더 쪽도 같은 이유로 같은 방식을 쓴다. 실행이 하루 밀려도 알아서 따라잡는다.
⚠️ 파일 mtime 을 쓰지 마라. 새로 clone 하면 전부 체크아웃 시각이라 쓸모가 없다. **git 이력이 정본이다.**

### 0. 할 회차 정하기 — 스크립트가 정한다

```
node tools/scene-video/backlog.mjs --extend
```

이 한 줄이 판정과 순서표 갱신을 함께 한다. 출력의 **상태**만 보고 갈라라.

| 상태 | 뜻 | 할 일 |
|---|---|---|
| `PENDING` | 순서표에 아직 대본 없는 회차가 있다 | 그 회차로 1절 진행 |
| `EXTENDED` | 순서표를 다 만들어서 **새 블로그 글 한 편을 회차로 편입했다** | 그 회차로 1절 진행 |
| `IDLE` | **재료가 없다** | 아무것도 하지 마라. "만들 회차 없음"만 보고하고 끝낸다. **커밋도 하지 마라** |

🔴 **`IDLE` 은 사고가 아니다.** 순서표를 다 만들었고 아직 영상이 안 된 발행 글도 없다는 뜻이다.
블로그가 다음 글을 발행하면 그다음 실행이 `EXTENDED` 로 저절로 재개된다.
**억지로 소재를 찾지 마라** — 미발행 밀스톤을 앞질러 만들거나, 이미 만든 회차를 다시 쓰거나,
번외를 마음대로 끌어오는 것 전부 금지다.

- ⚠️ `state/uploads.json` 은 보지 마라. 그건 로컬이 렌더한 기록이라 클라우드와 박자가 다르다.
  **기준은 오직 `scene.json` 의 존재 여부다**(스크립트도 그것으로 판정한다).
- 🔑 `EXTENDED` 로 새로 편입된 회차는 **`Docs/영상_시리즈_구성안.md` 표에 행이 없다**(그 문서는
  1~13편 + 번외까지만 다룬다). 막·주제는 원문에서 읽어라. 구성안이 없다고 SKIP 하지 마라.
- 🔑 편입 줄에 `⚠️ 공개 URL 미확인` 이 붙었으면 `tools/blog-automation/BLOG_COVERAGE.md` 나
  `tools/blog-automation/state/blog_last_published_commit.md` 에서 그 글의 공개 URL 을 찾아
  `schedule.json` 의 `url` 을 채워라. 영상 설명의 블로그 링크가 거기서 나온다.
  못 찾으면 `null` 로 두고 그 사실을 보고에 적어라 — **지어내지 마라.**

### 1. 대본 만들기
`tools/scene-video/routine-prompt.md` 의 절차를 그대로 따른다. 4개 부서를 순서대로:
`scene-planner` → `scene-writer` → `scene-reviewer` → `scene-master`.
각 단계 산출은 반드시 `tools/scene-video/episodes/<id>/notes/` 에 파일로 남긴다
(서브에이전트는 무상태라 대화로 못 물려받는다).

정본 문서를 먼저 읽어라 — `routine-prompt.md` 와 `.claude/agents/scene-*.md` 에
길이 규칙·다음 편 예고·효과음·그림 소유·**라벨 언어**·**영어 자막** 규약이 전부 들어 있다.
🔑 그 파일들은 **리포에 있으므로 항상 최신이다.** 이 프롬프트와 어긋나면 **리포가 이긴다.**

### 1-B. 영어 자막도 같은 회차 안에서 함께 쓴다 (ADR-V-16)

**영상은 하나다.** 더빙도 번인 자막도 한국어로 나가고, 영어권 시청자는 **CC 를 켜서** 본다.

🔴 **영어 대본 파일(`scene.en.json`)을 만들지 마라**(ADR-V-9 폐기). 영어는 `scene.json` **안에**
산다 — 줄마다 `en`, 그리고 `youtube.en`(title·blurb). **파일 개수가 안 늘어야** `backlog.mjs` 가
새 파일을 "대본 없는 회차"로 오해하지 않는다. 커밋 경로도 그대로다(3절 무변경).

```jsonc
"lines": [{ "text": "…\n…", "say": "…", "en": "…\n…", "pauseAfter": 150 }],
"youtube": { "title": "…", "blurb": "…", "tags": [], "en": { "title": "…", "blurb": "…" } }
```

지켜야 할 것 — 상세는 `.claude/agents/scene-writer.md` 의 「영어 자막」 절이 정본이다.

- 🔴 **`text` 를 번역한다. `say` 가 아니다**(`say` 는 숫자를 소리대로 푼다).
- 🔴 **`\n` 으로 2줄 이하.** 3줄이면 `check.mjs` red — 그림을 덮는다.
- 🔴 **전부 채우거나 하나도 안 채우거나.** 일부만 있으면 red 이고 `srt.mjs` 가 멈춘다.
  ⚠️ **0줄이면 게이트가 조용히 건너뛴다**(옛 회차용 규칙). 즉 **번역을 빼먹어도 green 이 뜬다** —
  검수팀이 `en` 0줄을 발견하면 그것만으로 반려해라.
- 🔴 **이름은 로마자가 아니라 뜻으로**(ADR-V-19): 순둥이 → `the easygoing one`,
  고집쟁이 → `the stubborn one`, 농부 → `the farmer`. 화면 라벨은 한국어로 남는다 — 의도한 것이다.
- 🔴 **원문이 가정법이면 영어도 가정법.** 완료형으로 옮기면 **안 난 사고를 난 것으로** 만든다.
- 🔴 **직역 금지.** 영어권 개발자가 쓰는 말로. ⚠️ 이 항목만은 **기계가 못 본다**(ADR-V-18) —
  게이트를 만들지 말고, 애매하면 통과시키지 마라.
- `youtube.en.title` 100자 이하 · 꼬리는 `· Unity GOAP Devlog #N #shorts` ·
  blurb 끝에 `Full write-up (in Korean):` + 원문 링크. **`en` 에 태그는 넣지 마라**(번역 탭에 칸이 없다).

🔴 **`srt.mjs` 를 부르지 마라.** 그건 엔진 타임라인 실측을 읽으므로 **렌더가 되는 로컬에서만**
의미가 있고, 산출물은 `build/`(gitignore) 다. 너는 `en` 만 채우면 된다.

### 1-C. 그림 안 짧은 라벨은 영어다 (ep06s부터 · ADR-V-10)

판정 잣대 한 줄: **"이게 코드나 인스펙터에 그대로 있을 법한 말인가?"** → 그렇다면 영어.
서술이면 한국어. 상태·축·계기판 이름(`HUNGER`·`TASK POOL`·`ORDER`)은 영어,
자막 전체와 판정 서술(`note`·`quote`)은 한국어, **고유명사(`순둥이`·`농부`)도 한국어**다 —
C# 소스에 한국어 문자열 리터럴로 실재하기 때문이다.
🔴 게이트는 없다(ADR-V-11 — 임계값의 근거가 없다). 작성팀과 검수팀이 지킨다.

### 2. 이 환경에서 못 하는 것
- 🔴 **렌더하지 마라.** `tts.mjs` · `render.mjs` · `publish.mjs` 를 부르지 마라.
  크로미움도 ffmpeg 도 TTS 모델도 없다. 렌더는 로컬 PC 가 한다.
- 🟡 **`check.mjs` 는 일단 돌려 봐라**(2026-08-04 변경). 예전엔 크로미움이 없어 못 돌았는데
  `lib-node.mjs` 의 `findBrowser()` 가 Playwright 자리(`/opt/pw-browsers/chromium*`)와
  `SCENE_VIDEO_CHROME` 환경변수도 보도록 넓혔고, root 로 도는 컨테이너용 `--no-sandbox` 도
  붙였다. 돌면 픽셀 항목까지 여기서 잡힌다. **"크롬/엣지를 못 찾았다"가 나오면** 그때
  글 수준 검수만 하고 그 사실을 리포트에 적어라.
  🔴 **「실측 타임라인 존재」는 여기서 언제나 red 다** — TTS 모델이 없어 `timed.json` 이
  안 생긴다. **정상이고 반려 사유가 아니다.** 그 자리에 「산정 길이 (참고 · 판정 아님)」이
  대신 찍히는데, **그 값으로 길이를 판정하지 마라.** 합성 `timed.json` 을 만들어 채우는 것도
  금지다 — 남으면 다음 `check.mjs` 가 지어낸 값으로 길이 게이트를 통과시킨다.
  🔑 **길이 정본은 「총 길이 50초 이하」 · 「총 길이 30~45초 권장대역」이고, 실측은 로컬 몫이다.**
  넘칠 것 같으면 자막을 지우지 말고 **편을 나눈다.** 나누는 단위는 챕터가 아니라 **사건**이고
  (ADR-V-1), 글당 최대 3편이며 하한은 없다(ADR-V-2 — 사건이 하나뿐인 글은 안 쪼갠다).
  분할 id 는 **`ep04s-1`·`ep04s-2` 처럼 하이픈+숫자**다.
  🔴 ~~"상한 90초"~~ 와 ~~`ep04s2`~~ 는 **폐기된 규칙이다**(2026-08-06 이 프롬프트에서 삭제).
  실측 절대 시청 시간이 16~27초인데 영상이 80~122초여서 만든 것의 70~80%를 아무도 안 봤다 —
  `Docs/영상_이탈률_개선_실행명세서.md` W3 가 30~45초/50초로 뒤집었는데, 리포만 고치고
  **이 프롬프트가 옛 숫자를 그대로 들고 있었다.** 「크론과 프롬프트는 짝」의 실제 사례다.

  브라우저를 못 찾은 경우 검수팀은 **글 수준 검수만** 한다 —
  원문 대조 · 화면 문자열 사실 확인 · 다음 편 예고 정본 대조 ·
  돌려막기(앞 회차 `kinds/*.js` 와 장치 비교) · 효과음 배치와 절제.
  - 🔴 **말·그림 도착 시각은 인덱스 대조로 끝내지 마라.** `cue` 를 **안** 쓰는 조건
    (`frac(t/N)` 같은 `t` 기반 · `rk > 0.7` 처럼 다른 자막의 진행도로 만든 문턱)이
    언제 참이 되는지 초로 환산해서 대조해라. 이 계열로 **이미 두 번 샜다**
    (ep02s `tripwire` · ep00i `runmark` — 후자는 결말이 자막보다 1.64초 먼저 터졌다).
    이건 크로미움 없이도 되는 검수다 — 계산이지 픽셀이 아니다.
  - 🔴 **효과음 `gain` 상한.** 렌더 로그의 천장 경고는 **로컬에서만** 나오므로 여기가
    유일한 사전 방어선이다. 기본 0.060 이고 상한이 kind 마다 다르다 —
    `tick` 0.065 · `latch` 0.071 · 나머지 넷 0.085 이상 여유.
    넘기면 그 소리만 조용해진다(RMS 정규화가 없애 둔 불균형이 되살아난다).
  - 🔴 **영어 자막 대조.** 게이트 2종(전 줄 존재 · 2줄 상한)은 파일만 보므로 크로미움 없이
    돈다. 기계가 **못 보는** 것을 검수팀이 본다 — `text` ↔ `en` 한 줄씩 대조해
    ①원문에 없는 수치·주장이 번역에만 섞이지 않았는가 ②가정법이 완료형이 되지 않았는가
    ③이름이 로마자가 아니라 뜻인가 ④`say` 를 번역하지 않았는가 ⑤직역으로 읽히는가.
    🔴 **`en` 이 0줄이면 게이트가 조용히 통과시키므로 그것만으로 반려해라.**
  픽셀 판정(잘림·정적 구간·팔레트·안전영역)은 **로컬이 렌더할 때 잡는다** — 그걸 전제로 통과시켜라.
  검수 보고서에 **"픽셀 판정 미실시(클라우드)"** 라고 명시해라.
- 🔴 유튜브에 올리지 마라. 업로드는 언제나 사람이 한다.
- 🔴 `Docs/영상_시리즈_구성안.md` 를 수정하지 마라. 읽기 전용이다.

### 3. 커밋과 push
성공했으면 **대본 산출물만** 커밋한다.
```
tools/scene-video/episodes/<id>/scene.json
tools/scene-video/episodes/<id>/kinds/*.js
tools/scene-video/episodes/<id>/notes/*
tools/scene-video/state/schedule.json      ← 0절이 회차를 편입했을 때만
```
🔴 **다른 경로를 건드리지 마라.** 워크플로우가 이 네 경로만 받고, 벗어나면 머지를 거부한다.
⚠️ `schedule.json` 은 **0절의 `--extend` 가 실제로 고쳤을 때만** 들어간다. 손으로 `everyDays`·
`scriptBy`·기존 매핑을 고치지 마라 — 그건 사람이 정하는 값이다.
`.claude/agent-memory/` 도 이번엔 커밋하지 마라 — 경로 밖이라 브랜치가 통째로 막힌다.

커밋 메시지: `feat(scene-video): <id> 대본 — 클라우드 루틴`

push 는 **현재 작업 브랜치 그대로** 한다(샌드박스는 세션 브랜치로만 push 를 허용한다).
`.github/workflows/scene-script-auto-merge.yml` 이 받아서 main 에 fast-forward 머지한다.

### 4. 멈춰야 하는 조건
- 0절이 `IDLE`(재료 없음) — 커밋 없이 즉시 종료
- 기획팀 `SKIP`(원본 글 미발행 등)
- 검수 반려 3회 / 마스터 반려 3회
멈출 때는 `notes/` 에 사유를 남기고 **`scene.json` 을 만들지 마라.**
`scene.json` 이 없으면 로컬이 "대본 대기"로 조용히 넘어가고, 다음 회차에 다시 시도한다.
**애매하면 통과시키지 말고 멈춰라** — 영상은 글과 달리 되돌리는 비용이 크다.

### 5. 끝나고 보고
한 줄로, 그리고 **반드시 이 형식으로**:

```
판 2026-08-06 · <회차> · <N>샷 <M>자막 · 영어자막 <M>/<M>줄 · 반려 <k>회
```

만들지 않았으면 `판 2026-08-06 · 만들 회차 없음(IDLE)` 처럼 **판 표시는 그대로 두고** 사유를 적는다.

🔑 **판 표시가 이 보고의 목적 중 하나다.** 이 프롬프트는 claude.ai 에 등록된 **사본**이라
리포를 고쳐도 저절로 안 바뀐다. 보고에 `판 2026-08-06` 이 안 보이면 **옛 사본이 돌고 있다는
뜻**이고, 그때 사람이 이 파일을 다시 붙여넣으면 된다. 프롬프트가 낡았다는 사실을
**대본이 이상해진 뒤에 눈치채는 대신 매 실행마다 보이게** 만든 장치다.
