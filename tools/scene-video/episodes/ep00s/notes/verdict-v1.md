# 최종 판정 — ep00s

## APPROVED

- 판정일: 2026-07-29
- 누적 반려: **1회** (1차 4건 → 수정 → 2차 통과 → 마스터 승인). 상한 3회에 도달하지 않음.
- 판정 근거는 **전부 마스터가 직접 재실행·직접 대조한 것**이다. 검수 보고서의 수치를 근거로 인용한 항목은 없다.

---

## 승인 조건 4개 — 직접 재검증

### ① `check.mjs` 실패 0 ✅

마스터가 직접 실행(`node tools/scene-video/check.mjs ep00s`). 11항목 중 **FAIL 0 · ⚠ 1**.

```
OK shot 메타 완비 / kind 파일 존재 / 자막 2줄 상한(27줄) / 큰 쉼 2회(S1,S9)
OK 자막 2초 미만 없음 / 말 속도 6.3자/초 / 픽셀 읽기 71 / 결정성 474프레임 불일치 0
OK 3색 팔레트 / 아래 가장자리 잘림 없음(선언 예외 S5)
⚠  정적 구간 3초 이하   S7 5.8s, S9 4.4s, S10 3.4s
```

⚠ 는 실패가 아니다. 다만 **넘긴 항목이므로 아래 「넘긴 것」 절에 별도 기록**한다.

### ② 수치 표본 3개 — 전부 원문에 있음 ✅

씬 파일이나 검수 보고가 아니라 원문 HTML을 직접 검색해 대조했다.
(원문 = `tools/blog-automation/published/2026-07-09-goap-pathfinding-honest-failure.html`)

| # | 자막 (수치 포함) | 원문 실물 | 판정 |
|---|---|---|---|
| 1 | S7-1 "규칙 문서에 **아홉 번째** 조항을 박았어요" / S7-2 "이동 실패를 성공으로 위장해서 전달하지 않는다" | 24행 "…**아홉 번째 불변 규칙**으로 명시했습니다. \"이동 실패를 성공으로 위장해서 전달하지 않는다\"" | ✅ 문장까지 일치 |
| 2 | S9 caption `ByReason=[Plan=3, Res=0, Path=0, Pre=0]` / 카드 계획3·자원0·경로0·전제0 | 30행 `<blockquote><code>ByReason=[Plan=3, Res=0, Path=0, Pre=0]</code></blockquote>` | ✅ 한 글자도 다르지 않음 |
| 3 | S6-1 "커밋 **여섯** 개 중에, 버그를 지운 건 **두** 개" / S6-2 "나머지 **넷**" | 20행 "M1부터 M6까지 **6개 커밋**" + 21행 1단계 **(M1~M2)** = 이름 붙이기 + 결함 C 몸통 삭제 / 나머지 = M3·M4~M5·M6 **4개** | ✅ 2·4 모두 원문 단계 구분에서 그대로 나옴 |

추가로 확인한 것(표본 외):
- S8-1 "테스트도 두 개" → 23행 EditMode 테스트 2종 신설 ✅
- S8 화면 `Brain.TileX = targetX` · S8-2 "영 건인지" → 32행 원문 그대로 ✅
- S3 "결함 C" · "구조적 결함" → 18행 ✅

**화면이 주장하는 수치는 셋(커밋 2·4 / 규칙 9 / ByReason 3·0·0·0)뿐이고 전부 원문에 있다. 지어낸 숫자 0개.**
S1 `tripCounter` 는 애니메이션 시계가 만드는 값이라 나레이션이 특정 숫자를 주장하지 않는다 —
`notes.counterHonesty` 의 자백과 실제 자막이 일치한다.

### ③ 모든 샷에 `reads`·`source` 가 있고 빈말이 아님 ✅

10샷 전수 확인(`reads` 36~84자 · `source` 111~255자). 전부 원문 인용이고, `reads` 는
"무엇이 어떤 순서로 움직이는가"를 구체적으로 약속한다. 약속과 코드가 맞는지 두 곳을 직접 대조했다.

- **S6 reads** "먼저 자라는 왼쪽 막대가 2, 그 두 배로 자라는 오른쪽이 4" → `bars.js:23 keys=['b','a']`
  (b 가 왼쪽), 씬의 `b.cue 0` / `a.cue 1`. **약속대로다.**
- **S1 reads / `notes.coldOpen`** "0초부터 이미 고장난 상태" → `stage2d.js:60-62`
  `cue(appearCue, appearLead ?? 0.2, 0.4)` 에 `appearLead 9` → 등장 연출이 t=0 에 이미 완료.
  **코드 주석("appearLead 를 크게 주면 등장 연출을 사실상 건너뛴다 — 콜드 오픈용")과 일치.**

### ④ 첫 10초 = 콜드 오픈 + 수수께끼 ✅

`build/ep00s.timed.json`(실제 TTS 실측)에서 마스터가 직접 타임라인을 뽑았다.

```
 0.00s  S1-1  이 주민, 목적지까지 갈 방법이 없어요.
 3.30s  S1-2  근데 게임 두뇌한텐, 도착한 걸로 올라가거든요.
 7.09s  S1-3  이거 하나 고치는 데 커밋 여섯 개 썼어요.   ← 수수께끼 (~10.08s 종료)
```

- **제목 카드 없음.** 첫 샷이 `title` 이 아니라 `stage2d` 다. 0초에 이미 고장난 무대로 시작한다.
- **수수께끼는 7.09초에 던져진다** — "버그 하나에 왜 커밋 여섯 개인가". 10초 안에 들어온다.
- 이 고리는 S6(47.59s / 51.48s)에서 2:4 로 닫힌다. 열고 닫는 지점이 모두 실재한다.
- **마지막 자막(88.54s) = "계획 칸의 저 3은, 다음 편입니다."** 여운이자 예고다.
  ep01s S1B 가 같은 로그에서 **계획 3** 칸을 켜며 시작하므로 손짓이 실제로 받힌다.
  원문 40행의 맺음말("다음 개발일지에서는 … 그 네 갈래 관측이 …")과도 방향이 같다.

전체 91.3초 · 10샷 · 자막 27줄.

---

## 🔴 넘긴 것 — 정적 구간 3건 (승인하되 기록으로 남긴다)

**S7 5.8s · S9 4.4s · S10 3.4s 가 3초 상한을 넘긴 채로 승인된다.**

검수팀 논거("kind 쪽 문제이지 대본 문제가 아니다")를 **그대로 믿지 않고 엔진 코드를 직접 읽어 검증**했다.

| kind | 코드가 줄 수 있는 cue 이벤트 | 근거 |
|---|---|---|
| `rule` (S7) | **2개** — `showCue`, `strikeCue`. `spec.label` 도 `strikeCue` 에 물려 있어 별도 이벤트가 아님 | `rule.js:22-23, 76` |
| `metrics` (S9) | **2개** — `showCue`(reveal), `dimCue`. caption 도 `reveal` 파생 | `metrics.js:34, 45, 88` |
| `title(outro)` (S10) | **2개** — `appear`, `cut`. `mood==='outro'` 분기엔 그 둘뿐 | `title.js:58, 70` |

세 kind 모두 자막 3줄(S10 은 2줄)에 이벤트 2개가 상한인 것이 **코드로 확인된다.**
자막을 더 깎으면 반대쪽 상한(2초 미만)에 걸리는 것도 사실이다 — 27줄 전부 2.32s 이상이라 여유가 없다.

**그럼에도 이것은 예외 승인이지 합격이 아니다.** 기준을 사후에 낮추지 않는다.

- 이번 회차만 넘긴다. **다음 회차가 이 판정을 선례로 인용하는 것을 허용하지 않는다.**
- 근본 해법은 대본이 아니라 엔진이다: `rule` 에 3번째 이벤트(예: 화살표 끝 상자 소멸),
  `metrics` 에 caption 독립 cue, `title(outro)` 에 취소선 이후 이벤트 하나.
  **셋 중 하나라도 들어오기 전까지, 같은 사유의 초과는 다음 회차에서 반려 사유로 올린다.**
- 완화 요인: S9 강조 카드의 숨쉬는 빛(`metrics.js:58`)은 정적 구간에도 계속 돈다.
  S1(진자)·S6(막대 성장)·S8(타자)은 자막이 흐르는 내내 움직인다. 회차 전체가 정적이지는 않다.

---

## 엔진 변경 파급 — 직접 확인 ✅

이번 회차 때문에 엔진이 바뀌었다(`bars.js`) — 다른 회차로 파급되므로 마스터가 직접 재실행했다.

**`node tools/scene-video/check.mjs ep01s` → 통과.** FAIL 0 · ⚠ 1(자막 2초 미만 4줄).
그 ⚠ 는 TTS 길이에서 나오는 기존 항목이고 이번 변경과 무관하다 —
`git diff` 로 `ep01s.json` 변경이 **`guideLabel` 한 줄 추가뿐**임을 확인했다(자막·pause 변경 0).

| 변경 | 파급 판정 |
|---|---|
| `guideLabel` 하드코딩 해제 (`bars.js:81`) | 기본값이 `'AI는 여기까지라고 봤다'` → `'여기까지라고 봤다'` 로 바뀌었으나 `ep01s.json` 이 원문구를 명시 보존. **회귀 없음** ✅ |
| 도장 자동 축소 (`bars.js:101-112`) | `measureText` 는 결정적 → 결정성 474프레임 불일치 0 으로 실증됨 ✅ |

---

## `excluded` 7건 — 되살릴 것 없음 ✅

전부 읽고 원문과 대조했다. 되살릴 항목은 없다.

- **`AbortReason`**(원문 93행이 이름 붙인 용어) — 가장 위험한 제외였으나 사유가 성립한다.
  이 편은 이미 `결함 C` 를 term 샷으로 세웠고, 실물은 S9 caption 의 `ByReason=` 로 화면에 남는다.
  **원문이 이름 붙인 것 중 화면에 흔적조차 없는 개념은 0개.**
- **"이 코드는 곧 지워진다" 예고 제외** — 원문 40행의 맺음말이 다음 편 주제를 직접 지정하고 있으므로
  씬의 엔딩이 원문을 따른 것이다. 0편이 먼저 재생되므로 손해 보는 쪽이 1편이라는 논리도 맞다.
- 도입부 GOAP 설명 제외 — 정적 텍스트 인트로는 가이드 FAIL. 용어는 `hud.aiHook` 이 상시 짊어진다. 타당.
- 5개 층 / 파급 2종 / EditMode 테스트 2종 / Claude Code 협업절 — 전부 뜻이 자막 한 줄로 살아 있거나
  그림이 서지 않는 것들이다. 되살리면 오히려 주인공(결함 C)이 흐려진다.

---

## DRAFT 예외 확인 ✅

원문이 `publish_status: DRAFT` 라 공개 URL 이 없다. `source.url = null` 이 맞다.
`publish.mjs:134` 가 `scene.source?.url || ''` 로 처리하므로 **링크는 안 나가고 설명에 빈 줄이 하나 들어간다** —
씬의 `source.note` 서술이 코드와 정확히 일치한다(줄을 건너뛰는 게 아니라는 정정이 반영됨).
정식 발행 시 `schedule.json.sources.ep00s.url` 과 함께 채우면 된다.

---

## 🟡 발행을 막지 않는 기록 (다음 회차 재료)

1. **`scene.hook`("실패했는데 / 성공이라고 보고했다")이 화면에 한 번도 안 뜬다.**
   `title.js:12, 22` 확인 — `mood === 'outro'` 분기는 hook 을 아예 렌더하지 않는다.
   이 편의 유일한 title 샷이 outro 라 hook 은 죽은 필드다. 이 편에서 가장 좋은 한 줄인데 낭비된다.
   (ep01s 도 같은 구조 — 회차 문제가 아니라 kind 문제다.)
2. **S6 도장 "두 배"가 작은 값(2) 막대 위에 앉는다.** `bars.js:97-98` 에서 `smallCi` 기준으로 위치가 정해진다.
   그 순간(51.48s) 목소리는 "나머지 넷은 전부 감시 장치였어요"라 **"두 배"를 말하지 않는다.**
   옆 막대가 눈에 띄게 2배라 실사용 오독 가능성은 낮다고 보아 통과시킨다.
   다음에 같은 자리를 쓸 때는 자막에 배수를 한 번 얹으면 완전히 닫힌다.
3. **말 속도 6.3자/초** — `check.mjs` 밴드(6.0~7.2)엔 들지만 `voice.json` rule 8 목표(6.5~7.0)보다 낮다.
   대본이 아니라 TTS 설정 건이다.
4. **도장 폭 계산에 글로우 미포함** — `setShadow(ctx, GLOW, 26, 4)` 가 `measureText` 폭에 안 잡힌다.
   "두 배"는 여유가 커서 안 걸린다. 폭이 딱 맞는 문구가 오면 글자는 안 잘려도 글로우가 잘린다.
5. **`02_writer_note.md` 137~144행이 아직 옛 수치(2.9/2.3/2.5초)로 "상한 안에 넣었다"고 적혀 있다.**
   씬 파일 `notes.staticSegments` 는 실측값으로 정정됐으나 작성팀 노트는 안 됐다.
   영상에 안 들어가는 스테이징 문서라 반려하지 않는다. 다음 회차 노트 작성 시 같은 계산 착오
   (= "이벤트 없는 자막 한 줄의 길이"를 정적 구간으로 세는 것)를 반복하지 말 것.

---

## 다음 단계

렌더는 마스터가 돌리지 않는다. 코디네이터가 실행한다.

```
node tools/scene-video/publish.mjs ep00s --prepare
```

유튜브 업로드는 사람이 한다(`publish.mjs` 주석 참조).
`youtube.privacy` 가 `private` 이므로 공개 전 사람 검토가 한 번 더 걸린다.

**🔴 첫 실행 시 확인할 것**: 이 판정은 전부 **엔진 실행 프레임** 기준이다.
`mp4` 인코딩 이후 프레임은 보지 않았다. 헤드리스 캡처가 같은 그림을 내는지 확인이 필요하다.
