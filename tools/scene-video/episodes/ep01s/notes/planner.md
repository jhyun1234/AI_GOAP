# 기획 브리프 — ep01s (재제작)

**회차 선정은 이미 끝났다.** 사용자 지시로 `ep01s`(1편)를 **그림(kinds)만** 전면 재설계한다.
순서표(`schedule.json.order`) 대조는 확인만 — `ep01s` 는 `order` 2번째 자리(`ep00s` 다음)에
이미 있고, 기존 `episodes/ep01s/scene.json` 이 실존하므로 스킵 사유 없음. 브리프 작성으로 진행.

## 회차 식별

- **회차 id**: `ep01s`
- **원본 파일 경로**: `tools/blog-automation/published/2026-07-09-unity-goap-ai-4096.html`
  (`schedule.json.sources.ep01s.local` — 리포에 사본 있음, WebFetch 불필요)
- **공개 URL**: https://gamedevclaude.blogspot.com/2026/07/unity-goap-ai-4096.html
- **글 제목**: `주민이 "철 캘래... 근데 어디 있지?"만 반복하다 계산 4096개를 통째로 날려버린 사연`
  (기존 `scene.json.source.title` 값, 원문 자체엔 별도 H1 제목 태그가 없어 이 제목을 그대로 쓴다)
- **막(act)**: `1막 · 지워질 마을` (구성안 "한눈에 보기" 표 1행, `scene.json.act` 와 동일)
- **출처 커밋**: `["87ac93c", "5448e68", "45d546c", "81d8c76", "4df41cc"]`
  (기존 `scene.json.source.commits` 값 — 원문이 말하는 "5단계" 작업의 실제 커밋 5개.
  원본 html 파일 자체가 리포에 처음 추가된 커밋은 `3729be1`(2026-07-10, 블로그 상태 파일
  리포화 커밋 — 글 작성 자체의 커밋은 아니고 이관 커밋이라 참고용)

## 이 편의 자리 — 1막 1편

- 구성안 "한눈에 보기" 표 1행: `1막 · 지워질 마을 | 1편 | 방향③ GatherIron 무해 봉합`
- 구성안 42~48행, 1막(영상 1~2) 지시 원문 그대로 인용:
  > 이 두 편이 다루는 시스템은 **곧 전부 삭제된다.** 그냥 틀면 시청자가 애써 이해한 구조가
  > 3화에서 사라져 헛수고가 된다.
  >
  > → **1화 도입에 "이 마을은 곧 통째로 지워집니다"를 미리 밝히는 편이 낫다.**
  > 결말을 먼저 말하는 셈이지만, 그래야 시청자가 "왜 지워졌을까"를 궁금해하며 본다.
  - 기존 `scene.json` 은 이 지시를 **엔딩**(S10, "근데 이 마을요, 3편에서 통째로 지워집니다")에서
    수행하고 있다. 도입이 아니라 결말 배치인데, `ep00s` 기획 브리프에서도 이 배치를 그대로
    인정하고 참고했었다(0편이 예고를 반복할 필요 없다는 근거로 삼음). **재제작 시 이 배치를
    바꿀지는 작성팀 판단 — 원래 지시는 "도입"이라 못박고 있으니 재고할 여지는 있다.**
- 구성안 81~83행, 재현 난이도 참고: "1막 두 편이 재현 난이도가 가장 높다. 舊 아키텍처(M0에서
  24,800줄 폐기)라 지금 빌드에 존재하지 않는다." — 게임 화면 자료는 없다(리포에 스크린샷 0건).
  화면 그림은 전부 애니메이션으로 새로 짜는 것이지 실제 화면 재현이 아니다.

## 🎨 재제작 맥락 — 반드시 읽을 것

- 기존 `episodes/ep01s/scene.json` 의 **대본(자막·나레이션 = `lines` 배열의 text/say/pauseAfter)과
  샷 순서는 이미 원문 재독을 거친 2차 개정본이라 품질이 검증됐다.** `scene.json.notes.reread` 에
  그 개정 기록이 남아 있다(2026-07-29 2차 개정 — 원문을 다시 통째로 읽고 "무해(無解)" 이름
  붙이기·ByReason 카드 그리드 형식 변경·깊이와 스프링 모션 3가지를 반영한 결과).
  **재제작 대상은 이 대본이 아니라 그림 어휘다.**
- 기존 그림 11종 — `bars counter gap metrics morph plan rule stage2d term thrash title` —
  은 공용 라이브러리에서 갈라 온 것이라 **다른 회차와 돌려막기가 된다.** 사용자 판정:
  > "비슷한 애니메이션을 돌려막기하는 건 내가 원하는 방향이 아니야. 각 블로그 글의 개성에
  > 맞게끔 매번 새롭게 만드는 게 내가 원하는 그림이야."
- 참고로 `ep00s` 는 재설계 결과 `wire stack dossier fork excise ledger checkpoint channels
  handoff` 9종이 되어 `ep01s` 와 **이름조차 하나도 안 겹친다.** (`episodes/ep00s/kinds/` 확인됨)
  같은 원칙으로 `ep01s` 새 그림도 위 11종·9종 어느 쪽과도 이름·형태가 겹치지 않게 완전히
  새로 설계해야 한다.
- 그림만 바뀌므로 `scene.json` 의 `shots[].lines`(자막·나레이션)는 원칙적으로 보존 대상이다.
  다만 그림 어휘가 통째로 바뀌면 `spec` 필드(각 kind가 요구하는 데이터 구조)도 새 kind에 맞게
  다시 설계해야 한다 — `kind` 이름과 `spec` 스키마는 새로 짜되, 그 샷이 전달하려는 정보
  (`source`/`reads` 주석에 적힌 것)는 유지하는 것이 원칙. 최종 판단은 작성팀 몫.

## 본문 전문

(HTML 태그 제거, 요약 없이 원문 그대로)

---

저는 지금 Unity로 마을 시뮬레이션 게임을 혼자 만들고 있습니다. 이 게임의 주민들은 GOAP(목표지향 행동 계획, Goal-Oriented Action Planning)이라는 AI 기법으로 움직입니다. "철을 모아라"라는 목표만 던져주면, 주민이 스스로 "탐험하고 → 캐고 → 가져온다" 같은 계획을 A*라는 탐색 알고리즘으로 짜냅니다.

이번 개발일지는 겉보기엔 아주 평범해 보이는 목표 하나 — "철이 부족하니 캐러 가라" — 가 실은 AI의 계산 예산을 통째로 집어삼키고 마을을 멈춰 세웠던 이야기입니다. 진단부터 봉합까지 5단계로 정리했습니다.

문제: 주민들이 제자리에서 갈팡질팡했다

방치 테스트 초반, 주민 세 명이 나무와 돌을 각각 30개 넘게 쌓아놓은 뒤였습니다. 그런데 이 셋이 갑자기 멈칫거리더니, 다시 기지로 돌아갔다가, 또 멈칫거리는 걸 반복하기 시작했습니다. 콘솔에는 이런 로그가 찍혔습니다.

Deadlock 감지! FallbackCounter=3, ByReason=[Plan=3,Res=0,Path=0,Pre=0]

Planning 실패 (NoSolutionFound). Goal=GatherIron

Res=0, Path=0 — 자원 예약 문제도, 이동 문제도 아니었습니다. 셋 다 Plan=3, 즉 "애초에 계획 자체를 못 짰다"는 뜻이었습니다. 세 주민 모두 Goal=GatherIron, 철 캐기 목표에서 막혀 있었습니다. 이 프로젝트 내부에서는 이런 상황을 "무해(無解)"라고 부릅니다. 한자 그대로 "풀이가 없다"는 뜻입니다.

GOAP_PERF_LOG라는 진단용 로그를 켜서 다시 재현해보니 원인이 숫자로 드러났습니다.

Agent=a5c9af49 Goal=GatherIron Nodes=4096 Result=NoSolution

이론상 정답은 "탐험하고 세 번 캔다"는 4단계짜리 아주 짧은 계획이었습니다. 그런데 AI는 4096번이나 후보를 뒤지고도 그 짧은 답을 못 찾았습니다.

해결 과정: 5단계로 무해를 봉합하다

1단계 — 진단 도구부터 켜려다가 진단 도구가 먼저 고장 났다.

Nodes=4096이라는 숫자를 보려면 GOAP_PERF_LOG라는 진단 로그를 켜야 했는데, 막상 켜보니 컴파일 자체가 깨졌습니다. 진단용 코드 블록 안에 있던 변수 이름(result)이 아래쪽 코드의 변수 이름과 겹쳐서 나는 오류였습니다. 변수 이름을 바꿔서 컴파일을 되살린 뒤에야 비로소 4096이라는 숫자를 눈으로 볼 수 있었습니다.

2단계 — "왜 4096개나 뒤지고도 못 찾았나"를 숫자로 확정했다.

철이 아직 발견되지 않은 상태에서는 철을 캐는 행동의 진짜 비용이 문맥 배율 때문에 150까지 치솟아 있었습니다. 반면 AI의 "대략 이 정도면 되겠지"라는 추정치는 여전히 9 정도였습니다. 실제 비용의 약 50분의 1 수준이었던 겁니다. 그래서 AI는 귀환하기, 바닥에 눕기, 원시 무기 만들기처럼 상관없지만 값싸 보이는 행동들을 먼저 계속 들여다보다가, 정작 필요한 "철 캐기"에는 닿지도 못한 채 예산 4096을 다 써버렸습니다. "예산을 그냥 늘리면 되지 않나"라는 생각도 들었지만, 이 프로젝트 규칙에는 이미 "NoSolutionFound는 MAX_NODES 인상으로 해결 금지"라는 조항이 못박혀 있었습니다.

3단계 — 철이 아직 안 보이면, 애초에 그 목표를 후보에서 뺐다.

근본 해결(휴리스틱 자체를 고치는 것)은 "플래너 코어"를 건드려야 하는 일이라 손대지 않기로 미리 정해둔 영역이었습니다. 대신 상위 계층에서 아예 무해한 목표가 AI에게 넘어가지 않도록 막기로 했습니다. 목표를 고르는 함수(GatherGoalSelector)에 "이 자원이 지금 발견됐는가"를 나타내는 인자 다섯 개를 추가했습니다. 옛날 버전 함수를 즉시 삭제해서 컴파일을 통째로 깨뜨렸습니다. 그래야 이 함수를 부르는 곳이 정말 한 곳뿐인지 바로 드러나기 때문입니다.

4단계 — 주민이 "철 캘래... 근데 어디 있지?"라고 갈팡질팡하던 지점을 고쳤다.

목표를 고르는 함수 쪽 준비가 끝나자, 이번엔 이 함수를 실제로 부르는 주민의 두뇌 코드(SelectGatherGoalId) 차례였습니다. 나무, 돌, 철, 구리, 식량 각 자원이 맵 위에서 발견됐는지를 한 번 죽 훑어서 확인한 뒤, 그 결과를 목표 선택 함수에 넘기도록 바꿨습니다. 판정 기준은 "발견됐는가, 그리고 남아있는가" 딱 두 가지로 단순하게 유지했습니다.

5단계 — 다시 안 생기게, 테스트와 규칙 문서에 박아 넣었다.

이 계약을 검증하는 자동 테스트 세 개를 새로 만들었습니다. "발견 안 된 자원은 목표로 뽑히면 안 된다", "발견된 자원은 여전히 목표로 뽑혀야 한다", "전부 미발견이면 그냥 아무 목표도 안 뽑고 탐험으로 넘어가야 한다"는 세 가지 시나리오입니다.

AI와 함께 푼 장면

이 프로젝트는 Claude Code와 함께 개발하고 있습니다. 함수 시그니처를 어떻게 확장할지 정할 때, 처음엔 자원 타입마다 bool 값을 일일이 나열하는 대신 "발견 여부를 물어보는 함수 하나"를 델리게이트로 넘기는 방식도 후보로 검토했습니다. 하지만 이 방식은 매 호출마다 작은 메모리 할당이 생겨서, 초당 여러 번 도는 이 게임의 AI 루프에는 부담이 될 수 있다는 이유로 접었습니다. 결국 인자 다섯 개를 그냥 나열하는 단순한 방식으로 확정했습니다.

이 프로젝트의 작업 규칙에는 이런 문장이 있습니다. "명세에 없는 판단이 필요해지면: 구현하지 말고, 선택지와 추천을 정리해 질문하거나 GitHub 이슈로 남긴다." 이번 작업도 코드를 먼저 짜지 않고, "왜 델리게이트 대신 5개 인자인가", "왜 플래너 코어는 안 건드리는가" 같은 판단들을 먼저 문서로 못박아 둔 뒤에 코드를 건드리는 순서로 진행했습니다.

의외의 디테일

이번 작업에서 가장 눈에 띄었던 부분은, 함수를 확장하면서 옛날 버전을 일부러 즉시 지워버린 결정이었습니다. 보통은 옛날 버전을 잠깐 남겨두고 새 버전과 병행하면 컴파일이 안 깨져서 더 편합니다. 하지만 그렇게 하면 아직 안 고친 호출부가 조용히 옛날 방식으로 계속 돌아가면서 눈에 안 띄게 숨어버릴 위험이 있었습니다. 그래서 일부러 컴파일을 깨뜨려서 "이 함수를 부르는 곳이 정말 한 곳인지"를 강제로 드러낸 겁니다.

한 가지 더, 이번 수정이 끝난 뒤 커밋 전에 확인해야 할 체크리스트에 새 항목이 하나 추가됐습니다. 목표 후보를 넓히는 커밋을 올릴 때마다, 새로 만든 회귀 테스트가 초록불인지, 그리고 목표 선택 함수를 부르는 곳이 정확히 한 곳이고 인자 개수가 열한 개가 맞는지 검색해서 확인하는 절차입니다.

왜 이게 중요한가

이번 이야기는 "철이 부족한 초반 몇 초"라는 아주 좁은 상황처럼 보이지만, 사실은 AI가 "겉보기엔 그럴듯한데 실제로는 답이 없는 목표"를 어떻게 다뤄야 하는가에 대한 이야기입니다. 이번 수정은 AI가 애초에 "이건 지금 풀 수 없는 문제"라는 걸 미리 알아채고, 그 대신 할 수 있는 일(탐험하기)로 자연스럽게 넘어가도록 만든 작업이었습니다.

겉으로 보이는 결과는 단순합니다. 아직 발견 못 한 자원을 캐겠다고 갈팡질팡하던 주민이, 그 자리에서 자연스럽게 주변을 탐험하러 나서도록 만들었습니다. 하지만 그 뒤에는 A* 탐색의 예산이 왜 소진됐는지를 숫자로 추적하고, 상위 계층에서 문제의 뿌리를 끊고, 다시 재발하지 않도록 테스트와 규칙 문서로 박아 넣는 다섯 단계가 있었습니다.

다음 개발일지에서는 이번에 손대지 않기로 미리 정해둔 부분 — AI의 "대략 이 정도면 되겠지"라는 추정치 자체가 왜 그렇게 낮게 잡혔는지, 그리고 이 문제를 언제 다시 열어볼지에 대한 이야기를 다뤄보겠습니다.

---

(원문 끝)

## 참고 — 기존 `scene.json` 이 이미 뺀 것 (`excluded` 배열, 그대로 옮김)

새 그림을 짤 때 원문 재독으로 이미 걸러진 판단이니 참고하되, 그림이 바뀌면 판단이 달라질 수
있다 — 강제하지 않는다.

- "1단계 — 진단 도구부터 켜려다가 먼저 고장 났다(변수 이름 충돌)" — 곁가지, 본 줄기를 끊는다는
  사유로 제외.
- "5단계 — 회귀 테스트 3개 신설 / 체크리스트 항목 추가" — 그림이 안 된다(초록불 세 개는 화면상
  정보가 아니다)는 사유로 제외.
- "AI와 함께 푼 장면 — 델리게이트 vs 인자 5개(GC 할당 때문에 접었다)" — ep01 정본(장편)에는
  있었으나 쇼츠 컷에서 제외. 결말 직전 곁줄기라는 사유. 델리게이트 판단은 번외(방법론 편)
  소재로 더 맞다는 의견 있음.
- 콘솔 로그 3줄(Deadlock 감지 / ByReason / NoSolutionFound)은 한 차례 뺐다가 되살림(기존
  S1B) — "핵심 수치 4개 동시 제시"라 카드 그리드 형식으로 표현하는 것이 맞다는 재독 결론.

## 직전 회차

- `ep00s` — 형식 참고용. `tools/scene-video/episodes/ep00s/scene.json` 과
  `tools/scene-video/episodes/ep00s/kinds/`(`wire stack dossier fork excise ledger
  checkpoint channels handoff` 9종)를 구현 참고로 읽을 것.
