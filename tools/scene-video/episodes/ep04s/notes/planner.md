# 기획 브리프 — ep04s

## 🔴 판정: SKIP (BLOCKED) — 원문을 확보하지 못했다. 작성팀은 착수하지 마라.

**이 브리프에는 「본문 전문」이 없다.** 원본 글의 본문을 한 글자도 확보하지 못했기 때문이다.
아래 「원문 확보 실패 기록」이 사유이고, 그 아래는 **다음 실행에서 재조사하지 않아도 되도록**
읽기 전용 정본(구성안 표 · `schedule.json`)에서 확정한 사실만 적어 둔 것이다.

🔴 **작성팀에게**: 이 파일에 「본문 전문」 절이 생기기 전까지 대본을 쓰지 마라.
아래의 막·주제·예고 재료만으로 대본을 쓰면 **원문에 없는 내용을 지어내는 것**이 된다.

---

## 원문 확보 실패 기록

| 항목 | 값 |
|---|---|
| 회차 id | **ep04s** |
| `sources.ep04s.local` | **null** — 리포에 사본이 없다 |
| 공개 URL | https://gamedevclaude.blogspot.com/2026/07/unity-goap-ai-ai-m1.html |
| WebFetch 결과 | **실패 — HTTP 403 Forbidden** |
| curl 결과 | **실패 — `curl: (56) CONNECT tunnel failed, response 403`** |

에이전트 프록시 상태 엔드포인트(`$HTTPS_PROXY/__agentproxy/status`)가 남긴 실패 기록:

```
{
  "ts": "2026-08-03T00:10:36.111Z",
  "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "gamedevclaude.blogspot.com:443"
}
```

즉 **블로그가 아니라 이 세션의 아웃바운드 정책이 `gamedevclaude.blogspot.com` 을 막고 있다.**
`/root/.ccr/README.md` 는 이 경우 "재시도하거나 우회하지 말고 막힌 호스트를 보고하라"고
못 박고 있으므로, 우회 경로(텍스트 프록시·미러 등)는 시도하지 않았다.

**리포 안에도 사본이 없다** — 실측:
- `tools/blog-automation/published/` 현재 파일 15개 중 M1(3편) 글 없음.
- `git log --all --pretty=format: --name-only` 로 **역대 등장한 published 파일명 전수 확인** —
  M1 글은 한 번도 커밋된 적이 없다(삭제된 사본조차 없다).
- 이 클론은 shallow 라 2026-07-31 이전 커밋을 열 수 없다 → 커밋에서 복원하는 길도 없다.

⚠️ **스킵 사유의 성격**: `backlog.mjs` 는 `PENDING / ep04s` 로 정상 판정했고(재확인함),
원본 글도 **발행된 글**이다. 즉 표준 스킵 조건 두 가지(IDLE · 미발행) 어디에도 해당하지 않는
**제3의 차단**이다. 재료가 없어서가 아니라 **재료에 손이 닿지 않아서** 멈춘 것이다.

### 사람이 해야 할 일 (둘 중 하나면 다음 실행에서 바로 PROCEED 된다)

1. **리포에 사본을 넣는다** — 발행된 HTML 을
   `tools/blog-automation/published/2026-07-1x-unity-goap-m1-leisure-command.html` 같은 이름으로
   커밋하고, `tools/scene-video/state/schedule.json` 의 `sources.ep04s.local` 을 그 파일명으로
   채운다. 이러면 표·pre·svg 제거 방어선(정규식 3줄)도 정상 적용된다 — 지금 방식(WebFetch)의
   약점까지 같이 사라지므로 **이쪽이 낫다.**
2. 또는 이 세션의 이그레스 정책에서 `gamedevclaude.blogspot.com` 을 허용한다.

🔑 **2편(ep02s)도 같은 `local: null` 이었고 그때는 WebFetch 가 통했다**
(`episodes/ep02s/notes/planner.md` 에 본문 전문이 남아 있다). 즉 이 차단은 이번 세션의
환경 변화다. 같은 사고가 앞으로 `local: null` 인 회차마다 반복되므로, 1번(사본 커밋)이
근본 처방이다.

---

## 아래는 정본에서 확정한 사실 — 재조사 불필요

읽기 전용 정본 두 개(`Docs/영상_시리즈_구성안.md` 표, `tools/scene-video/state/schedule.json`)
에서만 뽑았다. 원문 본문은 여기에 **한 줄도 섞이지 않았다.**

### 회차 식별

| 항목 | 값 |
|---|---|
| 회차 id | **ep04s** |
| order 인덱스 | `order` 5번째 (ep00s · ep01s · ep02s · ep03s **· ep04s** · ep05s …) |
| 영상 번호 | **4번** (구성안 「한눈에 보기」 표 — URL 대조로 확인) |
| 막 | **3막 · 다시 짓기** |
| 원본 | **3편** ⬅ *이동* (원래 발행 3번째지만 구성안이 4번 자리로 옮겼다) |
| 주제(구성안 표 그대로) | **M1 여가 · 명령과 거부** |
| 원본 파일 | **없음** (`local: null`) |
| 공개 URL | https://gamedevclaude.blogspot.com/2026/07/unity-goap-ai-ai-m1.html |
| 발행 제목 | **미확인** — 원문에 접근 못 함 |
| 출처 커밋 | **미확인** (shallow 클론 + 사본 없음) |
| 직전 회차 | **ep03s** — 참고 구현은 `tools/scene-video/episodes/ep03s/scene.json` 과 `episodes/ep03s/kinds/*.js` (경로 주의: `scenes/<id>.json` 이 아니라 `episodes/<id>/scene.json` 이다) |
| `schedule.json` note | "M1 여가·명령과 거부. 구성안에서 4번 자리로 이동된 편" |

### 🔴 이 회차에서 막이 바뀐다 — ep03s(2막) → ep04s(3막)

- ep03s = **2막 · 리셋** (M0 재설계, 24,800줄 → 5,977줄). 2막은 그 한 편으로 끝난다.
- ep04s = **3막 · 다시 짓기**의 **첫 편**. 3막은 영상 4~10, 일곱 편짜리 구간이다.

구성안이 3막에 대해 따로 적어 둔 지시(55~58행) 원문 그대로:

> ### 3막 · 다시 짓기 (영상 4~10)
>
> 한 편에 기능 하나씩 쌓이는 구조라 **가장 따라오기 쉬운 구간.**
> 여기서 순서를 흔들면 "이미 있던 기능"과 "새로 생긴 기능"이 뒤섞여 바로 어려워진다.

구성안이 이 편의 **자리 이동**에 대해 적어 둔 것(33행):

> - **3편(M1) → 4번 자리로.** 원래 M0 재설계보다 먼저 발행됐지만, 실제로는 재설계 *다음* 이야기다.

⚠️ 이 사실은 대본에서 다룰 가치가 있다 — 이 편의 원본 글은 **발행 순서로는 M0 재설계보다
먼저** 나왔다. 그래서 원문이 "앞으로 M0 재설계를 하겠다"는 식으로 말할 가능성이 있는데,
시리즈에서는 그것이 **이미 지나간 이야기(ep03s)** 다. 원문 확보 후 이 시제 충돌을 반드시
확인해야 한다.

### 직전 회차(ep03s)가 이 편에 대해 이미 예고한 것

`episodes/ep03s/notes/planner.md` 가 작성팀에 넘긴 예고 샷 요약 — **ep04s 는 이 예고를 받는
자리**이므로 톤을 이어받아야 한다:

```
다음 편 → ep04s
막      → 3막 · 다시 짓기   (🔴 2막 · 리셋은 이 편 하나로 끝난다)
주제    → M1 · 여가 · 명령과 거부
한 줄   → 지운 자리 위에, 촌장의 명령을 거부하는 주민이 선다
```

(실제 ep03s 대본이 이 예고를 어떻게 말로 옮겼는지는 `episodes/ep03s/scene.json` 마지막
씬에서 확인할 것 — 그 문장을 ep04s 도입이 받아야 이어진다.)

---

## 다음 편 예고 재료

정본: `schedule.json` 의 `order` + `Docs/영상_시리즈_구성안.md` 「한눈에 보기」 표.
표 행은 **URL 대조**로 찾았다(영상 번호로 세지 않았다).

| 항목 | 값 |
|---|---|
| 다음 회차 id | **ep05s** (`order` 에서 ep04s 바로 다음) |
| 대조에 쓴 URL | `sources.ep05s.url` = https://gamedevclaude.blogspot.com/2026/07/unity-goap.html → 구성안 표 20행의 링크와 **일치** |
| 구성안 표 행 | `| 5 | 3막 | 5편 | M2·M3 생산 체인과 집 |` |
| 막 | **3막 · 다시 짓기** |
| 원본 회차 | **5편** (발행 순서 그대로 — 이동 없음) |
| 주제 | **M2·M3 생산 체인과 집** |
| `sources.ep05s.note` | 없음 |
| 리포 사본 | 있음 — `tools/blog-automation/published/2026-07-16-unity-goap-m2-m3-production-housing.html` |

### 🔴 막은 바뀌지 않는다

ep04s 도 **3막 · 다시 짓기**, ep05s 도 **3막**이다. 막 전환 연출을 넣지 마라 —
막이 바뀌는 자리는 이번 편 **도입**(2막 → 3막)이지 **예고**가 아니다.
예고는 "3막이 계속된다, 이번엔 무엇이 하나 더 쌓이는가"의 박자다.

### 예고 샷 요약 (작성팀이 화면에 올릴 최소 세트)

```
다음 편 → ep05s
막      → 3막 · 다시 짓기  (이번 편과 같은 막 — 전환 아님)
주제    → M2 · M3 · 생산 체인과 집
```

⚠️ **원문의 "다음 글에서는…" 문장을 그대로 옮기지 마라.** 이 편의 원본은 발행 순서상 3편이라,
원문이 가리키는 "다음 글"은 시리즈의 다음 편(ep05s)이 **아닐 가능성이 높다** — 발행 순서로
이 글 다음은 M0 재설계(4편 = 시리즈의 ep03s, **이미 지나간 편**)다. 실제로 ep02s 가 원문
마지막 문장을 근거로 예고를 냈다가 틀린 전례가 있다. 정본은 위 표뿐이다.

---

## 참고 — 이 환경의 제약

- 렌더/TTS/크로미움 없음. `tts.mjs` · `render.mjs` · `publish.mjs` · `check.mjs` 를 부르지 마라.
  길이는 실측이 아니라 산정치이므로 scene 의 `notes.길이` 에 "미실측"이라고 명시한다.
- 유튜브 업로드 금지.
- `Docs/영상_시리즈_구성안.md` 수정 금지(읽기 전용) — 이번 실행에서도 읽기만 했다.
- git 히스토리 shallow (2026-07-31 이후만) — 옛 커밋 조회 불가.
- 아웃바운드에서 `gamedevclaude.blogspot.com` 차단됨(위 참조).
