/* 🔴 폐기된 그림이다. 이 회차는 `onecall.js` 를 쓴다.

   1차 검수 R4: 이 파일의 구도(가로 시간축 1개 + 한 눈금 위의 세로 기둥 + `TICK` 라벨 +
   훑는 표식)가 형제 편 `episodes/ep08s-2/kinds/sametick.js` 와 통째로 같았다.
   갈리는 것이 표식 모양과 「모이느냐/앉느냐」뿐이었고, 하필 두 편 모두 가장 오래 머무는
   페이오프 컷이었다. 8-2 는 이미 전 항목 검수를 마쳤고 `order` 상 먼저 나가므로
   **뒤에 나가는 이 편이 바꾼다**는 판정이었다.

   🔑 대체본이 무엇을 다르게 하는지는 `onecall.js` 의 머리 주석에 있다
   (이동 0 · 눈금 있는 측정 축 없음 · 값을 지는 것이 자리가 아니라 각도).

   ⚠️ **이 파일은 지워야 한다.** 작성팀에 파일 삭제 수단이 없어 비석만 남긴다.
   어느 샷도 이 kind 를 참조하지 않으므로 엔진은 이 파일을 불러오지 않는다.
   혹시 배선되면 조용히 빈 화면이 되지 않도록 build 에서 크게 실패시킨다 —
   아무것도 안 그려진 화면이 정적 검사에서 가장 조용하게 통과한다(check.mjs 의 교훈). */

export default {
  build() {
    throw new Error(
      'ep08s-3/kinds/sametick.js 는 폐기된 그림이다(1차 검수 R4 — ep08s-2 와 구도 중복). ' +
      'onecall.js 를 쓰고 이 파일은 삭제할 것.'
    );
  }
};
