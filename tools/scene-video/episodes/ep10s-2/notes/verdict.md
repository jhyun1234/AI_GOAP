# ep10s-2 마스터 판정서 — 「목수한테 시킨 재료 검사를 지웠더니 2번째 부탁은 코드 0줄이었어요」

판정 2026-08-08 · 마스터(scene-master) · 검수 2차본 + 🟡 6건 반영본

## 판정 — **APPROVED**

누적 반려 **0회**. 승인 조건 넷을 전부 직접 확인했고, 검수 보고를 근거로 삼지 않았다.

| 승인 조건 | 결과 | 내가 한 것 |
|---|---|---|
| `check.mjs` 실패 0 | 🟢 **23종 전부 OK · 실패 0 · 경고 0** | 지금 직접 실행. 검수가 붙인 출력이 아니다 |
| 수치 표본이 원문에 실재 | 🟢 **3/3** | 원문 html 에 `grep` 직접 |
| 모든 샷 `reads`·`source` 실체 있음 | 🟢 **5/5** | 기계 추출 (source 88~205자 · reads 134~186자) |
| 첫 10초 콜드 오픈 + 수수께끼 | 🟢 | 자막 타임라인으로 검산 |

---

## 🔴 직접 재검증한 고위험 5항목

### ① `+11자` 복원분이 원문 축자인가 → 🟢 **둘 다 축자로 실재**

`tools/blog-automation/published/2026-07-21-unity-goap-m8-relationship-ownership-request.html`
을 직접 열어 `grep` 했다. 검수 보고를 인용하지 않았다.

| 줄 | 복원한 말 | `grep` 이 되돌려준 원문 |
|---|---|---|
| S3 | 「**목표 목록엔**」 | `목표 목록에 얹어줬습니다. 나머지는 AI…` |
| S4 | 「**계획을**」 | `재료가 없을 때 나무를 하러 가는 것부터 계획을 스스로 짜는 게 원래 GOAP이…` |

**원문에 없는 문자열을 만든 것이 아니다.** 줄이면서 뺐던 원문 어휘를 되돌린 것이 맞고,
S3 은 되돌리면서 **사실이 더 정확해졌다**(수락이 *어디에* 얹혔는지가 자막에 생겼다).

### ② 새 `sfx.freq` 두 값이 정말 비어 있는 자리인가 → 🟢 **값은 옳다. 🔴 근거표는 여전히 틀렸다**

`episodes/*/scene.json` + `_archive/` 를 스크립트로 **전수**로 세었다(작성팀 표를 안 읽고).

**`thud` = 14건**(이 편 포함). 작성팀 표 13개 값과 **완전 일치**. 🟢
`(unset)`=ep03s(기본값 150 점유) · 112 · 120 · 124 · 125 · 136 · 142 · 156 · 168 · 170 · 178 · 190 · 210.
→ **230 의 최근접은 210 (20Hz · 9.5%)**. dur 0.29 도 점유 13개 안에 없다. 작성팀 주장 그대로다.

**`tick` = 18건**(이 편 포함 · 자기 것 빼면 **17**). 🔴 **작성팀 표는 16개이고 두 군데가 틀렸다.**

| 결함 | 실제 |
|---|---|
| **누락 1건** | `ep05s-2 S3` = **freq 1080 / dur 0.07** — 표에 아예 없다 |
| **오귀속 1건** | 표는 `1180 (ep09s-1, **ep10s-1** ← 형제 편)` 이라고 적었으나, **`ep10s-1 S2` 의 실제 값은 `freq 2320 / dur 0.072`** (`ep10s-1/scene.json:124-126`). 1180 은 ep09s-1 하나뿐이다 |

🔑 **그래도 `1830` 은 살아남는다 — 내가 참값으로 다시 쟀다.**
참 점유 = 880·960·1050·**1080**·1180·1250·1380×2·1420·1510·1650·1660·1760·1900·1980·2100·**2320**.
- **1830 의 최근접은 1760(−70Hz)과 1900(+70Hz)** — 작성팀이 주장한 「최근접 70Hz」가 그대로 성립한다.
- 형제 `ep10s-1` 과의 거리는 **표가 주장한 +55% 가 아니라 실제로는 490Hz(+26.8%)** — 주장보다 **더 벌어져 있다.**
- `dur 0.068` 도 참 점유(0.04~0.09, 0.072 포함)에 없다.

**결론: 렌더되는 소리는 옳고, 그 소리를 고른 근거 문서가 틀렸다.**
→ 아래 「넘긴 채 통과시킨 항목」 ①.

### ③ `0줄` 제목 재사용 → 🟢 **유지가 옳다. 검수 근거는 사실이었다**

`episodes/ep05s-3/scene.json` 을 직접 열어 두 주장을 확인했다.

- **37행** `youtube.title` = 「**기능이 두 번 늘었는데 코드는 0줄이에요** · 유니티 GOAP 개발일지 5-3편 #shorts」 → 🟢 검수 주장대로다. 작성팀 초고의 「ep05s-3 은 새 기능 하나」라는 변별은 실제로 성립하지 않으며, 그 정정이 `notes.제목_후킹` 에 반영돼 있다.
- **131행** `spec.csNote` = 「**.cs 파일은 한 줄도 추가하지 않았습니다**」 → 🟢 실재한다.

따라서 준비돼 있던 대안(「코드를 한 줄도 안 고쳤어요」)은 **ep05s-3 의 화면 문자열을 옮겨 적는 꼴**이 되어
겹침이 **줄지 않고 늘어난다**. 검수의 「대안이 오히려 더 겹친다」는 **직접 확인한 사실**이다.
🔴 게다가 지침이 「앞 편과 훅이 겹쳐서 뺐다」를 금지한다(ep06s 가 그 사유로 숫자 0개 회차가 됐다).
**제목을 바꾸라고 요구하지 않는다.**

### ④ 아웃트로 카드가 페이오프(`CODE 0`)를 덮는가 → 🟢 **안 덮는다. `secondcard.js` 를 읽고 직접 검산**

`rel` L6=2,961 · L7=2,832 · `SHOT_TAIL` 350 → S5 길이 6,143ms. `TOTAL` 26,937 → 카드 = **S5 +3,543ms**.

| 사건 | 코드 근거 | 완성 | 여유 |
|---|---|---:|---:|
| 2번 카드 착지 | `secondcard.js:45` `dk=ease(clamp(c0/snapAt))`=1 → c0=0.74 | +1,714ms | 1,829ms |
| **`CODE 0` = 페이오프** | `:46` `vk=ease(clamp((c0−0.74)/0.26))`=1 → c0=1.0, `W`=0.15+2.961×0.80=2.5188s | **+2,369ms** | **+1,174ms** 🟢 |
| 선반 판 | `:43` `c1=cue(1,0.14,0.06)`, `W`=0.14+2.832×0.06=0.30992s | **+3,131ms** | **+412ms** 🟢 |

**검수 값(1,174 / 412ms)과 소수점까지 일치한다.** 페이오프는 전부 cue 0 안에서 끝나고,
예고 자막에 걸린 것은 짧고 큰 변화(선반 판)뿐이다. ep08s-3 이 반려된 자리의 **반대쪽**이다.

🔴 **`shelfCue` 의 `lead 0.14` · `span 0.06` 은 그대로 있고, 나는 바꾸라고 요구하지 않는다.**
그 412ms 가 저 두 값에서 나오고, 늘리는 손잡이는 `span` 하나뿐이다(완성 = 자막 시작 + `rel`×`span`
이므로 `lead` 는 시작만 당긴다). 검수의 🟡-5 도 이 둘을 안 건드리고 라벨 문턱만 내렸다 —
`:115` `sk > 0.20` · `:116` `clamp((sk−0.20)/0.40)` 로 반영된 것을 확인했다. **타이밍에 영향 0.**
🟡 🟡-5 의 나머지(폰트 12.5→14 · 판 6→8px)는 미반영이나 검수가 「(선택)」으로 낸 것이라 결함이 아니다.

### ⑤ 원문에 없는 문자열·수치가 화면·자막에 있는가 → 🟢 **0건. 기계 전수로 대조**

`spec` 을 통째로 훑어 화면에 뜨는 문자열 **17개 전부**를 뽑고 각각을 원문에 `grep` 했다.

`GOAL LIST`←「목표 목록」 · `PRECHECK`←「미리 재료가 있는지 검사」(1건) · `RESERVE`←「자원을 예약」(1건) ·
`ACCEPT`←「수락」(4건) · `GOAP`←「이 게임의 두뇌인 GOAP」(1건) · `PLAN`←「계획을」 · `CHOP`←「나무를 하러 가는 것」(1건) ·
`WOOD`·`HOUSE` · `BUILD HOUSE`←「집 좀 지어줄 수 있어」(1건) · `COOK MEAL`←「요리사에게 요리를 부탁」(1건) ·
`CODE`·`SHELF`←「선반」(1건) · `목수`(7건) · `1` · `2` · `0`.
**전부 원문에 실재한다. 지어낸 문자열 0건.**

🔴 **kind 코드 하드코딩 0건**을 직접 확인했다 — `grep -n "fillText(['\"\`]" kinds/*.js` → **0건**.
`fillText` 호출 4곳은 전부 `spec` 파생 배열(`steps[i]`·`mine[i]`·`chain[i]`·`drop[i]`)이다.
**화면의 모든 문자·수가 `spec` 경유다.**

**수치 표본 3개**(자막·화면의 숫자 전부):

| 표본 | 원문 `grep` 결과 |
|---|---|
| `0` (자막 「코드 0줄로」 · `spec.codeValue`) | 「…시험 삼아 추가해봤습니다. 결과는 **코드 수정 0줄**, 데이터 파일만 몇 개였습니…」 |
| `2` (자막 「2번째 부탁은」 · `spec.slots[1]`) | 「…데이터 파일 하나로 만든 뒤, **두 번째 부탁**("배고픈 주민이 요리사에게 요리를 부탁")을…」 |
| `1` (`spec.slots[0]`) | 「**첫 부탁**」 1건 실재 |

**3/3 원문 실재.** 브리프가 함정으로 짚은 값들(판정 순서 `4` · A~F `6개` · `5번째`)은 화면·자막에 0건.

**오염 경로도 구조적으로 없다** — 원본 html 에 `<table>` **0** · `<svg>` **0** · `<pre>` **0** · `<img>` **0**
(직접 셌다). 2026-07-21 발행이라 표·도식 정책(2026-08-02) 이전 글이고 산문뿐이다.

---

## 첫 자막 · 마지막 자막

- **첫 자막(0~4.04s)** 「집 없는 주민이 목수한테 집 좀 지어달라고 부탁해요.」
  → 🟢 **콜드 오픈이다.** 제목 카드도, 「이번엔 …을 얹었습니다」식 진행 상황도 없다. 0초에 이미 사건이 돌아간다.
  M8·밀스톤·시스템 이름이 한 번도 안 나오고, 10-1편을 안 봐도 혼자 선다.
- **수수께끼는 10초 안에 던져진다.** 둘째 줄(4.04~6.96s) 「저는 재료부터 검사시키려고 했거든요」가
  「내가 끼어들려 했다」를 깔고, 셋째 줄(7.31s~) 「근데 그게 게임 두뇌랑 정면으로 어긋났어요」가 **7.3초에** 고리를 연다. 🟢
- **마지막 자막** 「다음 편, 편한 기능을 일부러 지워요.」 = **2.8초**(상한 3.0).
  → 🟢 그 한 줄만 읽어도 다음 편이 뭔지 알면서 왜인지는 궁금하다. 6~7자로 쪼그라들지 않았다(21자).
  `outro.next`·`ep10s-3` 실물·`schedule.json` `order`(ep10s-1 → **ep10s-2 → ep10s-3**) 셋이 같은 곳을 가리킨다.
  🔴 원문 맨 끝 「다음 이야기 예고」(공동 밭·재해 = M9 = 12편)를 안 옮겼다 — ep02s 사고의 재발을 막았다.

## `excluded` 8건 — 되살릴 것이 있는가 → 🟢 **없다**

전부 읽었다. 가장 무거운 둘만 적는다.

- **소유(M8-C)** 를 「집 없는」 관형어 하나로만 남긴 것 — 🟢 옳다. 그걸 설명하면 첫 두 줄 중 하나를 먹고,
  이 채널에서 체류를 만드는 것은 첫 줄이다. 「집이 없으면 집을 원한다」는 설명 없이 읽힌다.
- **판정 순서(바쁨→배고픔→피로→친밀)와 거절·수락 대사** 통째 제외 — 🟢 옳다. **다른 사건이다**
  (부탁을 *어떻게 판정하느냐* ↔ 이 편은 *어떻게 실행하느냐*). 🔴 게다가 원문에 `4` 라는 글자가 없어
  화면에 세웠으면 **지어낸 수치**가 됐을 자리다. 버린 판단이 옳다.
- 🔑 되살릴 후보로 잠깐 본 것은 **`5번째`**(다섯 번째로 재현된 패턴)뿐인데, 나머지 넷(액션·계절·대화)을
  못 보여주면 **화면에서 셀 수 없는 수**이고 마지막 샷의 자리 둘 설계를 깬다. **작성팀 판단 유지.**

---

## 🔴 넘긴 채 통과시킨 항목 — **다음 회차가 선례로 인용할 수 없다**

### ① `tick` 돌려막기 대조표의 누락 1건 + 오귀속 1건 (`scene.json` S5 `sfx[0].why`)

**렌더되는 소리는 옳다**(1830 은 참 점유 목록의 최대 빈칸 1760~1900 한복판, 최근접 70Hz).
넘기는 것은 **그 소리를 고른 근거로 리포에 남는 문서**다.

- 누락: `ep05s-2 S3` = `tick 1080 / dur 0.07`
- 오귀속: `1180 (…, ep10s-1)` → **`ep10s-1` 의 실제 값은 `2320 / dur 0.072`**

🔴 **이것을 통과시키는 이유는 딱 하나다 — 승인 조건 넷 중 어느 것도 아니고, 렌더 산출물이 안 바뀐다.**
🔴 **선례로 삼을 수 없는 이유는 셋이다.**
1. `check.mjs` 에 **sfx 검사가 0건**이다. 이 대조표가 그 계층의 **유일한 게이트**이므로,
   틀린 표는 「기록 오류」가 아니라 **게이트가 꺼져 있는 상태**다.
2. **같은 표가 두 라운드 연속 틀렸다.** 작성팀 자신이 1차 정정 때 *"건수가 안 맞으면 목록이 틀린 것이다"*
   라고 적어 놓고, 2차 표도 건수가 안 맞았다(주장 16 / 실제 17).
3. 다음 편 **`ep10s-3` 이 바로 이 표를 베껴 쓴다.** 「1180 = ep10s-1」을 믿고 고르면 실제 2320·1080 과
   충돌할 수 있다.

**처리**: 렌더 전/후 어느 쪽이든 좋으니 **작성팀이 S5 `why` 의 tick 전수 목록만 참값으로 고칠 것.**
자막·`delayMs`·`freq` 값은 **하나도 안 건드린다**(전부 옳다). 순수 문서 정정이라 **재렌더 사유가 아니다.**
🔴 **다음 회차는 「ep10s-2 도 표가 틀린 채 통과했다」를 근거로 쓸 수 없다.**
표 오류가 값 오류로 이어졌다면 그 회차는 **반려**다. 이번엔 값이 우연히 살아남았을 뿐이다.

### ② 제목의 두 약속 중 뒤 절(`2번째`·`0줄`)이 21.5~23.1초(전체의 84%)에 이행된다

검수가 기록한 그대로이고, 나도 통과시킨다 — 앞 절(재료 검사)이 첫 두 줄에서 이행되어 게이트가 물고,
실측 평균 시청(16~27초) 안에 뒤 절도 들어온다. ep02s 사고(80초 영상의 34.3초)와는 성격이 다르다.
🔴 **다만 「페이오프를 84% 지점에 둬도 된다」는 선례로 쓸 수 없다.** 이 편이 통과한 이유는
**26.9초짜리라 84% 가 절대 시각으로 21.5초**이기 때문이지, 비율이 허용돼서가 아니다.

---

## 렌더 지시

```
node tools/scene-video/publish.mjs ep10s-2 --prepare
```

🔴 업로드는 사람이 한다(`publish.mjs` 주석 참조). 나는 대본을 고치지 않았다.

---

## 재제작 판정 2026-08-08

판정 2026-08-08 · 마스터(scene-master) · 대상 = **4단 고정 구조 재제작본**(8샷 10자막) ·
검수 「통과(조건부)」 + 정정 4건 반영본

## 판정 — **APPROVED**

누적 반려 **0회**. 승인 조건 넷을 **전부 직접 확인**했다. 검수 리포트의 표·수치는
근거로 쓰지 않고 `timed.json`·원문 html·`engine.js`·`sfx.mjs` 로 다시 풀었다.

| 승인 조건 | 결과 | 내가 한 것 |
|---|---|---|
| `check.mjs` 실패 0 | 🟢 **26종 전부 OK · 실패 0 · 경고 1** | 지금 직접 실행 |
| 수치 표본이 원문에 실재 | 🟢 **4/4** | 원문 html 에 `grep` 직접 |
| 모든 샷 `reads`·`source` 실체 있음 | 🟢 **8/8** | 기계 추출 + **스틸 5장으로 실물 대조** |
| 첫 10초에 수수께끼 | 🟢 | 타임라인 검산 + 스틸 |

🔴 **경고 1건(36.8초)은 반려 사유가 아니다.** 정본 ADR-V25-9 의 fail 은 **38초**이고
30~35 는 warn 대역이다. 사용자 판정 「35±3」 안이며 상한 38 아래다.
**폐기된 옛 숫자(25±3)로 반려하지 않는다.**

---

## 🔴 직접 재검증한 고위험 4항목

### ① 총 길이·구조 게이트 — 지금 직접 실행

`node check.mjs ep10s-2` → **통과 · 실패 0 · 경고 1**(총 길이 36.8초).
본문 21.8초(18~22 안) · 인트로 4.6초(≤5.5) · 예고 2.9초(≤3.0) · 말 속도 6.61자/초 ·
결정성 184프레임 불일치 0 · 잘림 0 · 정적 최대 0.0초.

### ② 아웃트로 카드 산수 (카드 = TOTAL − **3000**) — 🟢 페이오프 안전

`engine.js:183` 이 `OUTRO_MS = 3000` 임을 코드로 확인(2600 아님).
`timed.json` + `scene.json` 으로 타임라인을 **처음부터 다시 쌓았다**(`SHOT_TAIL 350`):

| 샷 | 시작 | 끝 |
|---|---:|---:|
| S0 | 0 | 4,649 |
| SH | 4,649 | 9,616 |
| S1 | 9,616 | 15,954 |
| S2 | 15,954 | 19,707 |
| S3 | 19,707 | 23,670 |
| S4 | 23,670 | 27,756 |
| S5 | 27,756 | 31,067 |
| SO | 31,067 | **36,816 = TOTAL** |

→ **카드 = 33,816ms.** 검수 표와 8행 전부 일치한다(독립 재현).

- **S5 페이오프** — 샷이 **31,067** 에 끝나므로 카드까지 **2,749ms 여유**.
  선반 판 완성은 샷 시작 +2,724 = **30,480ms** 로 카드보다 **3,336ms 이르다.**
  스틸 `build/stills/30.5s.png` 에서 `BUILD HOUSE`·`COOK MEAL`·`SHELF`·`CODE 0` 전부 완성 확인. ✅
- **nextpeek 노출** — `peekCue 0`·`cue(0,0.15,0.42)`·SO L1 `rel` 2,932 →
  창 W = 1.381초, 완성 = 자막 +1,231ms = **32,298ms**. 카드까지 **1,518ms 여유**. ✅
  스틸 `33.7s.png`(**카드 116ms 전**)에 벽이 올라간 집·빈 말풍선·`AUTO BUILD` 가 실제로 떠 있다.
- 🟢 **검수가 잡은 `linkCue` 0프레임(정정 ①)은 이미 반영됐다** — `kinds/nextpeek.js` 에
  `cue` 가 **하나뿐**이고(`peekCue`), 주석이 「소개 줄에는 아무것도 걸지 마라」로 바뀌었다.
  코드로 직접 확인했다. **죽은 코드가 남아 있지 않다.**

🔑 이 회차가 남기는 공식: **마지막 줄의 `rel + 0.35 < 3.0` 이면 그 줄 구간은 통째로 카드 밑**이다.

### ③ sfx 두 건의 근거 줄이 재제작에서 무변경인가 — 🟢 **무변경**

`git diff scene.json` 을 직접 떴다. S2 「근데 그게 게임 두뇌랑 정면으로 어긋났어요」와
S5 「2번째 부탁은 코드 0줄로 붙었어요」는 `text`·`say`·`pauseAfter` 가 **diff 에 한 줄도 안 나온다.**
`delayMs` 두 값도 diff 에 없다. `rel` 도 그대로다(S2 3,403 · S5 2,961 — `timed.json` 실측).

역산도 다시 풀었다(`headon.js:38` = `cue(0,0.15,0.85)` · `secondcard.js:44` = `cue(0,0.15,0.80)`, 코드 확인):

- S2 thud: `(−0.15 + 0.62 × (0.15 + 3.403×0.85)) × 1000` = **1,736.4** vs `1740` → **3.6ms(0.1프레임)**
- S5 tick: `(−0.15 + 0.74 × (0.15 + 2.961×0.80)) × 1000` = **1,713.9** vs `1714` → **0.1ms**

`sfx.mjs` 의 `synth` 를 직접 돌린 결과: thud 피크 **0.168** · tick 피크 **0.397**(천장 0.46 아래).
파형 상관도 직접 쟀다 — tick1830 ↔ 형제 ep10s-1 `2320/0.072` = **−0.147**,
thud230 ↔ `210/0.16` = **0.050**. `ep10s-1` 이 반려됐던 선(0.977)에서 멀다. ✅

### ④ 수치 표본 — 원문 `grep` 직접, **4/4 실재**

| 화면·자막에 오르는 수 | 원문 `grep` 결과 |
|---|---|
| S5 자막 「**2번째** 부탁」 | `두 번째 부탁("배고픈 주민이 요리사에게 요리를 부탁")을 시험 삼아 추가해봤습니다` |
| S5 자막·화면 「코드 **0**줄」 / `codeValue "0"` | `결과는 코드 수정 0줄, 데이터 파일만 몇 개였습니다` |
| 카드 형제줄 「**3군데**나 되돌아갔거든요」 | `되돌아가서 세 군데를 다듬었습니다` |
| 카드 형제줄 「**3초** 만에 보이게」 | `"쟤네 사이가 저렇구나"를 3초 안에 읽기에 충분했습니다` |

화면·자막 문자열을 기계로 전수 추출해 수를 셌다 — 나온 것은 위 넷 + 자리 번호 `1`·`2` 뿐이고
**원문에 없는 수가 화면에 오르는 경로가 없다.** 특히 판정 순서(바쁨→배고픔→피로→친밀)를
`4`로 세지 않았다(원문에 `4`라는 글자가 없다 — `excluded[1]` 이 그 판단을 명시).

---

## 코디네이터가 올린 판단 2건 — 🟢 **둘 다 승인. 단 근거를 하나 정정한다**

### ① 인트로 고정 문안의 정본 개정 — **승인**

- **회차 판정 기준은 「정본과 일치하는가」이고, 일치한다.**
  `Docs/영상_25초_전환_실행명세서.md:165-166` 의 음성 23자·자막 전문이 `scene.json` S0 과 글자 단위로 같다.
  `kinds/intro.js` 는 `engine/intro-brand.js` 한 줄 재수출(ADR-V25-11 준수).
  스틸 `3.8s.png` 에서 터미널 3줄·미니맵·`^-^` 가 실제로 그려진다(`reads` 가 빈말이 아니다).
- **개정의 필요성을 내가 다시 쟀다.** 이 회차 10줄의 실측으로 회귀를 냈다:
  **`dur ≈ 338 + 164.5 × 발화자수`**(R 없이 10점 최소자승).
  커밋된 옛 문안(발화 **34자**) → **5,929ms**, 지금 4,049ms 보다 **+1,880ms** →
  **TOTAL ≈ 38.7초 = 상한 38 초과.** 🟢 **개정은 구조적으로 불가피했다.**
- 🔴 **정정 — 코디네이터가 든 「42초대」는 최종 컷으로 재현되지 않는다(38.7초).**
  3차 감량 **전** 본문(+2.4초) 기준이면 ≈41초대라 그 맥락에서만 근사한다.
  **결론은 안 뒤집힌다**(어느 기준으로 재도 38 을 넘는다). 증거만 갈아 끼운다 —
  다음 회차가 「42초」를 상수로 베끼면 안 된다.
- 🙋 **사람 몫으로 남긴다**: 정본 개정이 아직 **미커밋**이다(`git diff Docs/…`).
  회차 승인과 별개로 **시리즈 결정이므로 커밋 전에 사람이 확인**할 자리다.

### ② 3차 감량 중 인트로 −2.0초 · 소개 −1.1초 — **명시적 예외로 승인**

ADR-V25-9 의 문언(「인트로·훅·예고·소개를 깎으면 반려」)과 표면상 부딪히지만 반려하지 않는다.

- **인트로 쪽은 「깎기」가 아니라 「정본 교체」다** — ①에서 확인했다. 옛 문안을 유지하면
  **게이트가 fail 한다.** 규칙이 막으려던 것(길이를 맞추려고 브랜딩을 희생하는 것)과 반대 방향이고,
  정체성 전문은 **자막·그림이 그대로 진다**(스틸 `3.8s.png` 로 확인).
- **소개 줄은 명세서가 「예시」로만 주는 문안**이고 게이트 `아웃트로 소개 있음` 을 통과하며
  「Claude Code 개발일지」라는 정체성 문장이 살아 있다. 예고 판별 정규식에도 안 걸린다(예고 2.9초 = 한 줄).
- **본문도 같이 깎였다**(S1 L1 · S3 · S4 = 2.4초). 「가운데를 안 깎고 앞뒤만 깎았다」가 아니다.
  본문 21.8초는 이미 18~22 대역 상단이라 더 깎으면 다른 게이트가 문다.

🔴 **예외의 범위를 못박는다**: 승인되는 것은 **「정본이 바뀌어서 짧아진 것」과
「예시 문안을 게이트 안에서 다시 쓴 것」** 둘뿐이다. **「길이가 넘쳐서 인트로·예고를 잘랐다」는
여전히 반려**다. 다음 회차가 이 판정을 그 뜻으로 인용하면 안 된다.

---

## 콜드 오픈 조건에 대하여 — 🟢 **ADR-V25-8 이 대체했다. 옛 규격으로 반려하지 않는다**

내 승인 조건 넷 중 「첫 10초가 콜드 오픈」은 **사용자 판정(ADR-V25-8, 2026-08-08)으로
「인트로 → 훅」 4단 구조가 대체**했다. 규격이 실제로 바뀌었으므로 옛 형태를 요구하지 않는다.
대신 **「첫 10초에 수수께끼가 던져지는가」**로 판정했다:

- 0~4.65초 = 인트로(무슨 채널인지). 4.65~9.62초 = 훅 —
  「오늘 개발 일지 내용은, 목수한테 시킨 검사 지우기예요.」
- 스틸 `8.6s.png`: `CHECK` 상자에 **지울 것 표시**가 씌워지고 그 아래 `GOAP` 캡슐이 밝아진다.
  **「왜 시킨 검사를 지우나 · 밑에 있는 저건 뭔가」가 9.6초 안에 던져진다.** ✅
- 첫 줄(= 훅) 검증된 두 규칙도 통과: 수치 스펙으로 안 열었고, 우리 진행 상황으로 안 열었다.

---

## 승인을 막지 않는 지적 4건

1. 🔴 **빌드 산출물 3개가 재제작 전 것이다** — `build/video.mp4`·`ep10s-2.en.srt`(cue 13)·
   `upload.txt` 가 **2026-08-08 00:47**(5샷본)이고 `timed.json`·`full.wav` 만 **16:06**(재제작본)이다.
   **이 상태로 올리면 옛 영상이 나간다.** 승인의 전제 = 코디네이터가 `--prepare` 로 재렌더.
2. 🟡 **아웃트로 카드가 10-3편을 서로 다른 문구로 두 번 부른다**(형제줄 = ep10s-3 `youtube.title`,
   `next` = 자유 문구). 같은 편을 가리키므로 ADR-V25-7 위반은 아니지만 다음 회차부터 맞추는 편이 좋다.
3. 🟡 **검수가 든 파형 상관 하나가 내 실측과 다르다** — tick1830 ↔ ep03s `1900` 을 검수는 **0.851**,
   나는 **0.595** 로 쟀다. 둘 다 0.977 에서 멀어 결론은 같지만, **다음 회차가 0.851 을 승계하면 안 된다.**
   상관은 `gain` 정규화·길이 정렬에 따라 갈리므로 **재는 방법을 함께 적어야 한다.**
4. 🟡 **시리즈 안에서 구조가 갈렸다** — ep10s-2 만 4단이고 형제 ep10s-1·ep10s-3·ep11s 는
   3단 완주본이다(업로드 대기 4건). 되돌릴지 그대로 낼지는 **사람 결정**이다. 회차 판정 밖.

---

## 렌더 지시

```
node tools/scene-video/publish.mjs ep10s-2 --prepare
```

🔴 업로드는 사람이 한다(`publish.mjs` 주석 참조). **나는 대본을 고치지 않았다.**

### 델타 판정 — 사용자 2차 반영 (2026-08-08)

**판정 = APPROVED (델타).** 변경분만 봤고 그 밖은 위 재제작 판정이 유효하다. 누적 반려 **0회**.

**직접 다시 푼 것** — 코디네이터 산수를 안 믿고 `timed.json` 으로 타임라인을 새로 쌓았다.

| 값 | 내 재현 | 코디네이터 |
|---|---:|---:|
| TOTAL | **37,939** | 37,939 ✅ |
| 카드(= TOTAL − 3000) | **34,939** | 34,939 ✅ |
| 소개 줄 시작 | **35,122** | 카드가 183ms 전 ✅ |
| 카드가 예고 줄을 덮는 길이 | **183ms** | 183 ✅ |

`node check.mjs ep10s-2` 직접 실행 = **전 항목 OK · 실패 0 · 경고 1**(37.9초 ≤ 상한 38,
사용자 「35±3」 안) · **예고 5초 이하 = 4.3초** · 본문 21.7 · 정적 최대 0.2초.

**ⓐ 예고 4.3초가 성립하는가 — 🟢 성립한다.**
「다음 편은, 집이 저 혼자 지어지던 기능을 일부러 지워요.」 — **행위(지운다)와 대상(집이 저 혼자
지어지던 기능)이 둘 다 한 문장 안에 있고**, 「다음 편,」의 끊김이 「다음 편은~」으로 이어졌다.
🔑 **음성과 그림이 이제 같은 것을 가리킨다** — 스틸 `33.2s.png` 에서 자막이 「저 혼자 지어지던」을
말하는 동안 미리보기 안에서 집이 저 혼자 올라가 있다. 사용자 판정의 취지 그대로다.
미리보기 노출도 줄지 않았다: 창이 그려지기 시작하는 **30,667** → 카드 **34,939** = **4,272ms**
(코디네이터 「~4.1초」와 일치), 집 루프가 실제로 드는 `ak ≥ 0.55` 부터만 세도 **3,195ms
= 0.5초 루프 6.4바퀴**, 완성(32,625)에서 카드까지 **2,314ms 여유**. 카드에 먹히지 않는다.
`TEASER_MAX 3.0 → 5.0` 은 **3.0 도 사용자 확정값이었으므로 사용자가 자기 값을 고친 것**이고,
`check.mjs` 주석과 정본 §2-C 둘 다에 인용과 함께 남았다 — **게이트를 무르게 한 것이 아니다.**
뜻 판정은 여전히 사람이 하고, 이 문장은 「그 한 줄만 읽고 다음 편이 뭔지 아는가」를 통과한다.

**ⓑ 기존 승인 조건을 깨지 않는가 — 🟢 안 깬다.**
- **수치 정직성** — 새 줄에 숫자가 **0개** 추가됐다(기계 확인). 화면에 오르는 수는 그대로 넷뿐.
- **원문 근거** — 「집이 저 혼자 지어지던 기능」의 출처를 `grep` 으로 직접 열었다:
  `원래는 집이 부족하면 목수가 알아서 집을 짓고, 빈집이 생기면 무주택 주민에게 자동으로 배정되게
  해뒀습니다`. **`nextpeek` 근거 절과 같은 문단**이고, 「알아서」를 「저 혼자」로 옮긴 압축이라
  뜻이 늘지 않았다. 🟢 10-3편의 **뒤집힘**(그 편리함이 부탁의 존재 이유를 갉아먹었다)은
  여전히 한 번도 안 말한다 — 예고가 스포일러가 되지 않았다.
- **`excluded` 흔적 0** — 버린 소재가 새로 새어 들어온 것 없음.
- **sfx** — `pauseAfter` 를 S2·S5 만 안 건드린 것을 `timed.json` 으로 확인했다
  (**rel 3,403 / 2,961 무변경**). `delayMs 1740 / 1714` 의 역산이 그대로 성립한다. ✅
- **`hud.outro`** — 스틸 `1.5s.png` 로 직접 봤다. `Claude Code로만 만드는 GOAP AI 마을 게임
  개발일지` 36자가 **한 줄로 다 그려지고 잘림 없다**(우측 여백 남음). URL 은 `upload.txt` 설명란에 남는다.

**🟡 정정 1건 (렌더를 막지 않는다 · 문서만)**
`kinds/nextpeek.js:18` 주석이 아직 *"자막은 「지운다」를 말하고 화면은 「무엇을」을 보인다 —
서로 다른 것을 진다"* 이다. **사용자 2차 판정이 정확히 그 분담을 뒤집었다**(이제 둘이 같은 것을
가리킨다). `scene.json` `excluded[6].why` 의 *"자막은 「편한 기능을 일부러 지워요」"* 인용도 옛 문구다.
🔴 **`linkCue` 때와 같은 형태의 죽은 주석**이라 다음 회차가 승계하기 전에 고칠 것.
