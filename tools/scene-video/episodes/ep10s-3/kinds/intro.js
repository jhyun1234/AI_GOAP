/* intro — 시리즈 공용 브랜딩 인트로(ADR-V25-11).
   🔴 회차가 그리지 않는다. 「그림은 회차가 소유한다」의 명시된 예외이고, 인트로는
   반대로 매회 같아야 브랜딩이 된다(아웃트로 카드와 같은 지위).
   check.mjs 의 `kind 파일 존재` 가 회차 폴더를 보므로 파일만 여기 두고 한 줄로 재수출한다. */
export { default } from '../../../engine/intro-brand.js';
