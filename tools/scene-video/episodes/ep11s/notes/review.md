# 검수 리포트 — ep11s (3차본)

**판정: ✅ 통과** (누적 반려 2회 — 1차 길이, 2차 그림 둘 + 예고 하나. 3차본에서 셋 다 해소)

기계 게이트 **23종 전부 통과**. 반려 사유 3건은 **전부 픽셀·원문 증거로 해소를 확인**했습니다.
남은 것은 기록 정정 5건(🟡)뿐이고, 영상에 나가는 것은 하나도 없습니다.

---

## 0. 🔴 먼저 — 2차 리포트의 🟡-3 은 **제 오판이었습니다. 철회합니다**

제가 *"「쫓아가서 건네준다」는 원문에 없다(원문은 「건네주기」/「직접 건네야 한다」)"* 라고 썼습니다.
**틀렸습니다.** 발행 HTML 에 **큰따옴표째로 두 번 실재**합니다(직접 재확인):

```
… 진짜 원인이 알고리즘이 아니라 "쫓아가서 건네준다"는 설계 전제라는 데까지 내려간 겁니다.
… 알고리즘만 만졌다면 영원히 못 고쳤을 문제가, "쫓아가서 건네준다"는 전제를 뒤집자 깨끗하게 풀렸습니다.
```

원인은 제 대조 방법입니다. `re.findall('쫓아가서 건네주', …)` 의 **개수만** 보고 `1` 이 나오자
「소제목 하나뿐」이라고 결론지었습니다. 매치 문맥을 출력했으면 바로 보였을 것입니다.
**개수는 사실 대조의 증거가 아닙니다 — 매치를 눈으로 봐야 합니다.** 이 교훈은 메모리에 넣었습니다.
`spec.quote` 는 **그대로 두는 것이 맞습니다.** 작성팀 반박이 옳았습니다.

---

## 1. 반려 3건 — 해소 확인

### A. `premise.js` 글자 복제 → ✅ 해소 (증거: 산수 + 스틸 + 픽셀 계측)

**산수.** 새 클립은 왼쪽 `[0, crackX−off)` + `translate(−off)` → 원본 `x ∈ [off, crackX)`,
오른쪽 `[crackX+off, w)` + `translate(+off)` → 원본 `x ∈ [crackX, w−off)`.
**두 구간이 안 겹칩니다.** 화면 `[crackX−off, crackX+off)` 가 비고, `off=0` 이면 이음매가 정확히 맞습니다.
잘려 나가는 원본 `[0, off)`·`[w−off, w)` 는 판 바깥입니다(판은 `26 … w−26`, `off ≤ 10`).

**픽셀 계측**(판 윗변 `y=190` · 아랫변 `y=268` 행에서 어두운 구간 폭):

| 시각 | off | 실제 틈 | 판정 |
|---|---:|---|---|
| 13,150ms | ≈3.3 | 172.7 ~ 179.3 (**6.6px**) | 틈 존재 |
| 13,650ms | 10 | 166.2 ~ 185.8 (**19.6px ≈ 2×off**) | 틈 존재 |
| 14,400ms | 10 | 166.2~178.2 **+** 181.4~185.8 | 균열이 틈 **한가운데**, 양옆이 검정 |
| 15,400ms | 10 | 동일 | 동일 |

**혼합 픽셀 0개**(`y` 190·246·268 × 4개 시각 전부) — 균열 글로우가 흰 판/글자에 안 번집니다.

**균열이 틈을 못 넘는 것은 대수로도 성립합니다**: 확장폭 = `min(4.2, 0.42·off) + 1.5`,
틈 반폭 = `off`. `0.42·off + 1.5 < off ⟺ off > 2.586` 이고 `crackA` 는 `off ≤ 3` 에서 0 이므로
**전 구간에서 참**입니다(`off=10` 최대 확장 5.7 < 10).

🔑 **금 자리를 낱말 사이 공백으로 옮긴 것이 이 수정의 값입니다.** `measureText` 로 계산한
`crackX ≈ w×0.50` 이라 클램프(`0.32~0.68`)는 발동하지 않고, 스틸에서
**「쫓아가서」와 「건네준다」가 각각 온전한 덩이로 벌어집니다.** 복제 0. 「 」 괄호도 온전합니다.
`off=0` 구간의 이음매도 봤습니다 — 테두리 밝기가 175.6px 지점에서 **215~230/255** 로 살짝 낮아질 뿐
200 미만 픽셀이 **0개**라 실제 배율에서 보이지 않습니다.

### B. 강조색 빗금이 강조색 라벨을 훑음 → ✅ 해소 (증거: 5배 확대 스틸)

`chase.js` `REWARD`(2,000ms)·`settle.js` `DEBT`(21,450ms) 둘 다 **완전히 읽힙니다.**
빗금이 테두리 **밖** 점선 후광으로 나갔고 글자를 한 번도 지나가지 않습니다.
`chase.js` 의 `hatch()` 는 호출부가 사라져 함수째 삭제된 것도 확인했습니다(죽은 코드 0).
아래 레인 빚 카드는 그대로 두었고 여전히 선명합니다 — 판단이 맞습니다.

🟢 **작성팀 걱정 「후광으로 바꿔 움직이는 면적이 줄었다」에 답합니다 — 여유가 큽니다.**
`check.mjs:534-537` 의 판정식을 그대로 복제해 5fps 전 프레임(122장, 아웃트로 덮인 구간 제외)의
프레임 간 변화량 `m` 을 직접 쟀습니다:

```
최소 0.00817 · 중앙 0.01289 · 최대 0.10708   임계 0.0008
→ 최소 프레임조차 임계의 10.2배 · 임계 미만 프레임 0개
가장 낮은 다섯 프레임: 12.0s / 11.2s / 16.4s / 12.4s / 15.0s  (전부 S2 구간, 0.0082~0.0085)
```

**정지에 가까운 프레임이 한 장도 없습니다.** 후광 교체는 위험을 만들지 않았습니다.

### C. 다음 편 예고 → ✅ 해소 (증거: ep12s 원문 직접 확인)

새 예고 = 음성 「다음 편, 마을에 처음 재해가 와요.」 / 카드 「12편 · 마을에 처음으로 재해가 생깁니다」.
`sources.ep12s.local`(`2026-07-26-unity-goap-m9-zone-disaster-flood.html`)을 열어 확인했습니다:

> 이번 개발일지는 **마을에 처음으로 "자리"(구역)와 "재난"(재해)이 생기고**, 식량을 개수가 아니라
> "며칠 버티나"로 말하게 된 회차입니다.
> … 결과물은 셋입니다. … **계절마다 재해가 확정적으로 내려오게 됐고** …

**그 글이 스스로 쓴 첫 요약 문장**이고, 폭염처럼 뒤집히는 조각이 아니라 **밀스톤 층위의 사실**입니다.
ⓑ·ⓒ 대신 ⓐ 를 고른 사유(*"ep12s 가 아직 분할 전이라 사건 단위 문구는 같은 사고를 재발시킬 수 있고,
ⓐ 는 어떻게 쪼개져도 참"*)도 옳습니다 — 제가 제안한 「밭을 한 개도 못 때려요」는 그 글이
**여러 편으로 갈릴 경우 다른 편으로 갈 수 있는** 문구였습니다. 작성팀 판단이 제 제안보다 낫습니다.

- **음성 ↔ 카드 일치** ✅ (같은 사건, 카드가 「~으로 생깁니다」로 한 겹 더 격식)
- **그 한 줄만 읽고 다음 편을 아는가** ✅ (마을에 처음 재해가 온다)
- **구성안 12행**(4막 · 블로그 13편 · M9 공간과 재해) 및 `order` 와 정합 ✅
- 카드 스틸(24,600ms)로 화면 표기도 확인 ✅ — 자막·`outro.next` 가 화면에서 같은 것을 부릅니다
- 🔴 **버린 문구가 새어 있지 않은지도 봤습니다** — `lines`·`outro`·`youtube.blurb`·`en` 전부에
  「폭염」·「heatwave」 **0건**. 남은 6건은 전부 `shots[3].source` 와 `notes` 안의 **정정 기록**입니다(정상).

---

## 2. 실측 재계산 — 타이밍이 이동했으므로 전부 다시 풀었습니다

`timed.json` 으로 조립한 타임라인(각 샷 `SHOT_TAIL` 0.35초 포함):

| 샷 | 시작 | 길이 | 줄 시작(절대) | `rel` |
|---|---:|---:|---|---|
| S1 | 0 | 9,607 | 0 / 4,326 | 4,326 / **4,931** |
| S2 | 9,607 | 6,492 | 9,607 / **12,744** | 3,137 / 3,005 |
| S3 | 16,099 | 3,450 | 16,099 | 3,100 |
| S4 | **19,549** | 7,524 | 19,549 / **24,024** | **4,475** / 2,699 |

**`TOTAL` 27,073ms · 아웃트로 카드 24,473ms** — 코디네이터 표와 완전 일치합니다.

**효과음 — 근거 `rel` 두 개가 안 움직인 것을 확인하고 다시 풀었습니다.**

| | `rel` | 창 W | 겨냥 | 재계산 | 씬 값 | 차 |
|---|---:|---:|---|---:|---:|---:|
| S1 `drop` | 4,931(무변) | `0.15+4.931×0.9` = 4.5879 | `fall` 개시 `c1=0.72` | **3,153ms** | 3,150 | 3ms |
| S4 `tick` | 4,475(무변) | `0.15+4.475×0.8` = 3.73 | `stamp` 개시 `pass=0.80` → `c0=0.4997` | **1,714ms** | 1,720 | 6ms |

둘 다 30fps 0.2프레임 이내. `3x²−2x³=0.8` → `x≈0.7120` 도 작성팀 값과 일치합니다.
피크 천장 재확인: `drop` 0.1281 · `tick` 0.4178 (< 0.46) — **경고 없음**, 파라미터 무변경이라 파형도 동일.

**페이오프 vs 아웃트로 카드** — `pass=1` 은 `c0=0.54` → `0.54×3.73−0.15 = 1.864s` →
절대 **21,413ms**. 카드 24,473ms → **여유 3.06초.**
확인 사살로 **카드 73ms 전 프레임(24,400ms)** 을 뽑았고 `PAID`·체크·`DEBT` 캡슐이
전부 완성돼 선명합니다.

**예고 자막 노출**(카드 전) = 24,473 − 24,024 = **0.449초**, 그 사이 밑줄이 폭의 **38%** 까지 자랍니다.

**말·그림 착탄**(전부 ±20프레임 = 667ms 안): S1 캡슐 소멸 3.84초 / 「사라졌어요」 구간 안 ·
S2 균열 완주 14,382ms / 「전제」 ≈14,794ms(**412ms**) · S4 `PAID` 21,413ms / 「갚」 ≈21,387ms ·
`hold` 개시 21,488ms / 「못」 ≈21,747ms.

---

## 3. 3차본에서 바뀐 것의 부작용 점검 — 없음

- **S2[0]「안 되더라고요」**(+2자) 로 S2 샷이 6,229 → 6,492ms. `premise` 의 `c0` 창이
  2.306 → 2.503초로 넓어졌지만 조준 넷(`AIMS` 0.10~0.70, `run` 폭 0.15)은 `c0=0.85` 에 끝나고
  `slabA` 는 `c0=0.5`(자막 1.10초)에 서므로 **전부 자막 안**입니다. 영어 `en` 은 그대로 두는 것이 맞습니다
  (「didn't work」가 「안 되더라고요」를 그대로 옮깁니다).
- **예고 교체**로 S4[1] `rel` 2,692 → 2,699(+7ms). `rule` 밑줄·정적 구간·페이오프 전부 무영향.
- **길이** 27.1초(22.6~28) · 본문 21.8(≥20) · 예고 2.7(≤3.0) — 세 항목 다 대역 안이고,
  🔴 **길이를 맞추려고 첫 두 줄이나 예고를 깎지 않았습니다.** 오히려 예고를 뜻이 통하게 **늘렸습니다.**
- **제목 이행** 겹침 4건(짓고·60초·품삯·사라졌어요) 유지 · **첫 줄** 무변경(수치 스펙 아님 ·
  프로젝트 진행 상황 아님 · 앞 편 없이 이해됨) · `hook`·`act`·`ep10s-3` 약속 이행 전부 유지.
- **영어** 7줄 전부 있음·2줄 이하 ✅, S4[1] `en` = `Next: the first disaster / reaches the village.`
  로 한국어와 일치 ✅. `en.title` 85자 ✅, `en` 에 `tags` 키 없음 ✅, `scene.en.json` 없음 ✅.
  `srt.mjs` 재생성 정상(7줄 · 마지막 24.0~26.7s).
- **말투** S2[0] 어미 교체로 ~요 5 · ~더라고요 1 · ~죠 1. 첫 두 줄을 안 건드린 판단이 옳습니다.

---

## 4. 🟡 정정 요구 5건 (전부 기록 — 영상에 안 나갑니다. 마스터 단계에서 닫으면 됩니다)

이번에도 **실측 뒤 낡은 칸**이 남았습니다. 반려 사유는 아니지만 다음 회차가 이 파일을 베낍니다.

1. **`notes.페이오프와 아웃트로 카드`** 가 통째로 2차본입니다 — 「21.36초 / 24.38초 / 여유 3.0초」
   → 3차 실측 **21.41 / 24.47 / 3.06**. 같은 절의 *"깎은 것은 같은 샷의 예고 줄(23→19자)"* 도
   3차본에서는 **줄인 게 아니라 통째로 교체(20자)** 라 사실과 다릅니다. `writer.md` 에도 같은 한 벌.
2. **`notes.정적 구간`** 의 *"3차본에서 예고가 1자 늘어 약 **0.59초**"* → 실측 **0.449초**
   (2차 0.442초와 사실상 같음). 예고 `rel` 이 +7ms 밖에 안 늘었습니다.
3. **`notes.길이`** 에 3차 산정치(예상 **27.2** / 예고 **2.84** / 본문 21.8)가 새로 들어왔는데
   실측이 났습니다 — **27.1 / 2.7 / 21.8** 로 갈아 끼우세요. (2차 산정치를 실측으로 바꾼 것은
   제대로 됐습니다. 같은 일이 한 칸 뒤로 밀렸을 뿐입니다.)
4. **`notes.말투`** 의 「어미 분포가 ~요 **4** · ~더라고요 1 · ~죠 1」 = 6줄분입니다. 7줄이므로 **~요 5**.
5. (기록용) 2차 리포트의 🟡-3 은 **철회**되었으므로, `notes.화면 문자열`·`영어 라벨` 절에
   그 지적을 반영해 고친 문장이 있다면 되돌려도 됩니다. 확인 결과 화면 문자열은 무변경이라
   실제로 되돌릴 것은 없습니다.

---

## 5. 사람이 볼 필요는 없습니다 — 다만 남겨 둘 관찰 하나

🟡 **첫 줄의 어긋남 위치**(2차 리포트 §5 와 동일, 검증 전 기준이라 **의견**입니다).
「목수가 집을 다 짓고 품삯 주러 의뢰인을 쫓아가요.」는 주민이 행동 중이고 혼자 서지만
어긋남은 둘째 줄로 밀려 있습니다. 체류 상위 3편은 첫 줄 안에서 이미 거스릅니다.
다만 이 편은 **직전 편이 한 약속을 0초에 이행해야 하는 제약**이 있고 제목 이행 겹침 4건이
거기 걸려 있으므로, 지금 구조를 바꾸라는 뜻이 아닙니다. 시리즈 단위로 볼 재료입니다.

---

## 6. 게이트 출력 전문

```
가이드 점검 · ep11s
────────────────────────────────────────────────────
  OK shot 메타 완비             4샷 전부 있음
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               7줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 영어 자막 전 줄 존재           7줄 전부 있음
  OK 영어 자막 2줄 상한            7줄 전부 2줄 이하
  OK 제목 이행 (첫 두 줄)          겹침: 짓고, 60초, 품삯, 사라졌어요
  OK 제목 후킹 (수치)             숫자 60
  OK 다음 편 예고 있음             S4 자막 1줄 (음성 예고)
  OK 예고 3초 이하               2.7초
  OK 자막 2초 미만 없음            전부 2초 이상
  OK 말 속도 6.0~7.2자/초        6.5자/초 (참고 영상 6.93)
  OK 총 길이 28초 이하            27.1초 = 자막 25.7 + 꼬리 1.40 (목표 22.6~28초 · 이 값이 실제 mp4 길이다)
  OK 총 길이 22.6~28초 권장대역     27.1초
  OK 본문 20초 이상              본문 21.8초 (예고 2.7 + 아웃트로 2.6 제외)
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 하단 안전영역 (바닥 443px 비움)  자막 바닥 1440 · 비주얼 바닥 1266 (한계 1477)
  OK 결정성 (3패스)              135프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  OK 정적 구간 3초 이하            최대 0.0s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
전부 통과
```

---

## 7. 이 회차가 남긴 것 (다음 회차가 가져갈 것)

1. 🔴 **「겹치지 않게 설계했다」는 주석은 근거가 아니다.** `premise.js` 초안은 그 주석을 세 문단
   써 놓고 코드가 정반대였습니다. 클립·`translate` 를 쓰면 **원본 구간을 손으로 적어 겹침을 확인**해야 합니다.
2. 🔴 **팔레트 게이트는 「같은 색끼리의 가림」과 「불투명한 덮음」을 구조적으로 못 봅니다.**
   혼합 픽셀이 0개라 아무 신호가 없습니다. 확대 스틸이 유일한 검출 수단입니다.
3. 🔴 **자막에서 뺀 정보를 화면에 미뤘으면, 그 글자가 실제로 읽히는 프레임을 확인해야 합니다.**
   S2[1] 축약의 근거가 3.2초 동안 깨져 있었습니다.
4. 🔴 **예고는 회차 id 가 맞아도 주장이 틀릴 수 있습니다.** 구성안 표는 주제 이름 한 줄뿐이므로
   **다음 편 원본 글을 열어 그 사건이 실제로 일어나는지** 봐야 합니다.
5. 🔴 **(검수 쪽 교훈) 문자열 대조는 개수가 아니라 매치 문맥으로 합니다.** 제 🟡-3 오판의 원인입니다.

## 8. 검수가 만든 것 / 지운 것

- 합성 `timed.json` 을 **만들지 않았습니다**(기존 것은 wav 7개가 딸린 실측본).
- 임시 스크립트 `tools/scene-video/_rev3_*.mjs` 5개를 만들어 쓰고 **전부 삭제**했습니다.
- `srt.mjs ep11s` 검증으로 `build/ep11s.en.srt`(7줄)가 갱신됐습니다 — 정상 산출물이라 남깁니다.

---

## 4단 재제작 검수 2026-08-08

판정 = 🔴 **반려 1건**(다음 편 예고·카드·미리보기가 가리키는 편이 틀렸습니다) + 🟡 정정 요구 4건.
그 한 건을 빼면 나머지는 전부 통과입니다 — 4단 구조의 뜻·cue 산수·sfx·영어 자막·그림은
직접 다시 풀었고 이상이 없었습니다.

### A. 기계가 본 것 (직접 재실행)

`node tools/scene-video/tts.mjs ep11s` → **새로 만듦 0 · 재사용 10**. 즉 `build/timed.json` 은
현재 `say` 10줄과 정확히 같은 wav 를 가리키는 **실측본**입니다(합성본 아님).
말하는 시간 33.2s · 침묵 1.5s · 합계 34.7s · 6.42자/초.

```
가이드 점검 · ep11s
────────────────────────────────────────────────────
  OK shot 메타 완비             7샷 전부 있음
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               10줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 인트로 있음                 S0 (kind=intro) — 「클로드 코드로만 만드는, 에이아이 마을 개발일지입니…」
  OK 훅 시작 문구                「오늘 개발 일지 내용은, 육십 초 만에 사라진 품삯이에…」
  OK 아웃트로 소개 있음             「클로드 코드 개발일지, 또 만나요.…」
  OK 영어 자막 전 줄 존재           10줄 전부 있음
  OK 영어 자막 2줄 상한            10줄 전부 2줄 이하
  OK 제목 이행 (첫 두 줄)          겹침: 품삯, 짓고, 60초
  OK 제목 후킹 (정보)             숫자 60 — 기본 축은 감정·사건+질문형(0-A), 뜻 판정은 검수팀
  OK 다음 편 예고 있음             SO 자막 1줄 (음성 예고)
  OK 예고 5초 이하               3.0초
  OK 자막 2초 미만 없음            전부 2초 이상
  OK 말 속도 6.0~7.2자/초        6.42자/초 (참고 영상 6.93)
  OK 총 길이 38초 이하            37.2초 = 자막 34.7 + 꼬리 2.45 (목표 30~35초 · 이 값이 실제 mp4 길이다)
  ⚠  총 길이 30~35초 권장대역       37.2초
  ⚠  본문 18~22초              본문 22.1초 (인트로 4.6 · 훅 4.7 · 아웃트로 5.8 제외)
  OK 인트로 5.5초 이하            4.6초
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 하단 안전영역 (바닥 443px 비움)  자막 바닥 1440 · 비주얼 바닥 1266 (한계 1477)
  OK 결정성 (3패스)              185프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  OK 정적 구간 3초 이하            최대 0.2s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
통과 — 경고 2건은 확인 권장
```

실측 타임라인(직접 재구성, `SHOT_TAIL 0.35`):

| 샷 | 시작 | 끝 | 줄 rel(dur+pause) |
|---|---:|---:|---|
| S0 intro | 0.000 | 4.599 | 4,249 |
| SH paygauge | 4.599 | 9.671 | 4,722 |
| S1 chase | 9.671 | 18.128 | 3,176 / 4,931 |
| S2 premise | 18.128 | 23.657 | 2,425 / 2,754 |
| S3 ledger | 23.657 | 26.555 | 2,548 |
| S4 settle | 26.555 | 31.380 | 4,475 |
| SO nextpeek | 31.380 | **37.163** | 2,966 / 2,467 |

**TOTAL 37,163ms · 아웃트로 카드 = 34,163ms.**

### B. 🔴 반려 — 다음 편 예고·카드·미리보기가 「다음 편」이 아닌 것을 가리킵니다

#### B-1. 정본이 이미 쪼개져 있습니다

```
$ git show HEAD:tools/scene-video/state/schedule.json → order
[… "ep10s-3","ep11s","ep12s-1","ep12s-2","ep12s-3","ep13s"]
```

`order` 상 **ep11s 의 다음 편은 `ep12s-1`** 입니다. 이 분할은 커밋 `ab46c76`
(2026-08-08 · ep10s-2 4단 재제작)에서 `order` 에 들어갔고, ep11s 재렌더 커밋 `ea48f98`
보다 **앞섭니다.** `sources.ep12s` 도 `"split": 3` 이고 note 가 사건 셋을 이렇게 갈라 둡니다 —
①조리식 20개가 주민 10명 시절 숫자 → 「며칠 버티나」 ②겨울 잔량 40 → 네 군데 조임
③홍수가 작물 0개 → **밭 시설을 부수는 재해로 전환**. 같은 note 가 못박습니다:
*"구역(ZoneService)·재해 선반(DisasterSO)은 어긋난 상태가 없어(만든 이야기) 편으로 세우지 않았고"*.

그리고 세 편은 이미 실물로 존재합니다(`episodes/ep12s-1|-2|-3/scene.json` + `build/`):

| id | hud | 첫 줄 |
|---|---|---|
| ep12s-1 | 12-1편 · 조리식 20개는 주민 10명 때 숫자였다 | 「**재해를 내리기 전에** 이상한 걸 찾았어요.」 |
| ep12s-2 | 12-2편 · 기준은 30인데 겨울 잔량이 40 | 「마을에 재해를 내렸는데 겨울이 편했어요.」 |
| ep12s-3 | 12-3편 · 홍수를 내렸는데 작물 0개 소실 | 「마을에 홍수를 내렸어요. 작물 쓸어가라고요.」 |

`ep12s-1/scene.json` 의 `source.note` 가 이 어긋남을 **스스로 적어 두었습니다**:

> 🔴 **직전 편 ep11s 의 `outro.next` 가 「12편 · 마을에 처음으로 재해가 생깁니다」였고 음성은
> 「다음 편, 마을에 처음 재해가 와요.」였다.** 이 편의 사건은 재해가 아니므로(실물은 12-2·12-3 이
> 낸다) **첫 자막이 재해를 명시적으로 이어받는다** — 「재해를 내리기 전에, 이상한 걸 찾았어요.」

즉 다음 편은 「재해가 오기 **전에** 찾은 숫자 이야기」이고, 재해가 실제로 내려오는 것은 12-2,
**밭 시설을 부수는 재해**는 12-3 입니다.

#### B-2. 그래서 셋이 각각 다른 것을 가리킵니다

- **음성 예고**(SO[0]) 「다음 편은, 마을에 처음 재해가 와요.」 → 다음 편(12-1)의 사건이 아닙니다.
  판정 문장(「그 한 줄만 읽고 다음 편이 무슨 이야기인지 알 수 있는가」)에 걸립니다 —
  읽고 나면 **재해 편**을 기대하는데 실제로 오는 것은 **겨울 식량 목표 숫자** 편입니다.
- **카드**(`outro.next`) 「**12편** · 마을에 처음으로 재해가 생깁니다」 → `order` 에 「12편」이라는
  회차는 없습니다. 다음 id 는 `ep12s-1`(자기 hud 는 「12-1편」)입니다.
- **미리보기**(`nextpeek`) — DISASTER 막대가 ZONE 안의 **밭 시설**을 부숩니다. 이 그림의 근거로
  `kinds/nextpeek.js` 주석이 인용한 문장은 *"홍수를 「작물을 태우는 재해」에서 「밭 시설 자체를
  부수는 재해」로 전환했습니다"* 인데, 그건 **ep12s-3 의 사건**입니다(원문 120~122행).
  ADR-V25-10 은 *"음성 예고·카드·미리보기 셋이 같은 편을 가리켜야 한다"* 고 못박습니다.

작성 근거(`notes/writer.md` §8-8, `scene.json` `notes.다음 편 예고`)는
*"ep12s 가 아직 분할 전이라 밀스톤 층위로 잡았다"* 인데, **그 전제가 사실이 아닙니다.**
분할은 이미 정본(`order`)에 있고 세 편 다 대본이 나와 있습니다.
이건 이 회차가 1·2차본에서 반려됐던 사고(앞 글의 예고 문단을 근거로 삼음)와 **같은 종류가
한 층 위에서 재발한 것**이고, 검수 지침이 "다시는 안 된다"고 적어 둔 ep02s 선례와 같은 형태입니다.

#### B-3. 고칠 것

1. 예고 한 줄을 **ep12s-1 의 사건**으로 바꿉니다. 근거는 그 글에 실재합니다 —
   *"겨울 대비 목표가 「조리식 20개」로 못 박혀 있었는데, 예전에 주민이 10명이던 시절에 잡은
   숫자였습니다. 그 뒤 주민을 4명으로 줄였는데도 20이라는 숫자는 그대로 남았습니다."*
   문형은 「다음 편은~」 유지.
2. `outro.next` 를 **「12-1편 · …」** 로 고칩니다(번호와 내용 둘 다).
3. `nextpeek` 그림을 다음 편 기능의 0.5초 테스트 장면으로 바꿉니다 — 지금 그림은 12-3 것입니다.
   ⚠️ 이 편의 축(간격)을 물려주지 않는다는 판단은 그대로 유효합니다. 다만 그리는 대상이
   「밖에서 들어와 밭을 부수는 재해」가 아니라 「인원이 줄어도 안 변하는 숫자 / 며칠 버티나」가
   돼야 합니다.
4. 🔴 **길이 예산을 같이 계산하세요.** 지금 SO[0] 은 `dur 2,866 + pause 100`. 새 예고가
   28~30자면 이 회차 실측 rate(약 142ms/자)로 **4.0~4.3초**라 총 길이가 **38.4~38.6초**가 되어
   **38초 fail 을 넘습니다.** 예고 상한은 5.0초라 예고 자체는 통과하지만 총 길이가 죽습니다.
   → 본문 가운데에서 **약 1.5초**를 더 덜어내세요(ADR-V25-4 — 쪼개지 말고 곁가지를 던다).
   그러면 본문 22.1 → 약 20.6 이 되어 **본문 warn 도 같이 닫힙니다.**
   🔴 단, `S1[1]` 과 `S4[0]` 은 **건드리면 안 됩니다**(아래 C-4 — `delayMs` 두 값의 유일한 근거).

### C. 반려 사유가 아닌 것 — 다시 풀어 본 결과 전부 통과

#### C-1. 4단 구조의 뜻

- **인트로 S0** — 문안이 명세 §2-C(ADR-V25-8) 정본과 **글자 단위로 같습니다**.
  음성 `클로드 코드로만 만드는, 에이아이 마을 개발일지입니다.` / 자막
  `Claude Code로만 만드는, 스스로\n생각하고 행동하는 AI 마을 개발일지`. 자의 개작 0.
  `kinds/intro.js` 는 `export { default } from '../../../engine/intro-brand.js';` **한 줄**입니다(ADR-V25-11 ✅).
- **훅 SH** — 「오늘 개발 일지 내용은, 60초 만에 사라진 품삯이에요.」 이 편의 사건(60초 타임아웃에
  품삯이 사라짐)을 그대로 약속합니다. 원문 근거 실재 — *"그렇게 60초가 지나면 보고 심부름은
  타임아웃으로 취소됐고, 그 순간 목수가 받아야 할 보상이 그냥 사라졌습니다."*
  🟢 **첫 줄 검사 1~3(실측 등급)** — 수치 스펙으로 시작하지 않고(「60초 만에 사라진 품삯」은
  스펙이 아니라 사건입니다), 우리 프로젝트 진행 상황을 말하지 않으며, 앞 편을 안 봐도 섭니다.
  🟡 4번(미검증 등급)은 의견으로만 — 훅 줄의 주어는 사물이지만 바로 다음 줄
  「목수가 집 짓고 품삯 주러 쫓아가요」가 주민의 행동이고 그 행동이 어긋나 있습니다.
- **소개 마무리** SO[1] 「Claude Code 개발일지, 또 만나요.」 — **「다음 편」 낱말 없음** ✅
  (있었으면 예고 판별 정규식에 걸려 예고가 5.4초로 계산됐을 것입니다. 게이트는 3.0초로 셌습니다).
- **`legacyFormat` 삭제 확인** — 현재 `scene.json` 에 그 키가 없습니다(HEAD 판에는 있었습니다).
  신규 규격으로 판정했습니다.

#### C-2. 아웃트로 카드 산수 — 페이오프·미리보기 둘 다 카드 전에 끝납니다

- **S4 페이오프.** `settle.js` `c0 = cue(0, 0.15, 0.8)`, S4[0] `rel = 4,475ms`
  → 창 `W = 0.15 + 4.475 × 0.8 = 3.730초`. 정산 완료 `pass = 1` 은 `c0 = 0.54`
  → 자막 시작 `+0.54 × 3.730 − 0.15 = 1.864초` = **절대 28.419초**.
  카드는 **34.163초** → 여유 **5.74초**. (샷 S4 자체가 31.380 에 끝나므로 충돌 자체가 없습니다.)
  스틸 `28.4s.png` 로 눈으로 확인 — `PAID` + 체크 + 건너간 `DEBT` 캡슐이 다 서 있습니다.
- **미리보기.** `nextpeek` 등장은 `cue(0, 0.15, 0.30)`, SO[0] `rel = 3,066ms`
  → `W = 1.070초`. `ak = 1` 은 샷 시작 **+0.920초**(절대 32.300초)이고 `inner` 도 그때 1 입니다.
  카드까지 **1.86초**가 남아 0.5초 루프가 **3바퀴 이상** 돕니다.
  스틸 두 장으로 확인 — `33.2s.png` 에 부서진 가운데 칸과 조각·점선 자국이,
  `34.1s.png`(카드 **직전 프레임**)에 막대가 내려온 완전한 그림이 있습니다.
- **소개 자막(SO[1])은 34.346초에 시작**해 통째로 카드 뒤입니다. 작성팀이 이 줄에 아무 그림도
  안 건 것은 **옳습니다**(`nextpeek.js` 주석·`notes.정적 구간`과 일치). 자막 글자 자체는
  카드가 안 덮으므로 읽힙니다 — `34.5s.png` 확인.

#### C-3. `settle.js` holdCue 이동 — 옳습니다

예고 줄이 SO 로 나가 S4 는 한 줄짜리 샷이 됐고, 옛 `holdCue = cue(1)` 은 **없는 줄**을
가리키게 됩니다(엔진은 `Math.min(i, rel.length-1)` 로 0번으로 접혀 조용히 오작동합니다).
새 값 `rule = ease(clamp((c0 − 0.66) / 0.34))` 를 풀면:
시작 `c0 = 0.66` → 샷 시작 **+2.312초**, 완성 `c0 = 1` → **+3.580초**.
페이오프가 **+1.864초**에 끝나므로 밑줄이 **그 뒤를 이어받습니다** — 주석의 주장과 실제가 같습니다.
샷 길이 4.825초라 꼬리 1.25초가 남지만 트랙 점선·바운스·표류·바닥 띠 빗금이 계속 돌고,
게이트 「정적 구간 최대 0.2s」가 이를 실측으로 확인합니다.

#### C-4. sfx — `rel` 이 안 바뀌었고 `delayMs` 두 값이 그대로 유효합니다

`build/timed.json` 실측:

| 줄 | dur | pause | rel | 주석이 근거로 쓴 rel | 판정 |
|---|---:|---:|---:|---:|---|
| S1[1] | 4,681 | 250 | **4,931** | 4,931 | 일치 ✅ |
| S4[0] | 4,275 | 200 | **4,475** | 4,475 | 일치 ✅ |

- **S1 drop.** `chase.js` `c1 = cue(1, 0.15, 0.9)` → `W = 0.15 + 4.931 × 0.9 = 4.5879`.
  `fall` 시작 `c1 = 0.72` → 줄 시작 `+0.72 × 4.5879 − 0.15 = 3.1533초`.
  `delayMs 3150` → **Δ3ms(30fps 0.09프레임)** ✅
- **S4 tick.** `settle.js` `c0 = cue(0, 0.15, 0.8)` → `W = 3.730`.
  `stamp` 가 열리는 `pass = 0.80` 의 smoothstep 역함수는 **0.7120**
  (검산 `3×0.712² − 2×0.712³ = 0.7989`) → `c0 = 0.40 + 0.14 × 0.7120 = 0.4997`
  → 줄 시작 `+1.7139초`. `delayMs 1720` → **Δ6ms(0.18프레임)** ✅
  (작성팀이 `why` 에서 초안의 역산 오류를 스스로 정정한 것도 맞습니다.)
- **피크 천장** — 표를 믿지 않고 `synth()` 를 실제 인자로 불렀습니다.
  `drop{gain .072, dur .5, from 640, to 84}` → peak **0.1281**(천장까지 gain 0.2585 여유),
  `tick{gain .056, dur .044, freq 2540}` → peak **0.4178**, 천장 0.46 기준 **최대 gain 0.0617**.
  0.056 은 그 아래라 안 걸립니다. 경고 출력 0 ✅
- **돌려막기** — `episodes/**/scene.json` 27개 · sfx **79건**(ep12s-1/-2/-3 포함해 다시 셌습니다)을
  기계로 훑었습니다. `drop (0.50, 640, 84)` 과 `tick (0.044, 2540)` 은 **둘 다 이 회차뿐**입니다.
  두 kind 는 `rng` 를 안 부르므로 파라미터 대조 = 파형 대조가 맞습니다.
- **밀도** — 본문 22.1초에 2개. 기준(23초에 1~2개) 안이고 인트로·훅·아웃트로는 무음입니다 ✅

#### C-5. 조여진 본문 6줄 — 원문 대조

원본 HTML(`2026-07-22-…-pathfinder-boundary.html`)을 태그 제거해 전문으로 놓고 대조했습니다.

| 줄 | 자막 | 원문 근거 |
|---|---|---|
| S1[0] | 목수가 집 짓고 품삯 주러 쫓아가요. | *"목수 주민이 의뢰인의 집을 다 지으면, 다 됐다고 알리러 의뢰인에게 걸어갑니다"* + 전제 인용 *"보상을 주려면 상대를 쫓아가서 직접 건네야 한다"* |
| S1[1] | 둘 다 걷는 속도가 같아서 / 60초 뒤엔 품삯이 사라졌어요. | *"목수와 의뢰인의 걷는 속도가 똑같습니다"* / *"그렇게 60초가 지나면 … 보상이 그냥 사라졌습니다"* |
| S2[0] | 다시 조준해봐도 안 되더라고요. | *"「걷는 중에 목표 위치를 다시 조준하는」 기능"* / *"실제로 게임에 넣고 돌려보니, 안 됐습니다"* |
| S2[1] | 알고리즘이 아니라 전제가 틀렸죠. | *"제가 고치려던 건 알고리즘이었는데 … 설계 전제 자체가 틀렸던 겁니다"* |
| S3[0] | 쫓아가길 버리고 빚으로 적었죠. | 소제목 *"1단계 — 쫓아가기를 버리고 「빚으로 적어두기」로 바꿨다."* |
| S4[0] | 이제 마주치면 바로 갚고, / 못 만나도 빚은 안 사라져요. | *"언젠가 마주치면 그때 갚을게"* / *"끝내 못 만나더라도 빚은 사라지지 않는다"* |

- **화면 문자열 전수** — 60 · TIMEOUT 60s · REWARD · CARPENTER · CLIENT · GAP · RETARGET ·
  ALGORITHM · PREMISE · 「쫓아가서 건네준다」 · CHASE · DEBT · PAID · REWARD -> CLIENT.
  전부 원문에 대응이 있고 **원문에 없는 수는 0개**입니다(속도값·보상액·조우 거리·커밋 수 없음 ✅).
  「쫓아가서 건네준다」는 발행 HTML 27·37행에 큰따옴표째 실재합니다.
- **지워진 표 대조** — 원본 HTML 에 `<table>`·`<svg>`·`<pre>` **0건**(직접 확인).
  브리프 밖 값이 화면에 샐 자리가 구조적으로 없습니다.
- 🟡 **S1[0] 의 「다」가 빠진 것**(집 다 짓고 → 집 짓고)만 뜻이 반 칸 얇아집니다. 원문의 강조는
  *"열심히 집을 다 지어놓고"* 입니다. 제목이 「집을 다 짓고」를 지고 있어 반려는 아닙니다.
  B-3 의 본문 조임을 할 때 **여기를 더 깎기보다 다른 곳을 덜 것**을 권합니다.

#### C-6. 훅 그림(`paygauge`) — 스틸로 실물 확인

작성팀이 §8-9 4번에서 「손으로 푼 좌표를 못 봤다」고 자백했으므로 두 시점을 뽑았습니다.

- `6.5s.png`(링 60% · 캡슐 반쯤 옅어짐) — 링 안쪽과 캡슐 후광이 **안 겹칩니다.**
  코드 검산도 같습니다: 후광 바깥 반경 `(96+8)/2 = 52` + 선폭 1.5 = 53.5 < 링 안쪽 **57**.
  `REWARD` 글자를 후광 점선이 **한 번도 지나가지 않습니다**(후광은 테두리 밖).
- `8.2s.png`(페이오프) — 흰 점선 빈 캡슐 + `REWARD` 이름만 남습니다. 「이름은 남았는데 물건이
  없다」가 자막 없이 읽힙니다. 강조색 캡슐은 그 시점에 알파 0 이라 **흰색과 겹친 픽셀이 없습니다**
  (팔레트 게이트가 못 보는 종류인데, 여기서는 시간으로 갈라 놓은 것이 실제로 맞았습니다).
- `TIMEOUT 60s` 는 링 아래에 따로 서 있고 어느 것과도 안 닿습니다.
- 🟡 관찰(반려 아님): 페이오프의 `REWARD` 이름이 빈 캡슐 **안**이 아니라 **바로 아래**에 섭니다
  (`cy + CAP_H/2 + 16`). `reads` 의 「같은 자리에 … 빈 캡슐과 REWARD 라는 이름만 남는다」와
  어긋나지는 않고 가독성도 좋습니다. 다음에 `reads` 를 손볼 일이 있으면 「캡슐 아래」로 적으세요.

#### C-7. `nextpeek` 좌표 검산 (작성팀 §8-9 4번 ②)

`barLow = gy − 50`, 두께 최대 `10 + 6 = 16` → 막대 아랫변 **gy − 42**.
조각 윗변 `fy − d = (gy − 22) − 14` = **gy − 36**(선폭 포함 gy − 37.5).
구역 테두리 윗변 **gy − 34**. → **강조색(막대)과 흰 것(조각·구역) 사이 4.5px 이상 빈다** ✅
조각 윗변선과 구역 윗변선은 1px 스치지만 **둘 다 비강조색**이라 혼합 픽셀이 안 생깁니다.

#### C-8. 그 밖에 다시 확인한 것

- **S2 `premise.js` 의 옛 반려 결함(글자 복제·틈 0px)** — `22.6s.png` 에서
  「쫓아가서 / 건네준다」가 **낱말 사이**에서 벌어지고 복제 글자가 없습니다. 재발 없음 ✅
- **자막 옮겨적기** — 스틸 6장의 자막 문자열이 `scene.json` 의 `text` 와 전부 일치합니다.
- **영어 자막 10줄** — `text` 와 한 줄씩 대조했습니다. 원문에 없는 수치·주장 0.
  조건절이 조건절로 남았습니다(`if they never do, the debt still stands` — 완료형으로 안 밀림 ✅).
  이름은 로마자가 아니라 뜻(`the carpenter` · `the client`) ✅. `say` 는 번역 안 함 ✅.
  `youtube.en.title` **98자** ≤ 100 ✅, `en` 에 `tags` 키 없음 ✅. `scene.en.json` 없음 ✅.
  ⚠️ SO[0] 의 `en`(`Next up: the village gets its first disaster.`)은 B 항과 함께 바뀌어야 합니다.
- **`excluded` 7건** — 사유가 납득됩니다. 원문이 이름 붙인 개념 중 「조각 Y」는 뺐지만
  **뺀 사유가 적혀 있고**(내부 코드네임이라 뜻을 세우는 데 자막 한 줄이 든다) 대신 원문이 같은
  문단에서 쓰는 낱말 `DEBT` 를 화면에 올렸습니다 — 통과 형태입니다.
- **말투** — 종결 10줄이 ~요/에요/어요 6 · ~죠 2 · ~더라고요 1 · ~입니다 1. 3연속 없음.

### D. 🟡 정정 요구 4건 (영상에 안 나갑니다 — 반려 수정과 함께 닫으세요)

1. **`notes.길이` 가 아직 「새 구성의 실측은 아직 없다」로 남아 있습니다.** 스스로 적어 둔 지시대로
   `check.mjs` 줄을 그대로 인용해 갈아 끼우세요 — **총 37.2초 / 본문 22.1초**.
   (자체 산정식을 새로 적지 마세요. 지금 문서가 그 선을 지킨 것은 맞습니다.)
2. **조임 표 두 곳이 실물과 다릅니다**(`scene.json` `notes.4단 구조 개정` ⑤ · `writer.md` §8-5).
   표는 S1[0] = 「목수가 집 **다** 짓고 …」(22자) · S2 `pauseAfter` **200** 이라고 적혀 있는데,
   실물은 「목수가 집 짓고 …」(`timed.json` chars **20**) · `pauseAfter` **150** 입니다.
   코디네이터가 §8-9 의 예비 카드 ①②를 쓴 것이므로 **그 사실을 표에 적으세요.**
3. **`settle.js` 197행 주석이 옛 구성입니다** — `/* ── 예고 자막이 여는 변화 — 두 레인을 잇는 밑줄 ─ */`.
   이 샷에 예고 자막은 이제 없습니다(같은 파일 24~29행 블록 주석은 정확합니다).
4. **「카드까지 5초 이상」이라고만 적힌 자리에 실측을 넣으세요**(`notes.페이오프와 아웃트로 카드` ·
   `settle.js` 16~19행). 실측은 **5.74초**(28.419 → 34.163)입니다. 「렌더 뒤 확인할 것」이라고
   적어 둔 자리를 비워 두면 다음 회차가 그 문장을 그대로 복사합니다.

### E. 길이 두 경고에 대한 검수 판단

- **총 37.2초** — 상한 38 안, 사용자 확정 「35 ± 3」 안입니다. 그리고 **인트로·훅·예고·소개를 한
  글자도 안 깎았습니다**(대조 확인). 명세가 금지한 방식으로 길이를 만든 흔적이 없으므로
  이 경고만으로는 반려하지 않습니다.
- **본문 22.1초** — 상한 22.0 을 **0.1초**(30fps 3프레임) 넘습니다. 이 0.1 을 맞추려고 뜻을 더
  깎으라고 요구하지 않습니다. 다만 **B-3 의 예고 교체가 어차피 본문 1.5초 감축을 요구**하므로
  두 경고가 그 작업 하나로 함께 닫힙니다. 덜 곳은 곁가지이지 편 분할이 아닙니다(ADR-V25-4).
  🔴 **덜 때 `S1[1]`·`S4[0]` 은 제외**입니다 — 두 줄의 `rel` 이 `delayMs` 3,150·1,720 의 유일한
  근거이고, 한 글자만 바뀌어도 sfx 두 개를 처음부터 다시 풀어야 합니다.

### F. 검수가 만든 것 / 지운 것

- **합성 `timed.json` 을 만들지 않았습니다.** `tts.mjs` 를 실제로 돌렸고 10줄 전부 캐시 재사용
  (새로 만듦 0)이라, 지금 `build/timed.json` 은 현재 대본의 실측본입니다.
- 스틸을 추가로 뽑았습니다(`8.2 · 12.9 · 15.5 · 16.1 · 19.5 · 22.6 · 28.4 · 30.9 · 33.05 · 34.1 · 34.5s.png`).
  정상 산출물이라 `build/stills/` 에 남깁니다.
- 임시 스크립트는 만들지 않았습니다(전부 `node -e` 일회성).

---

### G. 델타 재검 (2026-08-08 · 반려 수정본) — 🟢 반려 해소, 승인 추천

재실행: `tts.mjs` → **새로 만듦 0 · 재사용 9**(현 대본의 실측본, 합성 아님) · 말 속도 6.45자/초.
`check.mjs` → **fail 0 · warn 1**(총 길이 37.2초, 권장대역 30~35 초과 · 상한 38 안).
1차본에서 걸렸던 **「본문 18~22초」 경고가 OK(20.8초)로 닫혔습니다.** 정적 최대 0.4s.

실측 타임라인(재구성):

| 샷 | 시작 | 끝 | 줄 rel |
|---|---:|---:|---|
| S0 | 0 | 4,599 | 4,249 |
| SH | 4,599 | 9,671 | 4,722 |
| S1 | 9,671 | 18,128 | 3,176 / **4,931** |
| S2 | 18,128 | 22,378 | 3,900 (두 줄 → 한 줄) |
| S3 | 22,378 | 25,276 | 2,548 |
| S4 | 25,276 | 30,101 | **4,475** |
| SO | 30,101 | **37,175** | 4,257 / 2,467 |

**TOTAL 37,175ms · 카드 34,175ms.**

#### G-1. 삼자가 ep12s-1 을 가리키는가 — ✅ 해소

- **정본 대조.** `order` 의 ep11s 다음 = `ep12s-1`. 그 편의 사건 = 「조리식 20개가 주민 10명 때
  숫자였는데 4명으로 줄이고도 그대로」(`hud`: 12-1편 · 조리식 20개는 주민 10명 때 숫자였다).
- **음성** 「다음 편은, 주민 4명인데 겨울 목표가 20개였어요.」 → 그 사건입니다. 문형 「다음 편은~」 ✅
  이 한 줄만 읽고 다음 편이 무슨 이야기인지 알 수 있습니다(제가 반려 때 쓴 판정 문장 통과).
- **카드** `outro.next` = 「12-1편 · 주민을 10명에서 4명으로 줄였는데 겨울 목표는 20개 그대로였어요」.
  작성팀 주장대로 **ep12s-1 `youtube.title` 머리절과 문자 단위로 일치**합니다(기계 대조 `true`).
  회차 번호도 12편 → **12-1편**으로 고쳐졌습니다.
- **미리보기** `nextpeek` — WINTER GOAL 20 이 강조색 점선에 잠긴 채 **한 픽셀도 안 움직이는 동안**
  아래 VILLAGERS 표식 열 개가 오른쪽부터 여섯 개 꺼져 넷이 됩니다(0.5초 루프).
  `ZONE`·`DISASTER`·재해 막대·밭 칸은 **코드에서 사라졌습니다**(`kinds/nextpeek.js` 전면 재작성).
  스틸 `33s.png` 로 실물 확인 — 셋이 같은 편, 같은 사건을 가리킵니다.
  🔑 층위 분리도 유지됩니다: 음성이 「4명인데 20개」를 말하고, 그림은 **그 20이 안 움직이는 것**을 보입니다.

#### G-2. 수치 4·10·20 의 실재 — ✅

ep12s-1 원문(`2026-07-26-…-flood.html`) 원문장:
> *"겨울 대비 목표가 「조리식 20개」로 못 박혀 있었는데, 예전에 주민이 **10명**이던 시절에 잡은
> 숫자였습니다. 그 뒤 주민을 **4명**으로 줄였는데도 **20**이라는 숫자는 그대로 남았습니다."*

세 수 모두 실재합니다. **화면에 그려지는 숫자는 `spec.goalNumber = "20"` 하나뿐**이라는 주장도
코드로 확인했습니다 — `HEADS = 10` · `KEEP = 4` 는 **표식 개수**로만 쓰이고 어디서도
`fillText` 되지 않습니다(그리는 문자열은 `NEXT` · `WINTER GOAL` · `VILLAGERS` · `20`).
스틸에서도 숫자는 20 하나입니다. 본문의 60 과 합쳐 회차 전체 화면 숫자는 **60 · 20 둘**이고
둘 다 각자의 정본 원문에 있습니다.

#### G-3. S2 합침 — 사실성 ✅ · cue 산수 ✅ (실측으로 검증)

- **문구** 「다시 조준해도 안 되더라고요. 전제를 의심했죠.」
  원문 소제목 *"AI와 함께 푼 장면: 증상 말고 전제를 의심하기"* · 본문 *"한 겹 아래 전제를 의심한
  덕분에 방향을 완전히 뒤집을 수 있었습니다"* — **「전제를 의심했다」는 원문의 말 그대로**입니다.
  🟡 잃은 것 하나: 대구 「알고리즘이 **아니라** 전제」가 자막에서 빠졌습니다. 다만 화면이
  `ALGORITHM` 층을 흐리고 `PREMISE` 판을 가르는 것으로 그 대비를 지고 있고, 영어 자막도
  `Re-targeting didn't help, so I started doubting the premise.` 로 인과를 유지합니다. 통과입니다.
- **cue 산수.** `c0 = cue(0, 0.15, 0.75)`, S2 `rel = 3,900ms` → `W = 3.075초`.
  `c1 = clamp((c0 − 0.70)/0.30)`, `off = 10·ease(clamp((c1−0.10)/0.30))`,
  `crackA = clamp((off−3.0)/3.5)`, `prog = ease(clamp((c1−0.22)/0.48))`.
  - 판이 벌어지기 시작(off > 0) = `c0 0.70` → 줄 시작 **+2.003초**
  - 균열이 실제로 그려지기 시작(`crackA > 0.02` **및** `prog > 0.01`) = `c0 0.774` → **+2.231초**
- 🔑 **「전제」가 실제로 발음되는 시각을 wav 로 쟀습니다**(추정 말고 실측).
  `build/audio/04-1mdtwmn.wav` 를 20ms 창 RMS 로 훑어 무음 구간을 뽑으면
  `[0–460] [1980–2160] [2900–2980] [3140–3760]` 이고, 문장 사이 침묵이 **1,980~2,160ms** 라
  **「전제를」의 발성 시작 = 2,160ms** 입니다.
  → 판이 벌어지는 것이 **−157ms**, 균열이 서는 것이 **+71ms**(30fps 2프레임).
  가이드 한도 ±20프레임(0.67초) 안에 넉넉히 듭니다. **나레이션이 「전제」를 부르는 순간 판이 갈라집니다.**
- 조준선 넉 줄(`AIMS 0.06·0.22·0.38·0.54`, 수명 0.26)의 마지막은 `c0 0.68`(+1.941초)에 완성되고
  `c0 0.80`(+2.310초)에 알파가 0 이 됩니다. 균열 시작(+2.231초)과 **0.08초 겹치지만** 위층은
  `upA` 로 이미 어두워졌고 y 대역이 갈려 있어 문제 없습니다(스틸 `20.4s.png` — 균열이 막 서는
  프레임에서 조준선은 이미 안 보입니다).
- **옛 반려 결함 재발 없음** — `20.4s.png` 에서 판이 **낱말 사이 공백**에서 벌어지고
  「쫓아가서 / 건네준다」 어느 글자도 복제·절단되지 않았습니다.

#### G-4. sfx 근거 줄 — ✅ 불변

`timed.json` 실측이 그대로입니다: **S1[1] 4,681 + 250 = 4,931ms** · **S4[0] 4,275 + 200 = 4,475ms**.
sfx 블록도 `at`·`kind`·`gain`·`dur`·`freq`·`from`·`to`·`delayMs`(3,150 / 1,720)·`why` 전부 그대로입니다.
줄이 바뀐 곳은 S2(sfx 없음)와 SO[0](sfx 없음)뿐이고, `cue` 창은 자기 줄 `rel` 만 쓰므로
**두 `delayMs` 는 재계산이 필요 없습니다.** 앞서 검증한 Δ3ms · Δ6ms 가 그대로 유효합니다.
밀도는 본문 20.8초에 2개로 기준(23초에 1~2개) 안입니다.

#### G-5. 아웃트로 카드 산수 재검 — ✅ 여유가 더 늘었습니다

TOTAL 이 37,163 → **37,175ms**, 카드가 34,163 → **34,175ms** 로 12ms 이동했고,
예고 줄 `rel` 이 3,066 → **4,257ms** 로 길어졌습니다. 다시 풀면:

- **S4 페이오프** — S4 시작 25,276 + `c0 0.54` 지점 1,864 = **27.140초**. 카드까지 **7.04초**
  (1차본 5.74초에서 더 벌어졌습니다). 스틸 `28.4s.png` 계열과 같은 그림입니다.
- **미리보기** — `ak = 1` 은 `W = 0.15 + 4.257 × 0.30 = 1.427` 로 샷 시작 **+1.277초**
  = 절대 **31.378초**. 카드까지 **2.80초**가 남아 0.5초 루프가 **5바퀴 이상** 돕니다
  (1차본 3바퀴 → 개선). `33s.png` 가 루프의 「4명만 남은」 국면입니다.
- **소개 자막(SO[1])은 34.358초 시작**으로 여전히 카드 뒤이고, 그 줄에 그림을 안 건 판단은 유효합니다.
- 예고 줄이 길어져 `nextpeek` 등장 창(`W`)도 함께 늘었으므로 **cue 산수가 자동으로 따라왔습니다** —
  자막 길이를 바꾸고 kind 를 안 열어 본 자리가 없는지 확인했고, `settle.js`·`premise.js`·`nextpeek.js`
  세 곳 다 자기 줄 `rel` 로만 계산합니다.

#### G-6. 얽힌 자리 재확인 (통과 항목 중 델타의 영향권)

- **제목 이행** — 훅 줄·S1[0] 를 안 건드렸으므로 겹침 3건(품삯·짓고·60초) 그대로 ✅
- **영어 자막** — 9줄 전부 존재·2줄 이하 ✅. 새 두 줄 대조:
  S2 `Re-targeting didn't help, so I started doubting the premise.` /
  SO[0] `Next up: only 4 villagers left, but the winter goal was still 20.` —
  원문에 없는 수치·주장 0, 시제(과거)가 한국어와 맞습니다.
- **말투** — 종결 9줄: ~요/에요/어요 5 · ~죠 2 · ~더라고요 1 · ~입니다 1. 3연속 없음 ✅
- **게이트 픽셀 4종**(팔레트·정적·잘림·결정성) 전부 green, 결정성 185프레임 불일치 0 ✅

#### G-7. 🟡 잔여 정정 2건 (영상에 안 나갑니다 — 마스터 단계에서 닫으세요)

1. **`notes.길이` 에 2차(델타) 실측 칸이 또 비어 있습니다.** 지금은 1차 4단본 값
   (37.2 / 본문 **22.1** / 예고 3.0 / 6.42자/초)을 인용하고 *"새 실측은 아직 없다 — 렌더 뒤
   두 줄을 다시 인용해 갈아 끼울 것"* 으로 끝납니다. 델타 실측이 났으니 채우세요 —
   **총 37.2초 · 본문 20.8초(OK) · 예고 4.3초 · 말 속도 6.45자/초.**
   (1차 때 낸 같은 지적이 한 칸 뒤로 밀린 형태입니다. 자체 산정식을 안 쓴 것은 계속 맞습니다.)
2. **`premise.js` 주석이 옛 문구와 산정 `rel` 로 검산돼 있습니다.** 주석은 자막을
   「다시 조준해봐**도** 안 되**길래**, 전제를 의심했죠.」(앞 15자/뒤 10자)로 적고
   균열 시작을 「+2.03초」라 했는데, 실물 자막은 「다시 조준해도 안 되더라고요. 전제를 의심했죠.」
   이고 실측 `rel` 3,900ms 로 풀면 **+2.231초**입니다. **결론은 안 바뀝니다**(실측 발성 2,160ms 와
   +71ms) — 문구와 숫자만 실측으로 고치세요.

#### G-8. 판정

**🟢 승인 추천.** 반려 사유였던 「예고·카드·미리보기가 다른 편을 가리킴」은 **정본 기준으로 해소**됐고,
그 수정이 부른 길이 이동·cue 이동·wav 발성 위치까지 실측으로 다시 풀어 전부 맞습니다.
남은 warn 1건(총 37.2초)은 상한 38 안·사용자 「35 ± 3」 안이며, 인트로·훅·예고·소개를 깎아
만든 길이가 아닙니다(본문 가운데에서만 덜었습니다).
🔴 **승인 후 `render.mjs` → `srt.mjs` → `publish.mjs` 재실행이 필요합니다** — `build/` 의
`video.mp4`·`ep11s.en.srt`·`upload.txt` 는 아직 이전 판본이고, 게이트는 이 셋을 보지 않습니다.
