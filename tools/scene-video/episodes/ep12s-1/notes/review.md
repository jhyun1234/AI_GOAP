# 검수 리포트 — `ep12s-1` (12-1편)

> ⚠️ **이 절은 3단 대본(개정 전) 기준의 1차 검수입니다.** 2026-08-08 4단 구조 전환본의
> 검수는 이 파일 아래쪽 **「검수 리포트 ② — 4단 구조 전환본」** 을 보세요.
> (여기 적힌 길이 규격 22.6~28초 · 예고 3.0초 · 「4샷/7줄」은 전부 옛 규격입니다.)

**판정: 🔴 반려 (지적 1건)**
막는 것은 **한 줄뿐**입니다 — `youtube.blurb` 에 형제 편(12-2·12-3)으로 가는 길이 없습니다.
**영상은 안 건드립니다**(blurb 은 mp4 에 안 들어갑니다). 나머지 전 항목은 통과이고,
검산한 값은 아래에 전부 남깁니다.

검수일 2026-08-08 · 리포 `C:\Users\anjyo\AI_GOAP`

---

## 0. 기계 점검 — `check.mjs` 23종 전부 통과

`build/timed.json` 은 **진짜 TTS 산출물**입니다(`build/full.wav` 2,231,386바이트 +
`build/audio/` 7개 wav 가 같이 있고 `timed.json` 의 `file` 이 그 파일들을 가리킵니다).
**합성 `timed.json` 을 만들지 않았고, 지운 것도 없습니다.**

```
가이드 점검 · ep12s-1
────────────────────────────────────────────────────
  OK shot 메타 완비             4샷 전부 있음
  OK 형제 라벨 = 그 편의 youtube.title 3개 전부 일치
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               7줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 영어 자막 전 줄 존재           7줄 전부 있음
  OK 영어 자막 2줄 상한            7줄 전부 2줄 이하
  OK 제목 이행 (첫 두 줄)          겹침: 주민, 10명에, 겨울, 목표, 20개
  OK 제목 후킹 (수치)             숫자 10, 4, 20
  OK 다음 편 예고 있음             S4 자막 1줄 (음성 예고)
  OK 예고 3초 이하               2.6초
  OK 자막 2초 미만 없음            전부 2초 이상
  OK 말 속도 6.0~7.2자/초        6.6자/초 (참고 영상 6.93)
  OK 총 길이 28초 이하            26.7초 = 자막 25.3 + 꼬리 1.40 (목표 22.6~28초 · 이 값이 실제 mp4 길이다)
  OK 총 길이 22.6~28초 권장대역     26.7초
  OK 본문 20초 이상              본문 21.5초 (예고 2.6 + 아웃트로 2.6 제외)
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 하단 안전영역 (바닥 443px 비움)  자막 바닥 1440 · 비주얼 바닥 1266 (한계 1477)
  OK 결정성 (3패스)              133프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  OK 정적 구간 3초 이하            최대 0.2s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
전부 통과
```

`notes.길이` 는 이 출력을 **인용만** 하고 자체 산정식을 적지 않았습니다 — 정정 요구 없음.

---

## 🔴 반려 사유 (1건) — `youtube.blurb` 에 형제 편으로 가는 길이 없다

한국어 `blurb` 마지막 문단이 「조건이 바뀔 때마다 … 누군가 계산을 잊습니다.」로 끝나고,
**12-2편·12-3편이 한 번도 안 나옵니다.**

```
$ node -e "... blurb 안의 /\d+-\d+편/ 전수"
ep10s-3 | ko blurb 형제 언급: 10-1편,10-2편
ep12s-1 | ko blurb 형제 언급: 없음
ep12s-2 | ko blurb 형제 언급: 없음
ep12s-3 | ko blurb 형제 언급: 없음
```

이 리포의 선례는 **다섯 편이 전부 넣었습니다**(`ep08s-1` · `ep08s-3` · `ep10s-1` ·
`ep10s-2` · `ep10s-3`). 형태도 같습니다 —

> `ep10s-1` blurb 끝: 「(같은 글에서 나온 **3편 중 1편** — 주민이 목수에게 집을 지어달라고
> 부탁하는 이야기는 **10-2편**, 다 만들어 놓고 편한 기능 하나를 일부러 지운 이야기는 **10-3편**)」

`outro.siblings` 가 있는 회차(`ep08s-1`·`ep10s-1`)도 **둘 다** 갖고 있으므로,
카드가 생겼다고 blurb 쪽이 면제된 것이 아닙니다. `en.blurb` 에는
`Series: … Episode 12, part 1 of 3` 이 있는데 한국어에만 없습니다.

**요구**: `youtube.blurb` 끝에 한 줄. 12-2·12-3 을 **번호와 사건으로** 가리키세요.
영상·타이밍·자막은 그대로 두면 됩니다(재렌더 불필요).

---

## 1. 사실관계 — 원문 대조 🟢

### 브리프 오염 / 지워진 표
원본 HTML 을 직접 열어 셌습니다:

```
$ node -e "... 2026-07-26-unity-goap-m9-zone-disaster-flood.html"
table 0 pre 0 svg 0 comments 0 img 0 ul 0   (본문 10,165자)
```

**`<table>` 이 애초에 0개**라 기획팀 정규식이 지운 것이 없습니다 — 「표 안에만 있던 값」이라는
경로 자체가 성립하지 않습니다. 주석 0개라 `selected_commits_range` 도 없고, 작성팀이
**커밋 수를 화면·제목·blurb 어디에도 안 썼습니다.** ✅

### 원문 숫자 전수 vs 화면 숫자
원문 본문에 나오는 수(중복 제거): `10 30 1 20 4 2 3 5 0 40`
화면에 실제로 그려지는 수: **`20`**(S1 대형 숫자 · S1 격자 20칸 · S2 막대 20칸) ·
**`10` → `4`**(S2 `headcount.js:104,108`). **셋 다 원문 문장 그대로**입니다.

- 「겨울 대비 목표가 "조리식 20개"로 못 박혀 있었는데, 예전에 주민이 10명이던 시절에 잡은 숫자였습니다.」
- 「그 뒤 주민을 4명으로 줄였는데도 20이라는 숫자는 그대로 남았습니다.」

화면에 **없는** 것도 확인했습니다 — `30`·`40`(12-2 몫) · `3일치`(명세서에 적어 둔 바람이지
관측값이 아님) · `+2`·`2`·`5개`·`1명`(5단계 곁가지) · 반경 · 포만감 값 · 계절 배율. ✅

### 화면 문자열 20종 전수
`WINTER GOAL`(겨울 대비 목표) · `FIXED`(못 박혀 있었는데) · `DISASTER`(재난(재해)) ·
`VILLAGERS`(주민) · `PER PERSON`(10명이 먹을 양을 4명이 나눠 먹으니) · `WAREHOUSE`(창고) ·
`COOKED`/`RAW`(조리식과 생식) · `SEASON`(계절 소모 속도) · `FoodDaysLeft`(원문 그대로) ·
`식량 N일치`(원문 그대로, N 포함) · `SPRING`/`SUMMER`/`AUTUMN`/`WINTER`(봄·여름·가을·겨울 전부
원문에 있음 — 실측 확인) · `MANUAL`(손으로 다시 계산) · `AUTO`(자동 비례) ·
`겨울이 안락했다`(원문 **절 제목** 그대로). **지어낸 문자열 0건.** ✅

### 자막 7줄
| 줄 | 원문 근거 |
|---|---|
| 재해를 내리기 전에 / 이상한 걸 찾았어요 | 원문 전개가 실제로 그 순서다 — 「문제: 겨울이 안락했다」를 먼저 쓰고, 그 뒤 「그래서 이번 밀스톤의 목표를 두 축으로 잡았습니다. … 재해 축 — 하늘이 실제로 마을을 때려서 잉여를 깎는다.」 **재해는 이 발견의 대응책**이지 앞선 사건이 아니다 |
| 겨울 목표가 조리식 20개 / 주민 10명 때 숫자예요 | 원문 그대로 |
| 근데 4명으로 줄이고도 / 20은 그대로였죠 | 원문 그대로 |
| 10명 몫을 4명이 먹으니 / 겨울이 편하죠 | 「10명이 먹을 양을 4명이 나눠 먹으니 겨울이 안락할 수밖에요」 |
| 그래서 개수를 버리고 / 「며칠 버티나」로 바꿨어요 | 「밸런스의 언어를 개수에서 일수로 바꿨다」 / 「지금 이 마을은 며칠 버티나」 |
| 주민이 두 배면 일수는 절반 / 계산을 잊을 일이 없죠 | 「주민이 두 배가 되면 일수는 절반이 됩니다」 / 「언젠가 반드시 누군가 계산을 잊습니다」 |
| 다음 편, 재해에도 / 겨울이 편해요 | 「재해까지 넣었는데도 겨울이 여전히 편했던 겁니다」(12-2 가 맡을 문장) |

---

## 2. 🔴 앞 편(`ep11s`)의 약속을 이어받았는가 — **정직하다. 통과**

`ep11s/scene.json` 의 `outro.next` = 「12편 · 마을에 처음으로 재해가 생깁니다」,
음성 = 「다음 편, 마을에 처음 재해가 와요.」 (직접 확인함). 이 편의 사건은 재해가 아닙니다.

**① 인과 잠금이 원문에서 참입니다** — 순서를 못 바꾼다는 기획팀 주장을 원문으로 검증했습니다.

> 「그래서 재조정이 줄줄이 이어졌습니다. **첫째, 밭 재파종을 식량 일수로 잠갔습니다.**」(12-2)
> 「바로 앞 단계에서 제가 직접 넣은 **재파종 잠금 때문이었습니다.**」(12-3 의 원인)

즉 `FoodDaysLeft`(12-1) → 식량 일수로 재파종 잠금(12-2) → 홍수가 밭을 못 때림(12-3) 이
**원문이 스스로 적은 인과 사슬**입니다. 12-1 을 뒤로 미루면 12-2 의 잠금이 근거를 잃습니다.
**순서 변경 불가는 사실입니다.**

**② 갚는 방식이 시청자를 속이지 않습니다.**
- 0초 첫 낱말이 「재해」입니다. ep11s 에서 들은 말을 **첫 자막 첫 단어로** 만납니다.
- 문장이 「내리기 **전에**」라고 **명시적으로 유예**합니다 — 재해가 일어났다고 주장하지 않습니다.
- 그림도 같은 말을 합니다: `quota.js` 의 `DISASTER` 쐐기가 `descend = ease(clamp(c0/0.45))`
  로 `lerp(-34, 44)` 까지만 내려오고 **멈춥니다**(스틸 `2000ms` 확인 — 판 위에 떠 있음).
- 3.24초에 이 편의 사건(조리식 20개)이 도착합니다. 도입부로 안 끌었습니다.
- 아웃트로 카드가 형제 셋을 다 띄우고 그중 12-2·12-3 이 재해 편임을 글자로 말합니다
  (스틸 `24400ms` 확인).

**판정**: 「속았다」가 아니라 「그 앞 이야기부터 하는구나」로 읽힙니다. **반려 사유 아님.**
🟡 다만 이 편의 유일한 시각적 유예 신호가 「쐐기가 멈춘다」뿐이라, 멈춘 줄 모르는 시청자는
0~1초 사이 재해를 기대할 수 있습니다. 둘째 줄이 3초 안에 정정하므로 문제 삼지 않습니다.

---

## 3. 🔴 첫 줄 판정 — 실측 3기준 전부 통과

> 「재해를 내리기 전에 / 이상한 걸 찾았어요.」

1. **수치 스펙으로 안 열었다.** 숫자가 한 개도 없습니다. 제목에 10·4·20 이 있는데도 첫 줄이
   수치 설명으로 밀리지 않았습니다 — 「제목의 숫자 때문에 첫 줄이 수치 설명이 되면 반려」에
   해당하지 **않습니다.** (꼴찌 17.35% 사례의 형태가 아님) ✅
2. **우리 프로젝트의 진행 상황부터 말하지 않았다.** 18.30% 를 만든 「재설계가 끝난 그 자리에서」는
   *개발 공정*이 주어였습니다. 이 문장의 「재해를 내린다」는 **게임 안에서 벌어지는 일**이고,
   이 프로젝트를 몰라도 뜻이 섭니다. ✅
3. **앞 편을 봐야 이해되는 문장이 아니다.** 「성격은 붙였는데요」(39.15%)와 달리 전제가 없습니다.
   ep11s 를 안 본 사람도 「재해를 내리기 전에 이상한 걸 찾았다」로 완결됩니다. ✅

🟡 **의견(4번 기준 · 미검증이므로 반려 아님)**: 첫 줄의 주어가 **개발자(나)** 이고,
주민이 무언가를 하다가 어긋나는 그림은 아닙니다. 상위 3편(거절·몇 분째 반복·거절)의 형태와는
다릅니다. **다만 이 기준은 15편을 본 뒤에 만든 미검증 분류**이므로 등급을 섞지 않고 의견으로만
올립니다. 참고로 이 편에서 어긋남은 **둘째 줄**이 집니다(「주민 10명 때 숫자예요」).

---

## 4. 제목 · 제목 후킹

- 제목: 「주민을 10명에서 4명으로 줄였는데 겨울 목표는 20개 그대로였어요」
- **제목 이행**: 첫 두 줄 겹침 5개(주민·10명·겨울·목표·20개). 게이트 통과 + 사람 눈으로도
  둘째 자막이 **3.24초**에 제목의 약속을 화면(대형 `20`)과 소리로 동시에 도착시킵니다.
- **후킹(수치)**: 아라비아 숫자 10·4·20. 🔑 **셋 다 원문 문장 그대로**임을 기획 브리프
  「수치 목록」과 원문 양쪽에서 대조했습니다 — **지어낸 수 0건.**
  모순 쌍(「사람은 줄었는데 숫자는 그대로」)이라 지침이 말한 가장 센 형태입니다.

---

## 5. 그림이 뜻을 나르는가 — 스틸 7장 확인

찍은 시각(샷 시작은 `SHOT_TAIL` 0.35초 포함 누적 — S1 0 / S2 8,152 / S3 15,611 / S4 19,551 ·
`TOTAL` 26,699): `1400 · 2000 · 6900 · 11000 · 14300 · 18400 · 22200 · 24050 · 24400`

- **S1 `quota`** — 사람이 한 명도 없는 화면에 20칸이 차고 대형 `20` 이 뜬 뒤 흰 죔쇠가 물리며
  `FIXED`. 「덩어리만 있고 나누는 쪽이 없다」가 그대로 읽힙니다. `reads` 가 빈말이 아닙니다.
- **S2 `headcount`** — 위 20칸은 한 칸도 안 변하고(죔쇠만 굵어짐), 아래 표식이 10→4 로 꺼지고,
  `PER PERSON` 강조색 막대가 자랍니다. **이 회차의 심장이고 축이 한 화면에 다 보입니다.**
- **S3 `daysleft`** — 분자(COOKED+RAW) / 분수선 / 분모(VILLAGERS·SEASON) / 결과 카드.
  **계산식 안에 숫자가 한 자도 없습니다** — 원문의 「계산식 안에 숫자를 복사해 넣지
  않았습니다」를 그림이 그대로 지킵니다.
- **S4 `autoscale`** — 표식 4→8, 막대 절반, `MANUAL`→`AUTO`, 마지막에 `WINTER` 칸이 채워지며
  막대가 한 단 더. 세운 축을 흔들어 보이는 증명입니다.

### 🟡 지적 없음 — S4 밀도(요소 5개)는 스틸로 통과
작성팀이 스스로 걱정한 자리입니다. `22200` · `24050` 스틸에서 **표식 8 · 막대 · 계절 칸 4 ·
계절 이름 4 · 배지** 가 전부 서로 안 겹치고, 계절 이름이 칸 **밖 아래**(y 196)에 있어
겨울 블록이 글자를 덮지 않습니다. 라벨 가림 0건. **배지를 뺄 필요 없습니다.**

### 🟡 지적 (1) — S2 「1인 몫 다섯 칸」: **통과. 다만 근거를 정확히 적어 둡니다**
`headcount.js:152` `const n = lerp(2, 5, share)` — 20÷10=2, 20÷4=5 입니다.
- 화면에 **숫자를 한 자도 안 적습니다.** 스틸 `11000`/`14300` 으로 확인.
- 5 는 **원문의 두 수(20·4)에서 나오는 나눗셈 결과**이고, 원문 자신이 그 나눗셈을 문장으로
  씁니다 — 「10명이 먹을 양을 4명이 나눠 먹으니」. 원문에 없는 **새 사실**을 주장하지 않습니다.
- 눈금자(`i=0..6`, `PITCH` 15.4)가 있어 「몇 칸」을 세는 것은 가능하지만, 세어도 나오는 값이
  20÷4 라 **틀린 수가 나올 수 없습니다.**
- **판정: 지어낸 수치 아님.** 「성격 거리 6종 중 산문에 남은 양 끝 둘만 그렸다」와 같은
  통과 형태입니다.

### 🟡 지적 (2) — **작성 노트의 겹침 주장이 부정확하다 (그림이 아니라 노트를 고칠 것)**
`writer.md` §5 와 `quota.js` 주석이 *"검사 테두리는 격자 바깥으로 10px 물러나 그리고,
3px 획을 감안해도 칸 경계(y 90)에 4.5px 못 미친다"* 라고 단언합니다. **사실이 아닙니다.**

`quota.js:126-128` 의 링은 **높이가 자라는 사각형**입니다:
`rh = (RING_B - RING_T) * ring` → 아래 획이 y 88 에서 y 158 까지 **쓸고 내려갑니다.**
격자는 y 90~150 이므로 **두 줄을 통과합니다.**

증거(267프레임 전수 스캔, 캔버스 픽셀 직접 판독):
```
S1 : 8프레임에서 강조색×흰(track) 혼합 검출, 최대 2,188 device px, y 88~150
     1800ms 표본 색 = rgb(67,255,167) @ a87, y 147~150, x 20~326
```
스틸 `1400ms` 에서 눈으로도 보입니다 — 점선 테두리의 아래 획이 첫 줄 칸을 가로지릅니다.

**그런데 반려는 아닙니다.** ① 그때 칸은 아직 **`tone('track')` = 흰 0.12** 짜리 안내선일 뿐
흰 글자도 흰 채움도 아닙니다(격자 채움 `fill` 은 다음 자막 `c1` 에서 시작). ② 움직임이
「검사 테두리가 격자를 훑는다」로 읽혀 오히려 뜻과 맞습니다. **고칠 것은 그림이 아니라
「안 겹친다」는 노트의 단언입니다.**

### 🟢 흰색 × 강조색 — **불투명 합성 0px**
`openEngine` 으로 열어 267프레임(100ms 간격) 전 구간의 캔버스를 직접 읽었습니다.
흰 α 를 `#00FF88` 에 얹은 픽셀은 `g=255 · r=255α · b=255α+136(1−α)` 로 역산되므로 그 형태만 셌습니다.

```
합성 알파 a>120 (실질 불투명)  →  0 프레임 / 0 px      ← 흰 글자·흰 선을 덮은 자리 없음
합성 알파 a>20  (희미한 것 포함) →  최대 rgb(232,255,247)@a33
```
`a>20` 쪽에 잡히는 것은 두 가지뿐이고 **둘 다 `setShadow(GLOW,14~15)` 의 후광이
0.12 알파 안내선에 닿은 것**입니다(S1 위 ②, S4 는 `autoscale.js:110` 의 트랙 왼쪽 획이
rgb(232,255,247) 로 아주 살짝 물듦). **강조색이 흰 것을 덮은 자리는 한 프레임도 없습니다.**
🟡 노트가 「6px 물러났다 / 4px 떨어져 있다」로 **좌표만** 따졌는데 GLOW 후광은 그 거리를
넘습니다 — 결과는 무해하지만 노트의 논거를 고쳐 두는 편이 다음 회차에 안전합니다.

---

## 6. 말과 그림이 같은 순간에 도착하는가 — cue 감사 (직접 풀었음)

`cue(i, lead, span)` = `a = l.t − lead`, `b = l.t + l.dur × span` (`engine.js:289-293`).
`l.dur` 은 **`rel` = `dur` + `pauseAfter`** 입니다(`engine.js:163`). 실측 `rel`:
S1 3,237 / 4,565 · S2 3,550 / 3,559 · S3 3,590 · S4 4,234 / 2,564.

### ① `t` 로 도는 조건 — **사건 0건**
네 kind 를 전부 열어 `t` 를 읽는 자리를 뽑았습니다. 전부 **장식**입니다 —
바닥/윤곽 점선 `lineDashOffset`, 표식 바운스 `Math.sin(t*…)`, 막대 안 홈의 흐름 `(t*17)%PITCH`,
분수선 위를 도는 표식 `(t*74)%span`, 상자 미세 표류. **`frac(t/N)` 형 사건 트리거 없음.** ✅

### ② 다른 cue 에서 파생된 문턱 — **0건**
| kind | 사건 | 어느 cue 에서 나오나 |
|---|---|---|
| `quota` | `descend`·`ring` | `c0`(findCue 0) |
| `quota` | `fill`·`nA`·`nail` | `c1`(fixCue 1) |
| `headcount` | `cut`·`clampK` | `c0`(cutCue 0) |
| `headcount` | `share`·`verdict` | `c1`(shareCue 1) |
| `daysleft` | `num`·`line`·`den`·`arr`·`res` | `c0`(buildCue 0) 하나 |
| `autoscale` | `grow`·`halve`·`badge` | `c0`(halveCue 0) |
| `autoscale` | `winter` | `c1`(winterCue 1) |
**A 자막 진행도로 B 자막의 사건을 그리는 자리가 없습니다.** ✅ (ep00i `runmark` 형 사고 없음)

### ③ 착탄 시각을 초로 환산해 발화 구간 안인지 확인 (가이드 한도 ±0.67초)
| 사건 | 진행도 | 자막 시작 기준 | 그때 말하는 음절 |
|---|---|---|---|
| `ring` 완성(이상한 걸 찾았다) | `c0` 0.90 | +2.02초 | 「이상**한**」 |
| 대형 `20` 완성 | `c1` 0.58 | +1.92초 | 「스**무**」 |
| 죔쇠 `nail` 시작 | `c1` 0.62 | +2.07초 | 「스**무** 개」 |
| 표식 10→4 완료 | `c0` 0.75 | +1.98초 | 「줄이**고**도」 |
| 판정 「겨울이 안락했다」 완성 | `c1` 0.92 | +2.35초 | 「겨**울**이」 |
| 표식 배증 완료 | `c0` 0.40 | +0.84초 | 「두 **배**」 |
| 막대 반감 완성 | `c0` 0.86 | +1.98초 | 「절**반**」 |
전부 그 사건을 부르는 자막의 발화 구간 안입니다. ✅

---

## 7. 🔴 마지막 2.6초 — 아웃트로 카드가 페이오프를 먹지 않는다

작성팀 계산을 **독립적으로 다시 풀었습니다.**

```
S4 시작 = 0(S1) + 8,152 + 7,459 + 3,940 = 19,551ms          (샷마다 SHOT_TAIL 350ms 포함)
TOTAL   = 19,551 + (4,234 + 2,564 + 350) = 26,699ms
카드 시각 = TOTAL − OUTRO_MS(2,600) = 24,099ms              (engine.js:181, 259)

c0 창 W = 0.15 + 4.234 × 0.55 = 2.4787초
  halve 완성 c0=0.86 → 0.86×2.4787 − 0.15 = +1.982초 → 절대 21.533초
  badge(MANUAL→AUTO) 완성 c0=1.00 → +2.329초 → 절대 21.880초
여유 = 24.099 − 21.880 = 2.219초   ✅
```
작성팀 기재값(21.88 / 24.099 / 2.22초)과 **일치**합니다.
확인 사살로 **카드 직전 프레임(`24050ms`)** 을 뽑았습니다 — 8명·반감 막대·`AUTO`·`WINTER` 채움이
**전부 화면에 있습니다.** `ep08s-3` 형 사고(알파 0.078) 없음.

예고 자막(S4[1])은 페이오프를 안 지고 `winter` 만 집니다. 카드 전 노출 = 24,099 − 23,785 =
**0.314초**, 그 안에 `winter` 가 0 → 0.787 까지 가고 막대가 폭의 23.6% 를 줄입니다
(`c1` 창 W = 0.15 + 2.564 × 0.80 = 2.2012). 실측 정적 구간 최대 0.2초와 맞습니다. ✅

---

## 8. 🔴 효과음 — 통과 (형제 편 `thud`+`drop` 동일 쌍 포함)

### 배치·개수
26.7초에 **2개**(S1 `thud` · S4 `drop`, 나머지 두 샷 무음). 기준 「23초에 1~2개」 대역 안이고
밀도 0.075개/초로 0.06~0.09 안입니다. 둘 다 **나레이션이 부르는 사건**에 붙었습니다
(못 박힌다 / 반으로 내려앉는다). 정적을 메우려고 깐 소리 없음. `why` 전부 채워져 있음. ✅

### `delayMs` — 독립 재계산
```
S1 thud (at=1)  W = 0.15 + 4.565 × 0.75 = 3.5738 ;  nail 열림 c1=0.62
                → 0.62 × 3.5738 − 0.15 = 2.0657초 = 2,066ms   (기재 2,070 · 오차 4ms)
S4 drop (at=0)  W = 0.15 + 4.234 × 0.55 = 2.4787 ;  halve 열림 c0=0.72
                → 0.72 × 2.4787 − 0.15 = 1.6347초 = 1,635ms   (기재 1,630 · 오차 5ms)
```
둘 다 **0ms 동시 발사가 아니고**, 지속음을 「움직임이 시작되는 첫 프레임」에 건 것이 맞습니다.
`thud` 은 「스무 개」를, `drop` 은 「절」을 덮고 그 구간이 막대가 실제로 내려가는 구간
(1.635~1.992초)과 겹칩니다. ✅

### 피크 천장 — 인자 그대로 `synth()` 를 호출해 확인
```
peak thud(0.064,0.19,264) = 0.1894      peak drop(0.070,0.36,430→150) = 0.1324
천장 0.46 → 경고 출력 0건
```
「kind 별 상한표」를 믿지 않고 실제 인자로 돌렸습니다. 여유 충분. ✅

### 🔴 형제 `ep12s-3` 과 kind 쌍이 같다 — **판정: 돌려막기 아님. 통과**
작성팀이 자진 신고한 자리입니다. `sfx.mjs:60-70`(`thud`)·`114-124`(`drop`) 을 열어
**둘 다 `rng` 를 안 부르는 것**을 확인했고, 그래서 파라미터 대조가 파형 대조라는 주장은
맞습니다. 다만 **주장을 그대로 받지 않고 파형을 실제로 만들어 상관을 쟀습니다**:

```
corr thud  ep12s-1(0.19/264) vs ep12s-3(0.25/198) = 0.1295
corr drop  ep12s-1(0.36/430→150) vs ep12s-3(0.44/700→130) = 0.0196
corr drop  ep12s-1 vs ep11s(0.50/640→84)               = 0.0290
```
(직전 회차가 반려된 `latch` 사고는 상관 **0.977** 이었습니다. 여기는 두 자릿수 아래입니다.)

부르는 사건도 갈립니다 — `thud`: 숫자가 **고정**된다 / 12-3 은 **헛닿음**.
`drop`: 값이 **반이 된다**(좋은 일) / 12-3 은 밭이 **부서진다**(파괴).
소리 상자에 kind 가 여섯뿐이라 **세 편에서 쌍이 겹치지 않게 하라는 요구는 과도**하고,
`order` 상 이 편이 **먼저**이므로 부담이 있다면 12-3 쪽입니다.
🟡 「둘 다 thud 로 열고 drop 으로 닫는다」는 형태 반복은 사실로 기록해 둡니다 — 값은 그대로 두세요.

---

## 9. 그림이 이 회차 소유인가 — 형제 8개 직접 대조

| 편 | kind | 기본 도형 |
|---|---|---|
| **ep12s-1** | `quota`·`headcount`·`daysleft`·`autoscale` | **칸(cell)과 그것을 나누는 수** |
| ep12s-2 | `overshoot`·`knobs`·`costgap`·`pairline` | **선 하나와 그 선에 걸린 값** |
| ep12s-3 | `flood`·`hunt`·`flip`·`between` | **밭 칸 한 줄** |

`ep12s-3` 이 「밭 칸 한 줄」이라 계열이 겹칠 위험이 있어 **스틸을 뽑아 나란히 봤습니다.**
12-3 S1 은 큰 밭 칸 5개 + 점선 빈 자리 + 물띠 + 로그 카드이고, 12-1 S1 은 작은 칸 20개 +
대형 숫자 + 죔쇠입니다. **같은 그림으로 안 읽힙니다.** 리포 전체 `kinds/*.js` 해시 중복 0건,
파일 이름도 이 넷은 이 회차만 씁니다. ✅

---

## 10. 🔴 예고 — 「그 한 줄만 읽고 다음 편을 아는가」

- 음성: **「다음 편, 재해에도 겨울이 편해요.」** (실측 2.564초 · 상한 3.0초)
- 카드: `outro.next` = **「12-2편 · 재해를 넣었는데 겨울이 편했어요」**
- `ep12s-2` 의 **실제 첫 두 줄**(직접 열어봄): **「마을에 재해를 내렸는데 / 겨울이 편했어요.」**

**셋이 같은 것을 가리킵니다.** ep08s-2 의 「다음 편, 다 같이.」 형 사고(둘이 서로 다른 이름)
아닙니다. 그리고 **그 한 줄만 읽어도 다음 편이 뭔지 압니다** — 「재해를 넣었는데도 겨울이 편했다」.
숫자(40)를 안 넣은 판단도 옳습니다(맥락 없이 40 은 뜻이 0). ✅
사실성도 원문에서 확인: 「결과는 40이었습니다.」 / 「재해까지 넣었는데도 겨울이 여전히 편했던 겁니다.」

**길이를 맞추려고 예고나 첫 줄을 깎은 흔적 없음** — 예고 2.6초는 상한 아래이고
본문 21.5초로 20초 하한 위, 총 26.7초로 대역 한복판입니다.

---

## 11. 버린 것 — `excluded` 9건 전부 납득됨

원문이 **이름 붙인 개념**이 버려졌는지 따로 봤습니다: `ZoneService`·앵커·`DisasterSO`·
`DisasterService`·「창고는 성역」·재파종 잠금·「콘텐츠 추가 비용표」. **전부 12-2/12-3 에
배분됐고 사유가 「어긋난 상태가 없다(구현)」 또는 「다른 편의 사건」으로 일관됩니다.**

특히 두 건이 좋습니다:
- 「방치가 안락하면 실패다 / 10분, 30분」 — 이 글에서 가장 센 문장인데, **10·30 이 제목의
  10·4·20 과 같은 축의 수로 오독된다**는 이유로 뺐습니다. 대신 판정 결과만 원문 절 제목 그대로
  화면에 올렸습니다(「겨울이 안락했다」). 정확한 판단입니다.
- 「3일치」 — **관측값이 아니라 명세서에 적어 둔 바람**임을 알아보고 HUD 칩을 원문 그대로
  `식량 N일치` 로 뒀습니다. 이것이 이 편에서 가장 위험했던 자리였고, 안 밟았습니다.

---

## 12. 🌍 영어 자막 — 한 줄씩 대조

| # | `text` | `en` | 판정 |
|---|---|---|---|
| 1 | 재해를 내리기 전에 / 이상한 걸 찾았어요 | Before I sent a disaster in, / I found something odd. | ✅ |
| 2 | 겨울 목표가 조리식 20개 / 주민 10명 때 숫자예요 | The winter goal was 20 cooked meals — / set back when we had 10 villagers. | ✅ 수치 20·10 그대로, 추가 0 |
| 3 | 근데 4명으로 줄이고도 / 20은 그대로였죠 | Then I cut them down to 4, / and the 20 never moved. | ✅ |
| 4 | 10명 몫을 4명이 먹으니 / 겨울이 편하죠 | Four people eating ten people's food. / Of course winter felt easy. | ✅ 직역 회피(명사구 + 판정 분리) |
| 5 | 그래서 개수를 버리고 / 「며칠 버티나」로 바꿨어요 | So I threw out the count and asked / how many days the village can last. | ✅ |
| 6 | 주민이 두 배면 일수는 절반 / 계산을 잊을 일이 없죠 | Twice the villagers, half the days. / No number left for me to forget. | ✅ **조건절을 완료형으로 안 바꿨다** — 같은 꼴 두 번으로 비례를 구문화 |
| 7 | 다음 편, 재해에도 / 겨울이 편해요 | Next: even with disasters, / winter is still easy. | ✅ |

- 🔴 **`en` 0줄 사각지대 아님** — 7줄 전부 있습니다.
- 원문에 없는 수치·주장이 영어에만 섞인 자리 **0건**.
- **가정법→완료형 사고 없음**(6번이 정확히 그 함정이었는데 피했습니다).
- 이름(로마자) 문제 없음 — 등장인물이 없고 `villagers` 는 보통명사.
- `say` 를 안 번역하고 `text` 를 옮겼습니다(`say` 는 20→스무, 10→열로 푸는 TTS 용).
- `youtube.en.title` **89자**(≤100) · `youtube.en` 에 `tags` 키 **없음**(`_`·`title`·`blurb` 뿐).
- 🔴 **`scene.en.json` 없습니다** — 회차 폴더에 `scene.json` 하나뿐입니다(ADR-V-9 준수).

---

## 13. 말투

어미 배열 = ~어요 / ~예요 / ~죠 / ~죠 / ~어요 / ~죠 / ~해요.
**같은 어미 3연속 없음**(최대 2연속, `voice.json` `rules.1` 이 허용하는 범위).
담화표지 = 「근데」(S2[0]) · 「그래서」(S3[0]) 둘 / 7줄 — 대역 안.
첫 줄에 담화표지를 안 넣은 판단(앞 편을 안 본 시청자에게 아무것도 안 가리키는 접속사가 됨)이
맞습니다. 소리 내어 읽어 걸리는 자리 없음.
🟡 참고: 가장 긴 `say` 두 줄이 30자(`한 호흡 25자 안팎`의 위쪽 끝)인데, 실측 말 속도 6.6자/초로
게이트 안이고 자막도 2줄이라 문제 삼지 않습니다.

---

## 14. 길이

| 항목 | 실측 | 규격 | |
|---|---|---|---|
| 총 길이 | **26.7초** | 22.6~28초 · 상한 28 | ✅ 대역 한복판 |
| 본문 | **21.5초** | 20초 이상 | ✅ |
| 예고 | **2.6초** | 3.0초 이하 | ✅ |

원문의 좋은 재료를 안 쓰고 짧게 끝낸 자리 없음(사건 3조각 + 남는 한 줄이 다 들어갔습니다).
**자막을 지우라거나 편을 더 쪼개라는 요구를 하지 않습니다.**

---

## 15. 되돌려 보낼 것 — 한 줄

1. 🔴 **`youtube.blurb` 끝에 형제 편으로 가는 길 한 줄.** (§반려 사유)
   선례 형태: `(같은 글에서 나온 3편 중 1편 — …는 12-2편, …는 12-3편)`
2. 🟡 (같이 고치면 좋음) `notes/writer.md` §5 와 `kinds/quota.js` 주석의
   *"검사 테두리가 격자에 4.5px 못 미친다"* 를 사실에 맞게. 링은 **높이가 자라는 사각형**이라
   아래 획이 y 88→158 을 쓸며 격자 두 줄을 지납니다(그림은 그대로 두세요 — 그 시점 칸은
   0.12 알파 안내선이고 「훑는다」로 읽힙니다).
3. 🟡 (같이) 겹침 논거에 **`setShadow(GLOW,14~15)` 후광**을 넣으세요. 좌표 6px·4px 만으로는
   후광을 못 막습니다(무해했지만 논거가 반쪽입니다).

**2·3 은 반려 사유가 아닙니다.** 1만 고치면 통과입니다.

---
---

# 검수 리포트 ② — `ep12s-1` **4단 구조 전환본**

**판정: 🔴 반려 (막는 지적 1건 · 정정 2건)**
막는 것은 **아웃트로 미리보기(SO `nextpeek`)의 흰 글자 두 줄이 서로 붙어 뭉개지는 것** 하나입니다.
`WINTER LEFTOVER`(baseline y66) 와 `FAIL 30`(baseline y74) 이 **x 를 절반 넘게 공유한 채
세로 간격 0px** 로 그려집니다 — 다음 편을 파는 유일한 화면에서 실패선 숫자가 안 읽힙니다.
그 외 **기계 27종 전부 통과(경고 1)**, 사실 대조·cue 산수·카드 산수·sfx·영어 자막은 전부 통과입니다.

검수일 2026-08-08 · 리포 `C:\Users\anjyo\AI_GOAP` · 검수 2회차(1차는 3단 대본, 이번은 4단 전환본)

---

## A0. 기계 점검 — 실측을 새로 뽑았다

`node tools/scene-video/tts.mjs ep12s-1` → `새로 만듦 0 · 재사용 9` (기존 wav 9개가 지금 대본과
해시가 맞는다는 뜻이라 `timed.json` 은 **이 대본의 진짜 실측**입니다).
🔴 **합성 `timed.json` 을 만들지 않았고, 지운 것도 없습니다.** 검수 중 만든 것은
`build/stills/*.png`(증거용)뿐이며 임시 raw 파일은 지웠습니다.

```
가이드 점검 · ep12s-1
────────────────────────────────────────────────────
  OK shot 메타 완비             7샷 전부 있음
  OK 형제 라벨 = 그 편의 youtube.title 3개 전부 일치
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               9줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 인트로 있음                 S0 (kind=intro) — 「클로드 코드로만 만드는, 에이아이 마을 개발일지입니…」
  OK 훅 시작 문구                「오늘 개발 일지 내용은, 주민 네 명인데 겨울 목표는 …」
  OK 아웃트로 소개 있음             「클로드 코드 개발일지, 또 만나요.…」
  OK 영어 자막 전 줄 존재           9줄 전부 있음
  OK 영어 자막 2줄 상한            9줄 전부 2줄 이하
  OK 제목 이행 (첫 두 줄)          겹침: 겨울, 주민, 4명인데, 목표, 20개
  OK 제목 후킹 (정보)             숫자 4, 10, 20 — 기본 축은 감정·사건+질문형(0-A), 뜻 판정은 검수팀
  OK 다음 편 예고 있음             SO 자막 1줄 (음성 예고)
  OK 예고 5초 이하               3.5초
  OK 자막 2초 미만 없음            전부 2초 이상
  OK 말 속도 6.0~7.2자/초        6.69자/초 (참고 영상 6.93)
  OK 총 길이 38초 이하            36.1초 = 자막 33.6 + 꼬리 2.45 (목표 30~35초 · 이 값이 실제 mp4 길이다)
  ⚠  총 길이 30~35초 권장대역       36.1초
  OK 본문 18~22초              본문 20.1초 (인트로 4.6 · 훅 5.1 · 아웃트로 6.3 제외)
  OK 인트로 5.5초 이하            4.6초
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 하단 안전영역 (바닥 443px 비움)  자막 바닥 1440 · 비주얼 바닥 1266 (한계 1477)
  OK 결정성 (3패스)              180프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  OK 정적 구간 3초 이하            최대 0.2s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
통과 — 경고 1건은 확인 권장
```

**경고 1건(36.1초)은 사유 확인 완료 — 반려하지 않습니다.** 상한 38 안이고 사용자 확정 35±3 안입니다.
🔴 **길이를 맞추려고 인트로·훅·예고·소개를 깎으라고 요구하지 않습니다**(ADR-V25-9).
본문은 20.1초로 18~22 대역 한복판이라 깎을 곳도 없습니다.

---

## A1. 🔴 막는 지적 — SO `nextpeek` 의 라벨 두 줄이 붙어 뭉개진다

**증거(스틸 실물, `build/stills/31s.png` 과 그 확대본 `build/stills/crop_peek.png`):**
`WINTER LEFTOVER` 의 글리프 아래와 `FAIL 30` 의 글리프 위가 맞닿아 한 덩어리로 읽힙니다.

**픽셀 실측**(`31s.png` 을 gray 로 뽑아 행별 밝은 픽셀 x 범위를 센 결과):

| 스틸 y | x 범위 | 무엇 |
|---|---|---|
| 576–600 | 383–696 | `WINTER LEFTOVER` |
| 601–623 | 557–692 | `FAIL 30` |

- **두 덩어리 사이에 빈 행이 0줄**입니다(600 → 601 연속).
- x 는 **557–692 구간을 공유**합니다 — `FAIL 30` 의 거의 전체 폭이 `LEFTOVER` 아래에 겹칩니다.
- 캔버스 좌표 환산(실패선 삼각형 `TIP_T=80`→스틸 639, `TIP_B=92`→스틸 671 로 배율 2.667·오프셋 425.7):
  `WINTER LEFTOVER` = 캔버스 y **56.4–65.4**, `FAIL 30` = 캔버스 y **65.7–74.0** → **간격 0.3px**.

**원인**은 `kinds/nextpeek.js` 의 두 상수입니다.

```
const GAUGE_Y = 66, FAIL_LBL_Y = 74;   // baseline 간격 8px, 글리프 높이 9.6/8.3px
```

`gaugeLabel` 은 `cx`(≈186) 중앙정렬, `FAIL 30` 은 `fx = IX + IW*failAt`(≈219) 중앙정렬이라
**같은 레인에서 x 가 겹칩니다.**

**노출 구간**: `ak` 는 `ease(cue(0, 0.15, 0.30))` 이라 SO 첫 줄 시작 +1.06초에 완성되고,
아웃트로 카드는 SO 시작 +3.349초에 뜹니다 → **이 뭉개진 그림이 화면에 있는 시간 전부(약 2.3초)** 가
그렇습니다. 미리보기가 보이는 시간이 그게 전부입니다.

**부수 지적 — 작성팀의 겹침 감사가 이 쌍을 안 봤습니다.** `kinds/nextpeek.js` 주석과
`notes.그림 설계 — 흰색과 강조색을 안 겹쳤다` ⑥ 은 *"가장 가까운 쌍이 … 12px"* 라고 적었는데,
그건 **강조색↔흰색** 쌍만 잰 값입니다. **흰색↔흰색**이 0px 인 것은 어디에도 안 적혀 있습니다.
다음부터 겹침 감사에 **같은 색끼리의 세로 간격**도 넣으세요.

**고치는 법(제안, 그림 문법은 그대로)** — 둘 중 하나면 됩니다.
1. `GAUGE_Y` 를 66 → **54** 로 올린다(구분선 y36 과 글리프 위 44 사이 8px, `FAIL 30` 과 11.7px).
2. 계기 이름을 왼쪽 정렬로 빼서 x 공유를 없앤다(`ctx.textAlign='left'`, x=14).
🔴 고친 뒤 **`31s.png` 을 다시 뽑아 눈으로 확인**해 주세요. 게이트는 이걸 못 봅니다.

---

## A2. 4단 고정 구조 — 뜻 검수 (ADR-V25-8~12)

| 단 | 검수 | 판정 |
|---|---|---|
| ① 인트로 | `shots[0].kind=intro` · say `클로드 코드로만 만드는, 에이아이 마을 개발일지입니다.` · text `Claude Code로만 만드는, 스스로\n생각하고 행동하는 AI 마을 개발일지` — **명세서 §2-C 고정 문안과 글자까지 일치**(자의 개작 0) | ✅ |
| 인트로 그림 | `kinds/intro.js` = `export { default } from '../../../engine/intro-brand.js';` **한 줄 재수출**(ADR-V25-11) · 직접 그린 코드 0줄 | ✅ |
| ② 훅 | 「오늘 개발 일지 내용은, 주민 4명인데 겨울 목표는 20개예요.」 — 이 편의 사건(20 대 4의 어긋남)을 정면으로 약속 | ✅ |
| ③ 본문 | 20.1초 · ①어긋남(S1·S2) ②뒤집힘(S3·S4) ③남는 한 줄(S4 「계산을 잊을 일이 없죠」) | ✅ |
| ④ 아웃트로 | 예고 3.5초 + 소개 마무리 + 0.5초 미리보기 루프 | ⚠️ 그림은 A1 |
| `legacyFormat` | **필드 없음** — 신규 회차이므로 올바름 | ✅ |
| 소개 마무리 줄 | 「Claude Code 개발일지, 또 만나요.」 — **「다음 편」 낱말 없음** → 예고 길이 부풀림 없음(예고 3.5초는 SO[0] 만) | ✅ |

**훅 5.1초(34자)에 대한 판정 — 통과입니다.** 명세서의 「약 3초」를 넘지만 그 5.1초 중
**1.44초는 고정 문구**(「오늘 개발 일지 내용은,」 11자 / 자당 131ms)이고 약속에 쓴 것은 3.4초입니다.
그 3.4초가 제목 이행 겹침 5개 중 4개(주민·4명인데·겨울·목표·20개)를 지고 있어 줄이면
게이트가 흔들립니다. 🔴 **길이 경고를 이유로 훅을 깎으라고 요구하지 않습니다.**

---

## A3. 이월 반영 확인 2건 — 둘 다 반영됨

**① 첫 본문 줄 교체 · 재해 삭제** — `S1[0]` = 「이 마을은 겨울이 안 힘들었어요.」
옛 이음말(「재해를 내리기 전에…」)은 `excluded[0]` 에 사유와 함께 있습니다.
**「재해」가 이 편의 화면·자막에 남아 있는지 전수 확인 → 예고 한 줄뿐입니다.**

- 자막 9줄: `재해` 는 SO[0] 「다음 편은, 재해를 넣었는데도…」 **1건**뿐.
- `spec` 전 필드: `DISASTERS ON`(SO, 다음 편 배경) 외 0건. `quota.js` 의 옛 `DISASTER` 쐐기는
  코드에서 사라졌고 스틸 `12s.png`·`15.5s.png` 에도 없습니다(1년 축 + 평평한 EVENTS 선으로 교체).
- 첫 줄 기준 3종(2026-08-06 신설)으로 다시 봄: **수치 스펙 아님**(숫자 0개) ·
  **우리 진행 상황 아님** · **앞 편을 안 봐도 혼자 섭니다.** ✅

**② nextpeek 수치가 spec 경유인지** — `kinds/nextpeek.js` 에 숫자 상수가 **0개**입니다.
`spec.failNumber "30"` · `spec.resultNumber "40"` · `spec.failAt 0.6` · `spec.resultAt 0.8`
전부 `spec` 에서 받고, 눈금 비율 0.60:0.80 = 30:40 이며 **축 최댓값은 화면에 안 적습니다.**
(ep11s 에서 `HEADS`/`KEEP` 를 파일 상수로 박았던 지적이 반영됨.) ✅

---

## A4. 사실 대조 — 원문 직접 대조 (주어·방향까지)

원본 HTML `tools/blog-automation/published/2026-07-26-unity-goap-m9-zone-disaster-flood.html` 을
직접 열어 태그를 벗겨 대조했습니다.
🔴 **지워진 표와의 대조**: 원본에 `<table>` **0개** · `<pre>` 0개 · `<svg>` 0개 · 주석 0개(실측).
**표가 지워진 자리가 없으므로 표 안에만 있던 값이 새어 나올 경로 자체가 없습니다.**

| 화면·자막 | 원문 문장 | 주어/방향 |
|---|---|---|
| 20 (조리식 20개) | "겨울 대비 목표가 \"조리식 20개\"로 못 박혀 있었는데" | ✅ |
| 10 | "예전에 주민이 10명이던 시절에 잡은 숫자였습니다" | ✅ |
| 4 | "그 뒤 주민을 4명으로 줄였는데도 20이라는 숫자는 그대로 남았습니다" | ✅ |
| 「10명 몫을 4명이 먹으니 겨울이 편하죠」 | "10명이 먹을 양을 4명이 나눠 먹으니 겨울이 안락할 수밖에요" | ✅ 먹는 쪽·나누는 방향 동일 |
| 「겨울이 안락했다」(S2 판정문) | 절 제목 "문제: 겨울이 안락했다" | ✅ 원문 그대로 |
| EVENTS 평평 / 「겨울이 안 힘들었어요」 | "마을에 아무 사건도 없이 1년이 지나가는 겁니다" | ✅ |
| FoodDaysLeft / COOKED·RAW ÷ VILLAGERS·SEASON | "창고에 있는 조리식과 생식을 각각 포만감으로 환산해서 다 더하고, 그걸 지금 살아 있는 주민 수와 계절 소모 속도로 나눕니다" | ✅ 분자·분모 방향 동일 |
| 「식량 N일치」 | "HUD의 달력 옆에 \"식량 N일치\"를 띄웠습니다" | ✅ N 그대로 |
| x2 / 막대 절반 | "주민이 두 배가 되면 일수는 절반이 됩니다" | ✅ |
| MANUAL → AUTO | "밸런스를 손으로 다시 계산할 일이 없습니다" | ✅ |
| FAIL 30 / 40 / DISASTERS ON (SO) | "30을 넘으면 \"안락 실패\"" / "결과는 40이었습니다" / "재해까지 넣었는데도" | ✅ 다음 편 재료로만 |

**원문에 없는 수치 0건.** 「3일치」·농경지 반경·포만감 값·소모 배율·커밋 수는 화면에 없습니다(확인).
S2 의 1인 몫 막대가 2칸→5칸인 것은 20÷10, 20÷4 의 **길이**이고 숫자를 적지 않습니다
(`headcount.js` 에 그 숫자 출력 없음 — `headFrom "10"`·`headTo "4"` 만 텍스트로 나옵니다). ✅

**게임 에셋 실재 확인(ADR-V-10)**: `FoodDaysLeft` → `Assets/Scripts/M0/Data/SlotId.cs` 등 실재.
「식량 N일치」 → `Assets/Scripts/M0/UI/SeasonHud.cs:336` 의 `$" · 식량 {foodDays}일치"` 실재. ✅

---

## A5. cue 재배선 셋 — 접힘·위상 손 검산

산식: 창 `W = lead + rel.dur × span`, `rel.dur = wav dur + pauseAfter`(engine.js:163·169),
진행도 p 인 시각 = `p×W − lead`(자막 시작 기준). 어절 위치는 **말하는 시간(wav dur)** 으로 환산.

**① S1 `quota` — 위쪽 블록 교체, c1 계열 불변**
- `findCue 0`(rel 2.494s, W=1.8958): axisOn 완성 +0.61초 · flatK 완성 +1.56초 → 발화 2.344초 안. ✅
- `fixCue 1`(rel 4.565s, W=3.5738): fill 완성 +1.82초 · nail 0.62→0.86 = **+2.066 → +2.923초**
  → 「스무 개」(자당 215.8ms · 「무」=1.942~2.158) 위. ✅ **c1 창 인자·문턱 불변 확인**
  (`cue(fixCue, 0.15, 0.75)` · `(c1-0.62)/0.24`) → `delayMs 2070` 이 그대로 옳음(계산값 2,066).

**② S2 `headcount` — cue 둘 → `beatCue` 하나로 접음(고장 예방 확인)**
`shareCue: 1` 을 남겼다면 `s.rel[Math.min(i, len-1)]` 로 **조용히 cue(0) 으로 접혀** 1인 몫 막대가
첫 프레임에 다 자라 있었을 자리입니다. 실제 spec 에 `beatCue: 0` 하나만 있는 것 확인. ✅
`beatCue 0`(rel 3.439s, W=2.83242) · 자당 137.9ms(24자):

| 단계 | 진행도 | 자막 시작 기준 | 걸치는 말 |
|---|---|---|---|
| cut(표식 6개 꺼짐) | 0 → 0.42 | −0.15 → +1.040초 | 「열 명 몫을 네」 ✅ |
| clampK(죔쇠) | 0.30 → 0.60 | +0.700 → +1.550 | 「몫을 네 명이」 ✅ |
| share(2칸→5칸) | 0.46 → 0.78 | +1.153 → +2.059 | 「명이 먹으니」 ✅ |
| verdict(「겨울이 안락했다」) | 0.80 → 0.98 | +2.116 → +2.626 | 「겨울이 편하죠」 ✅ |

전부 발화 구간(0~3.309초) 안이고 순서가 값→장식으로 지켜졌습니다. 스틸 `20.3s.png` 로 확인.

**③ S4 `autoscale` — 계절 칸 삭제, c0 계열 불변**
`halveCue 0`(rel 4.234s, W=2.4787) · 자당 134.5ms(30자):
grow 완성 +0.842초(「두 배면」) · **halve 0.72→0.86 = +1.635 → +1.992초 = 「절반」(13·14번째 글자)** ·
badge +2.081 → +2.329초(「계산을」). ✅ `winterCue: 1` 잔재 없음(spec·코드 모두 확인).
→ `delayMs 1630` 이 그대로 옳음(계산값 1,635).

**④ 안 건드린 S3 `daysleft`** — `buildCue 0`(rel 3.590s, W=2.663): 결과 카드 res 0.58→0.92 =
+1.394 → +2.300초 → 「며칠 버티나」 위. ✅
**⑤ SH `mismatch`**(rel 5.056s, W=4.9532 · 자당 131.2ms): appear 「오늘 개발」 · headOn +1.336~2.723
「주민 네 명인데」 · goalOn +2.822~4.011 「겨울 목표는」 · **breakK +4.110~4.803 「(스)무 개예요」**
→ 발화 끝 4.856초 안. ✅ (스틸 `8.5s.png` 이음, `9.8s.png` 끊김)

**t 로만 도는 것**(자막과 무관): quota 의 1년 축 표식 `frac(t/3.4)` · 각 샷 점선 흐름 · 표식 바운스 ·
nextpeek 의 0.5초 루프 `frac(t/0.5)`. **전부 장식이고 페이오프를 지고 있는 것이 없습니다.** ✅

---

## A6. 아웃트로 카드 산수 (TOTAL − 3000)

`timed.json` + `SHOT_TAIL 0.35s` 로 타임라인을 다시 세운 값:

```
TOTAL 36,076ms   카드 시작 33,076ms (= TOTAL − OUTRO_MS 3000, engine.js:183·262)
SO   start 29,727  dur 6,349   rel[0] t=0 dur=3.532   rel[1] t=3.532 dur=2.467
카드 시각 = SO 기준 +3.349초
```

- **미리보기는 카드 전에 완성됩니다**: `ak = ease(cue(0, 0.15, 0.30))` → W=1.2096 → **+1.060초에 완성**,
  카드까지 **2.29초 노출**. 0.5초 루프가 4.5바퀴 돕니다. ✅ (ep08s-3 형 사고 없음)
- 결과 숫자 `40` 은 루프의 ph 0.42~0.88 구간에 뜨므로 노출 구간에서 **4회 이상** 보입니다. ✅
- 소개 마무리 줄(SO[1], 시작 +3.532초)은 **카드가 뜬 뒤에 시작**하지만 이는 4단 구조의 설계대로이고
  (`index.html` 의 `.cap` 은 `.outrocard` 와 형제라 자막은 안 가려집니다), 그 줄이 지는 페이오프는
  없습니다(`nextpeek` 은 cue 1 에 아무것도 안 걸었습니다). ✅
- 본문의 페이오프(S4 halve 완성 = 절대 27.14초)는 카드보다 **5.9초 앞**입니다. ✅
- 🔴 **sfx `delayMs` 두 값의 근거 `rel` 이 불변인지 확인**: S1[1] rel = 4,315+250 = **4,565ms**,
  S4[0] rel = 4,034+200 = **4,234ms** — 작성팀이 `why` 에 적은 값과 실측이 같습니다. ✅

---

## A7. 효과음

| 샷 | kind | 파라미터 | 실측 피크 | 판정 |
|---|---|---|---|---|
| S1 | thud | gain .064 · dur .19 · freq 264 · delay 2070 | **0.189** | ✅ 천장 0.46 여유 |
| S4 | drop | gain .070 · dur .36 · 430→150 · delay 1630 | **0.132** | ✅ |

- `synth()` 를 **실제 인자로 직접 호출**했고 `⚠️ sfx …: 피크 천장에 걸려` 경고가 **안 났습니다**
  (kind 별 상한표를 믿지 않고 실행으로 확인).
- **밀도**: 총 36.1초에 2개(본문 20.1초에 2개) → 기준(4단 구조 2~3개) 안. 인트로·훅·아웃트로 무음. ✅
- **돌려막기 대조**: `episodes/**/scene.json`(`_archive` 포함) sfx 를 전수로 뽑아 대조했습니다.
  thud (dur .19, freq 264) 조합 **전례 0건**(freq 264 는 기존 최고 230보다 높음),
  drop (dur .36, 430→150) 조합 **전례 0건**. ⚠️ `dur 0.19` 자체는 `ep04s-2` 에 있으나 그건 **latch** 라
  파형이 다릅니다 — 작성팀 주장은 thud 목록 기준으로 성립합니다.
  ⚠️ **형제 `ep12s-3` 이 thud+drop 으로 kind 쌍이 같습니다.** 작성팀이 스스로 남긴 그대로이고,
  파라미터(198 vs 264 / 700→130 vs 430→150)와 사건(헛닿음 vs 못 박힘, 파괴 vs 자동 비례)이 갈립니다.
  **뜻이 맞는 kind 를 피하려고 틀린 소리를 고르는 것은 더 나쁘므로 이대로 둡니다.**
- `delayMs` 는 `render.mjs:113` 대로 **그 자막 시작 기준**이고 `at` 은 샷 안 인덱스(render.mjs:109)입니다 —
  둘 다 맞게 쓰였습니다. `why` 가 ease 입출력을 섞지 않았습니다(문턱을 c 값으로만 씁니다). ✅

---

## A8. 예고·미리보기·형제 링크

- **음성**: 「다음 편은, 재해를 넣었는데도 겨울이 편했어요.」 → **「다음 편은~」 이음 문형** ✅ · 3.5초 ✅
  **그 한 줄만 읽고 다음 편이 무슨 이야기인지 압니다**(재해를 넣었는데도 겨울이 편했다). ✅
- **카드** `outro.next` = 「12-2편 · 겨울에 식량이 40이나 남았는데 왜 실패일까요? 기준을 30으로 미리
  정해놨거든요.」 = `episodes/ep12s-2/scene.json` 의 `youtube.title` 과 **글자까지 일치**. ✅
- **미리보기**: FAIL 30 을 지나 40 에서 멈추는 잔량 막대 + DISASTERS ON.
  → 셋이 **같은 편(12-2)의 같은 사건**(기준 30 · 결과 40 · 재해를 켠 채)을 가리킵니다.
  ep08s-2 형(둘이 다른 편을 부름) 사고 없음. ✅
- **정본 대조**: `state/schedule.json` 의 `order` 에서 `ep12s-1` 다음이 **`ep12s-2`** ✅.
  재료는 원문 "30을 넘으면 \"안락 실패\"" / "결과는 40이었습니다" / "재해까지 넣었는데도" 에 실재 ✅.
  🔴 원문 끝의 「다음 이야기」(M10 늑대) 문단을 근거로 안 쓴 것 확인 — ep11s 사고 재발 없음. ✅
- **앞 편과의 이음**: `ep11s` 의 예고 「다음 편은, 주민 4명인데 겨울 목표가 20개였어요.」와
  `outro.next`(이 편의 새 제목)가 이 편의 훅과 정확히 맞물립니다. ✅
- **형제 라벨** 3개 = 각 편 `youtube.title` 과 일치(기계 게이트 + 눈으로 재대조). ✅
- **blurb 상호 참조**: 1차 반려 사유였던 항목이 고쳐졌습니다 —
  「(같은 글에서 나온 3편 중 1편 — …는 12-2편, …는 12-3편)」 ✅
  ⚠️ **이 편 밖의 관측**: `ep12s-3` 의 blurb 은 여전히 「같은 글에서 나온 3편 중 3편입니다.」로 끝나
  **12-1·12-2 로 가는 길이 없습니다.** 이 편의 반려 사유는 아니지만 코디네이터가 트리오로 처리하세요.

---

## A9. 영어 자막 (`Docs/영상_영어권_확장_실행명세서.md`)

9줄 전부 `en` 존재 ✅ · 전부 2줄 이하 ✅ · `scene.en.json` **없음**(ADR-V-9) ✅ ·
`youtube.en.title` **99자**(≤100) ✅ · `youtube.en` 에 태그 칸 없음 ✅.

한 줄씩 `text` ↔ `en` 대조 결과 **덧붙인 수치·주장 0건**. 확인한 항목:
- 가정법 → 완료형 오역 **없음**: 「주민이 두 배면 일수는 절반」 = `Twice the villagers, half the days.`
  (조건을 일어난 사건으로 못 박지 않았습니다.)
- 이름의 로마자 표기 문제 없음(고유명 0개, `villagers` 는 보통명사) ✅
- `say` 를 번역하지 않음(정본 `text` 기준) ✅
- 직역투 아님 — 「10명 몫을 4명이 먹으니」를 `Because four people eat…` 로 끌지 않고
  `Four people eating ten people's food. / Of course winter felt easy.` 로 끊었습니다. 통과.

---

## A10. 제목 (ADR-V25-12 · 도치형)

「**겨울이 왜 이렇게 편했을까요? 주민은 4명인데 겨울 목표가 10명 시절 20개 그대로였거든요.**」

- **도치형** — 질문이 0번째 글자에서 열림 ✅
- **드라마 서술** — 기능 설명(「~보이게 했어요」)이 아니라 마을에 일어난 일 ✅
- **열린 질문** — 답(며칠 버티나로 바꿨다)을 제목이 미리 주지 않음 ✅
- **자극이 사실을 안 넘음** — 4·10·20 셋 다 원문 문장 그대로(§A4) ✅
- **첫 줄이 수치 설명이 되지 않았는가** — 4단 구조의 첫 줄 = 훅입니다. 훅이 숫자를 지되
  그다음 첫 본문 줄에는 숫자가 **0개**입니다. 반려 사유 아님 ✅

---

## A11. 말투 · 그 밖

- 어미: ~니다1 / ~예요2 / ~어요3 / ~죠2 / ~요1 — **같은 어미 3연속 없음** ✅
- 담화표지 「그래서」 1회(9줄) — `voice.json` 3번 규칙(서너 문장에 하나) 대역 ✅
- 큰 쉼 0회 ✅ · 자막 = 음성의 정제본(스틸 9장으로 대조, **옮겨적기 오류 0건**) ✅
- `excluded` 7건 사유 납득 — 특히 ①옛 이음말 ②옛 S2 첫 줄(훅과 중복) ③계절 칸(자막이 안 부르는 장식)은
  **4단 전환의 필연**이고, 구역(ZoneService)·플래너 함정·재해 절은 12-2/12-3 의 몫으로 정확히 갈렸습니다.
  🔴 **원문이 이름 붙인 개념 중 이 편 소관인 것(FoodDaysLeft · 「식량 N일치」 · 「조리식 20개」 ·
  「겨울이 안락했다」)은 하나도 안 버려졌습니다.** ✅
- `build/` 에 옛 판본 mp4·srt·upload 잔재 **없음**(audio/ · full.wav · timed.json 뿐, 전부 오늘 22:36). ✅

---

## A12. 정정 요구 2건 (반려 사유 아님 — A1 과 함께 고치면 좋음)

1. 🟡 **`notes.길이` 가 자체 산정식을 적고 있습니다.** *"그렇게 더하면 총 36.6초 · 본문 20.3초 ·
   인트로 4.6초 · 예고 3.8초"* — 작성 시점엔 실측이 없어 겨냥이라고 밝혀 뒀지만 **지금은 실측이 있습니다.**
   산정식은 `check.mjs` 안에만 둡니다. 그 문단을 **check.mjs 출력 줄로 갈아 끼우세요**:
   `총 길이 36.1초 = 자막 33.6 + 꼬리 2.45 · 본문 20.1 · 인트로 4.6 · 예고 3.5`.
   (겨냥 자체는 나쁘지 않았습니다 — 36.6 vs 36.1.)
2. 🟡 **`notes.다음 편 예고…` 의 카드 인용이 낡았습니다.** 거기엔 `outro.next` 가
   「12-2편 · 재해를 넣었는데 겨울이 편했어요」로 적혀 있는데 실제 필드는 제목 확정 뒤 기계 파생된
   「12-2편 · 겨울에 식량이 40이나 남았는데 왜 실패일까요? …」입니다. **JSON 이 맞고 notes 가 틀렸습니다.**

## A13. 의견 (등급 분리 — 반려 사유 아님, 미검증 항목)

🟡 **첫 줄 기준 4번(주민이 무언가를 하고 있는가·어긋나 있는가)** 은 15편 관측 뒤에 만든 **미검증** 분류라
의견으로만 올립니다. 이 편의 첫 줄(훅)은 주어가 「주민 수/목표」라는 **숫자**이고, 첫 본문 줄의 주어는
「이 마을」입니다 — 상위 3편(거절·몇 분째 반복·거절)처럼 **주민이 거스르는 그림이 아닙니다.**
바꾸라는 요구가 아닙니다(제목 이행 겹침이 훅에 몰려 있어 손대면 게이트가 흔들립니다).
다음 회차 기획 때 참고 재료로만 남깁니다.

---

## A14. 되돌려 보낼 것

1. 🔴 **`kinds/nextpeek.js` — `WINTER LEFTOVER` 와 `FAIL 30` 을 떼어 놓으세요.** (§A1, 픽셀 실측 첨부)
   고친 뒤 **스틸 재확인 필수**(게이트가 못 보는 자리입니다).
2. 🟡 `notes.길이` 를 `check.mjs` 출력 줄로 교체. (§A12-1)
3. 🟡 `notes` 의 옛 `outro.next` 인용 정정. (§A12-2)

**2·3 은 반려 사유가 아닙니다. 1만 고치면 통과입니다** — 대본·타이밍·사실·소리·영어는 전부 통과입니다.
