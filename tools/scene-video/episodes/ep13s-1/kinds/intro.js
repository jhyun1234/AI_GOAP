/* 시리즈 공용 인트로 — ADR-V25-11 이 명시한 「그림은 회차가 소유한다」의 유일한 예외다.
   브랜딩은 매회 같아야 브랜딩이므로 회차가 직접 그리지 않고 한 줄로 재수출한다.
   (check.mjs 의 `kind 파일 존재` 가 회차 폴더를 보므로 파일 자체는 여기 있어야 한다) */
export { default } from '../../../engine/intro-brand.js';
