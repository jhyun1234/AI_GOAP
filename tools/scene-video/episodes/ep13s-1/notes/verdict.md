# ep13s-1 마스터 판정 = 🟢 **APPROVED**

2026-08-10 · scene-master · **누적 반려 1회에서 멈춤** (검수 1차 1회 · 마스터 반려 0 · 정지선 3회 미도달)

원본 = `tools/blog-automation/published/2026-07-28-unity-goap-m10-wolf-injury-wanderer.html`
검수 = `notes/review.md` (781줄 · 1차 반려 3건 + 2차 통과)

**나는 검수 보고를 근거로 쓰지 않았다.** 아래 수치는 전부 내가 이 세션에서 직접 돌려 얻은 것이고,
검수 값과 어긋난 자리는 §5 에 따로 적었다.

---

## 승인 조건 4/4

| 조건 | 결과 |
|---|---|
| `check.mjs` 실패 0 | ✅ fps 5 · fps 30 **둘 다 내가 직접 돌림**. 27 OK · fail 0 · warn 1(총 길이 35.4초) |
| 수치 표본이 전부 원문에 있음 | ✅ **표본 3개가 아니라 18개를 전수 대조** — 18/18 적중 |
| 모든 샷에 `reads`·`source` 가 있고 빈말이 아님 | ✅ 6샷 전부. 최단 `reads` 107자 · 최단 `source` 206자, 전부 원문 인용 또는 명세 조항을 짚는다 |
| 첫 10초가 콜드 오픈이고 수수께끼가 던져짐 | ✅ 인트로 0~4.60s → **훅 4.60~9.11s** 가 10초 안에 완결. 그림이 말보다 먼저 무덤을 세운다(스틸 확인) |

---

## 1. 급소 ③ — 30fps 결정성. **내가 돌렸고, 스틸도 내가 다시 떴다**

### (a) 게이트 — 직접 실행

```
$ node tools/scene-video/check.mjs ep13s-1
  OK 결정성 (3패스)   177프레임 불일치 0
  OK 정적 구간 3초 이하   최대 0.2s
  ⚠  총 길이 30~35초 권장대역   35.4초
  통과 — 경고 1건

$ node tools/scene-video/check.mjs ep13s-1 --fps 30
  OK 결정성 (3패스)   1062프레임 불일치 0     ← 1차 44건
  OK 정적 구간 3초 이하   최대 1.7s
  통과 — 경고 1건
```

**이로써 1062프레임 불일치 0 은 세 번(검수 · 코디네이터 · 나) 독립 재현됐다.**

### (b) 🔴 「모양 동일 · 그림 불변」 — **검수 표를 그대로 믿지 않고, 더 좁은 실험으로 다시 했다**

마스터 교훈(ep12s-3)대로 kind 를 고쳤으면 스틸을 다시 뜬 뒤에 말한다.
🔑 **검수의 실험 설계에는 결함이 있었다** — 1차 스틸(커밋 `8d3701a5`) ↔ 2차 스틸을 비교하면
`dashRule` 변경과 **의도한 편집(계절 라벨 · 인트로 한국어화)이 한 프레임에 섞인다.**
그래서 나는 **`scene.json` 을 지금 것으로 고정한 채 `kinds/` 만 `8d3701a5` 로 되돌려** 스틸을 뜨고,
같은 시각의 현재 스틸과 픽셀을 맞댔다. 이러면 남는 차이는 **오직 `dashRule` 재작성의 효과**다.

```
스틸 = render.mjs --still 7.6,15.123,18,24,30.2  (추출과 같은 경로)
비교 = ffmpeg rgb24 원시 디코드 후 채널 바이트 단위 절대차
```

| 시각 | 샷 | kind 변경? | 다른 채널바이트 | **Δ>8** | 최대 Δ | 어긋난 행 |
|---|---|---|---|---|---|---|
| 7.6s | SH `firstgrave` | **변경됨** | **0** | 0 | **0** | — |
| 15.123s | S1 `quietwinter` | 변경됨 | 7,983 | **0** | 4 | 635~644 · 1104~1113 |
| 18s | S2 `emptyland` | 변경됨 | 5,487 | **0** | 4 | 558~559 · 566~567 · 1143~1152 |
| 24s | S3 `chase` | 변경됨 | 11,817 | **0** | 2 | 646~647 · 654~655 · 762~771 · 1177~1178 · 1185~1186 |
| 30.2s | SO `nextpeek` | **안 변경됨** | 0 | 0 | 0 | — |

- **`firstgrave` 는 파일이 바뀌었는데 픽셀이 비트 단위로 같다** — `fillRect` 판이 `setLineDash` 판과
  완전히 일치한다는 주장의 **유효한 증거는 이 한 장뿐**이다.
- 나머지 셋은 점선 행에서만 어긋나고 **Δ 최대 4/255(1.6%) · Δ>8 이 한 픽셀도 없다** = 안티에일리어싱.
  행 번호가 검수가 적은 것(558 · 646 · 763 · 1144 · 1177)과 **일치**한다.
- 🔴 **의도하지 않은 그림 변화 0건** — 내 설계에서는 S1 도 Δ>8 = 0 이다(검수 표의 S1 Δ>8 1,752 는
  라벨 편집이 섞인 값이고, 라벨을 고정하면 사라진다). **「그림 불변」은 참이다.**
- 렌더된 화면을 눈으로도 봤다: S1 끝 라벨 **「겨울 끝」**(「봄」 아님) · SH 무덤 · S3 「12 → 10 → 8」.

---

## 2. 급소 ① — 인용문 위조 재발 여부. **표본이 아니라 전수로 다시 돌렸다**

이 파이프라인에서 가장 무거운 위반 계열이므로(ep02s 두 번 · 이 편 1차 ①) 기계로 다시 훑었다.
범위 = `scene.json` 전체(`shots`·`excluded`·`notes`·`youtube`)의 작은따옴표·큰따옴표 쌍 **100쌍**,
대조 대상 = 원본 HTML 평문 + 기획 브리프, 공백·인용부호·가운뎃점·말줄임표 정규화 후 포함 검사.

```
원문 HTML 적중 86 · 브리프에만 1 · 둘 다 미적중 13
```

미적중 13건을 손으로 하나씩 열었다:

- **통째로 없는 문장 = 0건.** 🟢 **1차 ①(「봄」 근거로 지어낸 문장)은 재발하지 않았고 잔존도 없다.**
  「봄이 옵니다」·「배고프면 가서 밥을 먹고」는 `scene.json` 어디에도 없다.
- 오탐 6건 = 파싱 아티팩트(`"seed"`·`tone('track')`·영어 소유격이 만든 가짜 쌍)와
  지시서·사용자 판정 인용(`딱 · 표식 도착` / `철컥 · 잠긴다 · 확정` / `다음 영상에서 구현할…` /
  `쳐지고 이어지지 않는다`) — 원문이 출처가 아닌 자리다.
- 오탐 2건 = `/` 로 목록을 이어 붙인 것(S2 `source` 감지 반경 5줄)과 말줄임 elision.
  조각별로 다시 재면 전부 원문에 있다.
- 🟡 **실재 결함 5건 — 전부 「뜻은 같은데 글자가 원문과 다른」 인용이다.** §5-2 에 목록.

**판정: 화면·자막에 원문에 없는 문자열은 0개다.** 화면 문자열 23개는 kind 에 하드코딩이 하나도 없고
전부 `spec` 에서 온다(`fillText` 호출 전수 확인 — 리터럴 문자열 0개), 그 23개를 원문에 대조해 전부 적중.

---

## 3. 급소 ② — `latch seed 211`. **파형을 내가 다시 합성해 쟀다**

척도 = **절대 표본 정렬** 피어슨 상관(정규화 리샘플 금지). 대조군 = `shot.sfx` 를 파싱해 모은
리포 전수 latch(**`_archive/eptest-m11` 포함 23건**, 이 편 제외 22건).

| 상대 | 전체 상관 | 클릭 전이(앞 662표본) | 검수값 |
|---|---|---|---|
| `ep08s-3` seed 97 ← 최근접 | **0.7983** | 0.7980 | 0.7983 / 0.7980 ✅ 일치 |
| `lf02 S5` seed 157 (1차 충돌 상대) | 0.7698 | 0.7589 | 0.7698 / 0.7589 ✅ 일치 |
| `ep13s-2` seed 191 (형제) | 0.7585 | 0.7477 | 0.7585 / 0.7477 ✅ 일치 |
| 22건 전체 대역 | **0.6285 ~ 0.7983** | 0.618~0.798 | 0.63~0.80 ✅ 일치 |
| **[대조군] 1차 157 ↔ lf02 157** | **0.9963** | **0.9982** | ✅ 일치 |

**검수의 기준선 갱신(0.71~0.78 → 0.63~0.80)은 옳다.** 옛 대역은 10쌍 표본이었고,
전수로 재면 아래로 `ep05s-3`(seed 37 · dur 0.115)이 **0.6285**, 위로 `ep08s-3` 가 **0.7983** 이라
양쪽 다 옛 대역 밖이다. 판별선은 상관값이 아니라 **0.99대**라는 것도 대조군이 그대로 보인다.

- **tick(0.038 / 560Hz)**: 리포 전수 tick 26건(archive 포함) 중 최근접 상관 **0.0201**(`ep07s-2`).
  사실상 무상관 = 유일. 작성팀의 「25건」 열거도 내 파싱과 **정확히 일치**한다.
- **피크 천장 이분탐색(내가 다시 실행)**: tick 0.057 → 무경고 상한 **0.07209** ·
  latch 0.060 → 무경고 상한 **0.07501**. 둘 다 경고 없음. 렌더 로그 경고는 계산상 0건이다.

---

## 4. 내가 판단해 확정한 것

### 4-1. 총 길이 35.4초 — **검수 판정을 승계한다. 손대지 않는다.**

- 상한 38(fail) 아래 · 사용자 확정 **35±3** 안 · 선례 `ep10s-2` 37.93초 · `ep12s-1` 36.1초.
- 🔴 **깎을 곳이 없다는 것을 산수로 확인했다.** 본문 18.8초, 하한 18 까지 0.8초.
  §2-C(ADR-V25-9)가 인트로·훅·예고·소개를 못 깎게 한다. 남은 본문에서 한 줄을 빼면
  본문이 하한을 뚫고, `wolfCue: 1`(S1)·`graveCue: 1`(S3)이 `cue(0)` 으로 **조용히 접혀** 그림이 고장 난다.
- 🔴 **늘릴 곳도 없다.** 버린 것 중 가장 아까운 「만드는 시간보다 물게 만드는 시간이 더 길었다」를
  되살리면 6번째 본문 줄이 되고, 20자 × 6.58자/초 + pause ≈ **+3.15초 → 총 38.56초 = 상한 초과 fail**.
  **버린 판단이 길이 게이트로 강제된 것임을 확인했다** — `excluded[0]` 은 되살릴 수 없다.
- 결론 = 검수의 *"어느 쪽도 여유가 없다 — 손대지 않는 것이 정답"* 을 그대로 승계한다.

### 4-2. 한국어 라벨 (개정 `ADR-V-10` 첫 적용) — **전수로 확인. 통과.**

kind 5개의 `fillText`/`strokeText` 호출을 전수로 열어 **하드코딩 문자열이 0개**임을 먼저 확인했다
(모든 글자가 `spec` 에서 온다). 따라서 화면 글자 집합 = `spec` 문자열 전체다.

```
마을 · 처음 생긴 무덤 · 겨울 · 겨울 끝 · 늑대 · 부상자 · 0 · 감지 반경 ·
새침이 1.2 · 고집쟁이 0.6 · 빈 땅 · 임계값 · 12 · 10 · 8 · 추격 · 무덤 ·
다음 편 · 최저 비용 · 5 · 1 · 탐색 한도 · 돌 캐기 계획 실패 · NoSolutionFound
```

**로마자가 든 것은 `NoSolutionFound` 하나** — 개정 ADR-V-10 표의 예외 ① (코드·로그 그대로 인용).
화살표는 글리프가 아니라 선+삼각으로 그렸다(예외 ②에도 안 기댄다). 고집쟁이·새침이는 고유명사.
HUD 의 `Claude Code`·`GOAP` 는 개정 ADR-V-10 이 예외 ③에 **이름을 적어 둔 바로 그 둘**이고,
`ep12s-3` 의 HUD 와 **문자열이 동일** = 시리즈 고정값이지 이 편의 선택이 아니다.
🔴 `ep06s`~`ep12s-3` 을 선례로 인용한 흔적은 없다(작성팀이 그 사실을 노트에 명시했고 확인했다).

### 4-3. 직전 편 약속 이행 — **확인. 이행됐다.**

`episodes/ep12s-3/scene.json` 실사: `outro.next` = 「13편 · 마을에 처음으로 무덤이 생깁니다」,
마지막 예고 자막 = 「다음 편은, 이 마을에 처음으로 무덤이 생겨요.」

- **훅(4.60~9.11s)** 이 실물로 갚는다 — 스틸에서 흰 사람 다섯 사이에 강조색 무덤이 솟아 있고
  「처음 생긴 무덤」이 켜져 있다. 말도 「늑대가 만든 이 마을 첫 무덤이에요.」
- **마지막 본문 줄(23.96~27.55s)** 이 그 자리에 착지한다 — 「그제야 물렸고, 이 마을에 첫 무덤이 생겼습니다.」
- 훅과 착지가 같은 물건이지만 그림이 다르다(결과의 사진 ↔ 좌표에 앉는 사건). 되받기 1회. 정당하다.

### 4-4. 다음 편 예고 3단계 대조 (`procedure_next_preview_fact_check`) — **통과**

| 단계 | 결과 |
|---|---|
| 원문에 있는가 | ✅ '간호 액션의 기본 비용을 1로' · '가장 싼 액션이 5였다가 갑자기 1짜리가 등장' · '탐색 한도를 넘겨 계획 실패' · '간호를 싸게 만들었더니 광부가 돌을 못 캐게 된' 전부 적중 |
| **그 편이 버린 것이 아닌가** | ✅ `ep13s-2/scene.json` 실사 — 훅이 「간호 탓에 돌 못 캔 광부」이고 본문 5줄이 통째로 이 사건이다. **버린 것이 아니라 그 편의 전부다** |
| 쪼개져도 참인가 | ✅ 음성·카드·미리보기 셋이 같은 사건 하나를 가리킨다. 원문 맨 끝의 「다음 이야기 예고」(=블로그 다음 글)는 안 썼다 |

### 4-5. 첫 자막 / 마지막 자막

- 첫 = 시리즈 고정 인트로(0~4.05s) → 곧바로 훅. **10초 안에 사건이 약속된다.**
- 마지막 = 「다음 편은, 간호를 싸게 만들자 광부가 돌을 못 캤어요.」(예고, 4.4초 · 상한 5.0)
  → 「Claude Code 개발일지, 또 만나요.」 **예고로 닫힌다.** 소개 줄에 「다음 편」이 없어
  예고 길이 판별 정규식이 소개를 삼키지 않는다.

### 4-6. `excluded` 12건 — **되살릴 것 없음**

가장 아까운 두 건을 직접 판정했다.
① 「만드는 시간보다 물게 만드는 시간이 더 길었다」 = §4-1 산수로 **되살리면 상한 fail**. 유지.
② 「방랑자」 절 = 통로 설명이 먼저 필요해 자막 2줄이 더 든다 + `{Y}/{N}`·5일·0.7일이 전부
   이 편이 안 다루는 눈금. 유지. 결과만 「임계값도 낮추고」로 옮긴 판단이 맞다.
플레이스홀더(`{N}`·`{일수}`·`{WarnDays}`)를 화면에 안 올린 것, 마을 규모 선을 안 그린 것,
감지 반경 가운데 셋에 라벨을 안 단 것 — 셋 다 **지어낸 수를 만들지 않으려는 같은 잣대**다. 일관된다.

### 4-7. `delayMs` 두 값 — **엔진 코드를 열어 직접 검산했다**

`engine.js:396` `cue(i, lead, span)` → 창 `W = lead + rel[i].dur × span`,
`engine.js:199` `rel.dur = timed.dur + pauseAfter` (**분모가 발화+쉼이 맞다**),
`render.mjs:114` `at = line.t + delayMs` (**줄 시작 기준이 맞다**).

- **S2 tick**: `rel` = 4,052 + 250 = 4.302s · `cue(0, 0.15, 0.85)` → W = 3.8067 ·
  `walk` 완료 `c = 0.74` → 0.74 × 3.8067 − 0.15 = **2.667s** ✅ = `delayMs 2667`
- **S3 latch**: `rel` = 3,585 + 250 = 3.835s · `cue(1, 0.15, 0.75)` → W = 3.02625 ·
  `seat` 개시 `c1 = 0.72` → 0.72 × 3.02625 − 0.15 = **2.029s** ✅ = `delayMs 2029`

상수 0.85 / 0.74 / 0.75 / 0.72 를 `emptyland.js:104-105` · `chase.js:97,229` 에서 눈으로 확인했다.
아웃트로 카드는 `TOTAL − 3000`(ADR-V25-10) = **32.41s** 이고 S3 페이오프는 27.55s 안에 끝난다 — 안 먹힌다.

### 4-8. 공용 인트로 한국어화 — **사용자 판단. 승계만 기록한다.**

`engine/intro-brand.js` 의 터미널 3줄 중 지어낸 2줄이 한국어(`> 마을을 지어줘` · `배치 중...`)가 되고
`$ claude code` 는 예외 ①로 영어를 유지했다. 세 편 공통이고 마스터 판정 대상이 아니다.
🔴 다만 **이것은 전 회차 공용 파일이다** — `ep00s`~`ep12s-3` 은 ADR-V-15 로 재렌더하지 않아
실제 영향이 0 이라는 검수 판정에 동의한다.

### 4-9. `build/timed.json` — 합성물 아님

`voice: "ME"` · `engine: supertonic` · mtime 2026-08-10 10:21 KST · 6샷 9줄 ·
`summary.charsPerSec 6.58` · 내가 손으로 누산한 총합 **35.41초** = `check.mjs` 의 35.4초.
🟢 `"SYNTHETIC-NOT-REAL"` 없음. 검수가 임시 산출물을 남기지 않았다는 것도 확인했다.

---

## 5. 🔴 검수 보고에서 내가 잡은 오류 (렌더는 막지 않는다 — 다음 층이 인용하지 말 것)

### 5-1. 「`episodes/ep13s-1/` 안의 「봄」 **0건**」 — **거짓이다. 4건 있고 그중 2건은 낡았다.**

```
scene.json:97   "초고의 spec.seasonTo 가 「봄」이었고"              → 반려 이력 기록. 정당
scene.json:298  "반려 ①로 오른쪽 라벨이 「봄」(2자) → 「겨울 끝」"    → 변경 서술. 정당
scene.json:301  "초고의 S1 시간 띠 끝 라벨이 「봄」이었고"           → 반려 이력 기록. 정당
kinds/quietwinter.js:18  "초고의 끝 라벨은 「봄」이었는데"           → 이력 주석. 정당
🔴 scene.json:297  "위 시간 띠가 「겨울」에서 「봄」까지 차는 동안"   → 지금 그림을 틀리게 서술
🔴 scene.json:298  "「겨울」(좌)과 「봄」(우)은 같은 행이지만"        → 지금 그림을 틀리게 서술
```

- **화면은 옳다**(스틸로 확인 — 「겨울 끝」). 자막·`say`·`delayMs` 도 안 건드렸다. **렌더 무관.**
- 인용 부호가 붙은 위조가 아니라 **작성팀 자신의 그림 서술이 낡은 것**이라, 1차 ① 과는 계열이 다르다.
- 🔴 **그러나 무거운 것은 이것이다: 검수가 「0건」이라고 단언한 낱말이 같은 파일에 살아 있었다.**
  같은 문단(298) 안에서 한 문장은 「겨울 끝으로 바뀌었다」고 적고 다른 문장은 「봄」이라고 적는다.
  다음 작성팀이 `notes.그림 설계` 를 설계 정본으로 읽으면 **반려를 낳은 낱말이 되돌아온다.**
- **처리: 다음 회차 착수 전에 두 문장을 고친다.** 렌더를 막지 않으므로 이번 승인은 유지한다.
  🔴 **이 판정을 「노트 오류는 통과한다」는 선례로 인용할 수 없다** — 통과 사유는 「노트라서」가 아니라
  **「화면이 옳다는 것을 내가 픽셀로 확인했기 때문」**이다. 확인 없이 같은 일이 반복되면 반려다.

### 5-2. 「X 로 남은 10건은 **전부 대조기 오탐**」 — **거짓이다. 5건은 실재한다.**

전부 **뜻은 보존됐고 화면·자막에는 안 나오는** 인용 표기 결함이다(= 반려 사유 아님).
검수는 이 중 **1건만** 스스로 🟡 로 열었다(2-5.3). 나머지 4건은 못 봤다.

| 위치 | 적힌 것 | 원문 |
|---|---|---|
| `SO.source` (검수가 연 것) | '어림값이 낮아지면 플래너는 훨씬 더 많은…' | '플래너는 **"아직 멀았나?" 하며** 훨씬 더 많은…' |
| 🔴 `notes.영어 자막` | '방랑자를 한 명도 받지 않으면 마을 규모가 멈춰 버렸습니다' | '…마을 규모가 **특정 지점에서** 멈춰 버렸습니다' |
| 🔴 `excluded[3].source` | '결말을 섞지 않는다 — 이탈은 굶주림에서만…' | '결말을 섞지 않는다 — **마을을 떠나는** 이탈은…**발생합니다**' |
| 🔴 `excluded[3].why` | '사망은 부상에서만 발생**한다**' | '사망은 부상에서만 발생**합니다**' |
| 🔴 `notes.제목` | '야생 위협(늑대)…**이** 한 번에 들어가면서' | '야생 위협(늑대)·부상과…전멸 종료 화면**까지** 한 번에 들어가면서' |

🔑 **말줄임표 없는 생략은 「원문에 없는 문자열」의 약한 형태다.** 이 편의 1차 ①이 그 강한 형태였고,
같은 회차 안에서 약한 형태가 다섯 번 더 나왔다. **처리는 §5-1 과 같이 다음 회차 착수 전.**

### 5-3. 「SH·SO 5장은 픽셀이 완전히 같다 — `fillRect` 판이 `setLineDash` 판과 비트 단위로 일치」

- SO 쪽은 **공허한 증거다.** `git diff 8d3701a5 HEAD -- kinds/` 로 확인한 결과
  **`nextpeek.js` 는 애초에 안 바뀌었다.** 안 바뀐 파일이 같은 픽셀을 내는 것은 아무것도 증명하지 않는다.
- SH(`firstgrave.js`)는 실제로 바뀌었고 Δ=0 이므로 **증거는 이 한 장으로 선다.** 결론은 유지.
- 🔑 **「바뀐 파일만 세어라」** — 대조표를 만들 때 변경 여부 열이 없으면 이 착시가 다시 난다.

### 5-4. 「22건 전수 기준 **서로 다른 seed 쌍**의 상관은 0.63~0.80」 — 문장이 과일반화됐다

내가 잰 값: **이 편 ↔ 나머지 22건** = 0.6285~0.7983 (검수와 일치, 이 편 판정에는 이게 맞다).
그러나 **리포의 서로 다른 seed 쌍 250쌍 전체**로 재면 **0.6285 ~ 0.8446** 이다.
다음 검수가 이 문장을 그대로 인용하면 0.84 를 이상값으로 오판한다.
🔴 **대역 문장은 「무엇과 무엇 사이인지」를 붙여 적을 것.** 판별선(0.99대 + 최대 절대차 0.02 미만)은 그대로 유효하다.

### 5-5. 「앞 662표본 최대 절대차」 열은 **척도 미기재라 재현되지 않는다**

검수 0.2906(ep08s-3) ↔ 내 0.3924. 원인은 `gain` 정규화 위치 차이다(나는 `gain:1`).
상관값은 척도 불변이라 소수 넷째 자리까지 일치했지만, **절대차는 gain 을 안 적으면 못 맞춘다.**
🔴 **절대차를 표에 쓰려면 `gain` 값을 같은 칸에 적을 것.** 판정에는 안 썼다.

### 5-6. `sfx.why` 의 latch 천장이 낡았다

`scene.json` 이 「무경고 상한 **0.07282**(dur 0.135 · seed 157 기준)」이라 적었는데
**seed 211 의 실측은 0.07501** 이다(내 이분탐색 · 검수 2차와 일치). 근거 조건을 명시했고
보수적인 방향이라 위험은 0. 다음 갱신 때 고친다.

---

## 6. 🟡 넘기는 것 (렌더 무관 · **다음 회차가 선례로 인용할 수 없다**)

1. **§5-1 「봄」 잔존 2문장** — 다음 회차 착수 전 수정. `notes` 는 다음 작성팀의 설계 정본이다.
2. **§5-2 말줄임 없는 생략 5건** — 인용 부호 안은 원문 그대로거나 `…` 를 넣는다.
3. **`notes.길이` 문구** — 「늘릴 카드 쪽이 현실적」 → 「어느 쪽도 여유가 없다」로. 행동은 없다(§4-1).
4. **어절 검산이 근사임을 밝힐 것** — `why` 의 「15번째 글자 「민」」·「11번째 「첫」」은 wav 선행 무음을
   무시한 비례 추정이다. **판정은 문턱 시각(2,667 / 2,029)으로 했고 그 값은 §4-7 로 정확하다.**
   숫자 옆에 「대략」을 붙여 다음 사람이 근거로 쓰지 못하게 한다.
5. **§5-3~5-6 검수 방법론 4건** — 검수팀 메모리에 반영할 것.

🔴 **위 다섯은 전부 「화면·소리·타이밍에 닿지 않는다」는 것을 내가 개별로 확인했기 때문에 넘긴다.**
「노트니까 넘어간다」가 아니다. 확인 없이 같은 종류가 다시 올라오면 반려한다.

---

## 7. 결론

| 1차 반려 | 내 독립 재검증 |
|---|---|
| ① 지어낸 인용문 · 「봄」 | ✅ 닫힘 — 인용 100쌍 전수 재대조, 통째로 없는 것 **0건**. 화면 라벨 「겨울 끝」 스틸 확인. (검수의 「「봄」 0건」 주장만 §5-1 로 정정) |
| ② `latch seed 157` 충돌 | ✅ 닫힘 — 파형 재합성. 최근접 **0.7983**, 대역 0.6285~0.7983, 대조군 0.9963 과 자릿수가 다르다 |
| ③ 30fps 결정성 44건 | ✅ 닫힘 — **1062프레임 불일치 0**(3회째 독립 재현) + `kinds` 만 되돌린 격리 실험으로 **의도 외 그림 변화 0건** |

# 🟢 APPROVED

렌더는 기계가 한다:

```
node tools/scene-video/publish.mjs ep13s-1 --prepare
```

렌더 로그에 「⚠️ sfx …: 피크 천장에 걸려」 가 없어야 한다 —
계산상 없다(tick 0.057 / 상한 0.07209 · latch 0.060 / 상한 0.07501, 둘 다 내가 재측정).
**업로드는 사람이 한다**(`publish.mjs` 주석 참조).
