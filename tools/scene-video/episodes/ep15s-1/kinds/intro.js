/* intro — 이 샷의 그림은 **블렌더가 구운 3D 프레임**이다(설계 §7-1). 그리는 일은
   `scene3d.js` 하나가 한다. 어느 프레임 폴더를 쓰는지는 `scene.json` 의
   `spec.frames` 가 정한다.

   🔴 파일 이름(= kind 이름)을 바꾸지 마라. `check.mjs` 는 kind 이름으로 **구조**를 센다
      (`kind === 'intro'` 로 인트로 유무·제목 이행 시작 지점·4단 구조를 판정한다).
      이름은 샷의 **역할**이고 파일 내용은 **그림**이다 — 3D 로 바뀐 것은 뒤쪽뿐이다.
   🔴 옛 2D 도형판은 git 에 있다(이 커밋 직전). 3D 판정이 뒤집히면 거기서 꺼내라.
*/
export { default } from '../../../engine/scene3d.js';
