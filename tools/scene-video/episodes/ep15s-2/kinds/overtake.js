/* 이 샷의 그림은 **블렌더가 구운 3D 프레임**이다(설계 §7-1). 그리는 일은 엔진의
   `scene3d.js` 하나가 한다. 어느 프레임 폴더를 쓰는지는 `scene.json` 의 `spec.frames` 가 정한다.

   🔴 파일 이름(= kind 이름)을 바꾸지 마라. `check.mjs` 는 kind 이름으로 **구조**를 센다.
      이름은 샷의 **역할**이고 파일 내용은 **그림**이다.
   🔴 옛 2D 도형판은 git 에 있다. 다만 새 그림을 그릴 때 **참고하지 마라**(2026-08-13 지시) —
      그림은 대본이 말하는 사건에서 나온다(runbook §3-0).
*/
export { default } from '../../../engine/scene3d.js';
