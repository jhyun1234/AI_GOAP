/* 이 샷의 그림은 **블렌더가 구운 3D 프레임**이다(설계 §7-1). 그리는 일은 엔진의
   `scene3d.js` 하나가 한다. 어느 폴더를 쓰는지는 `scene.json` 의 `spec.frames` 가 정한다.
   🔴 kind 이름은 **역할**이다 — `check.mjs` 가 이 이름으로 구조를 센다. 바꾸지 마라.
   🔴 옛 2D 도형판을 **참고하지 마라**(2026-08-13 지시) — 그림은 대본의 사건에서 나온다.
*/
export { default } from '../../../engine/scene3d.js';
