# lf01 검수 보고 — 「우리는 왜 GOAP로 AI 마을 게임을 만드는가」

*검수 2026-08-09 · 검수팀(scene-reviewer). 판정 정본 = `Docs/롱폼_대본_문법.md` (format:"wide").
쇼츠 규격(4단 구조·30~35초·예고 사슬)은 이 회차에 적용하지 않았다.*

## 판정: 🔴 **반려** (반려 5건 · 정정 4건 · 관찰 2건)

원문 대조는 **126줄 전수**로 돌렸고 **지어낸 수치·대사는 한 건도 없다.** 반려는 전부
**클립·구조·순서** 쪽이다. 대본의 사실 층은 이 회차에서 가장 튼튼한 부분이다.

---

## 0. 먼저 — 기계 판정에 오염이 있었다 (내가 치웠다)

첫 `node tools/scene-video/check.mjs lf01` 은 이렇게 찍혔다:

```
  OK 총 길이 (wide 참고 · 판정 아님) 10.6초 = 자막 9.6 + 꼬리 1.05 (본편 기준 300~600초 · 승격은 W7 뒤)
  OK 자막 2초 미만 없음            전부 2초 이상
  OK 말 속도 6.0~7.2자/초        6.38자/초 (참고 영상 6.93)
```

126줄짜리 회차가 10.6초일 수 없다. `episodes/lf01/build/timed.json` 을 열었더니 **샷 3개짜리
W1 프로필 시험용 더미**였다:

```
shots 3
{"spokenMs":9560,"pauseMs":0,"totalMs":9560,"fullMs":9561,"chars":61,"charsPerSec":6.38,...}
0 가로 프로필 시험, 첫 샷입니다.
1 두 번째 샷, 치수를 확인합니다.
2 이건 실제 게임 화면입니다. 모닥불이 방금 완공됐습니다.
   (mtime 2026-08-09 04:07)
```

즉 **`자막 2초 미만 없음`·`말 속도`·`총 길이` 세 항목이 lf01 대본이 아니라 더미 3줄로
판정되고 있었고**, `실측 타임라인 존재`(red)와 `산정 길이 (참고)` 줄은 아예 안 찍혔다.

- 조치: `episodes/lf01/build/` 를 통째로
  `…\scratchpad\lf01_build_backup\build` 로 **옮겼다**(삭제 아님 — 되돌릴 수 있다).
  아래 §5 의 check 출력 전문은 옮긴 뒤의 것이다.
- 🔴 **TTS 를 돌리기 전까지 그 폴더를 되돌리지 마라.** 되돌리면 다음 check 가 다시
  더미 값으로 길이 게이트를 통과시킨다.
- 🔴 **합성 `timed.json` 은 만들지 않았다.** 다만 스틸 확인을 위해
  `node render.mjs lf01 --still …` 을 돌렸으므로 `episodes/lf01/build/stills/*.png` 가
  새로 생겼다(증거로 이 문서가 인용한다). 리뷰 종료 후 지워도 된다.

---

## 1. 원문 대조 (문법 §5 · §8-1) — ✅ 전수 통과

`published/*.html` 은 태그를 제거한 사본으로, `Docs/`·`devlog/`·`Assets/` 는 원문
그대로 `re.finditer` 문맥 출력으로 대조했다. **표본이 아니라 126줄 전수다.**

### 1-A. 수치 — 전부 원문에 실재 ✅

| 샷 | 화면·자막의 수치 | 대조한 정본 (원문 문장) |
|---|---|---|
| S5 | 6개 파일, 55곳 | `published/2026-07-15-unity-goap-m0-redesign.html` — "세어 보니 이런 패턴이 6개 파일, 55곳에 흩어져 있었습니다" ✅ |
| S5 | 3,476줄 | 같은 글 — "VillagerFSM 파일 하나가 3,476줄이었습니다" ✅ |
| S6 | 24,800 → 5,977 | 같은 글 — "코드 24,800줄을 5,977줄로 줄인 M0 재설계" ✅ |
| S6 | 파일 59개 | 같은 글 — "세션 로그 기록 기준으로 59개 파일이 삭제됐고" ✅ |
| S7 | 코드 0줄 · 「돌 채집」 | 같은 글 — "코드를 한 줄도 쓰지 않았습니다" / "말풍선이 "돌 채집"을 표시하는 순간" ✅ |
| S7 | 재해·위협 실행 0줄 | `published/2026-08-04-unity-goap-m13-events-and-traces.html` — "재해와 위협을 추가할 때 실행 코드가 0줄이었던 게 그 증거입니다" ✅ |
| S10 | 562 / 1,110 / 딱 두 배 | `published/2026-07-19-unity-goap-m6-season-winter-crisis.html` — "필요한 포만이 562인데 … 보유한 건 1,110 … 필요량의 딱 2배" ✅ |
| S18 | 열네 곳 / 상태형 0개 | M13 글 — "열네 곳이 나왔는데, 세어 보니 상태형 알림이 0개였습니다" ✅ |
| S19 | Day 45 / 일곱 명 / 한 줄 | M13 글 — "Day 45에 주민 일곱 명이 죽었습니다. 그런데 … 한 줄만 떠 있었습니다" ✅ |
| S25 | 12→10→8 · 코드 0줄 | `published/2026-07-28-unity-goap-m10-wolf-injury-wanderer.html` — "12에서 10으로, 다시 10에서 8로요 … 코드 변경은 0줄이었습니다" ✅ |
| S26 | 세율 0% vs 30% · 아사자 2명 | `Docs/M19_화폐전면철거와_실물마을_실행명세서.md` 0절 — "세율 0% 판과 30% 판의 아사자가 동일(2명)" ✅ |
| S30 | 목수 5배 | `devlog/sessions/2026-08-09.md` [00:30] — "목수 0.2 = 1:5 (사용자 확정 "주민이 1이면 목수는 5")" ✅ **방향 일치**(목수가 빠른 쪽) |
| S31 | 모션 5종 | `Docs/M23_비주얼_실행명세서.md` 0절 3항 — "주민 행동 모션 **5종**이 재생된다: 도끼질 … 맨손 타격" ✅ |
| S33 | 겨울 3번 · Day 44 · 최대 8명 | `published/2026-08-06-unity-goap-m14-autumn-winter.html` — 화면 문자열 그대로 ✅ |
| S33 | 대비한 넷 / 대비 안 한 넷 | 같은 글 — "대비한 주민 넷은 겨울을 세 번 넘겼고, 대비하지 않은 넷은 첫 겨울에 전원 굶어 죽었습니다" ✅ |

`say` 의 한글 읽기도 검산했다 — "이만 사천 팔백"=24,800 · "오천 구백 칠십칠"=5,977 ·
"삼천 사백 칠십육"=3,476 · "천 백 십"=1,110 · "사십오 일차"=Day 45. ✅

### 1-B. 인용·판정 문장 — 전부 원문에 실재 ✅

M6 「기술은 만점인데 재미가 0점」·「전혀 위기감 없었고, 재미없어서 가만히 앉아만 있었다」·
「관찰자만 남고 플레이어가 사라지는 게임」 / M10 「지난주에 무슨 일이 있었지?」·「재수 없어서
물렸다」·「고집쟁이 한 명만 밭에 남아 있습니다」·「도망이 100% 승리하는 구조」·「늑대를 만드는
데 든 시간보다 …」 / M12 「게으름뱅이 둘 다 그냥 죽었어, 별 이야기가 없었다」·「이야기가
로그에만 있었습니다」·「재료는 완성됐고 전달이 0이었습니다」·「일곱 밀스톤을 전부 시스템으로」·
「화면은 거의 그대로였습니다」 / M13 「질문은 두 개였습니다」·「부합한다」·「적대 AI는 유능할수록
재미있습니다 / 관리 대상 AI는 유능할수록 …」·「플래너 자리에 앉아 있고 캐릭터는 실행기」·
「자율성이 이 게임의 정체성이니까요」·「왜 지금 저기로 가는가」·「가장 안 보이는 상태」·
「가장 싼 길은 …」·「회색 원」·「영구 흔적」·「좌표뿐」·「{shortName} · Day {day}」·
「관계 기록을 양방향으로 삭제」 / M14 「성장하며 버티는 기록 경주」·「기회는 세계가 열어 주지만」 /
M22 한 줄 목표·「출몰을 생략」·「완전 무적」 / M23 한 줄 목표·「색 원과 정지 스프라이트」 /
devlog 「잘못 설치했을 때 없앨 방법이 없다」·「철거와 파괴의 갈림길」·「시트에 괭이가 없다 …
물뿌리개로 정정」·「위협(늑대·곰) 스프라이트: 에셋 조달(사람 몫)이 선행」 /
`Docs/CLAUDE.md` 「방치가 안락하면 실패다」 — **전부 HIT.**

### 1-C. 「낱말이 아니라 주어·방향」 검사 ✅ (ep11s 사고 재발 방지)

- **S3 「주민은 명령을 거부할 수 있습니다」** — `Docs/CLAUDE.md` 4행은 "(M1 예정)"이라
  적혀 있어 예고문일 수 있었다. 코드 실사로 확인: `Assets/Scripts/M0/Agent/VillagerAgent.cs:1334`
  `enum OrderResult { Accepted, RefusedHungry, RefusedTired, FailedNoStock, RefusedInjured }`,
  1337~1340행 `JudgeOrder`(ADR-M1-2). **구현돼 있다** ✅. 「보상을 걸고 다시 설득」도
  M6 글 원문 그대로 ✅.
- **S2 「이 둘은 제가 시킨 게 아니에요 / 촌장인 저는 그냥 보고 있었습니다」** — 클립 스틸에
  `늑대 무리 — 마을 습격 중 (주민 선택 후 F = 맞서라)` 이 떠 있어 "F 로 시킨 것 아닌가"를
  의심했다. 실사: 말풍선 `내가 막을게!` 의 출처는 `Assets/M0Config/Actions/Action_Fight.asset:18`,
  그 액션을 쓰는 `Goal_Fight` 는 `Assets/Scenes/M0Scene.unity:640` 의 **자율 `_goals` 목록에
  등록돼 있다**(guid `5a2f8d61…`). 즉 F 명령 없이도 스스로 고를 수 있고, 촬영은 무인
  방치(ADR-LF-4)다. **주장 성립** ✅.
- **S30 「위협이 부순 자리는 주민이 다시 짓고, 철거한 자리는 안 그러게 갈랐어요」** —
  devlog [00:51] 원문의 방향과 일치 ✅.

### 1-D. 지워진 표와의 대조 ✅

원본 HTML 을 태그 제거 전/후 둘 다 훑었다. 화면에 오른 문자열 중 **표 안에만 있던 값은
없다.** 캔버스 라벨의 영문(`Goal_P0_Hunger`·`HarvestCrop`·`CookMeal`·`EatCookedFood`·
`MineStone.asset`·`Threat_Tier1_Wolf`·`Threat_Tier2_Pack`)은 실제 에셋/카탈로그 이름이고,
`gravemark` 이 좌표를 값이 아니라 형식(`( x , y )`)으로만 그린 것·`threshold` 아래 두 시간
막대에 눈금을 안 넣은 것·`notifystack` 이 번호만 띄운 것은 **원문에 없는 값을 안 만든
올바른 처리**다(writer.md §5). 이건 통과 형태다.

### 1-E. `excluded` 8건 ✅

전부 사유가 납득되고, `shots` 안에 흔적이 0건이다(순둥이 81%·게으름뱅이 2%·218→252·
슬롯 52칸·게이트 373/373·`Player_Actions.png` 78조각 — 전부 화면에 없음을 grep 으로 확인).
특히 7번(게이트 수·배선 개수를 뺀 사유 = "④에서 스스로 비판한 '시스템으로만 채운
밀스톤'과 같은 모양이 된다")은 좋은 판단이다.

---

## 2. 🔴 반려 사유

### R1. 게임 HUD 겹침 — 문법 §4 가 「반려 사유」로 못 박은 항목

`engine/wide.css` 의 `.hud{position:absolute;top:3%;…}` 띠(제목 `.ep` 18px + `.airow` +
`.prog`)가 클립 좌상단 게임 HUD 위에 그대로 얹힌다. **엔진 실측 스틸로 확인했다.**

| 스틸 | 샷 | 무엇이 지워지는가 |
|---|---|---|
| `episodes/lf01/build/stills/362s.png` | **S18** | 게임 상태줄 `■ 굶주리는 주민 C — 포만 0, 체력이 깎이는 중 · 식량 0일치` 의 **가운데가 `왜 GOAP로 만드는가` 로 완전히 덮여** 읽을 수 없다 |
| `episodes/lf01/build/stills/227s.png` | **S12** | `■ 늑대 무리 — 1일 뒤` 가 같은 방식으로 덮여 `■` 와 끝자락만 남는다 |
| `episodes/lf01/build/stills/454s.png`·`456s.png` | **S23** | 게임 알림줄 가운데가 덮여 `…일) · G(1일)` 만 보인다 |
| `episodes/lf01/build/stills/538s.png` | **S27** | 제목이 상태줄 1번을, `Claude Code로만 만드는 GOAP AI 마을 게임 개발일지` 가 상태줄 3번을 덮어 **둘 다 판독 불가** |
| `episodes/lf01/build/stills/4s.png`·`6s.png` | **S1** | aiHook 줄이 주민 말풍선 `잘 익은 것만 골라야지` 와 겹친다 |

원본 게임 HUD 문자열은 클립 스틸로 대조했다 —
`D:\AI_GOAP-videos\clips\library\stills\death-260809-0424-80-b.png`(굶주림 줄) ·
`season-260809-0424-135-b.png`(`■ 늑대 무리 — 1일 뒤`) ·
`strike-farm-260809-0424-139-b.png`(알림줄 4줄).

🔴 **가장 나쁜 자리에서 났다.** S18 의 자막은 「알림 나는 자리가 열네 곳인데, 상태형은
0개였어요 / **그래서 저 줄을 새로 얹었습니다**」이고, S12 의 자막은 「**위에는 늑대 무리가
하루 뒤라고 떠 있는데**」다. 둘 다 **말이 가리키는 바로 그 줄을 우리 HUD 가 지운다.**
브리프 §0-2 가 예고했고 writer.md §4 가 "제목을 9자로 짧게 잡았다"로 대응했지만,
문제는 **가로 폭이 아니라 y 좌표**다 — 9자로도 게임 HUD 2~3번째 줄 높이에 그대로 앉는다.

**고칠 방향(택1, 대본 몫과 엔진 몫이 갈린다)**
- (대본 몫) 겹침이 나는 4샷의 클립을 **좌상단 게임 텍스트가 1줄뿐인 컷**으로 교체
  (`strike-villager-…-192` 처럼 — `stills/…-192-b.png` 는 HUD 가 `Day 31 …` 한 줄뿐이다).
  단 S18·S12 는 그 줄을 보여 주는 게 목적이라 교체로는 못 푼다.
- (엔진 몫 · 마스터 판단) 클립 샷일 때 HUD 띠를 **아래/오른쪽으로 밀거나 반투명 밑판을
  깔기**. `.hud` 는 `wide.css` 한 곳이므로 프로필 안에서 처리된다.
  ⚠️ 이걸 고른다면 `style.css` 와 함께 고칠 필요가 없는지(문법 §3 마지막 줄) 확인할 것.

### R2. S23 클립에 나레이션이 말하는 것이 없다 — §4 스틸 확인 의무 위반

`node tools/scene-video/clips.mjs stills forecast-260809-0424-58` 로 직접 떴다.

- `D:\AI_GOAP-videos\clips\library\stills\forecast-260809-0424-58-a.png` (0.0s):
  **완전히 빈 초록 들판**. HUD = `Day 3 · 1년째 여름 (남은 3일)` +
  `■ 겨울까지 5일 — 준비 부족: C(1일) · G(1일)` 뿐.
- 같은 폴더 `-b.png` (3.5s): 마을은 보이지만 HUD =
  `■ 배고픈 주민 G — 포만 33 · 식량 1일치` + `■ 겨울까지 5일 — 준비 부족: C(1일) · G(1일)`.

**두 프레임 어디에도 늑대 예고줄(`■ 늑대 무리 — N일 뒤`)이 없다.** 카탈로그 `detail` 이
`Threat_Tier1_Wolf` 라도 화면에 예고가 안 떠 있으면 그림이 말을 못 나른다.
S23 자막은 「늑대는 예고부터 뜹니다 / 예고, 등장, 타격, 퇴장 순서로요」이고
`reads` 는 「위협이 갑자기 나타나지 않고 **예고부터 화면에 뜬다**」다 — **말과 그림 불일치.**

덧붙여 이 컷은 `camDist 11.0` 인데 `in:0` 으로 잡아 **앞 ~3초가 빈 들판**이다
(문법 §4 "camDist 가 큰 컷(카메라가 늦게 도착)은 스틸 없이 채택하지 않는다").

writer.md 131~134행이 스스로 적었다 — *"이 작업 환경에서는 `clips.mjs stills` 를 돌릴 수
없었다 … 「사용자 확인」이 달린 `forecast-58` 만 채택"*. **그 표시가 틀렸다.** 스틸을
직접 못 뜨는 환경이면 채택하지 말았어야 한다.

→ **교체 또는 캔버스화.** 브리프 §4⑥ 의 `forecast-…-99`(Tier2 예고)·`-198`(Tier3 곰)을
쓰려면 **스틸을 먼저 뜨고 그 파일명을 `source` 에 적어라.** 예고줄이 실제로 떠 있는 컷이
없으면 「예고 → 등장 → 타격 → 퇴장」은 캔버스로 그리는 편이 정직하다.

### R3. S8 클립이 `reads` 와 다르다

`gothome-260809-0424-12` 스틸(`…-b.png` 3.5s / `…-c.png` 6.8s):
주민 `E · 농부` 가 **물뿌리개를 휘두르고**(b) 곧이어 **망치를 들고**(c) 있으며 말풍선은
`모닥불을 짓자` · `여기가 농사짓기 좋겠어`. Day 0, 텐트 1동 + 밭 1칸.
**집도, 입주도 화면에 없다.**

`reads` 는 「재건된 규칙 위에서 **주민이 제 집으로 들어간다**」이고 브리프 §4③ 은
「입주 순간. 마을이 커지는 그림」이라고 적었다 — 화면에 없는 사건이다.
엔진 스틸 `episodes/lf01/build/stills/155s.png` 에서는 아예 **주민이 한 명도 안 보인다**
(텐트·밭·점 하나뿐).

자막 자체(「재설계가 끝나고, 마을은 다시 돌아갔습니다」)는 그림과 모순되지 않는다. 하지만
`reads` 는 검수 대상이고(문법 §8 · 지시서 4번) 틀렸다. **`reads` 를 화면대로 고치거나
클립을 바꿔라.** 굳이 이 컷을 남길 거면 `reads` 는 「주민이 물뿌리개·망치를 들고 밭과
모닥불을 오간다 — 재건된 규칙 위에서 마을이 계속 돈다」쪽이 사실이다.

### R4. 콜드 오픈·챕터 길이가 §1 대역 밖

식은 `check.mjs` 안의 것만 썼다(CPS_REF 6.7 · SHOT_TAIL 0.35 · 구두점 제외, 실측 오차 ±2.0초).
회차 총계는 그 식이 찍은 **423.6초**와 일치한다.

| 마디 | 샷 | 산정 | §1 규격 | 판정 |
|---|---|---|---|---|
| 콜드 오픈 「습격」 | S1~S3 | **25.6초** | 30~60초 | ❌ 미달(오차 감안 27.6초여도 미달) |
| 챕터1 「재설계」 | S4~S8 | 67.7초 | 1~2분 | ✅ |
| 챕터2 「정적」 | S9~S12 | **41.7초** | 1~2분 | ❌ 미달 |
| 챕터3 「역설」 | S13~S16 | **54.9초** | 1~2분 | ❌ 경계 아래 |
| 챕터4 「화면」 | S17~S21 | 60.7초 | 1~2분 | ✅ (턱걸이) |
| 챕터5 「늑대」 | S22~S26 | **59.1초** | 1~2분 | ❌ 경계 아래 |
| 챕터6 「지금」 | S27~S31 | 62.2초 | 1~2분 | ✅ |
| 마무리 「기록」 | S32~S34 | 51.9초 | ~1분 | ✅ |
| **총계** | 34샷 | **423.6초** | 300~600 / 지시 420~540 | ✅ |

콜드 오픈이 짧은 원인은 명확하다 — S1 5.6초·S2 4.6초로 **클립 두 샷이 각각 2줄뿐**이다
(writer.md §4 가 "클립 샷은 자막 2줄·say 45자 안쪽"으로 묶었다).
🔴 **길이를 맞추려고 다른 곳을 깎지 마라.** 브리프 §2① 이 준 재료 중 아직 안 쓴 것이 있다 —
화면에 실재하는 `늑대 무리 — 마을 습격 중 (주민 선택 후 F = 맞서라)`(= 촌장이 개입할 수
있다는 이 게임의 문법)과 무덤 고리. 콜드 오픈은 거기서 늘려라.
「정적」 챕터도 M6 의 「좋은 AI는 문제를 없애지만, 좋은 게임은 문제가 필요합니다」(브리프
§2④ 인용, 아직 미사용)를 되살리면 자연스럽게 찬다(ADR-V25-4 의 "줄이면서 뺐던 원문
어휘를 되살린다"와 같은 처방).

### R5. S29 「그래서 공성을 먼저 넣었습니다」 — 인용한 정본과 순서가 반대

`Docs/M22_방어건설_실행명세서.md` 5절 「커밋 분할 계획」:

```
| 4 | W4 건설 사슬 | 울타리가 서고 막기 시작 (위협은 아직 생략 — 알려진 중간 상태로 커밋 메시지에 명시) |
| 5 | W5 공성 + 파괴 + 복구 | 루프의 절반 완성 → **리뷰⓪** |

⚠️ 4번 커밋 직후는 "울타리 = 무적" 상태다 (전제 확인 ②). W4·W5를 같은 세션에서 잇고,
사이에 Play 판정을 하지 않는다 — 이 중간 상태로 밸런스를 판단하면 오판이 된다.
```

**울타리(W4)가 먼저 서고 공성(W5)이 뒤에 붙었다.** 정본이 명시적으로 "4번 커밋 직후는
울타리 = 무적 상태"라고 적어 둔 이상, 「공성을 먼저 넣었다」는 서술은 성립하지 않는다.
앞 문장(「명세를 쓰기 전에 전제를 확인하다 걸렸습니다」)은 같은 문서 1절
「전제 확인 (0단계 실사 산출물 — 2026-08-08 코드 실사 3건)」과 정확히 맞으므로 ✅ 그대로 두고,
마지막 줄만 정본이 말하는 순서로 고쳐라 — 예: 「그래서 명세의 핵심 축을 공성으로 잡았습니다」
또는 「그래서 울타리와 공성을 같은 세션에서 붙여 넣었습니다」.

---

## 3. 🟡 정정 요구 (반려는 아니지만 고쳐야)

### C1. S34 마지막 줄 시제 — 아직 없는 쇼츠를 「있습니다」로 약속

> 「재설계도, 늑대도, 허물어 버린 화폐도 각 편의 이야기는 이 채널 쇼츠 시리즈에 **있습니다**.」

- 재설계 = `state/schedule.json` 의 `ep03s`(`2026-07-15-unity-goap-m0-redesign.html`) — **존재** ✅
- 늑대 = `order` 의 마지막 `ep13s`(`2026-07-28-unity-goap-m10-wolf-injury-wanderer.html`) —
  **아직 제작 전**(`episodes/ep13s` 폴더 없음)
- 화폐 = 블로그도 미발행(`published/2026-08-08-unity-goap-m19-currency-teardown-DRAFT-NOT-PUBLISHED.html`),
  순서표에도 없음

문법 §6 의 **궤도 조건은 셋 다 만족**한다 — `tools/blog-automation/BLOG_COVERAGE.md`
154~164행이 대기열을 `M14 → M15 → M16 → M17 → M18` 로 명시하고, 같은 자리에
"M16·M18은 완결이 아니라 폐기·대체"라는 재프레이밍 권고까지 달아 뒀다. **유도 자체는 적법.**
문제는 시제뿐이다 → 「시리즈에서 하나씩 다룹니다」·「전말은 시리즈에서」로.
`youtube.blurb` 마지막 문장(「각 회차의 자세한 이야기는 이 채널의 쇼츠 시리즈에 있습니다」)도 같다.

### C2. 효과음 개수 자기보고가 틀렸다 — 15개가 아니라 **17개**

`scene.json` 의 `notes.효과음` 과 `writer.md` §6 첫 줄이 「15개」라고 적었다. 실제로 세면
S1·S3·S4·S5·S6·S7·S17·S20·S21·S22·S24·S25·S28·S29·S31·S32·S33 = **17개**다.
같은 문단의 kind별 표(thud 2 + riser 3 + drop 3 + tick 3 + sweep 2 + latch 4)는 17로
맞으므로 **머리 숫자만 틀렸다.** 밀도 자체는 문제 없다(423.6초에 17개 = 초당 0.040개).

효과음 나머지는 통과다:
- **gain 상한** ✅ — `tick` 3개 전부 0.060/0.062 (상한 0.065) · `latch` 4개 0.062 (상한 0.071).
- **seed 중복 없음** ✅ — `grep -o '"seed": *[0-9]*' episodes/*/scene.json | sort -u` 결과
  기존 회차는 23·29·37·41·43·53·61·67·71·79·83·89·97·101·103·113 이고 lf01 의
  127·131·137·139·149·151 은 **lf01 에서만 나온다.**
- **돌려막기 아님** ✅ — kind별로 dur/freq/seed 를 갈라 둔 근거가 `why` 마다 적혀 있고,
  다른 회차 씬의 sfx 와 성격이 겹치지 않는다.

### C3. `notes.길이` 를 실제 출력으로 갈아 끼울 것

`notes.길이` 와 writer.md §8 이 "check 의 「산정 길이」 줄이 나오면 그 줄로 갈아 끼운다"고
예고만 하고 값이 비어 있다. 지금 그 줄은:

```
OK 산정 길이 (참고 · 판정 아님)     423.6초 = 발화 411.7 + 꼬리 11.90 · 실측 오차 ±2.0초
```

이 문장을 **그대로** 인용해라. 🔴 자체 계산식(÷6.7 이든 뭐든)을 문서에 베껴 적지 마라 —
식은 `check.mjs` 안에만 둔다.
(writer.md §8 의 「약 2,560자 · 7분 안팎」도 자체 산정이므로 같이 정리 대상.)

### C4. 정적 구간 — 캔버스 3샷은 설계 구멍이다

`⚠ 정적 구간 3초 이하` 경고 10건 중 **캔버스 샷 셋은 진짜 결함**이다.

- **S7 `zerocode` 14.6초** — writer.md §3 의 「계속 도는 것」 목록에 zerocode 가 **아예 없다.**
  `episodes/lf01/kinds/zerocode.js:68` `if (kRun > 0.05) { const kk = (t % 2.6) / 2.6; … }`
  — 유일한 연속 모션(주민 점)이 `kRun = ease(cue(2))` 뒤에만 돈다. 자막 0·1 구간이
  통째로 정지다. 엔진 스틸 `episodes/lf01/build/stills/136s.png` 로 확인 —
  인스펙터 3칸이 다 채워지고 `CODE WRITTEN 0 LINES` 까지 선 뒤 오른쪽이 빈 채로 멈춰 있다.
- **S13 `twoq` 6.6초** — `kinds/twoq.js:57~58` `band(x1, kCards > 0.6 && kAns < 0.5)` /
  `band(x2, kCards > 0.6)`. 검토 밴드가 `cue(1)` 뒤에만 돌아 첫 자막 구간이 정지다.
- **S31 `motion5` 4.6초** — writer.md §3 표가 "칸 등장을 `(cue(2)+cue(3))/2` 로 옮겼다"고
  적었지만 여전히 남았다.

writer.md §3 마지막 줄이 *"산수로 잡은 것이라 픽셀 실측은 검수/렌더 때 다시 봐야 한다"*
라고 예고했다 — 재 봤고, 셋이 남았다. **연속 모션을 cue 게이트 밖으로 빼라**(refusal·
logscreen·stock 이 이미 쓰고 있는 방식 그대로).

나머지 7건(S2·S8·S9·S12·S18·S23·S27 = 클립 샷)은 아래 O1 과 같은 뿌리로 보인다.

---

## 4. 🟠 관찰 — 마스터/파이프라인 몫 (대본으로 못 고침)

### O1. 🔴 클립 레이어가 프레임을 안 넘기는 것으로 보인다 (엔진 W5)

**측정 둘이 독립적으로 같은 말을 한다.**

① `render.mjs --still` 로 뜬 엔진 스틸의 **클립 영역이 시간이 흘러도 비트 단위로 같다.**
ffmpeg PSNR (`crop=1920:560:0:260`, 자막·HUD 제외 영역):

```
451 vs 454 :: average:inf      455 vs 454 :: average:inf
452 vs 454 :: average:inf      456 vs 454 :: average:inf
453 vs 454 :: average:inf      457 vs 454 :: average:inf     ← S23 전 구간 6.6초
358 vs 362 :: average:inf      366 vs 362 :: average:inf     ← S18
  1 vs 6   :: average:inf                                     ← S1
```

② `check.mjs` 가 클립 7샷을 **샷 길이만큼 정지**로 보고한다:
`S2 8.2s · S8 7.8s · S9 9s · S12 9.4s · S18 10.2s · S23 7.4s · S27 8.6s`
(각 샷의 임시 타이밍 길이 8.4/8.1/9.2/9.7/10.4/7.7/8.8초와 거의 같다).

③ **영상 파일 자체는 멀쩡하다** — 같은 클립을 ffmpeg 로 뽑은 `-a/-b/-c` 스틸은 시각마다
다른 그림이다(예: `built-260809-0424-39-b.png` 에는 망치 든 목수가 있고 `-a` 에는 없다).

→ 원인은 `engine/engine.js` 348~366행의 클립 seek·draw 경로(`v.currentTime = want` +
`setTimeout(done, 500)` 폴백) 쪽으로 보인다. **이대로 렌더하면 클립 8샷이 전부 정지
사진**이라 롱폼이 내세운 「연출이 아니라 관측」이 무너진다.
🔑 대본이 고칠 수 있는 문제가 아니다 — 파이프라인에서 규명하거나, "스틸 경로만의
아티팩트"임을 실렌더로 증명해야 한다. **그 전에는 승인 못 한다.**

### O2. `kinds/widetest.js` 잔여

`scene.json` 이 참조하지 않고 writer.md §10 이 "지워도 된다"고 적었다. 기계는 안 본다.
정리 시점은 마스터 판단.

---

## 5. 기계 판정 — `node tools/scene-video/check.mjs lf01` 출력 전문

*(§0 대로 W1 더미 `build/` 를 치운 뒤의 출력. `실측 타임라인 존재` red 는 TTS 전이라
정상이며 판정에서 제외했다.)*

```
준비 중. 완료

가이드 점검 · lf01
────────────────────────────────────────────────────
  OK shot 메타 완비             34샷 전부 있음
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               126줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 인트로 있음                 wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 훅 시작 문구                wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 아웃트로 소개 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 영어 자막 (없음 · 판정 안 함)    이 회차는 en 이 0줄이다 — 영어 자막 대상이 아닌 회차로 보고 건너뛴다
  OK 제목 이행 (첫 두 줄)          wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 제목 후킹 (정보)             숫자 0 — 기본 축은 감정·사건+질문형(0-A), 뜻 판정은 검수팀
  OK 다음 편 예고 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 예고 5초 이하               wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  🔴 실측 타임라인 존재             episodes/lf01/build/timed.json 이 없다 — tts.mjs 를 먼저 돌려라
  OK 산정 길이 (참고 · 판정 아님)     423.6초 = 발화 411.7 + 꼬리 11.90 · 실측 오차 ±2.0초 — notes.길이 는 이 값을 인용할 것
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 무대 프로필 일치              가로세로비 1.7778 (기대 1.7778 · format=wide)
  OK 콘텐츠 안전영역 (비주얼 ≤ 850)   비주얼 바닥 850 (한계 850)
  OK 자막영역 850~1060 안        자막 머리 874 · 바닥 1037
  OK 결정성 (3패스)              3665프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  ⚠  정적 구간 3초 이하            S2 8.2s, S7 14.6s, S8 7.8s, S9 9s, S12 9.4s, S13 6.6s, S18 10.2s, S23 7.4s, S27 8.6s, S31 4.6s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
🔴 1건 실패
```

⚠️ `결정성 (3패스) 불일치 0` 은 O1 이 사실이라면 **정지한 클립이 사소하게 결정적인 것**이므로
클립 재생이 고쳐진 뒤 다시 봐야 한다.

---

## 6. 항목별 요약

| # | 항목 (문법 §) | 판정 | 근거 |
|---|---|---|---|
| 1 | 원문 대조 — 수치 (§5) | ✅ | §1-A 표 · 정본 15종 전수 HIT, 지어낸 수치 0 |
| 2 | 원문 대조 — 인용 (§5) | ✅ | §1-B · 30여 문장 전수 HIT |
| 3 | 주어·방향 (지시서) | ✅ | §1-C · S3 코드 실사 · S2 `Goal_Fight` 자율 등록 확인 |
| 4 | 지워진 표 오염 (지시서) | ✅ | §1-D · 표 전용 값 0건 |
| 5 | `excluded` 사유 (지시서 3) | ✅ | §1-E · 8건 전부 흔적 0 |
| 6 | 콜드 오픈 30~60초 (§1) | ❌ | **R4** — 25.6초 |
| 7 | 챕터 4~6개 · 각 1~2분 (§1) | ❌ | **R4** — 6개는 ✅, 「정적」41.7초 등 3개 미달 |
| 8 | 총 길이 300~600 / 420~540 (§1) | ✅ | 423.6초 |
| 9 | 콜드 오픈 = 사건으로 열기 · 제목 카드 금지 (§1) | ✅ | S1 첫 프레임부터 늑대 습격 클립, 카드 없음 |
| 10 | 열린 고리 (§1) | ✅ | 무덤(S1→S20) · Q2(S13→S15) · 가려진 사슬(S16→S17) · 전제 뒤집기(S28→S29) |
| 11 | 클립 ≥5개 (W8 DoD) | ✅ | 8개 |
| 12 | 클립 — 사건이 프레임 안에 (§4) | ❌ | **R2**(forecast-58 예고줄 없음) · **R3**(gothome-12 입주 없음). 나머지 6개는 ✅ |
| 13 | 클립 — 게임 HUD 겹침 (§4) | ❌ | **R1** — 스틸 5장 |
| 14 | 클립 — 스틸 확인 흔적 (§4) | ❌ | **R2** — writer.md 131행이 스틸 미확인을 자백 |
| 15 | 클립 — 연출 의심 (ADR-LF-5) | ✅ | 디버그 스폰 0. 울타리·문·고스트를 「보고 있다」고 쓰지 않고 캔버스로 처리(S28·S31)한 것은 정확한 판단 |
| 16 | 시점 — 화폐 과거 완결형 (§6) | ✅ | S26 「지었다가 통째로 허물었습니다」·「같았거든요」, 「추가 예정」류 0건 |
| 17 | 시점 — 미래형은 「지금」·「북극성」에만 (§6) | ✅ | S27~S34 밖에 미래형 없음 |
| 18 | 유도 문구 궤도 (§6) | ⚠️ | 궤도 ✅(BLOG_COVERAGE 154~164행) / 시제 ❌ → **C1** |
| 19 | 유도는 한 번만 (브리프 §5) | ✅ | S34 마지막 줄 1회 |
| 20 | 그림 돌려막기 금지 (§2) | 🟡 | `teardown.js` 는 ep01s 와 완전히 다른 장치 ✅. `refusal.js` 는 ep04s-1 과 **「명령 칩이 주민 앞에서 되튕긴다」장치가 겹친다** — lf01 은 REWARD 토큰이 통과하는 층을 더했으니 그 차이를 `reads`/주석에 명시하거나 장치를 바꿔라(의견) |
| 21 | 모든 샷에 그림 (§2) | ✅ | 34샷 전부 kind 또는 clip |
| 22 | 3색 팔레트 (§2) | ✅ | check `위반 없음` |
| 23 | 안전영역 (§3) | ✅ | 비주얼 바닥 850 / 자막 874~1037 |
| 24 | 정적 구간 | ❌ | **C4**(캔버스 3샷) + **O1**(클립 7샷) |
| 25 | 효과음 절제·배치 (§7) | ✅ | 17개 = 초당 0.040. gain 상한·seed 중복 전부 통과 |
| 26 | 효과음 개수 자기보고 | ❌ | **C2** — 15 ≠ 17 |
| 27 | 앵비언트 — 대본이 부르는 샷에만 (§7) | ✅ | 8샷(S1 + 각 챕터 첫 샷). 전 구간 상시 아님 |
| 28 | `notes.길이` 자체 계산 금지 | ⚠️ | **C3** — 값이 비어 있음, 423.6초로 채울 것 |
| 29 | 영어 자막 | — | **대상 아님.** `Docs/롱폼_영상_트랙_실행명세서.md` 37행 비목표: *"영어판(`--lang en`) 롱폼 — 한국어 파일럿 먼저"*. `scene.en.json` 도 없음 ✅ |
| 30 | 말투 (voice.json) | ✅ | 어미 3연속 육안 점검 통과(S6·S24·S28·S31 표본) |

---

## 7. 작성팀에 — 고칠 것 목록 (어느 샷 · 무엇이 · 정본 어디와 어긋남)

1. **S18·S12·S27·S23·S1** — 엔진 HUD 가 게임 HUD 를 덮음.
   `Docs/롱폼_대본_문법.md` §4 마지막 줄과 어긋남. 증거 = `build/stills/362s.png`·`227s.png`·
   `538s.png`·`454s.png`·`4s.png`. 대본 몫(클립 교체) / 엔진 몫(HUD 좌표)을 갈라 마스터에 올릴 것.
2. **S23** — `forecast-260809-0424-58` 에 늑대 예고줄이 없음. §4 「스틸 확인 의무」와 어긋남.
   증거 = `…\stills\forecast-260809-0424-58-a.png`·`-b.png`. 교체하거나 캔버스로.
3. **S8** — `reads` 「주민이 제 집으로 들어간다」가 화면에 없음. 증거 =
   `…\stills\gothome-260809-0424-12-b.png`·`-c.png`, `build/stills/155s.png`.
4. **콜드 오픈(S1~S3) 25.6초 · 챕터 「정적」41.7초** — §1 대역 미달. 원문 어휘를 되살려 채울 것
   (미사용 재료: `늑대 무리 — 마을 습격 중 (주민 선택 후 F = 맞서라)` / M6 「좋은 AI는 문제를
   없애지만, 좋은 게임은 문제가 필요합니다」). 🔴 다른 마디를 깎아 맞추지 말 것.
5. **S29 마지막 줄** — 「공성을 먼저 넣었습니다」가 `Docs/M22_방어건설_실행명세서.md` 5절
   (W4 울타리 → W5 공성, "4번 커밋 직후는 울타리 = 무적")과 순서가 반대.
6. **S34 마지막 줄 · `youtube.blurb`** — 「쇼츠 시리즈에 있습니다」→ 아직 없는 회차
   (늑대=`ep13s` 미제작 · 화폐=미발행). 시제만 고칠 것.
7. **`notes.효과음` · writer.md §6** — 「15개」→ **17개**.
8. **`notes.길이`** — `423.6초 = 발화 411.7 + 꼬리 11.90` 을 그대로 인용. 자체 식 금지.
9. **`kinds/zerocode.js`·`twoq.js`·`motion5.js`** — 연속 모션을 cue 게이트 밖으로.
   증거 = check `정적 구간` S7 14.6s · S13 6.6s · S31 4.6s, `build/stills/136s.png`.
10. (의견) **`kinds/refusal.js`** — ep04s-1 과 장치가 겹칠 소지. 차이를 명시하거나 바꿀 것.

---

## 8. 검수가 남기는 말

이 대본의 **사실 층은 지금까지 본 회차 중 가장 정확하다.** 126줄 전수 대조에서 원문에 없는
수치·대사가 0건이고, `source` 칸이 파일 경로와 행 번호까지 달려 있어 대조가 빨랐다.
`gravemark` 이 좌표를 값이 아니라 형식으로 그린 것, `threshold` 에 눈금을 안 넣은 것,
울타리·문·고스트를 "화면에서 보고 있다"고 쓰지 않고 캔버스로 돌린 것 — 셋 다 원문이
말하지 않은 것을 안 만든 좋은 판단이다.

무너진 곳은 전부 **"실제로 화면에서 확인했는가"** 한 줄에 걸려 있다. writer.md 131행이
`clips.mjs stills` 를 못 돌렸다고 적어 뒀고, 그 자리에서 정확히 두 건이 샜다(R2·R3).
그리고 브리프 §0-2 가 경고한 HUD 겹침은 제목 글자 수로는 못 막혔다(R1).
**클립 여덟 개는 스틸을 다시 뜬 뒤에 다시 고르라** — 카탈로그의 `detail` 은 사건 이름이지
프레임 안에 그것이 있다는 보증이 아니다.

---
---

# 2차 검수 (2026-08-09) — 반려 5건·정정 4건 대응 재검

*범위 = 1차에서 걸린 것 + 새로 들어온 것. 사실 층 전수 재검은 하지 않았다
(1차에서 126줄 전수 통과 · 2차는 **새 17줄만** 대조).*

## 판정: 🔴 **반려** (반려 1건 · 정정 2건 · 관찰 2건) — 이 회차 **2번째 반려**

**1차 반려 5건은 전부 해소됐다.** 남은 반려는 **2차 개정에서 새로 들어온 자막 한 줄**이고,
1차 R2 와 **같은 종류의 실수**(확인했다고 적은 것이 실제 프레임과 다름)다.

---

## A. 🔴 최고 우선 항목 — 클립이 실제로 움직이는가 → **해소 ✅**

지시대로 같은 샷 안 두 시각을 떠서 클립 영역만 비교했다
(`crop=1920:560:0:260`, 자막·HUD 제외).

| 샷 | 스틸 두 장 | ffmpeg PSNR | check 정적식 `m` (문턱 0.0008) |
|---|---|---|---|
| S1 `strike-farm-…-139` | `3s` ↔ `12s` | **19.82 dB** | 0.019635 (24배) |
| S12 `season-…-135` | `280s` ↔ `285s` | **19.40 dB** | 0.023787 (30배) |
| S18 `death-…-80` | `437s` ↔ `443s` | **18.29 dB** | — |
| S23 `forecast-…-99` | `540s` ↔ `550s` | — | 0.010225 (13배) |
| S27 `built-…-39` | `636s` ↔ `642s` | **18.13 dB** | 0.022821 (29배) |

1차에서는 같은 측정이 **전부 `PSNR = inf`(비트 단위 동일)** 였다. 지금은 유한하고 낮다 =
프레임이 실제로 바뀐다. **HTTP Range 수리가 먹었다 — 관찰 O1 종결.** ✅

### A-2. 그런데 `check.mjs` 에 클립 정적 경고가 아직 7건 있다 → **TTS 전 산물이다 (결함 아님)**

```
⚠ 정적 구간 3초 이하   S1 8s, S2 3.8s, S8 5.8s, S9 11s, S12 11.6s, S18 3.2s, S23 7s
```

원인을 재서 확정했다. `timed.json` 이 없으면 엔진이 **임시 타이밍**(`PROVISIONAL_CPS 5.6`)을
쓰는데, 그 길이가 실제 TTS 길이보다 훨씬 길어 클립 창을 넘긴다. 넘긴 만큼 마지막 프레임이
얼어붙고 그게 그대로 정적으로 잡힌다.

| 샷 | 클립 창 | 산정 길이(6.7) | 임시 길이(5.6) | 임시 초과 | check 정적 |
|---|---:|---:|---:|---:|---:|
| S1 | 13.0 | 11.98 ✅ | 21.12 | 8.1 | **8.0** |
| S2 | 9.0 | 7.38 ✅ | 13.00 | 4.0 | **3.8** |
| S8 | 10.0 | 9.07 ✅ | 15.90 | 5.9 | **5.8** |
| S9 | 10.0 | 8.99 ✅ | 15.94 | 5.9 | 11.0 |
| S12 | 18.0 | 17.26 ✅ | 29.83 | 11.8 | **11.6** |
| S18 | 7.0 | 5.89 ✅ | 10.39 | 3.4 | **3.2** |
| S23 | 12.0 | 10.60 ✅ | 18.62 | 6.6 | **7.0** |
| S27 | 7.0 | 5.30 ✅ | 8.78 | 1.8 | (미보고) |

7건 중 6건이 「임시 초과」와 오차 0.4초 안에서 일치한다. **모든 클립 창이 산정 길이보다
크므로 TTS 뒤에는 이 정지가 사라진다** — 작성팀의 B-1 규칙(`out − in ≥ 샷 길이`)이 옳게
적용됐다. 🔑 **TTS 후 `check.mjs` 를 다시 돌려 확인할 것.**

예외는 **S9(11.0초)** 다. `broll-…-160` 은 **게임 화면 자체가 멈춰 있다** —
`190s` ↔ `190.2s` 의 `m = 0.000005`(문턱의 1/160)이고, `188s` ↔ `192s` 는 0.005084 로
움직인다. 즉 초 단위로는 도는데 프레임 단위로는 거의 안 변한다. 이 샷의 뜻이
「겨울인데 아무 일도 안 일어난다」이므로 **설계와 일치**한다. 경고로 남긴다.

### A-3. 캔버스 정적 3샷 → **해소 ✅**

1차의 `S7 14.6s · S13 6.6s · S31 4.6s` 가 **전부 목록에서 사라졌다.** B-7 처방
(연속 모션을 cue 게이트 밖으로)이 먹었다.

---

## B. 1차 반려 5건 재검

### R1 게임 HUD 겹침 → **해소 ✅**

`wide.css` HUD 우측 46% 정렬. 1차에 겹침을 지적한 다섯 자리를 전부 다시 떴다.

| 스틸 | 샷 | 결과 |
|---|---|---|
| `build/stills/437s.png` | S18 | `■ 굶주리는 주민 C — 포만 0, 체력이 깎이는 중 · 식량 0일치` **전부 판독 가능** ✅ |
| `build/stills/270s.png` | S12 | `■ 늑대 무리 — 1일 뒤` 판독 가능. 자막 「위에는 늑대 무리가 하루 뒤라고 떠 있는데」와 화면이 같은 것을 가리킨다 ✅ |
| `build/stills/636s.png` | S27 | 상태줄 3줄 전부 판독 가능 + `A · 목수` 가 **망치를 든 채** `다들 힘내세요!` — 자막과 정확히 일치 ✅ |
| `build/stills/546s.png` | S23 | 좌상단 경보 2줄 판독 가능 ✅ |
| `build/stills/12.5s.png` | S1 | `□ 늑대 무리 2마리 — 마을 습격 중 (주민 선택 후 F = 맞서라)` 판독 가능 ✅ |

🟡 **대가 하나** — HUD 가 우측으로 갔더니 S23 의 무덤 한 기(x≈1715, y≈106)가 제목
`왜 GOAP로 만드는가` 뒤에 부분적으로 잠긴다. 글자 위 글자가 아니라 그림 위 글자라
R1 급은 아니지만, 그 샷의 자막이 하필 **무덤을 세는 줄**이라 아래 R6 과 맞물린다.

### R2 S23 클립 교체 → **클립은 ✅, 자막 한 줄이 새 반려 (R6)**

새 클립 `forecast-260809-0424-99`(camDist 16 · durSec 18 · `in:6`)를 스틸로 확인했다.
`D:\AI_GOAP-videos\clips\library\stills\forecast-260809-0424-99-c.png` 와
엔진 스틸 `build/stills/540s.png`:

- `Day 12 · 2년째 온화 · 여름까지 3일`
- `늑대 무리이(가) 다가옵니다 — 2.5일 뒤`
- **`■ 늑대 무리 — 3일 뒤`** ← 자막 「화면 위에 사흘 뒤라고 떠 있습니다」와 정확히 일치 ✅

1차 R2 의 핵심(예고줄이 프레임에 없다)은 해소됐다.

### R3 S8 서술 교정 → **해소 ✅**

`build/stills/170s.png`: 주민 `E · 농부` 가 **물뿌리개를 휘두르는 궤적**과 함께
`모닥불을 짓자` 말풍선. 새 `reads`(「주민이 물뿌리개와 망치를 들고 밭과 모닥불을 오간다」)와
화면이 일치한다 ✅. 자막도 M0 글 83행으로 교체돼 노동 장면과 말이 같은 것을 가리킨다.

### R4 마디 길이 → **해소 ✅**

`check.mjs` 식(CPS_REF 6.7 · SHOT_TAIL 0.35 · 구두점 제외)으로 재계산.
총계가 check 의 「산정 길이 485.0초」와 일치하므로 마디 합산도 같은 자에서 나온 값이다.

| 마디 | 1차 | 2차 | §1 규격 | 판정 |
|---|---:|---:|---|---|
| 콜드 오픈 「습격」 | 25.6 | **34.7** | 30~60초 | ✅ |
| 챕터1 「재설계」 | 67.7 | **71.9** | 1~2분 | ✅ |
| 챕터2 「정적」 | 41.7 | **65.4** | 1~2분 | ✅ |
| 챕터3 「역설」 | 54.9 | **62.3** | 1~2분 | ✅ |
| 챕터4 「화면」 | 60.7 | **60.7** | 1~2분 | ✅ |
| 챕터5 「늑대」 | 59.1 | **72.2** | 1~2분 | ✅ |
| 챕터6 「지금」 | 62.2 | **66.4** | 1~2분 | ✅ |
| 마무리 「기록」 | 51.9 | **51.4** | ~1분 | ✅ |
| **총계** | 423.6 | **485.0** | 300~600 / 지시 420~540 | ✅ |

🔑 **깎아서 맞추지 않았다** — 8마디 중 줄어든 곳이 없고(화면 60.7 동일, 기록 −0.5),
늘어난 줄은 전부 브리프가 준 미사용 원문이다. ADR-V25-4 의 올바른 처방이다.

### R5 S29 순서 → **해소 ✅**

```
4) 그래서 울타리와 공성을 / 같은 세션에 붙여 넣었습니다.
5) 중간 상태로 밸런스를 보면 / 오판이 되니까요.
```
`Docs/M22_방어건설_실행명세서.md` 5절 ⚠️ 줄 —
*"W4·W5를 같은 세션에서 잇고, 사이에 Play 판정을 하지 않는다 — 이 중간 상태로 밸런스를
판단하면 오판이 된다."* 두 줄 다 이 문장에서 나왔고 순서도 맞다 ✅.

---

## C. 🔴 새 반려 — R6. S23 「무덤 두 기 / 왼쪽」이 화면과 다르다

> S23 자막 1) **「그리고 그 왼쪽에는 / 무덤 두 기가 서 있고요.」**

이 줄의 출처는 원문이 아니라 **화면**이다. 그래서 화면으로 검산했다.

**① 개수 — 두 기가 아니라 네 기.**
자막 1 이 떠 있는 구간(임시 타이밍 543.4~548.5초)의 엔진 스틸 `build/stills/546s.png` 와
`552s.png` 를 480×270 으로 줄여 **무덤 색 픽셀을 군집으로 셌다**:

```
546s 회색 원 군집 4  [{cx:1715,cy:106},{cx:1498,cy:322},{cx:1066,cy:323},{cx:1391,cy:644}]
552s 회색 원 군집 4  (동일)
```

이 회색이 무덤이라는 것은 코드로 확정했다 —
`Assets/Scripts/M0/SimulationLoop.cs:668`
`sr.color = new Color(0.45f, 0.45f, 0.5f, 0.9f); // 회색 비석 마커`
이고, **게임 전체에서 이 회색을 쓰는 오브젝트는 무덤뿐**이다
(`grep "sr.color" Assets/Scripts/M0/` 전수 — 위협=`so.BodyColor`(빨강) ·
방랑자=`(0.4,0.75,0.4)`(초록) · 밭=`(0.45,0.85,0.35)`/`(1,0.85,0.2)` ·
문 자물쇠=노랑 · 내구도=검붉음). 클립 스틸 `forecast-260809-0424-99-c.png` 에서도
같은 네 개가 보이고 그중 하나에 이름표 `G · Day 10` 이 달려 있다.

**② 방향 — 「왼쪽」이 아니라 오른쪽.**
네 기의 x 좌표가 **1066 · 1391 · 1498 · 1715**(화면 폭 1920)로 전부 오른쪽 절반이다.
「그」가 가리키는 경보줄은 좌상단(x < 320)이므로 「그 왼쪽」이면 x < 320 이어야 한다.
정확히 반대다.

🔴 이것은 **1차 R2 와 같은 종류의 실수**다. `writer.md` B-2 가
*"교체본은 스틸 `-c` 실측으로 … 무덤 2기 「G · Day 10」 이 확인됐다"* 라고 적었는데,
그 `-c` 스틸에도 무덤은 넷이다. **확인했다고 적은 것이 프레임과 다르다.**
지시서가 가장 무겁게 다루는 위반(수·방향이 실물과 어긋남, ep11s 급)이라 반려로 올린다.

**고칠 방향** — 셋 중 하나:
- (a) 화면대로 고친다: 「그리고 오른쪽에는 무덤이 네 기 서 있고요.」
  (이름표가 읽히는 것은 한 기뿐이니 「그중 하나엔 G · Day 10 이라고 적혀 있고요」로
  이어 붙이면 S20 의 이름표 고리와도 맞물린다.)
- (b) 세지 않는다: 「그리고 그 옆엔 이미 무덤이 서 있습니다.」 — 수·방향을 안 말하면 안 틀린다.
- (c) 줄을 뺀다(그러면 「늑대」 챕터가 72.2 → 약 67초, 여전히 §1 안).

🔴 어느 쪽을 택하든 **고친 뒤 그 자막 구간의 엔진 스틸을 다시 떠서 확인 흔적을
`source` 에 남길 것.** 이번에 두 번 같은 자리에서 났다.

---

## D. 🟡 정정 요구 (반려 아님)

### C3-2. `notes.길이` — 이제 값이 나왔다

현재 `notes.길이` 는 1차 값(423.6초)을 인용하면서 *"이 값은 1차본의 것이라 지금 대본에는
안 맞는다"* 고 적어 두었다. 정직한 처리지만 미완이다. 방금 나온 줄로 갈아 끼워라:

```
산정 길이 (참고 · 판정 아님)     485.0초 = 발화 473.1 + 꼬리 11.90 · 실측 오차 ±2.0초
```

### O2-2. `kinds/widetest.js` 여전히 미참조 (1차 관찰 그대로)

---

## E. 🟠 관찰

1. **S12 가 5줄짜리 클립 샷이 됐다.** 챕터2의 결론 두 줄(「관찰자만 남고 플레이어가
   사라지는 게임이 됐어요」·「좋은 AI는 문제를 없애지만…」)이 캔버스가 아니라 클립 위에
   얹혔다. `reads` 는 여전히 앞 두 줄만 설명한다. 사실·규격 위반은 아니고
   그림이 말을 방해하지도 않지만(화면이 한산하다), **`reads` 를 5줄 전체를 덮게
   고쳐 두면 다음 사람이 헷갈리지 않는다.**
2. **HUD 우측 정렬의 대가** — B의 🟡 참조. 클립의 오른쪽 위에 사건이 있는 컷을 고를 때는
   이제 **오른쪽**을 조심해야 한다. 규칙이 뒤집혔다는 것을 `writer.md` 나 브리프에
   한 줄 남겨 두면 다음 회차가 같은 자리를 다시 밟지 않는다.

---

## F. 새 자막 17줄 출처 대조 — **16줄 ✅ / 1줄 ❌**

| 샷 | 새 줄 | 정본 | 판정 |
|---|---|---|---|
| S1 | 「화면 위에는 마을 습격 중이라고 떠 있어요」 | 화면 문자열 `□ 늑대 무리 2마리 — 마을 습격 중 (주민 선택 후 F = 맞서라)` (`build/stills/12.5s.png` 실측) | ✅ |
| S1 | 「주민을 고르고 F를 누르면 맞서라고 시킬 수 있습니다」 | 같은 문자열 + `Assets/Scripts/M0/UI/PlayerInputController.cs:179` `Input.GetKeyDown(KeyCode.F) … TryGiveOrder(_orderFight)` | ✅ |
| S2 | 「촌장인 저는 아무것도 안 눌렀습니다」 | 촬영 = 무인 방치(ADR-LF-4) · `Goal_Fight` 는 `M0Scene.unity:640` 자율 `_goals` 등록(1차 검증) | ✅ |
| S2 | 「주민이 스스로 고른 행동이에요」 | M0 글 1행 "스스로 상황을 판단하고 계획을 세웁니다" | ✅ |
| S8 | 「주민이 스스로 노동하고, 명령을 거부하고, 없으면 굶는」 + 「그걸 만들 수 있는 토대가 생겼어요」 | M0 글 83행 원문 그대로 | ✅ |
| S9 | 「겨울이 끝났고, 아무도 마을을 떠나지 않았어요. 한 명도요」 | M10 글 9행 | ✅ |
| S10 | 「주민들 판단이 유능할수록 위기를 완벽하게 회피했어요」 + 「그럴수록 제가 개입할 이유가 사라졌고요」 | M6 글 56행 | ✅ |
| S11 | 「숫자만 보면 긴장이 있었어요」 | M10 글 11행 | ✅ |
| S12 | 「겨울 내내 밭을 갈고, 열매를 줍고, 모닥불 앞에서 밥을 먹었어요」 | M10 글 9행 | ✅ |
| S12 | 「관찰자만 남고 플레이어가 사라지는 게임이 됐어요」 | M6 글 56행 | ✅ |
| S12 | 「좋은 AI는 문제를 없애지만, 좋은 게임은 문제가 필요하거든요」 | M6 글 85행 | ✅ |
| S13 | 「GOAP를 처음 쓴 사람이 노린 건 코드 없이 콘텐츠를 늘리는 거였어요」 + 「이 프로젝트는 실제로 그 대가를 회수하고 있고요」 | M13 글 31행 | ✅ |
| S22 | 「결말을 섞지 않기로 했어요. 이탈은 굶주림에서만, 사망은 부상에서만」 | M10 글 26행 | ✅ |
| S23 | 「화면 위에 사흘 뒤라고 떠 있습니다」 | 화면 `■ 늑대 무리 — 3일 뒤` 실측 | ✅ |
| S23 | **「그 왼쪽에는 무덤 두 기가 서 있고요」** | 화면 = **무덤 4기 · 전부 오른쪽** | 🔴 **R6** |
| S23 | 「이 밀스톤에서 마을에 처음으로 잃는 일이 생겼습니다」 | M10 글 1행 | ✅ |
| S24 | 「다리가… 움직일 수가 없어.」 | M10 글 58행 | ✅ |
| S29 | 「울타리와 공성을 같은 세션에 붙여 넣었습니다」 + 「중간 상태로 밸런스를 보면 오판이 되니까요」 | M22 명세 5절 ⚠️ 줄 | ✅ |
| S34 | 「…쇼츠 시리즈에서 하나씩 다룹니다」(시제 정정) | C1 대응 — 궤도는 `BLOG_COVERAGE.md` 154~164행 | ✅ |

정정 C2(효과음 15→17)도 확인했다 — `scene.json` 을 세니 **정확히 17개**이고
`notes.효과음` 도 17로 고쳐졌다. `youtube.blurb` 끝 문장도 「하나씩 다룹니다」로 바뀌었다 ✅.

---

## G. 기계 판정 — `node tools/scene-video/check.mjs lf01` (2차, 전문)

*`build/timed.json` 은 여전히 없다(1차 §0 대로 W1 더미를 치운 상태 유지).
`실측 타임라인 존재` red 는 TTS 전이라 정상이며 판정에서 제외했다.
`build/stills/*.png` 는 이번 재검수에서 내가 뜬 증거 스틸이다.*

```
가이드 점검 · lf01
────────────────────────────────────────────────────
  OK shot 메타 완비             34샷 전부 있음
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               143줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 인트로 있음                 wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 훅 시작 문구                wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 아웃트로 소개 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 영어 자막 (없음 · 판정 안 함)    이 회차는 en 이 0줄이다 — 영어 자막 대상이 아닌 회차로 보고 건너뛴다
  OK 제목 이행 (첫 두 줄)          wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 제목 후킹 (정보)             숫자 0 — 기본 축은 감정·사건+질문형(0-A), 뜻 판정은 검수팀
  OK 다음 편 예고 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 예고 5초 이하               wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  🔴 실측 타임라인 존재             episodes/lf01/build/timed.json 이 없다 — tts.mjs 를 먼저 돌려라
  OK 산정 길이 (참고 · 판정 아님)     485.0초 = 발화 473.1 + 꼬리 11.90 · 실측 오차 ±2.0초 — notes.길이 는 이 값을 인용할 것
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 무대 프로필 일치              가로세로비 1.7778 (기대 1.7778 · format=wide)
  OK 콘텐츠 안전영역 (비주얼 ≤ 850)   비주얼 바닥 850 (한계 850)
  OK 자막영역 850~1060 안        자막 머리 874 · 바닥 1037
  OK 결정성 (3패스)              4197프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  ⚠  정적 구간 3초 이하            S1 8s, S2 3.8s, S8 5.8s, S9 11s, S12 11.6s, S18 3.2s, S23 7s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
🔴 1건 실패
```

🔑 1차와 견주면 **캔버스 정적 3샷(S7·S13·S31)이 사라졌고**, 남은 7건은 §A-2 대로
TTS 전 임시 타이밍이 클립 창을 넘겨 생긴 것이다. **결정성은 클립이 실제로 도는 상태에서도
4,197프레임 불일치 0** — Range 수리가 결정성을 깨지 않았다는 뜻이라 이 값이 이번엔 의미가 있다.

---

## H. 작성팀에 — 고칠 것 (3차)

1. 🔴 **S23 자막 1** — 「그 왼쪽에는 무덤 두 기」 → 화면은 **무덤 4기 · 전부 오른쪽**.
   증거 = `build/stills/546s.png`·`552s.png`(군집 4개, x 1066·1391·1498·1715) ·
   `forecast-260809-0424-99-c.png` · 색 근거 `SimulationLoop.cs:668`.
   고친 뒤 **그 자막 구간 스틸을 다시 떠서 `source` 에 확인 흔적을 남길 것.**
2. 🟡 `notes.길이` → 「485.0초 = 발화 473.1 + 꼬리 11.90」 으로 갈아 끼울 것.
3. 🟡 S12 `reads` 를 5줄 전체를 덮게 고칠 것(선택).

**이 밖에는 고칠 것이 없다.** 1의 한 줄만 처리되면 3차에서 통과 판정을 낸다.

## I. 검수가 남기는 말

2차 개정은 **처방을 그대로, 그리고 옳은 방향으로** 집행했다. 특히 R4 를 「깎기」가 아니라
「원문 되살리기」로 푼 것(8마디 중 줄어든 곳 0), 클립 `in`/`out` 을 재슬라이스 규격에 맞춰
전면 재계산해 **여덟 창 모두 산정 길이보다 크게** 잡은 것, `refusal` 의 차이를 축 셋으로
명시한 것은 좋은 작업이다. 캔버스 정적 셋도 원인(연속 모션이 cue 게이트 뒤)을 정확히
제거했다.

남은 한 줄이 아쉬운 이유는 **같은 실수가 두 번째**이기 때문이다. 1차 R2 는
「브리프의 사용자 확인 표시를 스틸 확인으로 갈음」했고, 2차 R6 은 「스틸을 봤다고 적었는데
개수를 잘못 셌다」이다. 둘 다 *"확인했다"* 는 자기보고가 프레임과 어긋난 경우다.
**화면 문자열·개수·방향을 말하는 자막은, 그 자막이 떠 있는 시각의 스틸을 떠서
그 위에서 세라.** 클립 스틸(`-a/-b/-c`)은 샷이 실제로 쓰는 창과 다를 수 있다 —
`forecast-99` 는 `in:6` 이라 `-a`·`-b` 는 아예 화면 밖 구간이다.

🔴 **반려 2회.** 다음 반려가 3회째이므로, 3차에서도 같은 자리(화면과 자막의 불일치)가
남으면 지적을 쓰지 말고 사람에게 넘긴다.

---
---

# 3차 확인 검수 (2026-08-09) — R6 + 정정 2건만

*범위 = 2차 판정이 남긴 세 줄(H-1 R6 · H-2 `notes.길이` · H-3 S12 `reads`)과 **무변경 확인**뿐이다.
사실 층 전수 재검은 하지 않았다(1차 126줄 전수 · 2차 새 17줄 대조로 이미 끝났다).*

## 판정: ✅ **통과** — 마스터로 올린다

2차가 건 조건(*"1의 한 줄만 처리되면 3차에서 통과 판정을 낸다"*)이 충족됐고,
**새 red 도 새 결함도 없다.** 반려 횟수는 2회로 멈춘다.

---

## 1. R6 해소 — ✅ 화면과 자막이 일치한다

### 1-A. 문구

`scene.json` S23 `lines[1]`:

```
text : "그리고 화면 오른쪽에는\n무덤이 서 있고요."
say  : "그리고 화면 오른쪽에는 무덤이 서 있고요."
```

지시받은 문구 그대로다. 「두 기」(틀린 수)와 「그 왼쪽」(틀린 방향)이 **둘 다 사라졌다.**

### 1-B. 스틸 재실측 — 내가 다시 떴다

`node tools/scene-video/render.mjs lf01 --still 546` 로 **이번 대본 기준의 프레임을 새로 떠서**
(`build/stills/546s.png` 갱신) 2차 실측을 독립 재현했다.
회색 비석 픽셀을 480×270 에서 군집으로 세는 방식을 그대로 다시 돌렸다:

```
546s 회색 군집 4  (1714,105)n=102  (1067,322)n=104  (1498,322)n=115  (1390,645)n=83
     (+ (1784,203)n=19 = 우리 진행바 회색 트랙 — 무덤 아님. S1 스틸에도 같은 자리에 있다)
```

- **개수 = 4** — 2차 실측(1066·1391·1498·1715)과 좌표가 ±2px 안에서 같다. 재현됨 ✅
- **방향 = 전부 오른쪽** — 최소 x 가 **1067**(화면 폭 1920, 중앙 960)이므로
  네 기가 모두 오른쪽 절반이다. 새 자막 「화면 오른쪽에는」과 **어긋나지 않는다** ✅
- 눈으로도 확인했다. 같은 스틸에 자막 「그리고 화면 오른쪽에는 / 무덤이 서 있고요.」가
  떠 있고, 오른쪽 위 무덤 한 기(1714,105)는 제목 `왜 GOAP로 만드는가` 뒤에 절반쯤
  잠겨 있으며, (1390,645) 무덤엔 이름표 `G · Day 10` 이 붙어 있다.

### 1-C. 「개수를 안 세는 쪽」 선택에 대한 판정 — 타당하다

작성팀이 (a)「네 기」 대신 (b)「세지 않는다」를 고른 사유 두 개를 다 검산했다.

1. **가림이 실재한다** — 위 스틸에서 (1714,105) 무덤이 제목 글자에 부분적으로 덮인다.
   화면에 넷이 있어도 **시청자가 온전히 셀 수 있는 것은 셋**이므로, 수를 말하면
   「화면과 맞다」와 「보는 사람이 세면 맞다」가 갈린다. 방향만 말하는 쪽이 안전하다.
   ⚠️ 다만 이건 **가림을 방치해도 된다는 뜻이 아니다** — 아래 O3.
2. **자막 길이가 그대로다** — 구두점·공백을 뺀 글자 수가 옛 줄 **17자**, 새 줄 **17자**로
   같다. 그래서 아래 §3 의 산정 길이가 그대로 485.0초다. 자기보고가 사실이었다 ✅
   (`say` 원문 길이는 25 → 23자로 2자 줄었다. 이건 §3-B 에서 따로 쓴다.)

### 1-D. `source`·`reads` 에 실측 흔적이 남았는가 — ✅

`source` 에 스틸 파일명(`546s.png`·`552s.png`) · 네 기의 x 좌표 · 회색 근거
(`Assets/Scripts/M0/SimulationLoop.cs:668`) · **왜 개수를 안 쓰는지**까지 적혀 있다.
`reads` 도 「화면 오른쪽 절반에는 이미 잃은 사람의 무덤이 서 있다」로 화면과 맞다.
2차가 요구한 「고친 뒤 스틸을 다시 떠서 흔적을 남길 것」이 지켜졌다.

---

## 2. `notes.길이` — ✅ 실측 줄로 갈아 끼워졌다

```
"길이": "「산정 길이 (참고 · 판정 아님)  485.0초 = 발화 473.1 + 꼬리 11.90 · 실측 오차 ±2.0초」
         — 2차본에 대해 check.mjs lf01 이 찍은 줄 그대로다(검수 2차 실측). 34샷 · 자막 143줄.
         문법 §1(300~600초)과 지시받은 목표(420~540초) 안이다. 🔴 여기에 자체 계산식을 적지
         않는다 — 식은 check.mjs 안에만 둔다. TTS 뒤 timed.json 이 생기면 「총 길이」 실측 줄이
         이 값을 대신한다."
```

- 1차 값(423.6초)과 *"이 값은 낡았다"* 단서가 **지워졌다** ✅
- **자체 산정식이 없다** ✅ — ÷6.7·CPS_REF·SHOT_TAIL 같은 식이 한 글자도 없다.
  뒤에 붙은 「34샷 · 143줄」·「대역 안이다」는 계산이 아니라 사실 서술이라 문제 없다.
- `writer.md` §8 의 「약 2,560자 · 7분 안팎」 자체 산정도 여전히 지워진 상태다 ✅
- 🔑 이 값이 **방금 돌린 check 출력과 글자 단위로 같다**(아래 §4). 인용이 정확하다.

---

## 3. 다른 곳 무변경 — ✅ 주장대로다

### 3-A. 구조 불변 — 직접 셌다

| 항목 | 작성 보고 | 내가 센 값 | 판정 |
|---|---|---|---|
| 샷 | 34 | **34** | ✅ |
| 자막 | 143줄 | **143** | ✅ |
| 효과음 | 17 | **17** (S1·S3·S4·S5·S6·S7·S17·S20·S21·S22·S24·S25·S28·S29·S31·S32·S33) | ✅ |
| `en` | 0줄 | **0 / 143** · `scene.en.json` 없음 | ✅ (§29 대로 롱폼 파일럿은 영어 비목표) |
| `legacyFormat` | — | **없음** | ✅ |

**클립 `in`/`out` 8샷 전수** — 2차 §B-1 표와 한 칸도 다르지 않다:

```
S1  strike-farm-…-139     in 1  out 14      S12 season-…-135    in 0  out 18
S2  strike-villager-…-192 in 1  out 10      S18 death-…-80      in 2  out 9
S8  gothome-…-12          in 1  out 11      S23 forecast-…-99   in 6  out 18
S9  broll-…-160           in 1  out 11      S27 built-…-39      in 1  out 8
```

### 3-B. 「한 줄만 건드렸다」의 물증 — 기계 출력의 미세 차이가 정확히 그것만 설명한다

`check.mjs` 2차 ↔ 3차 출력에서 달라진 곳은 **딱 둘**이고, 둘 다 그 한 줄로 설명된다.

| 항목 | 2차 | 3차 | 설명 |
|---|---|---|---|
| 산정 길이 | 485.0초 | **485.0초** | 구두점 제외 글자 수가 17 → 17 로 같다 → **변화 0** |
| S23 정적 구간 | 7s | **6.6s** | `say` 원문이 25 → 23자. `engine/engine.js:186` 의 임시 타이밍은 `say.length / PROVISIONAL_CPS(5.6)` 라 **2 ÷ 5.6 = 0.357초** 짧아진다 → 클립 창을 넘긴 정지가 그만큼 준다(7.0 − 0.36 ≈ 6.6) |
| 결정성 프레임 | 4197 | **4196** | 위와 같은 0.357초만큼 총 임시 길이가 줄어 표본 1프레임 감소 |

🔑 이 셋은 **다른 자막을 하나라도 건드렸다면 성립하지 않는다.** 산정 길이가 소수점까지
동일하다는 것은 143줄 전체의 구두점 제외 글자 총합이 안 바뀌었다는 뜻이고, 정적·프레임
차이는 정확히 「그 한 줄의 원문이 2자 짧아진 것」과 크기가 맞는다.

### 3-C. mtime 대조 (git 이 답을 못 줌 — HEAD 는 W1 더미 3샷본이다)

`git show HEAD:…/lf01/scene.json` 은 샷 3개짜리 **W1 프로필 시험 더미**라 3차 개정의
기준선이 못 된다. 그래서 파일 시각으로 갈랐다:

```
scene.json      2026-08-09 07:11   ← 3차 개정
notes/writer.md            07:12   ← 3차 개정 기록
notes/review.md            07:08   ← 2차 검수
kinds/*.js      최신이 refusal 06:46 · motion5 06:46 · twoq 06:45 · zerocode 06:45
                = 전부 2차 개정 시각. 3차에 그림 파일은 하나도 안 건드렸다 ✅
```

### 3-D. `build/` 오염 없음 — ✅ 합성 `timed.json` 은 만들어지지 않았다

```
episodes/lf01/build/           → stills/ 뿐
episodes/lf01/build/timed.json → 없음
```

1차 §0 에서 옮겨 둔 **W1 더미 `build/`** 는 여전히
`…\scratchpad\lf01_build_backup\build\`(timed.json·video.mp4·track.wav 등)에 그대로 있고
회차 폴더로 돌아오지 않았다. 🔴 **TTS 전까지 되돌리지 마라** — 되돌리면 다음 check 가
「10.6초」짜리 더미로 길이·말 속도·자막 2초 게이트를 통과시킨다.

이번 검수에서 내가 새로 만든 것은 **증거 스틸 두 장뿐**이다
(`build/stills/546s.png` 갱신 · `build/stills/1s.png` 갱신 — 아래 O4 용).
합성 타임라인은 만들지 않았다.

---

## 4. 기계 판정 — `node tools/scene-video/check.mjs lf01` (3차, 전문)

*`실측 타임라인 존재` red 는 TTS 전이라 정상이며 판정에서 제외했다(지시 3번).*

```
준비 중.. 완료

가이드 점검 · lf01
────────────────────────────────────────────────────
  OK shot 메타 완비             34샷 전부 있음
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               143줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 인트로 있음                 wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 훅 시작 문구                wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 아웃트로 소개 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 영어 자막 (없음 · 판정 안 함)    이 회차는 en 이 0줄이다 — 영어 자막 대상이 아닌 회차로 보고 건너뛴다
  OK 제목 이행 (첫 두 줄)          wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 제목 후킹 (정보)             숫자 0 — 기본 축은 감정·사건+질문형(0-A), 뜻 판정은 검수팀
  OK 다음 편 예고 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 예고 5초 이하               wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  🔴 실측 타임라인 존재             episodes/lf01/build/timed.json 이 없다 — tts.mjs 를 먼저 돌려라
  OK 산정 길이 (참고 · 판정 아님)     485.0초 = 발화 473.1 + 꼬리 11.90 · 실측 오차 ±2.0초 — notes.길이 는 이 값을 인용할 것
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 무대 프로필 일치              가로세로비 1.7778 (기대 1.7778 · format=wide)
  OK 콘텐츠 안전영역 (비주얼 ≤ 850)   비주얼 바닥 850 (한계 850)
  OK 자막영역 850~1060 안        자막 머리 874 · 바닥 1037
  OK 결정성 (3패스)              4196프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  ⚠  정적 구간 3초 이하            S1 8s, S2 3.8s, S8 5.8s, S9 11s, S12 11.6s, S18 3.2s, S23 6.6s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
🔴 1건 실패
```

- **새 red 0건** ✅ — 유일한 red 는 지시가 제외한 `실측 타임라인 존재`다.
- **산정 길이 485.0초 그대로** ✅
- **경고는 2차와 동일 7건**(S23 만 7 → 6.6). 2차 §A-2 에서 「TTS 전 임시 타이밍이
  클립 창을 넘겨 생긴 것 · 여덟 창 모두 산정 길이보다 크므로 TTS 뒤 사라진다」로
  원인이 확정된 것들이라 **새 결함이 아니다.** 예외 S9(11.0초)도 2차 판정대로
  「겨울에 아무 일도 안 일어난다」는 설계와 일치하는 경고다.
  🔑 **TTS 뒤 `check.mjs` 를 한 번 더 돌려 이 7건이 실제로 사라지는지 확인할 것**(마스터 몫).

---

## 5. 🟠 관찰 — 반려 아님, 마스터/다음 회차 몫

### O3. 우측 HUD 가 무덤 한 기를 덮는 것은 그대로다 (2차 🟡의 연장)

이번 자막은 개수를 안 말해 **틀리지 않게** 됐지만, 가림 자체는 남아 있다
(`546s.png` 에서 (1714,105) 무덤이 제목 뒤에 절반쯤 잠긴다). 대본으로 푼 것이 아니라
**대본이 피해 간 것**이다. 1차 R1(좌상단 겹침)을 우측 정렬로 풀면서 오른쪽에 사건이
있는 컷이 새 위험지대가 됐다 — 2차 E-2 가 이미 짚었다.
**다음 회차 브리프/`writer.md` 에 「이제 조심할 곳은 오른쪽」을 한 줄 남길 것.**

### O4. S20 의 무덤 고리 — 검산해 봤고 **사실이다** (내가 의심했다가 기각한 건)

S23 이 화면-판독 문장으로 두 번 걸렸으므로, **같은 종류의 문장 하나를 더** 검산했다.
S20 자막 4 「맨 처음 늑대 장면에서 화면 왼쪽에 있던 그 무덤이 이겁니다.」

- 처음엔 무덤이 **없는 줄 알았다.** S1 클립 `strike-farm-…-139` 18초 전체를 2fps 로
  훑어 회색 비석 군집을 셌더니 **0건**이었다(546s 에서는 같은 잣대로 4건이 잡힌다).
- 🔴 **그 측정이 틀렸다.** 원본 프레임을 확대해 보니 이 컷에서는 무덤 마커가 **주황 원에
  가려져 있고 이름표만 보인다** — 클립 `t=6s` 확대 스틸에서 「C · Day 9」 라벨이
  주황 원(중심 x≈1174) 위에 겹쳐 있다. 색으로 세는 방법이 여기서만 안 통한다.
- **방향도 맞다.** 엔진 스틸을 새로 떠서 확인했다(`build/stills/1s.png` 재렌더) —
  S1 첫 자막 구간에 「C · Day 9」 가 **x ≈ 578 / 1920 = 화면 왼쪽 30%** 지점에 있다.
  (클립 뒤쪽 `t=6s` 에서는 카메라가 움직여 x≈1174 로 오른쪽에 오고, `t=14s` 엔 화면
  밖으로 나간다. 자막이 말하는 「맨 처음」 시점 기준으로는 **왼쪽이 맞다.**)
- 🔑 남기는 말: **색 군집 세기는 만능이 아니다.** 다른 오브젝트가 앞에 겹치면 0으로
  나온다. 화면 판독은 마지막에 **확대 스틸을 눈으로** 봐야 한다.
- 🟡 의견 하나 — S20 캔버스는 무덤을 **회색 원**으로 그리는데(M13 원문 「회색 원」이라
  근거는 정확하다) S1 화면에서 그 자리는 **주황 원 + 이름표**로 보인다. 고리가
  이름표(`C · Day 9`)로는 이어지지만 색으로는 안 이어진다. 반려도 정정도 아니고,
  다음에 이 컷을 다시 쓸 때 알고 있으면 좋은 사실이다.

### O5. `kinds/widetest.js` 잔여 (1·2차 관찰 그대로) — 여전히 미참조. 정리 시점은 마스터 판단.

### O6. S12 `reads` 정정(2차 H-3, 선택 항목)은 반영됐다

「그 평온이 곧 *관찰자만 남았다*는 판정의 근거」 절이 붙어 다섯 줄 전체를 덮는다 ✅

---

## 6. 3차 확인 요약

| 확인 항목 | 판정 | 근거 |
|---|---|---|
| R6 — S23 자막 문구 교체 | ✅ | `lines[1].text` = 「그리고 화면 오른쪽에는 / 무덤이 서 있고요.」 |
| R6 — 2차 실측과 어긋나지 않는가 | ✅ | 재렌더 `546s.png` 독립 재측정: 무덤 4기, x 1067·1390·1498·1714 전부 오른쪽 |
| R6 — 실측 흔적을 `source` 에 남겼는가 | ✅ | 스틸명·좌표·`SimulationLoop.cs:668`·개수 생략 사유까지 |
| `notes.길이` 실측 인용 | ✅ | 「485.0초 = 발화 473.1 + 꼬리 11.90」 · 자체 식 0건 |
| 산정 길이 485.0 유지 | ✅ | check 3차 출력이 소수점까지 동일 |
| 새 red 없음 | ✅ | red 1건 = `실측 타임라인 존재`(TTS 전 · 판정 제외) |
| 샷 34 · 자막 143 · 클립 in/out 불변 | ✅ | 전수 대조 |
| 그림·효과음 무변경 | ✅ | `kinds/*.js` mtime 전부 2차 시각 · sfx 17개 동일 |
| 합성 `timed.json` 없음 | ✅ | `build/` 에 stills 뿐 · 더미는 scratchpad 에 격리 유지 |

## 7. 마스터에게 넘기며

**통과.** 반려 2회로 끝났다.

세 번 도는 동안 이 회차가 실제로 고친 것은 **화면 판독 하나**였다.
1차 R2(스틸을 안 뜨고 채택) → 2차 R6(스틸을 봤는데 잘못 셌다) → 3차(수와 방향을
말하지 않는 문장으로 바꾸고 `source` 에 좌표를 박음). **원인을 없애는 방향으로 수렴했고**,
작성팀이 `writer.md` B-9 에 세운 규칙(*"화면에서 읽어 세거나 방향을 말하는 문장은,
셀 수 있으면 세고 셀 수 없으면 말하지 않는다"*)은 그대로 다음 회차 자산이다.

남은 것은 **파이프라인 몫 둘**이다:

1. `tts.mjs lf01` → `check.mjs lf01` 재실행. 볼 것은 ①`실측 타임라인 존재` red 해소
   ②「총 길이」 실측이 485.0 ±2.0초 안인지 ③**정적 경고 7건이 사라지는지**(2차 §A-2 의
   예측이 맞는지 검증하는 자리다. S9 하나는 설계상 남을 수 있다).
2. 🔴 그 전에 `…\scratchpad\lf01_build_backup\build\` 의 **W1 더미를 회차 폴더로
   되돌리지 말 것.** TTS 가 새 `timed.json` 을 쓰면 그 백업은 그대로 버려도 된다.
