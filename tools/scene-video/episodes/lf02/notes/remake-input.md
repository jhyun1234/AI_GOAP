# lf02 재제작 — 코디네이터 입력 (2026-08-10)

> 이 파일은 **코디네이터(세션)가 직접 측정하고 사용자와 합의한 것**이다.
> 기획팀 → 작성팀 → 검수팀 → 마스터가 전부 이 파일을 정본으로 읽는다.
> 🔴 v1 산출물(`notes/brief.md`·`writer.md`·`review.md`·`verdict.md`)은 **2026-08-09 기준**이다.
> 수치·사실은 이 파일이 이기고, 방법론·지뢰 기록은 v1 문서가 여전히 유효하다.

---

## 0. 왜 다시 만드는가

사용자 판정(2026-08-10): *"나는 개발을 하면서 보니깐 괜찮은데, 이 영상을 처음 보는 사람들이
너무 어렵다, 이해하기 힘들다, 무슨 내용인지 모르겠다, 단어가 어렵다 … **비개발자들에게도
충분히 쉽게** 설명을 해줄 수 있게끔 대본과 애니메이션이 만들어져야 할 거 같아."*

→ `ADR-V25-13`~`15` 가 신설됐고 `Docs/롱폼_대본_문법.md` **§2-A** 가 롱폼 계승을 못박았다.
`lf02` 는 그 규격이 생기기 **전날**(2026-08-09) 만들어졌다.

🔴 **업로드 여부 확인 완료 = 미업로드.** 재렌더해도 잃는 것이 없다(사용자 확인 2026-08-10).

---

## 1. 🔴 사용자가 정한 범위 = **B′**

| | 내용 |
|---|---|
| ✅ **자막 114줄 전부 재작성** | 새 규격(`ADR-V25-13`~`15` + §2-A wide 조정 둘)으로 |
| ✅ **화면 라벨 전면 한국어화** | `ADR-V-10` 개정 이행. 선택사항이 아니다 |
| ✅ **그림 21종 구조는 살린다** | 라벨 언어만 바꾼다. `reads` 가 이미 통과한 19종은 구조를 건드리지 않는다 |
| 🔴 **그림 2종만 재설계** | `ledger`(S5) · `identity`(S6) — 라벨 가림 시험 미달 (§4) |
| ✅ **클립 5종 재촬영 없음** | 클립 샷 자막 10줄이 이미 전부 쉬운 낱말이다 (§5) |
| 🔴 **영어 자막(`lines[].en`) 은 0줄 유지** | **사용자 결정 2026-08-10**: *"롱폼에서는 일단 영어 자막을 따로 만들지 않기로 하자. 해외 시청자 데이터가 모이면 그때부터 만들어도 늦지않으니깐."* → `youtube.en` 도 안 만든다. `check.mjs` 는 en 이 **0줄이면 통째로 건너뛰고 반만 채우면 fail** 이다 |
| ✅ **챕터 7개 구성 유지** | 사고 / 증명 / 명세 / 경계 / 실수 / 기억 / 지금 |
| ✅ **효과음 11개 · 앵비언트 구조 유지** | 자막 인덱스가 바뀌면 `sfx[].at` 을 따라 옮긴다 |

---

## 2. 🔬 코디네이터 측정 ① — 자막 114줄 중 **50줄(44%)** 이 벽이다

**훑은 경로**: `episodes/lf02/scene.json` 의 `shots[].lines[].text` 114줄 전수.
챕터 길이는 `build/timed.json` 의 `lines[].dur + pause + SHOT_TAIL 0.35` 합.

| 챕터 | 길이 | 벽인 줄 | 비율 | 무너지는 낱말 |
|---|---:|---:|---:|---|
| 사고 | 57.0s | 3/12 | 25% | 액션 · 카탈로그 |
| 증명 | 61.0s | 7/13 | 54% | 커밋 · 저장소 · C#/스크립트 · 에셋 · 실행명세서 · 에디터 · 게이트 |
| **명세** | 72.4s | **10/15** | **67%** 🔴 | 명세 · 선행 · 빌드 · 메모리 · 토글 · **안티패턴** · **기억 기반 설계** · 「한 건짜리」 |
| 경계 | 91.4s | 6/18 | 33% | 구현 · **문자열 스위치** · **갓 클래스** · 트레이드오프 · 「밟혔다」 |
| **실수** | 111.6s | **12/24** | 50% 🔴 | **대수적 항등식** · **단조성** · **정의 대조** · 순액/총액 · **BOM** · **YAML** · 셸 명령 · 게이트 |
| 기억 | 90.2s | 10/18 | 56% | 세션 · 커밋 · 저장소 · 에셋 · 프롬프트 · .cs |
| 지금 | 66.9s | 2/14 | **14%** ✅ | 스프라이트 |

### 🔑 벽이 두 종류다 — 처방이 다르다

**(a) 반복형 6종** — 등장 줄수: `명세` 10 · `커밋` 6 · `게이트` 5 · `세션` 5 · `저장소` 3 · `에셋` 3.
→ **§2-A wide 조정 ①**(처음 한 번만 풀고 그 뒤로 그대로)이 정확히 이걸 위한 것이다.
**여섯 번 풀어쓰기를 심으면 벽의 대부분이 한꺼번에 풀린다.** 매번 풀면 아는 사람에게 지루하다.
🔴 **낱말마다 「어디서 풀었는가」를 `notes` 에 적어라** — 검수가 그 자리를 연다.

**(b) 치명형** — 실수 챕터(`대수적 항등식`·`단조성`·`정의 대조`·`BOM`·`YAML`)와
경계 챕터(`갓 클래스`·`문자열 스위치`), 명세 챕터(`안티패턴`·`기억 기반 설계`).
→ ①로는 안 된다. **②비유가 필수**인 자리다. 🔴 비유는 **그 챕터 안에서 닫아라**(조정 ②).

### 🟢 이미 잘 된 자리 — 지우지 마라

- **L7** 「거리를 **어림잡던** 값이 5분의 1로 쪼그라들었습니다」 — 「휴리스틱」을 원문 재료로 푼 ①이다.
- **L14→L15** 「목표만 주면 스스로 계획을 세워요」 → 「**GOAP**라는 기법이에요」 —
  낱말을 **말하기 전에** 풀어 뒀다. 고유 기술명 처리의 모범이다.
- **L64~L66** 「하루에 20%씩 줄면 5일 뒤에 사라진다고 적었는데 / 곱셈이라 5일 뒤에 32가 남고」 —
  감쇠 산식을 산수로 푼 ①이다.
- **지금 챕터(14%)** 는 거의 손댈 게 없다. 🔴 단 **사실이 낡았다** — §6.

### 🔴 배정 없이 남기면 반려다 (`ADR-V25-13`)

위 표의 낱말 **전수**를 ①풀어쓰기 / ②비유 / ③버리기에 배정하고 `notes` 에 적어라.
🟢 **wide 는 550초가 있어 ③을 먼저 집을 이유가 쇼츠보다 훨씬 적다.**
🔴 **고유명사·게임 안 사물(주민·목수·창고·밭·집·모닥불·무덤)은 어려운 낱말이 아니다** — 손대지 마라.

---

## 3. 🔬 코디네이터 측정 ② — 🔴 화면 글자가 **전부 영어**다

`ADR-V-10` 은 2026-08-10 에 뒤집혔는데 `lf02` 는 **전날** 만들어졌다.

**훑은 경로**: `kinds/*.js` 21개 파일 → 주석 제거 후 문자열 리터럴 전수 + `fillText`/`strokeText` 1번째 인자 추출.

| | 영어 | 한국어 |
|---|---:|---:|
| `fillText`/`strokeText` **직접 리터럴** | **57** | **0** |
| 주석 제거 후 **전체 문자열 리터럴** | **106** | 49 |

🔴 **`costfloor` · `ledger` · `specfirst` 3종은 한국어가 0개다.**

회차 `notes["화면 라벨의 언어"]` 가 **옛 잣대**(「코드나 인스펙터에 그대로 있을 법한 말인가」)를
근거로 적혀 있다. **그 잣대는 폐기됐다.** 새 잣대 한 줄:

> **"이걸 한국어로 옮기면 뜻이 상하나?"** → 안 상하면 **한국어**.

**영어로 남는 것은 셋뿐**:
① 코드·로그를 **그 문자열 그대로 인용**하는 자리 — `NoSolutionFound` · `TendInjured` ·
   `SeasonSO`·`DisasterSO`·`ThreatSO`·`CookMeal.asset` · `CLAUDE.md` · `.cs` ·
   `M0-T1`·`M10-T2` · `spec-write`/`spec-implement`/`spec-review` · `devlog`
② **기호·단위** — `%` · `→` · `x4`
③ 옮기면 뜻이 무너지는 **고유 기술명** — `GOAP` · `Claude Code` · `Unity` · `YAML` · `BOM`

**나머지는 전부 번역이었지 인용이 아니다** — 아래는 반드시 한국어로 간다(전수):

| kind | 지금 (영어) |
|---|---|
| `amnesia` | SESSION · DEPTH |
| `antipattern` | KEY MAP · ANTI-PATTERN · FREE · EDITOR ONLY · SPEED · WANDERER |
| `blindspot` | ONE SCREENSHOT · PANEL · LIST · CHECKED · ALREADY THERE |
| `bomrecover` | ASSET YAML x4 · HUMAN ATTENTION · EDITMODE GATE · WIDEN THE GATE |
| `bothways` | HUMAN · AI · HOLE |
| `costfloor` | ACTION COST · MIN COST · GATE · PLAN · TEST FAILS FIRST |
| `decay` | WINTER 4 · DAY · BLOCKERS / AI |
| `falsetest` | ALWAYS TRUE · CAN FAIL · PASS · FAIL · WRITTEN 3 TIMES |
| `handoff` | HUMAN · REPO · NEXT SESSIONS · CHANNEL ENTRANCE |
| `handraise` | IMPLEMENT · STOP · OPTIONS + 1 RECOMMENDATION · DECISION |
| `humanonly` | STANDARD FLOW · HUMAN · CAUGHT BY PLAY / ONE DAY |
| `identity` | IDENTITY · TOP · TOP VALUE · source |
| `ledger` | SO ASSETS · SPECS · COMMITS game/repo · DAYS · C# SCRIPTS · files · lines · EDITMODE GATES |
| `mechanisms` | THINKING MECHANISMS · HOW TO JUDGE = EMPTY · EVIDENCE COMMIT |
| `nowline` | NOW |
| `premise` | DESIGN · PREMISE x7 · ONE WRONG PREMISE -> EVERYTHING ABOVE FALLS |
| `reader` | METHODOLOGY (how to judge) · (what to obey) WINS · NOT THE READER · SESSION |
| `roadmap` | QUESTIONS |
| `specfirst` | SPEC · DOES · DOES NOT · 1 ITEM · COMMIT · BUILD GREEN AT EVERY COMMIT · W1~W3 |
| `thirdway` | DIAGNOSIS · CODE (session log) |
| `zerolines` | CODE WRITTEN · CODE ADDED · ASSET FILE · CONTENT · LINES · zerocode |

🔴 **`M1 - M20`(mechanisms)은 지금 20개가 아니다** — §6 을 볼 것.

---

## 4. 🔬 코디네이터 측정 ③ — `ADR-V25-15` 라벨 가림 시험

**잣대**: `reads` 칸의 문장에서 **라벨 낱말을 지웠을 때도 사건이 남는가?**

🟢 **캔버스 21종 중 19종은 통과한다.** 예:
- `S2 costfloor` — *"값 1짜리 카드가 끼어들자 바닥 기준선이 그 카드까지 끌려 내려가고, 오른쪽 계획 칸이 차례로 비어 버린다"* → 라벨 없이도 **사건이 남는다** ✅
- `S8 premise` — *"일곱 칸을 하나씩 열어 보고 그중 두 칸이 뒤집혀 X 가 된다 — 그 두 칸 위에 얹혀 있던 층이 함께 기운다"* ✅
- `S19 costfloor` — *"기준선 자리에 굵은 문이 서 있고, 값 1짜리 카드가 내려오다 그 문에 튕겨 나간다"* ✅

🔴 **미달 2종 — 이것만 새로 설계한다:**

| 샷 | kind | 지금 `reads` | 왜 미달인가 |
|---|---|---|---|
| **S5** | `ledger` | *"수치 칸이 위에서부터 한 줄씩 채워지고, 마지막 게이트 칸만 강조색으로 잠긴다"* | 라벨을 지우면 **「칸이 채워지고 마지막 칸이 잠긴다」**만 남는다. 사건이 없다 — 글자가 그림 행세를 한다 |
| **S6** | `identity` | *"규칙 문서의 몇 번째 줄에 무엇이 박혀 있는지가 행 번호 태그(04·05)와 함께 새겨지고…"* | 인용문 자체가 내용이라 **라벨을 지우면 아무것도 안 남는다** |

💡 방향 제안(강제 아님): `ledger` 는 **수치 나열**이 아니라 **46일→47일 동안 무언가가 쌓여 가는
움직임**이 사건이 되게. `identity` 는 인용 두 줄의 **위아래 순서 자체**(재미가 기술 위에 있다)가
사건이 되게. 🔑 둘 다 **라벨을 가려도 「무엇이 쌓였다」·「무엇이 무엇 위에 섰다」가 남아야** 한다.

---

## 5. 🟢 클립 5개 — 재촬영 없음, 자막도 손댈 필요 없음

| 샷 | 클립 id | 창 | 자막 | 자막 예산(창−0.35−0.2) |
|---|---|---:|---:|---:|
| S1 | `built-260809-0328-08` | 7.00s | 2줄 | **6.45s** |
| S4 | `forecast-260809-0424-58` | 13.00s | 3줄 | **12.45s** |
| S12 | `disaster-260809-0424-65` | 7.00s | 2줄 | **6.45s** |
| S17 | `death-260809-0424-162` | 7.00s | 2줄 | **6.45s** |
| S25 | `broll-260809-0424-178` | 7.00s | 2줄 | **6.45s** |

🔑 **클립 샷 자막 10줄은 이미 전부 쉬운 낱말이다** (늑대·무덤·마을·나무꾼 같은 일상어).
→ 🔴 **v1 지뢰 3(S1 실제 여유가 +0.39 가 아니라 +0.041초 = 1.2프레임)을 안 밟는다.**
**클립 샷 자막을 늘리면 그 여유를 정확히 먹는다.** 꼭 고쳐야 할 이유가 생기면
**반드시 예산을 다시 계산하고 그 계산을 `notes` 에 적어라.**

🔴 **S25 는 창을 늘리지 마라** — v1 지뢰 6: 18초본으로 늘렸더니 7.4초부터 게임 날짜가
Day 28 로 넘어가 자막(「27일째」)을 화면이 반박했다.

---

## 6. 🔴 지뢰 4 이행 — 화면의 모든 수를 재측정했다

> 🔴🔴 **2026-08-10 14:40 개정 — 기준 시각을 아래 커밋으로 못박는다. 더 이상 갱신하지 않는다.**
>
> **저장소가 이 작업 중에도 자라고 있다.** 다른 세션이 M25 를 구현 중이라
> 14:04 → 14:25 사이에만 커밋 7건이 들어왔고, 기획팀이 14:11 에 다시 쟀을 때
> 여덟 값 중 **넷이 또 움직였다.** 렌더 직전에 다시 재도 렌더가 끝나기 전에 또 낡는다 —
> **쫓아갈 수 없는 종류의 드리프트다.**
>
> 🔑 **v1 이 실패한 것은 「낡아서」가 아니라 「선언한 시각과 화면값이 안 맞아서」다**
> (검수 C13: 기준 시각 16:20 을 선언하고 16:18 값을 화면에 뒀다).
> **선언한 시각의 값이 그 시각에 정확하면 그 수는 영구히 옳다.** 그래서 얼리고 선언한다.
>
> ## 🧊 기준 시각 = **2026-08-10 14:11:52 · `03d0c17df9fdbbf0c7fc617926553c3d8ff74b1f`**
>
> 🔴 **이 커밋에서 잰 값만 화면에 올린다. 더 최신 값을 봤더라도 쓰지 마라.**
> 🔴 **`youtube.blurb` 와 화면에 이 기준 시각을 명시한다.**
> 🔴 **코디네이터가 이 커밋에서 여덟 값을 전부 직접 재검산했다**(기획팀 보고를 믿지 않고 —
> §9 규칙). 아래 표의 「지금」 열이 그 결과다.

(v1 기준 시각 = 2026-08-09 16:20 · `f85e00de`)

명령(v1 검수가 쓴 것과 동일):
```
git log --reverse --format=%ad                       # 첫 커밋
git rev-list --count HEAD                            # 저장소 전체
git rev-list --count HEAD -- . ':(exclude)tools' ':(exclude)youtube-editor' ':(exclude).claude'
git ls-tree -r HEAD Assets/M0Config | grep -c '\.asset$'
git -c core.quotepath=false ls-tree -r --name-only HEAD Docs | grep -c '실행명세서'
git ls-tree -r --name-only HEAD Assets/Scripts | grep '\.cs$'   # 수 + 각 파일 wc -l 합
```

| 값 | v1 (8/9 16:20) | 🧊 **정본 (`03d0c17d` · 8/10 14:11:52)** | 자리 |
|---|---:|---:|---|
| **일수** (첫 커밋 `2026-06-24 23:07:54` 부터) | 46일 | **47일** | 🔴 제목 · `hud.title` · S1 L1 · S5 L16 · `ledger` |
| 저장소 전체 커밋 | 1,009 | **1,061** | S5 L17 · `ledger` |
| 게임 스코프 커밋 | 798 | **839** | S5 L17 · `ledger` |
| C# 스크립트 파일 | 116 | **125** | S5 L18 · `ledger` |
| C# 줄 | 20,365 | **22,478** | S5 L18 · `ledger` |
| 설정 에셋(`.asset`) | 116 | **121** | S5 L19 · `ledger` |
| 실행명세서 | 32 | **34** | S5 L19 · `ledger` |
| 에디터 테스트 게이트 | 386 | **433** | S5 L20 · `ledger` · `chapters.실수` · `youtube.blurb` |
| 방법론 메커니즘 | 20 | **20** (M1~M20 · 변동 없음, 다시 셈) | L89 「스무 개」 ✅ · `mechanisms.js` `M1 - M20` ✅ |

**재검산 명령** (전부 `03d0c17d` 트리에서):
```
git rev-list --count 03d0c17d                                      → 1061
git rev-list --count 03d0c17d -- . ':(exclude)tools' ':(exclude)youtube-editor' ':(exclude).claude'  → 839
git ls-tree -r 03d0c17d Assets/M0Config | grep -c '\.asset$'       → 121
git -c core.quotepath=false ls-tree -r --name-only 03d0c17d Docs | grep -c '실행명세서'  → 34
git ls-tree -r --name-only 03d0c17d Assets/Scripts | grep -c '\.cs$'  → 125
  (같은 목록 각 파일 git show | wc -l 합)                          → 22478
git show 03d0c17d:Docs/개발_방법론_명세서.md | grep -c '^### M[0-9]'  → 20
git show 03d0c17d:devlog/sessions/2026-08-10.md | grep '게이트 [0-9]*/[0-9]*' | tail -1
  → 1298행 「게이트 433/433 (results-20260810-123139.xml, 신규 3건)」
```
🔴 **기획팀은 게이트 근거로 `:1576`(`results-20260810-140715.xml`)을 인용했는데 그 행은
`03d0c17d` 트리에 없다**(더 뒤 커밋의 것이다). **값 433 은 같지만 근거 행은 `:1298` 이 맞다.**
— 이것이 §9 「각 층의 보고를 그대로 믿지 마라」가 이번에 값을 한 자리다.

🔴 **여덟 값이 전부 움직였다.** v1 검수가 남긴 교훈 그대로다 —
*"저장소는 같은 날 안에서도 자란다."* **「변동 없음」 자기보고 금지.**
🔴 **기준 시각을 `youtube.blurb` 에 명시해라**(v1 도 그렇게 했다).
🔴 **기준 시각을 바꾸면 여덟 자리를 동시에 고쳐야 한다** — `ledger` `ROWS` · S5 자막 `text` ·
S5 `say` · `chapters` 맵 · `youtube.title` · `youtube.blurb` · `hud.title` · `excluded`.

### 🔴 그리고 「지금」 챕터의 **사실이 낡았다**

| 줄 | v1 자막 | 지금 |
|---|---|---|
| L107 | 「지금은 마을에 **울타리와 문을 짓는 중**이고」 | 🔴 **M22 는 끝났다** (`Docs/M22_3차_망루함정_실행명세서.md` 완료 · 리뷰① 통과 2026-08-09) |
| L108 | 「주민이 하는 일이 몸짓으로 보이게 만드는 작업까지 끝냈습니다」 | ✅ 여전히 참 (M23 완료) |
| L109 | 「**늑대와 곰**은 아직 색 원 그대로예요. **스프라이트를 못 구했거든요.**」 | 🔴🔴 **거짓이 됐다.** M24 W1(`665f79cc`)이 늑대·곰을 **지웠다.** 지금 위협은 `Assets/M0Config/Threats/` 의 **고블린·오크·기사단·타락천사** 4종이고 **그림은 배선까지 끝났다**(W6 `83fea176`) |

🎁 **이건 손해가 아니라 재료다.** 이 영상의 제목이 약속한 것이 *"실제로 부러진 지점들"* 인데,
늑대가 사라진 전말이 **정확히 그것**이다 — 7라운드 그림 반려 → 조달 조사 → 유료 팩 13종에
**개과(犬科)가 0** → *"「늑대 유지」와 「화풍 유지」는 동시에 성립하지 않는다"* →
**그림이 기획을 바꿨다.** 🔴 1차 소스 = `Docs/종족침공축_브레인스토밍_기록_2026-08-09.md`
(커밋 `2b40f6a5`) · `Docs/M24_1차_종족침공축_실행명세서.md`.
🔴 **다만 「지금」 챕터는 14% 로 가장 깨끗한 챕터다** — 사실만 갱신하고 쉬운 말투는 그대로 살려라.

### 🔴 `mechanisms`(S21) 도 확인해라
자막 L89 「처음엔 여덟 개였고, 지금은 **스무 개**입니다」 + `kinds/mechanisms.js` 의 `M1 - M20`.
**`Docs/개발_방법론_명세서.md` 에서 지금 개수를 직접 세라.** 재측정 대상이다.

---

## 7. 🔴 `notes.비유` 칸이 없고, 표지 없는 비유가 이미 있다

`scene.json` 의 `notes` 키 15개 중 **`비유` 칸이 없다**(`ADR-V25-14` 계약 4 위반 — 빈 칸 금지, 없으면 「없음」).

그리고 **L84** 「이전 대화를 기억하지 못하는, **매일 아침 기억을 잃고 출근하는 동료예요**」는
**표지 없는 비유**다(계약 1 위반). 「~같은 거예요」·「~라고 보면 돼요」가 없어서
**게임 안/실제의 사실 주장으로 읽힌다.**

🔑 이 비유 자체는 좋다 — 살리되 **표지를 붙이고 `notes.비유` 에 등재**해라.

**비유 계약 다섯**(`ADR-V25-14` + §2-A):
1. 비유임이 문장에 드러날 것 — 「~같은 거예요」·「~라고 보면 돼요」·「말하자면 ~」
2. **게임 안의 사실을 주장하지 말 것** — 「플래너는 길찾기 앱 같은 거예요」 ✓ / 「플래너가 지도를 켰어요」 ✗
3. **비유 안에 수치 금지**
4. 🔴 **`notes.비유` 에 전부 기재** — 「어느 줄 · 무엇을 무엇에 빗댔나 · 그 낱말이 왜 안 통하나」.
   없으면 「없음」. **이 칸이 검수가 그 줄을 「원문에 없는 문자열」 대조에서 빼는 유일한 근거다.**
5. **비유 그림은 자막이 그 비유를 말하는 동안만** 화면에
6. 🔴 **(wide 전용) 비유는 그 챕터 안에서 닫는다** — 10분짜리에서 끌면 시청자가 비유를 사실로
   기억한다. 챕터가 끝나면 원래 낱말로 돌아온다.

---

## 8. 🔴 v1 이 남긴 지뢰 — 같은 걸 두 번 밟지 마라

정본 = `memory/project_longform_track.md` 「lf02 가 남긴 것」 + `notes/verdict.md`.

1. 🔴 **「Claude Code로만」은 리포에 근거가 없다.** 발행 글 14편이 전부 「함께」라 쓰고
   사람 몫(방향 결정·Play 관측·에셋 조달·업로드)은 오히려 명시돼 있다. v1 은 「~로만」을 버리고
   그 경계 자체를 **챕터 3(경계)** 주제로 삼았다.
   🔴 **그래서 `hud` 문구가 lf01(「로만 만드는」)과 다르다 — 채널 통일 여부는 사용자 판단 대기다.
   재제작하면서 임의로 통일하지 마라.** 지금 값을 그대로 쓴다:
   `hud.aiHook` = `Claude Code와 함께 만드는 유니티 GOAP AI` ·
   `hud.outro` = `Claude Code와 함께 만드는 GOAP AI 마을 게임 개발일지`.
2. 🔴 **「전수 확인했다」가 세 라운드 연속 전수가 아니었다.** 원인 = 「겹치는가」만 보고
   「맞는가」를 안 봄. **렌더 후 검증은 네 기준으로**: ①겹침 ②가로선이 획을 먹는가
   ③리빌 중간 프레임 오독 ④값이 정본과 맞는가.
3. 🔴 **클립 창 여유는 발화 합이 아니라 `SHOT_TAIL 0.35` 를 더한 샷 길이다** — §5.
4. 🔴 **기준 시각 수치는 그 시각 트리에서 전수 재측정** — §6 (이행 완료).
5. 정적 경고는 창 문제가 아닐 수 있다 — 판정식을 복제해 원본 클립을 따로 재면 갈린다.
6. 🔴 **재슬라이스가 항상 답은 아니다** — S25 18초본 사고. §5.
7. **산정 길이 = 실측의 82%** (lf02 450.2 → 550.5 = ×1.223 · lf01 ×1.219).
   길이를 미리 가늠할 때만 쓴다. 🔴 **판정은 `tts.mjs` 실측만이 정본이고 보정치를 새로 만들지 마라.**
8. 곁가지 둘:
   - 게임 HUD 의 움직이는 스프라이트가 엔진 제목과 겹친다 (S4 65.3~66.3s 실측).
   - 🔴 **`S8 premise` 의 세로 기둥이 라벨 줄 글자 사이 공백에 아슬하게 떨어져 있다 —
     문자열이 한 글자만 바뀌면 획을 먹는다.** ← **자막·라벨을 고치는 이번 작업이 정확히 그 조건이다.**

---

## 9. 파이프라인에 걸린 규칙 (어기면 반려)

- 🔴 **원문에 없는 문자열 금지.** 가장 무거운 반려 사유다(`ep02s` 두 번 · `ep13s-1` 한 번).
  **비유만이 예외**이고 §7 계약을 지켜야 한다.
- 🔴 **그림 돌려막기 금지.** 그림은 회차가 소유한다. 다른 회차 kind 를 복사하지 마라.
- 🔴 **화면 글자는 한국어** (§3).
- 🔴 **소재 정본 사다리(ADR-LF-6)**: 발행 글 있는 구간은 `tools/blog-automation/published/`,
  없는 구간(M16~M24)은 `devlog/sessions/` · `Docs/` 실행명세서 · git 커밋.
  **모든 수치·사건 서술에 출처 파일을 명시**해 대조한다.
- 🔴 **모든 샷에 `reads`** · **모든 샷에 `source`** · **버린 것은 `excluded` 에 사유와 함께**.
- 🔴 **각 층의 보고를 그대로 믿지 마라.** 「전수」가 전수가 아니었던 사례가 **여섯 번**이고
  그중 하나는 코디네이터 것이었다. 🔑 **전수를 셀 때는 훑은 경로를 함께 적어라.**
- 🔴 **`format:"wide"` 회차이므로 쇼츠 규격(4단 고정·30~35초·인트로 고정 문안·예고 사슬)은
  적용되지 않는다.** 정본은 `Docs/롱폼_대본_문법.md` 다.

---

## 9-A. 🔴 클립이 늙었다 — 코디네이터 판단 (2026-08-10 14:40)

기획팀이 새로 잡은 위험(`brief-v2.md` §D-5)을 코디네이터가 실물로 확인했다.

**확인한 것**:
- `engine/engine.js:300` 이 **모든 클립에 `지금 빌드 화면` 배지를 자동으로 단다**
  (`ADR-LF-7` 사실 정직성 — **대본이 끌 수 없다**).
- 늑대·곰은 **2026-08-10 02:57 커밋 `665f79cc`**(M24 W1)에서 지워졌다.
- 🔴 **클립 라이브러리 226개가 전부 `260809` 촬영이다**
  (`D:/AI_GOAP-videos/clips/library/library.json` 파싱 — 촬영일 분포 `{"260809":226}`).
  → **새 종족이 찍힌 클립은 하나도 없다.**

**따라서 S4 는 이런 상태다**: 화면에 「외로운 늑대 — 2일 뒤」가 떠 있고, 엔진 배지가
「지금 빌드 화면」이라 말하고, 자막 L13 이 「이게 **지금** 빌드예요」라고 못을 박는다.
**지금 빌드에 늑대는 없다.**

### 판단 = **영상이 스스로 밝힌다** (기획팀 권고 채택)

세 선택지를 재 봤다:
| 안 | 비용 | 판정 |
|---|---|---|
| 재촬영 | 촬영 세션 필요 · 게임 트랙이 M25 구현 중 | 🔴 **트랙 경계 침범**([[feedback_track_boundary]]) |
| 배지 문구 변경 | `engine.js` 수정 → lf01 까지 바뀜 | 🔴 `ADR-LF-7` 이 의도한 장치를 무르게 하는 것 |
| **자막이 밝힌다** | 자막 한두 줄 | ✅ **채택** |

🎁 **이건 손실을 메우는 것이 아니라 이 영상의 주제를 이행하는 것이다.** 제목이 약속한 것이
*"실제로 부러진 지점들"* 인데, **영상 자신의 화면이 만드는 사이에 낡은 것**이 바로 그 실물이다.
그리고 「지금」 챕터의 늑대 폐기 전말(§6)로 **정확히 갚아진다** — 열린 고리가 하나 더 생긴다.

🔴 **작성팀에게 거는 조건**:
- 배지를 **부정하지 말고 발판으로 써라.** 배지는 계속 「지금 빌드 화면」이라고 뜬다.
- 🔴 **비유가 아니다 — 사실 서술이다.** `notes.비유` 에 넣지 마라. 대신 `source` 에
  `665f79cc` 와 클립 촬영일(`260809`)을 근거로 달아라.
- 🔴 **클립 샷 자막을 늘리면 창 예산을 먹는다** — S4 예산은 12.45초로 여유가 크지만
  (§5) **반드시 다시 계산하고 그 계산을 `notes` 에 적어라.**
- 🔴 **여기서 다 설명하지 마라.** 증명 챕터는 「진짜 만들어지나」가 질문이다.
  전말은 「지금」 챕터가 갚는다 — **열린 고리로 남겨라.**

---

## 10. 코디네이터가 돌리는 것 (에이전트는 못 돌린다)

```
node tools/scene-video/tts.mjs lf02
node tools/scene-video/check.mjs lf02
node tools/scene-video/publish.mjs lf02 --prepare --force
```
🔴 `--force` 는 `uploads.json` 에 이미 `--prepare` 기록이 있어서 필요하다.
🔴 **업로드는 사람 몫이다.**
