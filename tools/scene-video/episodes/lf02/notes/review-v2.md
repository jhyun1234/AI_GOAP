# lf02 v2 검수 보고서 — 🔴 반려 1건

*검수 2026-08-10 · 검수팀(scene-reviewer). 판정 정본 = `Docs/롱폼_대본_문법.md`(`format:"wide"`).
🔴 작성팀·기획팀·코디네이터의 자기보고를 근거로 쓰지 않았다 — `check.mjs` 직접 재실행,
`03d0c17d` 트리에서 여덟 값 직접 재검산, `kinds/*.js` 21개 문자열 재추출, `timed.json` 으로
cue 산수 재계산, **엔진 스틸 24장 직접 렌더**, 1차 소스 `git show`·`grep -n` 직접 실행.*

---

## 판정: 🔴 **반려 (REJECTED) — 반려 사유 1건 · 정정 6건**

기계 게이트·구조·길이·수치·비유 계약·라벨 한국어화·라벨 가림 시험은 **전부 통과**했다.
특히 §6 이 요구한 여덟 값을 내가 `03d0c17d` 트리에서 다시 세었고 **여덟 다 소수점까지 맞았다.**

반려는 **한 자리**다 — S23/4 자막의 「게임 코드는 0줄」이 **정본이 그 수를 붙여 둔 사건과 다른
사건에 붙어 있다.** 1차 소스가 그 작업을 「코드 **14파일**」로 세고 있다.

> 🔑 이건 `ep11s` 「목수가 품삯 주러 쫓아가요」와 같은 모양이다 — 낱말(0줄)은 원문에 실재하는데
> **주어와 사건이 어긋났다.** v1 이 마스터에게 잡힌 M-R1 도 같은 종류였다(화면 수치의 귀속).

🔑 **반려 횟수 계산 주의**: v1 은 누적 3회(검수 1·2 + 마스터)에 도달해 사람에게 넘어갔고,
사용자가 **새 규격(`ADR-V25-13`~`15`)으로 재제작**을 지시해 v2 가 열렸다. 이 보고서는
**v2 의 1차 반려**다. v1 누적을 이어 세는 것이 맞는지는 코디네이터 판단 몫으로 남긴다.

---

## 1. 🔴 반려 사유 — R1

### R1. S23/4 「위협 넷을 통째로 갈아 끼울 때도 게임 코드는 0줄이었습니다」 — 정본에 없는 귀속

**자막 원문** (`scene.json` S23 / 4번 줄 · 전체 L103):

```
얼마 전에 위협 넷을 통째로 갈아 끼울 때도
게임 코드는 0줄이었습니다.
```

**작성팀이 `source` 에 단 근거** (S23 `source`):
> *"M24 1차 W1 이 종족 넷을 **기존 필드만으로** 만들었고(§W1 대상 파일 — "기존 필드만 사용"),
> W6 그림 배선의 DoD 가 **"게임 코드 diff 0줄 — 새 파일은 도구와 에셋뿐"**(§W6, 커밋 `83fea176`)"*

**내가 직접 잰 것**

① 「0줄」이라는 문자열은 **W6(그림 배선)** 의 DoD 다. W1(교체)의 DoD 에는 없다.

```
$ git log -1 --format='%s' 83fea176
feat(goap): M24-1차 W6 — 종족 4종 그림 배선 (색 원 졸업)
   본문: "게임 코드 diff 0줄 — 새로 생긴 것은 도구와 에셋뿐이다."     ← 이 사건은 「그림 배선」
$ git log -1 --format='%s' 665f79cc
refactor(goap): M24-1차 W1 — 늑대·곰 → 종족 4종 교체                  ← 자막이 말하는 사건
```

② **정본이 W1 을 「코드 14파일」로 세고 있다.**

```
Docs/M24_1차_종족침공축_실행명세서.md:235~236
  에셋 6개(Threat_Tier1_Wolf·Tier2_Pack·Tier3_Bear·WolfSprites·BearSprites·
  BuildFence.asset 문구) + 코드 **14파일**. 파일 목록은 W1 DoD에 전수 기재.
```
같은 문서 §W1 「대상 파일」은 그 14파일을 「문구 수정」으로, 그리고 그 아래에
**「게이트 일반화: `M21_CombatGates.cs`(:301·:546·:686·:695) · `M22_DefenseGates.cs`(:245·:669·:808)」**
7건을 따로 적어 둔다. 커밋 본문도 *"부수 수확 — 게이트가 에셋을 **이름으로** 붙잡고 있던 7곳을
일반화했다"* 라고 쓴다. **교체는 공짜가 아니었다.**

③ `git show --numstat 665f79cc` 실측:

```
Assets/Scripts/M0/**            13파일   42 +  / 39 −
Assets/Tests/EditMode/**         2파일   91 +  / 40 −   ← Tier1Threat() 헬퍼 신설 · M21_T11 재작성
```

**작성팀에게 공정하게 적는다 — 실질은 참이다.** 내가
`git show 665f79cc -- 'Assets/Scripts/M0/*'` 를 주석·`[Tooltip]` 인자를 걸러 다시 훑었더니
**실행되는 문장은 단 한 줄도 안 바뀌었다** (전부 「늑대」→「위협」 문구 치환).
그러니 **「게임 로직은 한 줄도 안 고쳤다」는 참이고**, 「게임 코드는 0줄」은 **거짓**이다.
어느 축으로 재도 0 이 안 나온다: Scripts 42줄 · Tests 91줄 · 정본 자신의 셈 14파일.

**왜 반려인가**
- 문법 §5-3: *"모든 수치·사건 서술에 출처 파일을 명시해 대조한다 — 출처가 안 달린 단언은 반려."*
  출처는 달렸는데 **그 출처가 다른 사건의 것**이다.
- 검수 규율 2026-08-08 신설: *"낱말 실재만으로 통과시키지 마세요 — 주어와 방향까지 원문과
  맞춰야 합니다."* 여기서 어긋난 것은 방향이 아니라 **사건**이다.
- 마스터가 `git show --stat 665f79cc` 한 줄이면 잡는 자리다. v1 의 M-R1 이 정확히 그렇게 잡혔다.

**처방 (작성팀 선택 · 둘 다 자막 글자 수가 늘지 않는다)**

| 안 | 문장 | 근거 |
|---|---|---|
| **①** (권장) | 「얼마 전에 위협 넷을 통째로 갈아 끼울 때도 / **돌아가는 코드는 한 줄도 안 고쳤어요.**」 | 내 실측 — `Assets/Scripts/M0/**` 변경분 42줄이 전부 주석·툴팁 문구다. `source` 에 이 축(「주석·`[Tooltip]` 제외 시 0」)을 반드시 적을 것 |
| ② | 「거기에 **그림을 붙일 때도** / 게임 코드는 0줄이었습니다.」 | `83fea176` DoD 문자열 그대로. 다만 이 편의 논지(위협 추가 = 에셋 하나)와 약간 어긋난다 |

🔴 어느 쪽이든 **`source` 의 근거 문장도 함께 고쳐라.** 지금은 W1 의 「기존 필드만 사용」과
W6 의 「0줄」을 한 문장으로 이어 붙여 둬서, 다음 검수가 같은 자리에서 또 헛돈다.

---

## 2. 🟡 정정 6건 (반려 아님 · 렌더 전에 닫을 것)

### C1. `nowline.js` 의 「천사」 — 사유(폭)가 실측으로 깨진다

`kinds/nowline.js:23` `const RACES = ['고블린', '오크', '기사단', '천사'];`
작성 노트 §5-3: *"화면 표식 라벨은 **폭 때문에** 「천사」로 줄였고"*

**실측** (`build/stills/557s.png` 확대 판독):
- 라벨 `고블린 · 오크 · 기사단 · 천사` 실제 폭 = **334 device px**
- 그 칸의 허용 폭(`cw − 16`) = **497 device px** (칸 폭 544 − 16×1920/649 ≈ 47)
- 「타락」 두 글자 추가분 ≈ 66px → **400 < 497. 들어간다.**
  게다가 `nowline.js:161` 의 루프가 `ns` 를 8까지 줄일 수 있어 폭은 애초에 구속 조건이 아니었다.

정본은 `Assets/M0Config/Threats/Race_Demon.asset:15 DisplayName: "타락천사"` 이고
**자막은 「타락천사」**라고 부른다. 화면만 「천사」다 — 「타락」이 빠지면 적이 아니게 읽힌다.
👉 `'천사'` → `'타락천사'` 로 고치고 §5-3 의 사유 문장을 지워라.
(🟢 `고블린`·`오크`·`기사단` 은 `무리`·`약탈` 만 뗀 일관된 줄임이라 그대로 둬도 된다.)

### C2. S19 `reads` 「오른쪽 계획 칸은 하나도 비지 않는다」 — 샷의 첫 15.1초 동안 넷 다 X 다

`costfloor.js:165` `const kIn = P1 ? clamp((kPlan - i*0.14)/0.4) : 1;` · `kPlan = ease(cue(4))`.
cue(4) 창은 **샷 기준 14.53s ~ 18.85s** (`timed.json` 실측)이므로, S19 26.65초 중
**0 ~ 약 15.1초 동안 네 칸이 전부 X** 다 — S2 의 실패 그림과 같은 글리프다.

**증거**: `build/stills/405s.png` (샷 기준 16.53s · cue4=0.463) — 넷 중 **둘이 아직 X**.

🟢 설계 자체는 옳다고 본다(「부서진 상태를 이어받아 → 문이 서고 → 계획이 되살아난다」는
아크가 성립하고, 소리(latch)도 게이트가 서는 자리에 정확히 있다). **틀린 것은 `reads` 문장**이다.
👉 「…값 1짜리 카드가 문에 튕겨 되올라가고, **그제야 오른쪽 계획 칸이 하나씩 도로 찬다**」로 고쳐라.
검수는 `reads` 로 그림을 판정한다 — 끝 상태만 적으면 다음 라운드가 또 헛돈다.

### C3. `notes.길이` — 실측이 나왔는데 「아직 인용할 실측이 없다」로 남아 있다

지금 값: *"🔴 아직 인용할 실측이 없다 … 작성팀이 손으로 셈한 참고치는 **약 592초**"*
👉 `check.mjs` 출력 줄을 **그대로** 갈아 끼워라:

```
총 길이 (wide 참고 · 판정 아님) 589.5초 = 자막 580.1 + 꼬리 9.45 (본편 기준 300~600초)
```

🔴 그리고 **자체 셈 「약 592초」와 그 산정식(자당 135.9ms × …)을 지워라.** 산정식은
`check.mjs` 안에만 둔다 — 회차 여섯이 각자 다른 식을 적었고 여섯 다 틀렸다.
(참고로 이번 자체 셈은 592 vs 실측 589.5 = 0.4% 오차로 잘 맞았다. 그래도 규칙은 규칙이다.)

### C4. `thirdway.js` 「조건 분기 55곳」 — 자막이 쓰는 말과 다르다

자막 S11/2 는 「이 행동이 나무 자르기면, 하는 **갈림길**이 코드 55곳」으로 `문자열 스위치`를
①풀어썼는데, 화면(`build/stills/232s.png`)은 「**조건 분기** 55곳」이다.
`ADR-V-10` 은 통과한다(한국어다). 다만 **비개발자에게는 「조건 분기」가 「문자열 스위치」만큼
낯설다** — 같은 것을 두 이름으로 부르고, 화면 쪽이 더 어렵다.
👉 「**갈림길 55곳**」으로 통일 권고. (`ADR-V25-13` 은 자막 규격이라 강제는 아니다 — 🟡)

### C5. S5 `latch` 의 `why` 첫 문장이 그 시각에 없는 것을 지목한다

`why` = *"마지막 층(게이트 433)이 얹히고 **탑 꼭대기에 빗장이 걸리는** 자리"*
**cue 산수**: `k = (560/1000 + 0.15) / (0.15 + 0.7×4.576) = 0.212` → `ease = 0.116`.
`ledger.js:121` 의 빗장은 `kLast > 0.5` 에서야 뜬다 — **소리 시각에 빗장은 없다.**
🟢 같은 `why` 의 뒷문장(*"560ms 는 마지막 층이 열리기 시작하는 자리"*)은 **맞다**
(`ledger.js:70` 층은 `k > 0.02` 부터 그려진다). v1 마스터가 이 칸을 지적한 바로 그 자리이므로
👉 **첫 문장에서 「빗장」을 빼라.** 소리는 「마지막 층이 얹히기 시작하는 자리」다.

### C6. 작성 노트 §6 의 S4 여유 예상치가 실측과 0.32초 어긋난다

| 샷 | 노트 §6 예상 여유 | **실측 여유** (창 − 샷 길이) | 차 |
|---|---:|---:|---|
| S1 | +0.22s | **+0.326s** | +0.11 (보수적, 안전) |
| **S4** | **+0.77s** | **+0.446s** | **−0.32 (낙관)** |
| S12 | +0.572s | +0.572s | 0 |
| S17 | +0.288s | +0.288s | 0 |
| S25 | +0.445s | +0.445s | 0 |

전부 0 초과라 **결함은 아니다.** 다만 「자당 135.9ms」 환산이 자막을 새로 쓴 샷(S4)에서
낙관 쪽으로 0.32초 틀렸다는 사실을 §6 표에 적어 둬라 — 다음 회차가 이 상수를 그대로 믿는다.

---

## 3. 축별 판정 — 내가 다시 센 것과 훑은 경로

### 축 1. `ADR-V25-13` 전수 배정 — ✅ 통과 (내가 다시 셌다)

**훑은 경로**: `node -e` 로 `scene.json` `shots[].lines[].text` **122줄 전수 출력** →
샷/줄 번호를 붙여 한 줄씩 육안 판정 (이 보고서 작성 중 전량 출력 · 위 R1 도 그 과정에서 나왔다).
추가로 `remake-input.md §2` 의 낱말 17개에 대해 `String.includes` 전수 매칭을 돌렸다.

**33개 산수 재검산 (일치)**: 표 원소 2+7+8+5+8+6+1 = 37, 챕터 간 중복 4(게이트·커밋·저장소·에셋)
→ **33**. 갈래 합 ①23 + ②2 + ③8 = **33**. ✅ 작성팀 셈이 맞다.

**낱말 실재 재검증 (`includes` 전수)**:
```
커밋   (4) S5/0, S7/3, S21/1, S21/3        저장소 (3) S5/1, S22/0, S27/2
게이트 (5) S5/4, S16/4, S18/3, S18/4, S19/3  명세  (7) S7/1,S7/3,S8/0,S8/1,S9/4,S10/1,S16/1
세션   (5) S20/0, S20/2, S20/3, S21/4, S22/0
에셋 0 · 에디터 0 · 스프라이트 0 · 액션 0 · 카탈로그 0 · 빌드 0 · 토글 0 · 메모리 0 · YAML 0 · 안티패턴 0
```
③버리기 8개 중 **자막에 살아남은 낱말은 0개**다. 「버렸다」가 실제로 버려졌다. ✅

**③ 8개의 `excluded` 대조**: `정의 대조`(#1) · `YAML·참조`(#2) 둘만 `excluded` 에 있다.
🟢 **이것이 옳은 처리다** — 나머지 여섯(에셋·빌드·메모리·토글·트레이드오프·스프라이트)은
**사건이 아니라 낱말만** 버렸으므로(사건은 다른 말로 전부 살아 있다) `excluded`(소재 폐기)가
아니라 `notes` 가 맞는 자리다. `notes.쉬운 말 배정` 에 여섯 다 사유가 적혀 있다. ✅

**🟡 내가 추가로 잡은 경계 낱말 6종 (반려 아님 · 의견)**
`Play`(S13/1·S13/3 — 로마자다. 다만 S13/3 은 `Docs/CLAUDE.md:65` 직접 인용) ·
`명제`(S15/3) · `패널`(S24/2) · `모델`(S20/2 — 원문은 「같은 AI 모델」인데 「AI」가 빠졌다) ·
`사고법`(S22/2) · `반려`(S26/1).
전부 ①`remake-input §2` 의 벽 목록에 없고 ②일상어이거나 널리 쓰이는 외래어이며
③「게임을 안 만들어 본 사람이 아는가」에서 **명확히 떨어지지는 않는다.** 반려하지 않는다.
가장 신경 쓰이는 것은 `Play` 하나다 — 고칠 거면 S13/1 만 「직접 게임을 돌려 보는 검증」으로.

### 축 2. 비유 계약 다섯 — ✅ 통과 (역방향까지 확인)

| 계약 | 비유① 세션 (S20/1) | 비유② BOM (S18/1) |
|---|---|---|
| ①표지 | 「**말하자면**」+「**같은 존재**」 ✅ | 「**말하자면**」+「**거예요**」 ✅ |
| ②게임 안 사실 주장 | 없음(AI 얘기다) ✅ | 없음(파일·검사 얘기다) ✅ |
| ③비유 안 수치 | 없음 ✅ | 없음 ✅ — 「파일 넷」은 **다음 줄**(S18/2)이다 ✅ |
| ④`notes.비유` 등재 | ✅ 어느 줄·무엇을 무엇에·왜 안 통하나 3요소 전부 | ✅ 동일 |
| ⑤그림 동기화 | ✅ `amnesia.js` 는 비유를 안 그린다 — `fillText` 인자 전수 = `세션`·`#n`·`판단의 깊이`. 동료·아침·출근 **0건** | ✅ 코드 직접 확인, 아래 |
| ⑥(wide)챕터 안 닫힘 | ✅ 같은 샷에서 「세션마다」로 복귀 · S21·S22 는 「세션」만 | ✅ 다음 줄부터 사실 서술 |

**⑤ BOM 그림을 코드로 풀었다** (`bomrecover.js:78`):
`vis = clamp(kBom) * (1 - clamp(kWipe * 1.6))` · `kBom=ease(cue(1))` · `kWipe=ease(cue(2))`
`timed.json` 대입 → 표식은 **363.45s 에 뜨기 시작해 371.09s 에 완전히 사라진다**
(최대 알파 **1.000**, α≥0.6 구간 **4.56초**). 자막 L76(비유)은 363.60s 에 시작한다.
🟢 다음 줄(L77 「**그것** 때문에」)로 1.6초 물리지만, 그 줄이 여전히 BOM 을 가리키므로
계약 5 위반이 아니다. **증거 스틸 `build/stills/366s.png`** — 자막과 표식이 함께 떠 있다.

**🔴 역방향 확인 (목록에 없는 비유가 있는가)**: 122줄 전수를
`/말하자면|같은 거|같은 존재|라고 보면|처럼|듯이|마치|같아요|비유/` 로 훑었다. 히트 3건 중
등재 안 된 것은 **S14/2 「맨 앞의 그 사고**처럼**요」** 하나이고, 이건 **이 영상 안에서 실제로
벌어진 사건(S2)을 되받는 지시**이지 비유가 아니다. ✅ **미등재 비유 0건.**

**🔴 S20 비유가 「원문 복원」인지 직접 확인** — 작성팀 주장이 맞다:
```
$ grep -n "말하자면" tools/blog-automation/published/2026-07-24-...-spec-constellation.html
6: … 이전 대화를 기억하지 못하는, 말하자면 매일 아침 기억을 잃고 출근하는 동료 같은 존재죠.
```
🔑 **행 번호는 작성팀(6행)이 맞고 기획 브리프(11행)가 틀렸다.** 작성팀이 다시 셌다는 뜻이다.

**🔴 S4/1 「이 늑대, 지금은 게임에 없어요」가 비유가 아니라 사실인지** — 맞다. 직접 대조:
```
$ git log -1 --format='%H %ad %s' 665f79cc --date=iso
665f79cc… 2026-08-10 02:57:04 +0900  refactor(goap): M24-1차 W1 — 늑대·곰 → 종족 4종 교체
$ grep -rn "늑대\|Wolf\|Bear" Assets/Scripts Assets/M0Config   → 1건 (ThreatService.cs:1005 주석)
```
클립 촬영일 `260809` 도 `library.json` 촬영일 분포로 확인됨. **`notes.비유` 에 안 넣은 것이 옳다.** ✅

### 축 3. wide 전용 조정 둘 — ✅ 통과 (지도가 가리키는 자리를 전부 열었다)

**조정 ① 「어디서 풀었는가」 지도 — 6종 전부 그 자리를 열어 확인**

| 낱말 | 지도가 가리킨 자리 | 내가 연 결과 | 그 뒤 다시 푸는가 |
|---|---|---|---|
| 커밋 | S5/0 | 「작업 하나를 저장한 기록이 커밋인데,」 ✅ 풀이 실재 | S7/3·S21/1·S21/3 — **안 푼다** ✅ |
| 저장소 | S5/1 | 「**도구까지 다 든** 저장소 전체로는」 ✅ | S22/0(원문 인용)·S27/2 — 안 푼다 ✅ |
| 게이트 | S5/4 | 「**게임을 켜기 전에 도는 검사**, 게이트가」 ✅ | S16/4·S18/3·S18/4·S19/3 — 안 푼다 ✅ |
| 명세 | S7/1~2 | 「무엇을 어떤 순서로 넣을지, 무엇을 하지 않을지를 **미리 못 박아 두는 문서**」 ✅ | S7/3·S8·S9/4·S16/1 — 안 푼다 ✅ |
| 세션 | S20/0~1 | 「매번 새로운 세션으로 시작합니다」+비유 ✅ | S20/2~3·S21/4·S22/0 — 안 푼다 ✅ |
| 에셋 | (안 씀) | 자막·화면 어디에도 **0건** ✅ | — |

**🔴 「풀지 않은 채 처음 등장한 낱말」 확인** — 위 `includes` 전수의 **첫 히트가 곧 푼 자리**다
(커밋 S5/0 · 저장소 S5/1 · 게이트 S5/4 · 명세 S7/1 · 세션 S20/0). ✅ 선행 등장 0건.
화면 쪽도 확인했다: `costfloor` phase 0(S2)에서는 `kGate=0` 이라 「게이트」가 안 그려지고,
`ledger`(S5)는 「실행명세서」 대신 「계획 문서」를 쓴다 — **화면에서도 안 앞선다.** ✅

**🔴 「S5 에 다섯이 몰려서 셋만 풀고 둘을 미뤘다」는 처리 — 타당하다고 판정한다.**
근거: ①`§2-A` 조정 ①의 문언은 「**처음 나오는 자리에서** 한 번 풀고」인데, **에셋은 낱말을
아예 안 써서 「처음 나오는 자리」가 없어졌고**(규칙의 적용 대상이 사라진 것이지 위반이 아니다)
②명세는 S5 에서 **낱말 자체를 안 쓰고**(「계획 문서」) S7 첫 등장에서 풀었으므로 **조정 ①을
문자 그대로 지켰다.** 「미뤘다」는 작성팀의 표현이고 실제로는 **첫 등장 자체를 뒤로 옮긴 것**이다.
👉 **`§2-A` 개정은 불필요하다는 것이 내 의견이다.** 조정 ①은 「첫 등장에서 푼다」이지
「첫 언급 후보에서 푼다」가 아니므로, 낱말을 안 쓰거나 뒤로 옮기는 것은 이미 허용된 해법이다.
다만 문장이 그 점을 명시하지 않아 세 층이 모두 고민했으므로, 정말 손댄다면
**한 줄 각주**로 족하다: *"낱말을 쓰지 않거나 첫 등장을 뒤로 옮기는 것도 이 조정의 이행이다."*
🔴 **조항을 「한 샷에 N개까지」 같은 수치로 만들지는 마라** — 근거가 없다(ADR-V-7·11).

**조정 ② 비유가 챕터를 넘는가** — 122줄 전수 스캔(위 축 2)으로 확인.
「동료·아침·출근」 계열 문자열은 S20/1 **한 줄에만** 있고, 「안 보이는 글자」는 S18/1 **한 줄에만**
있다. 뒤 챕터에서 되살아나는 자리 **0건**. ✅

### 축 4. 화면 글자 한국어 — ✅ 통과 (내가 다시 셌다 · 작성팀 22 vs 내 24)

**훑은 경로**: `kinds/*.js` **21개 전수** → ①블록/라인 주석 제거 후 ②`fillText`/`strokeText`
1번째 인자 정규식 추출(58건) ③따옴표 문자열 리터럴 전량 분류(566건) ④백틱 템플릿 리터럴
별도 추출(3건 — `#${i+1}` · `최소 비용 ${…}` · `세션 #${i+1}`). 작성팀 축(fillText 1인자 +
라벨 배열)에 **템플릿 리터럴을 추가**해서 훑었다.

**결과**: 팔레트/API 문자열(`ink`·`accent`·`sub`·`track`·`canvas`·`alphabetic`·import 경로)을
제외하면 **화면에 실제로 뜨는 영어 = 24개**. 작성팀 보고 22 + **내가 더 찾은 2개**:

| 추가 발견 | 자리 | 판정 |
|---|---|---|
| `Y / N` | `antipattern.js:9` | ✅ **②기호** — 키보드에 새겨진 키. `1-9`·`?` 와 같은 부류인데 작성팀이 셈에서만 빠뜨렸다 |
| `C` | `antipattern.js:11` | ✅ **②기호** — 원문 M15 글 35행이 「C 키」라고 부른다 |

**영어 잔존 24개를 하나씩 「한국어로 옮기면 뜻이 상하나?」로 판정**

- **①인용 (17)** — 전부 통과. 내가 리포에서 실재를 확인한 것:
  - `spec-write`·`spec-implement`·`spec-review`·`devlog` → `Docs/개발_방법론_명세서.md:24` 등에
    그 문자열 그대로 있다(§4 표준 작업 흐름). ✅
  - `M0-T1`·`M10-T2` → `devlog/sessions/2026-08-05.md:65~66` *"기존 게이트 2개(M0-T1·M10-T2)가
    즉시 잡아냈다"* ✅
  - `NoSolutionFound` → `Docs/CLAUDE.md` ADR-M0-5 본문 ✅ (콘솔 로그 문자열)
  - `SeasonSO`·`DisasterSO`·`ThreatSO`·`CookMeal.asset` → `Docs/CLAUDE.md` 비용표 ✅
  - `Docs/CLAUDE.md`·`CLAUDE.md` 파일명 · `W1`~`W3` 명세 항목 번호 · `M1 - M20` 절 번호 ✅
- **②기호 (5)** — `A`·`B`·`C`(handraise 선택지 표식) + `Y / N`·`C`(키 이름).
  🔴 **지시서가 의심하라고 한 `A`·`B`·`C` 를 따로 봤다**: `handraise.js:6` 의 셋은
  **선택지에 붙는 번호**이고 `handraise.js:70` 의 본문 라벨은 「선택지 + 추천 하나」로
  **한국어다**. ①②③ 으로 바꿔도 되지만 **옮겨서 뜻이 상하지도, 살지도 않는다** —
  번호는 언어가 아니다. **통과.**
- **③고유 기술명 (2)** — `BOM` · `AI`.
  🔴 **`AI` 를 의심해 봤다.** 「인공지능」으로 옮기면 뜻은 안 상한다. 그러나 ①이 회차의
  **자막이 122줄 내내 「AI」라고 부르고**(S3/0·S9/4·S13/0·S14/1·S15/0 …) ②`hud.aiHook` 도
  「GOAP **AI**」다. 화면만 「인공지능」으로 바꾸면 **화면과 말이 다른 이름을 쓴다.**
  🔑 그리고 `ADR-V-10` 의 잣대는 「옮기면 뜻이 상하나」인데, 여기서 상하는 것은 뜻이 아니라
  **일관성**이다 — 그것도 반려 사유가 아니다. **통과.** (다만 분류는 ③보다 「고유명사」에 가깝다.)

**🔴 `notes["화면 라벨의 언어"]` 가 새 잣대로 다시 쓰였는가** — ✅ 다시 쓰였다.
첫 문장이 *"v1 이 쓰던 잣대(「코드나 인스펙터에 그대로 있을 법한 말인가」)는 **폐기됐다**"* 로
시작하고, 새 잣대 한 줄과 **개정 근거(해외 0.7%)** 까지 적혀 있다. 옛 잣대 잔존 **0건**.

**스틸로 실물 확인한 한국어 라벨** (24장 중): `행동 비용`·`계획`·`간호`·`최소 비용 8`·`게이트`·
`검사가 먼저 실패한다`(405s) / `설정 파일 4개`·`사람의 주의력`·`게이트 — 기계가 하는 검사`(366s) /
`전제 7개`·`설계`(160s) / `키 배정`·`배속`·`방랑자`·`에디터 전용`·`비어 있음`(192s) /
`사고법 — 어떻게 판단하나`·`세션 #n`·`수신인이 아니다`(466s) / `사람`·`저장소`·`다음 세션들`·
`판단 기준`·`채널 정문 영상`(586.3s) / `커밋 — 게임 파일 / 전부`·`코드 줄 · 125개 파일`(95s).
**깨진 글리프·폴백 이상 0건** (한국어가 전부 Pretendard 로 그려진다).

### 축 5. `ADR-V25-15` 라벨 가림 시험 — ✅ 27샷 전부 통과

**판정 방법**: `reads` 27개를 전량 출력해 **라벨 낱말을 지우고 남는 문장**을 만들었다.
🔑 **이 회차의 `reads` 는 처음부터 라벨 낱말을 안 쓴다** — 전부 도형·운동으로만 쓰여 있어
「지울 라벨」이 없다. 그래서 판정을 뒤집어, **스틸로 그 도형 서술이 실물과 맞는지** 확인했다.

**재설계 2종 — 둘 다 통과**

| 샷 | 미달이던 v1 | v2 `reads` (라벨 0개) | 스틸 확인 |
|---|---|---|---|
| **S5 `ledger`** | *"수치 칸이 채워지고 마지막 칸이 잠긴다"* → 사건 없음 | *"바닥선 위에 층이 하나씩 얹혀 다섯 층 탑이 되고, 다 쌓인 뒤 맨 위에 굵은 빗장이 걸린다"* | `95s.png` — 아래에서 위로 4층이 쌓여 있고 5층이 흐리게 내려앉는 중. **「쌓였다」가 라벨 없이 남는다** ✅ |
| **S6 `identity`** | 인용문 자체가 내용 → 가리면 0 | *"판 둘이 위아래로 서 있다가, 아래 있던 판이 좁아지며 옆으로 비켜 올라가 위쪽 자리를 차지한다"* | `118s.png`(04 위/05 아래) → `122.5s.png`(**05 위/04 아래로 뒤집힘**). **「아래 것이 위로 올라가 위에 섰다」가 남는다** ✅ |

🟢 `identity` 의 겹침 방지도 코드로 확인했다: `pwIn = (pw-20)*(1-0.52·sin(π·sw))` 이라
**두 판 폭의 합 ≤ pw-20** 이 항상 성립 → x 구간이 교집합을 가질 수 없다. 좌표가 아니라
부등식으로 막았다. `122.5s.png` 교차 후 프레임에서 겹침 0. ✅

**🔴 「나머지 19종이 한국어화로 미달로 떨어지지 않았는가」** — 스틸 판독으로 확인.
한국어 라벨이 영어보다 넓어져 새로 생긴 문제 **0건**. 폭 방어 세 자리도 실물에서 확인:
`costfloor` 의 `최소 비용 8`/`게이트` 가 계획 칸 열보다 앞에서 멈춘다(`405s.png`) ·
`blindspot` 머리 라벨 둘이 안 겹친다(`518s.png`) · `specfirst` 바닥 문구가 여백 안(`155.5s.png`).
🟢 `518s.png` 의 「겹친 글자」 이중 인쇄는 **결함이 아니라 이 샷의 소재 자체**다
(M15 글 「같은 스크린샷 안에 두 줄로 늘어난 목록과 **겹친 글자**가 이미 찍혀 있었다」).
다음 검수가 잘림으로 오독하지 않도록 적어 둔다.

### 축 6. 🧊 얼린 수치 — ✅ 여덟 값 전부 일치 (내가 `03d0c17d` 에서 직접 다시 셌다)

```
$ git log -1 --format='%H %ad' --date=iso 03d0c17d
  03d0c17df9fdbbf0c7fc617926553c3d8ff74b1f  2026-08-10 14:11:52 +0900   ✅ 선언한 시각과 일치
$ git log --reverse --format=%ad --date=iso | head -1   → 2026-06-24 23:07:54   → 47일 ✅
$ git rev-list --count 03d0c17d                                            → 1061  ✅
$ git rev-list --count 03d0c17d -- . ':(exclude)tools' ':(exclude)youtube-editor' ':(exclude).claude'
                                                                           →  839  ✅
$ git ls-tree -r --name-only 03d0c17d Assets/Scripts | grep -c '\.cs$'      →  125  ✅
$   (같은 목록 각 파일 git show | wc -l 합)                                 → 22478 ✅
$ git ls-tree -r 03d0c17d Assets/M0Config | grep -c '\.asset$'              →  121  ✅
$ git -c core.quotepath=false ls-tree -r --name-only 03d0c17d Docs | grep -c '실행명세서' → 34 ✅
$ git show 03d0c17d:Docs/개발_방법론_명세서.md | grep -c '^### M[0-9]'      →   20  ✅
$ git show 03d0c17d:devlog/sessions/2026-08-10.md | sed -n '1298p'
  → "검증: 게이트 433/433 (results-20260810-123139.xml, 신규 3건)."         →  433  ✅
```
🔑 **근거 행 1298 도 맞다** — 기획팀이 인용했던 `:1576` 은 이 트리에 없다는 코디네이터 지적이
사실이고, 작성팀이 `S5 source` 에 **1298 로 고쳐 적었다.** ✅

**🔴 `46` 전수 grep** — `scene.json` + `kinds/*.js` 전수:
```
scene.json  히트 3건 → note(「46일 → 47일」 개정 이력) · S1 source(같은 이력) · pauseAfter 460
kinds/*.js  히트 18건 → 전부 좌표/폭 상수 (cy=46 · laneH=46 · fillRect(...,46,...) 등)
```
**화면·자막·제목·`hud`·`chapters`·`blurb`·`ledger` 에 남은 「46일」 0건.** ✅
실물 확인: 렌더한 스틸 24장 전부에서 우상단 `hud.title` 이 「**Claude Code로 47일**」로 뜬다.
`thumbnail.kicker` 도 「Claude Code로 47일」 ✅.

**🔴 기준 시각이 `youtube.blurb` 에 명시됐는가** — ✅
> *"47일, 게임 파일 기준 839커밋, 게이트(게임을 켜기 전에 도는 검사) 433개 —
> **2026-08-10 14:11:52 커밋 03d0c17d 기준**으로 잰 값입니다. 저장소가 같은 날 안에서도
> 자라기 때문에 시각까지 밝힙니다."*

🟢 그리고 **화면에도** 적혀 있다 — `ledger.js:33` `2026-06-24  ->  2026-08-10 14:11 기준`
(`95s.png` 좌상단에서 판독 확인). remake-input §6 의 「화면 어딘가에도」 요구 이행 ✅.

### 축 7. 「지금」 챕터의 사실 갱신 — ✅ 통과 (1차 소스 직접 대조)

| 자막 | 1차 소스 | 판정 |
|---|---|---|
| S26/0 「울타리와 문이 서 있고, 주민이 하는 일도 몸짓으로 보입니다」 | `Docs/M22_3차_망루함정_실행명세서.md`(완료) · M23 완료 | ✅ |
| S26/1 「늑대 그림에서 막혔어요. **일곱 번**을 다시 그렸는데 전부 반려」 | `종족침공축_브레인스토밍_기록_2026-08-09.md:12` *"재작업 1회(**총 7라운드**) … **여전히 반려**"* | ✅ (🟡 아래) |
| S26/2 「사서 쓰려니 **화풍**이 안 맞았고, 쓰던 그림 묶음 **13종**엔 **개 종류**가 없었어요」 | 같은 문서 :13~14 *"통과한 둘은 **화풍 탈락**"* · :19 *"Kenmi 팩 **13종 전수** — **개과 0**"* | ✅ |
| S26/3 「늑대와 곰을 지웠습니다」 | `665f79cc` DoD `grep` 0건 — 내가 지금 트리에서 재실행: 히트 1건(`ThreatService.cs:1005` 주석뿐) | ✅ |
| S26/4 「고블린 무리, 오크 무리, 약탈 기사단, 타락천사」 | `Assets/M0Config/Threats/Race_*.asset:15 DisplayName` 4건 실측 — **네 문자열 그대로** | ✅ |
| S26/5 「그림을 못 그려서 적을 통째로 바꾼 겁니다」 | 같은 문서 :21 *"결론: 「늑대를 유지한다」와 「화풍을 지킨다」는 동시에 성립하지 않는다"* → :22 사용자 결정 | ✅ |

**🔴 지어낸 서사 검사** — 지시서가 콕 집은 「유료 팩 13종에 개과가 0」을 원문에서 찾았다:
`:19 "Kenmi 팩 **13종 전수** — **개과 0.** Dungeon = 환경 전용. Characters = 인간형만."` ✅ 실재.
🟢 자막은 **「유료 팩」이라 안 하고 「쓰던 그림 묶음」**이라 한다 — 원문이 「이미 쓰던 팩」이므로
자막 쪽이 더 정확하다. ✅

**🟢 「완성됐습니다」를 안 썼다** — 확인. S26 은 「지웠습니다」·「바꾼 겁니다」까지만 말한다.
`Docs/M24_1차_종족침공축_실행명세서.md:4` 는 *"W1~W10 + W7R 완료 · **사용자 Play 대기**"* 다. ✅
**M24 내부 용어(압력 예산제·침입 특성 축·밴드 3단 폐지·등급 4단)도 자막·화면 0건** ✅
(`excluded` #3 에 사유까지 적혀 있다).

🟡 **딱 하나 문구**: 「**일곱 번을 다시** 그렸는데」는 원문 「총 7라운드」를 「7회 재작업」으로
읽은 것이다. 원문은 「자작 1회 + 재작업(총 7라운드)」라 **재작업 횟수가 7인지 6인지 원문이
확정하지 않는다.** 수 7 은 원문에 실재하므로 반려하지 않는다. 안전하게 가려면
「**일곱 라운드를 돌렸는데**」가 원문에 더 가깝다. (선택 · 강제 아님)

### 축 8. 유지 조건 — ✅ 전부 통과

- 🔴 **`hud` 세 줄** — remake-input §8-1 지정값과 **글자까지 동일**:
  `title` 「Claude Code로 47일」(46→47 만 갱신 ✅) ·
  `aiHook` 「Claude Code**와 함께** 만드는 유니티 GOAP AI」 ·
  `outro` 「Claude Code**와 함께** 만드는 GOAP AI 마을 게임 개발일지」.
  임의 통일 **0건** ✅. 스틸 24장에서 실렌더도 확인.
- 🔴 **`lines[].en` / `youtube.en`** — `en` 이 붙은 줄 **0/122**, `youtube.en` **없음** ✅.
  `check.mjs` 도 「이 회차는 en 이 0줄이다 — 영어 자막 대상이 아닌 회차로 보고 건너뛴다」.
  🔑 **이 회차의 en 0줄은 사각지대가 아니라 사용자 결정**(remake-input §1 · `excluded` #15)이므로
  「번역을 안 했으니 반려」 규칙을 적용하지 않는다.
- 🔴 **`scene.en.json` 파일 부재** ✅ (`episodes/lf02/` 에 없다).
- 🔴 **`legacyFormat` 부재** ✅ (wide 회차라 애초에 무관).
- 🔴 **효과음 11개** — `at` 이 가리키는 줄을 **11개 전부 열어 봤다**. 전부 그 사건을 말하는 줄이다:
  S2/4 「1짜리가 들어오면서」→drop · S3/0 첫 질문→tick · S5/4 「게이트가 433개」→latch ·
  S8/3 「그중 둘이 틀렸습니다」→thud · S11/5 「제3의 길을 고른 건 사람」→latch ·
  S16/2 「곱셈이라 5일 뒤 32」→drop · S18/3 「게이트 2개가 즉시 잡아냈어요」→latch ·
  S19/3 「게이트를 하나 걸었습니다」→latch · S21/2 「여덟 → 스무」→riser ·
  S23/3 「재해도, 위협도 같습니다」→tick · S26/3 「늑대와 곰을 지웠습니다」→sweep. ✅
  **자막 인덱스 이동 넷(S2 3→4 · S11 4→5 · S18 2→3 · S26 1→3)이 전부 따라 옮겨져 있다** ✅
- 🔴 **효과음 돌려막기** — 리포의 **모든 회차 `scene.json` 을 파싱**해
  `(kind, dur, from, to, freq, seed)` 서명으로 대조했다. **lf02 의 11개 서명이 전부 유일**하고,
  다른 회차와 겹치는 것 **0건**, lf02 내부 중복도 **0건**.
  `latch` 넷은 seed 157/163/167/179 + dur 0.14~0.20 으로 갈렸고(latch 는 seed 를 읽는다),
  `drop` 둘은 **seed 를 안 읽는 kind** 라 `dur` 0.28 vs 0.22 로 갈랐다 ✅ ·
  `tick` 둘은 freq 1240 vs 1420 ✅.
  **gain 상한**: latch 0.062 < 0.071 ✅ · tick 0.056·0.060 < 0.065 ✅ · 나머지 0.060~0.062 ✅.
- 🔴 **`delayMs` 11개 cue 재산정** — `k=(delayMs/1000+0.15)/(0.15+0.7·dur)` 을 `timed.json`
  실측 `dur` 로 전수 계산했다. 결과 `ease` = **0.055 ~ 0.239** — 전부 「리빌 시작」 대역이고
  `why` 들이 스스로 선언한 대역과 맞는다. 🔑 v1 마스터가 잡았던 「임계 이후를 가리킨다」 병은
  해소됐다. **단 S5 의 `why` 첫 문장만 C5 로 정정 요구.**
- 🔴 **`S8 premise` 세로 기둥 (v1 지뢰 8)** — **구조로 닫혔다. 스틸로 확인.**
  `premise.js` 가 기둥이 지나는 띠(y 78 ~ `cy=h−104`)에서 글자를 전부 치웠다:
  머리 라벨 「전제 7개」 → **y 26** · 판정 줄 → **`cy+ch+24`** · 「이 파일은 한 건짜리였네」 삭제.
  기울기도 안 키웠다(0.055 유지 — 캔버스 머리 y 13~17 의 진행 막대 회피).
  **증거 `build/stills/160s.png`**: 기둥 7개가 지나는 구간이 **완전히 빈 띠**다. 획 잠식 0. ✅
  🔑 이제 문자열이 어떻게 바뀌어도 다시 안 뚫린다 — 좌표가 아니라 배치로 막았기 때문이다.
- 🔴 **그림 돌려막기** — `lf01/kinds` 24종과 **파일명 교집합 0건**.
  리포 전체에서 같은 이름을 쓰는 5종(`ledger`·`premise`·`blindspot`·`handoff`·`thirdway`)의
  **머리 주석과 장치를 열어 대조**했다: ep00s `ledger`=세로 커밋 사슬 / ep11s `ledger`=빚 원장 카드
  vs lf02=쌓이는 탑 · ep11s `premise`=두 층 조준 vs lf02=전제 7칸+설계층 ·
  ep04s-3 `blindspot`=피로 축 vs lf02=액자 4칸 · ep00s `handoff`=이름표+칸 하나 vs lf02=사람→저장소→세션 ·
  ep03s `thirdway`=한 점에서 뻗는 갈래 3+걸쇠 vs lf02=칸 3+표식 내려앉음+수치 변환.
  **장치가 전부 다르다.** ✅

---

## 4. 기계 판정 — `check.mjs` 직접 재실행 (코디네이터 값과 대조)

```
가이드 점검 · lf02
────────────────────────────────────────────────────
  OK shot 메타 완비             27샷 전부 있음
  OK kind 파일 존재             전부 존재
  OK 자막 2줄 상한               122줄 전부 2줄 이하
  OK 큰 쉼 회차당 2회 이하          0회
  OK 인트로 있음                 wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 훅 시작 문구                wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 아웃트로 소개 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 영어 자막 (없음 · 판정 안 함)    이 회차는 en 이 0줄이다 — 영어 자막 대상이 아닌 회차로 보고 건너뛴다
  OK 제목 이행 (첫 두 줄)          wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 제목 후킹 (정보)             숫자 47 — 기본 축은 감정·사건+질문형(0-A), 뜻 판정은 검수팀
  OK 다음 편 예고 있음             wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 예고 5초 이하               wide — 쇼츠 규격 비대상 (롱폼 규격은 Docs/롱폼_대본_문법.md)
  OK 자막 2초 미만 없음            전부 2초 이상
  OK 말 속도 6.0~7.2자/초        6.76자/초 (참고 영상 6.93)
  OK 총 길이 (wide 참고 · 판정 아님) 589.5초 = 자막 580.1 + 꼬리 9.45 (본편 기준 300~600초 · 승격은 W7 뒤)
  OK 픽셀 읽기 신뢰 가능            검정 위 알파 0.28 → 71 (기대 71±8)
  OK 그림 로드 성공               전 kind 로드됨
  OK 무대 프로필 일치              가로세로비 1.7778 (기대 1.7778 · format=wide)
  OK 콘텐츠 안전영역 (비주얼 ≤ 850)   비주얼 바닥 850 (한계 850)
  OK 자막영역 850~1060 안        자막 머리 874 · 바닥 1037
  OK 결정성 (3패스)              2947프레임 불일치 0
  OK 3색 팔레트                 위반 없음
  ⚠  정적 구간 3초 이하            S17 6.4s
  OK 좌우 가장자리 잘림 없음          없음
  OK 아래 가장자리 잘림 없음          없음
────────────────────────────────────────────────────
통과 — 경고 1건은 확인 권장
```

**🔑 코디네이터 값과 한 글자도 안 갈렸다.** 실패 0 · 경고 1.

### 경고 1건 — `정적 구간 S17 6.4s` : 확인 결과 **창 문제가 아니다**

v1 지뢰 5(*"정적 경고는 창 문제일 수 있다"*)를 따라 갈랐다.

- **창 초과가 아니다**: S17 창 7.00s − 샷 길이 6.712s = **여유 +0.288s > 0** (아래 표).
  즉 클립이 끝까지 재생되고도 남는다 — 마지막 프레임에서 멈춰 선 구간이 없다.
- **원본 클립 자체가 정적이다**: `build/stills/354s.png` 를 떠 보니
  겨울 들판에 천막 넷·무덤 표식·주민 하나(`B · 광부`)뿐이고 카메라도 고정이다.
  이 샷의 `reads` 자체가 「**이미 둘 서 있는** 겨울 마을에 세 번째 무덤이 생긴다」 = **상태 샷**이다.
- v1 도 같은 클립에서 정적 경고가 났고 **마스터가 「상태(무덤) 샷」으로 통과**시켰다
  (`verdict.md` §2-E). v1 9.2s → v2 **6.4s** 로 오히려 줄었다.

👉 **재슬라이스·컷 교체를 요구하지 않는다.** 다만 마스터가 최종 판단할 수 있게 적어 둔다.
🔴 **자막을 지우는 처방은 금지**(v1 이 이미 한 번 깎였다).

### 클립 창 여유 — 다섯 다 재현됨 (코디네이터 값과 일치)

`timed.json` 으로 샷 길이를 다시 잡고(`Σ(dur+pause) + SHOT_TAIL 0.35`) 창에서 뺐다:

| 샷 | 클립 | 창 | 샷 길이 | **여유** | 코디네이터 보고 |
|---|---|---:|---:|---:|---:|
| S1 | `built-…-08` | 7.00 | 6.674 | **+0.326** | +0.326 ✅ |
| S4 | `forecast-…-58` | 13.00 | 12.554 | **+0.446** | +0.446 ✅ |
| S12 | `disaster-…-65` | 7.00 | 6.428 | **+0.572** | +0.572 ✅ |
| S17 | `death-…-162` | 7.00 | 6.712 | **+0.288** | +0.288 ✅ |
| S25 | `broll-…-178` | 7.00 | 6.555 | **+0.445** | +0.445 ✅ |

🟢 v1 의 S1 +0.041s(1.2프레임)가 **+0.326s(약 10프레임)로 풀렸다.** pause −40ms + 쉼표 삭제가
실제로 값을 벌었다. **S25 창도 안 늘렸다**(v1 지뢰 6 미재발) ✅.

### 🔴 `build/timed.json` 신선도 — 합성 더미 아님

`shots` **27개**(대본과 일치) · `summary.charsPerSec 6.76` · 파일 mtime **2026-08-10 18:28**
(= `full.wav` 와 같은 시각). **코디네이터의 실제 TTS 산출물이다.**
🔴 **나는 합성 `timed.json` 을 만들지 않았다.** 만들 필요가 없었다.

---

## 5. 마지막 3.0초 — 아웃트로 카드가 페이오프를 먹지 않았는가

`OUTRO_MS = 3000` (`engine/engine.js:339` `$('outroCard').hidden = !(t >= TOTAL - OUTRO_MS)`).

1. `TOTAL` = **589,505ms** · 카드 시각 = **586,505ms**
2. 마지막 샷 S27 시작 = 564,582ms → **카드 시각 = 샷 기준 21.923s**
3. 페이오프 = `handoff.js:142` 「기술은 만점, 재미는 0점」 = `kJudge = ease(cue(4))`.
   S27 line4: rel `t` 19.370s · `dur` 4.863s → `a = 19.220` · `b = 22.774` · `W = 3.554s`
   → cue(4) at 21.923 = **0.7606** → **`ease` = 0.856**
4. 🔴 **확인 사살 — `build/stills/586.3s.png`(카드 직전 프레임)**:
   「**기술은 만점, 재미는 0점**」이 강조색으로 **또렷하게 판독된다.** 그 위 「채널 정문 영상」
   라벨(kDoor=1.0)도 완성돼 있고, 취소선 문구·판단 기준 블록·세션 칸 다섯도 전부 서 있다.

✅ **`ep08s-3` 사고(카드 시각 알파 0.078) 미재발.** 여유는 넉넉하다 —
알파 0.6 도달이 585.81s 로 카드보다 **0.70초 빠르다.**
🟡 참고: 완성(α=1.0)은 587.36s 로 카드보다 0.85초 늦다. **판독에는 지장이 없으나**,
다음에 마지막 자막이 짧아지면 이 여유가 먼저 없어진다 — 손잡이는 `span` 하나다.

---

## 6. 구조·시점 판정 (문법 §1 · §6)

| 항목 | 판정 | 근거 |
|---|---|---|
| 콜드 오픈 0초부터 사건 · 제목 카드 금지 | ✅ | S1 = 클립(무인 방치 녹화). 카드 0 |
| 콜드 오픈 30~60초 | ✅ | S1+S2+S3 = **65.19초** … 🟡 아래 |
| 챕터 4~6개 · 각 1~2분 | 🟡 | **7개** · 62.6~104.3초 |
| 총 300~600초 | ✅ | **589.5초** (상한까지 10.5초) |
| 열린 고리 | ✅ | S2 사고 → S3 「네 번째에서」 → **S19 회수** · S4 「이 늑대 지금은 없어요」 → **S26 회수**(이 편에서 가장 긴 고리) |
| `chapters` 7개 제목 이행 | ✅ | 챕터별 자막과 대조 완료. 「게이트 **433**개」·「**47일**」 갱신 확인 |
| 과거 완결형 (§6) | ✅ | 「했습니다/됐습니다」 일관. 폐기된 것을 「예정」으로 쓴 곳 0 |
| 미래형은 「지금」 절에만 | ✅ | S26·S27 밖 0건 |
| 유도 문구 궤도 (§6) | ✅ | S27/3 lf01 유도 한 번뿐. `state/uploads.json` 에 lf01 등재 확인 |
| 모든 샷에 `reads`·`source` | ✅ | 27/27. 빈말 0 (다만 S19 `reads` 는 C2) |

🟡 **콜드 오픈이 65.19초로 §1 상한(60초)을 5.19초 넘는다.** 반려하지 않는 이유:
①문법 §1 은 *"⏳ 기계 게이트 승격은 파일럿 실측 후(ADR-LF-9) — 지금 check 는 길이를 참고로만
찍는다"* 라고 스스로 유예해 뒀고 `check.mjs` 도 재지 않는다 ②v1 이 55.4초(추정)로 통과했는데
v2 가 넘긴 원인은 **`ADR-V25-13` 이 S2 를 6줄 → 8줄로 늘린 것**이다 — 즉 새 규격이 만든 길이다.
👉 **줄이라고 요구하지 않는다.** S2 의 늘어난 두 줄(「어림값이 낮아지면…」·「결국 한도를 넘겨…」)이
바로 사용자가 지시한 「가운데를 되살려라」의 이행이기 때문이다. **마스터 판단 항목으로 올린다.**

🟡 **챕터 7개는 §1 의 「4~6개」를 넘는다.** 이건 `remake-input §1`(「챕터 7개 구성 유지」)과
v1 마스터 승인이 정한 것이라 v2 의 새 결함이 아니다. 기록만 남긴다.

---

## 7. 🔴 이 검수가 만든 산출물 — 렌더 전에 지울 것

```
tools/scene-video/episodes/lf02/build/stills/   ← 24장 (내가 이번에 렌더했다)
  90 · 95 · 110 · 118 · 122.5 · 155.5 · 160 · 192 · 232 · 300 · 354 · 362 · 364 · 366 ·
  405 · 428 · 466 · 489 · 518 · 545 · 548 · 557 · 586.3 · 587 (초)
```

🔴 **`build/timed.json` 은 손대지 않았다** — 코디네이터의 실측본 그대로다(27샷 · mtime 18:28).
**합성 더미를 만들지 않았고, 지울 것도 없다.**

---

## 8. 작성팀에게 — 이번 라운드에서 닫을 것

| # | 등급 | 무엇 | 어디 |
|---|---|---|---|
| **R1** | 🔴 **반려** | 「위협 넷을 갈아 끼울 때도 게임 코드는 0줄」 → 정본 귀속 정정 | `scene.json` S23 lines[4] + `source` |
| C1 | 🟡 정정 | 「천사」 → 「타락천사」 (폭은 구속 조건이 아니었다) | `kinds/nowline.js:23` + `writer-v2.md §5-3` |
| C2 | 🟡 정정 | S19 `reads` 에 「계획 칸이 도로 찬다」는 아크를 적을 것 | `scene.json` S19 `reads` |
| C3 | 🟡 정정 | `notes.길이` 를 `check.mjs` 출력 줄로 교체 + 자체 셈 592초 삭제 | `scene.json` `notes.길이` |
| C4 | 🟡 정정 | 「조건 분기 55곳」 → 「갈림길 55곳」 | `kinds/thirdway.js` |
| C5 | 🟡 정정 | S5 latch `why` 첫 문장에서 「빗장」 제거 | `scene.json` S5 `sfx[0].why` |
| C6 | 🟡 정정 | §6 표에 S4 예상 +0.77 vs 실측 +0.446 을 적을 것 | `writer-v2.md §6` |

🔴 **고친 뒤 다시 돌릴 것**: `tts.mjs`(자막을 건드리면) → `check.mjs`.
R1·C2·C3·C5·C6 은 자막 글자 수를 **0자** 바꾸므로 클립 예산·길이에 영향이 없다.
**R1 의 처방 ①만 자막을 건드린다** — 「게임 코드는 0줄이었습니다」(11자) →
「돌아가는 코드는 한 줄도 안 고쳤어요」(18자)로 **+7자**. S23 은 클립 샷이 아니므로 창 제약이 없다.

---

## 9. 검수가 남기는 말

**v1 → v2 의 개선 폭이 크다.** 특히 세 자리가 눈에 띈다.

1. **v1 지뢰 8(`premise` 세로 기둥)을 좌표가 아니라 배치로 닫았다.** 「이번엔 안 겹친다」가
   아니라 「글자와 기둥이 같은 y 를 쓰지 않는다」로 바꿨으므로 **다음에 문자열이 바뀌어도 안 뚫린다.**
   같은 사고방식이 `identity` 의 폭 부등식, `costfloor` 의 라벨 열 분리에도 반복된다.
2. **`reads` 27개가 전부 도형·운동으로만 쓰여 있다.** `ADR-V25-15` 를 「지울 라벨이 없게」
   만든 것이라, 라벨 가림 시험이 **판정할 것도 없이 통과**한다. 이건 다음 회차가 베낄 만한 형태다.
3. **얼린 기준 시각이 실제로 이행됐다.** 여덟 값이 화면·자막·`say`·`chapters`·`blurb`·`hud`·
   `thumbnail` 일곱 자리에서 동시에 47/1,061/839/125/22,478/121/34/433 로 맞았고,
   **화면 머리줄에 기준 시각까지 적혀 있다.** v1 의 「16:20 을 선언하고 16:18 값을 뒀다」가 재발하지 않았다.

**그래서 남은 한 자리가 아깝다.** R1 은 v1 M-R1 과 **같은 병**이다 — 낱말은 원문에 있는데
그 낱말이 붙어 있던 사건이 다르다. v1 은 화면의 행 번호였고, v2 는 자막의 「0줄」이다.
🔑 이 회차가 스스로 세운 규율(*"수치는 반드시 기준을 붙여 말한다 — 게임 파일만 839 · 저장소 전체
1,061"*, `excluded` #11 *"측정 범위가 같다는 보장이 없어 두 수치를 각각 따로 말했다"*)이
**S23 한 줄에서만 지켜지지 않았다.** 같은 규율을 그 줄에 한 번 더 대면 끝난다.

🟢 그리고 이번 검수는 **작성팀 자기보고 중 실제로 갈린 곳이 둘뿐**이었다(영어 라벨 22 vs 24 ·
S4 여유 예상 +0.77 vs 실측 +0.446). 둘 다 결함이 아니라 셈의 차이다. v1 이 「전수라고 적힌
검사가 전수가 아니었던」 회차였던 것과 비교하면 **자기검산의 신뢰도가 눈에 띄게 올랐다.**

---
---

# 2차 검수 (재검) — 2026-08-10 · ✅ **통과 (APPROVED)**

*🔴 1차 판정(위)은 지우지 않는다 — 무엇이 왜 반려됐는지가 기록으로 남아야 한다.
🔴 작성팀 자기보고를 근거로 쓰지 않았다: `git show` 전문 재판독 + 변경줄 **자동 분류** ·
`kinds/*.js` 영어 재추출 · 새 `timed.json`(19:12, `spokenMs 555,116`)으로 타임라인 재계산 ·
**엔진 스틸 7장 새로 렌더** · `voice.json` 규칙 전수 검사.*

## 판정: ✅ **통과** — 반려 0 · 🟡 렌더 전 인계 2건

**R1 이 닫혔고 C1~C6 이 전부 이행됐다.** 부수 수정 다섯도 깨뜨린 것이 없고,
그중 하나는 **R1 이 만든 새 위반(어미 3연속)을 작성팀이 스스로 막은 것**이었다.
🔴 마스터에게 넘긴다. **회차 누적 반려 = v2 1회**(3회 한도 미도달).

---

## 1. 🔴 R1 검산 — **실행 문장 변경이 정말 0 줄이다**

**자막 (실물 확인)**

```
S23/4  얼마 전에 위협 넷을 통째로 갈아 끼울 때도
       게임이 돌아가는 코드는 한 줄도 안 고쳤고요.
```

「0줄」이라는 수를 **다른 사건에서 빌려 오지 않는다.** 대신 **이 사건에서 실제로 0 인 축**을
이름으로 부른다. 그 축이 참인지를 내가 다시 쟀다.

**① `Assets/Scripts/M0/` 변경줄을 전량 자동 분류했다** (사람 눈이 아니라 분류기로):

```
$ git show --format="" 665f79cc -- Assets/Scripts/M0/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)'
  → 총 변경줄 81 | /// 문서주석 37 | // 줄주석 22 | [Tooltip]·문자열 인자 22 | 🔴 그 외 0
```

**「그 외」 = 0.** 실행되는 문장은 **한 줄도 안 바뀌었다.**
🔑 코디네이터가 지목한 `ThreatService.cs` **11+/8−** 도 열어 봤다 — `<summary>` 두 줄이
세 줄로 늘고 「📌 M24-1차 이월: 메서드 이름 `IsPredatorHungry`("포식자")는 …」 두 줄이
새로 붙은 것이라 **비대칭이 난 것뿐이고 전부 `///` 주석**이다.

**② 그 축 밖에 다른 게임 코드가 없는지도 확인했다**
`git show --numstat 665f79cc | grep 'Assets/Scripts'` → **13파일 전부 `Assets/Scripts/M0/` 아래**다.
`M0` 밖 스크립트 경로 **0건**. 나머지는 에셋(`M0Config`)·게이트(`Tests/EditMode`)·명세서·`tools/`.

**③ 「게이트는 게임이 돌아가는 코드가 아니다」는 구분이 타당한가** — ✅ 타당하다.
`Assets/Tests/EditMode/**` 91+/40− 는 **에디터에서만 도는 검사**이고, 이 회차가 S5/4·S18·S19 에서
내내 「게임을 켜기 **전에** 도는 검사」라고 부르며 **게임과 분리해 설명해 온 대상**이다.
자막이 「게임이 **돌아가는** 코드」라고 못 박았으므로 화면·자막·정본이 같은 선을 긋는다.

**④ `source` 에 두 커밋이 실제로 달렸고 인용문이 실재한다**

```
$ git log -1 --format='%s' 665f79cc   → refactor(goap): M24-1차 W1 — 늑대·곰 → 종족 4종 교체
$ git log -1 --format='%s' 83fea176   → feat(goap): M24-1차 W6 — 종족 4종 그림 배선 (색 원 졸업)
   83fea176 본문 : "게임 코드 diff 0줄 — 새로 생긴 것은 도구와 에셋뿐이다."   ← source 인용문과 일치
```

새 `source` 는 ①두 커밋을 **사건 이름과 함께** 달고 ②`git show --numstat` 실측치(13파일 42+/39− ·
2파일 91+/40−)를 적고 ③정본이 이 작업을 「코드 **14파일**」로 센다는 사실(`:235~236`)까지 적었다.
**「어느 축으로 재도 0 이 아니다」를 스스로 밝힌 뒤 참인 축을 고른 형태**다 — 1차 처방 ①의 정확한 이행.

**⑤ 스틸로 화면과 대조** (`build/stills/499s.png`, S23 cue(4) 구간):
화면 오른쪽 계기가 「**새로 쓴 코드 0 줄**」을 가리키고 자막이 「한 줄도 안 고쳤고요」라고 말한다.
**화면의 「0」은 `Docs/CLAUDE.md` 비용표의 것**(S23/2 가 인용)이고 자막은 그 수를 빌리지 않는다 —
둘이 같은 말을 하되 **근거가 갈려 있다.** ✅

**⑥ 🔴 코디네이터 지적 — 남은 「0줄」 하나(S23/2)의 인용 실재 확인**

```
$ grep -n "코드 0줄" Docs/CLAUDE.md
 86: | 계절 추가 | SeasonSO 에셋 1개 + WorldConfig.SeasonCycle 등록. **코드 0줄** (M6-F 가을 리허설 증명) …
 87: | 재해 추가 | DisasterSO 에셋 1개 … **코드 0줄** (M9-F 홍수 리허설 증명) …
 88: | 위협/티어 추가 | ThreatSO 에셋 1개 … **코드 0줄** (M10-G 곰 리허설 증명) …
```

S23/2 「계절 하나 더 넣는 비용은 설정 파일 하나. **코드는 0줄.**」 = **86행 직접 인용** ✅.
🔑 **이것이 R1 과 다른 이유**: 86행은 **일반 규칙(비용표)** 이고 자막도 규칙으로 말한다.
R1 은 **특정 커밋의 실측치**로 말해 놓고 다른 커밋의 수를 가져온 것이었다. **별건이 맞다.**

> ✅ **R1 종결.** 자막은 이제 참이고, 참인 축이 자막 안에 이름으로 들어 있다.

---

## 2. C1~C6 이행 대조 — **내가 1차에 적은 요구와 한 줄씩**

| # | 1차 요구 | 실물 | 판정 |
|---|---|---|---|
| **C1** | `'천사'` → `'타락천사'` + 「폭 때문에」 사유를 실측값으로 교체 | `nowline.js:30` `['고블린','오크','기사단','타락천사']` · 머리 주석이 **내 실측치를 그대로 인용**(334 / 497 / 400px + `ns` 축소 루프) | ✅ **완전 이행** |
| **C2** | S19 `reads` 에 아크를 적을 것 | *"…오른쪽 계획 칸 넷이 **처음에는 S2 끝처럼 전부 X 인 채로 이어받는다.** … 카드가 그 문에 튕겨 되올라가면, **그제야 계획 칸이 왼쪽부터 하나씩 도로 찬다**"* | ✅ **완전 이행 + 실증** |
| **C3** | `notes.길이` 를 실측 인용으로 교체 + 자체 셈 삭제 | 인용으로 교체 ✅ · 자체 셈 592초와 산정식 **삭제 확인** ✅ · 🟡 인용값이 R1 직전 것(589.5) | ⚠️ **인계 (§6 N1)** |
| **C4** | `조건 분기 55곳` → 자막과 통일 | `thirdway.js:21` `DIAG = ['갈림길 55곳', '모든 걸 혼자 아는 파일']` | ✅ **완전 이행** |
| **C5** | S5 `why` 첫 문장에서 「빗장」 제거 | 첫 문장 = 「마지막 층(게이트 433)이 **얹히기 시작하는** 자리」 · 내 cue 산수(0.212 → ease 0.116)와 `kLast>0.5` 를 근거로 적어 뒀다 · **소리는 안 옮겼다** | ✅ **완전 이행** |
| **C6** | §6·`notes.클립` 예상치를 실측으로 교체 | 표가 「**v2 실측 여유**」로 바뀌고 다섯 값이 **내 측정과 소수점까지 일치** · 「자당 환산은 새로 쓴 샷에서 낙관 쪽으로 틀린다(S4 +0.77 → +0.446)」를 교훈으로 박아 뒀다 | ✅ **완전 이행** |

**C1·C2·C4 를 스틸로 실증했다** (이번에 새로 렌더한 7장 중):

- `stills/558s.png` — `nowline` 셋째 칸에 표식 넷 + 「**고블린 · 오크 · 기사단 · 타락천사**」.
  칸 안에 여유 있게 들어가고 잘림 0. **자막과 화면이 같은 이름으로 적을 부른다** ✅
- `stills/392s.png`(S19 rel **3.53s**) — 계획 칸 **넷 전부 X**. 새 `reads` 첫 문장 그대로 ✅
- `stills/405s.png`(S19 rel 16.53s) — 왼쪽 둘 채워짐 / 오른쪽 둘 X = **왼쪽부터 하나씩** ✅
  🔑 `kIn = clamp((kPlan − i×0.14)/0.4)` 이므로 i=0 이 먼저다 — 코드와 프레임과 `reads` 가 일치.
- `stills/233s.png` — `thirdway` 진단 칩이 「**갈림길 55곳**」. 자막(S11/2)과 같은 낱말 ✅

---

## 3. 셈 정정(영어 라벨 22 → 24) 검산 — ✅ **24 가 맞다**

**내 축으로 다시 셌다**(주석 제거 → 따옴표 + **백틱** 리터럴 전량 → import 경로·팔레트/API
문자열(`ink`·`accent`·`sub`·`track`·`canvas`·`alphabetic`) 제외 → 한글 포함 여부로 분기):

```
한국어 포함 리터럴 134 · 화면 영어 24
#${i+1}(amnesia) · Y / N · C(antipattern) · M0-T1 · M10-T2 · BOM · AI · NoSolutionFound ·
A · B · C(handraise) · spec-write · spec-implement · spec-review · devlog · Docs/CLAUDE.md ·
M1 - M20 · W1 · W2 · W3 · SeasonSO · DisasterSO · ThreatSO · CookMeal.asset
```

**24 로 일치한다.** 🔑 멤버십이 한 자리 다른데 **둘 다 옳다** — 작성팀은 `reader.js:34` 의
혼합 문자열 안 `CLAUDE.md` 를 따로 세고, 나는 그 문자열을 한글 포함이라 한국어로 분류하고
대신 `amnesia` 의 백틱 `#${i+1}`(렌더 결과 `#1`)을 세었다. **총계와 결론은 같다.**

**재분류 타당성 판정** — ✅ 타당하다.
`Y / N`·`C` 는 **키보드에 새겨진 키 이름**이고, 같은 kind 안에서 이미 ②로 분류돼 있던
`1-9`·`?` 와 **완전히 같은 부류**다(`antipattern.js` 의 `KEYS` 한 배열 원소들이다).
원문 M15 글 35행도 「숫자 1~9는 배속이고 **Y/N**은 방랑자 … **C**는 비어 있었습니다」라고
키 이름으로 부른다. **①로 옮겨도 통과하지만 ②가 더 정확하다.**
🟢 그리고 작성팀이 「셋 다 결함이 아니라 **셈의 축이 좁았던 것**」이라고 적은 것이 정확한 진단이다.

---

## 4. 🔴 부수 수정 다섯 — 깨진 것이 있는가

| # | 무엇 | 검증 | 판정 |
|---|---|---|---|
| 1 | S23/5 어미 「않았고요」 → 「않았습니다」 (+1자) | 🔑 **요구하지 않았지만 반드시 했어야 하는 수정이다** — 아래 별항 | ✅ **필수 수정** |
| 2 | `excluded` 영어자막 항 「114줄」 → 122줄 | 실제 자막 수 **122** 와 일치 ✅ | ✅ |
| 3 | `notes.사실 경계` ⑫ 를 R1 내용으로 교체 | ⑫ 가 두 커밋·실측치·「어느 축으로 재도 0 이 아니다」를 담고 있고 `source` 와 **서로 어긋나지 않는다** ✅ | ✅ |
| 4 | `notes.정적 대책` 을 실측으로 갱신 | *"✅ 렌더 실측에서 확인됐다 — `⚠ 정적 구간` 1건뿐이고 그것도 클립 샷(S17 6.4s). 재설계한 **S5·S6 는 안 들어왔다**"* — `check.mjs` 출력과 일치 ✅ | ✅ |
| 5 | `thirdway.js` cue 주석 | cue 매핑을 코드와 자막 7줄로 대조: 2=갈림길 55곳 · 3=혼자 아는 파일 · 4=세 가지 내놨다 · 5=사람이 골랐다(**sfx `at=5` 와 일치**) · 6=24,800→5,977. **전부 맞다** ✅ | ✅ |

### 🔑 부수 수정 ①이 없었으면 R1 정정이 새 위반을 만들었다

`voice.json` **규칙 1** = *"같은 어미 2연속까지, **3연속 금지**"*.
R1 정정 전 S23 의 어미는 이렇게 됐을 것이다:

```
S23/3 …그랬고요.         (고요)
S23/4 …안 고쳤고요.       (고요)   ← R1 이 새로 만든 어미
S23/5 …추가하지 않았고요.  (고요)   ← 1차본
                                 → 🔴 「고요」 3연속
```

「않았습니다」로 바꿔 **2연속에서 끊었다.** 뒤따르는 S24/0 「뒤집혔습니다」와는 2연속이라 안전하다.
🟢 **작성팀이 자기 수정의 파급을 스스로 잡은 자리**다 — 「고치는 김에 손대지 마라」의 예외로 옳다.

---

## 5. 🔴 1차에 통과시킨 축이 여전히 서 있는가 (자막이 바뀌었으므로 재검)

- **낱말 전수 배정** — 새로 들어온 표현 둘을 배정표와 대조했다.
  `갈림길`은 **원래부터 `문자열 스위치`의 ①풀이**로 배정표에 있다(화면이 뒤늦게 따라온 것) ✅.
  `게임이 돌아가는 코드`는 **낱말이 아니라 축을 밝히는 수식어**이고 전부 일상어라 배정 대상이 아니다.
  🔴 122줄 재출력 후 재판정 — **배정 없이 남은 어려운 낱말 0건** ✅
- **얼린 수치** — 자막·`say`·`hud`·`chapters`·`youtube.title`·`blurb`·`thumbnail` 전수 프로그램 검사:
  **「46」 잔존 0건** ✅ · `kinds/*.js` 화면 문자열에도 0건 ✅ ·
  S5 다섯 줄의 47 / 839·1,061 / 125·22,478 / 121·34 / 433 **전부 1차와 동일** ✅ ·
  `chapters.실수` 433 · `chapters.증명` 47일 · `hud.title` 47일 **불변** ✅ (새 스틸 7장에서 실렌더 확인)
- **sfx `at`** — 11개 전부 다시 열었다. S23 은 줄 수가 6줄 그대로라 `at=3` 이 여전히
  「재해도, 위협도 같습니다」를 가리킨다 ✅. **인덱스 밀림 0건**, gain·seed·dur 불변 ✅
- **en / `youtube.en`** — 0/122 · 없음 ✅ · **hud 세 줄 글자 불변** ✅
- **클립 여유** — 새 `timed.json` 으로 재계산: **+0.326 / +0.446 / +0.572 / +0.288 / +0.445**
  — 코디네이터 값과 **완전 일치**, 1차와도 완전 일치(클립 샷 무수정 확인) ✅
- **아웃트로 카드** — 새 타임라인으로 다시 풀었다. `TOTAL` 590,696ms → 카드 **587,696ms**,
  S27 시작 565.77s → **카드 = 샷 기준 21.93s**. cue(4) 창 19.22~22.77 → **ease ≈ 0.859**
  (1차 0.856과 사실상 동일). 🔴 **`stills/587.4s.png`(카드 0.3초 전)에서 「기술은 만점,
  재미는 0점」 판독 확인** ✅ — `ep08s-3` 사고 미재발
- **`check.mjs`** — 코디네이터 출력과 대조: 실패 0 · 경고 1(`S17 6.4s`, 1차와 동일 항목·동일 값) ·
  결정성 2,953프레임 불일치 0 · 3색 팔레트 0 · 안전영역·잘림 OK. **새로 깨진 게이트 0건** ✅
- **`timed.json` 신선도** — mtime 19:12 · `spokenMs 555,116` · `charsPerSec 6.76` · 27샷.
  🔴 **합성 더미 아님**, 나는 만들지 않았다 ✅

---

## 6. 🟡 렌더 전 인계 2건 (반려 아님)

### N1. `notes.길이` 가 **한 라운드 낡았다** — 갈아 끼울 것

지금 인용: `589.5초 = 자막 580.1 + 꼬리 9.45` (**R1 직전** 실측)
R1 재실측: **`590.7초 = 자막 581.2 + 꼬리 9.45`**

🟢 **작성팀이 이걸 스스로 예고해 뒀다** — 같은 칸에 *"위 인용은 R1 정정 「직전」의 실측이다 …
`tts.mjs` → `check.mjs` 를 다시 돌려 이 줄을 한 번 더 갈아 끼워야 한다"* 라고 적혀 있고,
**예상치를 적지 않는 규율도 지켰다.** 그러니 결함이 아니라 **순서상 남은 한 손동작**이다.
👉 아래 두 줄을 그대로 갈아 끼워라(자체 계산 금지):

```
총 길이 (wide 참고 · 판정 아님)  590.7초 = 자막 581.2 + 꼬리 9.45 (본편 기준 300~600초)
함께 실측된 것: 말하는 시간 555.1초 · 침묵 26.1초 · 6.76자/초 · 122줄
```

(같은 칸의 「553.9초」도 **555.1** 로. 상한 600 까지 **9.3초** 남았다.)

### N2. 🔴 S11/4~6 이 「~습니다」 **3연속** — 내가 1차에서 못 본 자리다

`voice.json` 규칙 1(3연속 금지) 전수 검사를 이번에 처음 돌렸다. 결과:

```
🔴 S11/4 …각각 무엇을 잃는지까지 붙여서 내놨습니다.
   S11/5 제3의 길을 고른 건 사람이었습니다.
   S11/6 그 결정 뒤에 코드 24,800줄이 5,977줄이 됐습니다.        → 「습니다」 3연속
🔴 S16/3 …사실상 영구 페널티였습니다.
   S16/4 …게이트에 나란히 박아 뒀습니다.
   S16/5 …AI 자신의 실수였습니다.
   S17/0 …결과가 화면에 이렇게 남습니다.                          → 「습니다」 4연속
```

**정직하게 적는다 — 이건 2차에서 생긴 것이 아니다.**

- **S16→S17 4연속은 v1(커밋본)에 글자 그대로 있다.** `git show HEAD:…/scene.json` 으로 대조했다.
  검수 3라운드 + **마스터 APPROVED** 를 그대로 통과한 상태다.
- **S11 3연속은 v2 1차에서 생겼다** — v1 은 `/3 …트레이드오프까지 붙여서 내놨**고요**` 였는데
  `ADR-V25-13`(갓 클래스 풀어쓰기)으로 그 줄을 둘로 쪼개면서 「내놨습니다」가 됐다.
- 🔴 **그리고 나는 1차 검수에서 이 검사를 안 돌렸다.** 지시서 6번(말투)이 명시한 항목인데
  놓쳤다. **작성팀 잘못이 아니다.**

**그래서 반려하지 않는다.** ①작성팀이 요구한 일곱 자리를 전부 정확히 닫았고
②이 자리는 내가 1차에 지적했어야 할 것이며 ③v1 이 더 무거운 4연속을 안고 승인된 선례가 있어
**이 트랙의 실효 기준이 문서와 갈려 있다** — 그 판단은 마스터 몫이다.

👉 **고칠 거면 한 곳이면 된다**(내용 불변, v1 어미로 되돌리는 것):
`S11/4` 「…붙여서 내놨**습니다**」 → 「…붙여서 내놨**고요**」
그러면 S11 은 2연속에서 끊긴다. S11 은 캔버스 샷이라 창 제약도 없다.
🔴 **S16→S17 은 손대지 마라** — v1 승인 상태이고, 여기를 건드리면 마스터가 승인한 감쇠 산수
세 줄(remake-input §2 「이미 잘 된 자리」·지우지 말라고 못 박은 자리)에 손이 간다.

---

## 7. 🟡 작성팀이 일부러 안 고친 것 — **그 판단이 맞다 (반려 사유 아님)**

`writer-v2.md §8-8`: S26/1 「일곱 번을 다시 그렸는데」를 「일곱 라운드를 돌렸는데」로
안 고치고 미결로 남겼다.

**검수 판정: ✅ 옳은 판단이다.**

1. 내 1차 지적은 **「(선택 · 강제 아님)」이라고 명시**했다. 반려 사유로 낸 적이 없다.
2. 수 **7 은 1차 소스에 실재한다** — `Docs/종족침공축_브레인스토밍_기록_2026-08-09.md:12`
   *"2026-08-09 재작업 1회(**총 7라운드**) … **여전히 반려.**"* 이번에 다시 열어 확인했다.
   그러므로 **「원문에 없는 수」가 아니다** — 최고 수위 위반에 해당하지 않는다.
3. 「고치는 김에 다른 것을 손대지 마라」는 이 파이프라인의 규율이고, **범위를 지킨 것이 옳다.**
   (v1 3차 라운드가 「지시받은 한 파일만 고쳤다」로 통과한 선례와 같은 형태다.)

🔑 **마스터가 판단할 것 하나**: 「7라운드」가 자작 1회를 포함하는지 원문이 확정하지 않으므로,
「일곱 번을 다시 그렸는데」는 **원문보다 한 번 더 강할 수 있다.** 바꾸고 싶다면
「일곱 라운드를 돌렸는데」가 원문에 가장 가깝고 **자수가 같다**. 안 바꿔도 반려가 아니다.

---

## 8. 2차에서 내가 훑은 경로

```
git show --format="" 665f79cc -- Assets/Scripts/M0/   → 변경줄 81 전량을 분류기로 자동 분류
git show --numstat 665f79cc                            → M0 밖 스크립트 0건 확인
git log -1 --format='%s' 665f79cc / 83fea176           → source 인용문 실재 대조
grep -n "코드 0줄" Docs/CLAUDE.md                       → S23/2 인용(86행) 실재 확인
git show HEAD:…/scene.json                             → S11·S16 어미를 v1 과 대조
kinds/*.js 21개 재추출(따옴표+백틱, 팔레트/API 제외)     → 화면 영어 24 재확인
scene.json 자막 122줄 재출력 + 프로그램 46 전수 검사     → 46 잔존 0
build/timed.json(19:12) 재계산                          → TOTAL 590.7s · 클립 여유 5개 · 카드 21.93s
render.mjs --still 587.4,392,405,499,558,233,96        → 스틸 7장 새로 렌더
voice.json 규칙 1 전수 검사(어미 3연속)                  → N2 발견
```

🔴 **이번에 새로 만든 스틸 7장**(`587.4 · 392 · 405 · 499 · 558 · 233 · 96`)도
1차의 24장과 함께 `build/stills/` 에 있다 — **렌더 전 삭제 대상**이다(총 31장).
🔴 **`build/timed.json` 은 코디네이터 실측본이다. 손대지 않았고 합성 더미도 만들지 않았다.**

---

## 9. 2차 요약

| 항목 | 1차 | 2차 |
|---|---|---|
| 반려 | 🔴 1건 (R1) | **0건** |
| 정정 | 🟡 6건 (C1~C6) | 🟡 인계 2건 (N1 길이 갈아 끼우기 · N2 어미 3연속 — **마스터 판단**) |
| `check.mjs` | 통과 · 경고 1 | 통과 · 경고 1 (동일 항목) |
| 실측 길이 | 589.5초 | **590.7초** (상한까지 9.3초) |

**작성팀이 반박 없이 일곱 자리를 전부 닫았고, 닫은 방식이 옳았다.**
특히 R1 을 「수를 바꿔 맞추는」 방식이 아니라 **「참인 축을 이름으로 부르는」 방식으로 푼 것**,
그리고 그 정정이 만들 뻔한 어미 3연속을 **자기가 먼저 잡은 것**이 이번 라운드의 값이다.

🔑 **내 1차 검수의 구멍도 함께 적어 둔다** — `voice.json` 어미 검사를 1차에 안 돌렸고,
그래서 v2 가 1차에서 만든 S11 3연속을 2차에 와서야 봤다. 다음 회차는 **자막이 바뀐 라운드마다
어미 전수 검사를 돌린다**(비용은 스크립트 한 줄이다).

**✅ 마스터에게 넘긴다.**
