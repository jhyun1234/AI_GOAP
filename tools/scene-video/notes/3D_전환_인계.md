# 3D 전환 인계 — ep16s-1 (2026-08-12)

한 세션에서 ep16s-1 을 여덟 번 다시 만들며 얻은 것. **다음 회차는 여기서 시작한다.**
브랜치 `exp/palette4` · 커밋 `9c77332c` → `f1deab1e`.

---

## 1. 무엇이 문제였나 (사용자 판정 순서대로)

| # | 판정 | 실측된 원인 |
|---|---|---|
| 1 | 「단조롭다」 | 샷당 6.0초 · 진입 모션 1종 · 여섯 샷의 피사체 크기가 전부 같음 |
| 2 | 「달라진 게 하나도 없다. 뭘 의미하는지 대사를 봐야 안다」 | 자막을 가리면 **동그라미와 점선 네모뿐**이라 해독 불가 |
| 3 | 「렉 걸린 것처럼 버벅거린다」 | 촬영 박자(on 2s) 도입 — **연속 파라미터를 양자화하면 프레임 드랍이다** |
| 4 | 「2D 에서 움직이고 있다. 3D 로 보이게」 | 확대(`camAt`)는 그림 전체를 똑같이 키워 「커졌다」로만 읽힘 |
| 5 | 「터지는 효과는 눈이 아프다」 | 반전 플래시·집중선 — 임팩트는 장식이지 역동성이 아님 |
| 6 | 「동그라미에 몸을 축 내린 형태」 | 사람이 머리 + 캡슐 하나 — **관절이 없어서** 어떤 자세도 늘어짐 |

🔑 **여섯 번 중 다섯 번, 내가 고친 층이 틀렸다.** 카메라·컷·이펙트·박자를 만지는 동안
진짜 원인은 **그리는 대상 자체**였다. 새 회차에서 「밋밋하다」가 나오면
**먼저 자막을 가린 프레임을 뽑아라**(아래 §5).

---

## 2. 지금 구조

```
engine/lib.js                    붓 — 전 회차 공용
  project(wx,wy,wz,cam,w,h)      핀홀 투영. x+ 오른쪽 · y+ 아래(지면 y=0) · z+ 안쪽
  cam3(track, t)                 3D 카메라 키프레임 {at, x, y, z, yaw, f}
  gray/litFill/ballFill/ball/capsule   조명 — 빛은 언제나 화면 왼쪽 위
  castShadow / DEPTH             바닥 그림자 · 깊이 명도 계단
  camAt(...)                     2D 확대(구형). 3D 샷에서는 안 쓴다
  applyEnter 는 engine.js        up / side / wipe / wipe-x / iris / cut

episodes/<ep>/kinds/_world.js    회차 공용 월드 — kind 가 아니다(형제 모듈)
  GRAVES · stone() · person() · box() · ground() · carve()

tools/scene-video/refs.mjs       레퍼런스 파이프라인 (§4)
```

**화면 좌표를 직접 쓰는 kind 가 하나도 없다.** 전부 월드 좌표를 원근으로 던진다.

---

## 3. 확정된 규칙

**그림**
- 형상은 그 물건처럼. 동그라미≠무덤. **비석·사람·모니터**로 그린다.
- 주인공은 화면의 36~71%. 옛 동그라미는 16% 였다(가이드 최소 40% 위반).
- 채운다. 검정 배경의 3px 윤곽선은 무게가 없다.
- 파낸 홈은 **`destination-out`**. `PALETTE.bg`(#0E1117)로 칠하면 경계에서 팔레트 위반 색이 난다.

**사람** — 8두신 정준(Loomis). 바닥 0 · 정수리 1 기준
- 턱 .875 · 어깨 .833 · 가슴 .750 · 허리 .625 · 사타구니 **.500** · 무릎 .250 · 발목 .055
- 너비(머리 높이 배수): 어깨 2.33 · 가슴 1.75 · 허리 1.20 · 골반 1.45 · 머리 0.72
- **관절이 핵심.** 팔 어깨–팔꿈치–손목, 다리 골반–무릎–발목 **두 마디**.
  한 마디 막대는 각도를 줘도 「기울어진 막대」다.
- 얼굴·손가락 금지(`MINIMAL_DESIGN_LANGUAGE:838`). 못 그리는 것은 **구도로 가린다**
  (손목을 키보드 뒤로).

**움직임**
- 깊이는 배율이 아니라 **시차**. 카메라를 z 로 움직여라.
- 사건으로 그린다(상태 나열 금지). anticipation → 스냅 → 오버슛.
- 무한 루프 금지. 정적 3초 게이트는 **사건**이 넘긴다. 카메라가 움직이면 원리적으로 안 걸린다.

**하지 말 것 (전부 실물로 확인하고 되돌림)**
- ❌ 촬영 박자 양자화(`shot.step`) — 렉으로 읽힌다. 손그림 on 2s 는 **매 스텝이 다른 그림**이라 성립한다.
- ❌ 반전 플래시 / 집중선 — 눈이 아프다.
- ❌ 2D 확대로 「가까워짐」 표현 — 「커졌다」로만 읽힌다.
- ❌ 지평선 쪽을 밝게 깐 지면 — 바닥이 아니라 **회색 벽**이 된다.

---

## 4. refs.mjs — 눈대중을 안 남긴다

```bash
node tools/scene-video/refs.mjs find "<검색어>" --n 3 --ep <회차>
node tools/scene-video/refs.mjs add <url> --ep <회차>
node tools/scene-video/refs.mjs measure <회차>
```
검색 → 내려받기 → 프레임 실측 → 컨택트 시트 → `episodes/<ep>/notes/refs.md`.
남의 영상은 리포에 안 담는다(`.gitignore refs/*/ref.mp4`).

**새 회차를 시작할 때 이걸 먼저 돌려라.** 레퍼런스를 손으로 뜯어 잰 30분이
그 세션의 어떤 수정보다 많은 것을 바꿨다.

---

## 5. 새 회차 점검 순서

1. `refs.mjs find` 로 레퍼런스를 받고 우리 것과 같은 잣대로 잰다.
2. 렌더 후 **자막·HUD 를 가린 프레임 그리드**를 뽑는다:
   ```bash
   ffmpeg -i build/video.mp4 -vf "drawbox=0:0:1080:420:black:t=fill,\
   drawbox=0:1266:1080:654:black:t=fill,crop=1080:846:0:420,fps=1/1.55,scale=190:-1,tile=8x3" \
   -frames:v 1 nocaption.png
   ```
   **읽히지 않으면 그림이 틀린 것이다.** 카메라·컷·이펙트를 만지지 마라.
   (모든 `scene.json` 의 `reads` 필드가 「라벨을 다 가려도 읽힌다」고 주장해 왔는데
    아무도 검증한 적이 없었다.)
3. `check.mjs` 33종. 확대·반전으로 잉크가 화면 밖에 닿는 샷은 `checks.edge` 로 사유 선언.

---

## 6. ep16s-1 현재 상태 · 남은 것

- 본문 네 샷(훅·묘지·책상·새김) **전부 하나의 3D 공간**. 게이트 33종 통과 ·
  결정성 3패스 불일치 0 · TTS 재합성 0회(문장을 한 자도 안 건드림).
- 비석에 새기는 이름 `spec.carvedName = '훈발롬'` — ⚠️ **원문에 없는 값**(사용자 지정 예외).

**남은 것**
1. **아웃트로 `nextpeek` 이 아직 2D** — 캡슐·점선 네모. 같은 월드로 옮기면 「위험은 화면에
   뜨는데 누구인지 못 고른다」를 3D 주민 위로 스캔선이 지나가는 것으로 그릴 수 있다.
2. **인트로는 시리즈 공용**(`engine/intro-brand.js`) — 손대면 45편 전체가 바뀐다. 별도 판단.
3. **홀드가 길다** — 우리 최장 4.57초 vs 레퍼런스 1.18초. 남은 가장 큰 격차이고,
   박자가 아니라 **사건 밀도**를 올려야 한다.
4. `person()` 의 `walk`(걸음 위상)·`reach` 파라미터가 **열려 있는데 아무 샷도 안 쓴다.**
   자동으로 받아 둔 마네킹 워크사이클(`refs/N0TNQygEYaQ/sheet.png`)이 참고 자료다.
5. **나머지 45편은 옛 어휘 그대로다.** 이 문서가 그 이관의 출발점이다.
