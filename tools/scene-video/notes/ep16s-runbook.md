# ep16s 를 3D 로 처음부터 — runbook

> 🔴 **다음 세션은 이 문서부터.** 그다음 [`Docs/3D_대본_문법.md`](../../../Docs/3D_대본_문법.md).
> 앞 세션 기록: [`2026-08-13-세션-인계-2.md`](./2026-08-13-세션-인계-2.md)

## 0. 지금 상태

| | |
|---|---|
| ep15s-1 | ✅ **업로드 완료 — 손대지 마라** |
| ep15s-2·3·4·5 | ✅ 3D 완성 (58.3 / 48.5 / 43.5 / 36.3초) · 게이트 33종 전부 통과(경고 0) |
| **ep16s-1** | ✅ **새 규격으로 재작업 끝**(2026-08-14 저녁 · 커밋 `0ac44fa4`·`c3aac7d9`) — mp4 58.0초 · 게이트 전부 통과. 🔴 **사용자 판정 대기** |
| **ep16s-2~5** | ⚠️ **첫 판 그대로다 — 반려 상태.** 대본·게이트는 통과했지만 그림이 옛 규격(인물 4%)이라 **다시 잡아야 한다.** 커밋 `2485f4a9`·`84ed33e5`·`3f4965ac`·`f70381f5` |

## 0-A. 🔴 2026-08-14 반려와 그 처방 — **다음 세션은 여기부터**

사용자 판정: *「ep16s 전체는 3D 애니메이션으로 처음 보는 사람이 내용을 전혀 이해하지
못했어.」* 지목된 증상 넷: ①그림이 작다 ②계기(격자·실·막대) 뜻을 모르겠다
③매 컷이 비슷해 진행이 안 보인다 ④자막 내용 자체가 어렵다.

🔴 **발원지 = 나는 3D 층 접점 시트만 보고 합성본(mp4)을 한 번도 안 봤다.**
실측: 그림이 화면 세로의 44% 안에 있고 그 안에서 인물이 10% → **화면의 4%.**

처방 셋(사용자 지시 「1·2·3 차례대로」):
1. **그림판** — `.vis` 970×846 → **1080×1020**(좌우 여백 0 · 하단 1440). 자막이 그림
   위에 얹힌다. ⚠️ 「하단 안전영역」 게이트는 상자 바닥에 **+33px** 을 얹어 잰다 —
   1470 은 1503 으로 잡혀 막히고 1440 이 1473(한계 1477)이다.
   🔴 **공용 인트로도 다시 구웠다**(`render_intro.py`) — 옛 970×846 이 새 상자에서 늘어난다.
2. **카메라** — `stage.cam_for(at, frac, lens, yaw, elev)` 로 **크기에서 역산**한다.
   `frac` = 인물 키 ÷ 화면 세로. 하한 `stage.FIG_MIN 0.28`, 굽는 로그에 실측이 찍힌다
   (`common.report`). 방위는 `face_yaw()` 로 **그 사람 앞쪽**에서 딴다.
   🔑 그래서 **일곱을 한 프레임에 안 넣는다** — 무리로 모아 두고 카메라가 옮겨 가며 센다.
3. **그림 문법** — 뜻이 안 읽히는 **계기를 걷어내고 사람의 행동으로 옮긴다.**
   ep16s-1 이 한 것: 사망 막대 → 무덤이 하나 더 솟음 · 머리 위 표식 → 산 사람이 든 망치
   (같은 도구인데 하나만 커진다) · 아웃트로 격자 → 주저앉은 사람 옆을 그냥 지나가는 사람.
   자막도 같이 쉽게 고친다(「죽는 그 순간에 넘기게」→「쓰러지는 그 순간에 이름을 넘기게」).

🔴 **16-2·16-3 은 격자가 그림의 중심이라 대체 그림을 새로 설계해야 한다.**
   후보(미확정): 명부 = 사람들이 줄지어 서는 것 · 덮어쓰기 = 한 사람만 남고 나머지가
   사라지는 것 · 관계 = 마주 서서 이야기하다 한쪽이 쓰러지면 남은 사람이 등을 돌리는 것.

카메라 각도로 **세 번 되돌린 자리**(ep16s-1 본문2): +24° 옆 무덤이 가림 → −38° **누운 몸
정면 = 흰 공 하나**(런북 §7 재발) → −82° 서쪽도 무덤이 가림 → **+82°(동남쪽)** 확정.

⚠️ **`state/uploads.json` 에 `ep16s-1~5` 항목이 있다** — 2D 시절 것을 `--force` 로 덮었다.
지우지 말고 `--force` 로 덮어라(그 파일이 「이미 처리했다」의 유일한 원장이다).
✅ **`state/schedule.json` 의 `order` 는 그대로 둔다** — 그게 순서표다.

---

## 1. 무엇을 만드나

**원문**: `2026-08-04-unity-goap-m13-events-and-traces.html` — **M13 사건과 흔적** (2026-08-04 KST)
`https://gamedevclaude.blogspot.com/2026/08/claude-code-ai-unity-goap.html`

**5편 분할** — 🔑 **사건 분할은 지운 2D 판의 기획을 그대로 써도 된다.**
분할은 원문 분석의 결과이지 그림이 아니다. **새로 쓰는 것은 대본과 그림뿐이다.**

| 편 | 맡은 사건 | 예고(다음 편) |
|---|---|---|
| 16-1 | Day 45 에 일곱이 죽었는데 화면에 남은 것은 **아무것도 안 적힌 무덤 일곱**뿐. 가진 건 「사망 +1」. → 죽는 순간(아직 살아 있는 함수 안)에 이름을 넘겨 비석에 새겼다 | 16-2 |
| 16-2 | 「굶는다」고 떴는데 **누구인지가 없었다** — 알림 칸이 최신 한 건만 유지해서 일곱 건이 서로를 지웠다 | 16-3 |
| 16-3 | 명부를 만들었더니 **한 명이 다 덮었다** — 게으름뱅이 하나의 「명령 거부(피로)」 아홉 연발이 기록을 채웠다 | 16-4 |
| 16-4 | **죽으면 관계까지 지워졌다** — 뒷정리가 둘이 친했던 기록을 양방향으로 날렸다 | 16-5 |
| 16-5 | 집도 밭도 있는데 왜 굶어 죽었나 — 같은 판을 다시 켜 두니 **회상이 달라졌다** | 17편 |

🔗 **ep15s-5 가 이미 예고를 내보냈다** — 「16편 · 무덤에 이름이 없던 마을 이야기」.
그리고 그 편 아웃트로가 **빈 비석 둘을 세워 무대를 미리 깔아 뒀다**(`village.grave`).
16-1 훅은 그 그림을 이어받는다. ⚠️ 미리보기는 무덤이 **둘**이었지만 원문 수치는 **일곱**이다.

---

## 2. 🔴 먼저 풀 것 — 「이름」을 3D 로 어떻게 그리나

**이게 이 회차의 급소다.** 이 세계에는 **글자가 없다**(블렌더 쪽에 폰트가 없고, 화면에
글자를 올리는 것은 자막·HUD·아웃트로 카드뿐이다). 그런데 16s 다섯 중 넷이
**이름 · 명부 · 알림 · 기록** — 전부 글자다. 2D 판은 캔버스에 글자를 찍어서 풀었다.
**그 길이 막혔다.**

🔴 **판단을 미루고 대본부터 쓰면 안 된다.** 이름을 못 그리는 채로 쓰면 다섯 편이
전부 「자막은 말하는데 화면은 아무 말도 안 하는」 편이 된다.

### 있는 재료로 풀 수 있는 길 (제안 — 다음 세션이 판단한다)

| 대본이 말하는 것 | 3D 가 지는 방법 |
|---|---|
| **이름** | **그 사람 자체.** 사람은 하는 일 · 든 도구(도끼·곡괭이·망치·물뿌리개) · 선 자리로 이미 구별된다 |
| **「이름이 없다」** | 비석이 **비어 있고**, 무덤 곁에 **아무도 안 선다.** 누구였는지 화면에 단서가 없다 |
| **「이름을 새겼다」** | 무덤 곁에 **그 사람의 표식**이 선다 — 성격 보라(`violet`)가 이미 「성격이 닿은 자리」 색이고, 도구를 비석 옆에 세우면 「이 사람이었다」가 된다 |
| **알림 칸 · 명부** | `instrument.build(cols, rows)` 격자가 **이미 있다.** 「최신 한 건만 남아 일곱이 서로를 지운다」 = 칸이 켜졌다 곧바로 꺼지는 것 |
| **「한 명이 다 덮었다」** | 격자 칸이 **한 색으로 다 칠해지는 것** |
| **「사망 +1」** | 막대가 한 칸 오르는 것. 🔴 수(일곱 · Day 45 · +1)는 **자막이 진다** |
| **「관계가 지워졌다」** | `instrument.thread`(두 점을 잇는 실)가 **끊기는 것.** 양방향이면 실이 둘 |

🆕 **새 소품이 필요하면**: 무덤은 이미 있다(`village.grave` — 흙더미 + 빈 비석, 조각 목록 반환).
그 밖의 것은 `render_prop_look.py` 로 **주민 옆에 세워** 판정한다(축척이 늘 문제였다).

---

## 3. 절차 (회차 하나당)

```bash
# 0) 읽는다: 이 문서 → Docs/3D_대본_문법.md → blender3d/README.md · PROPS.md
# 1) 기획 — 사건을 **동사로** 넘긴다 (scene-planner)
# 2) 작성 — scene.json + 자막 줄 ↔ 「몸짓·크기」 표 (scene-writer)
# 3) kinds/*.js 여섯을 scene3d 재수출 한 줄로
cp episodes/ep15s-5/kinds/intro.js episodes/ep16s-1/kinds/<각 kind>.js
# 4) 목소리 → timed.json (몸짓 시각의 정본)
node tts.mjs ep16s-1
# 5) 3D 샷 — blender3d/ep16s-1/{common.py, render_hook.py, render_body*.py, render_outro.py}
# 6) 굽는다 (회차당 벽시계 4~5분)
python blender3d/bake.py --ep ep16s-1
# 7) 🔴 프레임을 **눈으로 본다** (PIL 로 접점 시트 3~5장) — 결함 절반이 여기서 나온다
# 8) 게이트 → 렌더
node check.mjs ep16s-1 && node render.mjs ep16s-1
```

🔴 **공용 3D 인트로는 이미 구워져 있다**(`D:\AI_GOAP-videos\3d\_intro\frames`, 5.143초).
회차는 `spec.frames` 를 `/3d/_intro/frames` 로 겨누기만 한다 — 다시 굽지 마라.
`villager_rigged.blend` · `village.blend` 도 이미 있다.

---

## 4. 회차 무관 파일 — 고치면 **모든 편**이 영향받는다

`blender3d/` 의 `motions.py`(동작 22종) · `village.py`(소품) · `instrument.py`(계기) ·
`stage.py`(카메라·포즈·굽기) · `palette.py`(색표) · `bake.py`.
고치면 **`python test_*.py` 여덟 파일 88종**을 돌리고, 앞 회차도 다시 구워야 하는지 판단한다.

---

## 5. 지금 있는 어휘 (2026-08-13)

- **동작 22종** — 일 `farm` `chop` `mine` `hammer` `water` `draw` `gather` /
  삶 `eat` `warm` `rest` `stop` `look_around` `huddle` / 이동 `walk` `run` /
  주고받음 `talk` `nod` `reach` `look_up` / 끝 `collapse` `freeze` `attack`
- **소품** — 집 · 나무 · 우물 · 밭 · 덤불 · 모닥불 · **무덤** · 눈 · 마을 바깥 · 도구 넷
- **계기** — 막대(`gauge`) · 문턱 선(`gauge_mark`) · 땅에 그린 원(`ring`) ·
  잇는 실(`thread`) · 공중 격자(`build(cols, rows)`)
- **색** — 세계층(무채색+earth) / 뜻층 다섯 **한 프레임 둘까지** / 계기층(예산 밖)

---

## 6. 게이트 값 (2026-08-13 개정)

| | |
|---|---|
| 총 길이 | **63초 이하**(fail) · 권장 31~63 |
| 본문 | 18~50초 |
| 한 비트(홀드) | 1.5초 이하 |
| 정적 구간 | 3초 이하 |
| 뜻층 | 한 프레임 2색 |

🔴 **짧게 쓰려고 설명을 깎지 마라.** 목표는 **처음 보는 사람이 영상만으로 이해하는 것**이고,
자막은 그림의 중복이 아니라 **입장권**이다(2026-08-13 판정).

---

## 7. 먼저 막을 구멍 — 이번 5부작에서 **실제로 밟은** 것들

| 함정 | 어떻게 드러났나 |
|---|---|
| 소품 함수가 **조각 하나만 반환** | `hide_render` 로 끄면 나머지가 남는다. 집 지붕·모닥불 돌 테두리로 **두 번** 밟았다. 새 소품은 조각 목록을 반환하거나 `*_show()` 를 같이 만든다 |
| **빈 막대에 강조** | 홈까지 커져서 **값이 오르내리는 것처럼** 보인다. 값이 안 변하는 막대는 강조를 안 건다 |
| **문턱 선이 막대와 같은 색** | 채움이 그 선을 넘는 순간 먹혀 사라진다 — 봐야 할 그 순간에 안 보인다 |
| **빈 땅으로 「없다」를 그림** | 아무 말도 안 읽힌다. `ring` 으로 자리를 그리거나 사람이 기다리게 한다 |
| **누운 몸을 정면에서** | 머리가 커서 접힌 몸이 가려 **흰 공 하나**가 된다. 90° 돌려 낮게 잡는다 |
| **걷는 시간을 고정값으로** | 자리 간격이 제각각이라 도착 전에 망치질이 시작된다. **거리에서 역산**한다 |
| **`cam_right()` 를 화면 오른쪽으로 착각** | 막대 순서가 좌우로 뒤집힌다 — 자막이 먼저 부르는 값이 맨 오른쪽에 선다 |
| **`arm.rotation_euler` 를 읽어 보간** | 앞 프레임 값이 누적돼 **결정성 게이트가 죽는다.** 원래 각에서 다시 계산한다 |
| **뛰어 나갈 각을 눈대중으로** | 마을이 빽빽해서 통로가 사실상 하나다. 소품 반지름을 넣고 방위 360개를 훑어서 고른다 |
| **넓히면 빈 땅이 커진다** | 바닥(200)·마을 바깥(`outskirts`)·카메라 높이를 **같이** 넓혀야 한다 |

---

## 8. 판정 기준 — 굽고 나서 이걸로 본다

1. **자막을 가리고 봐도 무슨 일이 일어났는지** 보이는가
2. **자막을 가리고 봐도 그 사람이 무엇을 느꼈는지** 보이는가
3. **처음 보는 사람이 자막까지 보고 이해**가 되는가 ← 최종 목표
4. 줄마다 정한 몸짓이 **그 말을 할 때** 나오는가 (독스트링 표와 화면 대조)
